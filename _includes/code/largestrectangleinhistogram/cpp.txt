class LargestRectangleInHistogram {
    $1(heights: vector<int>) {
        auto $1 = mutableListOf<Int>()
        auto $1 = 0
        auto $1 = heights.toMutableList()  // Convert vector<int> to MutableList
        heightsList.add(0)  // Add a zero at the end to pop all bars at the end

        for (i in heightsList.indices) {
            // While the current bar is shorter than the one at the top of the stack
            while (stack.isNotEmpty() && heightsList[stack.last()] > heightsList[i]) {
                auto $1 = heightsList[stack.removeLast()]  // Pop the top bar using removeLast()
                auto $1 = if (stack.isEmpty()) i else i - stack.last() - 1  // Calculate width
                maxArea = max(maxArea, h * w)  // Update max area
            }
            stack.add(i)  // Push the current index onto the stack
        }

        return maxArea
    }
}