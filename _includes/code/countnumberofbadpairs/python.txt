class CountNumberOfBadPairs {
    def $1(nums: list[int]): Long {
        $1 = mutableMapOf<Int, Long>()
        $1 = 0L

        for (i in nums.indices) {
            $1 = nums[i] - i
            goodPairs += freq.getOrDefault(key, 0)
            freq[key] = freq.getOrDefault(key, 0) + 1
        }

        $1 = nums.size.toLong()
        $1 = (n * (n - 1)) / 2
        return totalPairs - goodPairs
    }
}