class ExcelSheetToColumnNumber {
    $1(columnTitle) {
        auto $1 = 1
        auto $1 = 0
        for (i in columnTitle.length - 1 downTo 0) {
            sum += (columnTitle[i] - 'A' + 1) * base
            base *= 26
        }
        return sum
    }
}