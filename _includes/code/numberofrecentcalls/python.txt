class NumberOfRecentCalls {
    $1 = LinkedList<Int>()
    $1 = 3000
    def $1(t: int): int {
        while (deque.isNotEmpty() && ( t - deque.first()) > TIME_WINDOW_MS)
            deque.removeFirst()

        deque.addLast(t)

        return deque.size
    }
}