class PlusOne {
    $1(digits: vector<int>): vector<int> {
        for (i in digits.size() - 1 downTo 0) {
            if (digits[i] < 9) {
                digits[i]++
                return digits
            }
            digits[i] = 0
        }

        return intArrayOf(1) + digits
    }
}