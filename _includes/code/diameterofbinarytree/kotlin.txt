package graph

import javax.swing.Spring.height

class DiameterOfBinaryTree {

/**
 * diameter Of Binary Tree — executes the core logic of this algorithm on the provided input.
 *
 * @param root the root/head node of the data structure
 * @return the computed integer result
 */
    fun diameterOfBinaryTree(root: TreeNode?): Int {
        var max = 0

/**
 * height — executes the core logic of this algorithm on the provided input.
 *
 * @param root the root/head node of the data structure
 * @return the computed integer result
 */
        fun height(root: TreeNode?): Int {
            if (null == root) return -1
            val left = height(root.left)
            val right = height(root.right)
            max = Math.max(max, 2 + left + right)
            return 1 + Math.max(left, right)
        }

        height(root)
        return max
    }
}