class RotateArray {
    $1(nums: vector<int>, k) {
        auto $1 = nums.size
        auto $1 = k % n // In case k is greater than n

        reverse(nums, 0, n - 1)
        reverse(nums, 0, steps - 1)
        reverse(nums, steps, n - 1)
    }

    private $1(nums: vector<int>, start, end) {
        auto $1 = start
        auto $1 = end
        while (s < e) {
            nums[s] = nums[e].also { nums[e] = nums[s] }
            s++
            e--
        }
    }
}