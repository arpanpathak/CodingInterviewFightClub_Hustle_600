package real_word_projects

interface  LifeForm {
    var age: Int
}

interface Animal: LifeForm {
    val name: String
    /**
    * Solves the Human problem.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    /**
    * Solves the Human problem.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    /**
    * Solves the Human problem.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    /**
    * Solves the Human problem.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    fun tattiKaro()

    /**
    * Solves the Human problem.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    /**
    * Solves the Human problem.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    /**
    * Solves the Human problem.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    /**
    * Solves the Human problem.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    fun benstokes() {
        println("GOAT")
    }
}

class Human(override var age: Int = 0, override val name: String) : Animal {
    /**
    * Implements/overrides the base class method.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    /**
    * Implements/overrides the base class method.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    /**
    * Implements/overrides the base class method.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    /**
    * Implements/overrides the base class method.
    *
    * @return Unit (no return value, modifies state in-place).
    */
    override fun tattiKaro() {
        println("Tatti karke bada sukun")
    }
}

/**
* Entry point for the program.
*
* @param args The input Array<String>.
* @return Unit (no return value, modifies state in-place).
*/
/**
* Entry point for the program.
*
* @param args The input Array<String>.
* @return Unit (no return value, modifies state in-place).
*/
/**
* Entry point for the program.
*
* @param args The input Array<String>.
* @return Unit (no return value, modifies state in-place).
*/
/**
* Entry point for the program.
*
* @param args The input Array<String>.
* @return Unit (no return value, modifies state in-place).
*/
fun main(args: Array<String>) {
    val human = Human(name = "Janwar")
    human.benstokes()

    val list = listOf(1,2,3,4,5,6,7,11,13,17)

    println(list.binarySearch ( 13) )
}