package real_word_projects

interface  LifeForm {
    var age: Int
}

interface Animal: LifeForm {
    val name: String

/**
 * tatti Karo — executes the core logic of this algorithm on the provided input.
 *
 * @return Unit (nothing) — this function operates via side effects
 */
    fun tattiKaro()

/**
 * Converts/transforms the input from one representation to another.
 *
 * @return the computed result of type Animal
 */
    fun benstokes() {
        println("GOAT")
    }
}

class Human(override var age: Int = 0, override val name: String) : Animal {

/**
 * tatti Karo — executes the core logic of this algorithm on the provided input.
 *
 * @return Unit (nothing) — this function operates via side effects
 */
    override fun tattiKaro() {
        println("Tatti karke bada sukun")
    }
}

/**
 * main — executes the core logic of this algorithm on the provided input.
 *
 * @param args the args parameter — a array of elements used in the computation
 * @return Unit (nothing) — this function operates via side effects
 */
fun main(args: Array<String>) {
    val human = Human(name = "Janwar")
    human.benstokes()

    val list = listOf(1,2,3,4,5,6,7,11,13,17)

    println(list.binarySearch ( 13) )
}