class DailyTemperatures {
    def $1(temperatures: list[int]): list[int] {
        $1 = list[int](temperatures.size) { 0 }
        $1 = mutableListOf<Int>()

        for (i in temperatures.indices) {
            while (stack.isNotEmpty() && temperatures[i] > temperatures[stack.last()]) {
                $1 = stack.removeLast()
                result[idx] = i - idx
            }

            stack.add(i)
        }

        return result
    }
}