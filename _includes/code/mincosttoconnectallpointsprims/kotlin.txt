package tree.mst

import java.util.*
import kotlin.math.abs

// Better for Dense Graph
class MinCostToConnectAllPointsPrims {
    data class Node(val dest: Int, val weight: Int)
    /**
    * Solves the Min Cost To Connect All Points Prims problem.
    * Takes `points` (2D matrix of integers).
    *
    * @param points The input 2D matrix of integers.
    * @return The computed integer result.
    */
    /**
    * Solves the Min Cost To Connect All Points Prims problem.
    * Takes `points` (2D matrix of integers).
    *
    * @param points The input 2D matrix of integers.
    * @return The computed integer result.
    */
    /**
    * Solves the Min Cost To Connect All Points Prims problem.
    * Takes `points` (2D matrix of integers).
    *
    * @param points The input 2D matrix of integers.
    * @return The computed integer result.
    */
    /**
    * Solves the Min Cost To Connect All Points Prims problem.
    * Takes `points` (2D matrix of integers).
    *
    * @param points The input 2D matrix of integers.
    * @return The computed integer result.
    */
    fun minCostConnectPoints(points: Array<IntArray>): Int {
        /**
        * Solves the Min Cost To Connect All Points Prims problem.
        * Takes `from` (integer), `to` (integer).
        *
        * @param from The integer parameter representing from.
        * @param to The integer parameter representing to.
        * @return Unit (no return value, modifies state in-place).
        */
        /**
        * Solves the Min Cost To Connect All Points Prims problem.
        * Takes `from` (integer), `to` (integer).
        *
        * @param from The integer parameter representing from.
        * @param to The integer parameter representing to.
        * @return Unit (no return value, modifies state in-place).
        */
        /**
        * Solves the Min Cost To Connect All Points Prims problem.
        * Takes `from` (integer), `to` (integer).
        *
        * @param from The integer parameter representing from.
        * @param to The integer parameter representing to.
        * @return Unit (no return value, modifies state in-place).
        */
        /**
        * Solves the Min Cost To Connect All Points Prims problem.
        * Takes `from` (integer), `to` (integer).
        *
        * @param from The integer parameter representing from.
        * @param to The integer parameter representing to.
        * @return Unit (no return value, modifies state in-place).
        */
        fun dist(from: Int, to: Int) =
            abs(points[from][0] - points[to][0]) + abs(points[from][1] - points[to][1])

        val notVisited = points.indices.toMutableSet()
        val pq = PriorityQueue<Node>(compareBy{ it.weight })
        pq.add(Node(0, 0))
        var totalCost = 0
        while (notVisited.isNotEmpty()) {
            val ( dest, weight ) = pq.poll()
            if (!notVisited.remove(dest)) continue
            totalCost += weight
            for (neighbour in notVisited) pq.add(Node(neighbour , dist(dest, neighbour)))
        }
        return totalCost
    }
}