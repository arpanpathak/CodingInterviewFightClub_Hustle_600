class RemoveElement {
    $1(nums: vector<int>, `val`) {
        auto $1 = 0

        for (j in nums.indices) {
            if (nums[j] != `val`) {
                nums[i++] = nums[j]
            }
        }

        return i
    }
}