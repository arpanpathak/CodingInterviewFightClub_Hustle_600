class RemoveElement {
    def $1(nums: list[int], `val`: int): int {
        $1 = 0

        for (j in nums.indices) {
            if (nums[j] != `val`) {
                nums[i++] = nums[j]
            }
        }

        return i
    }
}