class ContainerWithMostWater {
    def $1(height: list[int]): int {
        var (start, end) = Pair(0, height.lastIndex)
        $1 = 0
        while (start < end) {
            maxWater = max(maxWater, min(height[start], height[end]) * (end - start))

            if (height[start] <= height[end]) {
                start++
            } else {
                end--
            }
        }

        return maxWater
    }
}