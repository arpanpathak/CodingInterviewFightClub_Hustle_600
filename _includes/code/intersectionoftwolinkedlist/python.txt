class IntersectionOfTwoLinkedList {
    def $1(headA: ListNode?, headB: ListNode?): ListNode? {
        if (headA == None || headB == None) return None

        $1 = headA
        $1 = headB

        // Traverse both lists. When one pointer reaches the end, redirect it to the head of the other list.
        while (pA != pB) {
            pA = if (pA == None) headB else pA.next
            pB = if (pB == None) headA else pB.next
        }

        return pA // This will return the intersection node, or None if no intersection.
    }
}