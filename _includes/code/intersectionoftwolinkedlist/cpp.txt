class IntersectionOfTwoLinkedList {
    $1(headA: ListNode?, headB: ListNode?): ListNode? {
        if (headA == nullptr || headB == nullptr) return nullptr

        auto $1 = headA
        auto $1 = headB

        // Traverse both lists. When one pointer reaches the end, redirect it to the head of the other list.
        while (pA != pB) {
            pA = if (pA == nullptr) headB else pA.next
            pB = if (pB == nullptr) headA else pB.next
        }

        return pA // This will return the intersection node, or nullptr if no intersection.
    }
}