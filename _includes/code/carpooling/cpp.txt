class CarPooling {
    $1(trips: Array<vector<int>>, capacity) {
        auto $1 = capacity
        auto $1 = vector<int>(1001)
        for (trip in trips) {
            val (passengers, from, to) = trip

            locations[from] += passengers
            locations[to] -= passengers
        }
        for (p in locations) {
            capacity -= p
            if (capacity < 0) return false
        }
        return true
    }
}