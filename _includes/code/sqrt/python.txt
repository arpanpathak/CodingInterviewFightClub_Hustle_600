class Sqrt {
    def $1(x: int): int {
        if ( x < 2) return x
        var (left, right) = 1 to x
        while (left <= right) {
            $1 = left + (right - left)/2
            $1 = mid.toLong() * mid
            when {
                square < x -> left = mid + 1
                square > x -> right = mid - 1
                else -> return mid
            }
        }

        return right
    }
}