class SquaresOfASortedArray {
    $1(nums: vector<int>): vector<int> {
        auto $1 = vector<int>(nums.size)

        var (left, right, index) = listOf(0, nums.size() - 1, nums.size() - 1)

        while (left <= right) {
            when {
                abs(nums[left]) > abs(nums[right]) -> result[index--] = nums[left] * nums[left++]
                else -> result[index--] = nums[right] * nums[right--]
            }
        }

        return result
    }
}