class TaskScheduler {
    $1(tasks: CharArray, n) {
        auto $1 = tasks.toList().groupingBy { it }.eachCount().values
        auto $1 = freq.maxOrNull() ?: 0
        auto $1 = freq.count { it == maxFreq }

        return max(tasks.size, (maxFreq - 1) * (n + 1) + maxCount)
    }
}