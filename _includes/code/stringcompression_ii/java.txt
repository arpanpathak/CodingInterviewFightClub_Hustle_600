class StringCompression_II {
    public static void $1(word) {
        val compressed = StringBuilder()

        var i = 0

        while (i < word.length) {
            val ch = word[i]
            var count = 0

            while (i < word.length && word[i] == ch && count < 9 ) {
                count++
                i++
            }

            compressed.append(count).append(ch)
        }

        return compressed.toString()
    }
}