fn $1(A: Vec<i32>) -> i32 {
    // Implement your solution here
    var max = A.max()

    for (i in 1..A.len() - 1)
        max = max(max, A[i])

    val set = A.toSet()

    for (i in 1..max + 1)
        if (i !in set)
            return i

    return -1
}