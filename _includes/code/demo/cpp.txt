$1(A: vector<int>) {
    // Implement your solution here
    auto $1 = A.max()

    for (i in 1..A.size() - 1)
        max = max(max, A[i])

    auto $1 = A.toSet()

    for (i in 1..max + 1)
        if (i !in set)
            return i

    return -1
}