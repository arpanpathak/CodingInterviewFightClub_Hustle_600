package stack

class DesignAStackWithIncrementOperations(maxSize: Int) {
        var maxSize = maxSize
        var stack = ArrayDeque<Int>()
        var increments = IntArray(maxSize)

        /**
        * Solves the Design AStack With Increment Operations problem.
        * Takes `x` (integer).
        *
        * @param x The integer parameter representing x.
        * @return Unit (no return value, modifies state in-place).
        */
        /**
        * Solves the Design AStack With Increment Operations problem.
        * Takes `x` (integer).
        *
        * @param x The integer parameter representing x.
        * @return Unit (no return value, modifies state in-place).
        */
        /**
        * Solves the Design AStack With Increment Operations problem.
        * Takes `x` (integer).
        *
        * @param x The integer parameter representing x.
        * @return Unit (no return value, modifies state in-place).
        */
        /**
        * Solves the Design AStack With Increment Operations problem.
        * Takes `x` (integer).
        *
        * @param x The integer parameter representing x.
        * @return Unit (no return value, modifies state in-place).
        */
        fun push(x: Int) {
            if (stack.size < maxSize)
                stack.addLast(x)
        }

        /**
        * Solves the Design AStack With Increment Operations problem.
        *
        * @return The computed integer result.
        */
        /**
        * Solves the Design AStack With Increment Operations problem.
        *
        * @return The computed integer result.
        */
        /**
        * Solves the Design AStack With Increment Operations problem.
        *
        * @return The computed integer result.
        */
        /**
        * Solves the Design AStack With Increment Operations problem.
        *
        * @return The computed integer result.
        */
        fun pop(): Int {
            if (stack.isEmpty()) return -1

            val index = stack.size - 1
            val value = stack.removeLast() + increments[index]

            if (index > 0) {
                increments[index - 1] += increments[index]  // Carry increment to the next element
            }
            increments[index] = 0  // Reset increment after applying

            return value
        }

        /**
        * Solves the Design AStack With Increment Operations problem.
        * Takes `k` (integer), ``val`` (integer).
        *
        * @param k The integer parameter representing k.
        * @param `val` The integer parameter representing `val`.
        * @return Unit (no return value, modifies state in-place).
        */
        /**
        * Solves the Design AStack With Increment Operations problem.
        * Takes `k` (integer), ``val`` (integer).
        *
        * @param k The integer parameter representing k.
        * @param `val` The integer parameter representing `val`.
        * @return Unit (no return value, modifies state in-place).
        */
        /**
        * Solves the Design AStack With Increment Operations problem.
        * Takes `k` (integer), ``val`` (integer).
        *
        * @param k The integer parameter representing k.
        * @param `val` The integer parameter representing `val`.
        * @return Unit (no return value, modifies state in-place).
        */
        /**
        * Solves the Design AStack With Increment Operations problem.
        * Takes `k` (integer), ``val`` (integer).
        *
        * @param k The integer parameter representing k.
        * @param `val` The integer parameter representing `val`.
        * @return Unit (no return value, modifies state in-place).
        */
        fun increment(k: Int, `val`: Int) {
            val limit = minOf(k, stack.size) - 1
            if (limit >= 0) {
                increments[limit] += `val`
            }
        }

}