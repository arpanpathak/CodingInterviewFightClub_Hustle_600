---
layout: chapter
title: "String Matching"
chapter_number: 10
chapter_title: "String Matching"
toc: true
prev_chapter:
  url: "/chapters/09-disjoint-set-union"
  title: "Disjoint Set Union"
next_chapter:
  url: "/chapters/11-backtracking"
  title: "Backtracking"
---

# String Matching

> **56 problems** from the CrackGoogle repository. Click the tabs to switch between **Kotlin, Java, Python, Rust, and C++**.

## Complete Problem Set

| # | Problem | Topic | Code Available |
|---|---------|-------|----------------|
| 1 | **ApplySubstitutions** | string | ✅ All 5 languages |
| 2 | **CheckifaParenthesesStringCanBeValid** | string | ✅ All 5 languages |
| 3 | **CountAndSay** | string | ✅ All 5 languages |
| 4 | **CountWordsWithAGivenPrefix** | string | ✅ All 5 languages |
| 5 | **ExcelSheetToColumnNumber** | string | ✅ All 5 languages |
| 6 | **FindUniqueBinaryString** | string | ✅ All 5 languages |
| 7 | **GoatLatin** | string | ✅ All 5 languages |
| 8 | **GreatestCommonDivisorOfStrings** | string | ✅ All 5 languages |
| 9 | **GroupAnagrams** | string | ✅ All 5 languages |
| 10 | **IsSubsequence** | string | ✅ All 5 languages |
| 11 | **IsomorphicString** | string | ✅ All 5 languages |
| 12 | **LengthOfLastWord** | string | ✅ All 5 languages |
| 13 | **LongestCommonPrefix** | string | ✅ All 5 languages |
| 14 | **LongestPalidnromicSubstring** | string | ✅ All 5 languages |
| 15 | **MaximumLengthofaConcatenatedStringwithUniqueCharacters** | string | ✅ All 5 languages |
| 16 | **MaximumValueOfAStringIsAnArray** | string | ✅ All 5 languages |
| 17 | **MergeStringAlternatively** | string | ✅ All 5 languages |
| 18 | **MinimumDeletionToMakeCharacterFrequenciesUnique** | string | ✅ All 5 languages |
| 19 | **ReverseVowelOfString** | string | ✅ All 5 languages |
| 20 | **ReverseWordsInString** | string | ✅ All 5 languages |
| 21 | **StringCompression** | string | ✅ All 5 languages |
| 22 | **StringCompression_II** | string | ✅ All 5 languages |
| 23 | **ValidAnagram** | string | ✅ All 5 languages |
| 24 | **ValidNumber** | string | ✅ All 5 languages |
| 25 | **ValidPalindrome** | string | ✅ All 5 languages |
| 26 | **ValidPalindrome_II** | string | ✅ All 5 languages |
| 27 | **ValidWordAbbreviation** | string | ✅ All 5 languages |
| 28 | **ValidateIPAddress** | string | ✅ All 5 languages |
| 29 | **FindTheIndexofTheFirstOccurrenceIna String** | string/pattern_matching | ✅ All 5 languages |
| 30 | **FindTheIndexofTheFirstOccurrenceIna String_RabinKarp** | string/pattern_matching | ✅ All 5 languages |
| 31 | **GenerateParantheses** | string/backtracking | ✅ All 5 languages |
| 32 | **EditDistance** | string/dynamic_programming | ✅ All 5 languages |
| 33 | **InterleavingString** | string/dynamic_programming | ✅ All 5 languages |
| 34 | **LongestCommonSubsequence** | string/dynamic_programming | ✅ All 5 languages |
| 35 | **LongestCommonSubstring** | string/dynamic_programming | ✅ All 5 languages |
| 36 | **LongestPalindromicSubsequence** | string/dynamic_programming | ✅ All 5 languages |
| 37 | **LongestPalindromicSubsequence_BottomUp** | string/dynamic_programming | ✅ All 5 languages |
| 38 | **LongestStringChain** | string/dynamic_programming | ✅ All 5 languages |
| 39 | **RegularExpressionMatching** | string/dynamic_programming | ✅ All 5 languages |
| 40 | **ShortestCommonSupersequence** | string/dynamic_programming | ✅ All 5 languages |
| 41 | **ValidPalindrome_III** | string/dynamic_programming | ✅ All 5 languages |
| 42 | **ValidPalindrome_III_SpaceOptimized** | string/dynamic_programming | ✅ All 5 languages |
| 43 | **FindAllAnagrams** | string/sliding_window | ✅ All 5 languages |
| 44 | **MaximumNumberofVowelsinSubstringofGivenLength** | string/sliding_window | ✅ All 5 languages |
| 45 | **MinimumWindowSubstring** | string/sliding_window | ✅ All 5 languages |
| 46 | **BreakAPalindrome** | string/greedy | ✅ All 5 languages |
| 47 | **CustomSortString** | string/sorting | ✅ All 5 languages |
| 48 | **CustomSortString_Linear** | string/sorting | ✅ All 5 languages |
| 49 | **DetermineIfStringsAreClose** | string/hashtable | ✅ All 5 languages |
| 50 | **GroupShiftedStrings** | string/hashtable | ✅ All 5 languages |
| 51 | **PermutationsInString** | string/hashtable | ✅ All 5 languages |
| 52 | **UniqueLength3PalindromicSubsequence** | string/hashtable | ✅ All 5 languages |
| 53 | **UniqueSubstringWithEqualDigitFrequency** | string/hashtable | ✅ All 5 languages |
| 54 | **DecodeString** | string/stack | ✅ All 5 languages |
| 55 | **RemoveAllAdjacentDuplicatesInString** | string/stack | ✅ All 5 languages |
| 56 | **SimplifyPath** | string/stack | ✅ All 5 languages |

---

## ApplySubstitutions

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ApplySubstitutions.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ApplySubstitutions {\n    fun applySubstitutions(replacements: List<List<String>>, text: String): String {\n        val map = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        fun resolve(s: String): String {\n            val sb = StringBuilder()\n            var i = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    val key = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            val result = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}\n"  java="class ApplySubstitutions {\n    public static void $1(replacements: List<List<String>>, text) {\n        val map = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        public static void $1(s) {\n            val sb = StringBuilder()\n            var i = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    val key = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            val result = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}"  python="class ApplySubstitutions {\n    def $1(replacements: List<List<String>>, text: str): str {\n        $1 = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        def $1(s: str): str {\n            $1 = StringBuilder()\n            $1 = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    $1 = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            $1 = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}"  rust="class ApplySubstitutions {\n    fn $1(replacements: List<List<String>>, text -> String) -> String {\n        val map = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        fn $1(s -> String) -> String {\n            val sb = StringBuilder()\n            var i = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    val key = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            val result = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}"  cpp="class ApplySubstitutions {\n    $1(replacements: List<List<String>>, text) {\n        auto $1 = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        $1(s) {\n            auto $1 = StringBuilder()\n            auto $1 = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    auto $1 = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            auto $1 = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}" %}

---

## CheckifaParenthesesStringCanBeValid

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/CheckifaParenthesesStringCanBeValid.kt`

{% include code-tabs.html  kotlin="package string\n\nclass CheckifaParenthesesStringCanBeValid {\n    fun canBeValid(s: String, locked: String): Boolean {\n        // Odd number of brackets\n        if (s.length % 2 !=0) return false\n\n        var openCount = 0\n        for (i in 0 until s.length) {\n            if (s[i] == '(' || s[i] == ')' && locked[i] == '0')\n                openCount++\n            else openCount--\n\n            if (openCount < 0) return false\n\n        }\n\n        // Second pass: Right to left\n        var closeCount = 0\n        for (i in s.indices.reversed()) {\n            // If it's locked as ')' or can be treated as ')'\n            if (s[i] == ')' || (s[i] == '(' && locked[i] == '0')) {\n                closeCount++\n            } else {\n                closeCount--\n            }\n\n            // If at any point we have more opening parentheses than closing\n            if (closeCount < 0) return false\n        }\n\n        // If both counts are valid, the string is balanced\n        return true\n    }\n}\n"  java="class CheckifaParenthesesStringCanBeValid {\n    public static void $1(s, locked) {\n        // Odd number of brackets\n        if (s.length % 2 !=0) return false\n\n        var openCount = 0\n        for (i in 0 until s.length) {\n            if (s[i] == '(' || s[i] == ')' && locked[i] == '0')\n                openCount++\n            else openCount--\n\n            if (openCount < 0) return false\n\n        }\n\n        // Second pass: Right to left\n        var closeCount = 0\n        for (i in s.indices.reversed()) {\n            // If it's locked as ')' or can be treated as ')'\n            if (s[i] == ')' || (s[i] == '(' && locked[i] == '0')) {\n                closeCount++\n            } else {\n                closeCount--\n            }\n\n            // If at any point we have more opening parentheses than closing\n            if (closeCount < 0) return false\n        }\n\n        // If both counts are valid, the string is balanced\n        return true\n    }\n}"  python="class CheckifaParenthesesStringCanBeValid {\n    def $1(s: str, locked: str): bool {\n        // Odd number of brackets\n        if (s.length % 2 !=0) return False\n\n        $1 = 0\n        for (i in 0 until s.length) {\n            if (s[i] == '(' || s[i] == ')' && locked[i] == '0')\n                openCount++\n            else openCount--\n\n            if (openCount < 0) return False\n\n        }\n\n        // Second pass: Right to left\n        $1 = 0\n        for (i in s.indices.reversed()) {\n            // If it's locked as ')' or can be treated as ')'\n            if (s[i] == ')' || (s[i] == '(' && locked[i] == '0')) {\n                closeCount++\n            } else {\n                closeCount--\n            }\n\n            // If at any point we have more opening parentheses than closing\n            if (closeCount < 0) return False\n        }\n\n        // If both counts are valid, the string is balanced\n        return True\n    }\n}"  rust="class CheckifaParenthesesStringCanBeValid {\n    fn $1(s -> String, locked -> String) -> bool {\n        // Odd number of brackets\n        if (s.length % 2 !=0) return false\n\n        var openCount = 0\n        for (i in 0 until s.length) {\n            if (s[i] == '(' || s[i] == ')' && locked[i] == '0')\n                openCount++\n            else openCount--\n\n            if (openCount < 0) return false\n\n        }\n\n        // Second pass: Right to left\n        var closeCount = 0\n        for (i in s.indices.reversed()) {\n            // If it's locked as ')' or can be treated as ')'\n            if (s[i] == ')' || (s[i] == '(' && locked[i] == '0')) {\n                closeCount++\n            } else {\n                closeCount--\n            }\n\n            // If at any point we have more opening parentheses than closing\n            if (closeCount < 0) return false\n        }\n\n        // If both counts are valid, the string is balanced\n        return true\n    }\n}"  cpp="class CheckifaParenthesesStringCanBeValid {\n    $1(s, locked) {\n        // Odd number of brackets\n        if (s.length % 2 !=0) return false\n\n        auto $1 = 0\n        for (i in 0 until s.length) {\n            if (s[i] == '(' || s[i] == ')' && locked[i] == '0')\n                openCount++\n            else openCount--\n\n            if (openCount < 0) return false\n\n        }\n\n        // Second pass: Right to left\n        auto $1 = 0\n        for (i in s.indices.reversed()) {\n            // If it's locked as ')' or can be treated as ')'\n            if (s[i] == ')' || (s[i] == '(' && locked[i] == '0')) {\n                closeCount++\n            } else {\n                closeCount--\n            }\n\n            // If at any point we have more opening parentheses than closing\n            if (closeCount < 0) return false\n        }\n\n        // If both counts are valid, the string is balanced\n        return true\n    }\n}" %}

---

## CountAndSay

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/CountAndSay.kt`

{% include code-tabs.html  kotlin="package string\n\nclass CountAndSay {\n    fun countAndSay(n: Int): String {\n        var result = \"1\"\n        repeat(n - 1) {\n            val nextSequence = buildString {\n                var count = 1\n                for (i in 1 until result.length) {\n                    if (result[i] == result[i - 1]) {\n                        count++\n                    } else {\n                        append(count).append(result[i - 1])\n                        count = 1\n                    }\n                }\n                append(count).append(result.last())\n            }\n            result = nextSequence\n        }\n        return result\n    }\n}"  java="class CountAndSay {\n    public static void $1(n) {\n        var result = \"1\"\n        repeat(n - 1) {\n            val nextSequence = buildString {\n                var count = 1\n                for (i in 1 until result.length) {\n                    if (result[i] == result[i - 1]) {\n                        count++\n                    } else {\n                        append(count).append(result[i - 1])\n                        count = 1\n                    }\n                }\n                append(count).append(result.last())\n            }\n            result = nextSequence\n        }\n        return result\n    }\n}"  python="class CountAndSay {\n    def $1(n: int): str {\n        $1 = \"1\"\n        repeat(n - 1) {\n            $1 = buildString {\n                $1 = 1\n                for (i in 1 until result.length) {\n                    if (result[i] == result[i - 1]) {\n                        count++\n                    } else {\n                        append(count).append(result[i - 1])\n                        count = 1\n                    }\n                }\n                append(count).append(result.last())\n            }\n            result = nextSequence\n        }\n        return result\n    }\n}"  rust="class CountAndSay {\n    fn $1(n -> i32) -> String {\n        var result = \"1\"\n        repeat(n - 1) {\n            val nextSequence = buildString {\n                var count = 1\n                for (i in 1 until result.length) {\n                    if (result[i] == result[i - 1]) {\n                        count++\n                    } else {\n                        append(count).append(result[i - 1])\n                        count = 1\n                    }\n                }\n                append(count).append(result.last())\n            }\n            result = nextSequence\n        }\n        return result\n    }\n}"  cpp="class CountAndSay {\n    $1(n) {\n        auto $1 = \"1\"\n        repeat(n - 1) {\n            auto $1 = buildString {\n                auto $1 = 1\n                for (i in 1 until result.length) {\n                    if (result[i] == result[i - 1]) {\n                        count++\n                    } else {\n                        append(count).append(result[i - 1])\n                        count = 1\n                    }\n                }\n                append(count).append(result.last())\n            }\n            result = nextSequence\n        }\n        return result\n    }\n}" %}

---

## CountWordsWithAGivenPrefix

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/CountWordsWithAGivenPrefix.kt`

{% include code-tabs.html  kotlin="package string\n\nclass CountWordsWithAGivenPrefix {\n    fun prefixCount(words: Array<String>, prefix: String): Int {\n        var count = 0\n\n        for (word in words) {\n            if (word.startsWith(prefix)) {\n                count++\n            }\n        }\n\n        return count\n    }\n}\n"  java="class CountWordsWithAGivenPrefix {\n    public static void $1(words: Array<String>, prefix) {\n        var count = 0\n\n        for (word in words) {\n            if (word.startsWith(prefix)) {\n                count++\n            }\n        }\n\n        return count\n    }\n}"  python="class CountWordsWithAGivenPrefix {\n    def $1(words: Array<String>, prefix: str): int {\n        $1 = 0\n\n        for (word in words) {\n            if (word.startsWith(prefix)) {\n                count++\n            }\n        }\n\n        return count\n    }\n}"  rust="class CountWordsWithAGivenPrefix {\n    fn $1(words: Array<String>, prefix -> String) -> i32 {\n        var count = 0\n\n        for (word in words) {\n            if (word.startsWith(prefix)) {\n                count++\n            }\n        }\n\n        return count\n    }\n}"  cpp="class CountWordsWithAGivenPrefix {\n    $1(words: Array<String>, prefix) {\n        auto $1 = 0\n\n        for (word in words) {\n            if (word.startsWith(prefix)) {\n                count++\n            }\n        }\n\n        return count\n    }\n}" %}

---

## ExcelSheetToColumnNumber

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ExcelSheetToColumnNumber.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ExcelSheetToColumnNumber {\n    fun titleToNumber(columnTitle: String): Int {\n        var base = 1\n        var sum = 0\n        for (i in columnTitle.length - 1 downTo 0) {\n            sum += (columnTitle[i] - 'A' + 1) * base\n            base *= 26\n        }\n        return sum\n    }\n}\n"  java="class ExcelSheetToColumnNumber {\n    public static void $1(columnTitle) {\n        var base = 1\n        var sum = 0\n        for (i in columnTitle.length - 1 downTo 0) {\n            sum += (columnTitle[i] - 'A' + 1) * base\n            base *= 26\n        }\n        return sum\n    }\n}"  python="class ExcelSheetToColumnNumber {\n    def $1(columnTitle: str): int {\n        $1 = 1\n        $1 = 0\n        for (i in columnTitle.length - 1 downTo 0) {\n            sum += (columnTitle[i] - 'A' + 1) * base\n            base *= 26\n        }\n        return sum\n    }\n}"  rust="class ExcelSheetToColumnNumber {\n    fn $1(columnTitle -> String) -> i32 {\n        var base = 1\n        var sum = 0\n        for (i in columnTitle.length - 1 downTo 0) {\n            sum += (columnTitle[i] - 'A' + 1) * base\n            base *= 26\n        }\n        return sum\n    }\n}"  cpp="class ExcelSheetToColumnNumber {\n    $1(columnTitle) {\n        auto $1 = 1\n        auto $1 = 0\n        for (i in columnTitle.length - 1 downTo 0) {\n            sum += (columnTitle[i] - 'A' + 1) * base\n            base *= 26\n        }\n        return sum\n    }\n}" %}

---

## FindUniqueBinaryString

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/FindUniqueBinaryString.kt`

{% include code-tabs.html  kotlin="package string\n\n// Cantor’s Diagonalization Trick\nclass FindUniqueBinaryString {\n    fun findDifferentBinaryString(nums: Array<String>): String {\n        val n = nums.size\n        val sb = StringBuilder()\n\n        for (i in 0 until n) {\n            sb.append(if (nums[i][i] == '0') '1' else '0')\n        }\n\n        return sb.toString()\n    }\n}\n"  java="// Cantor’s Diagonalization Trick\nclass FindUniqueBinaryString {\n    public static void $1(nums: Array<String>) {\n        val n = nums.size\n        val sb = StringBuilder()\n\n        for (i in 0 until n) {\n            sb.append(if (nums[i][i] == '0') '1' else '0')\n        }\n\n        return sb.toString()\n    }\n}"  python="// Cantor’s Diagonalization Trick\nclass FindUniqueBinaryString {\n    def $1(nums: Array<String>): str {\n        $1 = nums.size\n        $1 = StringBuilder()\n\n        for (i in 0 until n) {\n            sb.append(if (nums[i][i] == '0') '1' else '0')\n        }\n\n        return sb.toString()\n    }\n}"  rust="// Cantor’s Diagonalization Trick\nclass FindUniqueBinaryString {\n    fn $1(nums: Array<String>) -> String {\n        val n = nums.size\n        val sb = StringBuilder()\n\n        for (i in 0 until n) {\n            sb.append(if (nums[i][i] == '0') '1' else '0')\n        }\n\n        return sb.toString()\n    }\n}"  cpp="// Cantor’s Diagonalization Trick\nclass FindUniqueBinaryString {\n    $1(nums: Array<String>) {\n        auto $1 = nums.size\n        auto $1 = StringBuilder()\n\n        for (i in 0 until n) {\n            sb.append(if (nums[i][i] == '0') '1' else '0')\n        }\n\n        return sb.toString()\n    }\n}" %}

---

## GoatLatin

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/GoatLatin.kt`

{% include code-tabs.html  kotlin="package string\n\nclass GoatLatin {\n    fun toGoatLatin(sentence: String): String {\n        // Define a set of vowels for faster lookup\n        val vowels = setOf('a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U')\n\n        // Process the sentence word by word\n        return sentence.split(\" \").mapIndexed { index, word ->\n            // Check if the word starts with a vowel\n            if (word[0] in vowels) {\n                // If the word starts with a vowel, append \"ma\" and the appropriate number of 'a's\n                word + \"ma\" + \"a\".repeat(index + 1)\n            } else {\n                // If the word starts with a consonant, move the first letter to the end, append \"ma\", and the appropriate number of 'a's\n                word.substring(1) + word[0] + \"ma\" + \"a\".repeat(index + 1)\n            }\n        }.joinToString(\" \")  // Join the words back into a single string\n    }\n}"  java="class GoatLatin {\n    public static void $1(sentence) {\n        // Define a set of vowels for faster lookup\n        val vowels = setOf('a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U')\n\n        // Process the sentence word by word\n        return sentence.split(\" \").mapIndexed { index, word ->\n            // Check if the word starts with a vowel\n            if (word[0] in vowels) {\n                // If the word starts with a vowel, append \"ma\" and the appropriate number of 'a's\n                word + \"ma\" + \"a\".repeat(index + 1)\n            } else {\n                // If the word starts with a consonant, move the first letter to the end, append \"ma\", and the appropriate number of 'a's\n                word.substring(1) + word[0] + \"ma\" + \"a\".repeat(index + 1)\n            }\n        }.joinToString(\" \")  // Join the words back into a single string\n    }\n}"  python="class GoatLatin {\n    def $1(sentence: str): str {\n        // Define a set of vowels for faster lookup\n        $1 = setOf('a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U')\n\n        // Process the sentence word by word\n        return sentence.split(\" \").mapIndexed { index, word ->\n            // Check if the word starts with a vowel\n            if (word[0] in vowels) {\n                // If the word starts with a vowel, append \"ma\" and the appropriate number of 'a's\n                word + \"ma\" + \"a\".repeat(index + 1)\n            } else {\n                // If the word starts with a consonant, move the first letter to the end, append \"ma\", and the appropriate number of 'a's\n                word.substring(1) + word[0] + \"ma\" + \"a\".repeat(index + 1)\n            }\n        }.joinToString(\" \")  // Join the words back into a single string\n    }\n}"  rust="class GoatLatin {\n    fn $1(sentence -> String) -> String {\n        // Define a set of vowels for faster lookup\n        val vowels = setOf('a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U')\n\n        // Process the sentence word by word\n        return sentence.split(\" \").mapIndexed { index, word ->\n            // Check if the word starts with a vowel\n            if (word[0] in vowels) {\n                // If the word starts with a vowel, append \"ma\" and the appropriate number of 'a's\n                word + \"ma\" + \"a\".repeat(index + 1)\n            } else {\n                // If the word starts with a consonant, move the first letter to the end, append \"ma\", and the appropriate number of 'a's\n                word.substring(1) + word[0] + \"ma\" + \"a\".repeat(index + 1)\n            }\n        }.joinToString(\" \")  // Join the words back into a single string\n    }\n}"  cpp="class GoatLatin {\n    $1(sentence) {\n        // Define a set of vowels for faster lookup\n        auto $1 = setOf('a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U')\n\n        // Process the sentence word by word\n        return sentence.split(\" \").mapIndexed { index, word ->\n            // Check if the word starts with a vowel\n            if (word[0] in vowels) {\n                // If the word starts with a vowel, append \"ma\" and the appropriate number of 'a's\n                word + \"ma\" + \"a\".repeat(index + 1)\n            } else {\n                // If the word starts with a consonant, move the first letter to the end, append \"ma\", and the appropriate number of 'a's\n                word.substring(1) + word[0] + \"ma\" + \"a\".repeat(index + 1)\n            }\n        }.joinToString(\" \")  // Join the words back into a single string\n    }\n}" %}

---

## GreatestCommonDivisorOfStrings

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/GreatestCommonDivisorOfStrings.kt`

{% include code-tabs.html  kotlin="package string\n\nclass GreatestCommonDivisorOfStrings {\n    fun gcd(a: Int, b: Int): Int {\n        return when {\n            a == b -> a\n            a > b -> gcd(a - b , b)\n            else -> gcd(a, b - a)\n        }\n    }\n\n    fun gcdOfStrings(str1: String, str2: String): String {\n        // Dhoyasha Foggy I didn't understand.\n        if ((str1 + str2) != (str2 + str1))\n            return \"\"\n\n        val gcdLength = gcd(str1.length, str2.length)\n\n        return str1.substring(0, gcdLength)\n    }\n}\n"  java="class GreatestCommonDivisorOfStrings {\n    public static void $1(a, b) {\n        return when {\n            a == b -> a\n            a > b -> gcd(a - b , b)\n            else -> gcd(a, b - a)\n        }\n    }\n\n    public static void $1(str1, str2) {\n        // Dhoyasha Foggy I didn't understand.\n        if ((str1 + str2) != (str2 + str1))\n            return \"\"\n\n        val gcdLength = gcd(str1.length, str2.length)\n\n        return str1.substring(0, gcdLength)\n    }\n}"  python="class GreatestCommonDivisorOfStrings {\n    def $1(a: int, b: int): int {\n        return when {\n            a == b -> a\n            a > b -> gcd(a - b , b)\n            else -> gcd(a, b - a)\n        }\n    }\n\n    def $1(str1: str, str2: str): str {\n        // Dhoyasha Foggy I didn't understand.\n        if ((str1 + str2) != (str2 + str1))\n            return \"\"\n\n        $1 = gcd(str1.length, str2.length)\n\n        return str1.substring(0, gcdLength)\n    }\n}"  rust="class GreatestCommonDivisorOfStrings {\n    fn $1(a -> i32, b -> i32) -> i32 {\n        return when {\n            a == b -> a\n            a > b -> gcd(a - b , b)\n            else -> gcd(a, b - a)\n        }\n    }\n\n    fn $1(str1 -> String, str2 -> String) -> String {\n        // Dhoyasha Foggy I didn't understand.\n        if ((str1 + str2) != (str2 + str1))\n            return \"\"\n\n        val gcdLength = gcd(str1.length, str2.length)\n\n        return str1.substring(0, gcdLength)\n    }\n}"  cpp="class GreatestCommonDivisorOfStrings {\n    $1(a, b) {\n        return when {\n            a == b -> a\n            a > b -> gcd(a - b , b)\n            else -> gcd(a, b - a)\n        }\n    }\n\n    $1(str1, str2) {\n        // Dhoyasha Foggy I didn't understand.\n        if ((str1 + str2) != (str2 + str1))\n            return \"\"\n\n        auto $1 = gcd(str1.length, str2.length)\n\n        return str1.substring(0, gcdLength)\n    }\n}" %}

---

## GroupAnagrams

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/GroupAnagrams.kt`

{% include code-tabs.html  kotlin="package string\n\nclass GroupAnagrams {\n    fun groupAnagrams(strs: Array<String>): List<List<String>> {\n        val anagramMap = mutableMapOf<List<Int>, MutableList<String>>()  // Using List<Int> as the key\n        for (word in strs) {\n            val count = IntArray(26)\n            // Count the frequency of each character\n            word.forEach { count[it - 'a']++ }\n            anagramMap.getOrPut(count.toList()) { mutableListOf() }.add(word)\n        }\n\n        // Return the list of grouped anagrams\n        return anagramMap.values.toList()\n    }\n}\n"  java="class GroupAnagrams {\n    public static void $1(strs: Array<String>): List<List<String>> {\n        val anagramMap = mutableMapOf<List<Int>, MutableList<String>>()  // Using List<Int> as the key\n        for (word in strs) {\n            val count = IntArray(26)\n            // Count the frequency of each character\n            word.forEach { count[it - 'a']++ }\n            anagramMap.getOrPut(count.toList()) { mutableListOf() }.add(word)\n        }\n\n        // Return the list of grouped anagrams\n        return anagramMap.values.toList()\n    }\n}"  python="class GroupAnagrams {\n    def $1(strs: Array<String>): List<List<String>> {\n        $1 = mutableMapOf<List<Int>, MutableList<String>>()  // Using List<Int> as the key\n        for (word in strs) {\n            $1 = list[int](26)\n            // Count the frequency of each character\n            word.forEach { count[it - 'a']++ }\n            anagramMap.getOrPut(count.toList()) { mutableListOf() }.add(word)\n        }\n\n        // Return the list of grouped anagrams\n        return anagramMap.values.toList()\n    }\n}"  rust="class GroupAnagrams {\n    fn $1(strs: Array<String>): List<List<String>> {\n        val anagramMap = mutableMapOf<List<Int>, MutableList<String>>()  // Using List<Int> as the key\n        for (word in strs) {\n            val count = Vec<i32>(26)\n            // Count the frequency of each character\n            word.forEach { count[it - 'a']++ }\n            anagramMap.getOrPut(count.toList()) { mutableListOf() }.add(word)\n        }\n\n        // Return the list of grouped anagrams\n        return anagramMap.values.toList()\n    }\n}"  cpp="class GroupAnagrams {\n    $1(strs: Array<String>): List<List<String>> {\n        auto $1 = mutableMapOf<List<Int>, MutableList<String>>()  // Using List<Int> as the key\n        for (word in strs) {\n            auto $1 = vector<int>(26)\n            // Count the frequency of each character\n            word.forEach { count[it - 'a']++ }\n            anagramMap.getOrPut(count.toList()) { mutableListOf() }.add(word)\n        }\n\n        // Return the list of grouped anagrams\n        return anagramMap.values.toList()\n    }\n}" %}

---

## IsSubsequence

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/IsSubsequence.kt`

{% include code-tabs.html  kotlin="package string\n\nclass IsSubsequence {\n    fun isSubsequence(s: String, t: String): Boolean {\n        var i = 0\n        var j = 0\n\n        while (i < s.length && j < t.length) {\n            if (s[i] == t[j]) {\n                i++\n                j++\n            } else {\n                j++\n            }\n        }\n\n        // Match found\n        return i == s.length\n    }\n}"  java="class IsSubsequence {\n    public static void $1(s, t) {\n        var i = 0\n        var j = 0\n\n        while (i < s.length && j < t.length) {\n            if (s[i] == t[j]) {\n                i++\n                j++\n            } else {\n                j++\n            }\n        }\n\n        // Match found\n        return i == s.length\n    }\n}"  python="class IsSubsequence {\n    def $1(s: str, t: str): bool {\n        $1 = 0\n        $1 = 0\n\n        while (i < s.length && j < t.length) {\n            if (s[i] == t[j]) {\n                i++\n                j++\n            } else {\n                j++\n            }\n        }\n\n        // Match found\n        return i == s.length\n    }\n}"  rust="class IsSubsequence {\n    fn $1(s -> String, t -> String) -> bool {\n        var i = 0\n        var j = 0\n\n        while (i < s.length && j < t.length) {\n            if (s[i] == t[j]) {\n                i++\n                j++\n            } else {\n                j++\n            }\n        }\n\n        // Match found\n        return i == s.length\n    }\n}"  cpp="class IsSubsequence {\n    $1(s, t) {\n        auto $1 = 0\n        auto $1 = 0\n\n        while (i < s.length && j < t.length) {\n            if (s[i] == t[j]) {\n                i++\n                j++\n            } else {\n                j++\n            }\n        }\n\n        // Match found\n        return i == s.length\n    }\n}" %}

---

## IsomorphicString

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/IsomorphicString.kt`

{% include code-tabs.html  kotlin="package string\n\nclass IsomorphicString {\n    fun encode(s: String): String {\n        val map = mutableMapOf<Char, Int>()\n        val sb = StringBuilder()\n        var code = 0\n\n        for (c in s) {\n            if (c !in map) map[c] = code++\n            sb.append(map[c]).append(\" \")\n        }\n\n        return sb.toString()\n    }\n\n    fun isIsomorphic(s: String, t: String): Boolean {\n        return encode(s) == encode(t)\n    }\n}\n"  java="class IsomorphicString {\n    public static void $1(s) {\n        val map = mutableMapOf<Char, Int>()\n        val sb = StringBuilder()\n        var code = 0\n\n        for (c in s) {\n            if (c !in map) map[c] = code++\n            sb.append(map[c]).append(\" \")\n        }\n\n        return sb.toString()\n    }\n\n    public static void $1(s, t) {\n        return encode(s) == encode(t)\n    }\n}"  python="class IsomorphicString {\n    def $1(s: str): str {\n        $1 = mutableMapOf<Char, Int>()\n        $1 = StringBuilder()\n        $1 = 0\n\n        for (c in s) {\n            if (c !in map) map[c] = code++\n            sb.append(map[c]).append(\" \")\n        }\n\n        return sb.toString()\n    }\n\n    def $1(s: str, t: str): bool {\n        return encode(s) == encode(t)\n    }\n}"  rust="class IsomorphicString {\n    fn $1(s -> String) -> String {\n        val map = mutableMapOf<Char, Int>()\n        val sb = StringBuilder()\n        var code = 0\n\n        for (c in s) {\n            if (c !in map) map[c] = code++\n            sb.append(map[c]).append(\" \")\n        }\n\n        return sb.toString()\n    }\n\n    fn $1(s -> String, t -> String) -> bool {\n        return encode(s) == encode(t)\n    }\n}"  cpp="class IsomorphicString {\n    $1(s) {\n        auto $1 = mutableMapOf<Char, Int>()\n        auto $1 = StringBuilder()\n        auto $1 = 0\n\n        for (c in s) {\n            if (c !in map) map[c] = code++\n            sb.append(map[c]).append(\" \")\n        }\n\n        return sb.toString()\n    }\n\n    $1(s, t) {\n        return encode(s) == encode(t)\n    }\n}" %}

---

## LengthOfLastWord

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/LengthOfLastWord.kt`

{% include code-tabs.html  kotlin="package string\n\nclass LengthOfLastWord {\n    fun lengthOfLastWord(s: String): Int {\n        var i = s.length - 1\n        var len = 0\n\n        while (i >= 0 && s[i] == ' ') i--     // skip trailing spaces\n        while (i >= 0 && s[i] != ' ') {\n            len++\n            i--\n        }\n\n        return len\n    }\n}"  java="class LengthOfLastWord {\n    public static void $1(s) {\n        var i = s.length - 1\n        var len = 0\n\n        while (i >= 0 && s[i] == ' ') i--     // skip trailing spaces\n        while (i >= 0 && s[i] != ' ') {\n            len++\n            i--\n        }\n\n        return len\n    }\n}"  python="class LengthOfLastWord {\n    def $1(s: str): int {\n        $1 = s.length - 1\n        $1 = 0\n\n        while (i >= 0 && s[i] == ' ') i--     // skip trailing spaces\n        while (i >= 0 && s[i] != ' ') {\n            len++\n            i--\n        }\n\n        return len\n    }\n}"  rust="class LengthOfLastWord {\n    fn $1(s -> String) -> i32 {\n        var i = s.length - 1\n        var len = 0\n\n        while (i >= 0 && s[i] == ' ') i--     // skip trailing spaces\n        while (i >= 0 && s[i] != ' ') {\n            len++\n            i--\n        }\n\n        return len\n    }\n}"  cpp="class LengthOfLastWord {\n    $1(s) {\n        auto $1 = s.length - 1\n        auto $1 = 0\n\n        while (i >= 0 && s[i] == ' ') i--     // skip trailing spaces\n        while (i >= 0 && s[i] != ' ') {\n            len++\n            i--\n        }\n\n        return len\n    }\n}" %}

---

## LongestCommonPrefix

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/LongestCommonPrefix.kt`

{% include code-tabs.html  kotlin="package string\n\nclass LongestCommonPrefix {\n    fun longestCommonPrefix(strs: Array<String>): String {\n        if (strs.isEmpty()) return \"\"\n\n        // Use the first string as the reference\n        for (i in strs[0].indices) {\n            val char = strs[0][i]\n            // Compare this character with the corresponding character in all other strings\n            for (j in 1 until strs.size) {\n                // If the current string is shorter or the characters don't match, return the prefix so far\n                if (i >= strs[j].length || strs[j][i] != char) {\n                    return strs[0].substring(0, i)\n                }\n            }\n        }\n        // If no mismatch is found, the entire first string is the common prefix\n        return strs[0]\n    }\n}"  java="class LongestCommonPrefix {\n    public static void $1(strs: Array<String>) {\n        if (strs.isEmpty()) return \"\"\n\n        // Use the first string as the reference\n        for (i in strs[0].indices) {\n            val char = strs[0][i]\n            // Compare this character with the corresponding character in all other strings\n            for (j in 1 until strs.size) {\n                // If the current string is shorter or the characters don't match, return the prefix so far\n                if (i >= strs[j].length || strs[j][i] != char) {\n                    return strs[0].substring(0, i)\n                }\n            }\n        }\n        // If no mismatch is found, the entire first string is the common prefix\n        return strs[0]\n    }\n}"  python="class LongestCommonPrefix {\n    def $1(strs: Array<String>): str {\n        if (strs.isEmpty()) return \"\"\n\n        // Use the first string as the reference\n        for (i in strs[0].indices) {\n            $1 = strs[0][i]\n            // Compare this character with the corresponding character in all other strings\n            for (j in 1 until strs.size) {\n                // If the current string is shorter or the characters don't match, return the prefix so far\n                if (i >= strs[j].length || strs[j][i] != char) {\n                    return strs[0].substring(0, i)\n                }\n            }\n        }\n        // If no mismatch is found, the entire first string is the common prefix\n        return strs[0]\n    }\n}"  rust="class LongestCommonPrefix {\n    fn $1(strs: Array<String>) -> String {\n        if (strs.isEmpty()) return \"\"\n\n        // Use the first string as the reference\n        for (i in strs[0].indices) {\n            val char = strs[0][i]\n            // Compare this character with the corresponding character in all other strings\n            for (j in 1 until strs.size) {\n                // If the current string is shorter or the characters don't match, return the prefix so far\n                if (i >= strs[j].length || strs[j][i] != char) {\n                    return strs[0].substring(0, i)\n                }\n            }\n        }\n        // If no mismatch is found, the entire first string is the common prefix\n        return strs[0]\n    }\n}"  cpp="class LongestCommonPrefix {\n    $1(strs: Array<String>) {\n        if (strs.isEmpty()) return \"\"\n\n        // Use the first string as the reference\n        for (i in strs[0].indices) {\n            auto $1 = strs[0][i]\n            // Compare this character with the corresponding character in all other strings\n            for (j in 1 until strs.size) {\n                // If the current string is shorter or the characters don't match, return the prefix so far\n                if (i >= strs[j].length || strs[j][i] != char) {\n                    return strs[0].substring(0, i)\n                }\n            }\n        }\n        // If no mismatch is found, the entire first string is the common prefix\n        return strs[0]\n    }\n}" %}

---

## LongestPalidnromicSubstring

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/LongestPalidnromicSubstring.kt`

{% include code-tabs.html  kotlin="package string\n\nclass LongestPalidnromicSubstring {\n    fun longestPalindrome(s: String): String {\n        if (s.isEmpty()) return \"\"\n\n        var start = 0\n        var maxLength = 1\n\n        // Helper function to expand around the center\n        fun expandAroundCenter(left: Int, right: Int): Int {\n            var l = left\n            var r = right\n            while (l >= 0 && r < s.length && s[l] == s[r]) {\n                l--\n                r++\n            }\n            // Return the length of the palindrome\n            return r - l - 1\n        }\n\n        for (i in 0 until s.length) {\n            // Check for odd length palindrome (center is s[i])\n            val len1 = expandAroundCenter(i, i)\n            // Check for even length palindrome (center is between s[i] and s[i + 1])\n            val len2 = expandAroundCenter(i, i + 1)\n\n            val len = maxOf(len1, len2)\n\n            if (len > maxLength) {\n                maxLength = len\n                start = i - (len - 1) / 2  // Calculate the start of the palindrome\n            }\n        }\n\n        return s.substring(start, start + maxLength)\n    }\n}\n"  java="class LongestPalidnromicSubstring {\n    public static void $1(s) {\n        if (s.isEmpty()) return \"\"\n\n        var start = 0\n        var maxLength = 1\n\n        // Helper function to expand around the center\n        public static void $1(left, right) {\n            var l = left\n            var r = right\n            while (l >= 0 && r < s.length && s[l] == s[r]) {\n                l--\n                r++\n            }\n            // Return the length of the palindrome\n            return r - l - 1\n        }\n\n        for (i in 0 until s.length) {\n            // Check for odd length palindrome (center is s[i])\n            val len1 = expandAroundCenter(i, i)\n            // Check for even length palindrome (center is between s[i] and s[i + 1])\n            val len2 = expandAroundCenter(i, i + 1)\n\n            val len = maxOf(len1, len2)\n\n            if (len > maxLength) {\n                maxLength = len\n                start = i - (len - 1) / 2  // Calculate the start of the palindrome\n            }\n        }\n\n        return s.substring(start, start + maxLength)\n    }\n}"  python="class LongestPalidnromicSubstring {\n    def $1(s: str): str {\n        if (s.isEmpty()) return \"\"\n\n        $1 = 0\n        $1 = 1\n\n        // Helper function to expand around the center\n        def $1(left: int, right: int): int {\n            $1 = left\n            $1 = right\n            while (l >= 0 && r < s.length && s[l] == s[r]) {\n                l--\n                r++\n            }\n            // Return the length of the palindrome\n            return r - l - 1\n        }\n\n        for (i in 0 until s.length) {\n            // Check for odd length palindrome (center is s[i])\n            $1 = expandAroundCenter(i, i)\n            // Check for even length palindrome (center is between s[i] and s[i + 1])\n            $1 = expandAroundCenter(i, i + 1)\n\n            $1 = max(len1, len2)\n\n            if (len > maxLength) {\n                maxLength = len\n                start = i - (len - 1) / 2  // Calculate the start of the palindrome\n            }\n        }\n\n        return s.substring(start, start + maxLength)\n    }\n}"  rust="class LongestPalidnromicSubstring {\n    fn $1(s -> String) -> String {\n        if (s.isEmpty()) return \"\"\n\n        var start = 0\n        var maxLength = 1\n\n        // Helper function to expand around the center\n        fn $1(left -> i32, right -> i32) -> i32 {\n            var l = left\n            var r = right\n            while (l >= 0 && r < s.length && s[l] == s[r]) {\n                l--\n                r++\n            }\n            // Return the length of the palindrome\n            return r - l - 1\n        }\n\n        for (i in 0 until s.length) {\n            // Check for odd length palindrome (center is s[i])\n            val len1 = expandAroundCenter(i, i)\n            // Check for even length palindrome (center is between s[i] and s[i + 1])\n            val len2 = expandAroundCenter(i, i + 1)\n\n            val len = max(len1, len2)\n\n            if (len > maxLength) {\n                maxLength = len\n                start = i - (len - 1) / 2  // Calculate the start of the palindrome\n            }\n        }\n\n        return s.substring(start, start + maxLength)\n    }\n}"  cpp="class LongestPalidnromicSubstring {\n    $1(s) {\n        if (s.isEmpty()) return \"\"\n\n        auto $1 = 0\n        auto $1 = 1\n\n        // Helper function to expand around the center\n        $1(left, right) {\n            auto $1 = left\n            auto $1 = right\n            while (l >= 0 && r < s.length && s[l] == s[r]) {\n                l--\n                r++\n            }\n            // Return the length of the palindrome\n            return r - l - 1\n        }\n\n        for (i in 0 until s.length) {\n            // Check for odd length palindrome (center is s[i])\n            auto $1 = expandAroundCenter(i, i)\n            // Check for even length palindrome (center is between s[i] and s[i + 1])\n            auto $1 = expandAroundCenter(i, i + 1)\n\n            auto $1 = max(len1, len2)\n\n            if (len > maxLength) {\n                maxLength = len\n                start = i - (len - 1) / 2  // Calculate the start of the palindrome\n            }\n        }\n\n        return s.substring(start, start + maxLength)\n    }\n}" %}

---

## MaximumLengthofaConcatenatedStringwithUniqueCharacters

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/MaximumLengthofaConcatenatedStringwithUniqueCharacters.kt`

{% include code-tabs.html  kotlin="package string\n\nclass MaximumLengthofaConcatenatedStringwithUniqueCharacters {\n    fun maxLength(arr: List<String>): Int {\n        var maxLen = 0\n        val uniqueStrings = arr.filter { it.toCharArray().toSet().size == it.length }\n\n        fun backtrack(index: Int, currentMask: Int, currentLength: Int) {\n            if (index == uniqueStrings.size) {\n                if (currentLength > maxLen) {\n                    maxLen = currentLength\n                }\n                return\n            }\n\n            val str = uniqueStrings[index]\n            var newMask = currentMask\n            var canAdd = true\n\n            for (c in str) {\n                val bit = 1 shl (c - 'a')\n                if (newMask and bit != 0) {\n                    canAdd = false\n                    break\n                }\n                newMask = newMask or bit\n            }\n\n            if (canAdd) {\n                backtrack(index + 1, newMask, currentLength + str.length)\n            }\n            backtrack(index + 1, currentMask, currentLength)\n        }\n\n        backtrack(0, 0, 0)\n        return maxLen\n    }\n}\n"  java="class MaximumLengthofaConcatenatedStringwithUniqueCharacters {\n    public static void $1(arr: List<String>) {\n        var maxLen = 0\n        val uniqueStrings = arr.filter { it.toCharArray().toSet().size == it.length }\n\n        public static void $1(index, currentMask, currentLength) {\n            if (index == uniqueStrings.size) {\n                if (currentLength > maxLen) {\n                    maxLen = currentLength\n                }\n                return\n            }\n\n            val str = uniqueStrings[index]\n            var newMask = currentMask\n            var canAdd = true\n\n            for (c in str) {\n                val bit = 1 shl (c - 'a')\n                if (newMask and bit != 0) {\n                    canAdd = false\n                    break\n                }\n                newMask = newMask or bit\n            }\n\n            if (canAdd) {\n                backtrack(index + 1, newMask, currentLength + str.length)\n            }\n            backtrack(index + 1, currentMask, currentLength)\n        }\n\n        backtrack(0, 0, 0)\n        return maxLen\n    }\n}"  python="class MaximumLengthofaConcatenatedStringwithUniqueCharacters {\n    def $1(arr: List<String>): int {\n        $1 = 0\n        $1 = arr.filter { it.toCharArray().toSet().size == it.length }\n\n        def $1(index: int, currentMask: int, currentLength: int) {\n            if (index == uniqueStrings.size) {\n                if (currentLength > maxLen) {\n                    maxLen = currentLength\n                }\n                return\n            }\n\n            $1 = uniqueStrings[index]\n            $1 = currentMask\n            $1 = True\n\n            for (c in str) {\n                $1 = 1 shl (c - 'a')\n                if (newMask and bit != 0) {\n                    canAdd = False\n                    break\n                }\n                newMask = newMask or bit\n            }\n\n            if (canAdd) {\n                backtrack(index + 1, newMask, currentLength + str.length)\n            }\n            backtrack(index + 1, currentMask, currentLength)\n        }\n\n        backtrack(0, 0, 0)\n        return maxLen\n    }\n}"  rust="class MaximumLengthofaConcatenatedStringwithUniqueCharacters {\n    fn $1(arr: List<String>) -> i32 {\n        var maxLen = 0\n        val uniqueStrings = arr.filter { it.toCharArray().toSet().size == it.length }\n\n        fn $1(index -> i32, currentMask -> i32, currentLength -> i32) {\n            if (index == uniqueStrings.size) {\n                if (currentLength > maxLen) {\n                    maxLen = currentLength\n                }\n                return\n            }\n\n            val str = uniqueStrings[index]\n            var newMask = currentMask\n            var canAdd = true\n\n            for (c in str) {\n                val bit = 1 shl (c - 'a')\n                if (newMask and bit != 0) {\n                    canAdd = false\n                    break\n                }\n                newMask = newMask or bit\n            }\n\n            if (canAdd) {\n                backtrack(index + 1, newMask, currentLength + str.length)\n            }\n            backtrack(index + 1, currentMask, currentLength)\n        }\n\n        backtrack(0, 0, 0)\n        return maxLen\n    }\n}"  cpp="class MaximumLengthofaConcatenatedStringwithUniqueCharacters {\n    $1(arr: List<String>) {\n        auto $1 = 0\n        auto $1 = arr.filter { it.toCharArray().toSet().size == it.length }\n\n        $1(index, currentMask, currentLength) {\n            if (index == uniqueStrings.size) {\n                if (currentLength > maxLen) {\n                    maxLen = currentLength\n                }\n                return\n            }\n\n            auto $1 = uniqueStrings[index]\n            auto $1 = currentMask\n            auto $1 = true\n\n            for (c in str) {\n                auto $1 = 1 shl (c - 'a')\n                if (newMask and bit != 0) {\n                    canAdd = false\n                    break\n                }\n                newMask = newMask or bit\n            }\n\n            if (canAdd) {\n                backtrack(index + 1, newMask, currentLength + str.length)\n            }\n            backtrack(index + 1, currentMask, currentLength)\n        }\n\n        backtrack(0, 0, 0)\n        return maxLen\n    }\n}" %}

---

## MaximumValueOfAStringIsAnArray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/MaximumValueOfAStringIsAnArray.kt`

{% include code-tabs.html  kotlin="package string\n\nclass MaximumValueOfAStringIsAnArray {\n    fun maximumValue(strs: Array<String>): Int {\n        return strs.maxOf {\n            val numericValue = it.toIntOrNull()\n            numericValue?: it.length\n        }\n    }\n}\n"  java="class MaximumValueOfAStringIsAnArray {\n    public static void $1(strs: Array<String>) {\n        return strs.maxOf {\n            val numericValue = it.toIntOrNull()\n            numericValue?: it.length\n        }\n    }\n}"  python="class MaximumValueOfAStringIsAnArray {\n    def $1(strs: Array<String>): int {\n        return strs.max {\n            $1 = it.toIntOrNull()\n            numericValue?: it.length\n        }\n    }\n}"  rust="class MaximumValueOfAStringIsAnArray {\n    fn $1(strs: Array<String>) -> i32 {\n        return strs.max {\n            val numericValue = it.toIntOrNull()\n            numericValue?: it.length\n        }\n    }\n}"  cpp="class MaximumValueOfAStringIsAnArray {\n    $1(strs: Array<String>) {\n        return strs.max {\n            auto $1 = it.toIntOrNull()\n            numericValue?: it.length\n        }\n    }\n}" %}

---

## MergeStringAlternatively

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/MergeStringAlternatively.kt`

{% include code-tabs.html  kotlin="package string\n\nclass MergeStringAlternatively {\n    fun mergeAlternately(word1: String, word2: String): String {\n        val mergedString = StringBuilder()\n\n        for (i in 0 until maxOf(word1.length, word2.length)) {\n            if (i < word1.length)\n                mergedString.append(word1[i])\n            if (i < word2.length)\n                mergedString.append(word2[i])\n        }\n\n        return mergedString.toString()\n    }\n}"  java="class MergeStringAlternatively {\n    public static void $1(word1, word2) {\n        val mergedString = StringBuilder()\n\n        for (i in 0 until maxOf(word1.length, word2.length)) {\n            if (i < word1.length)\n                mergedString.append(word1[i])\n            if (i < word2.length)\n                mergedString.append(word2[i])\n        }\n\n        return mergedString.toString()\n    }\n}"  python="class MergeStringAlternatively {\n    def $1(word1: str, word2: str): str {\n        $1 = StringBuilder()\n\n        for (i in 0 until max(word1.length, word2.length)) {\n            if (i < word1.length)\n                mergedString.append(word1[i])\n            if (i < word2.length)\n                mergedString.append(word2[i])\n        }\n\n        return mergedString.toString()\n    }\n}"  rust="class MergeStringAlternatively {\n    fn $1(word1 -> String, word2 -> String) -> String {\n        val mergedString = StringBuilder()\n\n        for (i in 0 until max(word1.length, word2.length)) {\n            if (i < word1.length)\n                mergedString.append(word1[i])\n            if (i < word2.length)\n                mergedString.append(word2[i])\n        }\n\n        return mergedString.toString()\n    }\n}"  cpp="class MergeStringAlternatively {\n    $1(word1, word2) {\n        auto $1 = StringBuilder()\n\n        for (i in 0 until max(word1.length, word2.length)) {\n            if (i < word1.length)\n                mergedString.append(word1[i])\n            if (i < word2.length)\n                mergedString.append(word2[i])\n        }\n\n        return mergedString.toString()\n    }\n}" %}

---

## MinimumDeletionToMakeCharacterFrequenciesUnique

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/MinimumDeletionToMakeCharacterFrequenciesUnique.kt`

{% include code-tabs.html  kotlin="package string\n\nclass MinimumDeletionToMakeCharacterFrequenciesUnique {\n    fun minDeletions(s: String): Int {\n        val freq = IntArray(26)\n        for (ch in s) freq[ch - 'a']++\n\n        val seen = mutableSetOf<Int>()\n        var deletions = 0\n\n        for (f in freq) {\n            var currentFreq = f\n            while (currentFreq > 0 && !seen.add(currentFreq)) {\n                currentFreq--\n                deletions++\n            }\n        }\n\n        return deletions\n    }\n}"  java="class MinimumDeletionToMakeCharacterFrequenciesUnique {\n    public static void $1(s) {\n        val freq = IntArray(26)\n        for (ch in s) freq[ch - 'a']++\n\n        val seen = mutableSetOf<Int>()\n        var deletions = 0\n\n        for (f in freq) {\n            var currentFreq = f\n            while (currentFreq > 0 && !seen.add(currentFreq)) {\n                currentFreq--\n                deletions++\n            }\n        }\n\n        return deletions\n    }\n}"  python="class MinimumDeletionToMakeCharacterFrequenciesUnique {\n    def $1(s: str): int {\n        $1 = list[int](26)\n        for (ch in s) freq[ch - 'a']++\n\n        $1 = mutableSetOf<Int>()\n        $1 = 0\n\n        for (f in freq) {\n            $1 = f\n            while (currentFreq > 0 && !seen.add(currentFreq)) {\n                currentFreq--\n                deletions++\n            }\n        }\n\n        return deletions\n    }\n}"  rust="class MinimumDeletionToMakeCharacterFrequenciesUnique {\n    fn $1(s -> String) -> i32 {\n        val freq = Vec<i32>(26)\n        for (ch in s) freq[ch - 'a']++\n\n        val seen = mutableSetOf<Int>()\n        var deletions = 0\n\n        for (f in freq) {\n            var currentFreq = f\n            while (currentFreq > 0 && !seen.add(currentFreq)) {\n                currentFreq--\n                deletions++\n            }\n        }\n\n        return deletions\n    }\n}"  cpp="class MinimumDeletionToMakeCharacterFrequenciesUnique {\n    $1(s) {\n        auto $1 = vector<int>(26)\n        for (ch in s) freq[ch - 'a']++\n\n        auto $1 = mutableSetOf<Int>()\n        auto $1 = 0\n\n        for (f in freq) {\n            auto $1 = f\n            while (currentFreq > 0 && !seen.add(currentFreq)) {\n                currentFreq--\n                deletions++\n            }\n        }\n\n        return deletions\n    }\n}" %}

---

## ReverseVowelOfString

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ReverseVowelOfString.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ReverseVowelOfString {\n    fun reverseVowels(s: String): String {\n        val vowels = setOf('a', 'e', 'i', 'o', 'u')\n        val result = StringBuilder(s)\n\n        var (start, end) = Pair(0, s.lastIndex)\n\n        while (start < end) {\n            if (s[start] in vowels && s[end] in vowels) {\n                swap (result, start++, end--)\n            } else if (s[start].lowercaseChar() !in vowels)  {\n                start++\n            } else {\n                end--\n            }\n        }\n\n        return result.toString()\n    }\n\n    fun swap(s: StringBuilder, i: Int, j: Int) {\n        s[j] = s[i].also { s[i] = s[j]}\n    }\n}"  java="class ReverseVowelOfString {\n    public static void $1(s) {\n        val vowels = setOf('a', 'e', 'i', 'o', 'u')\n        val result = StringBuilder(s)\n\n        var (start, end) = Pair(0, s.lastIndex)\n\n        while (start < end) {\n            if (s[start] in vowels && s[end] in vowels) {\n                swap (result, start++, end--)\n            } else if (s[start].lowercaseChar() !in vowels)  {\n                start++\n            } else {\n                end--\n            }\n        }\n\n        return result.toString()\n    }\n\n    public static void $1(s: StringBuilder, i, j) {\n        s[j] = s[i].also { s[i] = s[j]}\n    }\n}"  python="class ReverseVowelOfString {\n    def $1(s: str): str {\n        $1 = setOf('a', 'e', 'i', 'o', 'u')\n        $1 = StringBuilder(s)\n\n        var (start, end) = Pair(0, s.lastIndex)\n\n        while (start < end) {\n            if (s[start] in vowels && s[end] in vowels) {\n                swap (result, start++, end--)\n            } else if (s[start].lowercaseChar() !in vowels)  {\n                start++\n            } else {\n                end--\n            }\n        }\n\n        return result.toString()\n    }\n\n    def $1(s: StringBuilder, i: int, j: int) {\n        s[j] = s[i].also { s[i] = s[j]}\n    }\n}"  rust="class ReverseVowelOfString {\n    fn $1(s -> String) -> String {\n        val vowels = setOf('a', 'e', 'i', 'o', 'u')\n        val result = StringBuilder(s)\n\n        var (start, end) = Pair(0, s.len() - 1)\n\n        while (start < end) {\n            if (s[start] in vowels && s[end] in vowels) {\n                swap (result, start++, end--)\n            } else if (s[start].lowercaseChar() !in vowels)  {\n                start++\n            } else {\n                end--\n            }\n        }\n\n        return result.toString()\n    }\n\n    fn $1(s: StringBuilder, i -> i32, j -> i32) {\n        s[j] = s[i].also { s[i] = s[j]}\n    }\n}"  cpp="class ReverseVowelOfString {\n    $1(s) {\n        auto $1 = setOf('a', 'e', 'i', 'o', 'u')\n        auto $1 = StringBuilder(s)\n\n        var (start, end) = Pair(0, s.size() - 1)\n\n        while (start < end) {\n            if (s[start] in vowels && s[end] in vowels) {\n                swap (result, start++, end--)\n            } else if (s[start].lowercaseChar() !in vowels)  {\n                start++\n            } else {\n                end--\n            }\n        }\n\n        return result.toString()\n    }\n\n    $1(s: StringBuilder, i, j) {\n        s[j] = s[i].also { s[i] = s[j]}\n    }\n}" %}

---

## ReverseWordsInString

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ReverseWordsInString.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ReverseWordsInString {\n    fun reverseWords(s: String): String {\n        val words = s.split(\" \").filter { it.isNotEmpty() }.toMutableList()\n\n        // Two pointers\n        var (start, end) = Pair(0, words.lastIndex)\n\n        while (start < end) {\n            words[end] = words[start].also { words[start] = words[end] }\n            start++\n            end--\n        }\n\n        return words.joinToString(separator = \" \").trim()\n\n    }\n\n    fun swap(nums: MutableList<String>, i: Int, j: Int) {\n        nums[j] = nums[i].also { nums[i] = nums[j] }\n    }\n}\n"  java="class ReverseWordsInString {\n    public static void $1(s) {\n        val words = s.split(\" \").filter { it.isNotEmpty() }.toMutableList()\n\n        // Two pointers\n        var (start, end) = Pair(0, words.lastIndex)\n\n        while (start < end) {\n            words[end] = words[start].also { words[start] = words[end] }\n            start++\n            end--\n        }\n\n        return words.joinToString(separator = \" \").trim()\n\n    }\n\n    public static void $1(nums: MutableList<String>, i, j) {\n        nums[j] = nums[i].also { nums[i] = nums[j] }\n    }\n}"  python="class ReverseWordsInString {\n    def $1(s: str): str {\n        $1 = s.split(\" \").filter { it.isNotEmpty() }.toMutableList()\n\n        // Two pointers\n        var (start, end) = Pair(0, words.lastIndex)\n\n        while (start < end) {\n            words[end] = words[start].also { words[start] = words[end] }\n            start++\n            end--\n        }\n\n        return words.joinToString(separator = \" \").trim()\n\n    }\n\n    def $1(nums: MutableList<String>, i: int, j: int) {\n        nums[j] = nums[i].also { nums[i] = nums[j] }\n    }\n}"  rust="class ReverseWordsInString {\n    fn $1(s -> String) -> String {\n        val words = s.split(\" \").filter { it.isNotEmpty() }.toMutableList()\n\n        // Two pointers\n        var (start, end) = Pair(0, words.len() - 1)\n\n        while (start < end) {\n            words[end] = words[start].also { words[start] = words[end] }\n            start++\n            end--\n        }\n\n        return words.joinToString(separator = \" \").trim()\n\n    }\n\n    fn $1(nums: MutableList<String>, i -> i32, j -> i32) {\n        nums[j] = nums[i].also { nums[i] = nums[j] }\n    }\n}"  cpp="class ReverseWordsInString {\n    $1(s) {\n        auto $1 = s.split(\" \").filter { it.isNotEmpty() }.toMutableList()\n\n        // Two pointers\n        var (start, end) = Pair(0, words.size() - 1)\n\n        while (start < end) {\n            words[end] = words[start].also { words[start] = words[end] }\n            start++\n            end--\n        }\n\n        return words.joinToString(separator = \" \").trim()\n\n    }\n\n    $1(nums: MutableList<String>, i, j) {\n        nums[j] = nums[i].also { nums[i] = nums[j] }\n    }\n}" %}

---

## StringCompression

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/StringCompression.kt`

{% include code-tabs.html  kotlin="package string\n\n// Data Compression Algorithm\n\nclass StringCompression {\n    fun compress(chars: CharArray): Int {\n        var count = 0\n        val sb = StringBuilder()\n\n        for (i in chars.indices) {\n            count++\n\n            if (i  == chars.lastIndex || chars[i] != chars[i+1]) {\n                sb.append(chars[i])\n\n                if (count > 1) {\n                    sb.append(count)\n                }\n                count = 0\n            }\n        }\n\n        for (i in 0 until sb.length) chars[i] = sb.get(i)\n\n        return sb.length\n    }\n\n    fun string_compression_inline(chars: CharArray): Int {\n        var partitionLength = 0\n        var i = 0\n\n        while ( i < chars.size) {\n            val currentChar = chars[i]\n            var count = 0\n            while ( i < chars.size && chars[i] == currentChar) {\n                count++\n                i++\n            }\n            chars[partitionLength++] = currentChar\n\n            if (count > 1) {\n                count.toString().forEach { ch -> chars[partitionLength++] = ch }\n            }\n        }\n\n        return partitionLength\n\n    }\n}"  java="// Data Compression Algorithm\n\nclass StringCompression {\n    public static void $1(chars: CharArray) {\n        var count = 0\n        val sb = StringBuilder()\n\n        for (i in chars.indices) {\n            count++\n\n            if (i  == chars.lastIndex || chars[i] != chars[i+1]) {\n                sb.append(chars[i])\n\n                if (count > 1) {\n                    sb.append(count)\n                }\n                count = 0\n            }\n        }\n\n        for (i in 0 until sb.length) chars[i] = sb.get(i)\n\n        return sb.length\n    }\n\n    public static void $1(chars: CharArray) {\n        var partitionLength = 0\n        var i = 0\n\n        while ( i < chars.size) {\n            val currentChar = chars[i]\n            var count = 0\n            while ( i < chars.size && chars[i] == currentChar) {\n                count++\n                i++\n            }\n            chars[partitionLength++] = currentChar\n\n            if (count > 1) {\n                count.toString().forEach { ch -> chars[partitionLength++] = ch }\n            }\n        }\n\n        return partitionLength\n\n    }\n}"  python="// Data Compression Algorithm\n\nclass StringCompression {\n    def $1(chars: CharArray): int {\n        $1 = 0\n        $1 = StringBuilder()\n\n        for (i in chars.indices) {\n            count++\n\n            if (i  == chars.lastIndex || chars[i] != chars[i+1]) {\n                sb.append(chars[i])\n\n                if (count > 1) {\n                    sb.append(count)\n                }\n                count = 0\n            }\n        }\n\n        for (i in 0 until sb.length) chars[i] = sb.get(i)\n\n        return sb.length\n    }\n\n    def $1(chars: CharArray): int {\n        $1 = 0\n        $1 = 0\n\n        while ( i < chars.size) {\n            $1 = chars[i]\n            $1 = 0\n            while ( i < chars.size && chars[i] == currentChar) {\n                count++\n                i++\n            }\n            chars[partitionLength++] = currentChar\n\n            if (count > 1) {\n                count.toString().forEach { ch -> chars[partitionLength++] = ch }\n            }\n        }\n\n        return partitionLength\n\n    }\n}"  rust="// Data Compression Algorithm\n\nclass StringCompression {\n    fn $1(chars: CharArray) -> i32 {\n        var count = 0\n        val sb = StringBuilder()\n\n        for (i in chars.indices) {\n            count++\n\n            if (i  == chars.len() - 1 || chars[i] != chars[i+1]) {\n                sb.append(chars[i])\n\n                if (count > 1) {\n                    sb.append(count)\n                }\n                count = 0\n            }\n        }\n\n        for (i in 0 until sb.length) chars[i] = sb.get(i)\n\n        return sb.length\n    }\n\n    fn $1(chars: CharArray) -> i32 {\n        var partitionLength = 0\n        var i = 0\n\n        while ( i < chars.size) {\n            val currentChar = chars[i]\n            var count = 0\n            while ( i < chars.size && chars[i] == currentChar) {\n                count++\n                i++\n            }\n            chars[partitionLength++] = currentChar\n\n            if (count > 1) {\n                count.toString().forEach { ch -> chars[partitionLength++] = ch }\n            }\n        }\n\n        return partitionLength\n\n    }\n}"  cpp="// Data Compression Algorithm\n\nclass StringCompression {\n    $1(chars: CharArray) {\n        auto $1 = 0\n        auto $1 = StringBuilder()\n\n        for (i in chars.indices) {\n            count++\n\n            if (i  == chars.size() - 1 || chars[i] != chars[i+1]) {\n                sb.append(chars[i])\n\n                if (count > 1) {\n                    sb.append(count)\n                }\n                count = 0\n            }\n        }\n\n        for (i in 0 until sb.length) chars[i] = sb.get(i)\n\n        return sb.length\n    }\n\n    $1(chars: CharArray) {\n        auto $1 = 0\n        auto $1 = 0\n\n        while ( i < chars.size) {\n            auto $1 = chars[i]\n            auto $1 = 0\n            while ( i < chars.size && chars[i] == currentChar) {\n                count++\n                i++\n            }\n            chars[partitionLength++] = currentChar\n\n            if (count > 1) {\n                count.toString().forEach { ch -> chars[partitionLength++] = ch }\n            }\n        }\n\n        return partitionLength\n\n    }\n}" %}

---

## StringCompression_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/StringCompression_II.kt`

{% include code-tabs.html  kotlin="package string\n\nclass StringCompression_II {\n    fun compressedString(word: String): String {\n        val compressed = StringBuilder()\n\n        var i = 0\n\n        while (i < word.length) {\n            val ch = word[i]\n            var count = 0\n\n            while (i < word.length && word[i] == ch && count < 9 ) {\n                count++\n                i++\n            }\n\n            compressed.append(count).append(ch)\n        }\n\n        return compressed.toString()\n    }\n}"  java="class StringCompression_II {\n    public static void $1(word) {\n        val compressed = StringBuilder()\n\n        var i = 0\n\n        while (i < word.length) {\n            val ch = word[i]\n            var count = 0\n\n            while (i < word.length && word[i] == ch && count < 9 ) {\n                count++\n                i++\n            }\n\n            compressed.append(count).append(ch)\n        }\n\n        return compressed.toString()\n    }\n}"  python="class StringCompression_II {\n    def $1(word: str): str {\n        $1 = StringBuilder()\n\n        $1 = 0\n\n        while (i < word.length) {\n            $1 = word[i]\n            $1 = 0\n\n            while (i < word.length && word[i] == ch && count < 9 ) {\n                count++\n                i++\n            }\n\n            compressed.append(count).append(ch)\n        }\n\n        return compressed.toString()\n    }\n}"  rust="class StringCompression_II {\n    fn $1(word -> String) -> String {\n        val compressed = StringBuilder()\n\n        var i = 0\n\n        while (i < word.length) {\n            val ch = word[i]\n            var count = 0\n\n            while (i < word.length && word[i] == ch && count < 9 ) {\n                count++\n                i++\n            }\n\n            compressed.append(count).append(ch)\n        }\n\n        return compressed.toString()\n    }\n}"  cpp="class StringCompression_II {\n    $1(word) {\n        auto $1 = StringBuilder()\n\n        auto $1 = 0\n\n        while (i < word.length) {\n            auto $1 = word[i]\n            auto $1 = 0\n\n            while (i < word.length && word[i] == ch && count < 9 ) {\n                count++\n                i++\n            }\n\n            compressed.append(count).append(ch)\n        }\n\n        return compressed.toString()\n    }\n}" %}

---

## ValidAnagram

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ValidAnagram.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ValidAnagram {\n    fun isAnagram(s: String, t: String): Boolean {\n        if (s.length != t.length) return false\n\n        val charCount = IntArray(26)\n\n        for (i in s.indices) {\n            charCount[s[i] - 'a']++\n            charCount[t[i] - 'a']--\n        }\n\n        // Check if all counts are zero\n        for (count in charCount) {\n            if (count != 0) {\n                return false\n            }\n        }\n\n        return true\n    }\n}\n"  java="class ValidAnagram {\n    public static void $1(s, t) {\n        if (s.length != t.length) return false\n\n        val charCount = IntArray(26)\n\n        for (i in s.indices) {\n            charCount[s[i] - 'a']++\n            charCount[t[i] - 'a']--\n        }\n\n        // Check if all counts are zero\n        for (count in charCount) {\n            if (count != 0) {\n                return false\n            }\n        }\n\n        return true\n    }\n}"  python="class ValidAnagram {\n    def $1(s: str, t: str): bool {\n        if (s.length != t.length) return False\n\n        $1 = list[int](26)\n\n        for (i in s.indices) {\n            charCount[s[i] - 'a']++\n            charCount[t[i] - 'a']--\n        }\n\n        // Check if all counts are zero\n        for (count in charCount) {\n            if (count != 0) {\n                return False\n            }\n        }\n\n        return True\n    }\n}"  rust="class ValidAnagram {\n    fn $1(s -> String, t -> String) -> bool {\n        if (s.length != t.length) return false\n\n        val charCount = Vec<i32>(26)\n\n        for (i in s.indices) {\n            charCount[s[i] - 'a']++\n            charCount[t[i] - 'a']--\n        }\n\n        // Check if all counts are zero\n        for (count in charCount) {\n            if (count != 0) {\n                return false\n            }\n        }\n\n        return true\n    }\n}"  cpp="class ValidAnagram {\n    $1(s, t) {\n        if (s.length != t.length) return false\n\n        auto $1 = vector<int>(26)\n\n        for (i in s.indices) {\n            charCount[s[i] - 'a']++\n            charCount[t[i] - 'a']--\n        }\n\n        // Check if all counts are zero\n        for (count in charCount) {\n            if (count != 0) {\n                return false\n            }\n        }\n\n        return true\n    }\n}" %}

---

## ValidNumber

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ValidNumber.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ValidNumber {\n    fun isNumber(s: String): Boolean {\n        // Initialize variables\n        var (hasNum, hasDot, hasE, hasDigitsAfterE) = listOf(false, false, false, false)\n\n        val str = s.trim() // Remove leading/trailing spaces\n        for (i in str.indices) {\n            val c = str[i]\n\n            when {\n                c in '0'..'9' -> {\n                    hasNum = true\n                    if (hasE) hasDigitsAfterE = true // Ensure digits appear after 'e'\n                }\n                c == '.' -> {\n                    if (hasDot || hasE) return false // Can't have multiple dots or dots after 'e'\n                    hasDot = true\n                }\n                c == 'e' || c == 'E' -> {\n                    if (hasE || !hasNum) return false // Can't have multiple 'e' or 'e' without preceding number\n                    hasE = true\n                    hasDigitsAfterE = false // Reset this to ensure we validate digits after 'e'\n                }\n                c == '+' || c == '-' -> {\n                    if (i != 0 && str[i - 1] != 'e' && str[i - 1] != 'E') return false // Sign only valid at start or after 'e'\n                }\n                else -> return false // Invalid character\n            }\n        }\n        // Valid only if we have digits and valid 'e' part\n        return hasNum && (!hasE || hasDigitsAfterE)\n    }\n}"  java="class ValidNumber {\n    public static void $1(s) {\n        // Initialize variables\n        var (hasNum, hasDot, hasE, hasDigitsAfterE) = listOf(false, false, false, false)\n\n        val str = s.trim() // Remove leading/trailing spaces\n        for (i in str.indices) {\n            val c = str[i]\n\n            when {\n                c in '0'..'9' -> {\n                    hasNum = true\n                    if (hasE) hasDigitsAfterE = true // Ensure digits appear after 'e'\n                }\n                c == '.' -> {\n                    if (hasDot || hasE) return false // Can't have multiple dots or dots after 'e'\n                    hasDot = true\n                }\n                c == 'e' || c == 'E' -> {\n                    if (hasE || !hasNum) return false // Can't have multiple 'e' or 'e' without preceding number\n                    hasE = true\n                    hasDigitsAfterE = false // Reset this to ensure we validate digits after 'e'\n                }\n                c == '+' || c == '-' -> {\n                    if (i != 0 && str[i - 1] != 'e' && str[i - 1] != 'E') return false // Sign only valid at start or after 'e'\n                }\n                else -> return false // Invalid character\n            }\n        }\n        // Valid only if we have digits and valid 'e' part\n        return hasNum && (!hasE || hasDigitsAfterE)\n    }\n}"  python="class ValidNumber {\n    def $1(s: str): bool {\n        // Initialize variables\n        var (hasNum, hasDot, hasE, hasDigitsAfterE) = listOf(False, False, False, False)\n\n        $1 = s.trim() // Remove leading/trailing spaces\n        for (i in str.indices) {\n            $1 = str[i]\n\n            when {\n                c in '0'..'9' -> {\n                    hasNum = True\n                    if (hasE) hasDigitsAfterE = True // Ensure digits appear after 'e'\n                }\n                c == '.' -> {\n                    if (hasDot || hasE) return False // Can't have multiple dots or dots after 'e'\n                    hasDot = True\n                }\n                c == 'e' || c == 'E' -> {\n                    if (hasE || !hasNum) return False // Can't have multiple 'e' or 'e' without preceding number\n                    hasE = True\n                    hasDigitsAfterE = False // Reset this to ensure we validate digits after 'e'\n                }\n                c == '+' || c == '-' -> {\n                    if (i != 0 && str[i - 1] != 'e' && str[i - 1] != 'E') return False // Sign only valid at start or after 'e'\n                }\n                else -> return False // Invalid character\n            }\n        }\n        // Valid only if we have digits and valid 'e' part\n        return hasNum && (!hasE || hasDigitsAfterE)\n    }\n}"  rust="class ValidNumber {\n    fn $1(s -> String) -> bool {\n        // Initialize variables\n        var (hasNum, hasDot, hasE, hasDigitsAfterE) = listOf(false, false, false, false)\n\n        val str = s.trim() // Remove leading/trailing spaces\n        for (i in str.indices) {\n            val c = str[i]\n\n            when {\n                c in '0'..'9' -> {\n                    hasNum = true\n                    if (hasE) hasDigitsAfterE = true // Ensure digits appear after 'e'\n                }\n                c == '.' -> {\n                    if (hasDot || hasE) return false // Can't have multiple dots or dots after 'e'\n                    hasDot = true\n                }\n                c == 'e' || c == 'E' -> {\n                    if (hasE || !hasNum) return false // Can't have multiple 'e' or 'e' without preceding number\n                    hasE = true\n                    hasDigitsAfterE = false // Reset this to ensure we validate digits after 'e'\n                }\n                c == '+' || c == '-' -> {\n                    if (i != 0 && str[i - 1] != 'e' && str[i - 1] != 'E') return false // Sign only valid at start or after 'e'\n                }\n                else -> return false // Invalid character\n            }\n        }\n        // Valid only if we have digits and valid 'e' part\n        return hasNum && (!hasE || hasDigitsAfterE)\n    }\n}"  cpp="class ValidNumber {\n    $1(s) {\n        // Initialize variables\n        var (hasNum, hasDot, hasE, hasDigitsAfterE) = listOf(false, false, false, false)\n\n        auto $1 = s.trim() // Remove leading/trailing spaces\n        for (i in str.indices) {\n            auto $1 = str[i]\n\n            when {\n                c in '0'..'9' -> {\n                    hasNum = true\n                    if (hasE) hasDigitsAfterE = true // Ensure digits appear after 'e'\n                }\n                c == '.' -> {\n                    if (hasDot || hasE) return false // Can't have multiple dots or dots after 'e'\n                    hasDot = true\n                }\n                c == 'e' || c == 'E' -> {\n                    if (hasE || !hasNum) return false // Can't have multiple 'e' or 'e' without preceding number\n                    hasE = true\n                    hasDigitsAfterE = false // Reset this to ensure we validate digits after 'e'\n                }\n                c == '+' || c == '-' -> {\n                    if (i != 0 && str[i - 1] != 'e' && str[i - 1] != 'E') return false // Sign only valid at start or after 'e'\n                }\n                else -> return false // Invalid character\n            }\n        }\n        // Valid only if we have digits and valid 'e' part\n        return hasNum && (!hasE || hasDigitsAfterE)\n    }\n}" %}

---

## ValidPalindrome

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ValidPalindrome.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ValidPalindrome {\n    fun isPalindrome(s: String): Boolean {\n        var left = 0\n        var right = s.lastIndex\n        val isAlpha = {ch: Char -> Character.isAlphabetic(ch.code) || Character.isDigit(ch.code)}\n\n        while (left < right) {\n            when {\n                isAlpha(s[left]) && isAlpha(s[right])\n                        && s[left].lowercaseChar() != s[right].lowercaseChar() -> return false\n                isAlpha(s[left]) && !isAlpha(s[right]) ->  right --\n                isAlpha(s[right]) && !isAlpha(s[left]) -> left++\n                else -> {\n                    left++\n                    right--\n                }\n\n            }\n        }\n\n        return true\n    }\n}"  java="class ValidPalindrome {\n    public static void $1(s) {\n        var left = 0\n        var right = s.lastIndex\n        val isAlpha = {ch: Char -> Character.isAlphabetic(ch.code) || Character.isDigit(ch.code)}\n\n        while (left < right) {\n            when {\n                isAlpha(s[left]) && isAlpha(s[right])\n                        && s[left].lowercaseChar() != s[right].lowercaseChar() -> return false\n                isAlpha(s[left]) && !isAlpha(s[right]) ->  right --\n                isAlpha(s[right]) && !isAlpha(s[left]) -> left++\n                else -> {\n                    left++\n                    right--\n                }\n\n            }\n        }\n\n        return true\n    }\n}"  python="class ValidPalindrome {\n    def $1(s: str): bool {\n        $1 = 0\n        $1 = s.lastIndex\n        $1 = {ch: Char -> Character.isAlphabetic(ch.code) || Character.isDigit(ch.code)}\n\n        while (left < right) {\n            when {\n                isAlpha(s[left]) && isAlpha(s[right])\n                        && s[left].lowercaseChar() != s[right].lowercaseChar() -> return False\n                isAlpha(s[left]) && !isAlpha(s[right]) ->  right --\n                isAlpha(s[right]) && !isAlpha(s[left]) -> left++\n                else -> {\n                    left++\n                    right--\n                }\n\n            }\n        }\n\n        return True\n    }\n}"  rust="class ValidPalindrome {\n    fn $1(s -> String) -> bool {\n        var left = 0\n        var right = s.len() - 1\n        val isAlpha = {ch: Char -> Character.isAlphabetic(ch.code) || Character.isDigit(ch.code)}\n\n        while (left < right) {\n            when {\n                isAlpha(s[left]) && isAlpha(s[right])\n                        && s[left].lowercaseChar() != s[right].lowercaseChar() -> return false\n                isAlpha(s[left]) && !isAlpha(s[right]) ->  right --\n                isAlpha(s[right]) && !isAlpha(s[left]) -> left++\n                else -> {\n                    left++\n                    right--\n                }\n\n            }\n        }\n\n        return true\n    }\n}"  cpp="class ValidPalindrome {\n    $1(s) {\n        auto $1 = 0\n        auto $1 = s.size() - 1\n        auto $1 = {ch: Char -> Character.isAlphabetic(ch.code) || Character.isDigit(ch.code)}\n\n        while (left < right) {\n            when {\n                isAlpha(s[left]) && isAlpha(s[right])\n                        && s[left].lowercaseChar() != s[right].lowercaseChar() -> return false\n                isAlpha(s[left]) && !isAlpha(s[right]) ->  right --\n                isAlpha(s[right]) && !isAlpha(s[left]) -> left++\n                else -> {\n                    left++\n                    right--\n                }\n\n            }\n        }\n\n        return true\n    }\n}" %}

---

## ValidPalindrome_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ValidPalindrome_II.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ValidPalindrome_II {\n    fun validPalindrome(s: String): Boolean {\n        var left = 0\n        var right = s.lastIndex\n\n        while (left < right) {\n            if (s[left] != s[right]) {\n                return isPalindrome(s, left, right - 1) || isPalindrome(s, left + 1, right)\n            }\n\n            left++\n            right--\n        }\n\n        return true\n    }\n\n    fun isPalindrome(s: String, i: Int, j: Int): Boolean {\n        var left = i\n        var right = j\n\n        while (left < right) {\n            if (s[left++] != s[right--]) {\n                return false\n            }\n        }\n\n        return true\n    }\n}"  java="class ValidPalindrome_II {\n    public static void $1(s) {\n        var left = 0\n        var right = s.lastIndex\n\n        while (left < right) {\n            if (s[left] != s[right]) {\n                return isPalindrome(s, left, right - 1) || isPalindrome(s, left + 1, right)\n            }\n\n            left++\n            right--\n        }\n\n        return true\n    }\n\n    public static void $1(s, i, j) {\n        var left = i\n        var right = j\n\n        while (left < right) {\n            if (s[left++] != s[right--]) {\n                return false\n            }\n        }\n\n        return true\n    }\n}"  python="class ValidPalindrome_II {\n    def $1(s: str): bool {\n        $1 = 0\n        $1 = s.lastIndex\n\n        while (left < right) {\n            if (s[left] != s[right]) {\n                return isPalindrome(s, left, right - 1) || isPalindrome(s, left + 1, right)\n            }\n\n            left++\n            right--\n        }\n\n        return True\n    }\n\n    def $1(s: str, i: int, j: int): bool {\n        $1 = i\n        $1 = j\n\n        while (left < right) {\n            if (s[left++] != s[right--]) {\n                return False\n            }\n        }\n\n        return True\n    }\n}"  rust="class ValidPalindrome_II {\n    fn $1(s -> String) -> bool {\n        var left = 0\n        var right = s.len() - 1\n\n        while (left < right) {\n            if (s[left] != s[right]) {\n                return isPalindrome(s, left, right - 1) || isPalindrome(s, left + 1, right)\n            }\n\n            left++\n            right--\n        }\n\n        return true\n    }\n\n    fn $1(s -> String, i -> i32, j -> i32) -> bool {\n        var left = i\n        var right = j\n\n        while (left < right) {\n            if (s[left++] != s[right--]) {\n                return false\n            }\n        }\n\n        return true\n    }\n}"  cpp="class ValidPalindrome_II {\n    $1(s) {\n        auto $1 = 0\n        auto $1 = s.size() - 1\n\n        while (left < right) {\n            if (s[left] != s[right]) {\n                return isPalindrome(s, left, right - 1) || isPalindrome(s, left + 1, right)\n            }\n\n            left++\n            right--\n        }\n\n        return true\n    }\n\n    $1(s, i, j) {\n        auto $1 = i\n        auto $1 = j\n\n        while (left < right) {\n            if (s[left++] != s[right--]) {\n                return false\n            }\n        }\n\n        return true\n    }\n}" %}

---

## ValidWordAbbreviation

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ValidWordAbbreviation.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ValidWordAbbreviation {\n    fun validWordAbbreviation(word: String, abbr: String): Boolean {\n        var i = 0\n        var j = 0\n\n        while (i < word.length && j < abbr.length) {\n            if (abbr[j].isDigit()) {\n                if (abbr[j] == '0') return false  // Leading zero check\n\n                var count = 0\n\n                while (j < abbr.length && abbr[j].isDigit()) {\n                    count = count * 10 + (abbr[j++] - '0')\n                }\n\n                i += count\n            } else {\n                if (word[i++] != abbr[j++]) return false\n            }\n        }\n\n        return i == word.length && j == abbr.length\n    }\n}\n"  java="class ValidWordAbbreviation {\n    public static void $1(word, abbr) {\n        var i = 0\n        var j = 0\n\n        while (i < word.length && j < abbr.length) {\n            if (abbr[j].isDigit()) {\n                if (abbr[j] == '0') return false  // Leading zero check\n\n                var count = 0\n\n                while (j < abbr.length && abbr[j].isDigit()) {\n                    count = count * 10 + (abbr[j++] - '0')\n                }\n\n                i += count\n            } else {\n                if (word[i++] != abbr[j++]) return false\n            }\n        }\n\n        return i == word.length && j == abbr.length\n    }\n}"  python="class ValidWordAbbreviation {\n    def $1(word: str, abbr: str): bool {\n        $1 = 0\n        $1 = 0\n\n        while (i < word.length && j < abbr.length) {\n            if (abbr[j].isDigit()) {\n                if (abbr[j] == '0') return False  // Leading zero check\n\n                $1 = 0\n\n                while (j < abbr.length && abbr[j].isDigit()) {\n                    count = count * 10 + (abbr[j++] - '0')\n                }\n\n                i += count\n            } else {\n                if (word[i++] != abbr[j++]) return False\n            }\n        }\n\n        return i == word.length && j == abbr.length\n    }\n}"  rust="class ValidWordAbbreviation {\n    fn $1(word -> String, abbr -> String) -> bool {\n        var i = 0\n        var j = 0\n\n        while (i < word.length && j < abbr.length) {\n            if (abbr[j].isDigit()) {\n                if (abbr[j] == '0') return false  // Leading zero check\n\n                var count = 0\n\n                while (j < abbr.length && abbr[j].isDigit()) {\n                    count = count * 10 + (abbr[j++] - '0')\n                }\n\n                i += count\n            } else {\n                if (word[i++] != abbr[j++]) return false\n            }\n        }\n\n        return i == word.length && j == abbr.length\n    }\n}"  cpp="class ValidWordAbbreviation {\n    $1(word, abbr) {\n        auto $1 = 0\n        auto $1 = 0\n\n        while (i < word.length && j < abbr.length) {\n            if (abbr[j].isDigit()) {\n                if (abbr[j] == '0') return false  // Leading zero check\n\n                auto $1 = 0\n\n                while (j < abbr.length && abbr[j].isDigit()) {\n                    count = count * 10 + (abbr[j++] - '0')\n                }\n\n                i += count\n            } else {\n                if (word[i++] != abbr[j++]) return false\n            }\n        }\n\n        return i == word.length && j == abbr.length\n    }\n}" %}

---

## ValidateIPAddress

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/ValidateIPAddress.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ValidateIPAddress {\n    fun validIPAddress(queryIP: String): String {\n        return when {\n            isValidIPv4(queryIP) -> \"IPv4\"\n            isValidIPv6(queryIP) -> \"IPv6\"\n            else -> \"Neither\"\n        }\n    }\n\n    private fun isValidIPv4(ip: String): Boolean {\n        if (ip.startsWith('.') || ip.endsWith('.')) return false\n\n        val segments = ip.split('.')\n\n        if (segments.size != 4) return false\n\n        return segments.all { segment ->\n            segment.isNotEmpty() &&\n                    segment.all { it.isDigit() } &&\n                    (segment.length == 1 || segment[0] != '0') &&\n                    segment.length <= 3 &&\n                    try {\n                        segment.toInt() in 0..255\n                    } catch (e: NumberFormatException) {\n                        false\n                    }\n        }\n    }\n\n    private fun isValidIPv6(ip: String): Boolean {\n        if (ip.startsWith(':') || ip.endsWith(':')) return false\n\n        val segments = ip.split(':')\n\n        if (segments.size != 8) return false\n\n        return segments.all { segment ->\n            segment.length in 1..4 &&\n                    segment.all { char ->\n                        char.isDigit() || char.lowercaseChar() in 'a'..'f'\n                    }\n        }\n    }\n}\n"  java="class ValidateIPAddress {\n    public static void $1(queryIP) {\n        return when {\n            isValidIPv4(queryIP) -> \"IPv4\"\n            isValidIPv6(queryIP) -> \"IPv6\"\n            else -> \"Neither\"\n        }\n    }\n\n    private public static void $1(ip) {\n        if (ip.startsWith('.') || ip.endsWith('.')) return false\n\n        val segments = ip.split('.')\n\n        if (segments.size != 4) return false\n\n        return segments.all { segment ->\n            segment.isNotEmpty() &&\n                    segment.all { it.isDigit() } &&\n                    (segment.length == 1 || segment[0] != '0') &&\n                    segment.length <= 3 &&\n                    try {\n                        segment.toInt() in 0..255\n                    } catch (e: NumberFormatException) {\n                        false\n                    }\n        }\n    }\n\n    private public static void $1(ip) {\n        if (ip.startsWith(':') || ip.endsWith(':')) return false\n\n        val segments = ip.split(':')\n\n        if (segments.size != 8) return false\n\n        return segments.all { segment ->\n            segment.length in 1..4 &&\n                    segment.all { char ->\n                        char.isDigit() || char.lowercaseChar() in 'a'..'f'\n                    }\n        }\n    }\n}"  python="class ValidateIPAddress {\n    def $1(queryIP: str): str {\n        return when {\n            isValidIPv4(queryIP) -> \"IPv4\"\n            isValidIPv6(queryIP) -> \"IPv6\"\n            else -> \"Neither\"\n        }\n    }\n\n    private def $1(ip: str): bool {\n        if (ip.startsWith('.') || ip.endsWith('.')) return False\n\n        $1 = ip.split('.')\n\n        if (segments.size != 4) return False\n\n        return segments.all { segment ->\n            segment.isNotEmpty() &&\n                    segment.all { it.isDigit() } &&\n                    (segment.length == 1 || segment[0] != '0') &&\n                    segment.length <= 3 &&\n                    try {\n                        segment.toInt() in 0..255\n                    } catch (e: NumberFormatException) {\n                        False\n                    }\n        }\n    }\n\n    private def $1(ip: str): bool {\n        if (ip.startsWith(':') || ip.endsWith(':')) return False\n\n        $1 = ip.split(':')\n\n        if (segments.size != 8) return False\n\n        return segments.all { segment ->\n            segment.length in 1..4 &&\n                    segment.all { char ->\n                        char.isDigit() || char.lowercaseChar() in 'a'..'f'\n                    }\n        }\n    }\n}"  rust="class ValidateIPAddress {\n    fn $1(queryIP -> String) -> String {\n        return when {\n            isValidIPv4(queryIP) -> \"IPv4\"\n            isValidIPv6(queryIP) -> \"IPv6\"\n            else -> \"Neither\"\n        }\n    }\n\n    private fn $1(ip -> String) -> bool {\n        if (ip.startsWith('.') || ip.endsWith('.')) return false\n\n        val segments = ip.split('.')\n\n        if (segments.size != 4) return false\n\n        return segments.all { segment ->\n            segment.isNotEmpty() &&\n                    segment.all { it.isDigit() } &&\n                    (segment.length == 1 || segment[0] != '0') &&\n                    segment.length <= 3 &&\n                    try {\n                        segment.toInt() in 0..255\n                    } catch (e: NumberFormatException) {\n                        false\n                    }\n        }\n    }\n\n    private fn $1(ip -> String) -> bool {\n        if (ip.startsWith(':') || ip.endsWith(':')) return false\n\n        val segments = ip.split(':')\n\n        if (segments.size != 8) return false\n\n        return segments.all { segment ->\n            segment.length in 1..4 &&\n                    segment.all { char ->\n                        char.isDigit() || char.lowercaseChar() in 'a'..'f'\n                    }\n        }\n    }\n}"  cpp="class ValidateIPAddress {\n    $1(queryIP) {\n        return when {\n            isValidIPv4(queryIP) -> \"IPv4\"\n            isValidIPv6(queryIP) -> \"IPv6\"\n            else -> \"Neither\"\n        }\n    }\n\n    private $1(ip) {\n        if (ip.startsWith('.') || ip.endsWith('.')) return false\n\n        auto $1 = ip.split('.')\n\n        if (segments.size != 4) return false\n\n        return segments.all { segment ->\n            segment.isNotEmpty() &&\n                    segment.all { it.isDigit() } &&\n                    (segment.length == 1 || segment[0] != '0') &&\n                    segment.length <= 3 &&\n                    try {\n                        segment.toInt() in 0..255\n                    } catch (e: NumberFormatException) {\n                        false\n                    }\n        }\n    }\n\n    private $1(ip) {\n        if (ip.startsWith(':') || ip.endsWith(':')) return false\n\n        auto $1 = ip.split(':')\n\n        if (segments.size != 8) return false\n\n        return segments.all { segment ->\n            segment.length in 1..4 &&\n                    segment.all { char ->\n                        char.isDigit() || char.lowercaseChar() in 'a'..'f'\n                    }\n        }\n    }\n}" %}

---

## FindTheIndexofTheFirstOccurrenceIna String

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/pattern_matching/FindTheIndexofTheFirstOccurrenceIna String.kt`

{% include code-tabs.html  kotlin="package string.pattern_matching\n\nimport oracle.net.aso.m\n\nclass `FindTheIndexofTheFirstOccurrenceIna String` {\n    fun strStr(haystack: String, needle: String): Int {\n        // If the needle is empty, return 0\n        if (needle.isEmpty()) return 0\n\n        // Use 'to' to assign both m and n in one line\n        val (m, n) = needle.length to haystack.length\n\n        // Step 1: Build the LPS array for the needle\n        val lps = buildLPS(needle)\n        println(needle)\n        println(lps.contentToString())\n        // Step 2: Search for the needle in the haystack using KMP algorithm\n        var (i, j) = 0 to 0  // Pairing to initialize indices for haystack and needle\n\n        while (i < n) {\n            when {\n                haystack[i] == needle[j] -> {  // Characters match\n                    i++\n                    j++\n                }\n                j > 0 -> j = lps[j - 1]  // Mismatch after some matches, use LPS array to skip ahead\n                else -> i++  // No matches yet, move to the next character in the haystack\n            }\n\n            // If we've matched the entire needle, return the start index\n            if (j == m) return i - j\n        }\n\n        return -1  // If no match found\n    }\n\n    // Function to build the LPS (Longest Prefix Suffix) array for the needle\n    fun buildLPS(needle: String): IntArray {\n        val lps = IntArray(needle.length)\n        var (i, j) = 0 to 1\n\n        while (j < needle.length) {\n            when {\n                needle[j] == needle[i] -> lps[j++] = ++i\n                i != 0 -> i = lps[i - 1]\n                else -> lps[j++] = 0\n            }\n        }\n        return lps\n    }\n}\n// lps =  [0 0 0 0 0 0 0 0 0]\n        // a b c x y z c b a\n        //   ^             ^\n        //   i             1"  java="class `FindTheIndexofTheFirstOccurrenceIna String` {\n    public static void $1(haystack, needle) {\n        // If the needle is empty, return 0\n        if (needle.isEmpty()) return 0\n\n        // Use 'to' to assign both m and n in one line\n        val (m, n) = needle.length to haystack.length\n\n        // Step 1: Build the LPS array for the needle\n        val lps = buildLPS(needle)\n        println(needle)\n        println(lps.contentToString())\n        // Step 2: Search for the needle in the haystack using KMP algorithm\n        var (i, j) = 0 to 0  // Pairing to initialize indices for haystack and needle\n\n        while (i < n) {\n            when {\n                haystack[i] == needle[j] -> {  // Characters match\n                    i++\n                    j++\n                }\n                j > 0 -> j = lps[j - 1]  // Mismatch after some matches, use LPS array to skip ahead\n                else -> i++  // No matches yet, move to the next character in the haystack\n            }\n\n            // If we've matched the entire needle, return the start index\n            if (j == m) return i - j\n        }\n\n        return -1  // If no match found\n    }\n\n    // Function to build the LPS (Longest Prefix Suffix) array for the needle\n    public static void $1(needle): IntArray {\n        val lps = IntArray(needle.length)\n        var (i, j) = 0 to 1\n\n        while (j < needle.length) {\n            when {\n                needle[j] == needle[i] -> lps[j++] = ++i\n                i != 0 -> i = lps[i - 1]\n                else -> lps[j++] = 0\n            }\n        }\n        return lps\n    }\n}\n// lps =  [0 0 0 0 0 0 0 0 0]\n        // a b c x y z c b a\n        //   ^             ^\n        //   i             1"  python="class `FindTheIndexofTheFirstOccurrenceIna String` {\n    def $1(haystack: str, needle: str): int {\n        // If the needle is empty, return 0\n        if (needle.isEmpty()) return 0\n\n        // Use 'to' to assign both m and n in one line\n        ($1, $2) = needle.length to haystack.length\n\n        // Step 1: Build the LPS array for the needle\n        $1 = buildLPS(needle)\n        print(needle)\n        print(lps.contentToString())\n        // Step 2: Search for the needle in the haystack using KMP algorithm\n        var (i, j) = 0 to 0  // Pairing to initialize indices for haystack and needle\n\n        while (i < n) {\n            when {\n                haystack[i] == needle[j] -> {  // Characters match\n                    i++\n                    j++\n                }\n                j > 0 -> j = lps[j - 1]  // Mismatch after some matches, use LPS array to skip ahead\n                else -> i++  // No matches yet, move to the next character in the haystack\n            }\n\n            // If we've matched the entire needle, return the start index\n            if (j == m) return i - j\n        }\n\n        return -1  // If no match found\n    }\n\n    // Function to build the LPS (Longest Prefix Suffix) array for the needle\n    def $1(needle: str): list[int] {\n        $1 = list[int](needle.length)\n        var (i, j) = 0 to 1\n\n        while (j < needle.length) {\n            when {\n                needle[j] == needle[i] -> lps[j++] = ++i\n                i != 0 -> i = lps[i - 1]\n                else -> lps[j++] = 0\n            }\n        }\n        return lps\n    }\n}\n// lps =  [0 0 0 0 0 0 0 0 0]\n        // a b c x y z c b a\n        //   ^             ^\n        //   i             1"  rust="class `FindTheIndexofTheFirstOccurrenceIna String` {\n    fn $1(haystack -> String, needle -> String) -> i32 {\n        // If the needle is empty, return 0\n        if (needle.isEmpty()) return 0\n\n        // Use 'to' to assign both m and n in one line\n        val (m, n) = needle.length to haystack.length\n\n        // Step 1: Build the LPS array for the needle\n        val lps = buildLPS(needle)\n        println!(needle)\n        println!(lps.contentToString())\n        // Step 2: Search for the needle in the haystack using KMP algorithm\n        var (i, j) = 0 to 0  // Pairing to initialize indices for haystack and needle\n\n        while (i < n) {\n            when {\n                haystack[i] == needle[j] -> {  // Characters match\n                    i++\n                    j++\n                }\n                j > 0 -> j = lps[j - 1]  // Mismatch after some matches, use LPS array to skip ahead\n                else -> i++  // No matches yet, move to the next character in the haystack\n            }\n\n            // If we've matched the entire needle, return the start index\n            if (j == m) return i - j\n        }\n\n        return -1  // If no match found\n    }\n\n    // Function to build the LPS (Longest Prefix Suffix) array for the needle\n    fn $1(needle -> String): Vec<i32> {\n        val lps = Vec<i32>(needle.length)\n        var (i, j) = 0 to 1\n\n        while (j < needle.length) {\n            when {\n                needle[j] == needle[i] -> lps[j++] = ++i\n                i != 0 -> i = lps[i - 1]\n                else -> lps[j++] = 0\n            }\n        }\n        return lps\n    }\n}\n// lps =  [0 0 0 0 0 0 0 0 0]\n        // a b c x y z c b a\n        //   ^             ^\n        //   i             1"  cpp="class `FindTheIndexofTheFirstOccurrenceIna String` {\n    $1(haystack, needle) {\n        // If the needle is empty, return 0\n        if (needle.isEmpty()) return 0\n\n        // Use 'to' to assign both m and n in one line\n        val (m, n) = needle.length to haystack.length\n\n        // Step 1: Build the LPS array for the needle\n        auto $1 = buildLPS(needle)\n        cout <<(needle)\n        cout <<(lps.contentToString())\n        // Step 2: Search for the needle in the haystack using KMP algorithm\n        var (i, j) = 0 to 0  // Pairing to initialize indices for haystack and needle\n\n        while (i < n) {\n            when {\n                haystack[i] == needle[j] -> {  // Characters match\n                    i++\n                    j++\n                }\n                j > 0 -> j = lps[j - 1]  // Mismatch after some matches, use LPS array to skip ahead\n                else -> i++  // No matches yet, move to the next character in the haystack\n            }\n\n            // If we've matched the entire needle, return the start index\n            if (j == m) return i - j\n        }\n\n        return -1  // If no match found\n    }\n\n    // Function to build the LPS (Longest Prefix Suffix) array for the needle\n    $1(needle): vector<int> {\n        auto $1 = vector<int>(needle.length)\n        var (i, j) = 0 to 1\n\n        while (j < needle.length) {\n            when {\n                needle[j] == needle[i] -> lps[j++] = ++i\n                i != 0 -> i = lps[i - 1]\n                else -> lps[j++] = 0\n            }\n        }\n        return lps\n    }\n}\n// lps =  [0 0 0 0 0 0 0 0 0]\n        // a b c x y z c b a\n        //   ^             ^\n        //   i             1" %}

---

## FindTheIndexofTheFirstOccurrenceIna String_RabinKarp

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/pattern_matching/FindTheIndexofTheFirstOccurrenceIna String_RabinKarp.kt`

{% include code-tabs.html  kotlin="package string.pattern_matching\n\nimport oracle.net.aso.m\n\nclass FindTheIndexOfTheFirstOccurrenceInString_RabinKarp {\n    fun strStr(haystack: String, needle: String): Int {\n        if (needle.isEmpty()) return 0\n        if (haystack.length < needle.length) return -1\n\n        val base = 26\n        val mod = 1_000_000_007\n        val m = needle.length\n        var targetHash = 0L\n        var windowHash = 0L\n        var power = 1L\n\n        // Precompute needle hash and initial window hash\n        for (i in 0 until m) {\n            targetHash = (targetHash * base + charValue(needle[i])) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n            if (i < m - 1) power = (power * base) % mod\n        }\n\n        // Early check for match at index 0\n        if (windowHash == targetHash && matches(haystack, needle, 0)) {\n            return 0\n        }\n\n        // Slide the window and update hash\n        for (i in m until haystack.length) {\n            // Remove leftmost character and add new character\n            windowHash = (windowHash - charValue(haystack[i - m]) * power % mod + mod) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n\n            // Check for match\n            val startIndex = i - m + 1\n            if (windowHash == targetHash && matches(haystack, needle, startIndex)) {\n                return startIndex\n            }\n        }\n\n        return -1\n    }\n\n    private fun charValue(c: Char): Int = c - 'a'\n\n    private fun matches(text: String, pattern: String, start: Int): Boolean {\n        for (i in pattern.indices) {\n            if (text[start + i] != pattern[i]) return false\n        }\n        return true\n    }\n}\n"  java="class FindTheIndexOfTheFirstOccurrenceInString_RabinKarp {\n    public static void $1(haystack, needle) {\n        if (needle.isEmpty()) return 0\n        if (haystack.length < needle.length) return -1\n\n        val base = 26\n        val mod = 1_000_000_007\n        val m = needle.length\n        var targetHash = 0L\n        var windowHash = 0L\n        var power = 1L\n\n        // Precompute needle hash and initial window hash\n        for (i in 0 until m) {\n            targetHash = (targetHash * base + charValue(needle[i])) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n            if (i < m - 1) power = (power * base) % mod\n        }\n\n        // Early check for match at index 0\n        if (windowHash == targetHash && matches(haystack, needle, 0)) {\n            return 0\n        }\n\n        // Slide the window and update hash\n        for (i in m until haystack.length) {\n            // Remove leftmost character and add new character\n            windowHash = (windowHash - charValue(haystack[i - m]) * power % mod + mod) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n\n            // Check for match\n            val startIndex = i - m + 1\n            if (windowHash == targetHash && matches(haystack, needle, startIndex)) {\n                return startIndex\n            }\n        }\n\n        return -1\n    }\n\n    private public static void $1(c: Char) = c - 'a'\n\n    private public static void $1(text, pattern, start) {\n        for (i in pattern.indices) {\n            if (text[start + i] != pattern[i]) return false\n        }\n        return true\n    }\n}"  python="class FindTheIndexOfTheFirstOccurrenceInString_RabinKarp {\n    def $1(haystack: str, needle: str): int {\n        if (needle.isEmpty()) return 0\n        if (haystack.length < needle.length) return -1\n\n        $1 = 26\n        $1 = 1_000_000_007\n        $1 = needle.length\n        $1 = 0L\n        $1 = 0L\n        $1 = 1L\n\n        // Precompute needle hash and initial window hash\n        for (i in 0 until m) {\n            targetHash = (targetHash * base + charValue(needle[i])) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n            if (i < m - 1) power = (power * base) % mod\n        }\n\n        // Early check for match at index 0\n        if (windowHash == targetHash && matches(haystack, needle, 0)) {\n            return 0\n        }\n\n        // Slide the window and update hash\n        for (i in m until haystack.length) {\n            // Remove leftmost character and add new character\n            windowHash = (windowHash - charValue(haystack[i - m]) * power % mod + mod) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n\n            // Check for match\n            $1 = i - m + 1\n            if (windowHash == targetHash && matches(haystack, needle, startIndex)) {\n                return startIndex\n            }\n        }\n\n        return -1\n    }\n\n    private def $1(c: Char): int = c - 'a'\n\n    private def $1(text: str, pattern: str, start: int): bool {\n        for (i in pattern.indices) {\n            if (text[start + i] != pattern[i]) return False\n        }\n        return True\n    }\n}"  rust="class FindTheIndexOfTheFirstOccurrenceInString_RabinKarp {\n    fn $1(haystack -> String, needle -> String) -> i32 {\n        if (needle.isEmpty()) return 0\n        if (haystack.length < needle.length) return -1\n\n        val base = 26\n        val mod = 1_000_000_007\n        val m = needle.length\n        var targetHash = 0L\n        var windowHash = 0L\n        var power = 1L\n\n        // Precompute needle hash and initial window hash\n        for (i in 0 until m) {\n            targetHash = (targetHash * base + charValue(needle[i])) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n            if (i < m - 1) power = (power * base) % mod\n        }\n\n        // Early check for match at index 0\n        if (windowHash == targetHash && matches(haystack, needle, 0)) {\n            return 0\n        }\n\n        // Slide the window and update hash\n        for (i in m until haystack.length) {\n            // Remove leftmost character and add new character\n            windowHash = (windowHash - charValue(haystack[i - m]) * power % mod + mod) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n\n            // Check for match\n            val startIndex = i - m + 1\n            if (windowHash == targetHash && matches(haystack, needle, startIndex)) {\n                return startIndex\n            }\n        }\n\n        return -1\n    }\n\n    private fn $1(c: Char) -> i32 = c - 'a'\n\n    private fn $1(text -> String, pattern -> String, start -> i32) -> bool {\n        for (i in pattern.indices) {\n            if (text[start + i] != pattern[i]) return false\n        }\n        return true\n    }\n}"  cpp="class FindTheIndexOfTheFirstOccurrenceInString_RabinKarp {\n    $1(haystack, needle) {\n        if (needle.isEmpty()) return 0\n        if (haystack.length < needle.length) return -1\n\n        auto $1 = 26\n        auto $1 = 1_000_000_007\n        auto $1 = needle.length\n        auto $1 = 0L\n        auto $1 = 0L\n        auto $1 = 1L\n\n        // Precompute needle hash and initial window hash\n        for (i in 0 until m) {\n            targetHash = (targetHash * base + charValue(needle[i])) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n            if (i < m - 1) power = (power * base) % mod\n        }\n\n        // Early check for match at index 0\n        if (windowHash == targetHash && matches(haystack, needle, 0)) {\n            return 0\n        }\n\n        // Slide the window and update hash\n        for (i in m until haystack.length) {\n            // Remove leftmost character and add new character\n            windowHash = (windowHash - charValue(haystack[i - m]) * power % mod + mod) % mod\n            windowHash = (windowHash * base + charValue(haystack[i])) % mod\n\n            // Check for match\n            auto $1 = i - m + 1\n            if (windowHash == targetHash && matches(haystack, needle, startIndex)) {\n                return startIndex\n            }\n        }\n\n        return -1\n    }\n\n    private $1(c: Char) = c - 'a'\n\n    private $1(text, pattern, start) {\n        for (i in pattern.indices) {\n            if (text[start + i] != pattern[i]) return false\n        }\n        return true\n    }\n}" %}

---

## GenerateParantheses

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/backtracking/GenerateParantheses.kt`

{% include code-tabs.html  kotlin="package string.backtracking\n\nimport java.lang.StringBuilder\n\nclass GenerateParantheses {\n    fun generateParenthesis(n: Int): List<String> {\n        val result = mutableListOf<String>()\n        generate(n, n, StringBuilder(), result)\n        return result\n    }\n\n    fun generate(open: Int, close: Int, current: StringBuilder, result: MutableList<String>) {\n        if (open == 0 && close == 0) {\n            result.add(current.toString())\n            return\n        }\n\n        if (open > 0) {\n            current.append(\"(\")\n            generate(open-1, close, current, result)\n            current.setLength(current.length - 1)\n        }\n\n        if (close > open) {\n            current.append(\")\")\n            generate(open, close - 1, current, result)\n            current.deleteCharAt(current.length - 1) // backtrack\n        }\n    }\n}"  java="class GenerateParantheses {\n    public static void $1(n): List<String> {\n        val result = mutableListOf<String>()\n        generate(n, n, StringBuilder(), result)\n        return result\n    }\n\n    public static void $1(open, close, current: StringBuilder, result: MutableList<String>) {\n        if (open == 0 && close == 0) {\n            result.add(current.toString())\n            return\n        }\n\n        if (open > 0) {\n            current.append(\"(\")\n            generate(open-1, close, current, result)\n            current.setLength(current.length - 1)\n        }\n\n        if (close > open) {\n            current.append(\")\")\n            generate(open, close - 1, current, result)\n            current.deleteCharAt(current.length - 1) // backtrack\n        }\n    }\n}"  python="class GenerateParantheses {\n    def $1(n: int): List<String> {\n        $1 = mutableListOf<String>()\n        generate(n, n, StringBuilder(), result)\n        return result\n    }\n\n    def $1(open: int, close: int, current: StringBuilder, result: MutableList<String>) {\n        if (open == 0 && close == 0) {\n            result.add(current.toString())\n            return\n        }\n\n        if (open > 0) {\n            current.append(\"(\")\n            generate(open-1, close, current, result)\n            current.setLength(current.length - 1)\n        }\n\n        if (close > open) {\n            current.append(\")\")\n            generate(open, close - 1, current, result)\n            current.deleteCharAt(current.length - 1) // backtrack\n        }\n    }\n}"  rust="class GenerateParantheses {\n    fn $1(n -> i32): List<String> {\n        val result = mutableListOf<String>()\n        generate(n, n, StringBuilder(), result)\n        return result\n    }\n\n    fn $1(open -> i32, close -> i32, current: StringBuilder, result: MutableList<String>) {\n        if (open == 0 && close == 0) {\n            result.add(current.toString())\n            return\n        }\n\n        if (open > 0) {\n            current.append(\"(\")\n            generate(open-1, close, current, result)\n            current.setLength(current.length - 1)\n        }\n\n        if (close > open) {\n            current.append(\")\")\n            generate(open, close - 1, current, result)\n            current.deleteCharAt(current.length - 1) // backtrack\n        }\n    }\n}"  cpp="class GenerateParantheses {\n    $1(n): List<String> {\n        auto $1 = mutableListOf<String>()\n        generate(n, n, StringBuilder(), result)\n        return result\n    }\n\n    $1(open, close, current: StringBuilder, result: MutableList<String>) {\n        if (open == 0 && close == 0) {\n            result.add(current.toString())\n            return\n        }\n\n        if (open > 0) {\n            current.append(\"(\")\n            generate(open-1, close, current, result)\n            current.setLength(current.length - 1)\n        }\n\n        if (close > open) {\n            current.append(\")\")\n            generate(open, close - 1, current, result)\n            current.deleteCharAt(current.length - 1) // backtrack\n        }\n    }\n}" %}

---

## EditDistance

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/EditDistance.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass EditDistance {\n    private var dp = mutableMapOf<String,Int>()\n\n    fun minDistance(word1: String, word2: String): Int {\n        return minDistance(word1, word2, word1.length, word2.length)\n    }\n\n    fun minDistance(word1: String, word2: String, m: Int, n: Int): Int {\n        val state = \"$m.$n\"\n\n        return when {\n            dp.contains(state) -> dp[state]!!\n            m == 0 -> n\n            n == 0 -> m\n            word1[m - 1] == word2[n - 1] -> minDistance(word1, word2, m - 1, n - 1)\n            else -> {\n                val insert = minDistance(word1, word2, m, n - 1)\n                val remove = minDistance(word1, word2, m - 1, n)\n                val replace = minDistance(word1, word2, m - 1, n - 1)\n                1 + minOf(insert, remove, replace)\n            }\n        }.also { dp[state] = it }\n    }\n\n    companion object {\n        @JvmStatic\n        fun main(args: Array<String>) {\n        }\n    }\n}"  java="class EditDistance {\n    private var dp = mutableMapOf<String,Int>()\n\n    public static void $1(word1, word2) {\n        return minDistance(word1, word2, word1.length, word2.length)\n    }\n\n    public static void $1(word1, word2, m, n) {\n        val state = \"$m.$n\"\n\n        return when {\n            dp.contains(state) -> dp[state]!!\n            m == 0 -> n\n            n == 0 -> m\n            word1[m - 1] == word2[n - 1] -> minDistance(word1, word2, m - 1, n - 1)\n            else -> {\n                val insert = minDistance(word1, word2, m, n - 1)\n                val remove = minDistance(word1, word2, m - 1, n)\n                val replace = minDistance(word1, word2, m - 1, n - 1)\n                1 + minOf(insert, remove, replace)\n            }\n        }.also { dp[state] = it }\n    }\n\n    companion object {\n        @JvmStatic\n        public static void $1(args: Array<String>) {\n        }\n    }\n}"  python="class EditDistance {\n    private $1 = mutableMapOf<String,Int>()\n\n    def $1(word1: str, word2: str): int {\n        return minDistance(word1, word2, word1.length, word2.length)\n    }\n\n    def $1(word1: str, word2: str, m: int, n: int): int {\n        $1 = \"$m.$n\"\n\n        return when {\n            dp.contains(state) -> dp[state]!!\n            m == 0 -> n\n            n == 0 -> m\n            word1[m - 1] == word2[n - 1] -> minDistance(word1, word2, m - 1, n - 1)\n            else -> {\n                $1 = minDistance(word1, word2, m, n - 1)\n                $1 = minDistance(word1, word2, m - 1, n)\n                $1 = minDistance(word1, word2, m - 1, n - 1)\n                1 + min(insert, remove, replace)\n            }\n        }.also { dp[state] = it }\n    }\n\n    companion object {\n        @JvmStatic\n        def $1(args: Array<String>) {\n        }\n    }\n}"  rust="class EditDistance {\n    private var dp = mutableMapOf<String,Int>()\n\n    fn $1(word1 -> String, word2 -> String) -> i32 {\n        return minDistance(word1, word2, word1.length, word2.length)\n    }\n\n    fn $1(word1 -> String, word2 -> String, m -> i32, n -> i32) -> i32 {\n        val state = \"$m.$n\"\n\n        return when {\n            dp.contains(state) -> dp[state]!!\n            m == 0 -> n\n            n == 0 -> m\n            word1[m - 1] == word2[n - 1] -> minDistance(word1, word2, m - 1, n - 1)\n            else -> {\n                val insert = minDistance(word1, word2, m, n - 1)\n                val remove = minDistance(word1, word2, m - 1, n)\n                val replace = minDistance(word1, word2, m - 1, n - 1)\n                1 + min(insert, remove, replace)\n            }\n        }.also { dp[state] = it }\n    }\n\n    companion object {\n        @JvmStatic\n        fn $1(args: Array<String>) {\n        }\n    }\n}"  cpp="class EditDistance {\n    private auto $1 = mutableMapOf<String,Int>()\n\n    $1(word1, word2) {\n        return minDistance(word1, word2, word1.length, word2.length)\n    }\n\n    $1(word1, word2, m, n) {\n        auto $1 = \"$m.$n\"\n\n        return when {\n            dp.contains(state) -> dp[state]!!\n            m == 0 -> n\n            n == 0 -> m\n            word1[m - 1] == word2[n - 1] -> minDistance(word1, word2, m - 1, n - 1)\n            else -> {\n                auto $1 = minDistance(word1, word2, m, n - 1)\n                auto $1 = minDistance(word1, word2, m - 1, n)\n                auto $1 = minDistance(word1, word2, m - 1, n - 1)\n                1 + min(insert, remove, replace)\n            }\n        }.also { dp[state] = it }\n    }\n\n    companion object {\n        @JvmStatic\n        $1(args: Array<String>) {\n        }\n    }\n}" %}

---

## InterleavingString

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/InterleavingString.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass InterleavingString {\n    fun isInterleave(s1: String, s2: String, s3: String): Boolean {\n        if (s1.length + s2.length != s3.length) return false\n\n        val memo = mutableMapOf<Pair<Int, Int>, Boolean>()\n\n        fun dfs(i: Int, j: Int): Boolean {\n            if (i == s1.length && j == s2.length) return true\n            if (memo.containsKey(i to j)) return memo[i to j]!!\n\n            val k = i + j\n            var result = false\n\n            if (i < s1.length && s1[i] == s3[k] && dfs(i + 1, j)) {\n                result = true\n            }\n            if (j < s2.length && s2[j] == s3[k] && dfs(i, j + 1)) {\n                result = true\n            }\n\n            memo[i to j] = result\n            return result\n        }\n\n        return dfs(0, 0)\n    }\n}\n"  java="class InterleavingString {\n    public static void $1(s1, s2, s3) {\n        if (s1.length + s2.length != s3.length) return false\n\n        val memo = mutableMapOf<Pair<Int, Int>, Boolean>()\n\n        public static void $1(i, j) {\n            if (i == s1.length && j == s2.length) return true\n            if (memo.containsKey(i to j)) return memo[i to j]!!\n\n            val k = i + j\n            var result = false\n\n            if (i < s1.length && s1[i] == s3[k] && dfs(i + 1, j)) {\n                result = true\n            }\n            if (j < s2.length && s2[j] == s3[k] && dfs(i, j + 1)) {\n                result = true\n            }\n\n            memo[i to j] = result\n            return result\n        }\n\n        return dfs(0, 0)\n    }\n}"  python="class InterleavingString {\n    def $1(s1: str, s2: str, s3: str): bool {\n        if (s1.length + s2.length != s3.length) return False\n\n        $1 = mutableMapOf<Pair<Int, Int>, Boolean>()\n\n        def $1(i: int, j: int): bool {\n            if (i == s1.length && j == s2.length) return True\n            if (memo.containsKey(i to j)) return memo[i to j]!!\n\n            $1 = i + j\n            $1 = False\n\n            if (i < s1.length && s1[i] == s3[k] && dfs(i + 1, j)) {\n                result = True\n            }\n            if (j < s2.length && s2[j] == s3[k] && dfs(i, j + 1)) {\n                result = True\n            }\n\n            memo[i to j] = result\n            return result\n        }\n\n        return dfs(0, 0)\n    }\n}"  rust="class InterleavingString {\n    fn $1(s1 -> String, s2 -> String, s3 -> String) -> bool {\n        if (s1.length + s2.length != s3.length) return false\n\n        val memo = mutableMapOf<Pair<Int, Int>, Boolean>()\n\n        fn $1(i -> i32, j -> i32) -> bool {\n            if (i == s1.length && j == s2.length) return true\n            if (memo.containsKey(i to j)) return memo[i to j]!!\n\n            val k = i + j\n            var result = false\n\n            if (i < s1.length && s1[i] == s3[k] && dfs(i + 1, j)) {\n                result = true\n            }\n            if (j < s2.length && s2[j] == s3[k] && dfs(i, j + 1)) {\n                result = true\n            }\n\n            memo[i to j] = result\n            return result\n        }\n\n        return dfs(0, 0)\n    }\n}"  cpp="class InterleavingString {\n    $1(s1, s2, s3) {\n        if (s1.length + s2.length != s3.length) return false\n\n        auto $1 = mutableMapOf<Pair<Int, Int>, Boolean>()\n\n        $1(i, j) {\n            if (i == s1.length && j == s2.length) return true\n            if (memo.containsKey(i to j)) return memo[i to j]!!\n\n            auto $1 = i + j\n            auto $1 = false\n\n            if (i < s1.length && s1[i] == s3[k] && dfs(i + 1, j)) {\n                result = true\n            }\n            if (j < s2.length && s2[j] == s3[k] && dfs(i, j + 1)) {\n                result = true\n            }\n\n            memo[i to j] = result\n            return result\n        }\n\n        return dfs(0, 0)\n    }\n}" %}

---

## LongestCommonSubsequence

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/LongestCommonSubsequence.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass LongestCommonSubsequence {\n    companion object {\n        private lateinit var M: Array<IntArray>\n\n        fun lcs(str1: String, str2: String): Int {\n            M = Array(str1.length + 1) { IntArray(str2.length + 1) }\n            for (i in 1..str1.length) {\n                for (j in 1..str2.length) {\n                    if (str1[i - 1] == str2[j - 1]) {\n                        M[i][j] = M[i - 1][j - 1] + 1\n                    } else {\n                        M[i][j] = maxOf(M[i - 1][j], M[i][j - 1])\n                    }\n                }\n            }\n            return M[str1.length][str2.length]\n        }\n\n        fun printLCS(str: String, i: Int, j: Int) {\n            if (i <= 0 || j <= 0) return\n            if (M[i][j] != M[i][j - 1] && M[i][j] != M[i - 1][j] && M[i][j] == M[i - 1][j - 1] + 1) {\n                print(str[j - 1])\n                printLCS(str, i - 1, j - 1)\n            } else if (M[i][j - 1] > M[i - 1][j]) {\n                printLCS(str, i, j - 1)\n            } else {\n                printLCS(str, i - 1, j)\n            }\n        }\n\n        @JvmStatic\n        fun main(args: Array<String>) {\n            println(lcs(\"abcxgzggmn\", \"acttvvmnvt\"))\n            printLCS(\"acttvvmnvt\", \"abcxgzggmn\".length, \"acttvvmnvt\".length) // Print in reverse order\n        }\n    }\n}\n"  java="class LongestCommonSubsequence {\n    companion object {\n        private lateinit var M: Array<IntArray>\n\n        public static void $1(str1, str2) {\n            M = Array(str1.length + 1) { IntArray(str2.length + 1) }\n            for (i in 1..str1.length) {\n                for (j in 1..str2.length) {\n                    if (str1[i - 1] == str2[j - 1]) {\n                        M[i][j] = M[i - 1][j - 1] + 1\n                    } else {\n                        M[i][j] = maxOf(M[i - 1][j], M[i][j - 1])\n                    }\n                }\n            }\n            return M[str1.length][str2.length]\n        }\n\n        public static void $1(str, i, j) {\n            if (i <= 0 || j <= 0) return\n            if (M[i][j] != M[i][j - 1] && M[i][j] != M[i - 1][j] && M[i][j] == M[i - 1][j - 1] + 1) {\n                print(str[j - 1])\n                printLCS(str, i - 1, j - 1)\n            } else if (M[i][j - 1] > M[i - 1][j]) {\n                printLCS(str, i, j - 1)\n            } else {\n                printLCS(str, i - 1, j)\n            }\n        }\n\n        @JvmStatic\n        public static void $1(args: Array<String>) {\n            println(lcs(\"abcxgzggmn\", \"acttvvmnvt\"))\n            printLCS(\"acttvvmnvt\", \"abcxgzggmn\".length, \"acttvvmnvt\".length) // Print in reverse order\n        }\n    }\n}"  python="class LongestCommonSubsequence {\n    companion object {\n        private lateinit var M: Array<list[int]>\n\n        def $1(str1: str, str2: str): int {\n            M = Array(str1.length + 1) { list[int](str2.length + 1) }\n            for (i in 1..str1.length) {\n                for (j in 1..str2.length) {\n                    if (str1[i - 1] == str2[j - 1]) {\n                        M[i][j] = M[i - 1][j - 1] + 1\n                    } else {\n                        M[i][j] = max(M[i - 1][j], M[i][j - 1])\n                    }\n                }\n            }\n            return M[str1.length][str2.length]\n        }\n\n        def $1(str: str, i: int, j: int) {\n            if (i <= 0 || j <= 0) return\n            if (M[i][j] != M[i][j - 1] && M[i][j] != M[i - 1][j] && M[i][j] == M[i - 1][j - 1] + 1) {\n                print(str[j - 1])\n                printLCS(str, i - 1, j - 1)\n            } else if (M[i][j - 1] > M[i - 1][j]) {\n                printLCS(str, i, j - 1)\n            } else {\n                printLCS(str, i - 1, j)\n            }\n        }\n\n        @JvmStatic\n        def $1(args: Array<String>) {\n            print(lcs(\"abcxgzggmn\", \"acttvvmnvt\"))\n            printLCS(\"acttvvmnvt\", \"abcxgzggmn\".length, \"acttvvmnvt\".length) // Print in reverse order\n        }\n    }\n}"  rust="class LongestCommonSubsequence {\n    companion object {\n        private lateinit var M: Array<Vec<i32>>\n\n        fn $1(str1 -> String, str2 -> String) -> i32 {\n            M = Array(str1.length + 1) { Vec<i32>(str2.length + 1) }\n            for (i in 1..str1.length) {\n                for (j in 1..str2.length) {\n                    if (str1[i - 1] == str2[j - 1]) {\n                        M[i][j] = M[i - 1][j - 1] + 1\n                    } else {\n                        M[i][j] = max(M[i - 1][j], M[i][j - 1])\n                    }\n                }\n            }\n            return M[str1.length][str2.length]\n        }\n\n        fn $1(str -> String, i -> i32, j -> i32) {\n            if (i <= 0 || j <= 0) return\n            if (M[i][j] != M[i][j - 1] && M[i][j] != M[i - 1][j] && M[i][j] == M[i - 1][j - 1] + 1) {\n                print(str[j - 1])\n                printLCS(str, i - 1, j - 1)\n            } else if (M[i][j - 1] > M[i - 1][j]) {\n                printLCS(str, i, j - 1)\n            } else {\n                printLCS(str, i - 1, j)\n            }\n        }\n\n        @JvmStatic\n        fn $1(args: Array<String>) {\n            println!(lcs(\"abcxgzggmn\", \"acttvvmnvt\"))\n            printLCS(\"acttvvmnvt\", \"abcxgzggmn\".length, \"acttvvmnvt\".length) // Print in reverse order\n        }\n    }\n}"  cpp="class LongestCommonSubsequence {\n    companion object {\n        private lateinit var M: Array<vector<int>>\n\n        $1(str1, str2) {\n            M = Array(str1.length + 1) { vector<int>(str2.length + 1) }\n            for (i in 1..str1.length) {\n                for (j in 1..str2.length) {\n                    if (str1[i - 1] == str2[j - 1]) {\n                        M[i][j] = M[i - 1][j - 1] + 1\n                    } else {\n                        M[i][j] = max(M[i - 1][j], M[i][j - 1])\n                    }\n                }\n            }\n            return M[str1.length][str2.length]\n        }\n\n        $1(str, i, j) {\n            if (i <= 0 || j <= 0) return\n            if (M[i][j] != M[i][j - 1] && M[i][j] != M[i - 1][j] && M[i][j] == M[i - 1][j - 1] + 1) {\n                print(str[j - 1])\n                printLCS(str, i - 1, j - 1)\n            } else if (M[i][j - 1] > M[i - 1][j]) {\n                printLCS(str, i, j - 1)\n            } else {\n                printLCS(str, i - 1, j)\n            }\n        }\n\n        @JvmStatic\n        $1(args: Array<String>) {\n            cout <<(lcs(\"abcxgzggmn\", \"acttvvmnvt\"))\n            printLCS(\"acttvvmnvt\", \"abcxgzggmn\".length, \"acttvvmnvt\".length) // Print in reverse order\n        }\n    }\n}" %}

---

## LongestCommonSubstring

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/LongestCommonSubstring.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass LongestCommonSubstring {\n}"  java="class LongestCommonSubstring {\n}"  python="class LongestCommonSubstring {\n}"  rust="class LongestCommonSubstring {\n}"  cpp="class LongestCommonSubstring {\n}" %}

---

## LongestPalindromicSubsequence

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/LongestPalindromicSubsequence.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass LongestPalindromicSubsequence {\n    fun longestPalindromeSubseq(s: String): Int {\n        val dp = Array(s.length) { IntArray(s.length) }\n\n        fun lps(start: Int, length: Int): Int {\n            if (length <= 1) return length\n            val end = start + length - 1\n\n            return when {\n                dp[start][end] != 0 -> dp[start][end] // Value already cached\n                s[start] == s[end] -> 2 + lps(start + 1, length - 2)\n                else -> maxOf(lps(start + 1, length - 1 ), lps(start, length - 1))\n            }.also { dp[start][end] = it}\n        }\n        return lps(0, s.length)\n    }\n}"  java="class LongestPalindromicSubsequence {\n    public static void $1(s) {\n        val dp = Array(s.length) { IntArray(s.length) }\n\n        public static void $1(start, length) {\n            if (length <= 1) return length\n            val end = start + length - 1\n\n            return when {\n                dp[start][end] != 0 -> dp[start][end] // Value already cached\n                s[start] == s[end] -> 2 + lps(start + 1, length - 2)\n                else -> maxOf(lps(start + 1, length - 1 ), lps(start, length - 1))\n            }.also { dp[start][end] = it}\n        }\n        return lps(0, s.length)\n    }\n}"  python="class LongestPalindromicSubsequence {\n    def $1(s: str): int {\n        $1 = Array(s.length) { list[int](s.length) }\n\n        def $1(start: int, length: int): int {\n            if (length <= 1) return length\n            $1 = start + length - 1\n\n            return when {\n                dp[start][end] != 0 -> dp[start][end] // Value already cached\n                s[start] == s[end] -> 2 + lps(start + 1, length - 2)\n                else -> max(lps(start + 1, length - 1 ), lps(start, length - 1))\n            }.also { dp[start][end] = it}\n        }\n        return lps(0, s.length)\n    }\n}"  rust="class LongestPalindromicSubsequence {\n    fn $1(s -> String) -> i32 {\n        val dp = Array(s.length) { Vec<i32>(s.length) }\n\n        fn $1(start -> i32, length -> i32) -> i32 {\n            if (length <= 1) return length\n            val end = start + length - 1\n\n            return when {\n                dp[start][end] != 0 -> dp[start][end] // Value already cached\n                s[start] == s[end] -> 2 + lps(start + 1, length - 2)\n                else -> max(lps(start + 1, length - 1 ), lps(start, length - 1))\n            }.also { dp[start][end] = it}\n        }\n        return lps(0, s.length)\n    }\n}"  cpp="class LongestPalindromicSubsequence {\n    $1(s) {\n        auto $1 = Array(s.length) { vector<int>(s.length) }\n\n        $1(start, length) {\n            if (length <= 1) return length\n            auto $1 = start + length - 1\n\n            return when {\n                dp[start][end] != 0 -> dp[start][end] // Value already cached\n                s[start] == s[end] -> 2 + lps(start + 1, length - 2)\n                else -> max(lps(start + 1, length - 1 ), lps(start, length - 1))\n            }.also { dp[start][end] = it}\n        }\n        return lps(0, s.length)\n    }\n}" %}

---

## LongestPalindromicSubsequence_BottomUp

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/LongestPalindromicSubsequence_BottomUp.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass LongestPalindromicSubsequence_BottomUp {\n    fun longestPalindromeSubseq(s: String): Int {\n        val n = s.length\n        val dp = Array(n) { IntArray(n) }\n\n        // Bottom-up fill the dp table for substrings of increasing length\n        for (length in 1..n) {  // Iterate over all substring lengths\n            for (start in 0..n - length) {  // Iterate over all possible start indices\n                val end = start + length - 1  // Calculate the end index of the substring\n\n                dp[start][end] = when {\n                    length == 1 -> 1  // Base case: single character is a palindrome of length 1\n                    s[start] == s[end] -> 2 + dp[start + 1][end - 1]  // Palindrome formed by adding the two matching characters\n                    else -> maxOf(dp[start + 1][end], dp[start][end - 1])  // Max of ignoring one end\n                }\n            }\n        }\n\n        // The result is stored in dp[0][n-1] (the entire string)\n        return dp[0][n - 1]\n    }\n}"  java="class LongestPalindromicSubsequence_BottomUp {\n    public static void $1(s) {\n        val n = s.length\n        val dp = Array(n) { IntArray(n) }\n\n        // Bottom-up fill the dp table for substrings of increasing length\n        for (length in 1..n) {  // Iterate over all substring lengths\n            for (start in 0..n - length) {  // Iterate over all possible start indices\n                val end = start + length - 1  // Calculate the end index of the substring\n\n                dp[start][end] = when {\n                    length == 1 -> 1  // Base case: single character is a palindrome of length 1\n                    s[start] == s[end] -> 2 + dp[start + 1][end - 1]  // Palindrome formed by adding the two matching characters\n                    else -> maxOf(dp[start + 1][end], dp[start][end - 1])  // Max of ignoring one end\n                }\n            }\n        }\n\n        // The result is stored in dp[0][n-1] (the entire string)\n        return dp[0][n - 1]\n    }\n}"  python="class LongestPalindromicSubsequence_BottomUp {\n    def $1(s: str): int {\n        $1 = s.length\n        $1 = Array(n) { list[int](n) }\n\n        // Bottom-up fill the dp table for substrings of increasing length\n        for (length in 1..n) {  // Iterate over all substring lengths\n            for (start in 0..n - length) {  // Iterate over all possible start indices\n                $1 = start + length - 1  // Calculate the end index of the substring\n\n                dp[start][end] = when {\n                    length == 1 -> 1  // Base case: single character is a palindrome of length 1\n                    s[start] == s[end] -> 2 + dp[start + 1][end - 1]  // Palindrome formed by adding the two matching characters\n                    else -> max(dp[start + 1][end], dp[start][end - 1])  // Max of ignoring one end\n                }\n            }\n        }\n\n        // The result is stored in dp[0][n-1] (the entire string)\n        return dp[0][n - 1]\n    }\n}"  rust="class LongestPalindromicSubsequence_BottomUp {\n    fn $1(s -> String) -> i32 {\n        val n = s.length\n        val dp = Array(n) { Vec<i32>(n) }\n\n        // Bottom-up fill the dp table for substrings of increasing length\n        for (length in 1..n) {  // Iterate over all substring lengths\n            for (start in 0..n - length) {  // Iterate over all possible start indices\n                val end = start + length - 1  // Calculate the end index of the substring\n\n                dp[start][end] = when {\n                    length == 1 -> 1  // Base case: single character is a palindrome of length 1\n                    s[start] == s[end] -> 2 + dp[start + 1][end - 1]  // Palindrome formed by adding the two matching characters\n                    else -> max(dp[start + 1][end], dp[start][end - 1])  // Max of ignoring one end\n                }\n            }\n        }\n\n        // The result is stored in dp[0][n-1] (the entire string)\n        return dp[0][n - 1]\n    }\n}"  cpp="class LongestPalindromicSubsequence_BottomUp {\n    $1(s) {\n        auto $1 = s.length\n        auto $1 = Array(n) { vector<int>(n) }\n\n        // Bottom-up fill the dp table for substrings of increasing length\n        for (length in 1..n) {  // Iterate over all substring lengths\n            for (start in 0..n - length) {  // Iterate over all possible start indices\n                auto $1 = start + length - 1  // Calculate the end index of the substring\n\n                dp[start][end] = when {\n                    length == 1 -> 1  // Base case: single character is a palindrome of length 1\n                    s[start] == s[end] -> 2 + dp[start + 1][end - 1]  // Palindrome formed by adding the two matching characters\n                    else -> max(dp[start + 1][end], dp[start][end - 1])  // Max of ignoring one end\n                }\n            }\n        }\n\n        // The result is stored in dp[0][n-1] (the entire string)\n        return dp[0][n - 1]\n    }\n}" %}

---

## LongestStringChain

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/LongestStringChain.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass LongestStringChain {\n    fun longestStrChain(words: Array<String>): Int {\n        words.sortBy { it.length }  // Sort words by length\n        val dp = mutableMapOf<String, Int>()\n        var maxLen = 1\n\n        for (word in words) {\n            dp[word] = 1  // Default length of chain ending at word is 1\n            for (i in word.indices) {\n                val prev = word.removeRange(i, i + 1)  // Remove one character\n                if (prev in dp) {\n                    dp[word] = maxOf(dp[word]!!, dp[prev]!! + 1)\n                }\n            }\n            maxLen = maxOf(maxLen, dp[word]!!)\n        }\n\n        return maxLen\n    }\n}\n"  java="class LongestStringChain {\n    public static void $1(words: Array<String>) {\n        words.sortBy { it.length }  // Sort words by length\n        val dp = mutableMapOf<String, Int>()\n        var maxLen = 1\n\n        for (word in words) {\n            dp[word] = 1  // Default length of chain ending at word is 1\n            for (i in word.indices) {\n                val prev = word.removeRange(i, i + 1)  // Remove one character\n                if (prev in dp) {\n                    dp[word] = maxOf(dp[word]!!, dp[prev]!! + 1)\n                }\n            }\n            maxLen = maxOf(maxLen, dp[word]!!)\n        }\n\n        return maxLen\n    }\n}"  python="class LongestStringChain {\n    def $1(words: Array<String>): int {\n        words.sortBy { it.length }  // Sort words by length\n        $1 = mutableMapOf<String, Int>()\n        $1 = 1\n\n        for (word in words) {\n            dp[word] = 1  // Default length of chain ending at word is 1\n            for (i in word.indices) {\n                $1 = word.removeRange(i, i + 1)  // Remove one character\n                if (prev in dp) {\n                    dp[word] = max(dp[word]!!, dp[prev]!! + 1)\n                }\n            }\n            maxLen = max(maxLen, dp[word]!!)\n        }\n\n        return maxLen\n    }\n}"  rust="class LongestStringChain {\n    fn $1(words: Array<String>) -> i32 {\n        words.sortBy { it.length }  // Sort words by length\n        val dp = mutableMapOf<String, Int>()\n        var maxLen = 1\n\n        for (word in words) {\n            dp[word] = 1  // Default length of chain ending at word is 1\n            for (i in word.indices) {\n                val prev = word.removeRange(i, i + 1)  // Remove one character\n                if (prev in dp) {\n                    dp[word] = max(dp[word]!!, dp[prev]!! + 1)\n                }\n            }\n            maxLen = max(maxLen, dp[word]!!)\n        }\n\n        return maxLen\n    }\n}"  cpp="class LongestStringChain {\n    $1(words: Array<String>) {\n        words.sortBy { it.length }  // Sort words by length\n        auto $1 = mutableMapOf<String, Int>()\n        auto $1 = 1\n\n        for (word in words) {\n            dp[word] = 1  // Default length of chain ending at word is 1\n            for (i in word.indices) {\n                auto $1 = word.removeRange(i, i + 1)  // Remove one character\n                if (prev in dp) {\n                    dp[word] = max(dp[word]!!, dp[prev]!! + 1)\n                }\n            }\n            maxLen = max(maxLen, dp[word]!!)\n        }\n\n        return maxLen\n    }\n}" %}

---

## RegularExpressionMatching

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/RegularExpressionMatching.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass RegularExpressionMatching {\n    val dp = mutableMapOf<String, Boolean>()\n\n    fun isMatch(s: String, p: String): Boolean {\n        return dfs(s, p, 0, 0)\n    }\n\n    fun dfs(s: String, p: String, i: Int, j: Int ): Boolean {\n        val state = \"$i.$j\"\n        return when {\n            dp.contains(state) -> dp[state]!!\n            i >= s.length && j >= p.length -> true\n            j >= p.length -> false\n            else -> {\n                val match = (i < s.length) &&(s[i] == p[j] || p[j] == '.')\n                when {\n                    j + 1 < p.length && p[j + 1] == '*'\n                        -> dfs(s, p, i, j + 2) || (match && dfs(s, p, i + 1, j))\n                    match -> dfs(s, p, i + 1, j + 1)\n                    else -> false\n                }\n            }\n        }.also { dp[state] = it }\n    }\n}"  java="class RegularExpressionMatching {\n    val dp = mutableMapOf<String, Boolean>()\n\n    public static void $1(s, p) {\n        return dfs(s, p, 0, 0)\n    }\n\n    public static void $1(s, p, i, j ) {\n        val state = \"$i.$j\"\n        return when {\n            dp.contains(state) -> dp[state]!!\n            i >= s.length && j >= p.length -> true\n            j >= p.length -> false\n            else -> {\n                val match = (i < s.length) &&(s[i] == p[j] || p[j] == '.')\n                when {\n                    j + 1 < p.length && p[j + 1] == '*'\n                        -> dfs(s, p, i, j + 2) || (match && dfs(s, p, i + 1, j))\n                    match -> dfs(s, p, i + 1, j + 1)\n                    else -> false\n                }\n            }\n        }.also { dp[state] = it }\n    }\n}"  python="class RegularExpressionMatching {\n    $1 = mutableMapOf<String, Boolean>()\n\n    def $1(s: str, p: str): bool {\n        return dfs(s, p, 0, 0)\n    }\n\n    def $1(s: str, p: str, i: int, j: int ): bool {\n        $1 = \"$i.$j\"\n        return when {\n            dp.contains(state) -> dp[state]!!\n            i >= s.length && j >= p.length -> True\n            j >= p.length -> False\n            else -> {\n                $1 = (i < s.length) &&(s[i] == p[j] || p[j] == '.')\n                when {\n                    j + 1 < p.length && p[j + 1] == '*'\n                        -> dfs(s, p, i, j + 2) || (match && dfs(s, p, i + 1, j))\n                    match -> dfs(s, p, i + 1, j + 1)\n                    else -> False\n                }\n            }\n        }.also { dp[state] = it }\n    }\n}"  rust="class RegularExpressionMatching {\n    val dp = mutableMapOf<String, Boolean>()\n\n    fn $1(s -> String, p -> String) -> bool {\n        return dfs(s, p, 0, 0)\n    }\n\n    fn $1(s -> String, p -> String, i -> i32, j -> i32 ) -> bool {\n        val state = \"$i.$j\"\n        return when {\n            dp.contains(state) -> dp[state]!!\n            i >= s.length && j >= p.length -> true\n            j >= p.length -> false\n            else -> {\n                val match = (i < s.length) &&(s[i] == p[j] || p[j] == '.')\n                when {\n                    j + 1 < p.length && p[j + 1] == '*'\n                        -> dfs(s, p, i, j + 2) || (match && dfs(s, p, i + 1, j))\n                    match -> dfs(s, p, i + 1, j + 1)\n                    else -> false\n                }\n            }\n        }.also { dp[state] = it }\n    }\n}"  cpp="class RegularExpressionMatching {\n    auto $1 = mutableMapOf<String, Boolean>()\n\n    $1(s, p) {\n        return dfs(s, p, 0, 0)\n    }\n\n    $1(s, p, i, j ) {\n        auto $1 = \"$i.$j\"\n        return when {\n            dp.contains(state) -> dp[state]!!\n            i >= s.length && j >= p.length -> true\n            j >= p.length -> false\n            else -> {\n                auto $1 = (i < s.length) &&(s[i] == p[j] || p[j] == '.')\n                when {\n                    j + 1 < p.length && p[j + 1] == '*'\n                        -> dfs(s, p, i, j + 2) || (match && dfs(s, p, i + 1, j))\n                    match -> dfs(s, p, i + 1, j + 1)\n                    else -> false\n                }\n            }\n        }.also { dp[state] = it }\n    }\n}" %}

---

## ShortestCommonSupersequence

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/ShortestCommonSupersequence.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass ShortestCommonSupersequence {\n    fun shortestCommonSupersequence(X: String, Y: String): String? {\n        val (m, n) = X.length to Y.length\n        val dp = Array(m + 1) { IntArray(n + 1) }\n\n        for (i in 1..m) for (j in 1..n) {\n            dp[i][j] = if (X[i - 1] == Y[j - 1]) dp[i - 1][j - 1] + 1 else maxOf(dp[i - 1][j], dp[i][j - 1])\n        }\n\n        val scs = StringBuilder()\n        var (i, j) = m to n\n\n        while (i > 0 && j > 0) {\n            when {\n                X[i - 1] == Y[j - 1] -> { scs.append(X[i - 1]); i--; j-- }\n                dp[i - 1][j] > dp[i][j - 1] -> { scs.append(X[i - 1]); i-- }\n                else -> { scs.append(Y[j - 1]); j-- }\n            }\n        }\n\n        // Add remaining characters from X or Y\n        while (i > 0) { scs.append(X[i - 1]); i-- }\n        while (j > 0) { scs.append(Y[j - 1]); j-- }\n\n        // Reverse the result and return as string\n        return scs.reverse().toString()\n    }\n}\n\n// abc, axyzabc, yzmnbc\n\n// yzmnbcaxyzabc\n\n// a -> b -> c\n// a -> x -> y -> x -> (a -> b -> c)\n\n// y -> z -> m -> n -> b -> c\n\n\n"  java="class ShortestCommonSupersequence {\n    public static void $1(X, Y)? {\n        val (m, n) = X.length to Y.length\n        val dp = Array(m + 1) { IntArray(n + 1) }\n\n        for (i in 1..m) for (j in 1..n) {\n            dp[i][j] = if (X[i - 1] == Y[j - 1]) dp[i - 1][j - 1] + 1 else maxOf(dp[i - 1][j], dp[i][j - 1])\n        }\n\n        val scs = StringBuilder()\n        var (i, j) = m to n\n\n        while (i > 0 && j > 0) {\n            when {\n                X[i - 1] == Y[j - 1] -> { scs.append(X[i - 1]); i--; j-- }\n                dp[i - 1][j] > dp[i][j - 1] -> { scs.append(X[i - 1]); i-- }\n                else -> { scs.append(Y[j - 1]); j-- }\n            }\n        }\n\n        // Add remaining characters from X or Y\n        while (i > 0) { scs.append(X[i - 1]); i-- }\n        while (j > 0) { scs.append(Y[j - 1]); j-- }\n\n        // Reverse the result and return as string\n        return scs.reverse().toString()\n    }\n}\n\n// abc, axyzabc, yzmnbc\n\n// yzmnbcaxyzabc\n\n// a -> b -> c\n// a -> x -> y -> x -> (a -> b -> c)\n\n// y -> z -> m -> n -> b -> c"  python="class ShortestCommonSupersequence {\n    def $1(X: str, Y: str): str? {\n        ($1, $2) = X.length to Y.length\n        $1 = Array(m + 1) { list[int](n + 1) }\n\n        for (i in 1..m) for (j in 1..n) {\n            dp[i][j] = if (X[i - 1] == Y[j - 1]) dp[i - 1][j - 1] + 1 else max(dp[i - 1][j], dp[i][j - 1])\n        }\n\n        $1 = StringBuilder()\n        var (i, j) = m to n\n\n        while (i > 0 && j > 0) {\n            when {\n                X[i - 1] == Y[j - 1] -> { scs.append(X[i - 1]); i--; j-- }\n                dp[i - 1][j] > dp[i][j - 1] -> { scs.append(X[i - 1]); i-- }\n                else -> { scs.append(Y[j - 1]); j-- }\n            }\n        }\n\n        // Add remaining characters from X or Y\n        while (i > 0) { scs.append(X[i - 1]); i-- }\n        while (j > 0) { scs.append(Y[j - 1]); j-- }\n\n        // Reverse the result and return as string\n        return scs.reverse().toString()\n    }\n}\n\n// abc, axyzabc, yzmnbc\n\n// yzmnbcaxyzabc\n\n// a -> b -> c\n// a -> x -> y -> x -> (a -> b -> c)\n\n// y -> z -> m -> n -> b -> c"  rust="class ShortestCommonSupersequence {\n    fn $1(X -> String, Y -> String) -> String? {\n        val (m, n) = X.length to Y.length\n        val dp = Array(m + 1) { Vec<i32>(n + 1) }\n\n        for (i in 1..m) for (j in 1..n) {\n            dp[i][j] = if (X[i - 1] == Y[j - 1]) dp[i - 1][j - 1] + 1 else max(dp[i - 1][j], dp[i][j - 1])\n        }\n\n        val scs = StringBuilder()\n        var (i, j) = m to n\n\n        while (i > 0 && j > 0) {\n            when {\n                X[i - 1] == Y[j - 1] -> { scs.append(X[i - 1]); i--; j-- }\n                dp[i - 1][j] > dp[i][j - 1] -> { scs.append(X[i - 1]); i-- }\n                else -> { scs.append(Y[j - 1]); j-- }\n            }\n        }\n\n        // Add remaining characters from X or Y\n        while (i > 0) { scs.append(X[i - 1]); i-- }\n        while (j > 0) { scs.append(Y[j - 1]); j-- }\n\n        // Reverse the result and return as string\n        return scs.reverse().toString()\n    }\n}\n\n// abc, axyzabc, yzmnbc\n\n// yzmnbcaxyzabc\n\n// a -> b -> c\n// a -> x -> y -> x -> (a -> b -> c)\n\n// y -> z -> m -> n -> b -> c"  cpp="class ShortestCommonSupersequence {\n    $1(X, Y)? {\n        val (m, n) = X.length to Y.length\n        auto $1 = Array(m + 1) { vector<int>(n + 1) }\n\n        for (i in 1..m) for (j in 1..n) {\n            dp[i][j] = if (X[i - 1] == Y[j - 1]) dp[i - 1][j - 1] + 1 else max(dp[i - 1][j], dp[i][j - 1])\n        }\n\n        auto $1 = StringBuilder()\n        var (i, j) = m to n\n\n        while (i > 0 && j > 0) {\n            when {\n                X[i - 1] == Y[j - 1] -> { scs.append(X[i - 1]); i--; j-- }\n                dp[i - 1][j] > dp[i][j - 1] -> { scs.append(X[i - 1]); i-- }\n                else -> { scs.append(Y[j - 1]); j-- }\n            }\n        }\n\n        // Add remaining characters from X or Y\n        while (i > 0) { scs.append(X[i - 1]); i-- }\n        while (j > 0) { scs.append(Y[j - 1]); j-- }\n\n        // Reverse the result and return as string\n        return scs.reverse().toString()\n    }\n}\n\n// abc, axyzabc, yzmnbc\n\n// yzmnbcaxyzabc\n\n// a -> b -> c\n// a -> x -> y -> x -> (a -> b -> c)\n\n// y -> z -> m -> n -> b -> c" %}

---

## ValidPalindrome_III

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/ValidPalindrome_III.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass ValidPalindrome_III {\n    fun isValidPalindrome(s: String, k: Int): Boolean {\n        val dp = Array(s.length) { IntArray(s.length) { 0 } }\n\n        fun lps(start: Int, length: Int): Int {\n            val end = start + length - 1\n            return when {\n                length in 0..1 -> length\n                dp[start][end] != 0 -> dp[start][end]\n                s[start] == s[end] -> lps(start + 1, length - 2) + 2\n                else -> maxOf(lps(start + 1, length - 1), lps(start, length - 1))\n            }.also {\n                dp[start][end] = it\n            }\n        }\n\n        // Calculate the length of the longest palindromic subsequence\n        val lpsLength = lps(0, s.length)\n\n        // Check if the number of deletions required is less than or equal to k\n        return (s.length - lpsLength) <= k\n    }\n}\n"  java="class ValidPalindrome_III {\n    public static void $1(s, k) {\n        val dp = Array(s.length) { IntArray(s.length) { 0 } }\n\n        public static void $1(start, length) {\n            val end = start + length - 1\n            return when {\n                length in 0..1 -> length\n                dp[start][end] != 0 -> dp[start][end]\n                s[start] == s[end] -> lps(start + 1, length - 2) + 2\n                else -> maxOf(lps(start + 1, length - 1), lps(start, length - 1))\n            }.also {\n                dp[start][end] = it\n            }\n        }\n\n        // Calculate the length of the longest palindromic subsequence\n        val lpsLength = lps(0, s.length)\n\n        // Check if the number of deletions required is less than or equal to k\n        return (s.length - lpsLength) <= k\n    }\n}"  python="class ValidPalindrome_III {\n    def $1(s: str, k: int): bool {\n        $1 = Array(s.length) { list[int](s.length) { 0 } }\n\n        def $1(start: int, length: int): int {\n            $1 = start + length - 1\n            return when {\n                length in 0..1 -> length\n                dp[start][end] != 0 -> dp[start][end]\n                s[start] == s[end] -> lps(start + 1, length - 2) + 2\n                else -> max(lps(start + 1, length - 1), lps(start, length - 1))\n            }.also {\n                dp[start][end] = it\n            }\n        }\n\n        // Calculate the length of the longest palindromic subsequence\n        $1 = lps(0, s.length)\n\n        // Check if the number of deletions required is less than or equal to k\n        return (s.length - lpsLength) <= k\n    }\n}"  rust="class ValidPalindrome_III {\n    fn $1(s -> String, k -> i32) -> bool {\n        val dp = Array(s.length) { Vec<i32>(s.length) { 0 } }\n\n        fn $1(start -> i32, length -> i32) -> i32 {\n            val end = start + length - 1\n            return when {\n                length in 0..1 -> length\n                dp[start][end] != 0 -> dp[start][end]\n                s[start] == s[end] -> lps(start + 1, length - 2) + 2\n                else -> max(lps(start + 1, length - 1), lps(start, length - 1))\n            }.also {\n                dp[start][end] = it\n            }\n        }\n\n        // Calculate the length of the longest palindromic subsequence\n        val lpsLength = lps(0, s.length)\n\n        // Check if the number of deletions required is less than or equal to k\n        return (s.length - lpsLength) <= k\n    }\n}"  cpp="class ValidPalindrome_III {\n    $1(s, k) {\n        auto $1 = Array(s.length) { vector<int>(s.length) { 0 } }\n\n        $1(start, length) {\n            auto $1 = start + length - 1\n            return when {\n                length in 0..1 -> length\n                dp[start][end] != 0 -> dp[start][end]\n                s[start] == s[end] -> lps(start + 1, length - 2) + 2\n                else -> max(lps(start + 1, length - 1), lps(start, length - 1))\n            }.also {\n                dp[start][end] = it\n            }\n        }\n\n        // Calculate the length of the longest palindromic subsequence\n        auto $1 = lps(0, s.length)\n\n        // Check if the number of deletions required is less than or equal to k\n        return (s.length - lpsLength) <= k\n    }\n}" %}

---

## ValidPalindrome_III_SpaceOptimized

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/dynamic_programming/ValidPalindrome_III_SpaceOptimized.kt`

{% include code-tabs.html  kotlin="package string.dynamic_programming\n\nclass ValidPalindrome_III_SpaceOptimized {\n    fun isValidPalindrome(s: String, k: Int): Boolean {\n        val n = s.length\n        val dp = IntArray(n) { 1 } // Initialize with 1 because each character is a palindrome of length 1\n\n        for (i in n - 1 downTo 0) {\n            var prev = 0 // Stores the value of dp[i+1][j-1] from the 2D DP approach\n            for (j in i + 1 until n) {\n                val temp = dp[j] // Store the current dp[j] before updating it\n                if (s[i] == s[j]) {\n                    dp[j] = 2 + prev // If characters match, add 2 to the LPS length of the inner substring\n                } else {\n                    dp[j] = maxOf(dp[j], dp[j - 1]) // Otherwise, take the maximum of excluding s[i] or s[j]\n                }\n                prev = temp // Update prev for the next iteration\n            }\n        }\n\n        val lpsLength = dp[n - 1] // The length of the LPS for the entire string\n        return (n - lpsLength) <= k\n    }\n}\n"  java="class ValidPalindrome_III_SpaceOptimized {\n    public static void $1(s, k) {\n        val n = s.length\n        val dp = IntArray(n) { 1 } // Initialize with 1 because each character is a palindrome of length 1\n\n        for (i in n - 1 downTo 0) {\n            var prev = 0 // Stores the value of dp[i+1][j-1] from the 2D DP approach\n            for (j in i + 1 until n) {\n                val temp = dp[j] // Store the current dp[j] before updating it\n                if (s[i] == s[j]) {\n                    dp[j] = 2 + prev // If characters match, add 2 to the LPS length of the inner substring\n                } else {\n                    dp[j] = maxOf(dp[j], dp[j - 1]) // Otherwise, take the maximum of excluding s[i] or s[j]\n                }\n                prev = temp // Update prev for the next iteration\n            }\n        }\n\n        val lpsLength = dp[n - 1] // The length of the LPS for the entire string\n        return (n - lpsLength) <= k\n    }\n}"  python="class ValidPalindrome_III_SpaceOptimized {\n    def $1(s: str, k: int): bool {\n        $1 = s.length\n        $1 = list[int](n) { 1 } // Initialize with 1 because each character is a palindrome of length 1\n\n        for (i in n - 1 downTo 0) {\n            $1 = 0 // Stores the value of dp[i+1][j-1] from the 2D DP approach\n            for (j in i + 1 until n) {\n                $1 = dp[j] // Store the current dp[j] before updating it\n                if (s[i] == s[j]) {\n                    dp[j] = 2 + prev // If characters match, add 2 to the LPS length of the inner substring\n                } else {\n                    dp[j] = max(dp[j], dp[j - 1]) // Otherwise, take the maximum of excluding s[i] or s[j]\n                }\n                prev = temp // Update prev for the next iteration\n            }\n        }\n\n        $1 = dp[n - 1] // The length of the LPS for the entire string\n        return (n - lpsLength) <= k\n    }\n}"  rust="class ValidPalindrome_III_SpaceOptimized {\n    fn $1(s -> String, k -> i32) -> bool {\n        val n = s.length\n        val dp = Vec<i32>(n) { 1 } // Initialize with 1 because each character is a palindrome of length 1\n\n        for (i in n - 1 downTo 0) {\n            var prev = 0 // Stores the value of dp[i+1][j-1] from the 2D DP approach\n            for (j in i + 1 until n) {\n                val temp = dp[j] // Store the current dp[j] before updating it\n                if (s[i] == s[j]) {\n                    dp[j] = 2 + prev // If characters match, add 2 to the LPS length of the inner substring\n                } else {\n                    dp[j] = max(dp[j], dp[j - 1]) // Otherwise, take the maximum of excluding s[i] or s[j]\n                }\n                prev = temp // Update prev for the next iteration\n            }\n        }\n\n        val lpsLength = dp[n - 1] // The length of the LPS for the entire string\n        return (n - lpsLength) <= k\n    }\n}"  cpp="class ValidPalindrome_III_SpaceOptimized {\n    $1(s, k) {\n        auto $1 = s.length\n        auto $1 = vector<int>(n) { 1 } // Initialize with 1 because each character is a palindrome of length 1\n\n        for (i in n - 1 downTo 0) {\n            auto $1 = 0 // Stores the value of dp[i+1][j-1] from the 2D DP approach\n            for (j in i + 1 until n) {\n                auto $1 = dp[j] // Store the current dp[j] before updating it\n                if (s[i] == s[j]) {\n                    dp[j] = 2 + prev // If characters match, add 2 to the LPS length of the inner substring\n                } else {\n                    dp[j] = max(dp[j], dp[j - 1]) // Otherwise, take the maximum of excluding s[i] or s[j]\n                }\n                prev = temp // Update prev for the next iteration\n            }\n        }\n\n        auto $1 = dp[n - 1] // The length of the LPS for the entire string\n        return (n - lpsLength) <= k\n    }\n}" %}

---

## FindAllAnagrams

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/sliding_window/FindAllAnagrams.kt`

{% include code-tabs.html  kotlin="package string.sliding_window\n\nclass FindAllAnagrams {\n    fun findAnagrams(s: String, p: String): List<Int> {\n        val result = mutableListOf<Int>()\n        if (s.length < p.length) return result\n\n        val pCount = IntArray(26)\n        val sCount = IntArray(26)\n\n        // Initialize frequency counts for p and the first window of s\n        for (i in p.indices) {\n            pCount[p[i] - 'a']++\n            sCount[s[i] - 'a']++\n        }\n\n        // Compare the initial window and check every subsequent window\n        for (i in p.length..s.length) {\n            if (pCount.contentEquals(sCount)) {\n                result.add(i - p.length)\n            }\n            if (i < s.length) {\n                sCount[s[i] - 'a']++          // Add new character to the window\n                sCount[s[i - p.length] - 'a']-- // Remove the old character from the window\n            }\n        }\n\n        return result\n    }\n}"  java="class FindAllAnagrams {\n    public static void $1(s, p): List<Int> {\n        val result = mutableListOf<Int>()\n        if (s.length < p.length) return result\n\n        val pCount = IntArray(26)\n        val sCount = IntArray(26)\n\n        // Initialize frequency counts for p and the first window of s\n        for (i in p.indices) {\n            pCount[p[i] - 'a']++\n            sCount[s[i] - 'a']++\n        }\n\n        // Compare the initial window and check every subsequent window\n        for (i in p.length..s.length) {\n            if (pCount.contentEquals(sCount)) {\n                result.add(i - p.length)\n            }\n            if (i < s.length) {\n                sCount[s[i] - 'a']++          // Add new character to the window\n                sCount[s[i - p.length] - 'a']-- // Remove the old character from the window\n            }\n        }\n\n        return result\n    }\n}"  python="class FindAllAnagrams {\n    def $1(s: str, p: str): List<Int> {\n        $1 = mutableListOf<Int>()\n        if (s.length < p.length) return result\n\n        $1 = list[int](26)\n        $1 = list[int](26)\n\n        // Initialize frequency counts for p and the first window of s\n        for (i in p.indices) {\n            pCount[p[i] - 'a']++\n            sCount[s[i] - 'a']++\n        }\n\n        // Compare the initial window and check every subsequent window\n        for (i in p.length..s.length) {\n            if (pCount.contentEquals(sCount)) {\n                result.add(i - p.length)\n            }\n            if (i < s.length) {\n                sCount[s[i] - 'a']++          // Add new character to the window\n                sCount[s[i - p.length] - 'a']-- // Remove the old character from the window\n            }\n        }\n\n        return result\n    }\n}"  rust="class FindAllAnagrams {\n    fn $1(s -> String, p -> String): List<Int> {\n        val result = mutableListOf<Int>()\n        if (s.length < p.length) return result\n\n        val pCount = Vec<i32>(26)\n        val sCount = Vec<i32>(26)\n\n        // Initialize frequency counts for p and the first window of s\n        for (i in p.indices) {\n            pCount[p[i] - 'a']++\n            sCount[s[i] - 'a']++\n        }\n\n        // Compare the initial window and check every subsequent window\n        for (i in p.length..s.length) {\n            if (pCount.contentEquals(sCount)) {\n                result.add(i - p.length)\n            }\n            if (i < s.length) {\n                sCount[s[i] - 'a']++          // Add new character to the window\n                sCount[s[i - p.length] - 'a']-- // Remove the old character from the window\n            }\n        }\n\n        return result\n    }\n}"  cpp="class FindAllAnagrams {\n    $1(s, p): List<Int> {\n        auto $1 = mutableListOf<Int>()\n        if (s.length < p.length) return result\n\n        auto $1 = vector<int>(26)\n        auto $1 = vector<int>(26)\n\n        // Initialize frequency counts for p and the first window of s\n        for (i in p.indices) {\n            pCount[p[i] - 'a']++\n            sCount[s[i] - 'a']++\n        }\n\n        // Compare the initial window and check every subsequent window\n        for (i in p.length..s.length) {\n            if (pCount.contentEquals(sCount)) {\n                result.add(i - p.length)\n            }\n            if (i < s.length) {\n                sCount[s[i] - 'a']++          // Add new character to the window\n                sCount[s[i - p.length] - 'a']-- // Remove the old character from the window\n            }\n        }\n\n        return result\n    }\n}" %}

---

## MaximumNumberofVowelsinSubstringofGivenLength

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/sliding_window/MaximumNumberofVowelsinSubstringofGivenLength.kt`

{% include code-tabs.html  kotlin="package string.sliding_window\n\nclass MaximumNumberofVowelsinSubstringofGivenLength {\n    fun maxVowels(s: String, k: Int): Int {\n        var (vowelWindowCount, maxCount) = Pair(0,0)\n        val vowels = setOf('a', 'e', 'i', 'o', 'u')\n\n        for (i in 0 until s.length) {\n\n            if (s[i] in vowels ) {\n                vowelWindowCount++\n            }\n\n            if ( i >= k -1) {\n                maxCount = maxOf(maxCount, vowelWindowCount)\n\n                if (s[i - k + 1] in vowels)\n                    vowelWindowCount--\n            }\n        }\n\n        return maxCount\n    }\n}"  java="class MaximumNumberofVowelsinSubstringofGivenLength {\n    public static void $1(s, k) {\n        var (vowelWindowCount, maxCount) = Pair(0,0)\n        val vowels = setOf('a', 'e', 'i', 'o', 'u')\n\n        for (i in 0 until s.length) {\n\n            if (s[i] in vowels ) {\n                vowelWindowCount++\n            }\n\n            if ( i >= k -1) {\n                maxCount = maxOf(maxCount, vowelWindowCount)\n\n                if (s[i - k + 1] in vowels)\n                    vowelWindowCount--\n            }\n        }\n\n        return maxCount\n    }\n}"  python="class MaximumNumberofVowelsinSubstringofGivenLength {\n    def $1(s: str, k: int): int {\n        var (vowelWindowCount, maxCount) = Pair(0,0)\n        $1 = setOf('a', 'e', 'i', 'o', 'u')\n\n        for (i in 0 until s.length) {\n\n            if (s[i] in vowels ) {\n                vowelWindowCount++\n            }\n\n            if ( i >= k -1) {\n                maxCount = max(maxCount, vowelWindowCount)\n\n                if (s[i - k + 1] in vowels)\n                    vowelWindowCount--\n            }\n        }\n\n        return maxCount\n    }\n}"  rust="class MaximumNumberofVowelsinSubstringofGivenLength {\n    fn $1(s -> String, k -> i32) -> i32 {\n        var (vowelWindowCount, maxCount) = Pair(0,0)\n        val vowels = setOf('a', 'e', 'i', 'o', 'u')\n\n        for (i in 0 until s.length) {\n\n            if (s[i] in vowels ) {\n                vowelWindowCount++\n            }\n\n            if ( i >= k -1) {\n                maxCount = max(maxCount, vowelWindowCount)\n\n                if (s[i - k + 1] in vowels)\n                    vowelWindowCount--\n            }\n        }\n\n        return maxCount\n    }\n}"  cpp="class MaximumNumberofVowelsinSubstringofGivenLength {\n    $1(s, k) {\n        var (vowelWindowCount, maxCount) = Pair(0,0)\n        auto $1 = setOf('a', 'e', 'i', 'o', 'u')\n\n        for (i in 0 until s.length) {\n\n            if (s[i] in vowels ) {\n                vowelWindowCount++\n            }\n\n            if ( i >= k -1) {\n                maxCount = max(maxCount, vowelWindowCount)\n\n                if (s[i - k + 1] in vowels)\n                    vowelWindowCount--\n            }\n        }\n\n        return maxCount\n    }\n}" %}

---

## MinimumWindowSubstring

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/sliding_window/MinimumWindowSubstring.kt`

{% include code-tabs.html  kotlin="package string.sliding_window\n\nclass MinimumWindowSubstring {\n    fun minWindow(s: String, t: String): String {\n        // Return early if either string is empty\n        if (s.isEmpty() || t.isEmpty()) return \"\"\n\n        // Frequency map for characters in t (required characters)\n        val required = mutableMapOf<Char, Int>()\n        t.forEach { required[it] = required.getOrDefault(it, 0) + 1 }\n\n        // Frequency map for the current window in s\n        val window = mutableMapOf<Char, Int>()\n\n        // Track the number of characters matched from the required set\n        var matched = 0\n        var (left, right) = 0 to 0  // Destructuring initialization for left and right pointers\n\n        // Initialize minLength and minStart in a single line\n        var (minLength, minStart) = Int.MAX_VALUE to 0\n\n        // Sliding window logic\n        while (right < s.length) {\n            // Expand the window by moving the right pointer\n            val rightChar = s[right]\n            window[rightChar] = window.getOrDefault(rightChar, 0) + 1\n\n            // Check if current window has all required characters in needed quantity\n            when {\n                required.containsKey(rightChar) && window[rightChar] == required[rightChar] -> matched++\n            }\n\n            // Shrink the window when all required characters are matched\n            while (matched == required.size) {\n                val currentLength = right - left + 1\n                if (currentLength < minLength) {\n                    minLength = currentLength\n                    minStart = left\n                }\n\n                // Move left pointer to shrink the window\n                val leftChar = s[left]\n                window[leftChar] = window[leftChar]!! - 1\n\n                when {\n                    required.containsKey(leftChar) && window[leftChar]!! < required[leftChar]!! -> matched--\n                }\n\n                left++\n            }\n\n            // Move right pointer to expand the window\n            right++\n        }\n\n        // Return the result\n        return if (minLength == Int.MAX_VALUE) \"\" else s.substring(minStart, minStart + minLength)\n    }\n}\n"  java="class MinimumWindowSubstring {\n    public static void $1(s, t) {\n        // Return early if either string is empty\n        if (s.isEmpty() || t.isEmpty()) return \"\"\n\n        // Frequency map for characters in t (required characters)\n        val required = mutableMapOf<Char, Int>()\n        t.forEach { required[it] = required.getOrDefault(it, 0) + 1 }\n\n        // Frequency map for the current window in s\n        val window = mutableMapOf<Char, Int>()\n\n        // Track the number of characters matched from the required set\n        var matched = 0\n        var (left, right) = 0 to 0  // Destructuring initialization for left and right pointers\n\n        // Initialize minLength and minStart in a single line\n        var (minLength, minStart) = Int.MAX_VALUE to 0\n\n        // Sliding window logic\n        while (right < s.length) {\n            // Expand the window by moving the right pointer\n            val rightChar = s[right]\n            window[rightChar] = window.getOrDefault(rightChar, 0) + 1\n\n            // Check if current window has all required characters in needed quantity\n            when {\n                required.containsKey(rightChar) && window[rightChar] == required[rightChar] -> matched++\n            }\n\n            // Shrink the window when all required characters are matched\n            while (matched == required.size) {\n                val currentLength = right - left + 1\n                if (currentLength < minLength) {\n                    minLength = currentLength\n                    minStart = left\n                }\n\n                // Move left pointer to shrink the window\n                val leftChar = s[left]\n                window[leftChar] = window[leftChar]!! - 1\n\n                when {\n                    required.containsKey(leftChar) && window[leftChar]!! < required[leftChar]!! -> matched--\n                }\n\n                left++\n            }\n\n            // Move right pointer to expand the window\n            right++\n        }\n\n        // Return the result\n        return if (minLength == Int.MAX_VALUE) \"\" else s.substring(minStart, minStart + minLength)\n    }\n}"  python="class MinimumWindowSubstring {\n    def $1(s: str, t: str): str {\n        // Return early if either string is empty\n        if (s.isEmpty() || t.isEmpty()) return \"\"\n\n        // Frequency map for characters in t (required characters)\n        $1 = mutableMapOf<Char, Int>()\n        t.forEach { required[it] = required.getOrDefault(it, 0) + 1 }\n\n        // Frequency map for the current window in s\n        $1 = mutableMapOf<Char, Int>()\n\n        // Track the number of characters matched from the required set\n        $1 = 0\n        var (left, right) = 0 to 0  // Destructuring initialization for left and right pointers\n\n        // Initialize minLength and minStart in a single line\n        var (minLength, minStart) = Int.MAX_VALUE to 0\n\n        // Sliding window logic\n        while (right < s.length) {\n            // Expand the window by moving the right pointer\n            $1 = s[right]\n            window[rightChar] = window.getOrDefault(rightChar, 0) + 1\n\n            // Check if current window has all required characters in needed quantity\n            when {\n                required.containsKey(rightChar) && window[rightChar] == required[rightChar] -> matched++\n            }\n\n            // Shrink the window when all required characters are matched\n            while (matched == required.size) {\n                $1 = right - left + 1\n                if (currentLength < minLength) {\n                    minLength = currentLength\n                    minStart = left\n                }\n\n                // Move left pointer to shrink the window\n                $1 = s[left]\n                window[leftChar] = window[leftChar]!! - 1\n\n                when {\n                    required.containsKey(leftChar) && window[leftChar]!! < required[leftChar]!! -> matched--\n                }\n\n                left++\n            }\n\n            // Move right pointer to expand the window\n            right++\n        }\n\n        // Return the result\n        return if (minLength == Int.MAX_VALUE) \"\" else s.substring(minStart, minStart + minLength)\n    }\n}"  rust="class MinimumWindowSubstring {\n    fn $1(s -> String, t -> String) -> String {\n        // Return early if either string is empty\n        if (s.isEmpty() || t.isEmpty()) return \"\"\n\n        // Frequency map for characters in t (required characters)\n        val required = mutableMapOf<Char, Int>()\n        t.forEach { required[it] = required.getOrDefault(it, 0) + 1 }\n\n        // Frequency map for the current window in s\n        val window = mutableMapOf<Char, Int>()\n\n        // Track the number of characters matched from the required set\n        var matched = 0\n        var (left, right) = 0 to 0  // Destructuring initialization for left and right pointers\n\n        // Initialize minLength and minStart in a single line\n        var (minLength, minStart) = Int.MAX_VALUE to 0\n\n        // Sliding window logic\n        while (right < s.length) {\n            // Expand the window by moving the right pointer\n            val rightChar = s[right]\n            window[rightChar] = window.getOrDefault(rightChar, 0) + 1\n\n            // Check if current window has all required characters in needed quantity\n            when {\n                required.containsKey(rightChar) && window[rightChar] == required[rightChar] -> matched++\n            }\n\n            // Shrink the window when all required characters are matched\n            while (matched == required.size) {\n                val currentLength = right - left + 1\n                if (currentLength < minLength) {\n                    minLength = currentLength\n                    minStart = left\n                }\n\n                // Move left pointer to shrink the window\n                val leftChar = s[left]\n                window[leftChar] = window[leftChar]!! - 1\n\n                when {\n                    required.containsKey(leftChar) && window[leftChar]!! < required[leftChar]!! -> matched--\n                }\n\n                left++\n            }\n\n            // Move right pointer to expand the window\n            right++\n        }\n\n        // Return the result\n        return if (minLength == Int.MAX_VALUE) \"\" else s.substring(minStart, minStart + minLength)\n    }\n}"  cpp="class MinimumWindowSubstring {\n    $1(s, t) {\n        // Return early if either string is empty\n        if (s.isEmpty() || t.isEmpty()) return \"\"\n\n        // Frequency map for characters in t (required characters)\n        auto $1 = mutableMapOf<Char, Int>()\n        t.forEach { required[it] = required.getOrDefault(it, 0) + 1 }\n\n        // Frequency map for the current window in s\n        auto $1 = mutableMapOf<Char, Int>()\n\n        // Track the number of characters matched from the required set\n        auto $1 = 0\n        var (left, right) = 0 to 0  // Destructuring initialization for left and right pointers\n\n        // Initialize minLength and minStart in a single line\n        var (minLength, minStart) = Int.MAX_VALUE to 0\n\n        // Sliding window logic\n        while (right < s.length) {\n            // Expand the window by moving the right pointer\n            auto $1 = s[right]\n            window[rightChar] = window.getOrDefault(rightChar, 0) + 1\n\n            // Check if current window has all required characters in needed quantity\n            when {\n                required.containsKey(rightChar) && window[rightChar] == required[rightChar] -> matched++\n            }\n\n            // Shrink the window when all required characters are matched\n            while (matched == required.size) {\n                auto $1 = right - left + 1\n                if (currentLength < minLength) {\n                    minLength = currentLength\n                    minStart = left\n                }\n\n                // Move left pointer to shrink the window\n                auto $1 = s[left]\n                window[leftChar] = window[leftChar]!! - 1\n\n                when {\n                    required.containsKey(leftChar) && window[leftChar]!! < required[leftChar]!! -> matched--\n                }\n\n                left++\n            }\n\n            // Move right pointer to expand the window\n            right++\n        }\n\n        // Return the result\n        return if (minLength == Int.MAX_VALUE) \"\" else s.substring(minStart, minStart + minLength)\n    }\n}" %}

---

## BreakAPalindrome

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/greedy/BreakAPalindrome.kt`

{% include code-tabs.html  kotlin="package string.greedy\n\nclass BreakAPalindrome {\n    fun breakPalindrome(palindrome: String): String {\n        if (palindrome.length == 1) return \"\"\n\n        val arr = palindrome.toCharArray()\n        for (i in 0 until palindrome.length / 2) {\n            if (arr[i] != 'a') {\n                arr[i] = 'a'\n                return String(arr)\n            }\n        }\n        // If all characters are 'a', change the last character to 'b'\n        arr[arr.lastIndex] = 'b'\n        return String(arr)\n    }\n}"  java="class BreakAPalindrome {\n    public static void $1(palindrome) {\n        if (palindrome.length == 1) return \"\"\n\n        val arr = palindrome.toCharArray()\n        for (i in 0 until palindrome.length / 2) {\n            if (arr[i] != 'a') {\n                arr[i] = 'a'\n                return String(arr)\n            }\n        }\n        // If all characters are 'a', change the last character to 'b'\n        arr[arr.lastIndex] = 'b'\n        return String(arr)\n    }\n}"  python="class BreakAPalindrome {\n    def $1(palindrome: str): str {\n        if (palindrome.length == 1) return \"\"\n\n        $1 = palindrome.toCharArray()\n        for (i in 0 until palindrome.length / 2) {\n            if (arr[i] != 'a') {\n                arr[i] = 'a'\n                return String(arr)\n            }\n        }\n        // If all characters are 'a', change the last character to 'b'\n        arr[arr.lastIndex] = 'b'\n        return String(arr)\n    }\n}"  rust="class BreakAPalindrome {\n    fn $1(palindrome -> String) -> String {\n        if (palindrome.length == 1) return \"\"\n\n        val arr = palindrome.toCharArray()\n        for (i in 0 until palindrome.length / 2) {\n            if (arr[i] != 'a') {\n                arr[i] = 'a'\n                return String(arr)\n            }\n        }\n        // If all characters are 'a', change the last character to 'b'\n        arr[arr.len() - 1] = 'b'\n        return String(arr)\n    }\n}"  cpp="class BreakAPalindrome {\n    $1(palindrome) {\n        if (palindrome.length == 1) return \"\"\n\n        auto $1 = palindrome.toCharArray()\n        for (i in 0 until palindrome.length / 2) {\n            if (arr[i] != 'a') {\n                arr[i] = 'a'\n                return String(arr)\n            }\n        }\n        // If all characters are 'a', change the last character to 'b'\n        arr[arr.size() - 1] = 'b'\n        return String(arr)\n    }\n}" %}

---

## CustomSortString

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/sorting/CustomSortString.kt`

{% include code-tabs.html  kotlin="package string.sorting\n\nclass CustomSortString {\n    fun customSortString(order: String, s: String): String {\n        val orderMap = mutableMapOf<Char, Int>()\n        order.forEachIndexed{ index, ch -> orderMap[ch] = index }\n\n        return s.toCharArray().sortedBy{ ch -> orderMap[ch] ?: Int.MAX_VALUE }\n            .joinToString(\"\")\n    }\n}"  java="class CustomSortString {\n    public static void $1(order, s) {\n        val orderMap = mutableMapOf<Char, Int>()\n        order.forEachIndexed{ index, ch -> orderMap[ch] = index }\n\n        return s.toCharArray().sortedBy{ ch -> orderMap[ch] ?.MAX_VALUE }\n            .joinToString(\"\")\n    }\n}"  python="class CustomSortString {\n    def $1(order: str, s: str): str {\n        $1 = mutableMapOf<Char, Int>()\n        order.forEachIndexed{ index, ch -> orderMap[ch] = index }\n\n        return s.toCharArray().sortedBy{ ch -> orderMap[ch] ?: int.MAX_VALUE }\n            .joinToString(\"\")\n    }\n}"  rust="class CustomSortString {\n    fn $1(order -> String, s -> String) -> String {\n        val orderMap = mutableMapOf<Char, Int>()\n        order.forEachIndexed{ index, ch -> orderMap[ch] = index }\n\n        return s.toCharArray().sortedBy{ ch -> orderMap[ch] ? -> i32.MAX_VALUE }\n            .joinToString(\"\")\n    }\n}"  cpp="class CustomSortString {\n    $1(order, s) {\n        auto $1 = mutableMapOf<Char, Int>()\n        order.forEachIndexed{ index, ch -> orderMap[ch] = index }\n\n        return s.toCharArray().sortedBy{ ch -> orderMap[ch] ?.MAX_VALUE }\n            .joinToString(\"\")\n    }\n}" %}

---

## CustomSortString_Linear

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/sorting/CustomSortString_Linear.kt`

{% include code-tabs.html  kotlin="package string.sorting\n\nclass CustomSortString_Linear {\n    fun customSortString(order: String, s: String): String {\n        // Count frequency of each character\n        val countMap = s.groupingBy { it }.eachCount().toMutableMap()\n\n        val result = StringBuilder()\n        order.forEach { ch ->\n            countMap[ch]?.let {\n                result.append(ch.toString().repeat(it))\n                countMap.remove(ch)\n            }\n        }\n\n        // Append Remaining characters which are not in order\n        countMap.forEach { (ch, count) -> result.append(ch.toString().repeat(count)) }\n        return result.toString()\n    }\n}"  java="class CustomSortString_Linear {\n    public static void $1(order, s) {\n        // Count frequency of each character\n        val countMap = s.groupingBy { it }.eachCount().toMutableMap()\n\n        val result = StringBuilder()\n        order.forEach { ch ->\n            countMap[ch]?.let {\n                result.append(ch.toString().repeat(it))\n                countMap.remove(ch)\n            }\n        }\n\n        // Append Remaining characters which are not in order\n        countMap.forEach { (ch, count) -> result.append(ch.toString().repeat(count)) }\n        return result.toString()\n    }\n}"  python="class CustomSortString_Linear {\n    def $1(order: str, s: str): str {\n        // Count frequency of each character\n        $1 = s.groupingBy { it }.eachCount().toMutableMap()\n\n        $1 = StringBuilder()\n        order.forEach { ch ->\n            countMap[ch]?.let {\n                result.append(ch.toString().repeat(it))\n                countMap.remove(ch)\n            }\n        }\n\n        // Append Remaining characters which are not in order\n        countMap.forEach { (ch, count) -> result.append(ch.toString().repeat(count)) }\n        return result.toString()\n    }\n}"  rust="class CustomSortString_Linear {\n    fn $1(order -> String, s -> String) -> String {\n        // Count frequency of each character\n        val countMap = s.groupingBy { it }.eachCount().toMutableMap()\n\n        val result = StringBuilder()\n        order.forEach { ch ->\n            countMap[ch]?.let {\n                result.append(ch.toString().repeat(it))\n                countMap.remove(ch)\n            }\n        }\n\n        // Append Remaining characters which are not in order\n        countMap.forEach { (ch, count) -> result.append(ch.toString().repeat(count)) }\n        return result.toString()\n    }\n}"  cpp="class CustomSortString_Linear {\n    $1(order, s) {\n        // Count frequency of each character\n        auto $1 = s.groupingBy { it }.eachCount().toMutableMap()\n\n        auto $1 = StringBuilder()\n        order.forEach { ch ->\n            countMap[ch]?.let {\n                result.append(ch.toString().repeat(it))\n                countMap.remove(ch)\n            }\n        }\n\n        // Append Remaining characters which are not in order\n        countMap.forEach { (ch, count) -> result.append(ch.toString().repeat(count)) }\n        return result.toString()\n    }\n}" %}

---

## DetermineIfStringsAreClose

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/hashtable/DetermineIfStringsAreClose.kt`

{% include code-tabs.html  kotlin="package string.hashtable\n\nclass DetermineIfStringsAreClose {\n    fun closeStrings(word1: String, word2: String): Boolean {\n        if (word1.length != word2.length)\n            return false\n\n        val freq1 = mutableMapOf<Char, Int>().apply {\n            word1.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n        val freq2 = mutableMapOf<Char, Int>().apply {\n            word2.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n\n        return when {\n            freq1.keys != freq2.keys -> false\n            else -> freq1.values.sorted() == freq2.values.sorted()\n        }\n    }\n}"  java="class DetermineIfStringsAreClose {\n    public static void $1(word1, word2) {\n        if (word1.length != word2.length)\n            return false\n\n        val freq1 = mutableMapOf<Char, Int>().apply {\n            word1.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n        val freq2 = mutableMapOf<Char, Int>().apply {\n            word2.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n\n        return when {\n            freq1.keys != freq2.keys -> false\n            else -> freq1.values.sorted() == freq2.values.sorted()\n        }\n    }\n}"  python="class DetermineIfStringsAreClose {\n    def $1(word1: str, word2: str): bool {\n        if (word1.length != word2.length)\n            return False\n\n        $1 = mutableMapOf<Char, Int>().apply {\n            word1.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n        $1 = mutableMapOf<Char, Int>().apply {\n            word2.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n\n        return when {\n            freq1.keys != freq2.keys -> False\n            else -> freq1.values.sorted() == freq2.values.sorted()\n        }\n    }\n}"  rust="class DetermineIfStringsAreClose {\n    fn $1(word1 -> String, word2 -> String) -> bool {\n        if (word1.length != word2.length)\n            return false\n\n        val freq1 = mutableMapOf<Char, Int>().apply {\n            word1.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n        val freq2 = mutableMapOf<Char, Int>().apply {\n            word2.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n\n        return when {\n            freq1.keys != freq2.keys -> false\n            else -> freq1.values.sorted() == freq2.values.sorted()\n        }\n    }\n}"  cpp="class DetermineIfStringsAreClose {\n    $1(word1, word2) {\n        if (word1.length != word2.length)\n            return false\n\n        auto $1 = mutableMapOf<Char, Int>().apply {\n            word1.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n        auto $1 = mutableMapOf<Char, Int>().apply {\n            word2.forEach { ch -> this[ch] = this.getOrDefault(ch, 0) + 1}\n        }\n\n        return when {\n            freq1.keys != freq2.keys -> false\n            else -> freq1.values.sorted() == freq2.values.sorted()\n        }\n    }\n}" %}

---

## GroupShiftedStrings

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/hashtable/GroupShiftedStrings.kt`

{% include code-tabs.html  kotlin="package string.hashtable\n\nclass GroupShiftedStrings {\n    fun groupStrings(strings: Array<String>): List<List<String>> {\n        val map = mutableMapOf<String, MutableList<String>>()\n\n        for (str in strings) {\n            val key = getKey(str)\n            map.computeIfAbsent(key) { mutableListOf() }.add(str)\n        }\n\n        return map.values.toList()\n    }\n\n    private fun getKey(str: String): String {\n        return buildString {\n            for (i in 1 until str.length) {\n                append( ((str[i] - str[i - 1] + 26) % 26).toString() )\n            }\n        }\n    }\n}"  java="class GroupShiftedStrings {\n    public static void $1(strings: Array<String>): List<List<String>> {\n        val map = mutableMapOf<String, MutableList<String>>()\n\n        for (str in strings) {\n            val key = getKey(str)\n            map.computeIfAbsent(key) { mutableListOf() }.add(str)\n        }\n\n        return map.values.toList()\n    }\n\n    private public static void $1(str) {\n        return buildString {\n            for (i in 1 until str.length) {\n                append( ((str[i] - str[i - 1] + 26) % 26).toString() )\n            }\n        }\n    }\n}"  python="class GroupShiftedStrings {\n    def $1(strings: Array<String>): List<List<String>> {\n        $1 = mutableMapOf<String, MutableList<String>>()\n\n        for (str in strings) {\n            $1 = getKey(str)\n            map.computeIfAbsent(key) { mutableListOf() }.add(str)\n        }\n\n        return map.values.toList()\n    }\n\n    private def $1(str: str): str {\n        return buildString {\n            for (i in 1 until str.length) {\n                append( ((str[i] - str[i - 1] + 26) % 26).toString() )\n            }\n        }\n    }\n}"  rust="class GroupShiftedStrings {\n    fn $1(strings: Array<String>): List<List<String>> {\n        val map = mutableMapOf<String, MutableList<String>>()\n\n        for (str in strings) {\n            val key = getKey(str)\n            map.computeIfAbsent(key) { mutableListOf() }.add(str)\n        }\n\n        return map.values.toList()\n    }\n\n    private fn $1(str -> String) -> String {\n        return buildString {\n            for (i in 1 until str.length) {\n                append( ((str[i] - str[i - 1] + 26) % 26).toString() )\n            }\n        }\n    }\n}"  cpp="class GroupShiftedStrings {\n    $1(strings: Array<String>): List<List<String>> {\n        auto $1 = mutableMapOf<String, MutableList<String>>()\n\n        for (str in strings) {\n            auto $1 = getKey(str)\n            map.computeIfAbsent(key) { mutableListOf() }.add(str)\n        }\n\n        return map.values.toList()\n    }\n\n    private $1(str) {\n        return buildString {\n            for (i in 1 until str.length) {\n                append( ((str[i] - str[i - 1] + 26) % 26).toString() )\n            }\n        }\n    }\n}" %}

---

## PermutationsInString

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/hashtable/PermutationsInString.kt`

{% include code-tabs.html  kotlin="package string.hashtable\n\nclass PermutationsInString {\n    fun checkInclusion(s1: String, s2: String): Boolean {\n        val targetFreq = IntArray(26)\n        val windowFreq = IntArray(26)\n\n        for(c in s1) targetFreq[c - 'a']++\n\n        for (i in s2.indices) {\n            windowFreq[s2[i] - 'a']++\n            if (i >= s1.length) {\n                windowFreq[s2[i - s1.length] - 'a']--\n            }\n            if (targetFreq.contentEquals(windowFreq))\n                return true\n        }\n\n        return false\n    }\n}\n"  java="class PermutationsInString {\n    public static void $1(s1, s2) {\n        val targetFreq = IntArray(26)\n        val windowFreq = IntArray(26)\n\n        for(c in s1) targetFreq[c - 'a']++\n\n        for (i in s2.indices) {\n            windowFreq[s2[i] - 'a']++\n            if (i >= s1.length) {\n                windowFreq[s2[i - s1.length] - 'a']--\n            }\n            if (targetFreq.contentEquals(windowFreq))\n                return true\n        }\n\n        return false\n    }\n}"  python="class PermutationsInString {\n    def $1(s1: str, s2: str): bool {\n        $1 = list[int](26)\n        $1 = list[int](26)\n\n        for(c in s1) targetFreq[c - 'a']++\n\n        for (i in s2.indices) {\n            windowFreq[s2[i] - 'a']++\n            if (i >= s1.length) {\n                windowFreq[s2[i - s1.length] - 'a']--\n            }\n            if (targetFreq.contentEquals(windowFreq))\n                return True\n        }\n\n        return False\n    }\n}"  rust="class PermutationsInString {\n    fn $1(s1 -> String, s2 -> String) -> bool {\n        val targetFreq = Vec<i32>(26)\n        val windowFreq = Vec<i32>(26)\n\n        for(c in s1) targetFreq[c - 'a']++\n\n        for (i in s2.indices) {\n            windowFreq[s2[i] - 'a']++\n            if (i >= s1.length) {\n                windowFreq[s2[i - s1.length] - 'a']--\n            }\n            if (targetFreq.contentEquals(windowFreq))\n                return true\n        }\n\n        return false\n    }\n}"  cpp="class PermutationsInString {\n    $1(s1, s2) {\n        auto $1 = vector<int>(26)\n        auto $1 = vector<int>(26)\n\n        for(c in s1) targetFreq[c - 'a']++\n\n        for (i in s2.indices) {\n            windowFreq[s2[i] - 'a']++\n            if (i >= s1.length) {\n                windowFreq[s2[i - s1.length] - 'a']--\n            }\n            if (targetFreq.contentEquals(windowFreq))\n                return true\n        }\n\n        return false\n    }\n}" %}

---

## UniqueLength3PalindromicSubsequence

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/hashtable/UniqueLength3PalindromicSubsequence.kt`

{% include code-tabs.html  kotlin="package string.hashtable\n\nclass UniqueLength3PalindromicSubsequence {\n    fun countPalindromicSubsequence(s: String): Int {\n        val charPositions = mutableMapOf<Char, MutableList<Int>>()\n\n        // Step 1: Record positions of each character\n        s.forEachIndexed { index, char ->\n            charPositions.computeIfAbsent(char) { mutableListOf() }.add(index)\n        }\n\n        var count = 0\n\n        // Step 2: Process each character's occurrences\n        for ((char, positions) in charPositions) {\n            val start = positions.first()\n            val end = positions.last()\n\n            // Skip if there's no room between the first and last occurrence\n            if (end - start <= 1) continue\n\n            val distinctChars = mutableSetOf<Char>()\n\n            // Count distinct characters between first and last occurrence\n            for (i in start + 1 until end) {\n                distinctChars.add(s[i])\n            }\n\n            count += distinctChars.size\n        }\n\n        return count\n    }\n}"  java="class UniqueLength3PalindromicSubsequence {\n    public static void $1(s) {\n        val charPositions = mutableMapOf<Char, MutableList<Int>>()\n\n        // Step 1: Record positions of each character\n        s.forEachIndexed { index, char ->\n            charPositions.computeIfAbsent(char) { mutableListOf() }.add(index)\n        }\n\n        var count = 0\n\n        // Step 2: Process each character's occurrences\n        for ((char, positions) in charPositions) {\n            val start = positions.first()\n            val end = positions.last()\n\n            // Skip if there's no room between the first and last occurrence\n            if (end - start <= 1) continue\n\n            val distinctChars = mutableSetOf<Char>()\n\n            // Count distinct characters between first and last occurrence\n            for (i in start + 1 until end) {\n                distinctChars.add(s[i])\n            }\n\n            count += distinctChars.size\n        }\n\n        return count\n    }\n}"  python="class UniqueLength3PalindromicSubsequence {\n    def $1(s: str): int {\n        $1 = mutableMapOf<Char, MutableList<Int>>()\n\n        // Step 1: Record positions of each character\n        s.forEachIndexed { index, char ->\n            charPositions.computeIfAbsent(char) { mutableListOf() }.add(index)\n        }\n\n        $1 = 0\n\n        // Step 2: Process each character's occurrences\n        for ((char, positions) in charPositions) {\n            $1 = positions.first()\n            $1 = positions.last()\n\n            // Skip if there's no room between the first and last occurrence\n            if (end - start <= 1) continue\n\n            $1 = mutableSetOf<Char>()\n\n            // Count distinct characters between first and last occurrence\n            for (i in start + 1 until end) {\n                distinctChars.add(s[i])\n            }\n\n            count += distinctChars.size\n        }\n\n        return count\n    }\n}"  rust="class UniqueLength3PalindromicSubsequence {\n    fn $1(s -> String) -> i32 {\n        val charPositions = mutableMapOf<Char, MutableList<Int>>()\n\n        // Step 1: Record positions of each character\n        s.forEachIndexed { index, char ->\n            charPositions.computeIfAbsent(char) { mutableListOf() }.add(index)\n        }\n\n        var count = 0\n\n        // Step 2: Process each character's occurrences\n        for ((char, positions) in charPositions) {\n            val start = positions.first()\n            val end = positions.last()\n\n            // Skip if there's no room between the first and last occurrence\n            if (end - start <= 1) continue\n\n            val distinctChars = mutableSetOf<Char>()\n\n            // Count distinct characters between first and last occurrence\n            for (i in start + 1 until end) {\n                distinctChars.add(s[i])\n            }\n\n            count += distinctChars.size\n        }\n\n        return count\n    }\n}"  cpp="class UniqueLength3PalindromicSubsequence {\n    $1(s) {\n        auto $1 = mutableMapOf<Char, MutableList<Int>>()\n\n        // Step 1: Record positions of each character\n        s.forEachIndexed { index, char ->\n            charPositions.computeIfAbsent(char) { mutableListOf() }.add(index)\n        }\n\n        auto $1 = 0\n\n        // Step 2: Process each character's occurrences\n        for ((char, positions) in charPositions) {\n            auto $1 = positions.first()\n            auto $1 = positions.last()\n\n            // Skip if there's no room between the first and last occurrence\n            if (end - start <= 1) continue\n\n            auto $1 = mutableSetOf<Char>()\n\n            // Count distinct characters between first and last occurrence\n            for (i in start + 1 until end) {\n                distinctChars.add(s[i])\n            }\n\n            count += distinctChars.size\n        }\n\n        return count\n    }\n}" %}

---

## UniqueSubstringWithEqualDigitFrequency

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/hashtable/UniqueSubstringWithEqualDigitFrequency.kt`

{% include code-tabs.html  kotlin="package string.hashtable\n\nclass UniqueSubstringWithEqualDigitFrequency {\n    fun equalDigitFrequency(s: String): Int {\n        val n = s.length\n        val uniqueSubstrings = mutableSetOf<String>()\n\n        // Precompute prefix frequency arrays\n        val prefixFreq = Array(n + 1) { IntArray(10) }\n        for (i in 1..n) {\n            for (d in 0..9) {\n                prefixFreq[i][d] = prefixFreq[i - 1][d]\n            }\n            prefixFreq[i][s[i - 1] - '0']++\n        }\n\n        // Iterate over all possible substrings\n        for (i in 0 until n) {\n            for (j in i + 1..n) {\n                val freq = IntArray(10)\n                for (d in 0..9) {\n                    freq[d] = prefixFreq[j][d] - prefixFreq[i][d]\n                }\n\n                if (hasEqualFrequency(freq)) {\n                    uniqueSubstrings.add(s.substring(i, j))\n                }\n            }\n        }\n\n        return uniqueSubstrings.size\n    }\n\n    private fun hasEqualFrequency(freq: IntArray): Boolean {\n        var uniqueFreq = -1\n        for (count in freq) {\n            if (count > 0) {\n                if (uniqueFreq == -1) {\n                    uniqueFreq = count\n                } else if (count != uniqueFreq) {\n                    return false\n                }\n            }\n        }\n        return true\n    }\n}"  java="class UniqueSubstringWithEqualDigitFrequency {\n    public static void $1(s) {\n        val n = s.length\n        val uniqueSubstrings = mutableSetOf<String>()\n\n        // Precompute prefix frequency arrays\n        val prefixFreq = Array(n + 1) { IntArray(10) }\n        for (i in 1..n) {\n            for (d in 0..9) {\n                prefixFreq[i][d] = prefixFreq[i - 1][d]\n            }\n            prefixFreq[i][s[i - 1] - '0']++\n        }\n\n        // Iterate over all possible substrings\n        for (i in 0 until n) {\n            for (j in i + 1..n) {\n                val freq = IntArray(10)\n                for (d in 0..9) {\n                    freq[d] = prefixFreq[j][d] - prefixFreq[i][d]\n                }\n\n                if (hasEqualFrequency(freq)) {\n                    uniqueSubstrings.add(s.substring(i, j))\n                }\n            }\n        }\n\n        return uniqueSubstrings.size\n    }\n\n    private public static void $1(freq: IntArray) {\n        var uniqueFreq = -1\n        for (count in freq) {\n            if (count > 0) {\n                if (uniqueFreq == -1) {\n                    uniqueFreq = count\n                } else if (count != uniqueFreq) {\n                    return false\n                }\n            }\n        }\n        return true\n    }\n}"  python="class UniqueSubstringWithEqualDigitFrequency {\n    def $1(s: str): int {\n        $1 = s.length\n        $1 = mutableSetOf<String>()\n\n        // Precompute prefix frequency arrays\n        $1 = Array(n + 1) { list[int](10) }\n        for (i in 1..n) {\n            for (d in 0..9) {\n                prefixFreq[i][d] = prefixFreq[i - 1][d]\n            }\n            prefixFreq[i][s[i - 1] - '0']++\n        }\n\n        // Iterate over all possible substrings\n        for (i in 0 until n) {\n            for (j in i + 1..n) {\n                $1 = list[int](10)\n                for (d in 0..9) {\n                    freq[d] = prefixFreq[j][d] - prefixFreq[i][d]\n                }\n\n                if (hasEqualFrequency(freq)) {\n                    uniqueSubstrings.add(s.substring(i, j))\n                }\n            }\n        }\n\n        return uniqueSubstrings.size\n    }\n\n    private def $1(freq: list[int]): bool {\n        $1 = -1\n        for (count in freq) {\n            if (count > 0) {\n                if (uniqueFreq == -1) {\n                    uniqueFreq = count\n                } else if (count != uniqueFreq) {\n                    return False\n                }\n            }\n        }\n        return True\n    }\n}"  rust="class UniqueSubstringWithEqualDigitFrequency {\n    fn $1(s -> String) -> i32 {\n        val n = s.length\n        val uniqueSubstrings = mutableSetOf<String>()\n\n        // Precompute prefix frequency arrays\n        val prefixFreq = Array(n + 1) { Vec<i32>(10) }\n        for (i in 1..n) {\n            for (d in 0..9) {\n                prefixFreq[i][d] = prefixFreq[i - 1][d]\n            }\n            prefixFreq[i][s[i - 1] - '0']++\n        }\n\n        // Iterate over all possible substrings\n        for (i in 0 until n) {\n            for (j in i + 1..n) {\n                val freq = Vec<i32>(10)\n                for (d in 0..9) {\n                    freq[d] = prefixFreq[j][d] - prefixFreq[i][d]\n                }\n\n                if (hasEqualFrequency(freq)) {\n                    uniqueSubstrings.add(s.substring(i, j))\n                }\n            }\n        }\n\n        return uniqueSubstrings.size\n    }\n\n    private fn $1(freq: Vec<i32>) -> bool {\n        var uniqueFreq = -1\n        for (count in freq) {\n            if (count > 0) {\n                if (uniqueFreq == -1) {\n                    uniqueFreq = count\n                } else if (count != uniqueFreq) {\n                    return false\n                }\n            }\n        }\n        return true\n    }\n}"  cpp="class UniqueSubstringWithEqualDigitFrequency {\n    $1(s) {\n        auto $1 = s.length\n        auto $1 = mutableSetOf<String>()\n\n        // Precompute prefix frequency arrays\n        auto $1 = Array(n + 1) { vector<int>(10) }\n        for (i in 1..n) {\n            for (d in 0..9) {\n                prefixFreq[i][d] = prefixFreq[i - 1][d]\n            }\n            prefixFreq[i][s[i - 1] - '0']++\n        }\n\n        // Iterate over all possible substrings\n        for (i in 0 until n) {\n            for (j in i + 1..n) {\n                auto $1 = vector<int>(10)\n                for (d in 0..9) {\n                    freq[d] = prefixFreq[j][d] - prefixFreq[i][d]\n                }\n\n                if (hasEqualFrequency(freq)) {\n                    uniqueSubstrings.add(s.substring(i, j))\n                }\n            }\n        }\n\n        return uniqueSubstrings.size\n    }\n\n    private $1(freq: vector<int>) {\n        auto $1 = -1\n        for (count in freq) {\n            if (count > 0) {\n                if (uniqueFreq == -1) {\n                    uniqueFreq = count\n                } else if (count != uniqueFreq) {\n                    return false\n                }\n            }\n        }\n        return true\n    }\n}" %}

---

## DecodeString

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/stack/DecodeString.kt`

{% include code-tabs.html  kotlin="package string.stack\n\nclass DecodeString {\n    private var index = 0\n\n    fun decodeString(s: String): String {\n        val result = StringBuilder()\n\n        while (index < s.length && s[index] != ']') {\n            val ch = s[index]\n            when {\n                !ch.isDigit() -> {\n                    result.append(ch)\n                    index++\n                }\n                else -> {\n                    var k = 0\n                    while (index < s.length && s[index].isDigit()) {\n                        k = k * 10 + (s[index++] - '0')\n                    }\n                    index++ // Skip the opening bracket '['\n                    val nestedDecodedString = decodeString(s)\n                    index++ // Skip the closing bracket ']'\n\n                    repeat(k) {\n                        result.append(nestedDecodedString)\n                    }\n                }\n            }\n        }\n\n        return result.toString()\n    }\n\n    // Short code\n    fun decodeString1(s: String): String {\n        var index = 0\n        fun decode(): String {\n            val result = StringBuilder()\n            var num = 0\n            while (index < s.length) {\n                when (val char = s[index]) {\n                    in '0'..'9' -> num = num * 10 + (char - '0')\n                    '[' -> {\n                        index++\n                        val decodedString = decode()\n                        result.append(decodedString.repeat(num))\n                        num = 0\n                    }\n                    ']' -> return result.toString()\n                    else -> result.append(char)\n                }\n                index++\n            }\n            return result.toString()\n        }\n        return decode()\n    }\n}"  java="class DecodeString {\n    private var index = 0\n\n    public static void $1(s) {\n        val result = StringBuilder()\n\n        while (index < s.length && s[index] != ']') {\n            val ch = s[index]\n            when {\n                !ch.isDigit() -> {\n                    result.append(ch)\n                    index++\n                }\n                else -> {\n                    var k = 0\n                    while (index < s.length && s[index].isDigit()) {\n                        k = k * 10 + (s[index++] - '0')\n                    }\n                    index++ // Skip the opening bracket '['\n                    val nestedDecodedString = decodeString(s)\n                    index++ // Skip the closing bracket ']'\n\n                    repeat(k) {\n                        result.append(nestedDecodedString)\n                    }\n                }\n            }\n        }\n\n        return result.toString()\n    }\n\n    // Short code\n    public static void $1(s) {\n        var index = 0\n        public static void $1() {\n            val result = StringBuilder()\n            var num = 0\n            while (index < s.length) {\n                when (val char = s[index]) {\n                    in '0'..'9' -> num = num * 10 + (char - '0')\n                    '[' -> {\n                        index++\n                        val decodedString = decode()\n                        result.append(decodedString.repeat(num))\n                        num = 0\n                    }\n                    ']' -> return result.toString()\n                    else -> result.append(char)\n                }\n                index++\n            }\n            return result.toString()\n        }\n        return decode()\n    }\n}"  python="class DecodeString {\n    private $1 = 0\n\n    def $1(s: str): str {\n        $1 = StringBuilder()\n\n        while (index < s.length && s[index] != ']') {\n            $1 = s[index]\n            when {\n                !ch.isDigit() -> {\n                    result.append(ch)\n                    index++\n                }\n                else -> {\n                    $1 = 0\n                    while (index < s.length && s[index].isDigit()) {\n                        k = k * 10 + (s[index++] - '0')\n                    }\n                    index++ // Skip the opening bracket '['\n                    $1 = decodeString(s)\n                    index++ // Skip the closing bracket ']'\n\n                    repeat(k) {\n                        result.append(nestedDecodedString)\n                    }\n                }\n            }\n        }\n\n        return result.toString()\n    }\n\n    // Short code\n    def $1(s: str): str {\n        $1 = 0\n        def $1(): str {\n            $1 = StringBuilder()\n            $1 = 0\n            while (index < s.length) {\n                when ($1 = s[index]) {\n                    in '0'..'9' -> num = num * 10 + (char - '0')\n                    '[' -> {\n                        index++\n                        $1 = decode()\n                        result.append(decodedString.repeat(num))\n                        num = 0\n                    }\n                    ']' -> return result.toString()\n                    else -> result.append(char)\n                }\n                index++\n            }\n            return result.toString()\n        }\n        return decode()\n    }\n}"  rust="class DecodeString {\n    private var index = 0\n\n    fn $1(s -> String) -> String {\n        val result = StringBuilder()\n\n        while (index < s.length && s[index] != ']') {\n            val ch = s[index]\n            when {\n                !ch.isDigit() -> {\n                    result.append(ch)\n                    index++\n                }\n                else -> {\n                    var k = 0\n                    while (index < s.length && s[index].isDigit()) {\n                        k = k * 10 + (s[index++] - '0')\n                    }\n                    index++ // Skip the opening bracket '['\n                    val nestedDecodedString = decodeString(s)\n                    index++ // Skip the closing bracket ']'\n\n                    repeat(k) {\n                        result.append(nestedDecodedString)\n                    }\n                }\n            }\n        }\n\n        return result.toString()\n    }\n\n    // Short code\n    fn $1(s -> String) -> String {\n        var index = 0\n        fn $1() -> String {\n            val result = StringBuilder()\n            var num = 0\n            while (index < s.length) {\n                when (val char = s[index]) {\n                    in '0'..'9' -> num = num * 10 + (char - '0')\n                    '[' -> {\n                        index++\n                        val decodedString = decode()\n                        result.append(decodedString.repeat(num))\n                        num = 0\n                    }\n                    ']' -> return result.toString()\n                    else -> result.append(char)\n                }\n                index++\n            }\n            return result.toString()\n        }\n        return decode()\n    }\n}"  cpp="class DecodeString {\n    private auto $1 = 0\n\n    $1(s) {\n        auto $1 = StringBuilder()\n\n        while (index < s.length && s[index] != ']') {\n            auto $1 = s[index]\n            when {\n                !ch.isDigit() -> {\n                    result.append(ch)\n                    index++\n                }\n                else -> {\n                    auto $1 = 0\n                    while (index < s.length && s[index].isDigit()) {\n                        k = k * 10 + (s[index++] - '0')\n                    }\n                    index++ // Skip the opening bracket '['\n                    auto $1 = decodeString(s)\n                    index++ // Skip the closing bracket ']'\n\n                    repeat(k) {\n                        result.append(nestedDecodedString)\n                    }\n                }\n            }\n        }\n\n        return result.toString()\n    }\n\n    // Short code\n    $1(s) {\n        auto $1 = 0\n        $1() {\n            auto $1 = StringBuilder()\n            auto $1 = 0\n            while (index < s.length) {\n                when (auto $1 = s[index]) {\n                    in '0'..'9' -> num = num * 10 + (char - '0')\n                    '[' -> {\n                        index++\n                        auto $1 = decode()\n                        result.append(decodedString.repeat(num))\n                        num = 0\n                    }\n                    ']' -> return result.toString()\n                    else -> result.append(char)\n                }\n                index++\n            }\n            return result.toString()\n        }\n        return decode()\n    }\n}" %}

---

## RemoveAllAdjacentDuplicatesInString

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/stack/RemoveAllAdjacentDuplicatesInString.kt`

{% include code-tabs.html  kotlin="package string.stack\n\nclass RemoveAllAdjacentDuplicatesInString {\n    fun removeDuplicates(s: String): String {\n        val stack = ArrayDeque<Char>()\n\n        s.forEach { ch ->\n            when {\n                stack.isNotEmpty() && stack.last() == ch -> stack.removeLast()\n                else -> stack.add(ch)\n            }\n        }\n\n        return stack.joinToString(\"\")\n    }\n}\n"  java="class RemoveAllAdjacentDuplicatesInString {\n    public static void $1(s) {\n        val stack = ArrayDeque<Char>()\n\n        s.forEach { ch ->\n            when {\n                stack.isNotEmpty() && stack.last() == ch -> stack.removeLast()\n                else -> stack.add(ch)\n            }\n        }\n\n        return stack.joinToString(\"\")\n    }\n}"  python="class RemoveAllAdjacentDuplicatesInString {\n    def $1(s: str): str {\n        $1 = ArrayDeque<Char>()\n\n        s.forEach { ch ->\n            when {\n                stack.isNotEmpty() && stack.last() == ch -> stack.removeLast()\n                else -> stack.add(ch)\n            }\n        }\n\n        return stack.joinToString(\"\")\n    }\n}"  rust="class RemoveAllAdjacentDuplicatesInString {\n    fn $1(s -> String) -> String {\n        val stack = ArrayDeque<Char>()\n\n        s.forEach { ch ->\n            when {\n                stack.isNotEmpty() && stack.last() == ch -> stack.removeLast()\n                else -> stack.add(ch)\n            }\n        }\n\n        return stack.joinToString(\"\")\n    }\n}"  cpp="class RemoveAllAdjacentDuplicatesInString {\n    $1(s) {\n        auto $1 = ArrayDeque<Char>()\n\n        s.forEach { ch ->\n            when {\n                stack.isNotEmpty() && stack.last() == ch -> stack.removeLast()\n                else -> stack.add(ch)\n            }\n        }\n\n        return stack.joinToString(\"\")\n    }\n}" %}

---

## SimplifyPath

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/string/stack/SimplifyPath.kt`

{% include code-tabs.html  kotlin="package string.stack\n\nclass SimplifyPath {\n    fun simplifyPath(path: String): String {\n        // Split the path by \"/\"\n        val tokens = path.split(\"/\")\n\n        // Initialize a mutable list to store valid directory names\n        val stack = mutableListOf<String>()\n\n        for (token in tokens) {\n            when (token) {\n                \"\", \".\" -> {\n                    // Ignore empty or current directory tokens\n                    continue\n                }\n                \"..\" -> {\n                    // Pop from the list if it's not empty (go back to the parent directory)\n                    if (stack.isNotEmpty()) {\n                        stack.removeLast()\n                    }\n                }\n                else -> {\n                    // Push valid directory names onto the list\n                    stack.add(token)\n                }\n            }\n        }\n\n        // Rebuild the simplified path from the list\n        return \"/\" + stack.joinToString(\"/\")\n    }\n}"  java="class SimplifyPath {\n    public static void $1(path) {\n        // Split the path by \"/\"\n        val tokens = path.split(\"/\")\n\n        // Initialize a mutable list to store valid directory names\n        val stack = mutableListOf<String>()\n\n        for (token in tokens) {\n            when (token) {\n                \"\", \".\" -> {\n                    // Ignore empty or current directory tokens\n                    continue\n                }\n                \"..\" -> {\n                    // Pop from the list if it's not empty (go back to the parent directory)\n                    if (stack.isNotEmpty()) {\n                        stack.removeLast()\n                    }\n                }\n                else -> {\n                    // Push valid directory names onto the list\n                    stack.add(token)\n                }\n            }\n        }\n\n        // Rebuild the simplified path from the list\n        return \"/\" + stack.joinToString(\"/\")\n    }\n}"  python="class SimplifyPath {\n    def $1(path: str): str {\n        // Split the path by \"/\"\n        $1 = path.split(\"/\")\n\n        // Initialize a mutable list to store valid directory names\n        $1 = mutableListOf<String>()\n\n        for (token in tokens) {\n            when (token) {\n                \"\", \".\" -> {\n                    // Ignore empty or current directory tokens\n                    continue\n                }\n                \"..\" -> {\n                    // Pop from the list if it's not empty (go back to the parent directory)\n                    if (stack.isNotEmpty()) {\n                        stack.removeLast()\n                    }\n                }\n                else -> {\n                    // Push valid directory names onto the list\n                    stack.add(token)\n                }\n            }\n        }\n\n        // Rebuild the simplified path from the list\n        return \"/\" + stack.joinToString(\"/\")\n    }\n}"  rust="class SimplifyPath {\n    fn $1(path -> String) -> String {\n        // Split the path by \"/\"\n        val tokens = path.split(\"/\")\n\n        // Initialize a mutable list to store valid directory names\n        val stack = mutableListOf<String>()\n\n        for (token in tokens) {\n            when (token) {\n                \"\", \".\" -> {\n                    // Ignore empty or current directory tokens\n                    continue\n                }\n                \"..\" -> {\n                    // Pop from the list if it's not empty (go back to the parent directory)\n                    if (stack.isNotEmpty()) {\n                        stack.removeLast()\n                    }\n                }\n                else -> {\n                    // Push valid directory names onto the list\n                    stack.add(token)\n                }\n            }\n        }\n\n        // Rebuild the simplified path from the list\n        return \"/\" + stack.joinToString(\"/\")\n    }\n}"  cpp="class SimplifyPath {\n    $1(path) {\n        // Split the path by \"/\"\n        auto $1 = path.split(\"/\")\n\n        // Initialize a mutable list to store valid directory names\n        auto $1 = mutableListOf<String>()\n\n        for (token in tokens) {\n            when (token) {\n                \"\", \".\" -> {\n                    // Ignore empty or current directory tokens\n                    continue\n                }\n                \"..\" -> {\n                    // Pop from the list if it's not empty (go back to the parent directory)\n                    if (stack.isNotEmpty()) {\n                        stack.removeLast()\n                    }\n                }\n                else -> {\n                    // Push valid directory names onto the list\n                    stack.add(token)\n                }\n            }\n        }\n\n        // Rebuild the simplified path from the list\n        return \"/\" + stack.joinToString(\"/\")\n    }\n}" %}
