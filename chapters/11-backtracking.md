---
layout: chapter
title: "Backtracking"
chapter_number: 11
chapter_title: "Backtracking"
toc: true
prev_chapter:
  url: "/chapters/10-string-matching"
  title: "String Matching"
next_chapter:
  url: "/chapters/12-caches"
  title: "Caches"
---

# Backtracking

> **10 problems** from the CrackGoogle repository. Click the tabs to switch between **Kotlin, Java, Python, Rust, and C++**.

## Complete Problem Set

| # | Problem | Topic | Code Available |
|---|---------|-------|----------------|
| 1 | **ExpressionAndAddOperators** | backtracking | ✅ All 5 languages |
| 2 | **ExpressionAndAddOperatorsOptimized** | backtracking | ✅ All 5 languages |
| 3 | **NQueen** | backtracking | ✅ All 5 languages |
| 4 | **NQueen_II** | backtracking | ✅ All 5 languages |
| 5 | **PalindromePartitioning** | backtracking | ✅ All 5 languages |
| 6 | **PartitionToKEqualSumSubsets** | backtracking | ✅ All 5 languages |
| 7 | **RestoreIPAddresses** | backtracking | ✅ All 5 languages |
| 8 | **Strobogrammatic_Number_II** | backtracking | ✅ All 5 languages |
| 9 | **SudokuSolver** | backtracking | ✅ All 5 languages |
| 10 | **SudokuSolverSet** | backtracking | ✅ All 5 languages |

---

## ExpressionAndAddOperators

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/ExpressionAndAddOperators.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nclass ExpressionAndAddOperators {\n    fun addOperators(num: String, target: Int): List<String> {\n        val result = mutableListOf<String>()\n\n        fun dfs(index: Int, prevOperand: Long, currentOperand: Long, value: Long, expression: String) {\n            if (index == num.length) {\n                if (value == target.toLong() && currentOperand == 0L) {\n                    result.add(expression)\n                }\n                return\n            }\n\n            // Extending the current operand by one digit\n            val currentDigit = num[index].toString().toLong()\n            val newOperand = currentOperand * 10 + currentDigit\n\n            // Avoid cases where numbers start with '0'\n            if (newOperand > 0) {\n                // No operator added, just extend the current operand\n                dfs(index + 1, prevOperand, newOperand, value, expression)\n            }\n\n            // Add '+'\n            dfs(index + 1, newOperand, 0, value + newOperand, \"$expression+$newOperand\")\n\n            // Add '-'\n            dfs(index + 1, -newOperand, 0, value - newOperand, \"$expression-$newOperand\")\n\n            // Add '*'\n            dfs(index + 1, prevOperand * newOperand, 0, value - prevOperand + (prevOperand * newOperand), \"$expression*$newOperand\")\n        }\n\n        for (i in 1..num.length) {\n            val currentOperand = num.substring(0, i).toLong()\n            if (currentOperand.toString().length != i) continue // Skip numbers with leading zeros\n\n            dfs(i, currentOperand, 0, currentOperand, currentOperand.toString())\n        }\n\n        return result\n    }\n\n    // ANother leetcode solution https://leetcode.com/problems/expression-add-operators/discuss/951147/Kotlin-C%2B%2B%3A-O(n4n)-time-and-O(n)-time-with-backtracking\n    fun addOperators2(num: String, target: Int): List<String> {\n        val res = ArrayList<String>()\n\n        fun partition(start: Int, path: String, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path)\n                }\n                return\n            }\n\n            // 1|23\n            // 12|3\n            // 123|\n            for (i in start..num.length - 1) {\n                val s = num.substring(start..i)\n                val current = s.toLong()\n                // invalid case: 1 + 05\n                if (s.length > 1 && s[0] == '0')\n                    break\n\n                if (path.isEmpty()) {\n                    partition(i + 1, s, current, current)\n                } else {\n                    partition(i + 1, \"$path+$s\", sum + current, current)\n                    partition(i + 1, \"$path-$s\", sum - current, -current)\n                    // 1 + 2 - 4 * 12 --> -1 - (-4) + (-4 * 12) --> 3 + (-48) --> -45\n                    partition(i + 1, \"$path*$s\", sum - prev + prev * current, prev * current)\n                }\n            }\n        }\n\n        partition(0, \"\", 0L, 0L)\n        return res.toList()\n    }\n\n}\n\n\nfun main(args: Array<String>) {\n    val test = ExpressionAndAddOperators()\n\n    println(test.addOperators(\"1310\", 130))\n}"  java="class ExpressionAndAddOperators {\n    public static void $1(num, target): List<String> {\n        val result = mutableListOf<String>()\n\n        public static void $1(index, prevOperand: Long, currentOperand: Long, value: Long, expression) {\n            if (index == num.length) {\n                if (value == target.toLong() && currentOperand == 0L) {\n                    result.add(expression)\n                }\n                return\n            }\n\n            // Extending the current operand by one digit\n            val currentDigit = num[index].toString().toLong()\n            val newOperand = currentOperand * 10 + currentDigit\n\n            // Avoid cases where numbers start with '0'\n            if (newOperand > 0) {\n                // No operator added, just extend the current operand\n                dfs(index + 1, prevOperand, newOperand, value, expression)\n            }\n\n            // Add '+'\n            dfs(index + 1, newOperand, 0, value + newOperand, \"$expression+$newOperand\")\n\n            // Add '-'\n            dfs(index + 1, -newOperand, 0, value - newOperand, \"$expression-$newOperand\")\n\n            // Add '*'\n            dfs(index + 1, prevOperand * newOperand, 0, value - prevOperand + (prevOperand * newOperand), \"$expression*$newOperand\")\n        }\n\n        for (i in 1..num.length) {\n            val currentOperand = num.substring(0, i).toLong()\n            if (currentOperand.toString().length != i) continue // Skip numbers with leading zeros\n\n            dfs(i, currentOperand, 0, currentOperand, currentOperand.toString())\n        }\n\n        return result\n    }\n\n    // ANother leetcode solution https://leetcode.com/problems/expression-add-operators/discuss/951147/Kotlin-C%2B%2B%3A-O(n4n)-time-and-O(n)-time-with-backtracking\n    public static void $1(num, target): List<String> {\n        val res = ArrayList<String>()\n\n        public static void $1(start, path, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path)\n                }\n                return\n            }\n\n            // 1|23\n            // 12|3\n            // 123|\n            for (i in start..num.length - 1) {\n                val s = num.substring(start..i)\n                val current = s.toLong()\n                // invalid case: 1 + 05\n                if (s.length > 1 && s[0] == '0')\n                    break\n\n                if (path.isEmpty()) {\n                    partition(i + 1, s, current, current)\n                } else {\n                    partition(i + 1, \"$path+$s\", sum + current, current)\n                    partition(i + 1, \"$path-$s\", sum - current, -current)\n                    // 1 + 2 - 4 * 12 --> -1 - (-4) + (-4 * 12) --> 3 + (-48) --> -45\n                    partition(i + 1, \"$path*$s\", sum - prev + prev * current, prev * current)\n                }\n            }\n        }\n\n        partition(0, \"\", 0L, 0L)\n        return res.toList()\n    }\n\n}\n\n\npublic static void $1(args: Array<String>) {\n    val test = ExpressionAndAddOperators()\n\n    println(test.addOperators(\"1310\", 130))\n}"  python="class ExpressionAndAddOperators {\n    def $1(num: str, target: int): List<String> {\n        $1 = mutableListOf<String>()\n\n        def $1(index: int, prevOperand: Long, currentOperand: Long, value: Long, expression: str) {\n            if (index == num.length) {\n                if (value == target.toLong() && currentOperand == 0L) {\n                    result.add(expression)\n                }\n                return\n            }\n\n            // Extending the current operand by one digit\n            $1 = num[index].toString().toLong()\n            $1 = currentOperand * 10 + currentDigit\n\n            // Avoid cases where numbers start with '0'\n            if (newOperand > 0) {\n                // No operator added, just extend the current operand\n                dfs(index + 1, prevOperand, newOperand, value, expression)\n            }\n\n            // Add '+'\n            dfs(index + 1, newOperand, 0, value + newOperand, \"$expression+$newOperand\")\n\n            // Add '-'\n            dfs(index + 1, -newOperand, 0, value - newOperand, \"$expression-$newOperand\")\n\n            // Add '*'\n            dfs(index + 1, prevOperand * newOperand, 0, value - prevOperand + (prevOperand * newOperand), \"$expression*$newOperand\")\n        }\n\n        for (i in 1..num.length) {\n            $1 = num.substring(0, i).toLong()\n            if (currentOperand.toString().length != i) continue // Skip numbers with leading zeros\n\n            dfs(i, currentOperand, 0, currentOperand, currentOperand.toString())\n        }\n\n        return result\n    }\n\n    // ANother leetcode solution https://leetcode.com/problems/expression-add-operators/discuss/951147/Kotlin-C%2B%2B%3A-O(n4n)-time-and-O(n)-time-with-backtracking\n    def $1(num: str, target: int): List<String> {\n        $1 = ArrayList<String>()\n\n        def $1(start: int, path: str, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path)\n                }\n                return\n            }\n\n            // 1|23\n            // 12|3\n            // 123|\n            for (i in start..num.length - 1) {\n                $1 = num.substring(start..i)\n                $1 = s.toLong()\n                // invalid case: 1 + 05\n                if (s.length > 1 && s[0] == '0')\n                    break\n\n                if (path.isEmpty()) {\n                    partition(i + 1, s, current, current)\n                } else {\n                    partition(i + 1, \"$path+$s\", sum + current, current)\n                    partition(i + 1, \"$path-$s\", sum - current, -current)\n                    // 1 + 2 - 4 * 12 --> -1 - (-4) + (-4 * 12) --> 3 + (-48) --> -45\n                    partition(i + 1, \"$path*$s\", sum - prev + prev * current, prev * current)\n                }\n            }\n        }\n\n        partition(0, \"\", 0L, 0L)\n        return res.toList()\n    }\n\n}\n\n\ndef $1(args: Array<String>) {\n    $1 = ExpressionAndAddOperators()\n\n    print(test.addOperators(\"1310\", 130))\n}"  rust="class ExpressionAndAddOperators {\n    fn $1(num -> String, target -> i32): List<String> {\n        val result = mutableListOf<String>()\n\n        fn $1(index -> i32, prevOperand: Long, currentOperand: Long, value: Long, expression -> String) {\n            if (index == num.length) {\n                if (value == target.toLong() && currentOperand == 0L) {\n                    result.add(expression)\n                }\n                return\n            }\n\n            // Extending the current operand by one digit\n            val currentDigit = num[index].toString().toLong()\n            val newOperand = currentOperand * 10 + currentDigit\n\n            // Avoid cases where numbers start with '0'\n            if (newOperand > 0) {\n                // No operator added, just extend the current operand\n                dfs(index + 1, prevOperand, newOperand, value, expression)\n            }\n\n            // Add '+'\n            dfs(index + 1, newOperand, 0, value + newOperand, \"$expression+$newOperand\")\n\n            // Add '-'\n            dfs(index + 1, -newOperand, 0, value - newOperand, \"$expression-$newOperand\")\n\n            // Add '*'\n            dfs(index + 1, prevOperand * newOperand, 0, value - prevOperand + (prevOperand * newOperand), \"$expression*$newOperand\")\n        }\n\n        for (i in 1..num.length) {\n            val currentOperand = num.substring(0, i).toLong()\n            if (currentOperand.toString().length != i) continue // Skip numbers with leading zeros\n\n            dfs(i, currentOperand, 0, currentOperand, currentOperand.toString())\n        }\n\n        return result\n    }\n\n    // ANother leetcode solution https://leetcode.com/problems/expression-add-operators/discuss/951147/Kotlin-C%2B%2B%3A-O(n4n)-time-and-O(n)-time-with-backtracking\n    fn $1(num -> String, target -> i32): List<String> {\n        val res = ArrayList<String>()\n\n        fn $1(start -> i32, path -> String, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path)\n                }\n                return\n            }\n\n            // 1|23\n            // 12|3\n            // 123|\n            for (i in start..num.length - 1) {\n                val s = num.substring(start..i)\n                val current = s.toLong()\n                // invalid case: 1 + 05\n                if (s.length > 1 && s[0] == '0')\n                    break\n\n                if (path.isEmpty()) {\n                    partition(i + 1, s, current, current)\n                } else {\n                    partition(i + 1, \"$path+$s\", sum + current, current)\n                    partition(i + 1, \"$path-$s\", sum - current, -current)\n                    // 1 + 2 - 4 * 12 --> -1 - (-4) + (-4 * 12) --> 3 + (-48) --> -45\n                    partition(i + 1, \"$path*$s\", sum - prev + prev * current, prev * current)\n                }\n            }\n        }\n\n        partition(0, \"\", 0L, 0L)\n        return res.toList()\n    }\n\n}\n\n\nfn $1(args: Array<String>) {\n    val test = ExpressionAndAddOperators()\n\n    println!(test.addOperators(\"1310\", 130))\n}"  cpp="class ExpressionAndAddOperators {\n    $1(num, target): List<String> {\n        auto $1 = mutableListOf<String>()\n\n        $1(index, prevOperand: Long, currentOperand: Long, value: Long, expression) {\n            if (index == num.length) {\n                if (value == target.toLong() && currentOperand == 0L) {\n                    result.add(expression)\n                }\n                return\n            }\n\n            // Extending the current operand by one digit\n            auto $1 = num[index].toString().toLong()\n            auto $1 = currentOperand * 10 + currentDigit\n\n            // Avoid cases where numbers start with '0'\n            if (newOperand > 0) {\n                // No operator added, just extend the current operand\n                dfs(index + 1, prevOperand, newOperand, value, expression)\n            }\n\n            // Add '+'\n            dfs(index + 1, newOperand, 0, value + newOperand, \"$expression+$newOperand\")\n\n            // Add '-'\n            dfs(index + 1, -newOperand, 0, value - newOperand, \"$expression-$newOperand\")\n\n            // Add '*'\n            dfs(index + 1, prevOperand * newOperand, 0, value - prevOperand + (prevOperand * newOperand), \"$expression*$newOperand\")\n        }\n\n        for (i in 1..num.length) {\n            auto $1 = num.substring(0, i).toLong()\n            if (currentOperand.toString().length != i) continue // Skip numbers with leading zeros\n\n            dfs(i, currentOperand, 0, currentOperand, currentOperand.toString())\n        }\n\n        return result\n    }\n\n    // ANother leetcode solution https://leetcode.com/problems/expression-add-operators/discuss/951147/Kotlin-C%2B%2B%3A-O(n4n)-time-and-O(n)-time-with-backtracking\n    $1(num, target): List<String> {\n        auto $1 = ArrayList<String>()\n\n        $1(start, path, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path)\n                }\n                return\n            }\n\n            // 1|23\n            // 12|3\n            // 123|\n            for (i in start..num.length - 1) {\n                auto $1 = num.substring(start..i)\n                auto $1 = s.toLong()\n                // invalid case: 1 + 05\n                if (s.length > 1 && s[0] == '0')\n                    break\n\n                if (path.isEmpty()) {\n                    partition(i + 1, s, current, current)\n                } else {\n                    partition(i + 1, \"$path+$s\", sum + current, current)\n                    partition(i + 1, \"$path-$s\", sum - current, -current)\n                    // 1 + 2 - 4 * 12 --> -1 - (-4) + (-4 * 12) --> 3 + (-48) --> -45\n                    partition(i + 1, \"$path*$s\", sum - prev + prev * current, prev * current)\n                }\n            }\n        }\n\n        partition(0, \"\", 0L, 0L)\n        return res.toList()\n    }\n\n}\n\n\n$1(args: Array<String>) {\n    auto $1 = ExpressionAndAddOperators()\n\n    cout <<(test.addOperators(\"1310\", 130))\n}" %}

---

## ExpressionAndAddOperatorsOptimized

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/ExpressionAndAddOperatorsOptimized.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nclass ExpressionAndAddOperatorsOptimized {\n    fun addOperators(num: String, target: Int): List<String> {\n        val res = ArrayList<String>()\n\n        fun partition(start: Int, path: StringBuilder, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path.toString())\n                }\n                return\n            }\n\n            for (i in start until num.length) {\n                val s = num.substring(start..i)\n                val current = s.toLong()\n\n                // Skip invalid numbers with leading zeros\n                if (s.length > 1 && s[0] == '0') break\n\n                val len = path.length // Remember the length of the path before modification\n\n                if (path.isEmpty()) {\n                    path.append(s)\n                    partition(i + 1, path, current, current)\n                    path.setLength(len) // Revert the path to its previous state\n                } else {\n                    // Try adding '+' operator\n                    path.append(\"+\").append(s)\n                    partition(i + 1, path, sum + current, current)\n                    path.setLength(len)\n\n                    // Try adding '-' operator\n                    path.append(\"-\").append(s)\n                    partition(i + 1, path, sum - current, -current)\n                    path.setLength(len)\n\n                    // Try adding '*' operator\n                    path.append(\"*\").append(s)\n                    partition(i + 1, path, sum - prev + prev * current, prev * current)\n                    path.setLength(len)\n                }\n            }\n        }\n\n        partition(0, StringBuilder(), 0L, 0L)\n        return res\n    }\n}"  java="class ExpressionAndAddOperatorsOptimized {\n    public static void $1(num, target): List<String> {\n        val res = ArrayList<String>()\n\n        public static void $1(start, path: StringBuilder, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path.toString())\n                }\n                return\n            }\n\n            for (i in start until num.length) {\n                val s = num.substring(start..i)\n                val current = s.toLong()\n\n                // Skip invalid numbers with leading zeros\n                if (s.length > 1 && s[0] == '0') break\n\n                val len = path.length // Remember the length of the path before modification\n\n                if (path.isEmpty()) {\n                    path.append(s)\n                    partition(i + 1, path, current, current)\n                    path.setLength(len) // Revert the path to its previous state\n                } else {\n                    // Try adding '+' operator\n                    path.append(\"+\").append(s)\n                    partition(i + 1, path, sum + current, current)\n                    path.setLength(len)\n\n                    // Try adding '-' operator\n                    path.append(\"-\").append(s)\n                    partition(i + 1, path, sum - current, -current)\n                    path.setLength(len)\n\n                    // Try adding '*' operator\n                    path.append(\"*\").append(s)\n                    partition(i + 1, path, sum - prev + prev * current, prev * current)\n                    path.setLength(len)\n                }\n            }\n        }\n\n        partition(0, StringBuilder(), 0L, 0L)\n        return res\n    }\n}"  python="class ExpressionAndAddOperatorsOptimized {\n    def $1(num: str, target: int): List<String> {\n        $1 = ArrayList<String>()\n\n        def $1(start: int, path: StringBuilder, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path.toString())\n                }\n                return\n            }\n\n            for (i in start until num.length) {\n                $1 = num.substring(start..i)\n                $1 = s.toLong()\n\n                // Skip invalid numbers with leading zeros\n                if (s.length > 1 && s[0] == '0') break\n\n                $1 = path.length // Remember the length of the path before modification\n\n                if (path.isEmpty()) {\n                    path.append(s)\n                    partition(i + 1, path, current, current)\n                    path.setLength(len) // Revert the path to its previous state\n                } else {\n                    // Try adding '+' operator\n                    path.append(\"+\").append(s)\n                    partition(i + 1, path, sum + current, current)\n                    path.setLength(len)\n\n                    // Try adding '-' operator\n                    path.append(\"-\").append(s)\n                    partition(i + 1, path, sum - current, -current)\n                    path.setLength(len)\n\n                    // Try adding '*' operator\n                    path.append(\"*\").append(s)\n                    partition(i + 1, path, sum - prev + prev * current, prev * current)\n                    path.setLength(len)\n                }\n            }\n        }\n\n        partition(0, StringBuilder(), 0L, 0L)\n        return res\n    }\n}"  rust="class ExpressionAndAddOperatorsOptimized {\n    fn $1(num -> String, target -> i32): List<String> {\n        val res = ArrayList<String>()\n\n        fn $1(start -> i32, path: StringBuilder, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path.toString())\n                }\n                return\n            }\n\n            for (i in start until num.length) {\n                val s = num.substring(start..i)\n                val current = s.toLong()\n\n                // Skip invalid numbers with leading zeros\n                if (s.length > 1 && s[0] == '0') break\n\n                val len = path.length // Remember the length of the path before modification\n\n                if (path.isEmpty()) {\n                    path.append(s)\n                    partition(i + 1, path, current, current)\n                    path.setLength(len) // Revert the path to its previous state\n                } else {\n                    // Try adding '+' operator\n                    path.append(\"+\").append(s)\n                    partition(i + 1, path, sum + current, current)\n                    path.setLength(len)\n\n                    // Try adding '-' operator\n                    path.append(\"-\").append(s)\n                    partition(i + 1, path, sum - current, -current)\n                    path.setLength(len)\n\n                    // Try adding '*' operator\n                    path.append(\"*\").append(s)\n                    partition(i + 1, path, sum - prev + prev * current, prev * current)\n                    path.setLength(len)\n                }\n            }\n        }\n\n        partition(0, StringBuilder(), 0L, 0L)\n        return res\n    }\n}"  cpp="class ExpressionAndAddOperatorsOptimized {\n    $1(num, target): List<String> {\n        auto $1 = ArrayList<String>()\n\n        $1(start, path: StringBuilder, sum: Long, prev: Long) {\n            if (start == num.length) {\n                if (sum == target.toLong()) {\n                    res.add(path.toString())\n                }\n                return\n            }\n\n            for (i in start until num.length) {\n                auto $1 = num.substring(start..i)\n                auto $1 = s.toLong()\n\n                // Skip invalid numbers with leading zeros\n                if (s.length > 1 && s[0] == '0') break\n\n                auto $1 = path.length // Remember the length of the path before modification\n\n                if (path.isEmpty()) {\n                    path.append(s)\n                    partition(i + 1, path, current, current)\n                    path.setLength(len) // Revert the path to its previous state\n                } else {\n                    // Try adding '+' operator\n                    path.append(\"+\").append(s)\n                    partition(i + 1, path, sum + current, current)\n                    path.setLength(len)\n\n                    // Try adding '-' operator\n                    path.append(\"-\").append(s)\n                    partition(i + 1, path, sum - current, -current)\n                    path.setLength(len)\n\n                    // Try adding '*' operator\n                    path.append(\"*\").append(s)\n                    partition(i + 1, path, sum - prev + prev * current, prev * current)\n                    path.setLength(len)\n                }\n            }\n        }\n\n        partition(0, StringBuilder(), 0L, 0L)\n        return res\n    }\n}" %}

---

## NQueen

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/NQueen.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nimport javax.swing.text.html.HTML.Attribute.N\n\nclass NQueen {\n    private lateinit var placed: IntArray\n\n    fun solveNQueens(n: Int): List<List<String>> {\n        val results = mutableListOf<List<String>>()\n        placed = IntArray(n) { -1 } // Initialize with -1 indicating no queens are placed\n\n        fun dfs(row: Int) {\n            if (row == n) {\n                // Convert the board configuration to the required output format\n                val board = List(n) { CharArray(n) { '.' } }\n                for (r in placed.indices) {\n                    board[r][placed[r]] = 'Q'\n                }\n                results.add(board.map { it.joinToString(\"\") })\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    dfs(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        dfs(0)\n        return results\n    }\n\n    private fun isSafe(row: Int, col: Int): Boolean {\n        for (prevRow in 0 until row) {\n            val prevCol = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return false\n            }\n        }\n        return true\n    }\n}"  java="class NQueen {\n    private lateinit var placed: IntArray\n\n    public static void $1(n): List<List<String>> {\n        val results = mutableListOf<List<String>>()\n        placed = IntArray(n) { -1 } // Initialize with -1 indicating no queens are placed\n\n        public static void $1(row) {\n            if (row == n) {\n                // Convert the board configuration to the required output format\n                val board = List(n) { CharArray(n) { '.' } }\n                for (r in placed.indices) {\n                    board[r][placed[r]] = 'Q'\n                }\n                results.add(board.map { it.joinToString(\"\") })\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    dfs(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        dfs(0)\n        return results\n    }\n\n    private public static void $1(row, col) {\n        for (prevRow in 0 until row) {\n            val prevCol = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return false\n            }\n        }\n        return true\n    }\n}"  python="class NQueen {\n    private lateinit var placed: list[int]\n\n    def $1(n: int): List<List<String>> {\n        $1 = mutableListOf<List<String>>()\n        placed = list[int](n) { -1 } // Initialize with -1 indicating no queens are placed\n\n        def $1(row: int) {\n            if (row == n) {\n                // Convert the board configuration to the required output format\n                $1 = List(n) { CharArray(n) { '.' } }\n                for (r in placed.indices) {\n                    board[r][placed[r]] = 'Q'\n                }\n                results.add(board.map { it.joinToString(\"\") })\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    dfs(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        dfs(0)\n        return results\n    }\n\n    private def $1(row: int, col: int): bool {\n        for (prevRow in 0 until row) {\n            $1 = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return False\n            }\n        }\n        return True\n    }\n}"  rust="class NQueen {\n    private lateinit var placed: Vec<i32>\n\n    fn $1(n -> i32): List<List<String>> {\n        val results = mutableListOf<List<String>>()\n        placed = Vec<i32>(n) { -1 } // Initialize with -1 indicating no queens are placed\n\n        fn $1(row -> i32) {\n            if (row == n) {\n                // Convert the board configuration to the required output format\n                val board = List(n) { CharArray(n) { '.' } }\n                for (r in placed.indices) {\n                    board[r][placed[r]] = 'Q'\n                }\n                results.add(board.map { it.joinToString(\"\") })\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    dfs(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        dfs(0)\n        return results\n    }\n\n    private fn $1(row -> i32, col -> i32) -> bool {\n        for (prevRow in 0 until row) {\n            val prevCol = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return false\n            }\n        }\n        return true\n    }\n}"  cpp="class NQueen {\n    private lateinit var placed: vector<int>\n\n    $1(n): List<List<String>> {\n        auto $1 = mutableListOf<List<String>>()\n        placed = vector<int>(n) { -1 } // Initialize with -1 indicating no queens are placed\n\n        $1(row) {\n            if (row == n) {\n                // Convert the board configuration to the required output format\n                auto $1 = List(n) { CharArray(n) { '.' } }\n                for (r in placed.indices) {\n                    board[r][placed[r]] = 'Q'\n                }\n                results.add(board.map { it.joinToString(\"\") })\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    dfs(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        dfs(0)\n        return results\n    }\n\n    private $1(row, col) {\n        for (prevRow in 0 until row) {\n            auto $1 = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return false\n            }\n        }\n        return true\n    }\n}" %}

---

## NQueen_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/NQueen_II.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nclass NQueen_II {\n    private lateinit var placed: IntArray\n\n    fun totalNQueens(n: Int): Int {\n        placed = IntArray(n) { -1 } // Initialize with -1 indicating no queens are placed\n        var solutionCount = 0\n\n        fun backtrack(row: Int) {\n            if (row == n) {\n                // Found a valid solution, increment the count\n                solutionCount++\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    backtrack(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        backtrack(0)\n        return solutionCount\n    }\n\n    private fun isSafe(row: Int, col: Int): Boolean {\n        for (prevRow in 0 until row) {\n            val prevCol = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return false\n            }\n        }\n        return true\n    }\n}"  java="class NQueen_II {\n    private lateinit var placed: IntArray\n\n    public static void $1(n) {\n        placed = IntArray(n) { -1 } // Initialize with -1 indicating no queens are placed\n        var solutionCount = 0\n\n        public static void $1(row) {\n            if (row == n) {\n                // Found a valid solution, increment the count\n                solutionCount++\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    backtrack(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        backtrack(0)\n        return solutionCount\n    }\n\n    private public static void $1(row, col) {\n        for (prevRow in 0 until row) {\n            val prevCol = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return false\n            }\n        }\n        return true\n    }\n}"  python="class NQueen_II {\n    private lateinit var placed: list[int]\n\n    def $1(n: int): int {\n        placed = list[int](n) { -1 } // Initialize with -1 indicating no queens are placed\n        $1 = 0\n\n        def $1(row: int) {\n            if (row == n) {\n                // Found a valid solution, increment the count\n                solutionCount++\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    backtrack(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        backtrack(0)\n        return solutionCount\n    }\n\n    private def $1(row: int, col: int): bool {\n        for (prevRow in 0 until row) {\n            $1 = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return False\n            }\n        }\n        return True\n    }\n}"  rust="class NQueen_II {\n    private lateinit var placed: Vec<i32>\n\n    fn $1(n -> i32) -> i32 {\n        placed = Vec<i32>(n) { -1 } // Initialize with -1 indicating no queens are placed\n        var solutionCount = 0\n\n        fn $1(row -> i32) {\n            if (row == n) {\n                // Found a valid solution, increment the count\n                solutionCount++\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    backtrack(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        backtrack(0)\n        return solutionCount\n    }\n\n    private fn $1(row -> i32, col -> i32) -> bool {\n        for (prevRow in 0 until row) {\n            val prevCol = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return false\n            }\n        }\n        return true\n    }\n}"  cpp="class NQueen_II {\n    private lateinit var placed: vector<int>\n\n    $1(n) {\n        placed = vector<int>(n) { -1 } // Initialize with -1 indicating no queens are placed\n        auto $1 = 0\n\n        $1(row) {\n            if (row == n) {\n                // Found a valid solution, increment the count\n                solutionCount++\n                return\n            }\n\n            for (col in 0 until n) {\n                if (isSafe(row, col)) {\n                    placed[row] = col\n                    backtrack(row + 1)\n                    placed[row] = -1 // Remove the queen (backtrack)\n                }\n            }\n        }\n\n        backtrack(0)\n        return solutionCount\n    }\n\n    private $1(row, col) {\n        for (prevRow in 0 until row) {\n            auto $1 = placed[prevRow]\n            if (prevCol == col || Math.abs(prevRow - row) == Math.abs(prevCol - col)) {\n                return false\n            }\n        }\n        return true\n    }\n}" %}

---

## PalindromePartitioning

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/PalindromePartitioning.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nclass PalindromePartitioning {\n    fun partition(s: String): List<List<String>> {\n        val result = mutableListOf<List<String>>()\n\n        fun isPalindrome(s: String, left: Int, right: Int): Boolean {\n            var (l, r) = left to right\n            while (l < r) {\n                if (s[l++] != s[r--]) return false\n            }\n            return true\n        }\n\n        fun dfs(start: Int, path: MutableList<String>) {\n            if (start == s.length) {\n                result.add(ArrayList(path))\n                return\n            }\n            for (end in start until s.length) {\n                if (isPalindrome(s, start, end)) {\n                    path.add(s.substring(start, end + 1))\n                    dfs(end + 1, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}\n"  java="class PalindromePartitioning {\n    public static void $1(s): List<List<String>> {\n        val result = mutableListOf<List<String>>()\n\n        public static void $1(s, left, right) {\n            var (l, r) = left to right\n            while (l < r) {\n                if (s[l++] != s[r--]) return false\n            }\n            return true\n        }\n\n        public static void $1(start, path: MutableList<String>) {\n            if (start == s.length) {\n                result.add(ArrayList(path))\n                return\n            }\n            for (end in start until s.length) {\n                if (isPalindrome(s, start, end)) {\n                    path.add(s.substring(start, end + 1))\n                    dfs(end + 1, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}"  python="class PalindromePartitioning {\n    def $1(s: str): List<List<String>> {\n        $1 = mutableListOf<List<String>>()\n\n        def $1(s: str, left: int, right: int): bool {\n            var (l, r) = left to right\n            while (l < r) {\n                if (s[l++] != s[r--]) return False\n            }\n            return True\n        }\n\n        def $1(start: int, path: MutableList<String>) {\n            if (start == s.length) {\n                result.add(ArrayList(path))\n                return\n            }\n            for (end in start until s.length) {\n                if (isPalindrome(s, start, end)) {\n                    path.add(s.substring(start, end + 1))\n                    dfs(end + 1, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}"  rust="class PalindromePartitioning {\n    fn $1(s -> String): List<List<String>> {\n        val result = mutableListOf<List<String>>()\n\n        fn $1(s -> String, left -> i32, right -> i32) -> bool {\n            var (l, r) = left to right\n            while (l < r) {\n                if (s[l++] != s[r--]) return false\n            }\n            return true\n        }\n\n        fn $1(start -> i32, path: MutableList<String>) {\n            if (start == s.length) {\n                result.add(ArrayList(path))\n                return\n            }\n            for (end in start until s.length) {\n                if (isPalindrome(s, start, end)) {\n                    path.add(s.substring(start, end + 1))\n                    dfs(end + 1, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}"  cpp="class PalindromePartitioning {\n    $1(s): List<List<String>> {\n        auto $1 = mutableListOf<List<String>>()\n\n        $1(s, left, right) {\n            var (l, r) = left to right\n            while (l < r) {\n                if (s[l++] != s[r--]) return false\n            }\n            return true\n        }\n\n        $1(start, path: MutableList<String>) {\n            if (start == s.length) {\n                result.add(ArrayList(path))\n                return\n            }\n            for (end in start until s.length) {\n                if (isPalindrome(s, start, end)) {\n                    path.add(s.substring(start, end + 1))\n                    dfs(end + 1, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}" %}

---

## PartitionToKEqualSumSubsets

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/PartitionToKEqualSumSubsets.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nclass PartitionToKEqualSumSubsets {\n    fun canPartitionKSubsets(nums: IntArray, k: Int): Boolean {\n        val totalSum = nums.sum()\n        if (totalSum % k !=0) return false\n\n        val targetSum = totalSum / k\n        val used = BooleanArray(nums.size)\n\n        fun backtrack(start: Int, currentSum: Int, remainingSubsets: Int): Boolean {\n            if (remainingSubsets == 0)\n                return true\n            if (currentSum == targetSum)\n                return backtrack(0, 0, remainingSubsets - 1)\n\n            for(i in start until nums.size) {\n                if (!used[i] && currentSum + nums[i] <= targetSum) {\n                    used[i] = true\n                    if (backtrack(i + 1, currentSum + nums[i], remainingSubsets))\n                        return true\n                    // backtracking\n                    used[i] = false\n                }\n            }\n\n            return false\n        }\n\n        return backtrack(0, 0, k)\n    }\n}"  java="class PartitionToKEqualSumSubsets {\n    public static void $1(nums: IntArray, k) {\n        val totalSum = nums.sum()\n        if (totalSum % k !=0) return false\n\n        val targetSum = totalSum / k\n        val used = BooleanArray(nums.size)\n\n        public static void $1(start, currentSum, remainingSubsets) {\n            if (remainingSubsets == 0)\n                return true\n            if (currentSum == targetSum)\n                return backtrack(0, 0, remainingSubsets - 1)\n\n            for(i in start until nums.size) {\n                if (!used[i] && currentSum + nums[i] <= targetSum) {\n                    used[i] = true\n                    if (backtrack(i + 1, currentSum + nums[i], remainingSubsets))\n                        return true\n                    // backtracking\n                    used[i] = false\n                }\n            }\n\n            return false\n        }\n\n        return backtrack(0, 0, k)\n    }\n}"  python="class PartitionToKEqualSumSubsets {\n    def $1(nums: list[int], k: int): bool {\n        $1 = nums.sum()\n        if (totalSum % k !=0) return False\n\n        $1 = totalSum / k\n        $1 = BooleanArray(nums.size)\n\n        def $1(start: int, currentSum: int, remainingSubsets: int): bool {\n            if (remainingSubsets == 0)\n                return True\n            if (currentSum == targetSum)\n                return backtrack(0, 0, remainingSubsets - 1)\n\n            for(i in start until nums.size) {\n                if (!used[i] && currentSum + nums[i] <= targetSum) {\n                    used[i] = True\n                    if (backtrack(i + 1, currentSum + nums[i], remainingSubsets))\n                        return True\n                    // backtracking\n                    used[i] = False\n                }\n            }\n\n            return False\n        }\n\n        return backtrack(0, 0, k)\n    }\n}"  rust="class PartitionToKEqualSumSubsets {\n    fn $1(nums: Vec<i32>, k -> i32) -> bool {\n        val totalSum = nums.sum()\n        if (totalSum % k !=0) return false\n\n        val targetSum = totalSum / k\n        val used = BooleanArray(nums.size)\n\n        fn $1(start -> i32, currentSum -> i32, remainingSubsets -> i32) -> bool {\n            if (remainingSubsets == 0)\n                return true\n            if (currentSum == targetSum)\n                return backtrack(0, 0, remainingSubsets - 1)\n\n            for(i in start until nums.size) {\n                if (!used[i] && currentSum + nums[i] <= targetSum) {\n                    used[i] = true\n                    if (backtrack(i + 1, currentSum + nums[i], remainingSubsets))\n                        return true\n                    // backtracking\n                    used[i] = false\n                }\n            }\n\n            return false\n        }\n\n        return backtrack(0, 0, k)\n    }\n}"  cpp="class PartitionToKEqualSumSubsets {\n    $1(nums: vector<int>, k) {\n        auto $1 = nums.sum()\n        if (totalSum % k !=0) return false\n\n        auto $1 = totalSum / k\n        auto $1 = BooleanArray(nums.size)\n\n        $1(start, currentSum, remainingSubsets) {\n            if (remainingSubsets == 0)\n                return true\n            if (currentSum == targetSum)\n                return backtrack(0, 0, remainingSubsets - 1)\n\n            for(i in start until nums.size) {\n                if (!used[i] && currentSum + nums[i] <= targetSum) {\n                    used[i] = true\n                    if (backtrack(i + 1, currentSum + nums[i], remainingSubsets))\n                        return true\n                    // backtracking\n                    used[i] = false\n                }\n            }\n\n            return false\n        }\n\n        return backtrack(0, 0, k)\n    }\n}" %}

---

## RestoreIPAddresses

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/RestoreIPAddresses.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nclass RestoreIPAddresses {\n    fun restoreIpAddresses(s: String): List<String> {\n        val result = mutableListOf<String>()\n\n        fun dfs(i: Int, path: MutableList<String>) {\n            if (path.size == 4) {\n                if (i == s.length) {\n                    result.add(path.joinToString(\".\"))\n                }\n                return\n            }\n\n            for (len in 1..3) {\n                if (i + len <= s.length) {\n                    val segment = s.substring(i, i + len)\n                    // Skip invalid segments\n                    if ((segment.length > 1 && segment[0] == '0') || segment.toInt() > 255) continue\n                    path.add(segment)\n                    dfs(i + len, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}"  java="class RestoreIPAddresses {\n    public static void $1(s): List<String> {\n        val result = mutableListOf<String>()\n\n        public static void $1(i, path: MutableList<String>) {\n            if (path.size == 4) {\n                if (i == s.length) {\n                    result.add(path.joinToString(\".\"))\n                }\n                return\n            }\n\n            for (len in 1..3) {\n                if (i + len <= s.length) {\n                    val segment = s.substring(i, i + len)\n                    // Skip invalid segments\n                    if ((segment.length > 1 && segment[0] == '0') || segment.toInt() > 255) continue\n                    path.add(segment)\n                    dfs(i + len, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}"  python="class RestoreIPAddresses {\n    def $1(s: str): List<String> {\n        $1 = mutableListOf<String>()\n\n        def $1(i: int, path: MutableList<String>) {\n            if (path.size == 4) {\n                if (i == s.length) {\n                    result.add(path.joinToString(\".\"))\n                }\n                return\n            }\n\n            for (len in 1..3) {\n                if (i + len <= s.length) {\n                    $1 = s.substring(i, i + len)\n                    // Skip invalid segments\n                    if ((segment.length > 1 && segment[0] == '0') || segment.toInt() > 255) continue\n                    path.add(segment)\n                    dfs(i + len, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}"  rust="class RestoreIPAddresses {\n    fn $1(s -> String): List<String> {\n        val result = mutableListOf<String>()\n\n        fn $1(i -> i32, path: MutableList<String>) {\n            if (path.size == 4) {\n                if (i == s.length) {\n                    result.add(path.joinToString(\".\"))\n                }\n                return\n            }\n\n            for (len in 1..3) {\n                if (i + len <= s.length) {\n                    val segment = s.substring(i, i + len)\n                    // Skip invalid segments\n                    if ((segment.length > 1 && segment[0] == '0') || segment.toInt() > 255) continue\n                    path.add(segment)\n                    dfs(i + len, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}"  cpp="class RestoreIPAddresses {\n    $1(s): List<String> {\n        auto $1 = mutableListOf<String>()\n\n        $1(i, path: MutableList<String>) {\n            if (path.size == 4) {\n                if (i == s.length) {\n                    result.add(path.joinToString(\".\"))\n                }\n                return\n            }\n\n            for (len in 1..3) {\n                if (i + len <= s.length) {\n                    auto $1 = s.substring(i, i + len)\n                    // Skip invalid segments\n                    if ((segment.length > 1 && segment[0] == '0') || segment.toInt() > 255) continue\n                    path.add(segment)\n                    dfs(i + len, path)\n                    path.removeAt(path.size - 1)\n                }\n            }\n        }\n\n        dfs(0, mutableListOf())\n        return result\n    }\n}" %}

---

## Strobogrammatic_Number_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/Strobogrammatic_Number_II.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nclass Strobogrammatic_Number_II {\n    fun findStrobogrammatic(n: Int): List<String> {\n        val pairs = listOf(\"0\" to \"0\", \"1\" to \"1\", \"6\" to \"9\", \"8\" to \"8\", \"9\" to \"6\")\n        fun generateStrobogrammatic(currentLength: Int): List<String> {\n            if (currentLength == 0) return listOf(\"\")\n            if (currentLength == 1) return listOf(\"0\", \"1\", \"8\")\n\n            val result = mutableListOf<String>()\n\n            for ((left, right) in pairs) {\n                // Avoid leading zeros\n                if (currentLength == n && left == \"0\") continue\n\n                // Generate the inner strobogrammatic numbers\n                val innerNumbers = generateStrobogrammatic(currentLength - 2)\n\n                // Append the current pair to the inner numbers\n                for (inner in innerNumbers) {\n                    result.add(left + inner + right)\n                }\n            }\n\n            return result\n        }\n\n        return generateStrobogrammatic(n)\n    }\n}"  java="class Strobogrammatic_Number_II {\n    public static void $1(n): List<String> {\n        val pairs = listOf(\"0\" to \"0\", \"1\" to \"1\", \"6\" to \"9\", \"8\" to \"8\", \"9\" to \"6\")\n        public static void $1(currentLength): List<String> {\n            if (currentLength == 0) return listOf(\"\")\n            if (currentLength == 1) return listOf(\"0\", \"1\", \"8\")\n\n            val result = mutableListOf<String>()\n\n            for ((left, right) in pairs) {\n                // Avoid leading zeros\n                if (currentLength == n && left == \"0\") continue\n\n                // Generate the inner strobogrammatic numbers\n                val innerNumbers = generateStrobogrammatic(currentLength - 2)\n\n                // Append the current pair to the inner numbers\n                for (inner in innerNumbers) {\n                    result.add(left + inner + right)\n                }\n            }\n\n            return result\n        }\n\n        return generateStrobogrammatic(n)\n    }\n}"  python="class Strobogrammatic_Number_II {\n    def $1(n: int): List<String> {\n        $1 = listOf(\"0\" to \"0\", \"1\" to \"1\", \"6\" to \"9\", \"8\" to \"8\", \"9\" to \"6\")\n        def $1(currentLength: int): List<String> {\n            if (currentLength == 0) return listOf(\"\")\n            if (currentLength == 1) return listOf(\"0\", \"1\", \"8\")\n\n            $1 = mutableListOf<String>()\n\n            for ((left, right) in pairs) {\n                // Avoid leading zeros\n                if (currentLength == n && left == \"0\") continue\n\n                // Generate the inner strobogrammatic numbers\n                $1 = generateStrobogrammatic(currentLength - 2)\n\n                // Append the current pair to the inner numbers\n                for (inner in innerNumbers) {\n                    result.add(left + inner + right)\n                }\n            }\n\n            return result\n        }\n\n        return generateStrobogrammatic(n)\n    }\n}"  rust="class Strobogrammatic_Number_II {\n    fn $1(n -> i32): List<String> {\n        val pairs = listOf(\"0\" to \"0\", \"1\" to \"1\", \"6\" to \"9\", \"8\" to \"8\", \"9\" to \"6\")\n        fn $1(currentLength -> i32): List<String> {\n            if (currentLength == 0) return listOf(\"\")\n            if (currentLength == 1) return listOf(\"0\", \"1\", \"8\")\n\n            val result = mutableListOf<String>()\n\n            for ((left, right) in pairs) {\n                // Avoid leading zeros\n                if (currentLength == n && left == \"0\") continue\n\n                // Generate the inner strobogrammatic numbers\n                val innerNumbers = generateStrobogrammatic(currentLength - 2)\n\n                // Append the current pair to the inner numbers\n                for (inner in innerNumbers) {\n                    result.add(left + inner + right)\n                }\n            }\n\n            return result\n        }\n\n        return generateStrobogrammatic(n)\n    }\n}"  cpp="class Strobogrammatic_Number_II {\n    $1(n): List<String> {\n        auto $1 = listOf(\"0\" to \"0\", \"1\" to \"1\", \"6\" to \"9\", \"8\" to \"8\", \"9\" to \"6\")\n        $1(currentLength): List<String> {\n            if (currentLength == 0) return listOf(\"\")\n            if (currentLength == 1) return listOf(\"0\", \"1\", \"8\")\n\n            auto $1 = mutableListOf<String>()\n\n            for ((left, right) in pairs) {\n                // Avoid leading zeros\n                if (currentLength == n && left == \"0\") continue\n\n                // Generate the inner strobogrammatic numbers\n                auto $1 = generateStrobogrammatic(currentLength - 2)\n\n                // Append the current pair to the inner numbers\n                for (inner in innerNumbers) {\n                    result.add(left + inner + right)\n                }\n            }\n\n            return result\n        }\n\n        return generateStrobogrammatic(n)\n    }\n}" %}

---

## SudokuSolver

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/SudokuSolver.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nclass SudokuSolver {\n    fun solveSudoku(board: Array<CharArray>) {\n        solve(board)\n    }\n\n    private fun solve(board: Array<CharArray>): Boolean {\n        for (row in 0..8) {\n            for (col in 0..8) {\n                if (board[row][col] == '.') {\n                    for (ch in '1'..'9') {\n                        if (isValid(board, row, col, ch)) {\n                            board[row][col] = ch\n                            if (solve(board)) return true\n                            board[row][col] = '.' // backtrack\n                        }\n                    }\n                    return false // no valid number found\n                }\n            }\n        }\n        return true // board solved\n    }\n\n    private fun isValid(board: Array<CharArray>, row: Int, col: Int, ch: Char): Boolean {\n        for (i in 0..8) {\n            if (board[row][i] == ch || board[i][col] == ch) return false\n            val boxRow = 3 * (row / 3) + i / 3\n            val boxCol = 3 * (col / 3) + i % 3\n            if (board[boxRow][boxCol] == ch) return false\n        }\n        return true\n    }\n}"  java="class SudokuSolver {\n    public static void $1(board: Array<CharArray>) {\n        solve(board)\n    }\n\n    private public static void $1(board: Array<CharArray>) {\n        for (row in 0..8) {\n            for (col in 0..8) {\n                if (board[row][col] == '.') {\n                    for (ch in '1'..'9') {\n                        if (isValid(board, row, col, ch)) {\n                            board[row][col] = ch\n                            if (solve(board)) return true\n                            board[row][col] = '.' // backtrack\n                        }\n                    }\n                    return false // no valid number found\n                }\n            }\n        }\n        return true // board solved\n    }\n\n    private public static void $1(board: Array<CharArray>, row, col, ch: Char) {\n        for (i in 0..8) {\n            if (board[row][i] == ch || board[i][col] == ch) return false\n            val boxRow = 3 * (row / 3) + i / 3\n            val boxCol = 3 * (col / 3) + i % 3\n            if (board[boxRow][boxCol] == ch) return false\n        }\n        return true\n    }\n}"  python="class SudokuSolver {\n    def $1(board: Array<CharArray>) {\n        solve(board)\n    }\n\n    private def $1(board: Array<CharArray>): bool {\n        for (row in 0..8) {\n            for (col in 0..8) {\n                if (board[row][col] == '.') {\n                    for (ch in '1'..'9') {\n                        if (isValid(board, row, col, ch)) {\n                            board[row][col] = ch\n                            if (solve(board)) return True\n                            board[row][col] = '.' // backtrack\n                        }\n                    }\n                    return False // no valid number found\n                }\n            }\n        }\n        return True // board solved\n    }\n\n    private def $1(board: Array<CharArray>, row: int, col: int, ch: Char): bool {\n        for (i in 0..8) {\n            if (board[row][i] == ch || board[i][col] == ch) return False\n            $1 = 3 * (row / 3) + i / 3\n            $1 = 3 * (col / 3) + i % 3\n            if (board[boxRow][boxCol] == ch) return False\n        }\n        return True\n    }\n}"  rust="class SudokuSolver {\n    fn $1(board: Array<CharArray>) {\n        solve(board)\n    }\n\n    private fn $1(board: Array<CharArray>) -> bool {\n        for (row in 0..8) {\n            for (col in 0..8) {\n                if (board[row][col] == '.') {\n                    for (ch in '1'..'9') {\n                        if (isValid(board, row, col, ch)) {\n                            board[row][col] = ch\n                            if (solve(board)) return true\n                            board[row][col] = '.' // backtrack\n                        }\n                    }\n                    return false // no valid number found\n                }\n            }\n        }\n        return true // board solved\n    }\n\n    private fn $1(board: Array<CharArray>, row -> i32, col -> i32, ch: Char) -> bool {\n        for (i in 0..8) {\n            if (board[row][i] == ch || board[i][col] == ch) return false\n            val boxRow = 3 * (row / 3) + i / 3\n            val boxCol = 3 * (col / 3) + i % 3\n            if (board[boxRow][boxCol] == ch) return false\n        }\n        return true\n    }\n}"  cpp="class SudokuSolver {\n    $1(board: Array<CharArray>) {\n        solve(board)\n    }\n\n    private $1(board: Array<CharArray>) {\n        for (row in 0..8) {\n            for (col in 0..8) {\n                if (board[row][col] == '.') {\n                    for (ch in '1'..'9') {\n                        if (isValid(board, row, col, ch)) {\n                            board[row][col] = ch\n                            if (solve(board)) return true\n                            board[row][col] = '.' // backtrack\n                        }\n                    }\n                    return false // no valid number found\n                }\n            }\n        }\n        return true // board solved\n    }\n\n    private $1(board: Array<CharArray>, row, col, ch: Char) {\n        for (i in 0..8) {\n            if (board[row][i] == ch || board[i][col] == ch) return false\n            auto $1 = 3 * (row / 3) + i / 3\n            auto $1 = 3 * (col / 3) + i % 3\n            if (board[boxRow][boxCol] == ch) return false\n        }\n        return true\n    }\n}" %}

---

## SudokuSolverSet

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/backtracking/SudokuSolverSet.kt`

{% include code-tabs.html  kotlin="package backtracking\n\nclass SudokuSolverSet {\n    fun solveSudoku(board: Array<CharArray>) {\n        val seen = mutableSetOf<String>()\n\n        // Add initial values to the seen set\n        for (i in 0..8) {\n            for (j in 0..8) {\n                val ch = board[i][j]\n                if (ch != '.') {\n                    addToSet(i, j, ch, seen)\n                }\n            }\n        }\n\n        fun isSafe(i: Int, j: Int, c: Char): Boolean {\n            return \"$c row $i\" !in seen &&\n                    \"$c col $j\" !in seen &&\n                    \"$c block ${i / 3}${j / 3}\" !in seen\n        }\n\n        fun dfs(): Boolean {\n            for (i in 0..8) {\n                for (j in 0..8) {\n                    if (board[i][j] != '.') continue\n                    for (c in '1'..'9') {\n                        if (!isSafe(i, j, c)) continue\n                        board[i][j] = c\n                        addToSet(i, j, c, seen)\n                        if (dfs()) return true\n                        board[i][j] = '.'\n                        removeFromSet(i, j, c, seen)\n                    }\n                    return false\n                }\n            }\n            return true\n        }\n\n        dfs()\n    }\n\n    fun addToSet(i: Int, j: Int, c: Char, seen: MutableSet<String>) {\n        seen.add(\"$c row $i\")\n        seen.add(\"$c col $j\")\n        seen.add(\"$c block ${i / 3}${j / 3}\")\n    }\n\n    fun removeFromSet(i: Int, j: Int, c: Char, seen: MutableSet<String>) {\n        seen.remove(\"$c row $i\")\n        seen.remove(\"$c col $j\")\n        seen.remove(\"$c block ${i / 3}${j / 3}\")\n    }\n}\n"  java="class SudokuSolverSet {\n    public static void $1(board: Array<CharArray>) {\n        val seen = mutableSetOf<String>()\n\n        // Add initial values to the seen set\n        for (i in 0..8) {\n            for (j in 0..8) {\n                val ch = board[i][j]\n                if (ch != '.') {\n                    addToSet(i, j, ch, seen)\n                }\n            }\n        }\n\n        public static void $1(i, j, c: Char) {\n            return \"$c row $i\" !in seen &&\n                    \"$c col $j\" !in seen &&\n                    \"$c block ${i / 3}${j / 3}\" !in seen\n        }\n\n        public static void $1() {\n            for (i in 0..8) {\n                for (j in 0..8) {\n                    if (board[i][j] != '.') continue\n                    for (c in '1'..'9') {\n                        if (!isSafe(i, j, c)) continue\n                        board[i][j] = c\n                        addToSet(i, j, c, seen)\n                        if (dfs()) return true\n                        board[i][j] = '.'\n                        removeFromSet(i, j, c, seen)\n                    }\n                    return false\n                }\n            }\n            return true\n        }\n\n        dfs()\n    }\n\n    public static void $1(i, j, c: Char, seen: MutableSet<String>) {\n        seen.add(\"$c row $i\")\n        seen.add(\"$c col $j\")\n        seen.add(\"$c block ${i / 3}${j / 3}\")\n    }\n\n    public static void $1(i, j, c: Char, seen: MutableSet<String>) {\n        seen.remove(\"$c row $i\")\n        seen.remove(\"$c col $j\")\n        seen.remove(\"$c block ${i / 3}${j / 3}\")\n    }\n}"  python="class SudokuSolverSet {\n    def $1(board: Array<CharArray>) {\n        $1 = mutableSetOf<String>()\n\n        // Add initial values to the seen set\n        for (i in 0..8) {\n            for (j in 0..8) {\n                $1 = board[i][j]\n                if (ch != '.') {\n                    addToSet(i, j, ch, seen)\n                }\n            }\n        }\n\n        def $1(i: int, j: int, c: Char): bool {\n            return \"$c row $i\" !in seen &&\n                    \"$c col $j\" !in seen &&\n                    \"$c block ${i / 3}${j / 3}\" !in seen\n        }\n\n        def $1(): bool {\n            for (i in 0..8) {\n                for (j in 0..8) {\n                    if (board[i][j] != '.') continue\n                    for (c in '1'..'9') {\n                        if (!isSafe(i, j, c)) continue\n                        board[i][j] = c\n                        addToSet(i, j, c, seen)\n                        if (dfs()) return True\n                        board[i][j] = '.'\n                        removeFromSet(i, j, c, seen)\n                    }\n                    return False\n                }\n            }\n            return True\n        }\n\n        dfs()\n    }\n\n    def $1(i: int, j: int, c: Char, seen: MutableSet<String>) {\n        seen.add(\"$c row $i\")\n        seen.add(\"$c col $j\")\n        seen.add(\"$c block ${i / 3}${j / 3}\")\n    }\n\n    def $1(i: int, j: int, c: Char, seen: MutableSet<String>) {\n        seen.remove(\"$c row $i\")\n        seen.remove(\"$c col $j\")\n        seen.remove(\"$c block ${i / 3}${j / 3}\")\n    }\n}"  rust="class SudokuSolverSet {\n    fn $1(board: Array<CharArray>) {\n        val seen = mutableSetOf<String>()\n\n        // Add initial values to the seen set\n        for (i in 0..8) {\n            for (j in 0..8) {\n                val ch = board[i][j]\n                if (ch != '.') {\n                    addToSet(i, j, ch, seen)\n                }\n            }\n        }\n\n        fn $1(i -> i32, j -> i32, c: Char) -> bool {\n            return \"$c row $i\" !in seen &&\n                    \"$c col $j\" !in seen &&\n                    \"$c block ${i / 3}${j / 3}\" !in seen\n        }\n\n        fn $1() -> bool {\n            for (i in 0..8) {\n                for (j in 0..8) {\n                    if (board[i][j] != '.') continue\n                    for (c in '1'..'9') {\n                        if (!isSafe(i, j, c)) continue\n                        board[i][j] = c\n                        addToSet(i, j, c, seen)\n                        if (dfs()) return true\n                        board[i][j] = '.'\n                        removeFromSet(i, j, c, seen)\n                    }\n                    return false\n                }\n            }\n            return true\n        }\n\n        dfs()\n    }\n\n    fn $1(i -> i32, j -> i32, c: Char, seen: MutableSet<String>) {\n        seen.add(\"$c row $i\")\n        seen.add(\"$c col $j\")\n        seen.add(\"$c block ${i / 3}${j / 3}\")\n    }\n\n    fn $1(i -> i32, j -> i32, c: Char, seen: MutableSet<String>) {\n        seen.remove(\"$c row $i\")\n        seen.remove(\"$c col $j\")\n        seen.remove(\"$c block ${i / 3}${j / 3}\")\n    }\n}"  cpp="class SudokuSolverSet {\n    $1(board: Array<CharArray>) {\n        auto $1 = mutableSetOf<String>()\n\n        // Add initial values to the seen set\n        for (i in 0..8) {\n            for (j in 0..8) {\n                auto $1 = board[i][j]\n                if (ch != '.') {\n                    addToSet(i, j, ch, seen)\n                }\n            }\n        }\n\n        $1(i, j, c: Char) {\n            return \"$c row $i\" !in seen &&\n                    \"$c col $j\" !in seen &&\n                    \"$c block ${i / 3}${j / 3}\" !in seen\n        }\n\n        $1() {\n            for (i in 0..8) {\n                for (j in 0..8) {\n                    if (board[i][j] != '.') continue\n                    for (c in '1'..'9') {\n                        if (!isSafe(i, j, c)) continue\n                        board[i][j] = c\n                        addToSet(i, j, c, seen)\n                        if (dfs()) return true\n                        board[i][j] = '.'\n                        removeFromSet(i, j, c, seen)\n                    }\n                    return false\n                }\n            }\n            return true\n        }\n\n        dfs()\n    }\n\n    $1(i, j, c: Char, seen: MutableSet<String>) {\n        seen.add(\"$c row $i\")\n        seen.add(\"$c col $j\")\n        seen.add(\"$c block ${i / 3}${j / 3}\")\n    }\n\n    $1(i, j, c: Char, seen: MutableSet<String>) {\n        seen.remove(\"$c row $i\")\n        seen.remove(\"$c col $j\")\n        seen.remove(\"$c block ${i / 3}${j / 3}\")\n    }\n}" %}
