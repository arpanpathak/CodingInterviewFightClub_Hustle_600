---
layout: chapter
title: "Caches"
chapter_number: 12
chapter_title: "Caches"
toc: true
prev_chapter:
  url: "/chapters/11-backtracking"
  title: "Backtracking"
---

# Caches

> **3 problems** from the CrackGoogle repository. Click the tabs to switch between **Kotlin, Java, Python, Rust, and C++**.

## Complete Problem Set

| # | Problem | Topic | Code Available |
|---|---------|-------|----------------|
| 1 | **LFUCache** | cache | ✅ All 5 languages |
| 2 | **LRUCache** | cache | ✅ All 5 languages |
| 3 | **LRUCacheLinkedList** | cache | ✅ All 5 languages |

---

## LFUCache

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/cache/LFUCache.kt`

{% include code-tabs.html  kotlin="package cache\n\nclass LFUCache(capacity: Int) {\n\n    private val vals = mutableMapOf<Int, Int>()\n    private val freq = mutableMapOf<Int, Int>()\n    private val lists = mutableMapOf<Int, LinkedHashSet<Int>>()\n\n    private val MAX_SIZE = capacity\n    private var min = -1\n\n    init {\n        lists[1] = LinkedHashSet()\n    }\n\n    fun get(key: Int): Int {\n        if (!vals.containsKey(key))\n            return -1\n        val count = freq[key]!!\n        freq[key] = count + 1\n        lists[count]?.remove(key)\n\n        if (count == min && lists[count]?.size == 0 ) {\n            min++\n        }\n\n        if (!lists.containsKey(count  + 1)) {\n            lists[count + 1] = LinkedHashSet()\n        }\n\n        lists[count + 1]?.add(key)\n\n        return vals[key]!!\n    }\n\n    fun put(key: Int, value: Int) {\n        if (MAX_SIZE <= 0)\n            return ;\n        if (vals.containsKey(key)) {\n            vals.put(key, value)\n            get(key)\n            return ;\n        }\n\n        if (vals.size >= MAX_SIZE) {\n            val evict = lists.get(min)?.first()\n\n            lists.get(min)?.remove(evict)\n            vals.remove(evict)\n            freq.remove(evict)\n        }\n\n        vals[key] = value\n        freq[key] = 1\n        min = 1\n        lists[1]?.add(key)\n    }\n\n}"  java="class LFUCache(capacity) {\n\n    private val vals = mutableMapOf<Int, Int>()\n    private val freq = mutableMapOf<Int, Int>()\n    private val lists = mutableMapOf<Int, LinkedHashSet<Int>>()\n\n    private val MAX_SIZE = capacity\n    private var min = -1\n\n    init {\n        lists[1] = LinkedHashSet()\n    }\n\n    public static void $1(key) {\n        if (!vals.containsKey(key))\n            return -1\n        val count = freq[key]!!\n        freq[key] = count + 1\n        lists[count]?.remove(key)\n\n        if (count == min && lists[count]?.size == 0 ) {\n            min++\n        }\n\n        if (!lists.containsKey(count  + 1)) {\n            lists[count + 1] = LinkedHashSet()\n        }\n\n        lists[count + 1]?.add(key)\n\n        return vals[key]!!\n    }\n\n    public static void $1(key, value) {\n        if (MAX_SIZE <= 0)\n            return ;\n        if (vals.containsKey(key)) {\n            vals.put(key, value)\n            get(key)\n            return ;\n        }\n\n        if (vals.size >= MAX_SIZE) {\n            val evict = lists.get(min)?.first()\n\n            lists.get(min)?.remove(evict)\n            vals.remove(evict)\n            freq.remove(evict)\n        }\n\n        vals[key] = value\n        freq[key] = 1\n        min = 1\n        lists[1]?.add(key)\n    }\n\n}"  python="class LFUCache(capacity: int) {\n\n    private $1 = mutableMapOf<Int, Int>()\n    private $1 = mutableMapOf<Int, Int>()\n    private $1 = mutableMapOf<Int, LinkedHashSet<Int>>()\n\n    private $1 = capacity\n    private $1 = -1\n\n    init {\n        lists[1] = LinkedHashSet()\n    }\n\n    def $1(key: int): int {\n        if (!vals.containsKey(key))\n            return -1\n        $1 = freq[key]!!\n        freq[key] = count + 1\n        lists[count]?.remove(key)\n\n        if (count == min && lists[count]?.size == 0 ) {\n            min++\n        }\n\n        if (!lists.containsKey(count  + 1)) {\n            lists[count + 1] = LinkedHashSet()\n        }\n\n        lists[count + 1]?.add(key)\n\n        return vals[key]!!\n    }\n\n    def $1(key: int, value: int) {\n        if (MAX_SIZE <= 0)\n            return ;\n        if (vals.containsKey(key)) {\n            vals.put(key, value)\n            get(key)\n            return ;\n        }\n\n        if (vals.size >= MAX_SIZE) {\n            $1 = lists.get(min)?.first()\n\n            lists.get(min)?.remove(evict)\n            vals.remove(evict)\n            freq.remove(evict)\n        }\n\n        vals[key] = value\n        freq[key] = 1\n        min = 1\n        lists[1]?.add(key)\n    }\n\n}"  rust="class LFUCache(capacity -> i32) {\n\n    private val vals = mutableMapOf<Int, Int>()\n    private val freq = mutableMapOf<Int, Int>()\n    private val lists = mutableMapOf<Int, LinkedHashSet<Int>>()\n\n    private val MAX_SIZE = capacity\n    private var min = -1\n\n    init {\n        lists[1] = LinkedHashSet()\n    }\n\n    fn $1(key -> i32) -> i32 {\n        if (!vals.containsKey(key))\n            return -1\n        val count = freq[key]!!\n        freq[key] = count + 1\n        lists[count]?.remove(key)\n\n        if (count == min && lists[count]?.size == 0 ) {\n            min++\n        }\n\n        if (!lists.containsKey(count  + 1)) {\n            lists[count + 1] = LinkedHashSet()\n        }\n\n        lists[count + 1]?.add(key)\n\n        return vals[key]!!\n    }\n\n    fn $1(key -> i32, value -> i32) {\n        if (MAX_SIZE <= 0)\n            return ;\n        if (vals.containsKey(key)) {\n            vals.put(key, value)\n            get(key)\n            return ;\n        }\n\n        if (vals.size >= MAX_SIZE) {\n            val evict = lists.get(min)?.first()\n\n            lists.get(min)?.remove(evict)\n            vals.remove(evict)\n            freq.remove(evict)\n        }\n\n        vals[key] = value\n        freq[key] = 1\n        min = 1\n        lists[1]?.add(key)\n    }\n\n}"  cpp="class LFUCache(capacity) {\n\n    private auto $1 = mutableMapOf<Int, Int>()\n    private auto $1 = mutableMapOf<Int, Int>()\n    private auto $1 = mutableMapOf<Int, LinkedHashSet<Int>>()\n\n    private auto $1 = capacity\n    private auto $1 = -1\n\n    init {\n        lists[1] = LinkedHashSet()\n    }\n\n    $1(key) {\n        if (!vals.containsKey(key))\n            return -1\n        auto $1 = freq[key]!!\n        freq[key] = count + 1\n        lists[count]?.remove(key)\n\n        if (count == min && lists[count]?.size == 0 ) {\n            min++\n        }\n\n        if (!lists.containsKey(count  + 1)) {\n            lists[count + 1] = LinkedHashSet()\n        }\n\n        lists[count + 1]?.add(key)\n\n        return vals[key]!!\n    }\n\n    $1(key, value) {\n        if (MAX_SIZE <= 0)\n            return ;\n        if (vals.containsKey(key)) {\n            vals.put(key, value)\n            get(key)\n            return ;\n        }\n\n        if (vals.size >= MAX_SIZE) {\n            auto $1 = lists.get(min)?.first()\n\n            lists.get(min)?.remove(evict)\n            vals.remove(evict)\n            freq.remove(evict)\n        }\n\n        vals[key] = value\n        freq[key] = 1\n        min = 1\n        lists[1]?.add(key)\n    }\n\n}" %}

---

## LRUCache

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/cache/LRUCache.kt`

{% include code-tabs.html  kotlin="package cache\n\nclass LRUCache(private val capacity: Int) {\n\n    private val cache: LinkedHashMap<Int, Int> = LinkedHashMap()\n\n    fun get(key: Int): Int {\n        val data = cache[key] ?: -1\n\n        // cache[key] = data // Move accessed element to the end to mark it as recently used\n        if (data !=- 1)\n            put(key, data)\n        return data\n    }\n\n    fun put(key: Int, value: Int) {\n        if (cache.containsKey(key)) {\n            cache.remove(key)\n        } else if (cache.size == this.capacity) {\n            // Eviction policy\n            cache.remove(cache.keys.first()) // Remove the least recently used element\n        }\n        cache[key] = value // Insert the new element\n    }\n}"  java="class LRUCache(private val capacity) {\n\n    private val cache: LinkedHashMap<Int, Int> = LinkedHashMap()\n\n    public static void $1(key) {\n        val data = cache[key] ?: -1\n\n        // cache[key] = data // Move accessed element to the end to mark it as recently used\n        if (data !=- 1)\n            put(key, data)\n        return data\n    }\n\n    public static void $1(key, value) {\n        if (cache.containsKey(key)) {\n            cache.remove(key)\n        } else if (cache.size == this.capacity) {\n            // Eviction policy\n            cache.remove(cache.keys.first()) // Remove the least recently used element\n        }\n        cache[key] = value // Insert the new element\n    }\n}"  python="class LRUCache(private val capacity: int) {\n\n    private val cache: LinkedHashMap<Int, Int> = LinkedHashMap()\n\n    def $1(key: int): int {\n        $1 = cache[key] ?: -1\n\n        // cache[key] = data // Move accessed element to the end to mark it as recently used\n        if (data !=- 1)\n            put(key, data)\n        return data\n    }\n\n    def $1(key: int, value: int) {\n        if (cache.containsKey(key)) {\n            cache.remove(key)\n        } else if (cache.size == this.capacity) {\n            // Eviction policy\n            cache.remove(cache.keys.first()) // Remove the least recently used element\n        }\n        cache[key] = value // Insert the new element\n    }\n}"  rust="class LRUCache(private val capacity -> i32) {\n\n    private val cache: LinkedHashMap<Int, Int> = LinkedHashMap()\n\n    fn $1(key -> i32) -> i32 {\n        val data = cache[key] ?: -1\n\n        // cache[key] = data // Move accessed element to the end to mark it as recently used\n        if (data !=- 1)\n            put(key, data)\n        return data\n    }\n\n    fn $1(key -> i32, value -> i32) {\n        if (cache.containsKey(key)) {\n            cache.remove(key)\n        } else if (cache.size == this.capacity) {\n            // Eviction policy\n            cache.remove(cache.keys.first()) // Remove the least recently used element\n        }\n        cache[key] = value // Insert the new element\n    }\n}"  cpp="class LRUCache(private val capacity) {\n\n    private val cache: LinkedHashMap<Int, Int> = LinkedHashMap()\n\n    $1(key) {\n        auto $1 = cache[key] ?: -1\n\n        // cache[key] = data // Move accessed element to the end to mark it as recently used\n        if (data !=- 1)\n            put(key, data)\n        return data\n    }\n\n    $1(key, value) {\n        if (cache.containsKey(key)) {\n            cache.remove(key)\n        } else if (cache.size == this.capacity) {\n            // Eviction policy\n            cache.remove(cache.keys.first()) // Remove the least recently used element\n        }\n        cache[key] = value // Insert the new element\n    }\n}" %}

---

## LRUCacheLinkedList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/cache/LRUCacheLinkedList.kt`

{% include code-tabs.html  kotlin="package cache\n\nimport java.util.*\n\nclass LRUCacheLinkedList(private val capacity: Int) {\n    private class Node(val key: Int, var value: Int) {\n        var prev: Node? = null\n        var next: Node? = null\n    }\n\n    private val cache = HashMap<Int, Node>() // Hash map for O(1) access\n    private val head = Node(-1, -1) // Dummy head\n    private val tail = Node(-1, -1) // Dummy tail\n\n    init {\n        head.next = tail\n        tail.prev = head\n    }\n\n    fun get(key: Int): Int {\n        val node = cache[key] ?: return -1 // Key not found\n        moveToHead(node) // Move the node to the head (most recently used)\n        return node.value\n    }\n\n    fun put(key: Int, value: Int) {\n        val node = cache[key]\n        if (node != null) {\n            // Update the value and move to head\n            node.value = value\n            moveToHead(node)\n        } else {\n            // Create a new node and add to head\n            val newNode = Node(key, value)\n            cache[key] = newNode\n            addToHead(newNode)\n\n            // If capacity is exceeded, remove the tail node (least recently used)\n            if (cache.size > capacity) {\n                val tailNode = removeTail()\n                cache.remove(tailNode.key)\n            }\n        }\n    }\n\n    private fun addToHead(node: Node) {\n        // Add the node to the head of the list\n        node.prev = head\n        node.next = head.next\n        head.next?.prev = node\n        head.next = node\n    }\n\n    private fun removeNode(node: Node) {\n        // Remove the node from the list\n        node.prev?.next = node.next\n        node.next?.prev = node.prev\n    }\n\n    private fun moveToHead(node: Node) {\n        // Move the node to the head of the list\n        removeNode(node)\n        addToHead(node)\n    }\n\n    private fun removeTail(): Node {\n        // Remove and return the tail node (least recently used)\n        val tailNode = tail.prev!!\n        removeNode(tailNode)\n        return tailNode\n    }\n}\n\nfun main() {\n    val lruCache = LRUCache(2)\n    lruCache.put(1, 1) // Cache is {1=1}\n    lruCache.put(2, 2) // Cache is {1=1, 2=2}\n    println(lruCache.get(1)) // Returns 1 (Cache is {2=2, 1=1})\n    lruCache.put(3, 3) // Evicts key 2, cache is {1=1, 3=3}\n    println(lruCache.get(2)) // Returns -1 (not found)\n    lruCache.put(4, 4) // Evicts key 1, cache is {3=3, 4=4}\n    println(lruCache.get(1)) // Returns -1 (not found)\n    println(lruCache.get(3)) // Returns 3 (Cache is {4=4, 3=3})\n    println(lruCache.get(4)) // Returns 4 (Cache is {3=3, 4=4})\n}\n"  java="class LRUCacheLinkedList(private val capacity) {\n    private class Node(val key, var value) {\n        var prev: Node? = null\n        var next: Node? = null\n    }\n\n    private val cache = HashMap<Int, Node>() // Hash map for O(1) access\n    private val head = Node(-1, -1) // Dummy head\n    private val tail = Node(-1, -1) // Dummy tail\n\n    init {\n        head.next = tail\n        tail.prev = head\n    }\n\n    public static void $1(key) {\n        val node = cache[key] ?: return -1 // Key not found\n        moveToHead(node) // Move the node to the head (most recently used)\n        return node.value\n    }\n\n    public static void $1(key, value) {\n        val node = cache[key]\n        if (node != null) {\n            // Update the value and move to head\n            node.value = value\n            moveToHead(node)\n        } else {\n            // Create a new node and add to head\n            val newNode = Node(key, value)\n            cache[key] = newNode\n            addToHead(newNode)\n\n            // If capacity is exceeded, remove the tail node (least recently used)\n            if (cache.size > capacity) {\n                val tailNode = removeTail()\n                cache.remove(tailNode.key)\n            }\n        }\n    }\n\n    private public static void $1(node: Node) {\n        // Add the node to the head of the list\n        node.prev = head\n        node.next = head.next\n        head.next?.prev = node\n        head.next = node\n    }\n\n    private public static void $1(node: Node) {\n        // Remove the node from the list\n        node.prev?.next = node.next\n        node.next?.prev = node.prev\n    }\n\n    private public static void $1(node: Node) {\n        // Move the node to the head of the list\n        removeNode(node)\n        addToHead(node)\n    }\n\n    private public static void $1(): Node {\n        // Remove and return the tail node (least recently used)\n        val tailNode = tail.prev!!\n        removeNode(tailNode)\n        return tailNode\n    }\n}\n\npublic static void $1() {\n    val lruCache = LRUCache(2)\n    lruCache.put(1, 1) // Cache is {1=1}\n    lruCache.put(2, 2) // Cache is {1=1, 2=2}\n    println(lruCache.get(1)) // Returns 1 (Cache is {2=2, 1=1})\n    lruCache.put(3, 3) // Evicts key 2, cache is {1=1, 3=3}\n    println(lruCache.get(2)) // Returns -1 (not found)\n    lruCache.put(4, 4) // Evicts key 1, cache is {3=3, 4=4}\n    println(lruCache.get(1)) // Returns -1 (not found)\n    println(lruCache.get(3)) // Returns 3 (Cache is {4=4, 3=3})\n    println(lruCache.get(4)) // Returns 4 (Cache is {3=3, 4=4})\n}"  python="class LRUCacheLinkedList(private val capacity: int) {\n    private class Node(val key: int, var value: int) {\n        var prev: Node? = None\n        var next: Node? = None\n    }\n\n    private $1 = HashMap<Int, Node>() // Hash map for O(1) access\n    private $1 = Node(-1, -1) // Dummy head\n    private $1 = Node(-1, -1) // Dummy tail\n\n    init {\n        head.next = tail\n        tail.prev = head\n    }\n\n    def $1(key: int): int {\n        $1 = cache[key] ?: return -1 // Key not found\n        moveToHead(node) // Move the node to the head (most recently used)\n        return node.value\n    }\n\n    def $1(key: int, value: int) {\n        $1 = cache[key]\n        if (node != None) {\n            // Update the value and move to head\n            node.value = value\n            moveToHead(node)\n        } else {\n            // Create a new node and add to head\n            $1 = Node(key, value)\n            cache[key] = newNode\n            addToHead(newNode)\n\n            // If capacity is exceeded, remove the tail node (least recently used)\n            if (cache.size > capacity) {\n                $1 = removeTail()\n                cache.remove(tailNode.key)\n            }\n        }\n    }\n\n    private def $1(node: Node) {\n        // Add the node to the head of the list\n        node.prev = head\n        node.next = head.next\n        head.next?.prev = node\n        head.next = node\n    }\n\n    private def $1(node: Node) {\n        // Remove the node from the list\n        node.prev?.next = node.next\n        node.next?.prev = node.prev\n    }\n\n    private def $1(node: Node) {\n        // Move the node to the head of the list\n        removeNode(node)\n        addToHead(node)\n    }\n\n    private def $1(): Node {\n        // Remove and return the tail node (least recently used)\n        $1 = tail.prev!!\n        removeNode(tailNode)\n        return tailNode\n    }\n}\n\ndef $1() {\n    $1 = LRUCache(2)\n    lruCache.put(1, 1) // Cache is {1=1}\n    lruCache.put(2, 2) // Cache is {1=1, 2=2}\n    print(lruCache.get(1)) // Returns 1 (Cache is {2=2, 1=1})\n    lruCache.put(3, 3) // Evicts key 2, cache is {1=1, 3=3}\n    print(lruCache.get(2)) // Returns -1 (not found)\n    lruCache.put(4, 4) // Evicts key 1, cache is {3=3, 4=4}\n    print(lruCache.get(1)) // Returns -1 (not found)\n    print(lruCache.get(3)) // Returns 3 (Cache is {4=4, 3=3})\n    print(lruCache.get(4)) // Returns 4 (Cache is {3=3, 4=4})\n}"  rust="class LRUCacheLinkedList(private val capacity -> i32) {\n    private class Node(val key -> i32, var value -> i32) {\n        var prev: Node? = None\n        var next: Node? = None\n    }\n\n    private val cache = HashMap<Int, Node>() // Hash map for O(1) access\n    private val head = Node(-1, -1) // Dummy head\n    private val tail = Node(-1, -1) // Dummy tail\n\n    init {\n        head.next = tail\n        tail.prev = head\n    }\n\n    fn $1(key -> i32) -> i32 {\n        val node = cache[key] ?: return -1 // Key not found\n        moveToHead(node) // Move the node to the head (most recently used)\n        return node.value\n    }\n\n    fn $1(key -> i32, value -> i32) {\n        val node = cache[key]\n        if (node != None) {\n            // Update the value and move to head\n            node.value = value\n            moveToHead(node)\n        } else {\n            // Create a new node and add to head\n            val newNode = Node(key, value)\n            cache[key] = newNode\n            addToHead(newNode)\n\n            // If capacity is exceeded, remove the tail node (least recently used)\n            if (cache.size > capacity) {\n                val tailNode = removeTail()\n                cache.remove(tailNode.key)\n            }\n        }\n    }\n\n    private fn $1(node: Node) {\n        // Add the node to the head of the list\n        node.prev = head\n        node.next = head.next\n        head.next?.prev = node\n        head.next = node\n    }\n\n    private fn $1(node: Node) {\n        // Remove the node from the list\n        node.prev?.next = node.next\n        node.next?.prev = node.prev\n    }\n\n    private fn $1(node: Node) {\n        // Move the node to the head of the list\n        removeNode(node)\n        addToHead(node)\n    }\n\n    private fn $1(): Node {\n        // Remove and return the tail node (least recently used)\n        val tailNode = tail.prev!!\n        removeNode(tailNode)\n        return tailNode\n    }\n}\n\nfn $1() {\n    val lruCache = LRUCache(2)\n    lruCache.put(1, 1) // Cache is {1=1}\n    lruCache.put(2, 2) // Cache is {1=1, 2=2}\n    println!(lruCache.get(1)) // Returns 1 (Cache is {2=2, 1=1})\n    lruCache.put(3, 3) // Evicts key 2, cache is {1=1, 3=3}\n    println!(lruCache.get(2)) // Returns -1 (not found)\n    lruCache.put(4, 4) // Evicts key 1, cache is {3=3, 4=4}\n    println!(lruCache.get(1)) // Returns -1 (not found)\n    println!(lruCache.get(3)) // Returns 3 (Cache is {4=4, 3=3})\n    println!(lruCache.get(4)) // Returns 4 (Cache is {3=3, 4=4})\n}"  cpp="class LRUCacheLinkedList(private val capacity) {\n    private class Node(val key, var value) {\n        var prev: Node? = nullptr\n        var next: Node? = nullptr\n    }\n\n    private auto $1 = HashMap<Int, Node>() // Hash map for O(1) access\n    private auto $1 = Node(-1, -1) // Dummy head\n    private auto $1 = Node(-1, -1) // Dummy tail\n\n    init {\n        head.next = tail\n        tail.prev = head\n    }\n\n    $1(key) {\n        auto $1 = cache[key] ?: return -1 // Key not found\n        moveToHead(node) // Move the node to the head (most recently used)\n        return node.value\n    }\n\n    $1(key, value) {\n        auto $1 = cache[key]\n        if (node != nullptr) {\n            // Update the value and move to head\n            node.value = value\n            moveToHead(node)\n        } else {\n            // Create a new node and add to head\n            auto $1 = Node(key, value)\n            cache[key] = newNode\n            addToHead(newNode)\n\n            // If capacity is exceeded, remove the tail node (least recently used)\n            if (cache.size > capacity) {\n                auto $1 = removeTail()\n                cache.remove(tailNode.key)\n            }\n        }\n    }\n\n    private $1(node: Node) {\n        // Add the node to the head of the list\n        node.prev = head\n        node.next = head.next\n        head.next?.prev = node\n        head.next = node\n    }\n\n    private $1(node: Node) {\n        // Remove the node from the list\n        node.prev?.next = node.next\n        node.next?.prev = node.prev\n    }\n\n    private $1(node: Node) {\n        // Move the node to the head of the list\n        removeNode(node)\n        addToHead(node)\n    }\n\n    private $1(): Node {\n        // Remove and return the tail node (least recently used)\n        auto $1 = tail.prev!!\n        removeNode(tailNode)\n        return tailNode\n    }\n}\n\n$1() {\n    auto $1 = LRUCache(2)\n    lruCache.put(1, 1) // Cache is {1=1}\n    lruCache.put(2, 2) // Cache is {1=1, 2=2}\n    cout <<(lruCache.get(1)) // Returns 1 (Cache is {2=2, 1=1})\n    lruCache.put(3, 3) // Evicts key 2, cache is {1=1, 3=3}\n    cout <<(lruCache.get(2)) // Returns -1 (not found)\n    lruCache.put(4, 4) // Evicts key 1, cache is {3=3, 4=4}\n    cout <<(lruCache.get(1)) // Returns -1 (not found)\n    cout <<(lruCache.get(3)) // Returns 3 (Cache is {4=4, 3=3})\n    cout <<(lruCache.get(4)) // Returns 4 (Cache is {3=3, 4=4})\n}" %}
