---
layout: chapter
title: "Arrays & Two Pointers"
chapter_number: 3
chapter_title: "Arrays & Two Pointers"
toc: true
prev_chapter:
  url: "/chapters/02-dynamic-programming"
  title: "Dynamic Programming"
next_chapter:
  url: "/chapters/04-linked-lists"
  title: "Linked Lists"
---

## Sample Problems

### PalindromeNumber
{% include code-tabs.html  kotlin="package numbers\n\nclass PalindromeNumber {\n    fun isPalindrome(x: Int): Boolean {\n        var (reduced, sum) = listOf(x, 0, 1)\n\n        while (reduced > 0)  {\n            sum = sum * 10 + reduced % 10\n            reduced /= 10\n        }\n        return sum == x\n    }\n}\n"  java="class PalindromeNumber {\n    public static void $1(x) {\n        var (reduced, sum) = listOf(x, 0, 1)\n\n        while (reduced > 0)  {\n            sum = sum * 10 + reduced % 10\n            reduced /= 10\n        }\n        return sum == x\n    }\n}"  python="class PalindromeNumber {\n    def $1(x: int): bool {\n        var (reduced, sum) = listOf(x, 0, 1)\n\n        while (reduced > 0)  {\n            sum = sum * 10 + reduced % 10\n            reduced /= 10\n        }\n        return sum == x\n    }\n}"  rust="class PalindromeNumber {\n    fn $1(x -> i32) -> bool {\n        var (reduced, sum) = listOf(x, 0, 1)\n\n        while (reduced > 0)  {\n            sum = sum * 10 + reduced % 10\n            reduced /= 10\n        }\n        return sum == x\n    }\n}"  cpp="class PalindromeNumber {\n    $1(x) {\n        var (reduced, sum) = listOf(x, 0, 1)\n\n        while (reduced > 0)  {\n            sum = sum * 10 + reduced % 10\n            reduced /= 10\n        }\n        return sum == x\n    }\n}" %}

### InsertDeleteGetRandomAtO1
{% include code-tabs.html  kotlin="package probability\n\nimport kotlin.random.Random\n\nclass RandomizedSet() {\n    private val list = mutableListOf<Int>()\n    private val map = mutableMapOf<Int, Int>()\n\n    fun insert(`val`: Int): Boolean {\n        if (!map.containsKey(`val`))\n            return false\n        map[`val`] = list.size\n        list.add(`val`)\n        return true\n    }\n\n    fun remove(`val`: Int): Boolean {\n        if (map.containsKey(`val`))\n            return false\n\n        val index = map[`val`]!!\n        val lastElement = list.last()\n        list[index] = lastElement\n        map[lastElement] = index\n\n        list.removeLast()\n        map.remove(`val`)\n\n        return true\n    }\n\n    fun getRandom(): Int {\n        return list[Random.nextInt(list.size)]\n    }\n\n}"  java="class RandomizedSet() {\n    private val list = mutableListOf<Int>()\n    private val map = mutableMapOf<Int, Int>()\n\n    public static void $1(`val`) {\n        if (!map.containsKey(`val`))\n            return false\n        map[`val`] = list.size\n        list.add(`val`)\n        return true\n    }\n\n    public static void $1(`val`) {\n        if (map.containsKey(`val`))\n            return false\n\n        val index = map[`val`]!!\n        val lastElement = list.last()\n        list[index] = lastElement\n        map[lastElement] = index\n\n        list.removeLast()\n        map.remove(`val`)\n\n        return true\n    }\n\n    public static void $1() {\n        return list[Random.nextInt(list.size)]\n    }\n\n}"  python="class RandomizedSet() {\n    private $1 = mutableListOf<Int>()\n    private $1 = mutableMapOf<Int, Int>()\n\n    def $1(`val`: int): bool {\n        if (!map.containsKey(`val`))\n            return False\n        map[`val`] = list.size\n        list.add(`val`)\n        return True\n    }\n\n    def $1(`val`: int): bool {\n        if (map.containsKey(`val`))\n            return False\n\n        $1 = map[`val`]!!\n        $1 = list.last()\n        list[index] = lastElement\n        map[lastElement] = index\n\n        list.removeLast()\n        map.remove(`val`)\n\n        return True\n    }\n\n    def $1(): int {\n        return list[Random.nextInt(list.size)]\n    }\n\n}"  rust="class RandomizedSet() {\n    private val list = mutableListOf<Int>()\n    private val map = mutableMapOf<Int, Int>()\n\n    fn $1(`val` -> i32) -> bool {\n        if (!map.containsKey(`val`))\n            return false\n        map[`val`] = list.size\n        list.add(`val`)\n        return true\n    }\n\n    fn $1(`val` -> i32) -> bool {\n        if (map.containsKey(`val`))\n            return false\n\n        val index = map[`val`]!!\n        val lastElement = list.last()\n        list[index] = lastElement\n        map[lastElement] = index\n\n        list.removeLast()\n        map.remove(`val`)\n\n        return true\n    }\n\n    fn $1() -> i32 {\n        return list[Random.nextInt(list.size)]\n    }\n\n}"  cpp="class RandomizedSet() {\n    private auto $1 = mutableListOf<Int>()\n    private auto $1 = mutableMapOf<Int, Int>()\n\n    $1(`val`) {\n        if (!map.containsKey(`val`))\n            return false\n        map[`val`] = list.size\n        list.add(`val`)\n        return true\n    }\n\n    $1(`val`) {\n        if (map.containsKey(`val`))\n            return false\n\n        auto $1 = map[`val`]!!\n        auto $1 = list.last()\n        list[index] = lastElement\n        map[lastElement] = index\n\n        list.removeLast()\n        map.remove(`val`)\n\n        return true\n    }\n\n    $1() {\n        return list[Random.nextInt(list.size)]\n    }\n\n}" %}

### LinkedListRandomNode
{% include code-tabs.html  kotlin="package probability\n\nimport linkedlist.ListNode\nimport kotlin.random.Random\n\nclass LinkedListRandomNode (private val head: ListNode?) {\n    fun getRandom(): Int {\n        var (count, result) = 0 to 0\n        var ptr = head\n\n        while (ptr != null) {\n            count++\n\n            if (Random.nextInt(count) == 0) {\n                result = ptr.`val`\n            }\n\n            ptr = ptr.next\n        }\n\n        return result\n    }\n\n    // Another possible solution\n    fun getRandom_2(): Int {\n        var current = head\n        var result = current?.`val` ?: throw IllegalArgumentException(\"List is empty\")\n        var count = 0\n\n        while (current != null) {\n            count++\n            // Select the current node if Random.nextInt(count) == count - 1\n            if (Random.nextInt(count) == count - 1) {\n                result = current.`val`\n            }\n            current = current.next\n        }\n\n        return result\n    }\n}"  java="class LinkedListRandomNode (private val head: ListNode?) {\n    public static void $1() {\n        var (count, result) = 0 to 0\n        var ptr = head\n\n        while (ptr != null) {\n            count++\n\n            if (Random.nextInt(count) == 0) {\n                result = ptr.`val`\n            }\n\n            ptr = ptr.next\n        }\n\n        return result\n    }\n\n    // Another possible solution\n    public static void $1() {\n        var current = head\n        var result = current?.`val` ?: throw IllegalArgumentException(\"List is empty\")\n        var count = 0\n\n        while (current != null) {\n            count++\n            // Select the current node if Random.nextInt(count) == count - 1\n            if (Random.nextInt(count) == count - 1) {\n                result = current.`val`\n            }\n            current = current.next\n        }\n\n        return result\n    }\n}"  python="class LinkedListRandomNode (private val head: ListNode?) {\n    def $1(): int {\n        var (count, result) = 0 to 0\n        $1 = head\n\n        while (ptr != None) {\n            count++\n\n            if (Random.nextInt(count) == 0) {\n                result = ptr.`val`\n            }\n\n            ptr = ptr.next\n        }\n\n        return result\n    }\n\n    // Another possible solution\n    def $1(): int {\n        $1 = head\n        $1 = current?.`val` ?: throw IllegalArgumentException(\"List is empty\")\n        $1 = 0\n\n        while (current != None) {\n            count++\n            // Select the current node if Random.nextInt(count) == count - 1\n            if (Random.nextInt(count) == count - 1) {\n                result = current.`val`\n            }\n            current = current.next\n        }\n\n        return result\n    }\n}"  rust="class LinkedListRandomNode (private val head: ListNode?) {\n    fn $1() -> i32 {\n        var (count, result) = 0 to 0\n        var ptr = head\n\n        while (ptr != None) {\n            count++\n\n            if (Random.nextInt(count) == 0) {\n                result = ptr.`val`\n            }\n\n            ptr = ptr.next\n        }\n\n        return result\n    }\n\n    // Another possible solution\n    fn $1() -> i32 {\n        var current = head\n        var result = current?.`val` ?: throw IllegalArgumentException(\"List is empty\")\n        var count = 0\n\n        while (current != None) {\n            count++\n            // Select the current node if Random.nextInt(count) == count - 1\n            if (Random.nextInt(count) == count - 1) {\n                result = current.`val`\n            }\n            current = current.next\n        }\n\n        return result\n    }\n}"  cpp="class LinkedListRandomNode (private val head: ListNode?) {\n    $1() {\n        var (count, result) = 0 to 0\n        auto $1 = head\n\n        while (ptr != nullptr) {\n            count++\n\n            if (Random.nextInt(count) == 0) {\n                result = ptr.`val`\n            }\n\n            ptr = ptr.next\n        }\n\n        return result\n    }\n\n    // Another possible solution\n    $1() {\n        auto $1 = head\n        auto $1 = current?.`val` ?: throw IllegalArgumentException(\"List is empty\")\n        auto $1 = 0\n\n        while (current != nullptr) {\n            count++\n            // Select the current node if Random.nextInt(count) == count - 1\n            if (Random.nextInt(count) == count - 1) {\n                result = current.`val`\n            }\n            current = current.next\n        }\n\n        return result\n    }\n}" %}

### ReservoirSampling
{% include code-tabs.html  kotlin="package probability\n\nimport kotlin.random.Random\n\nclass ReservoirSampling {\n    fun <T> reservoirSampling(stream: Sequence<T>, k: Int): List<T> {\n        val reservoir = mutableListOf<T>()\n        var count = 0\n\n        stream.forEach { item ->\n            count++\n            when {\n                count <= k -> reservoir.add(item)\n                else -> {\n                    val index = Random.nextInt(count)\n                    // Replace item\n                    if ( index < k)\n                        reservoir[index] = item\n                }\n            }\n        }\n\n        return reservoir\n    }\n}"  java="class ReservoirSampling {\n    fun <T> reservoirSampling(stream: Sequence<T>, k): List<T> {\n        val reservoir = mutableListOf<T>()\n        var count = 0\n\n        stream.forEach { item ->\n            count++\n            when {\n                count <= k -> reservoir.add(item)\n                else -> {\n                    val index = Random.nextInt(count)\n                    // Replace item\n                    if ( index < k)\n                        reservoir[index] = item\n                }\n            }\n        }\n\n        return reservoir\n    }\n}"  python="class ReservoirSampling {\n    fun <T> reservoirSampling(stream: Sequence<T>, k: int): List<T> {\n        $1 = mutableListOf<T>()\n        $1 = 0\n\n        stream.forEach { item ->\n            count++\n            when {\n                count <= k -> reservoir.add(item)\n                else -> {\n                    $1 = Random.nextInt(count)\n                    // Replace item\n                    if ( index < k)\n                        reservoir[index] = item\n                }\n            }\n        }\n\n        return reservoir\n    }\n}"  rust="class ReservoirSampling {\n    fun <T> reservoirSampling(stream: Sequence<T>, k -> i32): List<T> {\n        val reservoir = mutableListOf<T>()\n        var count = 0\n\n        stream.forEach { item ->\n            count++\n            when {\n                count <= k -> reservoir.add(item)\n                else -> {\n                    val index = Random.nextInt(count)\n                    // Replace item\n                    if ( index < k)\n                        reservoir[index] = item\n                }\n            }\n        }\n\n        return reservoir\n    }\n}"  cpp="class ReservoirSampling {\n    fun <T> reservoirSampling(stream: Sequence<T>, k): List<T> {\n        auto $1 = mutableListOf<T>()\n        auto $1 = 0\n\n        stream.forEach { item ->\n            count++\n            when {\n                count <= k -> reservoir.add(item)\n                else -> {\n                    auto $1 = Random.nextInt(count)\n                    // Replace item\n                    if ( index < k)\n                        reservoir[index] = item\n                }\n            }\n        }\n\n        return reservoir\n    }\n}" %}

### DiagonalTraverse
{% include code-tabs.html  kotlin="package array\n\nclass DiagonalTraverse {\n    // Enum to represent the direction of diagonal traversal: UP or DOWN\n    enum class Direction {\n        UP, DOWN\n    }\n\n    fun findDiagonalOrder(mat: Array<IntArray>): IntArray {\n        val m = mat.size        // Number of rows in the matrix\n        val n = mat[0].size     // Number of columns in the matrix\n        val result = mutableListOf<Int>()  // List to store the diagonal traversal result\n\n        var i = 0  // Row index\n        var j = 0  // Column index\n        var direction = Direction.UP  // Start moving diagonally in the UP direction\n\n        // Continue traversing until we collect all matrix elements\n        while (result.size < m * n) {\n            // Add the current element to the result list\n            result.add(mat[i][j])\n\n            // Handle the change in direction and boundary checks using `when`\n            direction = when (direction) {\n                Direction.UP -> when {\n                    // If we're at the top row (i == 0) or last column (j == n - 1)\n                    i == 0 || j == n - 1 -> {\n                        // If at the last column, move down\n                        // Otherwise, move right\n                        if (j == n - 1) i++ else j++\n                        Direction.DOWN  // Switch direction to DOWN\n                    }\n                    else -> {\n                        // Otherwise, continue moving up-left: decrement i and increment j\n                        i--; j++\n                        Direction.UP  // Continue moving UP\n                    }\n                }\n                Direction.DOWN -> when {\n                    // If we're at the left column (j == 0) or last row (i == m - 1)\n                    j == 0 || i == m - 1 -> {\n                        // If at the last row, move right\n                        // Otherwise, move down\n                        if (i == m - 1) j++ else i++\n                        Direction.UP  // Switch direction to UP\n                    }\n                    else -> {\n                        // Otherwise, continue moving down-right: increment i and decrement j\n                        i++; j--\n                        Direction.DOWN  // Continue moving DOWN\n                    }\n                }\n            }\n        }\n\n        // Convert the result list to an array and return it\n        return result.toIntArray()\n    }\n}"  java="class DiagonalTraverse {\n    // Enum to represent the direction of diagonal traversal: UP or DOWN\n    enum class Direction {\n        UP, DOWN\n    }\n\n    public static void $1(mat: Array<IntArray>): IntArray {\n        val m = mat.size        // Number of rows in the matrix\n        val n = mat[0].size     // Number of columns in the matrix\n        val result = mutableListOf<Int>()  // List to store the diagonal traversal result\n\n        var i = 0  // Row index\n        var j = 0  // Column index\n        var direction = Direction.UP  // Start moving diagonally in the UP direction\n\n        // Continue traversing until we collect all matrix elements\n        while (result.size < m * n) {\n            // Add the current element to the result list\n            result.add(mat[i][j])\n\n            // Handle the change in direction and boundary checks using `when`\n            direction = when (direction) {\n                Direction.UP -> when {\n                    // If we're at the top row (i == 0) or last column (j == n - 1)\n                    i == 0 || j == n - 1 -> {\n                        // If at the last column, move down\n                        // Otherwise, move right\n                        if (j == n - 1) i++ else j++\n                        Direction.DOWN  // Switch direction to DOWN\n                    }\n                    else -> {\n                        // Otherwise, continue moving up-left: decrement i and increment j\n                        i--; j++\n                        Direction.UP  // Continue moving UP\n                    }\n                }\n                Direction.DOWN -> when {\n                    // If we're at the left column (j == 0) or last row (i == m - 1)\n                    j == 0 || i == m - 1 -> {\n                        // If at the last row, move right\n                        // Otherwise, move down\n                        if (i == m - 1) j++ else i++\n                        Direction.UP  // Switch direction to UP\n                    }\n                    else -> {\n                        // Otherwise, continue moving down-right: increment i and decrement j\n                        i++; j--\n                        Direction.DOWN  // Continue moving DOWN\n                    }\n                }\n            }\n        }\n\n        // Convert the result list to an array and return it\n        return result.toIntArray()\n    }\n}"  python="class DiagonalTraverse {\n    // Enum to represent the direction of diagonal traversal: UP or DOWN\n    enum class Direction {\n        UP, DOWN\n    }\n\n    def $1(mat: Array<list[int]>): list[int] {\n        $1 = mat.size        // Number of rows in the matrix\n        $1 = mat[0].size     // Number of columns in the matrix\n        $1 = mutableListOf<Int>()  // List to store the diagonal traversal result\n\n        $1 = 0  // Row index\n        $1 = 0  // Column index\n        $1 = Direction.UP  // Start moving diagonally in the UP direction\n\n        // Continue traversing until we collect all matrix elements\n        while (result.size < m * n) {\n            // Add the current element to the result list\n            result.add(mat[i][j])\n\n            // Handle the change in direction and boundary checks using `when`\n            direction = when (direction) {\n                Direction.UP -> when {\n                    // If we're at the top row (i == 0) or last column (j == n - 1)\n                    i == 0 || j == n - 1 -> {\n                        // If at the last column, move down\n                        // Otherwise, move right\n                        if (j == n - 1) i++ else j++\n                        Direction.DOWN  // Switch direction to DOWN\n                    }\n                    else -> {\n                        // Otherwise, continue moving up-left: decrement i and increment j\n                        i--; j++\n                        Direction.UP  // Continue moving UP\n                    }\n                }\n                Direction.DOWN -> when {\n                    // If we're at the left column (j == 0) or last row (i == m - 1)\n                    j == 0 || i == m - 1 -> {\n                        // If at the last row, move right\n                        // Otherwise, move down\n                        if (i == m - 1) j++ else i++\n                        Direction.UP  // Switch direction to UP\n                    }\n                    else -> {\n                        // Otherwise, continue moving down-right: increment i and decrement j\n                        i++; j--\n                        Direction.DOWN  // Continue moving DOWN\n                    }\n                }\n            }\n        }\n\n        // Convert the result list to an array and return it\n        return result.toIntArray()\n    }\n}"  rust="class DiagonalTraverse {\n    // Enum to represent the direction of diagonal traversal: UP or DOWN\n    enum class Direction {\n        UP, DOWN\n    }\n\n    fn $1(mat: Array<Vec<i32>>): Vec<i32> {\n        val m = mat.size        // Number of rows in the matrix\n        val n = mat[0].size     // Number of columns in the matrix\n        val result = mutableListOf<Int>()  // List to store the diagonal traversal result\n\n        var i = 0  // Row index\n        var j = 0  // Column index\n        var direction = Direction.UP  // Start moving diagonally in the UP direction\n\n        // Continue traversing until we collect all matrix elements\n        while (result.size < m * n) {\n            // Add the current element to the result list\n            result.add(mat[i][j])\n\n            // Handle the change in direction and boundary checks using `when`\n            direction = when (direction) {\n                Direction.UP -> when {\n                    // If we're at the top row (i == 0) or last column (j == n - 1)\n                    i == 0 || j == n - 1 -> {\n                        // If at the last column, move down\n                        // Otherwise, move right\n                        if (j == n - 1) i++ else j++\n                        Direction.DOWN  // Switch direction to DOWN\n                    }\n                    else -> {\n                        // Otherwise, continue moving up-left: decrement i and increment j\n                        i--; j++\n                        Direction.UP  // Continue moving UP\n                    }\n                }\n                Direction.DOWN -> when {\n                    // If we're at the left column (j == 0) or last row (i == m - 1)\n                    j == 0 || i == m - 1 -> {\n                        // If at the last row, move right\n                        // Otherwise, move down\n                        if (i == m - 1) j++ else i++\n                        Direction.UP  // Switch direction to UP\n                    }\n                    else -> {\n                        // Otherwise, continue moving down-right: increment i and decrement j\n                        i++; j--\n                        Direction.DOWN  // Continue moving DOWN\n                    }\n                }\n            }\n        }\n\n        // Convert the result list to an array and return it\n        return result.toIntArray()\n    }\n}"  cpp="class DiagonalTraverse {\n    // Enum to represent the direction of diagonal traversal: UP or DOWN\n    enum class Direction {\n        UP, DOWN\n    }\n\n    $1(mat: Array<vector<int>>): vector<int> {\n        auto $1 = mat.size        // Number of rows in the matrix\n        auto $1 = mat[0].size     // Number of columns in the matrix\n        auto $1 = mutableListOf<Int>()  // List to store the diagonal traversal result\n\n        auto $1 = 0  // Row index\n        auto $1 = 0  // Column index\n        auto $1 = Direction.UP  // Start moving diagonally in the UP direction\n\n        // Continue traversing until we collect all matrix elements\n        while (result.size < m * n) {\n            // Add the current element to the result list\n            result.add(mat[i][j])\n\n            // Handle the change in direction and boundary checks using `when`\n            direction = when (direction) {\n                Direction.UP -> when {\n                    // If we're at the top row (i == 0) or last column (j == n - 1)\n                    i == 0 || j == n - 1 -> {\n                        // If at the last column, move down\n                        // Otherwise, move right\n                        if (j == n - 1) i++ else j++\n                        Direction.DOWN  // Switch direction to DOWN\n                    }\n                    else -> {\n                        // Otherwise, continue moving up-left: decrement i and increment j\n                        i--; j++\n                        Direction.UP  // Continue moving UP\n                    }\n                }\n                Direction.DOWN -> when {\n                    // If we're at the left column (j == 0) or last row (i == m - 1)\n                    j == 0 || i == m - 1 -> {\n                        // If at the last row, move right\n                        // Otherwise, move down\n                        if (i == m - 1) j++ else i++\n                        Direction.UP  // Switch direction to UP\n                    }\n                    else -> {\n                        // Otherwise, continue moving down-right: increment i and decrement j\n                        i++; j--\n                        Direction.DOWN  // Continue moving DOWN\n                    }\n                }\n            }\n        }\n\n        // Convert the result list to an array and return it\n        return result.toIntArray()\n    }\n}" %}

### DiagonalTraverse_II
{% include code-tabs.html  kotlin="package array\n\nimport java.util.*\n\ndata class Point(val row: Int, val col: Int)\n\nclass DiagonalTraverse_II {\n    fun findDiagonalOrder(nums: List<List<Int>>): List<Int> {\n        val result = mutableListOf<Int>()\n        val queue: Queue<Pair<Int, Int>> = LinkedList()\n        val visited = mutableSetOf<Pair<Int, Int>>()\n\n        // Start with the first element\n        queue.offer(0 to 0)\n\n        while (queue.isNotEmpty()) {\n            val (i, j) = queue.poll()\n            result.add(nums[i][j])\n\n            // Move to the next row (i+1, j)\n            if (i + 1 < nums.size && j < nums[i + 1].size) {\n                queue.offer(i + 1 to j)\n                visited.add(i + 1 to j)\n            }\n\n            // Move to the next column (i, j+1)\n            if (j + 1 < nums[i].size) {\n                queue.offer(i to j + 1)\n                visited.add(i to j + 1)\n            }\n        }\n\n        return result\n    }\n}\n"  java="data class Point(val row, val col)\n\nclass DiagonalTraverse_II {\n    public static void $1(nums: List<List<Int>>): List<Int> {\n        val result = mutableListOf<Int>()\n        val queue: Queue<Pair<Int, Int>> = LinkedList()\n        val visited = mutableSetOf<Pair<Int, Int>>()\n\n        // Start with the first element\n        queue.offer(0 to 0)\n\n        while (queue.isNotEmpty()) {\n            val (i, j) = queue.poll()\n            result.add(nums[i][j])\n\n            // Move to the next row (i+1, j)\n            if (i + 1 < nums.size && j < nums[i + 1].size) {\n                queue.offer(i + 1 to j)\n                visited.add(i + 1 to j)\n            }\n\n            // Move to the next column (i, j+1)\n            if (j + 1 < nums[i].size) {\n                queue.offer(i to j + 1)\n                visited.add(i to j + 1)\n            }\n        }\n\n        return result\n    }\n}"  python="data class Point(val row: int, val col: int)\n\nclass DiagonalTraverse_II {\n    def $1(nums: List<List<Int>>): List<Int> {\n        $1 = mutableListOf<Int>()\n        val queue: Queue<Pair<Int, Int>> = LinkedList()\n        $1 = mutableSetOf<Pair<Int, Int>>()\n\n        // Start with the first element\n        queue.offer(0 to 0)\n\n        while (queue.isNotEmpty()) {\n            ($1, $2) = queue.poll()\n            result.add(nums[i][j])\n\n            // Move to the next row (i+1, j)\n            if (i + 1 < nums.size && j < nums[i + 1].size) {\n                queue.offer(i + 1 to j)\n                visited.add(i + 1 to j)\n            }\n\n            // Move to the next column (i, j+1)\n            if (j + 1 < nums[i].size) {\n                queue.offer(i to j + 1)\n                visited.add(i to j + 1)\n            }\n        }\n\n        return result\n    }\n}"  rust="data class Point(val row -> i32, val col -> i32)\n\nclass DiagonalTraverse_II {\n    fn $1(nums: List<List<Int>>): List<Int> {\n        val result = mutableListOf<Int>()\n        val queue: Queue<Pair<Int, Int>> = LinkedList()\n        val visited = mutableSetOf<Pair<Int, Int>>()\n\n        // Start with the first element\n        queue.offer(0 to 0)\n\n        while (queue.isNotEmpty()) {\n            val (i, j) = queue.poll()\n            result.add(nums[i][j])\n\n            // Move to the next row (i+1, j)\n            if (i + 1 < nums.size && j < nums[i + 1].size) {\n                queue.offer(i + 1 to j)\n                visited.add(i + 1 to j)\n            }\n\n            // Move to the next column (i, j+1)\n            if (j + 1 < nums[i].size) {\n                queue.offer(i to j + 1)\n                visited.add(i to j + 1)\n            }\n        }\n\n        return result\n    }\n}"  cpp="data class Point(val row, val col)\n\nclass DiagonalTraverse_II {\n    $1(nums: List<List<Int>>): List<Int> {\n        auto $1 = mutableListOf<Int>()\n        val queue: Queue<Pair<Int, Int>> = LinkedList()\n        auto $1 = mutableSetOf<Pair<Int, Int>>()\n\n        // Start with the first element\n        queue.offer(0 to 0)\n\n        while (queue.isNotEmpty()) {\n            val (i, j) = queue.poll()\n            result.add(nums[i][j])\n\n            // Move to the next row (i+1, j)\n            if (i + 1 < nums.size && j < nums[i + 1].size) {\n                queue.offer(i + 1 to j)\n                visited.add(i + 1 to j)\n            }\n\n            // Move to the next column (i, j+1)\n            if (j + 1 < nums[i].size) {\n                queue.offer(i to j + 1)\n                visited.add(i to j + 1)\n            }\n        }\n\n        return result\n    }\n}" %}

### MergeIntervals
{% include code-tabs.html  kotlin="package array\n\nclass MergeIntervals {\n    fun merge(intervals: Array<IntArray>): Array<IntArray> {\n        // Sort the internals by start\n        intervals.sortWith(compareBy { it[0] })\n        var index = 0\n        for (i in 1..intervals.lastIndex) {\n            if (intervals[i][0] > intervals[index][1]) {\n                intervals[++index] = intervals[i]\n            } else {\n                intervals[index][1] = maxOf(intervals[index][1], intervals[i][1])\n            }\n        }\n\n        return intervals.copyOfRange(0, index + 1)\n    }\n}"  java="class MergeIntervals {\n    public static void $1(intervals: Array<IntArray>): Array<IntArray> {\n        // Sort the internals by start\n        intervals.sortWith(compareBy { it[0] })\n        var index = 0\n        for (i in 1..intervals.lastIndex) {\n            if (intervals[i][0] > intervals[index][1]) {\n                intervals[++index] = intervals[i]\n            } else {\n                intervals[index][1] = maxOf(intervals[index][1], intervals[i][1])\n            }\n        }\n\n        return intervals.copyOfRange(0, index + 1)\n    }\n}"  python="class MergeIntervals {\n    def $1(intervals: Array<list[int]>): Array<list[int]> {\n        // Sort the internals by start\n        intervals.sortWith(compareBy { it[0] })\n        $1 = 0\n        for (i in 1..intervals.lastIndex) {\n            if (intervals[i][0] > intervals[index][1]) {\n                intervals[++index] = intervals[i]\n            } else {\n                intervals[index][1] = max(intervals[index][1], intervals[i][1])\n            }\n        }\n\n        return intervals.copyOfRange(0, index + 1)\n    }\n}"  rust="class MergeIntervals {\n    fn $1(intervals: Array<Vec<i32>>): Array<Vec<i32>> {\n        // Sort the internals by start\n        intervals.sortWith(compareBy { it[0] })\n        var index = 0\n        for (i in 1..intervals.len() - 1) {\n            if (intervals[i][0] > intervals[index][1]) {\n                intervals[++index] = intervals[i]\n            } else {\n                intervals[index][1] = max(intervals[index][1], intervals[i][1])\n            }\n        }\n\n        return intervals.copyOfRange(0, index + 1)\n    }\n}"  cpp="class MergeIntervals {\n    $1(intervals: Array<vector<int>>): Array<vector<int>> {\n        // Sort the internals by start\n        intervals.sortWith(compareBy { it[0] })\n        auto $1 = 0\n        for (i in 1..intervals.size() - 1) {\n            if (intervals[i][0] > intervals[index][1]) {\n                intervals[++index] = intervals[i]\n            } else {\n                intervals[index][1] = max(intervals[index][1], intervals[i][1])\n            }\n        }\n\n        return intervals.copyOfRange(0, index + 1)\n    }\n}" %}

### MergeSortedArray
{% include code-tabs.html  kotlin="package array\n\nimport binarysearch.FindKClosestElements\nimport kotlin.math.max\n\nclass MergeSortedArray {\n\n    fun merge(nums1: IntArray, m: Int, nums2: IntArray, n: Int): Unit {\n        var (x, y, ptr) = listOf(m-1, n-1, m + n - 1)\n\n        while (x >=0  && y > 0) {\n            if (nums1[x] > nums2[y] )\n                nums1[ptr--] = nums1[x--]\n            else\n                nums1[ptr--] = nums2[y--]\n        }\n\n        while ( y >= 0) {\n            nums1[ptr--] = nums2[y--]\n        }\n\n        print(nums1.contentToString())\n    }\n\n    companion object {\n        @JvmStatic\n        fun main(args: Array<String>) {\n            val testClass = MergeSortedArray()\n\n            // Declaring a 2d Array in Kotlin is tricky\n            // Not that Bad. It's still idiomatic and nice....\n            var arr = Array(5) { IntArray(6)}\n\n\n            testClass.merge(intArrayOf(1,5,7,8, 0, 0, 0), 4, intArrayOf(3,6,17),3)\n        }\n    }\n}"  java="class MergeSortedArray {\n\n    public static void $1(nums1: IntArray, m, nums2: IntArray, n) {\n        var (x, y, ptr) = listOf(m-1, n-1, m + n - 1)\n\n        while (x >=0  && y > 0) {\n            if (nums1[x] > nums2[y] )\n                nums1[ptr--] = nums1[x--]\n            else\n                nums1[ptr--] = nums2[y--]\n        }\n\n        while ( y >= 0) {\n            nums1[ptr--] = nums2[y--]\n        }\n\n        print(nums1.contentToString())\n    }\n\n    companion object {\n        @JvmStatic\n        public static void $1(args: Array<String>) {\n            val testClass = MergeSortedArray()\n\n            // Declaring a 2d Array in Kotlin is tricky\n            // Not that Bad. It's still idiomatic and nice....\n            var arr = Array(5) { IntArray(6)}\n\n\n            testClass.merge(intArrayOf(1,5,7,8, 0, 0, 0), 4, intArrayOf(3,6,17),3)\n        }\n    }\n}"  python="class MergeSortedArray {\n\n    def $1(nums1: list[int], m: int, nums2: list[int], n: int) {\n        var (x, y, ptr) = listOf(m-1, n-1, m + n - 1)\n\n        while (x >=0  && y > 0) {\n            if (nums1[x] > nums2[y] )\n                nums1[ptr--] = nums1[x--]\n            else\n                nums1[ptr--] = nums2[y--]\n        }\n\n        while ( y >= 0) {\n            nums1[ptr--] = nums2[y--]\n        }\n\n        print(nums1.contentToString())\n    }\n\n    companion object {\n        @JvmStatic\n        def $1(args: Array<String>) {\n            $1 = MergeSortedArray()\n\n            // Declaring a 2d Array in Kotlin is tricky\n            // Not that Bad. It's still idiomatic and nice....\n            $1 = Array(5) { list[int](6)}\n\n\n            testClass.merge(intArrayOf(1,5,7,8, 0, 0, 0), 4, intArrayOf(3,6,17),3)\n        }\n    }\n}"  rust="class MergeSortedArray {\n\n    fn $1(nums1: Vec<i32>, m -> i32, nums2: Vec<i32>, n -> i32) {\n        var (x, y, ptr) = listOf(m-1, n-1, m + n - 1)\n\n        while (x >=0  && y > 0) {\n            if (nums1[x] > nums2[y] )\n                nums1[ptr--] = nums1[x--]\n            else\n                nums1[ptr--] = nums2[y--]\n        }\n\n        while ( y >= 0) {\n            nums1[ptr--] = nums2[y--]\n        }\n\n        print(nums1.contentToString())\n    }\n\n    companion object {\n        @JvmStatic\n        fn $1(args: Array<String>) {\n            val testClass = MergeSortedArray()\n\n            // Declaring a 2d Array in Kotlin is tricky\n            // Not that Bad. It's still idiomatic and nice....\n            var arr = Array(5) { Vec<i32>(6)}\n\n\n            testClass.merge(intArrayOf(1,5,7,8, 0, 0, 0), 4, intArrayOf(3,6,17),3)\n        }\n    }\n}"  cpp="class MergeSortedArray {\n\n    $1(nums1: vector<int>, m, nums2: vector<int>, n) {\n        var (x, y, ptr) = listOf(m-1, n-1, m + n - 1)\n\n        while (x >=0  && y > 0) {\n            if (nums1[x] > nums2[y] )\n                nums1[ptr--] = nums1[x--]\n            else\n                nums1[ptr--] = nums2[y--]\n        }\n\n        while ( y >= 0) {\n            nums1[ptr--] = nums2[y--]\n        }\n\n        print(nums1.contentToString())\n    }\n\n    companion object {\n        @JvmStatic\n        $1(args: Array<String>) {\n            auto $1 = MergeSortedArray()\n\n            // Declaring a 2d Array in Kotlin is tricky\n            // Not that Bad. It's still idiomatic and nice....\n            auto $1 = Array(5) { vector<int>(6)}\n\n\n            testClass.merge(intArrayOf(1,5,7,8, 0, 0, 0), 4, intArrayOf(3,6,17),3)\n        }\n    }\n}" %}

### MissingRanges
{% include code-tabs.html  kotlin="package array\n\nclass MissingRanges {\n    fun findMissingRanges(nums: IntArray, lower: Int, upper: Int): List<List<Int>> {\n        val result = mutableListOf<List<Int>>()\n        var currentRangePointer = lower\n\n        for(num in nums) {\n            if (num > currentRangePointer)\n                result.add(listOf(currentRangePointer, num - 1))\n            currentRangePointer = num + 1\n        }\n\n        if (currentRangePointer <= upper) {\n            result.add(listOf(currentRangePointer, upper))\n        }\n\n        return result\n    }\n}"  java="class MissingRanges {\n    public static void $1(nums: IntArray, lower, upper): List<List<Int>> {\n        val result = mutableListOf<List<Int>>()\n        var currentRangePointer = lower\n\n        for(num in nums) {\n            if (num > currentRangePointer)\n                result.add(listOf(currentRangePointer, num - 1))\n            currentRangePointer = num + 1\n        }\n\n        if (currentRangePointer <= upper) {\n            result.add(listOf(currentRangePointer, upper))\n        }\n\n        return result\n    }\n}"  python="class MissingRanges {\n    def $1(nums: list[int], lower: int, upper: int): List<List<Int>> {\n        $1 = mutableListOf<List<Int>>()\n        $1 = lower\n\n        for(num in nums) {\n            if (num > currentRangePointer)\n                result.add(listOf(currentRangePointer, num - 1))\n            currentRangePointer = num + 1\n        }\n\n        if (currentRangePointer <= upper) {\n            result.add(listOf(currentRangePointer, upper))\n        }\n\n        return result\n    }\n}"  rust="class MissingRanges {\n    fn $1(nums: Vec<i32>, lower -> i32, upper -> i32): List<List<Int>> {\n        val result = mutableListOf<List<Int>>()\n        var currentRangePointer = lower\n\n        for(num in nums) {\n            if (num > currentRangePointer)\n                result.add(listOf(currentRangePointer, num - 1))\n            currentRangePointer = num + 1\n        }\n\n        if (currentRangePointer <= upper) {\n            result.add(listOf(currentRangePointer, upper))\n        }\n\n        return result\n    }\n}"  cpp="class MissingRanges {\n    $1(nums: vector<int>, lower, upper): List<List<Int>> {\n        auto $1 = mutableListOf<List<Int>>()\n        auto $1 = lower\n\n        for(num in nums) {\n            if (num > currentRangePointer)\n                result.add(listOf(currentRangePointer, num - 1))\n            currentRangePointer = num + 1\n        }\n\n        if (currentRangePointer <= upper) {\n            result.add(listOf(currentRangePointer, upper))\n        }\n\n        return result\n    }\n}" %}

### MoveZeroes
{% include code-tabs.html  kotlin="package array\n\nclass MoveZeroes {\n    fun moveZeroes(nums: IntArray): Unit {\n        var nonZeroPointer = 0\n\n        // Move all non-zero elements to the front\n        for (i in nums.indices) {\n            if (nums[i] != 0) {\n                nums[nonZeroPointer++] = nums[i]\n            }\n        }\n\n        // Fill the rest of the array with zeros\n        while (nonZeroPointer < nums.size) {\n            nums[nonZeroPointer++] = 0\n        }\n    }\n}"  java="class MoveZeroes {\n    public static void $1(nums: IntArray) {\n        var nonZeroPointer = 0\n\n        // Move all non-zero elements to the front\n        for (i in nums.indices) {\n            if (nums[i] != 0) {\n                nums[nonZeroPointer++] = nums[i]\n            }\n        }\n\n        // Fill the rest of the array with zeros\n        while (nonZeroPointer < nums.size) {\n            nums[nonZeroPointer++] = 0\n        }\n    }\n}"  python="class MoveZeroes {\n    def $1(nums: list[int]) {\n        $1 = 0\n\n        // Move all non-zero elements to the front\n        for (i in nums.indices) {\n            if (nums[i] != 0) {\n                nums[nonZeroPointer++] = nums[i]\n            }\n        }\n\n        // Fill the rest of the array with zeros\n        while (nonZeroPointer < nums.size) {\n            nums[nonZeroPointer++] = 0\n        }\n    }\n}"  rust="class MoveZeroes {\n    fn $1(nums: Vec<i32>) {\n        var nonZeroPointer = 0\n\n        // Move all non-zero elements to the front\n        for (i in nums.indices) {\n            if (nums[i] != 0) {\n                nums[nonZeroPointer++] = nums[i]\n            }\n        }\n\n        // Fill the rest of the array with zeros\n        while (nonZeroPointer < nums.size) {\n            nums[nonZeroPointer++] = 0\n        }\n    }\n}"  cpp="class MoveZeroes {\n    $1(nums: vector<int>) {\n        auto $1 = 0\n\n        // Move all non-zero elements to the front\n        for (i in nums.indices) {\n            if (nums[i] != 0) {\n                nums[nonZeroPointer++] = nums[i]\n            }\n        }\n\n        // Fill the rest of the array with zeros\n        while (nonZeroPointer < nums.size) {\n            nums[nonZeroPointer++] = 0\n        }\n    }\n}" %}

### RemoveElement
{% include code-tabs.html  kotlin="package array\n\nclass RemoveElement {\n    fun removeElement(nums: IntArray, `val`: Int): Int {\n        var i = 0\n\n        for (j in nums.indices) {\n            if (nums[j] != `val`) {\n                nums[i++] = nums[j]\n            }\n        }\n\n        return i\n    }\n}"  java="class RemoveElement {\n    public static void $1(nums: IntArray, `val`) {\n        var i = 0\n\n        for (j in nums.indices) {\n            if (nums[j] != `val`) {\n                nums[i++] = nums[j]\n            }\n        }\n\n        return i\n    }\n}"  python="class RemoveElement {\n    def $1(nums: list[int], `val`: int): int {\n        $1 = 0\n\n        for (j in nums.indices) {\n            if (nums[j] != `val`) {\n                nums[i++] = nums[j]\n            }\n        }\n\n        return i\n    }\n}"  rust="class RemoveElement {\n    fn $1(nums: Vec<i32>, `val` -> i32) -> i32 {\n        var i = 0\n\n        for (j in nums.indices) {\n            if (nums[j] != `val`) {\n                nums[i++] = nums[j]\n            }\n        }\n\n        return i\n    }\n}"  cpp="class RemoveElement {\n    $1(nums: vector<int>, `val`) {\n        auto $1 = 0\n\n        for (j in nums.indices) {\n            if (nums[j] != `val`) {\n                nums[i++] = nums[j]\n            }\n        }\n\n        return i\n    }\n}" %}

### RotateImage
{% include code-tabs.html  kotlin="package array\n\nclass RotateImage {\n    fun rotate(matrix: Array<IntArray>): Unit {\n        for (i in matrix.indices)\n            for (j in i until matrix[i].size) {\n            swap(matrix, i, j, j, i)\n        }\n\n        for (i in matrix.indices) {\n            var l = 0\n            var r: Int = matrix[i].size - 1\n            while (l < r) {\n                swap(matrix, i, l, i, r)\n                l++\n                r--\n            }\n        }\n    }\n\n    fun swap(matrix: Array<IntArray>, i: Int, j: Int, m: Int, n: Int) {\n        matrix[i][j] = matrix[m][n].also { matrix[m][n] = matrix[i][j] }\n    }\n}\n\n/**\n * 1,2,3\n * 4,5,6\n * 7,8,9\n *\n * 1,4,7\n * 2,5,8\n * 3,6,9\n */\n\n"  java="class RotateImage {\n    public static void $1(matrix: Array<IntArray>) {\n        for (i in matrix.indices)\n            for (j in i until matrix[i].size) {\n            swap(matrix, i, j, j, i)\n        }\n\n        for (i in matrix.indices) {\n            var l = 0\n            var r = matrix[i].size - 1\n            while (l < r) {\n                swap(matrix, i, l, i, r)\n                l++\n                r--\n            }\n        }\n    }\n\n    public static void $1(matrix: Array<IntArray>, i, j, m, n) {\n        matrix[i][j] = matrix[m][n].also { matrix[m][n] = matrix[i][j] }\n    }\n}\n\n/**\n * 1,2,3\n * 4,5,6\n * 7,8,9\n *\n * 1,4,7\n * 2,5,8\n * 3,6,9\n */"  python="class RotateImage {\n    def $1(matrix: Array<list[int]>) {\n        for (i in matrix.indices)\n            for (j in i until matrix[i].size) {\n            swap(matrix, i, j, j, i)\n        }\n\n        for (i in matrix.indices) {\n            $1 = 0\n            var r: int = matrix[i].size - 1\n            while (l < r) {\n                swap(matrix, i, l, i, r)\n                l++\n                r--\n            }\n        }\n    }\n\n    def $1(matrix: Array<list[int]>, i: int, j: int, m: int, n: int) {\n        matrix[i][j] = matrix[m][n].also { matrix[m][n] = matrix[i][j] }\n    }\n}\n\n/**\n * 1,2,3\n * 4,5,6\n * 7,8,9\n *\n * 1,4,7\n * 2,5,8\n * 3,6,9\n */"  rust="class RotateImage {\n    fn $1(matrix: Array<Vec<i32>>) {\n        for (i in matrix.indices)\n            for (j in i until matrix[i].size) {\n            swap(matrix, i, j, j, i)\n        }\n\n        for (i in matrix.indices) {\n            var l = 0\n            var r -> i32 = matrix[i].size - 1\n            while (l < r) {\n                swap(matrix, i, l, i, r)\n                l++\n                r--\n            }\n        }\n    }\n\n    fn $1(matrix: Array<Vec<i32>>, i -> i32, j -> i32, m -> i32, n -> i32) {\n        matrix[i][j] = matrix[m][n].also { matrix[m][n] = matrix[i][j] }\n    }\n}\n\n/**\n * 1,2,3\n * 4,5,6\n * 7,8,9\n *\n * 1,4,7\n * 2,5,8\n * 3,6,9\n */"  cpp="class RotateImage {\n    $1(matrix: Array<vector<int>>) {\n        for (i in matrix.indices)\n            for (j in i until matrix[i].size) {\n            swap(matrix, i, j, j, i)\n        }\n\n        for (i in matrix.indices) {\n            auto $1 = 0\n            auto $1 = matrix[i].size - 1\n            while (l < r) {\n                swap(matrix, i, l, i, r)\n                l++\n                r--\n            }\n        }\n    }\n\n    $1(matrix: Array<vector<int>>, i, j, m, n) {\n        matrix[i][j] = matrix[m][n].also { matrix[m][n] = matrix[i][j] }\n    }\n}\n\n/**\n * 1,2,3\n * 4,5,6\n * 7,8,9\n *\n * 1,4,7\n * 2,5,8\n * 3,6,9\n */" %}

### SearchA2dMatrix_II
{% include code-tabs.html  kotlin="package array\n\nclass SearchA2dMatrix_II {\n    fun searchMatrix(matrix: Array<IntArray>, target: Int): Boolean {\n        var (row, col) = matrix.size - 1 to 0\n\n        while (row >=0 && col < matrix[0].size) {\n            when {\n                matrix[row][col] > target -> row--\n                matrix[row][col] < target -> col++\n                else -> return true\n            }\n        }\n\n        return false\n    }\n}\n"  java="class SearchA2dMatrix_II {\n    public static void $1(matrix: Array<IntArray>, target) {\n        var (row, col) = matrix.size - 1 to 0\n\n        while (row >=0 && col < matrix[0].size) {\n            when {\n                matrix[row][col] > target -> row--\n                matrix[row][col] < target -> col++\n                else -> return true\n            }\n        }\n\n        return false\n    }\n}"  python="class SearchA2dMatrix_II {\n    def $1(matrix: Array<list[int]>, target: int): bool {\n        var (row, col) = matrix.size - 1 to 0\n\n        while (row >=0 && col < matrix[0].size) {\n            when {\n                matrix[row][col] > target -> row--\n                matrix[row][col] < target -> col++\n                else -> return True\n            }\n        }\n\n        return False\n    }\n}"  rust="class SearchA2dMatrix_II {\n    fn $1(matrix: Array<Vec<i32>>, target -> i32) -> bool {\n        var (row, col) = matrix.size - 1 to 0\n\n        while (row >=0 && col < matrix[0].size) {\n            when {\n                matrix[row][col] > target -> row--\n                matrix[row][col] < target -> col++\n                else -> return true\n            }\n        }\n\n        return false\n    }\n}"  cpp="class SearchA2dMatrix_II {\n    $1(matrix: Array<vector<int>>, target) {\n        var (row, col) = matrix.size - 1 to 0\n\n        while (row >=0 && col < matrix[0].size) {\n            when {\n                matrix[row][col] > target -> row--\n                matrix[row][col] < target -> col++\n                else -> return true\n            }\n        }\n\n        return false\n    }\n}" %}

### ShortestPathInBinaryMatrix
{% include code-tabs.html  kotlin="package array\n\nimport java.util.*\n\nclass ShortestPathInBinaryMatrix {\n    data class State(val row: Int, val col: Int, val dist: Int)\n\n    fun shortestPathBinaryMatrix(grid: Array<IntArray>): Int {\n        val n = grid.size\n        val m = grid[0].size\n\n        // Early return if start or end is blocked\n        if (grid[0][0] == 1 || grid[n - 1][m - 1] == 1) return -1\n\n        // Directions for 8 neighbors (horizontal, vertical, and diagonal)\n        val directions = arrayOf(\n            -1 to 0, 1 to 0, 0 to -1, 0 to 1,  // Left, right, up, down\n            -1 to -1, -1 to 1, 1 to -1, 1 to 1 // Diagonal directions\n        )\n\n        val queue = LinkedList<State>()\n        queue.add(State(0, 0, 1))  // Start at (0,0) with distance 1\n\n        while (queue.isNotEmpty()) {\n            val (row, col, dist) = queue.poll()\n\n            // If we reached the bottom-right corner\n            if (row == n - 1 && col == m - 1) return dist\n\n            // Explore all 8 possible directions\n            for ((dr, dc) in directions) {\n                val (newRow, newCol) = row + dr to col + dc\n\n                // Skip invalid positions or visited cells\n                if (newRow !in 0 until n || newCol !in 0 until m || grid[newRow][newCol] != 0)\n                    continue\n\n                // Mark the cell as visited and add it to the queue\n                grid[newRow][newCol] = 2\n                queue.add(State(newRow, newCol, dist + 1))\n            }\n        }\n\n        return -1  // No path found\n    }\n}\n"  java="class ShortestPathInBinaryMatrix {\n    data class State(val row, val col, val dist)\n\n    public static void $1(grid: Array<IntArray>) {\n        val n = grid.size\n        val m = grid[0].size\n\n        // Early return if start or end is blocked\n        if (grid[0][0] == 1 || grid[n - 1][m - 1] == 1) return -1\n\n        // Directions for 8 neighbors (horizontal, vertical, and diagonal)\n        val directions = arrayOf(\n            -1 to 0, 1 to 0, 0 to -1, 0 to 1,  // Left, right, up, down\n            -1 to -1, -1 to 1, 1 to -1, 1 to 1 // Diagonal directions\n        )\n\n        val queue = LinkedList<State>()\n        queue.add(State(0, 0, 1))  // Start at (0,0) with distance 1\n\n        while (queue.isNotEmpty()) {\n            val (row, col, dist) = queue.poll()\n\n            // If we reached the bottom-right corner\n            if (row == n - 1 && col == m - 1) return dist\n\n            // Explore all 8 possible directions\n            for ((dr, dc) in directions) {\n                val (newRow, newCol) = row + dr to col + dc\n\n                // Skip invalid positions or visited cells\n                if (newRow !in 0 until n || newCol !in 0 until m || grid[newRow][newCol] != 0)\n                    continue\n\n                // Mark the cell as visited and add it to the queue\n                grid[newRow][newCol] = 2\n                queue.add(State(newRow, newCol, dist + 1))\n            }\n        }\n\n        return -1  // No path found\n    }\n}"  python="class ShortestPathInBinaryMatrix {\n    data class State(val row: int, val col: int, val dist: int)\n\n    def $1(grid: Array<list[int]>): int {\n        $1 = grid.size\n        $1 = grid[0].size\n\n        // Early return if start or end is blocked\n        if (grid[0][0] == 1 || grid[n - 1][m - 1] == 1) return -1\n\n        // Directions for 8 neighbors (horizontal, vertical, and diagonal)\n        $1 = arrayOf(\n            -1 to 0, 1 to 0, 0 to -1, 0 to 1,  // Left, right, up, down\n            -1 to -1, -1 to 1, 1 to -1, 1 to 1 // Diagonal directions\n        )\n\n        $1 = LinkedList<State>()\n        queue.add(State(0, 0, 1))  // Start at (0,0) with distance 1\n\n        while (queue.isNotEmpty()) {\n            val (row, col, dist) = queue.poll()\n\n            // If we reached the bottom-right corner\n            if (row == n - 1 && col == m - 1) return dist\n\n            // Explore all 8 possible directions\n            for ((dr, dc) in directions) {\n                ($1, $2) = row + dr to col + dc\n\n                // Skip invalid positions or visited cells\n                if (newRow !in 0 until n || newCol !in 0 until m || grid[newRow][newCol] != 0)\n                    continue\n\n                // Mark the cell as visited and add it to the queue\n                grid[newRow][newCol] = 2\n                queue.add(State(newRow, newCol, dist + 1))\n            }\n        }\n\n        return -1  // No path found\n    }\n}"  rust="class ShortestPathInBinaryMatrix {\n    data class State(val row -> i32, val col -> i32, val dist -> i32)\n\n    fn $1(grid: Array<Vec<i32>>) -> i32 {\n        val n = grid.size\n        val m = grid[0].size\n\n        // Early return if start or end is blocked\n        if (grid[0][0] == 1 || grid[n - 1][m - 1] == 1) return -1\n\n        // Directions for 8 neighbors (horizontal, vertical, and diagonal)\n        val directions = arrayOf(\n            -1 to 0, 1 to 0, 0 to -1, 0 to 1,  // Left, right, up, down\n            -1 to -1, -1 to 1, 1 to -1, 1 to 1 // Diagonal directions\n        )\n\n        val queue = LinkedList<State>()\n        queue.add(State(0, 0, 1))  // Start at (0,0) with distance 1\n\n        while (queue.isNotEmpty()) {\n            val (row, col, dist) = queue.poll()\n\n            // If we reached the bottom-right corner\n            if (row == n - 1 && col == m - 1) return dist\n\n            // Explore all 8 possible directions\n            for ((dr, dc) in directions) {\n                val (newRow, newCol) = row + dr to col + dc\n\n                // Skip invalid positions or visited cells\n                if (newRow !in 0 until n || newCol !in 0 until m || grid[newRow][newCol] != 0)\n                    continue\n\n                // Mark the cell as visited and add it to the queue\n                grid[newRow][newCol] = 2\n                queue.add(State(newRow, newCol, dist + 1))\n            }\n        }\n\n        return -1  // No path found\n    }\n}"  cpp="class ShortestPathInBinaryMatrix {\n    data class State(val row, val col, val dist)\n\n    $1(grid: Array<vector<int>>) {\n        auto $1 = grid.size\n        auto $1 = grid[0].size\n\n        // Early return if start or end is blocked\n        if (grid[0][0] == 1 || grid[n - 1][m - 1] == 1) return -1\n\n        // Directions for 8 neighbors (horizontal, vertical, and diagonal)\n        auto $1 = arrayOf(\n            -1 to 0, 1 to 0, 0 to -1, 0 to 1,  // Left, right, up, down\n            -1 to -1, -1 to 1, 1 to -1, 1 to 1 // Diagonal directions\n        )\n\n        auto $1 = LinkedList<State>()\n        queue.add(State(0, 0, 1))  // Start at (0,0) with distance 1\n\n        while (queue.isNotEmpty()) {\n            val (row, col, dist) = queue.poll()\n\n            // If we reached the bottom-right corner\n            if (row == n - 1 && col == m - 1) return dist\n\n            // Explore all 8 possible directions\n            for ((dr, dc) in directions) {\n                val (newRow, newCol) = row + dr to col + dc\n\n                // Skip invalid positions or visited cells\n                if (newRow !in 0 until n || newCol !in 0 until m || grid[newRow][newCol] != 0)\n                    continue\n\n                // Mark the cell as visited and add it to the queue\n                grid[newRow][newCol] = 2\n                queue.add(State(newRow, newCol, dist + 1))\n            }\n        }\n\n        return -1  // No path found\n    }\n}" %}

### SignOfTheProductOfAnArray
{% include code-tabs.html  kotlin="package array\n\nclass SignOfTheProductOfAnArray {\n    fun arraySign(nums: IntArray): Int {\n        var negativeCount = 0\n\n        nums.forEach {\n            if (it == 0)    return 0\n            if (it < 0) negativeCount++\n        }\n\n        return when {\n            negativeCount % 2 == 0 -> 1\n            else -> -1\n        }\n    }\n}\n"  java="class SignOfTheProductOfAnArray {\n    public static void $1(nums: IntArray) {\n        var negativeCount = 0\n\n        nums.forEach {\n            if (it == 0)    return 0\n            if (it < 0) negativeCount++\n        }\n\n        return when {\n            negativeCount % 2 == 0 -> 1\n            else -> -1\n        }\n    }\n}"  python="class SignOfTheProductOfAnArray {\n    def $1(nums: list[int]): int {\n        $1 = 0\n\n        nums.forEach {\n            if (it == 0)    return 0\n            if (it < 0) negativeCount++\n        }\n\n        return when {\n            negativeCount % 2 == 0 -> 1\n            else -> -1\n        }\n    }\n}"  rust="class SignOfTheProductOfAnArray {\n    fn $1(nums: Vec<i32>) -> i32 {\n        var negativeCount = 0\n\n        nums.forEach {\n            if (it == 0)    return 0\n            if (it < 0) negativeCount++\n        }\n\n        return when {\n            negativeCount % 2 == 0 -> 1\n            else -> -1\n        }\n    }\n}"  cpp="class SignOfTheProductOfAnArray {\n    $1(nums: vector<int>) {\n        auto $1 = 0\n\n        nums.forEach {\n            if (it == 0)    return 0\n            if (it < 0) negativeCount++\n        }\n\n        return when {\n            negativeCount % 2 == 0 -> 1\n            else -> -1\n        }\n    }\n}" %}

### SpiralMatrix
{% include code-tabs.html  kotlin="package array\n\nclass SpiralMatrix {\n    fun spiralOrder(matrix: Array<IntArray>): List<Int> {\n        if (matrix.isEmpty()) return emptyList()\n\n        val result = mutableListOf<Int>()\n        var (top, bottom, left, right) = listOf(0, matrix.size - 1, 0, matrix[0].size - 1)\n        var count = 0\n        val totalElements = matrix.size * matrix[0].size\n\n        while (count < totalElements) {\n            // Traverse top row\n            for (j in left..right) if (count++ < totalElements) result.add(matrix[top][j])\n            top++\n\n            // Traverse right column\n            for (i in top..bottom) if (count++ < totalElements) result.add(matrix[i][right])\n            right--\n\n            // Traverse bottom row\n            for (j in right downTo left) if (count++ < totalElements) result.add(matrix[bottom][j])\n            bottom--\n\n            // Traverse left column\n            for (i in bottom downTo top) if (count++ < totalElements) result.add(matrix[i][left])\n            left++\n        }\n\n        return result\n    }\n}"  java="class SpiralMatrix {\n    public static void $1(matrix: Array<IntArray>): List<Int> {\n        if (matrix.isEmpty()) return emptyList()\n\n        val result = mutableListOf<Int>()\n        var (top, bottom, left, right) = listOf(0, matrix.size - 1, 0, matrix[0].size - 1)\n        var count = 0\n        val totalElements = matrix.size * matrix[0].size\n\n        while (count < totalElements) {\n            // Traverse top row\n            for (j in left..right) if (count++ < totalElements) result.add(matrix[top][j])\n            top++\n\n            // Traverse right column\n            for (i in top..bottom) if (count++ < totalElements) result.add(matrix[i][right])\n            right--\n\n            // Traverse bottom row\n            for (j in right downTo left) if (count++ < totalElements) result.add(matrix[bottom][j])\n            bottom--\n\n            // Traverse left column\n            for (i in bottom downTo top) if (count++ < totalElements) result.add(matrix[i][left])\n            left++\n        }\n\n        return result\n    }\n}"  python="class SpiralMatrix {\n    def $1(matrix: Array<list[int]>): List<Int> {\n        if (matrix.isEmpty()) return emptyList()\n\n        $1 = mutableListOf<Int>()\n        var (top, bottom, left, right) = listOf(0, matrix.size - 1, 0, matrix[0].size - 1)\n        $1 = 0\n        $1 = matrix.size * matrix[0].size\n\n        while (count < totalElements) {\n            // Traverse top row\n            for (j in left..right) if (count++ < totalElements) result.add(matrix[top][j])\n            top++\n\n            // Traverse right column\n            for (i in top..bottom) if (count++ < totalElements) result.add(matrix[i][right])\n            right--\n\n            // Traverse bottom row\n            for (j in right downTo left) if (count++ < totalElements) result.add(matrix[bottom][j])\n            bottom--\n\n            // Traverse left column\n            for (i in bottom downTo top) if (count++ < totalElements) result.add(matrix[i][left])\n            left++\n        }\n\n        return result\n    }\n}"  rust="class SpiralMatrix {\n    fn $1(matrix: Array<Vec<i32>>): List<Int> {\n        if (matrix.isEmpty()) return emptyList()\n\n        val result = mutableListOf<Int>()\n        var (top, bottom, left, right) = listOf(0, matrix.size - 1, 0, matrix[0].size - 1)\n        var count = 0\n        val totalElements = matrix.size * matrix[0].size\n\n        while (count < totalElements) {\n            // Traverse top row\n            for (j in left..right) if (count++ < totalElements) result.add(matrix[top][j])\n            top++\n\n            // Traverse right column\n            for (i in top..bottom) if (count++ < totalElements) result.add(matrix[i][right])\n            right--\n\n            // Traverse bottom row\n            for (j in right downTo left) if (count++ < totalElements) result.add(matrix[bottom][j])\n            bottom--\n\n            // Traverse left column\n            for (i in bottom downTo top) if (count++ < totalElements) result.add(matrix[i][left])\n            left++\n        }\n\n        return result\n    }\n}"  cpp="class SpiralMatrix {\n    $1(matrix: Array<vector<int>>): List<Int> {\n        if (matrix.isEmpty()) return emptyList()\n\n        auto $1 = mutableListOf<Int>()\n        var (top, bottom, left, right) = listOf(0, matrix.size - 1, 0, matrix[0].size - 1)\n        auto $1 = 0\n        auto $1 = matrix.size * matrix[0].size\n\n        while (count < totalElements) {\n            // Traverse top row\n            for (j in left..right) if (count++ < totalElements) result.add(matrix[top][j])\n            top++\n\n            // Traverse right column\n            for (i in top..bottom) if (count++ < totalElements) result.add(matrix[i][right])\n            right--\n\n            // Traverse bottom row\n            for (j in right downTo left) if (count++ < totalElements) result.add(matrix[bottom][j])\n            bottom--\n\n            // Traverse left column\n            for (i in bottom downTo top) if (count++ < totalElements) result.add(matrix[i][left])\n            left++\n        }\n\n        return result\n    }\n}" %}

### SpiralMatrix_II
{% include code-tabs.html  kotlin="package array\n\nclass SpiralMatrix_II {\n    class Solution {\n        fun generateMatrix(n: Int): Array<IntArray> {\n            val matrix = Array(n) { IntArray(n) }\n            var (top, bottom, left, right, num) = arrayOf(0, n - 1, 0, n - 1, 1)\n\n            while (top <= bottom && left <= right) {\n                // Fill top row\n                for (i in left..right) matrix[top][i] = num++\n                top++\n\n                // Fill right column\n                for (i in top..bottom) matrix[i][right] = num++\n                right--\n\n                // Fill bottom row\n                if (top <= bottom) {\n                    for (i in right downTo left) matrix[bottom][i] = num++\n                    bottom--\n                }\n\n                // Fill left column\n                if (left <= right) {\n                    for (i in bottom downTo top) matrix[i][left] = num++\n                    left++\n                }\n            }\n\n            return matrix\n        }\n    }\n}\n"  java="class SpiralMatrix_II {\n    class Solution {\n        public static void $1(n): Array<IntArray> {\n            val matrix = Array(n) { IntArray(n) }\n            var (top, bottom, left, right, num) = arrayOf(0, n - 1, 0, n - 1, 1)\n\n            while (top <= bottom && left <= right) {\n                // Fill top row\n                for (i in left..right) matrix[top][i] = num++\n                top++\n\n                // Fill right column\n                for (i in top..bottom) matrix[i][right] = num++\n                right--\n\n                // Fill bottom row\n                if (top <= bottom) {\n                    for (i in right downTo left) matrix[bottom][i] = num++\n                    bottom--\n                }\n\n                // Fill left column\n                if (left <= right) {\n                    for (i in bottom downTo top) matrix[i][left] = num++\n                    left++\n                }\n            }\n\n            return matrix\n        }\n    }\n}"  python="class SpiralMatrix_II {\n    class Solution {\n        def $1(n: int): Array<list[int]> {\n            $1 = Array(n) { list[int](n) }\n            var (top, bottom, left, right, num) = arrayOf(0, n - 1, 0, n - 1, 1)\n\n            while (top <= bottom && left <= right) {\n                // Fill top row\n                for (i in left..right) matrix[top][i] = num++\n                top++\n\n                // Fill right column\n                for (i in top..bottom) matrix[i][right] = num++\n                right--\n\n                // Fill bottom row\n                if (top <= bottom) {\n                    for (i in right downTo left) matrix[bottom][i] = num++\n                    bottom--\n                }\n\n                // Fill left column\n                if (left <= right) {\n                    for (i in bottom downTo top) matrix[i][left] = num++\n                    left++\n                }\n            }\n\n            return matrix\n        }\n    }\n}"  rust="class SpiralMatrix_II {\n    class Solution {\n        fn $1(n -> i32): Array<Vec<i32>> {\n            val matrix = Array(n) { Vec<i32>(n) }\n            var (top, bottom, left, right, num) = arrayOf(0, n - 1, 0, n - 1, 1)\n\n            while (top <= bottom && left <= right) {\n                // Fill top row\n                for (i in left..right) matrix[top][i] = num++\n                top++\n\n                // Fill right column\n                for (i in top..bottom) matrix[i][right] = num++\n                right--\n\n                // Fill bottom row\n                if (top <= bottom) {\n                    for (i in right downTo left) matrix[bottom][i] = num++\n                    bottom--\n                }\n\n                // Fill left column\n                if (left <= right) {\n                    for (i in bottom downTo top) matrix[i][left] = num++\n                    left++\n                }\n            }\n\n            return matrix\n        }\n    }\n}"  cpp="class SpiralMatrix_II {\n    class Solution {\n        $1(n): Array<vector<int>> {\n            auto $1 = Array(n) { vector<int>(n) }\n            var (top, bottom, left, right, num) = arrayOf(0, n - 1, 0, n - 1, 1)\n\n            while (top <= bottom && left <= right) {\n                // Fill top row\n                for (i in left..right) matrix[top][i] = num++\n                top++\n\n                // Fill right column\n                for (i in top..bottom) matrix[i][right] = num++\n                right--\n\n                // Fill bottom row\n                if (top <= bottom) {\n                    for (i in right downTo left) matrix[bottom][i] = num++\n                    bottom--\n                }\n\n                // Fill left column\n                if (left <= right) {\n                    for (i in bottom downTo top) matrix[i][left] = num++\n                    left++\n                }\n            }\n\n            return matrix\n        }\n    }\n}" %}

### ToeplitzMatrix
{% include code-tabs.html  kotlin="package array\n\nclass ToeplitzMatrix {\n    fun isToeplitzMatrix(matrix: Array<IntArray>): Boolean {\n        val m = matrix.size\n        val n = matrix[0].size\n\n        var row = m - 1\n        var col = 0\n\n        while (row < m && col < n) {\n            var i = row\n            var j = col\n\n            val first = matrix[i][j]\n\n            while (i < m && j < n) {\n                if (matrix[i][j] != first)\n                    return false\n                i++\n                j++\n            }\n\n            when(row) {\n                0 -> col++\n                else -> row --\n            }\n        }\n\n        return true\n    }\n}"  java="class ToeplitzMatrix {\n    public static void $1(matrix: Array<IntArray>) {\n        val m = matrix.size\n        val n = matrix[0].size\n\n        var row = m - 1\n        var col = 0\n\n        while (row < m && col < n) {\n            var i = row\n            var j = col\n\n            val first = matrix[i][j]\n\n            while (i < m && j < n) {\n                if (matrix[i][j] != first)\n                    return false\n                i++\n                j++\n            }\n\n            when(row) {\n                0 -> col++\n                else -> row --\n            }\n        }\n\n        return true\n    }\n}"  python="class ToeplitzMatrix {\n    def $1(matrix: Array<list[int]>): bool {\n        $1 = matrix.size\n        $1 = matrix[0].size\n\n        $1 = m - 1\n        $1 = 0\n\n        while (row < m && col < n) {\n            $1 = row\n            $1 = col\n\n            $1 = matrix[i][j]\n\n            while (i < m && j < n) {\n                if (matrix[i][j] != first)\n                    return False\n                i++\n                j++\n            }\n\n            when(row) {\n                0 -> col++\n                else -> row --\n            }\n        }\n\n        return True\n    }\n}"  rust="class ToeplitzMatrix {\n    fn $1(matrix: Array<Vec<i32>>) -> bool {\n        val m = matrix.size\n        val n = matrix[0].size\n\n        var row = m - 1\n        var col = 0\n\n        while (row < m && col < n) {\n            var i = row\n            var j = col\n\n            val first = matrix[i][j]\n\n            while (i < m && j < n) {\n                if (matrix[i][j] != first)\n                    return false\n                i++\n                j++\n            }\n\n            when(row) {\n                0 -> col++\n                else -> row --\n            }\n        }\n\n        return true\n    }\n}"  cpp="class ToeplitzMatrix {\n    $1(matrix: Array<vector<int>>) {\n        auto $1 = matrix.size\n        auto $1 = matrix[0].size\n\n        auto $1 = m - 1\n        auto $1 = 0\n\n        while (row < m && col < n) {\n            auto $1 = row\n            auto $1 = col\n\n            auto $1 = matrix[i][j]\n\n            while (i < m && j < n) {\n                if (matrix[i][j] != first)\n                    return false\n                i++\n                j++\n            }\n\n            when(row) {\n                0 -> col++\n                else -> row --\n            }\n        }\n\n        return true\n    }\n}" %}

### TransposeMatrix
{% include code-tabs.html  kotlin="package array\n\nclass TransposeMatrix {\n    fun transpose(matrix: Array<IntArray>): Array<IntArray> {\n        val rows = matrix.size\n        val cols = matrix[0].size\n        val transposed = Array(cols) { IntArray(rows) }\n\n        for (i in matrix.indices) {\n            for (j in matrix[i].indices) {\n                transposed[j][i] = matrix[i][j]\n            }\n        }\n\n        return transposed\n    }\n}"  java="class TransposeMatrix {\n    public static void $1(matrix: Array<IntArray>): Array<IntArray> {\n        val rows = matrix.size\n        val cols = matrix[0].size\n        val transposed = Array(cols) { IntArray(rows) }\n\n        for (i in matrix.indices) {\n            for (j in matrix[i].indices) {\n                transposed[j][i] = matrix[i][j]\n            }\n        }\n\n        return transposed\n    }\n}"  python="class TransposeMatrix {\n    def $1(matrix: Array<list[int]>): Array<list[int]> {\n        $1 = matrix.size\n        $1 = matrix[0].size\n        $1 = Array(cols) { list[int](rows) }\n\n        for (i in matrix.indices) {\n            for (j in matrix[i].indices) {\n                transposed[j][i] = matrix[i][j]\n            }\n        }\n\n        return transposed\n    }\n}"  rust="class TransposeMatrix {\n    fn $1(matrix: Array<Vec<i32>>): Array<Vec<i32>> {\n        val rows = matrix.size\n        val cols = matrix[0].size\n        val transposed = Array(cols) { Vec<i32>(rows) }\n\n        for (i in matrix.indices) {\n            for (j in matrix[i].indices) {\n                transposed[j][i] = matrix[i][j]\n            }\n        }\n\n        return transposed\n    }\n}"  cpp="class TransposeMatrix {\n    $1(matrix: Array<vector<int>>): Array<vector<int>> {\n        auto $1 = matrix.size\n        auto $1 = matrix[0].size\n        auto $1 = Array(cols) { vector<int>(rows) }\n\n        for (i in matrix.indices) {\n            for (j in matrix[i].indices) {\n                transposed[j][i] = matrix[i][j]\n            }\n        }\n\n        return transposed\n    }\n}" %}

### NestedListWeightedSum
{% include code-tabs.html  kotlin="package array.dfs\n\n\n// Dummy implementation\nclass NestedInteger {\n         // Constructor initializes an empty nested list.\n    constructor()\n\n    constructor(value: Int)\n\n    fun isInteger(): Boolean {\n        return true\n    }\n\n    fun getInteger(): Int? {\n        return 2\n    }\n\n    fun setInteger(value: Int): Unit {\n\n    }\n\n    fun add(ni: NestedInteger): Unit {\n\n    }\n    fun getList(): List<NestedInteger>? {\n        return listOf()\n    }\n}\n\n\nclass NestedListWeightedSum {\n    fun depthSum(nestedList: List<NestedInteger>): Int {\n        return dfs(nestedList, 1)\n    }\n\n    fun dfs(list: List<NestedInteger>, depth: Int): Int {\n        var sum = 0\n        for (nested in list) {\n            if (nested.isInteger()) {\n                sum += (depth * nested.getInteger() !!)\n            } else {\n                sum += dfs(nested.getList()!!, depth + 1)\n            }\n        }\n\n        return sum\n    }\n}"  java="// Dummy implementation\nclass NestedInteger {\n         // Constructor initializes an empty nested list.\n    constructor()\n\n    constructor(value)\n\n    public static void $1() {\n        return true\n    }\n\n    public static void $1()? {\n        return 2\n    }\n\n    public static void $1(value) {\n\n    }\n\n    public static void $1(ni: NestedInteger) {\n\n    }\n    public static void $1(): List<NestedInteger>? {\n        return listOf()\n    }\n}\n\n\nclass NestedListWeightedSum {\n    public static void $1(nestedList: List<NestedInteger>) {\n        return dfs(nestedList, 1)\n    }\n\n    public static void $1(list: List<NestedInteger>, depth) {\n        var sum = 0\n        for (nested in list) {\n            if (nested.isInteger()) {\n                sum += (depth * nested.getInteger() !!)\n            } else {\n                sum += dfs(nested.getList()!!, depth + 1)\n            }\n        }\n\n        return sum\n    }\n}"  python="// Dummy implementation\nclass NestedInteger {\n         // Constructor initializes an empty nested list.\n    constructor()\n\n    constructor(value: int)\n\n    def $1(): bool {\n        return True\n    }\n\n    def $1(): int? {\n        return 2\n    }\n\n    def $1(value: int) {\n\n    }\n\n    def $1(ni: NestedInteger) {\n\n    }\n    def $1(): List<NestedInteger>? {\n        return listOf()\n    }\n}\n\n\nclass NestedListWeightedSum {\n    def $1(nestedList: List<NestedInteger>): int {\n        return dfs(nestedList, 1)\n    }\n\n    def $1(list: List<NestedInteger>, depth: int): int {\n        $1 = 0\n        for (nested in list) {\n            if (nested.isInteger()) {\n                sum += (depth * nested.getInteger() !!)\n            } else {\n                sum += dfs(nested.getList()!!, depth + 1)\n            }\n        }\n\n        return sum\n    }\n}"  rust="// Dummy implementation\nclass NestedInteger {\n         // Constructor initializes an empty nested list.\n    constructor()\n\n    constructor(value -> i32)\n\n    fn $1() -> bool {\n        return true\n    }\n\n    fn $1() -> i32? {\n        return 2\n    }\n\n    fn $1(value -> i32) {\n\n    }\n\n    fn $1(ni: NestedInteger) {\n\n    }\n    fn $1(): List<NestedInteger>? {\n        return listOf()\n    }\n}\n\n\nclass NestedListWeightedSum {\n    fn $1(nestedList: List<NestedInteger>) -> i32 {\n        return dfs(nestedList, 1)\n    }\n\n    fn $1(list: List<NestedInteger>, depth -> i32) -> i32 {\n        var sum = 0\n        for (nested in list) {\n            if (nested.isInteger()) {\n                sum += (depth * nested.getInteger() !!)\n            } else {\n                sum += dfs(nested.getList()!!, depth + 1)\n            }\n        }\n\n        return sum\n    }\n}"  cpp="// Dummy implementation\nclass NestedInteger {\n         // Constructor initializes an empty nested list.\n    constructor()\n\n    constructor(value)\n\n    $1() {\n        return true\n    }\n\n    $1()? {\n        return 2\n    }\n\n    $1(value) {\n\n    }\n\n    $1(ni: NestedInteger) {\n\n    }\n    $1(): List<NestedInteger>? {\n        return listOf()\n    }\n}\n\n\nclass NestedListWeightedSum {\n    $1(nestedList: List<NestedInteger>) {\n        return dfs(nestedList, 1)\n    }\n\n    $1(list: List<NestedInteger>, depth) {\n        auto $1 = 0\n        for (nested in list) {\n            if (nested.isInteger()) {\n                sum += (depth * nested.getInteger() !!)\n            } else {\n                sum += dfs(nested.getList()!!, depth + 1)\n            }\n        }\n\n        return sum\n    }\n}" %}
