---
layout: chapter
title: "Bit Manipulation"
chapter_number: 7
chapter_title: "Bit Manipulation"
toc: true
prev_chapter:
  url: "/chapters/06-graphs"
  title: "Graphs"
next_chapter:
  url: "/chapters/08-heaps"
  title: "Heaps"
---

# Bit Manipulation

> **9 problems** from the CrackGoogle repository. Click the tabs to switch between **Kotlin, Java, Python, Rust, and C++**.

## Complete Problem Set

| # | Problem | Topic | Code Available |
|---|---------|-------|----------------|
| 1 | **FirstLetterToAppearTwice** | bitset | ✅ All 5 languages |
| 2 | **LongestNiceSubarray** | bitset | ✅ All 5 languages |
| 3 | **MaximumXorOfTwoNumsInArray** | bitset | ✅ All 5 languages |
| 4 | **Number of Steps to ReduceaANumberInBinaryRepresentationtoOne** | bitset | ✅ All 5 languages |
| 5 | **NumberOfOneBits** | bitset | ✅ All 5 languages |
| 6 | **ReverseBits** | bitset | ✅ All 5 languages |
| 7 | **SingleNumber** | bitset | ✅ All 5 languages |
| 8 | **SingleNumber3** | bitset | ✅ All 5 languages |
| 9 | **SumOfAllSubsetXorTotal** | bitset | ✅ All 5 languages |

---

## FirstLetterToAppearTwice

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/bitset/FirstLetterToAppearTwice.kt`

{% include code-tabs.html  kotlin="package bitset\n\nimport java.util.*\n\nclass FirstLetterToAppearTwice {\n    /**\n     * This is more memory efficient approach...\n     */\n    fun repeatedCharacter(s: String): Char {\n\n        var bits = 0\n        for (ch in s) {\n            val ascii = ch - 'a'\n            val bit = 1 shl ascii\n\n            if ( (bits and bit) >= 1 ) {\n                return ch\n            }\n\n            bits = bits or bit\n\n        }\n\n        return 'x'\n    }\n\n    fun repeatedCharacterBitset(s: String): Char {\n\n        val seen = BitSet(26)  // BitSet for 26 lowercase English letters\n\n        for (ch in s) {\n            val index = ch - 'a'  // Calculate the index for the character\n\n            if (seen.get(index)) {  // Check if the bit at index is already set\n                return ch\n            }\n\n            seen.set(index)  // Set the bit at index\n        }\n\n        return 'x'  // Return null if no repeated character is found\n    }\n}"  java="class FirstLetterToAppearTwice {\n    /**\n     * This is more memory efficient approach...\n     */\n    public static void $1(s): Char {\n\n        var bits = 0\n        for (ch in s) {\n            val ascii = ch - 'a'\n            val bit = 1 shl ascii\n\n            if ( (bits and bit) >= 1 ) {\n                return ch\n            }\n\n            bits = bits or bit\n\n        }\n\n        return 'x'\n    }\n\n    public static void $1(s): Char {\n\n        val seen = BitSet(26)  // BitSet for 26 lowercase English letters\n\n        for (ch in s) {\n            val index = ch - 'a'  // Calculate the index for the character\n\n            if (seen.get(index)) {  // Check if the bit at index is already set\n                return ch\n            }\n\n            seen.set(index)  // Set the bit at index\n        }\n\n        return 'x'  // Return null if no repeated character is found\n    }\n}"  python="class FirstLetterToAppearTwice {\n    /**\n     * This is more memory efficient approach...\n     */\n    def $1(s: str): Char {\n\n        $1 = 0\n        for (ch in s) {\n            $1 = ch - 'a'\n            $1 = 1 shl ascii\n\n            if ( (bits and bit) >= 1 ) {\n                return ch\n            }\n\n            bits = bits or bit\n\n        }\n\n        return 'x'\n    }\n\n    def $1(s: str): Char {\n\n        $1 = BitSet(26)  // BitSet for 26 lowercase English letters\n\n        for (ch in s) {\n            $1 = ch - 'a'  // Calculate the index for the character\n\n            if (seen.get(index)) {  // Check if the bit at index is already set\n                return ch\n            }\n\n            seen.set(index)  // Set the bit at index\n        }\n\n        return 'x'  // Return None if no repeated character is found\n    }\n}"  rust="class FirstLetterToAppearTwice {\n    /**\n     * This is more memory efficient approach...\n     */\n    fn $1(s -> String): Char {\n\n        var bits = 0\n        for (ch in s) {\n            val ascii = ch - 'a'\n            val bit = 1 shl ascii\n\n            if ( (bits and bit) >= 1 ) {\n                return ch\n            }\n\n            bits = bits or bit\n\n        }\n\n        return 'x'\n    }\n\n    fn $1(s -> String): Char {\n\n        val seen = BitSet(26)  // BitSet for 26 lowercase English letters\n\n        for (ch in s) {\n            val index = ch - 'a'  // Calculate the index for the character\n\n            if (seen.get(index)) {  // Check if the bit at index is already set\n                return ch\n            }\n\n            seen.set(index)  // Set the bit at index\n        }\n\n        return 'x'  // Return None if no repeated character is found\n    }\n}"  cpp="class FirstLetterToAppearTwice {\n    /**\n     * This is more memory efficient approach...\n     */\n    $1(s): Char {\n\n        auto $1 = 0\n        for (ch in s) {\n            auto $1 = ch - 'a'\n            auto $1 = 1 shl ascii\n\n            if ( (bits and bit) >= 1 ) {\n                return ch\n            }\n\n            bits = bits or bit\n\n        }\n\n        return 'x'\n    }\n\n    $1(s): Char {\n\n        auto $1 = BitSet(26)  // BitSet for 26 lowercase English letters\n\n        for (ch in s) {\n            auto $1 = ch - 'a'  // Calculate the index for the character\n\n            if (seen.get(index)) {  // Check if the bit at index is already set\n                return ch\n            }\n\n            seen.set(index)  // Set the bit at index\n        }\n\n        return 'x'  // Return nullptr if no repeated character is found\n    }\n}" %}

---

## LongestNiceSubarray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/bitset/LongestNiceSubarray.kt`

{% include code-tabs.html  kotlin="package bitset\n\nclass LongestNiceSubarray {\n    fun longestNiceSubarray(nums: IntArray): Int {\n        var left = 0\n        var bitMask = 0\n        var maxLen = 0\n\n        for (right in nums.indices) {\n            while ((bitMask and nums[right]) != 0) {\n                bitMask = bitMask xor nums[left]\n                left++\n            }\n            bitMask = bitMask or nums[right]\n            maxLen = maxOf(maxLen, right - left + 1)\n        }\n\n        return maxLen\n    }\n}"  java="class LongestNiceSubarray {\n    public static void $1(nums: IntArray) {\n        var left = 0\n        var bitMask = 0\n        var maxLen = 0\n\n        for (right in nums.indices) {\n            while ((bitMask and nums[right]) != 0) {\n                bitMask = bitMask xor nums[left]\n                left++\n            }\n            bitMask = bitMask or nums[right]\n            maxLen = maxOf(maxLen, right - left + 1)\n        }\n\n        return maxLen\n    }\n}"  python="class LongestNiceSubarray {\n    def $1(nums: list[int]): int {\n        $1 = 0\n        $1 = 0\n        $1 = 0\n\n        for (right in nums.indices) {\n            while ((bitMask and nums[right]) != 0) {\n                bitMask = bitMask xor nums[left]\n                left++\n            }\n            bitMask = bitMask or nums[right]\n            maxLen = max(maxLen, right - left + 1)\n        }\n\n        return maxLen\n    }\n}"  rust="class LongestNiceSubarray {\n    fn $1(nums: Vec<i32>) -> i32 {\n        var left = 0\n        var bitMask = 0\n        var maxLen = 0\n\n        for (right in nums.indices) {\n            while ((bitMask and nums[right]) != 0) {\n                bitMask = bitMask xor nums[left]\n                left++\n            }\n            bitMask = bitMask or nums[right]\n            maxLen = max(maxLen, right - left + 1)\n        }\n\n        return maxLen\n    }\n}"  cpp="class LongestNiceSubarray {\n    $1(nums: vector<int>) {\n        auto $1 = 0\n        auto $1 = 0\n        auto $1 = 0\n\n        for (right in nums.indices) {\n            while ((bitMask and nums[right]) != 0) {\n                bitMask = bitMask xor nums[left]\n                left++\n            }\n            bitMask = bitMask or nums[right]\n            maxLen = max(maxLen, right - left + 1)\n        }\n\n        return maxLen\n    }\n}" %}

---

## MaximumXorOfTwoNumsInArray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/bitset/MaximumXorOfTwoNumsInArray.kt`

{% include code-tabs.html  kotlin="package bitset\n\nclass MaximumXorOfTwoNumsInArray {\n    data class Trie(val children: Array<Trie?> = arrayOfNulls(2))\n\n    fun findMaximumXOR(nums: IntArray): Int {\n        val root = buildTrie(nums)\n\n        var max = Int.MIN_VALUE\n        for (num in nums) {\n\n            var curNode = root\n            var curSum = 0\n            for (i in 31 downTo 0) {\n                val curBit = if ((1 shl i and num) != 0) 1 else 0\n                if (curNode.children[curBit xor 1] != null) {\n                    curSum += (1 shl i)\n                    curNode = curNode.children[curBit xor 1]!!\n                } else {\n                    curNode = curNode.children[curBit]!!\n                }\n            }\n            max = maxOf(max, curSum)\n        }\n\n        return max\n    }\n\n    private fun buildTrie(nums: IntArray): Trie {\n        val root = Trie()\n\n        for (num in nums) {\n            var ptr = root\n            for (i in 31 downTo 0) {\n                val currBit = if((1 shl i) and num !=0) 1 else 0\n                if (ptr.children[currBit] == null) {\n                    ptr.children[currBit] = Trie()\n                }\n\n                ptr = ptr.children[currBit] !!\n            }\n        }\n\n        return root\n    }\n}"  java="class MaximumXorOfTwoNumsInArray {\n    data class Trie(val children: Array<Trie?> = arrayOfNulls(2))\n\n    public static void $1(nums: IntArray) {\n        val root = buildTrie(nums)\n\n        var max = Int.MIN_VALUE\n        for (num in nums) {\n\n            var curNode = root\n            var curSum = 0\n            for (i in 31 downTo 0) {\n                val curBit = if ((1 shl i and num) != 0) 1 else 0\n                if (curNode.children[curBit xor 1] != null) {\n                    curSum += (1 shl i)\n                    curNode = curNode.children[curBit xor 1]!!\n                } else {\n                    curNode = curNode.children[curBit]!!\n                }\n            }\n            max = maxOf(max, curSum)\n        }\n\n        return max\n    }\n\n    private public static void $1(nums: IntArray): Trie {\n        val root = Trie()\n\n        for (num in nums) {\n            var ptr = root\n            for (i in 31 downTo 0) {\n                val currBit = if((1 shl i) and num !=0) 1 else 0\n                if (ptr.children[currBit] == null) {\n                    ptr.children[currBit] = Trie()\n                }\n\n                ptr = ptr.children[currBit] !!\n            }\n        }\n\n        return root\n    }\n}"  python="class MaximumXorOfTwoNumsInArray {\n    data class Trie(val children: Array<Trie?> = arrayOfNulls(2))\n\n    def $1(nums: list[int]): int {\n        $1 = buildTrie(nums)\n\n        $1 = Int.MIN_VALUE\n        for (num in nums) {\n\n            $1 = root\n            $1 = 0\n            for (i in 31 downTo 0) {\n                $1 = if ((1 shl i and num) != 0) 1 else 0\n                if (curNode.children[curBit xor 1] != None) {\n                    curSum += (1 shl i)\n                    curNode = curNode.children[curBit xor 1]!!\n                } else {\n                    curNode = curNode.children[curBit]!!\n                }\n            }\n            max = max(max, curSum)\n        }\n\n        return max\n    }\n\n    private def $1(nums: list[int]): Trie {\n        $1 = Trie()\n\n        for (num in nums) {\n            $1 = root\n            for (i in 31 downTo 0) {\n                $1 = if((1 shl i) and num !=0) 1 else 0\n                if (ptr.children[currBit] == None) {\n                    ptr.children[currBit] = Trie()\n                }\n\n                ptr = ptr.children[currBit] !!\n            }\n        }\n\n        return root\n    }\n}"  rust="class MaximumXorOfTwoNumsInArray {\n    data class Trie(val children: Array<Trie?> = arrayOfNulls(2))\n\n    fn $1(nums: Vec<i32>) -> i32 {\n        val root = buildTrie(nums)\n\n        var max = Int.MIN_VALUE\n        for (num in nums) {\n\n            var curNode = root\n            var curSum = 0\n            for (i in 31 downTo 0) {\n                val curBit = if ((1 shl i and num) != 0) 1 else 0\n                if (curNode.children[curBit xor 1] != None) {\n                    curSum += (1 shl i)\n                    curNode = curNode.children[curBit xor 1]!!\n                } else {\n                    curNode = curNode.children[curBit]!!\n                }\n            }\n            max = max(max, curSum)\n        }\n\n        return max\n    }\n\n    private fn $1(nums: Vec<i32>): Trie {\n        val root = Trie()\n\n        for (num in nums) {\n            var ptr = root\n            for (i in 31 downTo 0) {\n                val currBit = if((1 shl i) and num !=0) 1 else 0\n                if (ptr.children[currBit] == None) {\n                    ptr.children[currBit] = Trie()\n                }\n\n                ptr = ptr.children[currBit] !!\n            }\n        }\n\n        return root\n    }\n}"  cpp="class MaximumXorOfTwoNumsInArray {\n    data class Trie(val children: Array<Trie?> = arrayOfNulls(2))\n\n    $1(nums: vector<int>) {\n        auto $1 = buildTrie(nums)\n\n        auto $1 = Int.MIN_VALUE\n        for (num in nums) {\n\n            auto $1 = root\n            auto $1 = 0\n            for (i in 31 downTo 0) {\n                auto $1 = if ((1 shl i and num) != 0) 1 else 0\n                if (curNode.children[curBit xor 1] != nullptr) {\n                    curSum += (1 shl i)\n                    curNode = curNode.children[curBit xor 1]!!\n                } else {\n                    curNode = curNode.children[curBit]!!\n                }\n            }\n            max = max(max, curSum)\n        }\n\n        return max\n    }\n\n    private $1(nums: vector<int>): Trie {\n        auto $1 = Trie()\n\n        for (num in nums) {\n            auto $1 = root\n            for (i in 31 downTo 0) {\n                auto $1 = if((1 shl i) and num !=0) 1 else 0\n                if (ptr.children[currBit] == nullptr) {\n                    ptr.children[currBit] = Trie()\n                }\n\n                ptr = ptr.children[currBit] !!\n            }\n        }\n\n        return root\n    }\n}" %}

---

## Number of Steps to ReduceaANumberInBinaryRepresentationtoOne

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/bitset/Number of Steps to ReduceaANumberInBinaryRepresentationtoOne.kt`

{% include code-tabs.html  kotlin="package bitset\n\nclass `Number of Steps to ReduceaANumberInBinaryRepresentationtoOne` {\n    fun numSteps(s: String): Int {\n        var steps = 0\n        var carry = 0\n\n        for (i in s.length - 1 downTo 1) {\n            val digit = (s[i] - '0') + carry\n            if (digit % 2 == 1) {\n                steps += 2  // Add 1 (makes it even) + divide by 2\n                carry = 1   // Propagate carry\n            } else {\n                steps += 1  // Just divide by 2\n            }\n        }\n\n        return steps + carry  // Handle remaining carry at the first digit\n    }\n}"  java="class `Number of Steps to ReduceaANumberInBinaryRepresentationtoOne` {\n    public static void $1(s) {\n        var steps = 0\n        var carry = 0\n\n        for (i in s.length - 1 downTo 1) {\n            val digit = (s[i] - '0') + carry\n            if (digit % 2 == 1) {\n                steps += 2  // Add 1 (makes it even) + divide by 2\n                carry = 1   // Propagate carry\n            } else {\n                steps += 1  // Just divide by 2\n            }\n        }\n\n        return steps + carry  // Handle remaining carry at the first digit\n    }\n}"  python="class `Number of Steps to ReduceaANumberInBinaryRepresentationtoOne` {\n    def $1(s: str): int {\n        $1 = 0\n        $1 = 0\n\n        for (i in s.length - 1 downTo 1) {\n            $1 = (s[i] - '0') + carry\n            if (digit % 2 == 1) {\n                steps += 2  // Add 1 (makes it even) + divide by 2\n                carry = 1   // Propagate carry\n            } else {\n                steps += 1  // Just divide by 2\n            }\n        }\n\n        return steps + carry  // Handle remaining carry at the first digit\n    }\n}"  rust="class `Number of Steps to ReduceaANumberInBinaryRepresentationtoOne` {\n    fn $1(s -> String) -> i32 {\n        var steps = 0\n        var carry = 0\n\n        for (i in s.length - 1 downTo 1) {\n            val digit = (s[i] - '0') + carry\n            if (digit % 2 == 1) {\n                steps += 2  // Add 1 (makes it even) + divide by 2\n                carry = 1   // Propagate carry\n            } else {\n                steps += 1  // Just divide by 2\n            }\n        }\n\n        return steps + carry  // Handle remaining carry at the first digit\n    }\n}"  cpp="class `Number of Steps to ReduceaANumberInBinaryRepresentationtoOne` {\n    $1(s) {\n        auto $1 = 0\n        auto $1 = 0\n\n        for (i in s.length - 1 downTo 1) {\n            auto $1 = (s[i] - '0') + carry\n            if (digit % 2 == 1) {\n                steps += 2  // Add 1 (makes it even) + divide by 2\n                carry = 1   // Propagate carry\n            } else {\n                steps += 1  // Just divide by 2\n            }\n        }\n\n        return steps + carry  // Handle remaining carry at the first digit\n    }\n}" %}

---

## NumberOfOneBits

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/bitset/NumberOfOneBits.kt`

{% include code-tabs.html  kotlin="package bitset\n\nclass NumberOfOneBits {\n    fun hammingWeight(n: Int): Int {\n        var number = n\n        var count = 0\n\n        while (number > 0) {\n            number = number and (number - 1)\n            count++\n        }\n\n        return count\n    }\n}"  java="class NumberOfOneBits {\n    public static void $1(n) {\n        var number = n\n        var count = 0\n\n        while (number > 0) {\n            number = number and (number - 1)\n            count++\n        }\n\n        return count\n    }\n}"  python="class NumberOfOneBits {\n    def $1(n: int): int {\n        $1 = n\n        $1 = 0\n\n        while (number > 0) {\n            number = number and (number - 1)\n            count++\n        }\n\n        return count\n    }\n}"  rust="class NumberOfOneBits {\n    fn $1(n -> i32) -> i32 {\n        var number = n\n        var count = 0\n\n        while (number > 0) {\n            number = number and (number - 1)\n            count++\n        }\n\n        return count\n    }\n}"  cpp="class NumberOfOneBits {\n    $1(n) {\n        auto $1 = n\n        auto $1 = 0\n\n        while (number > 0) {\n            number = number and (number - 1)\n            count++\n        }\n\n        return count\n    }\n}" %}

---

## ReverseBits

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/bitset/ReverseBits.kt`

{% include code-tabs.html  kotlin="package bitset\n\nclass ReverseBits {\n    fun reverseBits(n: Int): Int {\n        var num = n\n        var result = 0\n\n        for (i in 0 until 32) {\n            val bit = num and 1   // Extract the last bit\n            result = (result shl 1) or bit  // Shift left and add the bit\n            num = num ushr 1       // Unsigned right shift n\n        }\n\n        return result\n    }\n}\n"  java="class ReverseBits {\n    public static void $1(n) {\n        var num = n\n        var result = 0\n\n        for (i in 0 until 32) {\n            val bit = num and 1   // Extract the last bit\n            result = (result shl 1) or bit  // Shift left and add the bit\n            num = num ushr 1       // Unsigned right shift n\n        }\n\n        return result\n    }\n}"  python="class ReverseBits {\n    def $1(n: int): int {\n        $1 = n\n        $1 = 0\n\n        for (i in 0 until 32) {\n            $1 = num and 1   // Extract the last bit\n            result = (result shl 1) or bit  // Shift left and add the bit\n            num = num ushr 1       // Unsigned right shift n\n        }\n\n        return result\n    }\n}"  rust="class ReverseBits {\n    fn $1(n -> i32) -> i32 {\n        var num = n\n        var result = 0\n\n        for (i in 0 until 32) {\n            val bit = num and 1   // Extract the last bit\n            result = (result shl 1) or bit  // Shift left and add the bit\n            num = num ushr 1       // Unsigned right shift n\n        }\n\n        return result\n    }\n}"  cpp="class ReverseBits {\n    $1(n) {\n        auto $1 = n\n        auto $1 = 0\n\n        for (i in 0 until 32) {\n            auto $1 = num and 1   // Extract the last bit\n            result = (result shl 1) or bit  // Shift left and add the bit\n            num = num ushr 1       // Unsigned right shift n\n        }\n\n        return result\n    }\n}" %}

---

## SingleNumber

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/bitset/SingleNumber.kt`

{% include code-tabs.html  kotlin="package bitset\n\nclass SingleNumber {\n    fun singleNumber(nums: IntArray): Int {\n        var xorSum = 0\n        nums.forEach { xorSum = xorSum xor it}\n        return xorSum\n    }\n}"  java="class SingleNumber {\n    public static void $1(nums: IntArray) {\n        var xorSum = 0\n        nums.forEach { xorSum = xorSum xor it}\n        return xorSum\n    }\n}"  python="class SingleNumber {\n    def $1(nums: list[int]): int {\n        $1 = 0\n        nums.forEach { xorSum = xorSum xor it}\n        return xorSum\n    }\n}"  rust="class SingleNumber {\n    fn $1(nums: Vec<i32>) -> i32 {\n        var xorSum = 0\n        nums.forEach { xorSum = xorSum xor it}\n        return xorSum\n    }\n}"  cpp="class SingleNumber {\n    $1(nums: vector<int>) {\n        auto $1 = 0\n        nums.forEach { xorSum = xorSum xor it}\n        return xorSum\n    }\n}" %}

---

## SingleNumber3

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/bitset/SingleNumber3.kt`

{% include code-tabs.html  kotlin="package bitset\n\nclass SingleNumber3 {\n    fun singleNumber(nums: IntArray): IntArray {\n        var xorSum = 0\n        for (num in nums) {\n            xorSum = xorSum.xor(num)\n        }\n\n        val rightmostSetBit = xorSum and xorSum\n        var (a, b) = listOf(0, 0)\n\n        for (num in nums) {\n            if (num and rightmostSetBit !=0) {\n                a = a.xor(num)\n            } else {\n                b = b.xor(num)\n            }\n        }\n\n        return intArrayOf(a, b)\n    }\n\n    companion object {\n        @JvmStatic\n        fun main(args: Array<String>) {\n            val test = SingleNumber3()\n\n            println(\"Single numbers are ${test.singleNumber(intArrayOf(1,2,1,3,2,5))}\")\n        }\n    }\n}"  java="class SingleNumber3 {\n    public static void $1(nums: IntArray): IntArray {\n        var xorSum = 0\n        for (num in nums) {\n            xorSum = xorSum.xor(num)\n        }\n\n        val rightmostSetBit = xorSum and xorSum\n        var (a, b) = listOf(0, 0)\n\n        for (num in nums) {\n            if (num and rightmostSetBit !=0) {\n                a = a.xor(num)\n            } else {\n                b = b.xor(num)\n            }\n        }\n\n        return intArrayOf(a, b)\n    }\n\n    companion object {\n        @JvmStatic\n        public static void $1(args: Array<String>) {\n            val test = SingleNumber3()\n\n            println(\"Single numbers are ${test.singleNumber(intArrayOf(1,2,1,3,2,5))}\")\n        }\n    }\n}"  python="class SingleNumber3 {\n    def $1(nums: list[int]): list[int] {\n        $1 = 0\n        for (num in nums) {\n            xorSum = xorSum.xor(num)\n        }\n\n        $1 = xorSum and xorSum\n        var (a, b) = listOf(0, 0)\n\n        for (num in nums) {\n            if (num and rightmostSetBit !=0) {\n                a = a.xor(num)\n            } else {\n                b = b.xor(num)\n            }\n        }\n\n        return intArrayOf(a, b)\n    }\n\n    companion object {\n        @JvmStatic\n        def $1(args: Array<String>) {\n            $1 = SingleNumber3()\n\n            print(\"Single numbers are ${test.singleNumber(intArrayOf(1,2,1,3,2,5))}\")\n        }\n    }\n}"  rust="class SingleNumber3 {\n    fn $1(nums: Vec<i32>): Vec<i32> {\n        var xorSum = 0\n        for (num in nums) {\n            xorSum = xorSum.xor(num)\n        }\n\n        val rightmostSetBit = xorSum and xorSum\n        var (a, b) = listOf(0, 0)\n\n        for (num in nums) {\n            if (num and rightmostSetBit !=0) {\n                a = a.xor(num)\n            } else {\n                b = b.xor(num)\n            }\n        }\n\n        return intArrayOf(a, b)\n    }\n\n    companion object {\n        @JvmStatic\n        fn $1(args: Array<String>) {\n            val test = SingleNumber3()\n\n            println!(\"Single numbers are ${test.singleNumber(intArrayOf(1,2,1,3,2,5))}\")\n        }\n    }\n}"  cpp="class SingleNumber3 {\n    $1(nums: vector<int>): vector<int> {\n        auto $1 = 0\n        for (num in nums) {\n            xorSum = xorSum.xor(num)\n        }\n\n        auto $1 = xorSum and xorSum\n        var (a, b) = listOf(0, 0)\n\n        for (num in nums) {\n            if (num and rightmostSetBit !=0) {\n                a = a.xor(num)\n            } else {\n                b = b.xor(num)\n            }\n        }\n\n        return intArrayOf(a, b)\n    }\n\n    companion object {\n        @JvmStatic\n        $1(args: Array<String>) {\n            auto $1 = SingleNumber3()\n\n            cout <<(\"Single numbers are ${test.singleNumber(intArrayOf(1,2,1,3,2,5))}\")\n        }\n    }\n}" %}

---

## SumOfAllSubsetXorTotal

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/bitset/SumOfAllSubsetXorTotal.kt`

{% include code-tabs.html  kotlin="package bitset\n\nclass SumOfAllSubsetXorTotal {\n    fun subsetXORSum(nums: IntArray): Int {\n        var result = 0\n        // Capture each bit that is set in any of the elements\n        for (num in nums) {\n            result = result or num // Update the result by OR-ing each number\n        }\n        // Multiply by the number of subset XOR totals that will have each bit set\n        return result shl (nums.size - 1) // Shift by (n-1) to account for all subsets\n    }\n}\n"  java="class SumOfAllSubsetXorTotal {\n    public static void $1(nums: IntArray) {\n        var result = 0\n        // Capture each bit that is set in any of the elements\n        for (num in nums) {\n            result = result or num // Update the result by OR-ing each number\n        }\n        // Multiply by the number of subset XOR totals that will have each bit set\n        return result shl (nums.size - 1) // Shift by (n-1) to account for all subsets\n    }\n}"  python="class SumOfAllSubsetXorTotal {\n    def $1(nums: list[int]): int {\n        $1 = 0\n        // Capture each bit that is set in any of the elements\n        for (num in nums) {\n            result = result or num // Update the result by OR-ing each number\n        }\n        // Multiply by the number of subset XOR totals that will have each bit set\n        return result shl (nums.size - 1) // Shift by (n-1) to account for all subsets\n    }\n}"  rust="class SumOfAllSubsetXorTotal {\n    fn $1(nums: Vec<i32>) -> i32 {\n        var result = 0\n        // Capture each bit that is set in any of the elements\n        for (num in nums) {\n            result = result or num // Update the result by OR-ing each number\n        }\n        // Multiply by the number of subset XOR totals that will have each bit set\n        return result shl (nums.size - 1) // Shift by (n-1) to account for all subsets\n    }\n}"  cpp="class SumOfAllSubsetXorTotal {\n    $1(nums: vector<int>) {\n        auto $1 = 0\n        // Capture each bit that is set in any of the elements\n        for (num in nums) {\n            result = result or num // Update the result by OR-ing each number\n        }\n        // Multiply by the number of subset XOR totals that will have each bit set\n        return result shl (nums.size - 1) // Shift by (n-1) to account for all subsets\n    }\n}" %}
