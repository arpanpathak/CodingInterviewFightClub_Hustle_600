---
layout: chapter
title: "Linked Lists"
chapter_number: 4
chapter_title: "Linked Lists"
toc: true
prev_chapter:
  url: "/chapters/03-arrays-two-pointers"
  title: "Arrays & Two Pointers"
next_chapter:
  url: "/chapters/05-trees"
  title: "Trees"
---

# Linked Lists

> **18 problems** from the CrackGoogle repository. Click the tabs to switch between **Kotlin, Java, Python, Rust, and C++**.

## Complete Problem Set

| # | Problem | Topic | Code Available |
|---|---------|-------|----------------|
| 1 | **AddTwoNumbers** | linkedlist | ✅ All 5 languages |
| 2 | **CopyLinkedListWithRandomPointer** | linkedlist | ✅ All 5 languages |
| 3 | **DeleteMiddleNodeOfLinkedList** | linkedlist | ✅ All 5 languages |
| 4 | **InsertIntoASortedCircularLinkedList** | linkedlist | ✅ All 5 languages |
| 5 | **InsertIntoASortedCircularList** | linkedlist | ✅ All 5 languages |
| 6 | **IntersectionOfTwoLinkedList** | linkedlist | ✅ All 5 languages |
| 7 | **LinkedListCycle** | linkedlist | ✅ All 5 languages |
| 8 | **LinkedListCycle_II** | linkedlist | ✅ All 5 languages |
| 9 | **MergeKSortedList** | linkedlist | ✅ All 5 languages |
| 10 | **MergeKSortedListIterative** | linkedlist | ✅ All 5 languages |
| 11 | **MergeTwoSortedLIst** | linkedlist | ✅ All 5 languages |
| 12 | **OddEvenLinkedList** | linkedlist | ✅ All 5 languages |
| 13 | **OddOrEvenLinkedList** | linkedlist | ✅ All 5 languages |
| 14 | **PalindromeLinkedList** | linkedlist | ✅ All 5 languages |
| 15 | **RemoveNthNodeFromEndOfList** | linkedlist | ✅ All 5 languages |
| 16 | **ReverseLinkedList** | linkedlist | ✅ All 5 languages |
| 17 | **ReverseLinkedListIterative** | linkedlist | ✅ All 5 languages |
| 18 | **ReverseNodesInKGroups** | linkedlist | ✅ All 5 languages |

---

## AddTwoNumbers

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/AddTwoNumbers.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass ListNode(var `val`: Int) {\n    var next: ListNode? = null\n}\n\nclass AddTwoNumbers {\n    fun addTwoNumbers(l1: ListNode?, l2: ListNode?): ListNode? {\n        var carry = 0\n        val head = ListNode(0)\n        var ptr = head\n        var (n1, n2) = Pair(l1, l2)\n\n        while (n1!= null || n2 != null) {\n            val sum = (n1?.`val` ?: 0) + (n2?.`val` ?: 0) + carry\n            ptr.next = ListNode(sum % 10)\n            ptr = ptr.next!!\n\n            carry = if (sum > 9) 1 else 0\n\n            if (n1 != null) n1 = n1.next\n\n            if (n2 != null) n2 = n2.next\n        }\n\n        if (carry > 0) {\n            ptr.next = ListNode(carry)\n        }\n\n        return head.next\n    }\n}"  java="class ListNode(var `val`) {\n    var next: ListNode? = null\n}\n\nclass AddTwoNumbers {\n    public static void $1(l1: ListNode?, l2: ListNode?): ListNode? {\n        var carry = 0\n        val head = ListNode(0)\n        var ptr = head\n        var (n1, n2) = Pair(l1, l2)\n\n        while (n1!= null || n2 != null) {\n            val sum = (n1?.`val` ?: 0) + (n2?.`val` ?: 0) + carry\n            ptr.next = ListNode(sum % 10)\n            ptr = ptr.next!!\n\n            carry = if (sum > 9) 1 else 0\n\n            if (n1 != null) n1 = n1.next\n\n            if (n2 != null) n2 = n2.next\n        }\n\n        if (carry > 0) {\n            ptr.next = ListNode(carry)\n        }\n\n        return head.next\n    }\n}"  python="class ListNode(var `val`: int) {\n    var next: ListNode? = None\n}\n\nclass AddTwoNumbers {\n    def $1(l1: ListNode?, l2: ListNode?): ListNode? {\n        $1 = 0\n        $1 = ListNode(0)\n        $1 = head\n        var (n1, n2) = Pair(l1, l2)\n\n        while (n1!= None || n2 != None) {\n            $1 = (n1?.`val` ?: 0) + (n2?.`val` ?: 0) + carry\n            ptr.next = ListNode(sum % 10)\n            ptr = ptr.next!!\n\n            carry = if (sum > 9) 1 else 0\n\n            if (n1 != None) n1 = n1.next\n\n            if (n2 != None) n2 = n2.next\n        }\n\n        if (carry > 0) {\n            ptr.next = ListNode(carry)\n        }\n\n        return head.next\n    }\n}"  rust="class ListNode(var `val` -> i32) {\n    var next: ListNode? = None\n}\n\nclass AddTwoNumbers {\n    fn $1(l1: ListNode?, l2: ListNode?): ListNode? {\n        var carry = 0\n        val head = ListNode(0)\n        var ptr = head\n        var (n1, n2) = Pair(l1, l2)\n\n        while (n1!= None || n2 != None) {\n            val sum = (n1?.`val` ?: 0) + (n2?.`val` ?: 0) + carry\n            ptr.next = ListNode(sum % 10)\n            ptr = ptr.next!!\n\n            carry = if (sum > 9) 1 else 0\n\n            if (n1 != None) n1 = n1.next\n\n            if (n2 != None) n2 = n2.next\n        }\n\n        if (carry > 0) {\n            ptr.next = ListNode(carry)\n        }\n\n        return head.next\n    }\n}"  cpp="class ListNode(var `val`) {\n    var next: ListNode? = nullptr\n}\n\nclass AddTwoNumbers {\n    $1(l1: ListNode?, l2: ListNode?): ListNode? {\n        auto $1 = 0\n        auto $1 = ListNode(0)\n        auto $1 = head\n        var (n1, n2) = Pair(l1, l2)\n\n        while (n1!= nullptr || n2 != nullptr) {\n            auto $1 = (n1?.`val` ?: 0) + (n2?.`val` ?: 0) + carry\n            ptr.next = ListNode(sum % 10)\n            ptr = ptr.next!!\n\n            carry = if (sum > 9) 1 else 0\n\n            if (n1 != nullptr) n1 = n1.next\n\n            if (n2 != nullptr) n2 = n2.next\n        }\n\n        if (carry > 0) {\n            ptr.next = ListNode(carry)\n        }\n\n        return head.next\n    }\n}" %}

---

## CopyLinkedListWithRandomPointer

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/CopyLinkedListWithRandomPointer.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass CopyLinkedListWithRandomPointer {\n    /**\n     * Example:\n     * var ti = Node(5)\n     * var v = ti.`val`\n     * Definition for a Node.\n     * class Node(var `val`: Int) {\n     *     var next: Node? = null\n     *     var random: Node? = null\n     * }\n     */\n\n    class Node(var `val`: Int) {\n        var next: Node? = null\n        var random: Node? = null\n    }\n\n    class Solution {\n        fun copyRandomList(node: Node?): Node? {\n            if (node == null) return null\n\n            val nodeMap = mutableMapOf<Node, Node>()\n\n            // Step 1: Create deep copies of all nodes and store the mapping\n            var ptr = node\n            while (ptr != null) {\n                nodeMap[ptr] = Node(ptr.`val`)\n                ptr = ptr.next\n            }\n\n            // Step 2: Set the next and random pointers for the copied nodes\n            ptr = node\n            while (ptr != null) {\n                nodeMap[ptr]?.next = nodeMap[ptr.next]\n                nodeMap[ptr]?.random = nodeMap[ptr.random]\n                ptr = ptr.next\n            }\n\n            // Return the head of the new copied list\n            return nodeMap[node]\n        }\n    }\n}"  java="class CopyLinkedListWithRandomPointer {\n    /**\n     * Example:\n     * var ti = Node(5)\n     * var v = ti.`val`\n     * Definition for a Node.\n     * class Node(var `val`) {\n     *     var next: Node? = null\n     *     var random: Node? = null\n     * }\n     */\n\n    class Node(var `val`) {\n        var next: Node? = null\n        var random: Node? = null\n    }\n\n    class Solution {\n        public static void $1(node: Node?): Node? {\n            if (node == null) return null\n\n            val nodeMap = mutableMapOf<Node, Node>()\n\n            // Step 1: Create deep copies of all nodes and store the mapping\n            var ptr = node\n            while (ptr != null) {\n                nodeMap[ptr] = Node(ptr.`val`)\n                ptr = ptr.next\n            }\n\n            // Step 2: Set the next and random pointers for the copied nodes\n            ptr = node\n            while (ptr != null) {\n                nodeMap[ptr]?.next = nodeMap[ptr.next]\n                nodeMap[ptr]?.random = nodeMap[ptr.random]\n                ptr = ptr.next\n            }\n\n            // Return the head of the new copied list\n            return nodeMap[node]\n        }\n    }\n}"  python="class CopyLinkedListWithRandomPointer {\n    /**\n     * Example:\n     * $1 = Node(5)\n     * $1 = ti.`val`\n     * Definition for a Node.\n     * class Node(var `val`: int) {\n     *     var next: Node? = None\n     *     var random: Node? = None\n     * }\n     */\n\n    class Node(var `val`: int) {\n        var next: Node? = None\n        var random: Node? = None\n    }\n\n    class Solution {\n        def $1(node: Node?): Node? {\n            if (node == None) return None\n\n            $1 = mutableMapOf<Node, Node>()\n\n            // Step 1: Create deep copies of all nodes and store the mapping\n            $1 = node\n            while (ptr != None) {\n                nodeMap[ptr] = Node(ptr.`val`)\n                ptr = ptr.next\n            }\n\n            // Step 2: Set the next and random pointers for the copied nodes\n            ptr = node\n            while (ptr != None) {\n                nodeMap[ptr]?.next = nodeMap[ptr.next]\n                nodeMap[ptr]?.random = nodeMap[ptr.random]\n                ptr = ptr.next\n            }\n\n            // Return the head of the new copied list\n            return nodeMap[node]\n        }\n    }\n}"  rust="class CopyLinkedListWithRandomPointer {\n    /**\n     * Example:\n     * var ti = Node(5)\n     * var v = ti.`val`\n     * Definition for a Node.\n     * class Node(var `val` -> i32) {\n     *     var next: Node? = None\n     *     var random: Node? = None\n     * }\n     */\n\n    class Node(var `val` -> i32) {\n        var next: Node? = None\n        var random: Node? = None\n    }\n\n    class Solution {\n        fn $1(node: Node?): Node? {\n            if (node == None) return None\n\n            val nodeMap = mutableMapOf<Node, Node>()\n\n            // Step 1: Create deep copies of all nodes and store the mapping\n            var ptr = node\n            while (ptr != None) {\n                nodeMap[ptr] = Node(ptr.`val`)\n                ptr = ptr.next\n            }\n\n            // Step 2: Set the next and random pointers for the copied nodes\n            ptr = node\n            while (ptr != None) {\n                nodeMap[ptr]?.next = nodeMap[ptr.next]\n                nodeMap[ptr]?.random = nodeMap[ptr.random]\n                ptr = ptr.next\n            }\n\n            // Return the head of the new copied list\n            return nodeMap[node]\n        }\n    }\n}"  cpp="class CopyLinkedListWithRandomPointer {\n    /**\n     * Example:\n     * auto $1 = Node(5)\n     * auto $1 = ti.`val`\n     * Definition for a Node.\n     * class Node(var `val`) {\n     *     var next: Node? = nullptr\n     *     var random: Node? = nullptr\n     * }\n     */\n\n    class Node(var `val`) {\n        var next: Node? = nullptr\n        var random: Node? = nullptr\n    }\n\n    class Solution {\n        $1(node: Node?): Node? {\n            if (node == nullptr) return nullptr\n\n            auto $1 = mutableMapOf<Node, Node>()\n\n            // Step 1: Create deep copies of all nodes and store the mapping\n            auto $1 = node\n            while (ptr != nullptr) {\n                nodeMap[ptr] = Node(ptr.`val`)\n                ptr = ptr.next\n            }\n\n            // Step 2: Set the next and random pointers for the copied nodes\n            ptr = node\n            while (ptr != nullptr) {\n                nodeMap[ptr]?.next = nodeMap[ptr.next]\n                nodeMap[ptr]?.random = nodeMap[ptr.random]\n                ptr = ptr.next\n            }\n\n            // Return the head of the new copied list\n            return nodeMap[node]\n        }\n    }\n}" %}

---

## DeleteMiddleNodeOfLinkedList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/DeleteMiddleNodeOfLinkedList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass DeleteMiddleNodeOfLinkedList {\n    \n    // Two Pass Approach\n    fun deleteMiddle(head: ListNode?): ListNode? {\n        var currentNode = head\n        var prevNode: ListNode? = null\n        var index = 0\n        var n =0\n        while (currentNode != null) {\n            n++\n            currentNode = currentNode.next\n\n        }\n\n        currentNode = head\n        while (currentNode != null) {\n            if (index++ == n/2)\n                break\n            prevNode = currentNode\n            currentNode = currentNode.next\n        }\n\n        if (prevNode != null)\n            prevNode.next = currentNode\n\n        return head\n    }\n\n    fun deleteMiddleTwoPointer(head: ListNode?): ListNode? {\n        var (slow, fast, prev) = listOf(head, head, null)\n\n        if (head?.next == null)\n            return null\n\n        while (fast?.next != null ) {\n            prev = slow\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        prev?.next = slow?.next\n\n        return head\n    }\n}"  java="class DeleteMiddleNodeOfLinkedList {\n    \n    // Two Pass Approach\n    public static void $1(head: ListNode?): ListNode? {\n        var currentNode = head\n        var prevNode: ListNode? = null\n        var index = 0\n        var n =0\n        while (currentNode != null) {\n            n++\n            currentNode = currentNode.next\n\n        }\n\n        currentNode = head\n        while (currentNode != null) {\n            if (index++ == n/2)\n                break\n            prevNode = currentNode\n            currentNode = currentNode.next\n        }\n\n        if (prevNode != null)\n            prevNode.next = currentNode\n\n        return head\n    }\n\n    public static void $1(head: ListNode?): ListNode? {\n        var (slow, fast, prev) = listOf(head, head, null)\n\n        if (head?.next == null)\n            return null\n\n        while (fast?.next != null ) {\n            prev = slow\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        prev?.next = slow?.next\n\n        return head\n    }\n}"  python="class DeleteMiddleNodeOfLinkedList {\n    \n    // Two Pass Approach\n    def $1(head: ListNode?): ListNode? {\n        $1 = head\n        var prevNode: ListNode? = None\n        $1 = 0\n        $1 = 0\n        while (currentNode != None) {\n            n++\n            currentNode = currentNode.next\n\n        }\n\n        currentNode = head\n        while (currentNode != None) {\n            if (index++ == n/2)\n                break\n            prevNode = currentNode\n            currentNode = currentNode.next\n        }\n\n        if (prevNode != None)\n            prevNode.next = currentNode\n\n        return head\n    }\n\n    def $1(head: ListNode?): ListNode? {\n        var (slow, fast, prev) = listOf(head, head, None)\n\n        if (head?.next == None)\n            return None\n\n        while (fast?.next != None ) {\n            prev = slow\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        prev?.next = slow?.next\n\n        return head\n    }\n}"  rust="class DeleteMiddleNodeOfLinkedList {\n    \n    // Two Pass Approach\n    fn $1(head: ListNode?): ListNode? {\n        var currentNode = head\n        var prevNode: ListNode? = None\n        var index = 0\n        var n =0\n        while (currentNode != None) {\n            n++\n            currentNode = currentNode.next\n\n        }\n\n        currentNode = head\n        while (currentNode != None) {\n            if (index++ == n/2)\n                break\n            prevNode = currentNode\n            currentNode = currentNode.next\n        }\n\n        if (prevNode != None)\n            prevNode.next = currentNode\n\n        return head\n    }\n\n    fn $1(head: ListNode?): ListNode? {\n        var (slow, fast, prev) = listOf(head, head, None)\n\n        if (head?.next == None)\n            return None\n\n        while (fast?.next != None ) {\n            prev = slow\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        prev?.next = slow?.next\n\n        return head\n    }\n}"  cpp="class DeleteMiddleNodeOfLinkedList {\n    \n    // Two Pass Approach\n    $1(head: ListNode?): ListNode? {\n        auto $1 = head\n        var prevNode: ListNode? = nullptr\n        auto $1 = 0\n        auto $1 = 0\n        while (currentNode != nullptr) {\n            n++\n            currentNode = currentNode.next\n\n        }\n\n        currentNode = head\n        while (currentNode != nullptr) {\n            if (index++ == n/2)\n                break\n            prevNode = currentNode\n            currentNode = currentNode.next\n        }\n\n        if (prevNode != nullptr)\n            prevNode.next = currentNode\n\n        return head\n    }\n\n    $1(head: ListNode?): ListNode? {\n        var (slow, fast, prev) = listOf(head, head, nullptr)\n\n        if (head?.next == nullptr)\n            return nullptr\n\n        while (fast?.next != nullptr ) {\n            prev = slow\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        prev?.next = slow?.next\n\n        return head\n    }\n}" %}

---

## InsertIntoASortedCircularLinkedList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/InsertIntoASortedCircularLinkedList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass Node(var `val`: Int) {\n    var next: Node? = null\n}\n\nclass InsertIntoASortedCircularLinkedList {\n    fun insert(head: Node?, insertVal: Int): Node? {\n        val newNode = Node(insertVal)\n        if (head == null) return newNode.apply { next = newNode }\n\n        var current: Node? = head\n        do {\n            when {\n                current?.`val`!! <= insertVal && insertVal <= current?.next?.`val`!! -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n                current?.`val`!! > current?.next?.`val`!! && (insertVal >= current?.`val`!! || insertVal <= current?.next?.`val`!!) -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n            }\n            current = current?.next\n        } while (current != head)\n\n        newNode.next = current?.next\n        current?.next = newNode\n        return head\n    }\n}\n"  java="class Node(var `val`) {\n    var next: Node? = null\n}\n\nclass InsertIntoASortedCircularLinkedList {\n    public static void $1(head: Node?, insertVal): Node? {\n        val newNode = Node(insertVal)\n        if (head == null) return newNode.apply { next = newNode }\n\n        var current: Node? = head\n        do {\n            when {\n                current?.`val`!! <= insertVal && insertVal <= current?.next?.`val`!! -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n                current?.`val`!! > current?.next?.`val`!! && (insertVal >= current?.`val`!! || insertVal <= current?.next?.`val`!!) -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n            }\n            current = current?.next\n        } while (current != head)\n\n        newNode.next = current?.next\n        current?.next = newNode\n        return head\n    }\n}"  python="class Node(var `val`: int) {\n    var next: Node? = None\n}\n\nclass InsertIntoASortedCircularLinkedList {\n    def $1(head: Node?, insertVal: int): Node? {\n        $1 = Node(insertVal)\n        if (head == None) return newNode.apply { next = newNode }\n\n        var current: Node? = head\n        do {\n            when {\n                current?.`val`!! <= insertVal && insertVal <= current?.next?.`val`!! -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n                current?.`val`!! > current?.next?.`val`!! && (insertVal >= current?.`val`!! || insertVal <= current?.next?.`val`!!) -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n            }\n            current = current?.next\n        } while (current != head)\n\n        newNode.next = current?.next\n        current?.next = newNode\n        return head\n    }\n}"  rust="class Node(var `val` -> i32) {\n    var next: Node? = None\n}\n\nclass InsertIntoASortedCircularLinkedList {\n    fn $1(head: Node?, insertVal -> i32): Node? {\n        val newNode = Node(insertVal)\n        if (head == None) return newNode.apply { next = newNode }\n\n        var current: Node? = head\n        do {\n            when {\n                current?.`val`!! <= insertVal && insertVal <= current?.next?.`val`!! -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n                current?.`val`!! > current?.next?.`val`!! && (insertVal >= current?.`val`!! || insertVal <= current?.next?.`val`!!) -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n            }\n            current = current?.next\n        } while (current != head)\n\n        newNode.next = current?.next\n        current?.next = newNode\n        return head\n    }\n}"  cpp="class Node(var `val`) {\n    var next: Node? = nullptr\n}\n\nclass InsertIntoASortedCircularLinkedList {\n    $1(head: Node?, insertVal): Node? {\n        auto $1 = Node(insertVal)\n        if (head == nullptr) return newNode.apply { next = newNode }\n\n        var current: Node? = head\n        do {\n            when {\n                current?.`val`!! <= insertVal && insertVal <= current?.next?.`val`!! -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n                current?.`val`!! > current?.next?.`val`!! && (insertVal >= current?.`val`!! || insertVal <= current?.next?.`val`!!) -> {\n                    newNode.next = current?.next\n                    current?.next = newNode\n                    return head\n                }\n            }\n            current = current?.next\n        } while (current != head)\n\n        newNode.next = current?.next\n        current?.next = newNode\n        return head\n    }\n}" %}

---

## InsertIntoASortedCircularList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/InsertIntoASortedCircularList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass InsertIntoASortedCircularList {\n    fun insert(head: Node?, insertVal: Int): Node? {\n        val newNode = Node(insertVal)\n        if (head == null)\n            return newNode.also { it.next = it }\n        var prev = head\n        var cur = head.next\n\n        while (cur != head) {\n            if (prev!!.`val` <= insertVal && insertVal <= cur!!.`val`) {\n                break\n            }\n\n            if (prev.`val` > cur!!.`val` && (insertVal >= prev.`val` || insertVal <= cur.`val`))\n                break\n            cur = cur.next\n            prev = prev.next\n        }\n\n        prev?.next = newNode\n        newNode.next = cur\n\n        return head\n    }\n}"  java="class InsertIntoASortedCircularList {\n    public static void $1(head: Node?, insertVal): Node? {\n        val newNode = Node(insertVal)\n        if (head == null)\n            return newNode.also { it.next = it }\n        var prev = head\n        var cur = head.next\n\n        while (cur != head) {\n            if (prev!!.`val` <= insertVal && insertVal <= cur!!.`val`) {\n                break\n            }\n\n            if (prev.`val` > cur!!.`val` && (insertVal >= prev.`val` || insertVal <= cur.`val`))\n                break\n            cur = cur.next\n            prev = prev.next\n        }\n\n        prev?.next = newNode\n        newNode.next = cur\n\n        return head\n    }\n}"  python="class InsertIntoASortedCircularList {\n    def $1(head: Node?, insertVal: int): Node? {\n        $1 = Node(insertVal)\n        if (head == None)\n            return newNode.also { it.next = it }\n        $1 = head\n        $1 = head.next\n\n        while (cur != head) {\n            if (prev!!.`val` <= insertVal && insertVal <= cur!!.`val`) {\n                break\n            }\n\n            if (prev.`val` > cur!!.`val` && (insertVal >= prev.`val` || insertVal <= cur.`val`))\n                break\n            cur = cur.next\n            prev = prev.next\n        }\n\n        prev?.next = newNode\n        newNode.next = cur\n\n        return head\n    }\n}"  rust="class InsertIntoASortedCircularList {\n    fn $1(head: Node?, insertVal -> i32): Node? {\n        val newNode = Node(insertVal)\n        if (head == None)\n            return newNode.also { it.next = it }\n        var prev = head\n        var cur = head.next\n\n        while (cur != head) {\n            if (prev!!.`val` <= insertVal && insertVal <= cur!!.`val`) {\n                break\n            }\n\n            if (prev.`val` > cur!!.`val` && (insertVal >= prev.`val` || insertVal <= cur.`val`))\n                break\n            cur = cur.next\n            prev = prev.next\n        }\n\n        prev?.next = newNode\n        newNode.next = cur\n\n        return head\n    }\n}"  cpp="class InsertIntoASortedCircularList {\n    $1(head: Node?, insertVal): Node? {\n        auto $1 = Node(insertVal)\n        if (head == nullptr)\n            return newNode.also { it.next = it }\n        auto $1 = head\n        auto $1 = head.next\n\n        while (cur != head) {\n            if (prev!!.`val` <= insertVal && insertVal <= cur!!.`val`) {\n                break\n            }\n\n            if (prev.`val` > cur!!.`val` && (insertVal >= prev.`val` || insertVal <= cur.`val`))\n                break\n            cur = cur.next\n            prev = prev.next\n        }\n\n        prev?.next = newNode\n        newNode.next = cur\n\n        return head\n    }\n}" %}

---

## IntersectionOfTwoLinkedList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/IntersectionOfTwoLinkedList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass IntersectionOfTwoLinkedList {\n    fun getIntersectionNode(headA: ListNode?, headB: ListNode?): ListNode? {\n        if (headA == null || headB == null) return null\n\n        var pA = headA\n        var pB = headB\n\n        // Traverse both lists. When one pointer reaches the end, redirect it to the head of the other list.\n        while (pA != pB) {\n            pA = if (pA == null) headB else pA.next\n            pB = if (pB == null) headA else pB.next\n        }\n\n        return pA // This will return the intersection node, or null if no intersection.\n    }\n}\n"  java="class IntersectionOfTwoLinkedList {\n    public static void $1(headA: ListNode?, headB: ListNode?): ListNode? {\n        if (headA == null || headB == null) return null\n\n        var pA = headA\n        var pB = headB\n\n        // Traverse both lists. When one pointer reaches the end, redirect it to the head of the other list.\n        while (pA != pB) {\n            pA = if (pA == null) headB else pA.next\n            pB = if (pB == null) headA else pB.next\n        }\n\n        return pA // This will return the intersection node, or null if no intersection.\n    }\n}"  python="class IntersectionOfTwoLinkedList {\n    def $1(headA: ListNode?, headB: ListNode?): ListNode? {\n        if (headA == None || headB == None) return None\n\n        $1 = headA\n        $1 = headB\n\n        // Traverse both lists. When one pointer reaches the end, redirect it to the head of the other list.\n        while (pA != pB) {\n            pA = if (pA == None) headB else pA.next\n            pB = if (pB == None) headA else pB.next\n        }\n\n        return pA // This will return the intersection node, or None if no intersection.\n    }\n}"  rust="class IntersectionOfTwoLinkedList {\n    fn $1(headA: ListNode?, headB: ListNode?): ListNode? {\n        if (headA == None || headB == None) return None\n\n        var pA = headA\n        var pB = headB\n\n        // Traverse both lists. When one pointer reaches the end, redirect it to the head of the other list.\n        while (pA != pB) {\n            pA = if (pA == None) headB else pA.next\n            pB = if (pB == None) headA else pB.next\n        }\n\n        return pA // This will return the intersection node, or None if no intersection.\n    }\n}"  cpp="class IntersectionOfTwoLinkedList {\n    $1(headA: ListNode?, headB: ListNode?): ListNode? {\n        if (headA == nullptr || headB == nullptr) return nullptr\n\n        auto $1 = headA\n        auto $1 = headB\n\n        // Traverse both lists. When one pointer reaches the end, redirect it to the head of the other list.\n        while (pA != pB) {\n            pA = if (pA == nullptr) headB else pA.next\n            pB = if (pB == nullptr) headA else pB.next\n        }\n\n        return pA // This will return the intersection node, or nullptr if no intersection.\n    }\n}" %}

---

## LinkedListCycle

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/LinkedListCycle.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass LinkedListCycle {\n    fun hasCycle(head: ListNode?): Boolean {\n        if (head == null || head.next == null) return false\n\n        var slow = head\n        var fast = head.next\n\n        while (slow != fast ) {\n            if (fast == null || fast.next == null)\n                return false\n\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        return true\n    }\n\n    // Better version\n    fun hasCycle2(head: ListNode?): Boolean {\n        var slow = head\n        var fast = head\n\n        while (fast != null && fast.next != null) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {\n                return true\n            }\n        }\n\n        return false\n    }\n}"  java="class LinkedListCycle {\n    public static void $1(head: ListNode?) {\n        if (head == null || head.next == null) return false\n\n        var slow = head\n        var fast = head.next\n\n        while (slow != fast ) {\n            if (fast == null || fast.next == null)\n                return false\n\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        return true\n    }\n\n    // Better version\n    public static void $1(head: ListNode?) {\n        var slow = head\n        var fast = head\n\n        while (fast != null && fast.next != null) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {\n                return true\n            }\n        }\n\n        return false\n    }\n}"  python="class LinkedListCycle {\n    def $1(head: ListNode?): bool {\n        if (head == None || head.next == None) return False\n\n        $1 = head\n        $1 = head.next\n\n        while (slow != fast ) {\n            if (fast == None || fast.next == None)\n                return False\n\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        return True\n    }\n\n    // Better version\n    def $1(head: ListNode?): bool {\n        $1 = head\n        $1 = head\n\n        while (fast != None && fast.next != None) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {\n                return True\n            }\n        }\n\n        return False\n    }\n}"  rust="class LinkedListCycle {\n    fn $1(head: ListNode?) -> bool {\n        if (head == None || head.next == None) return false\n\n        var slow = head\n        var fast = head.next\n\n        while (slow != fast ) {\n            if (fast == None || fast.next == None)\n                return false\n\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        return true\n    }\n\n    // Better version\n    fn $1(head: ListNode?) -> bool {\n        var slow = head\n        var fast = head\n\n        while (fast != None && fast.next != None) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {\n                return true\n            }\n        }\n\n        return false\n    }\n}"  cpp="class LinkedListCycle {\n    $1(head: ListNode?) {\n        if (head == nullptr || head.next == nullptr) return false\n\n        auto $1 = head\n        auto $1 = head.next\n\n        while (slow != fast ) {\n            if (fast == nullptr || fast.next == nullptr)\n                return false\n\n            slow = slow?.next\n            fast = fast?.next?.next\n        }\n\n        return true\n    }\n\n    // Better version\n    $1(head: ListNode?) {\n        auto $1 = head\n        auto $1 = head\n\n        while (fast != nullptr && fast.next != nullptr) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {\n                return true\n            }\n        }\n\n        return false\n    }\n}" %}

---

## LinkedListCycle_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/LinkedListCycle_II.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass LinkedListCycle_II {\n    fun detectCycle(head: ListNode?): ListNode? {\n        var slow = head\n        var fast = head\n\n        // Phase 1: Detect if a cycle exists\n        while (fast != null && fast.next != null) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {  // Cycle detected\n                // Phase 2: Find the start of the cycle\n                var entry = head\n                while (entry != slow) {\n                    entry = entry?.next\n                    slow = slow?.next\n                }\n                return entry\n            }\n        }\n\n        return null  // No cycle\n    }\n}\n"  java="class LinkedListCycle_II {\n    public static void $1(head: ListNode?): ListNode? {\n        var slow = head\n        var fast = head\n\n        // Phase 1: Detect if a cycle exists\n        while (fast != null && fast.next != null) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {  // Cycle detected\n                // Phase 2: Find the start of the cycle\n                var entry = head\n                while (entry != slow) {\n                    entry = entry?.next\n                    slow = slow?.next\n                }\n                return entry\n            }\n        }\n\n        return null  // No cycle\n    }\n}"  python="class LinkedListCycle_II {\n    def $1(head: ListNode?): ListNode? {\n        $1 = head\n        $1 = head\n\n        // Phase 1: Detect if a cycle exists\n        while (fast != None && fast.next != None) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {  // Cycle detected\n                // Phase 2: Find the start of the cycle\n                $1 = head\n                while (entry != slow) {\n                    entry = entry?.next\n                    slow = slow?.next\n                }\n                return entry\n            }\n        }\n\n        return None  // No cycle\n    }\n}"  rust="class LinkedListCycle_II {\n    fn $1(head: ListNode?): ListNode? {\n        var slow = head\n        var fast = head\n\n        // Phase 1: Detect if a cycle exists\n        while (fast != None && fast.next != None) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {  // Cycle detected\n                // Phase 2: Find the start of the cycle\n                var entry = head\n                while (entry != slow) {\n                    entry = entry?.next\n                    slow = slow?.next\n                }\n                return entry\n            }\n        }\n\n        return None  // No cycle\n    }\n}"  cpp="class LinkedListCycle_II {\n    $1(head: ListNode?): ListNode? {\n        auto $1 = head\n        auto $1 = head\n\n        // Phase 1: Detect if a cycle exists\n        while (fast != nullptr && fast.next != nullptr) {\n            slow = slow?.next\n            fast = fast.next?.next\n\n            if (slow == fast) {  // Cycle detected\n                // Phase 2: Find the start of the cycle\n                auto $1 = head\n                while (entry != slow) {\n                    entry = entry?.next\n                    slow = slow?.next\n                }\n                return entry\n            }\n        }\n\n        return nullptr  // No cycle\n    }\n}" %}

---

## MergeKSortedList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/MergeKSortedList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass MergeKSortedList {\n    fun mergeTwoLists(list1: ListNode?, list2: ListNode?): ListNode? {\n        val head = ListNode(0) // dummy node\n        var ptr = head\n\n        var ptr1 = list1\n        var ptr2 = list2\n\n        while (ptr1 != null && ptr2 != null) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n\n    fun mergeKLists(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty())\n            return null\n        fun merge(start: Int, end: Int): ListNode? {\n            if (start == end)\n                return lists[start]\n            val mid = start + (end - start) / 2\n            val left = merge(start, mid)\n            val right = merge(mid + 1, end)\n\n            return mergeTwoLists(left, right)\n        }\n        return merge(0, lists.lastIndex)\n    }\n}"  java="class MergeKSortedList {\n    public static void $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        val head = ListNode(0) // dummy node\n        var ptr = head\n\n        var ptr1 = list1\n        var ptr2 = list2\n\n        while (ptr1 != null && ptr2 != null) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n\n    public static void $1(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty())\n            return null\n        public static void $1(start, end): ListNode? {\n            if (start == end)\n                return lists[start]\n            val mid = start + (end - start) / 2\n            val left = merge(start, mid)\n            val right = merge(mid + 1, end)\n\n            return mergeTwoLists(left, right)\n        }\n        return merge(0, lists.lastIndex)\n    }\n}"  python="class MergeKSortedList {\n    def $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        $1 = ListNode(0) // dummy node\n        $1 = head\n\n        $1 = list1\n        $1 = list2\n\n        while (ptr1 != None && ptr2 != None) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n\n    def $1(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty())\n            return None\n        def $1(start: int, end: int): ListNode? {\n            if (start == end)\n                return lists[start]\n            $1 = start + (end - start) / 2\n            $1 = merge(start, mid)\n            $1 = merge(mid + 1, end)\n\n            return mergeTwoLists(left, right)\n        }\n        return merge(0, lists.lastIndex)\n    }\n}"  rust="class MergeKSortedList {\n    fn $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        val head = ListNode(0) // dummy node\n        var ptr = head\n\n        var ptr1 = list1\n        var ptr2 = list2\n\n        while (ptr1 != None && ptr2 != None) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n\n    fn $1(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty())\n            return None\n        fn $1(start -> i32, end -> i32): ListNode? {\n            if (start == end)\n                return lists[start]\n            val mid = start + (end - start) / 2\n            val left = merge(start, mid)\n            val right = merge(mid + 1, end)\n\n            return mergeTwoLists(left, right)\n        }\n        return merge(0, lists.len() - 1)\n    }\n}"  cpp="class MergeKSortedList {\n    $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        auto $1 = ListNode(0) // dummy node\n        auto $1 = head\n\n        auto $1 = list1\n        auto $1 = list2\n\n        while (ptr1 != nullptr && ptr2 != nullptr) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n\n    $1(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty())\n            return nullptr\n        $1(start, end): ListNode? {\n            if (start == end)\n                return lists[start]\n            auto $1 = start + (end - start) / 2\n            auto $1 = merge(start, mid)\n            auto $1 = merge(mid + 1, end)\n\n            return mergeTwoLists(left, right)\n        }\n        return merge(0, lists.size() - 1)\n    }\n}" %}

---

## MergeKSortedListIterative

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/MergeKSortedListIterative.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass MergeKSortedListIterative {\n    fun mergeKLists(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty()) return null\n\n        var interval = 1\n        val n = lists.size\n\n        while (interval < n) {\n            for (i in 0 until n - interval step interval * 2) {\n                lists[i] = mergeTwoLists(lists[i], lists[i + interval])\n            }\n            interval *= 2\n        }\n\n        return lists[0]\n    }\n\n    fun mergeTwoLists(list1: ListNode?, list2: ListNode?): ListNode? {\n        val head = ListNode(0) // Dummy node\n        var ptr = head\n\n        var ptr1 = list1\n        var ptr2 = list2\n\n        while (ptr1 != null && ptr2 != null) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ptr1\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ptr2\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}"  java="class MergeKSortedListIterative {\n    public static void $1(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty()) return null\n\n        var interval = 1\n        val n = lists.size\n\n        while (interval < n) {\n            for (i in 0 until n - interval step interval * 2) {\n                lists[i] = mergeTwoLists(lists[i], lists[i + interval])\n            }\n            interval *= 2\n        }\n\n        return lists[0]\n    }\n\n    public static void $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        val head = ListNode(0) // Dummy node\n        var ptr = head\n\n        var ptr1 = list1\n        var ptr2 = list2\n\n        while (ptr1 != null && ptr2 != null) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ptr1\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ptr2\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}"  python="class MergeKSortedListIterative {\n    def $1(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty()) return None\n\n        $1 = 1\n        $1 = lists.size\n\n        while (interval < n) {\n            for (i in 0 until n - interval step interval * 2) {\n                lists[i] = mergeTwoLists(lists[i], lists[i + interval])\n            }\n            interval *= 2\n        }\n\n        return lists[0]\n    }\n\n    def $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        $1 = ListNode(0) // Dummy node\n        $1 = head\n\n        $1 = list1\n        $1 = list2\n\n        while (ptr1 != None && ptr2 != None) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ptr1\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ptr2\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}"  rust="class MergeKSortedListIterative {\n    fn $1(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty()) return None\n\n        var interval = 1\n        val n = lists.size\n\n        while (interval < n) {\n            for (i in 0 until n - interval step interval * 2) {\n                lists[i] = mergeTwoLists(lists[i], lists[i + interval])\n            }\n            interval *= 2\n        }\n\n        return lists[0]\n    }\n\n    fn $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        val head = ListNode(0) // Dummy node\n        var ptr = head\n\n        var ptr1 = list1\n        var ptr2 = list2\n\n        while (ptr1 != None && ptr2 != None) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ptr1\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ptr2\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}"  cpp="class MergeKSortedListIterative {\n    $1(lists: Array<ListNode?>): ListNode? {\n        if (lists.isEmpty()) return nullptr\n\n        auto $1 = 1\n        auto $1 = lists.size\n\n        while (interval < n) {\n            for (i in 0 until n - interval step interval * 2) {\n                lists[i] = mergeTwoLists(lists[i], lists[i + interval])\n            }\n            interval *= 2\n        }\n\n        return lists[0]\n    }\n\n    $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        auto $1 = ListNode(0) // Dummy node\n        auto $1 = head\n\n        auto $1 = list1\n        auto $1 = list2\n\n        while (ptr1 != nullptr && ptr2 != nullptr) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ptr1\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ptr2\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}" %}

---

## MergeTwoSortedLIst

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/MergeTwoSortedLIst.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass MergeTwoSortedLIst {\n    fun mergeTwoLists(list1: ListNode?, list2: ListNode?): ListNode? {\n        val head = ListNode(0) // dummy node\n        var ptr = head\n\n        var ptr1 = list1\n        var ptr2 = list2\n\n        while (ptr1 != null && ptr2 != null) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}"  java="class MergeTwoSortedLIst {\n    public static void $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        val head = ListNode(0) // dummy node\n        var ptr = head\n\n        var ptr1 = list1\n        var ptr2 = list2\n\n        while (ptr1 != null && ptr2 != null) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}"  python="class MergeTwoSortedLIst {\n    def $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        $1 = ListNode(0) // dummy node\n        $1 = head\n\n        $1 = list1\n        $1 = list2\n\n        while (ptr1 != None && ptr2 != None) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}"  rust="class MergeTwoSortedLIst {\n    fn $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        val head = ListNode(0) // dummy node\n        var ptr = head\n\n        var ptr1 = list1\n        var ptr2 = list2\n\n        while (ptr1 != None && ptr2 != None) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}"  cpp="class MergeTwoSortedLIst {\n    $1(list1: ListNode?, list2: ListNode?): ListNode? {\n        auto $1 = ListNode(0) // dummy node\n        auto $1 = head\n\n        auto $1 = list1\n        auto $1 = list2\n\n        while (ptr1 != nullptr && ptr2 != nullptr) {\n            if (ptr1.`val` < ptr2.`val`) {\n                ptr.next = ListNode(ptr1.`val`)\n                ptr1 = ptr1.next\n            } else {\n                ptr.next = ListNode(ptr2.`val`)\n                ptr2 = ptr2.next\n            }\n            ptr = ptr.next!!\n        }\n\n        ptr.next = ptr1 ?: ptr2\n\n        return head.next\n    }\n}" %}

---

## OddEvenLinkedList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/OddEvenLinkedList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass OddEvenLinkedList {\n    fun oddEvenList(head: ListNode?): ListNode? {\n        var odd = head\n        var even = head?.next\n        var evenHead = even\n\n        while (even?.next != null) {\n\n            odd?.next = odd?.next?.next\n            odd = odd?.next\n\n            even.next = even.next?.next\n            even = even.next\n        }\n\n        odd?.next = evenHead\n        return head\n    }\n\n}"  java="class OddEvenLinkedList {\n    public static void $1(head: ListNode?): ListNode? {\n        var odd = head\n        var even = head?.next\n        var evenHead = even\n\n        while (even?.next != null) {\n\n            odd?.next = odd?.next?.next\n            odd = odd?.next\n\n            even.next = even.next?.next\n            even = even.next\n        }\n\n        odd?.next = evenHead\n        return head\n    }\n\n}"  python="class OddEvenLinkedList {\n    def $1(head: ListNode?): ListNode? {\n        $1 = head\n        $1 = head?.next\n        $1 = even\n\n        while (even?.next != None) {\n\n            odd?.next = odd?.next?.next\n            odd = odd?.next\n\n            even.next = even.next?.next\n            even = even.next\n        }\n\n        odd?.next = evenHead\n        return head\n    }\n\n}"  rust="class OddEvenLinkedList {\n    fn $1(head: ListNode?): ListNode? {\n        var odd = head\n        var even = head?.next\n        var evenHead = even\n\n        while (even?.next != None) {\n\n            odd?.next = odd?.next?.next\n            odd = odd?.next\n\n            even.next = even.next?.next\n            even = even.next\n        }\n\n        odd?.next = evenHead\n        return head\n    }\n\n}"  cpp="class OddEvenLinkedList {\n    $1(head: ListNode?): ListNode? {\n        auto $1 = head\n        auto $1 = head?.next\n        auto $1 = even\n\n        while (even?.next != nullptr) {\n\n            odd?.next = odd?.next?.next\n            odd = odd?.next\n\n            even.next = even.next?.next\n            even = even.next\n        }\n\n        odd?.next = evenHead\n        return head\n    }\n\n}" %}

---

## OddOrEvenLinkedList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/OddOrEvenLinkedList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass OddOrEvenLinkedList {\n\n}"  java="class OddOrEvenLinkedList {\n\n}"  python="class OddOrEvenLinkedList {\n\n}"  rust="class OddOrEvenLinkedList {\n\n}"  cpp="class OddOrEvenLinkedList {\n\n}" %}

---

## PalindromeLinkedList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/PalindromeLinkedList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass PalindromeLinkedList {\n    class Solution {\n        fun isPalindrome(head: ListNode?): Boolean {\n            if (head?.next == null) return true // Edge case: empty or single node\n\n            var slow = head\n            var fast = head\n\n            // Step 1: Find the middle\n            while (fast?.next != null && fast.next?.next != null) {\n                slow = slow?.next\n                fast = fast.next?.next\n            }\n\n            // Step 2: Reverse second half\n            var secondHalf = reverseList(slow?.next)\n            var firstHalf = head\n\n            // Step 3: Compare both halves\n            var temp = secondHalf\n            while (temp != null) {\n                if (firstHalf?.`val` != temp.`val`) return false\n                firstHalf = firstHalf?.next\n                temp = temp.next\n            }\n\n            // Step 4: Restore original list (optional)\n            slow?.next = reverseList(secondHalf)\n\n            return true\n        }\n\n        private fun reverseList(head: ListNode?): ListNode? {\n            var prev: ListNode? = null\n            var curr = head\n            while (curr != null) {\n                val next = curr.next\n                curr.next = prev\n                prev = curr\n                curr = next\n            }\n            return prev\n        }\n    }\n}\n"  java="class PalindromeLinkedList {\n    class Solution {\n        public static void $1(head: ListNode?) {\n            if (head?.next == null) return true // Edge case: empty or single node\n\n            var slow = head\n            var fast = head\n\n            // Step 1: Find the middle\n            while (fast?.next != null && fast.next?.next != null) {\n                slow = slow?.next\n                fast = fast.next?.next\n            }\n\n            // Step 2: Reverse second half\n            var secondHalf = reverseList(slow?.next)\n            var firstHalf = head\n\n            // Step 3: Compare both halves\n            var temp = secondHalf\n            while (temp != null) {\n                if (firstHalf?.`val` != temp.`val`) return false\n                firstHalf = firstHalf?.next\n                temp = temp.next\n            }\n\n            // Step 4: Restore original list (optional)\n            slow?.next = reverseList(secondHalf)\n\n            return true\n        }\n\n        private public static void $1(head: ListNode?): ListNode? {\n            var prev: ListNode? = null\n            var curr = head\n            while (curr != null) {\n                val next = curr.next\n                curr.next = prev\n                prev = curr\n                curr = next\n            }\n            return prev\n        }\n    }\n}"  python="class PalindromeLinkedList {\n    class Solution {\n        def $1(head: ListNode?): bool {\n            if (head?.next == None) return True // Edge case: empty or single node\n\n            $1 = head\n            $1 = head\n\n            // Step 1: Find the middle\n            while (fast?.next != None && fast.next?.next != None) {\n                slow = slow?.next\n                fast = fast.next?.next\n            }\n\n            // Step 2: Reverse second half\n            $1 = reverseList(slow?.next)\n            $1 = head\n\n            // Step 3: Compare both halves\n            $1 = secondHalf\n            while (temp != None) {\n                if (firstHalf?.`val` != temp.`val`) return False\n                firstHalf = firstHalf?.next\n                temp = temp.next\n            }\n\n            // Step 4: Restore original list (optional)\n            slow?.next = reverseList(secondHalf)\n\n            return True\n        }\n\n        private def $1(head: ListNode?): ListNode? {\n            var prev: ListNode? = None\n            $1 = head\n            while (curr != None) {\n                $1 = curr.next\n                curr.next = prev\n                prev = curr\n                curr = next\n            }\n            return prev\n        }\n    }\n}"  rust="class PalindromeLinkedList {\n    class Solution {\n        fn $1(head: ListNode?) -> bool {\n            if (head?.next == None) return true // Edge case: empty or single node\n\n            var slow = head\n            var fast = head\n\n            // Step 1: Find the middle\n            while (fast?.next != None && fast.next?.next != None) {\n                slow = slow?.next\n                fast = fast.next?.next\n            }\n\n            // Step 2: Reverse second half\n            var secondHalf = reverseList(slow?.next)\n            var firstHalf = head\n\n            // Step 3: Compare both halves\n            var temp = secondHalf\n            while (temp != None) {\n                if (firstHalf?.`val` != temp.`val`) return false\n                firstHalf = firstHalf?.next\n                temp = temp.next\n            }\n\n            // Step 4: Restore original list (optional)\n            slow?.next = reverseList(secondHalf)\n\n            return true\n        }\n\n        private fn $1(head: ListNode?): ListNode? {\n            var prev: ListNode? = None\n            var curr = head\n            while (curr != None) {\n                val next = curr.next\n                curr.next = prev\n                prev = curr\n                curr = next\n            }\n            return prev\n        }\n    }\n}"  cpp="class PalindromeLinkedList {\n    class Solution {\n        $1(head: ListNode?) {\n            if (head?.next == nullptr) return true // Edge case: empty or single node\n\n            auto $1 = head\n            auto $1 = head\n\n            // Step 1: Find the middle\n            while (fast?.next != nullptr && fast.next?.next != nullptr) {\n                slow = slow?.next\n                fast = fast.next?.next\n            }\n\n            // Step 2: Reverse second half\n            auto $1 = reverseList(slow?.next)\n            auto $1 = head\n\n            // Step 3: Compare both halves\n            auto $1 = secondHalf\n            while (temp != nullptr) {\n                if (firstHalf?.`val` != temp.`val`) return false\n                firstHalf = firstHalf?.next\n                temp = temp.next\n            }\n\n            // Step 4: Restore original list (optional)\n            slow?.next = reverseList(secondHalf)\n\n            return true\n        }\n\n        private $1(head: ListNode?): ListNode? {\n            var prev: ListNode? = nullptr\n            auto $1 = head\n            while (curr != nullptr) {\n                auto $1 = curr.next\n                curr.next = prev\n                prev = curr\n                curr = next\n            }\n            return prev\n        }\n    }\n}" %}

---

## RemoveNthNodeFromEndOfList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/RemoveNthNodeFromEndOfList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass RemoveNthNodeFromEndOfList {\n    fun removeNthFromEnd(head: ListNode?, n: Int): ListNode? {\n        var size = 0\n        var ptr = head\n\n        while(ptr != null) {\n            size++\n            ptr = ptr.next\n        }\n\n        val indexToRemove = (size - n)\n        return removeNthFromFront(head, indexToRemove, size)\n    }\n\n    private fun removeNthFromFront(head: ListNode?, indexToRemove: Int, size: Int): ListNode? {\n        var ptr = head\n        var prev: ListNode? = null\n        for(i in 0 until indexToRemove) {\n            prev = ptr\n            ptr = ptr?.next\n        }\n\n        return when(indexToRemove) {\n            0 -> head?.next\n            else -> {\n                prev?.next = ptr?.next\n                head\n            }\n        }\n    }\n\n    /**\n     * Two Pointer approach\n     */\n    fun removeNthFromEndTwoPointer(head: ListNode?, n: Int): ListNode? {\n        val dummy = ListNode(0).apply { next = head }\n        var first: ListNode? = dummy\n        var second: ListNode? = dummy\n\n        // Move first n + 1 steps ahead\n        for (i in 0..n) {\n            first = first?.next\n        }\n\n        // Move first to the end, maintaining the gap of size n\n        while (first != null) {\n            first = first.next\n            second = second?.next\n        }\n\n        // Remove the nth node from the end\n        second?.next = second?.next?.next\n\n        return dummy.next\n    }\n}"  java="class RemoveNthNodeFromEndOfList {\n    public static void $1(head: ListNode?, n): ListNode? {\n        var size = 0\n        var ptr = head\n\n        while(ptr != null) {\n            size++\n            ptr = ptr.next\n        }\n\n        val indexToRemove = (size - n)\n        return removeNthFromFront(head, indexToRemove, size)\n    }\n\n    private public static void $1(head: ListNode?, indexToRemove, size): ListNode? {\n        var ptr = head\n        var prev: ListNode? = null\n        for(i in 0 until indexToRemove) {\n            prev = ptr\n            ptr = ptr?.next\n        }\n\n        return when(indexToRemove) {\n            0 -> head?.next\n            else -> {\n                prev?.next = ptr?.next\n                head\n            }\n        }\n    }\n\n    /**\n     * Two Pointer approach\n     */\n    public static void $1(head: ListNode?, n): ListNode? {\n        val dummy = ListNode(0).apply { next = head }\n        var first: ListNode? = dummy\n        var second: ListNode? = dummy\n\n        // Move first n + 1 steps ahead\n        for (i in 0..n) {\n            first = first?.next\n        }\n\n        // Move first to the end, maintaining the gap of size n\n        while (first != null) {\n            first = first.next\n            second = second?.next\n        }\n\n        // Remove the nth node from the end\n        second?.next = second?.next?.next\n\n        return dummy.next\n    }\n}"  python="class RemoveNthNodeFromEndOfList {\n    def $1(head: ListNode?, n: int): ListNode? {\n        $1 = 0\n        $1 = head\n\n        while(ptr != None) {\n            size++\n            ptr = ptr.next\n        }\n\n        $1 = (size - n)\n        return removeNthFromFront(head, indexToRemove, size)\n    }\n\n    private def $1(head: ListNode?, indexToRemove: int, size: int): ListNode? {\n        $1 = head\n        var prev: ListNode? = None\n        for(i in 0 until indexToRemove) {\n            prev = ptr\n            ptr = ptr?.next\n        }\n\n        return when(indexToRemove) {\n            0 -> head?.next\n            else -> {\n                prev?.next = ptr?.next\n                head\n            }\n        }\n    }\n\n    /**\n     * Two Pointer approach\n     */\n    def $1(head: ListNode?, n: int): ListNode? {\n        $1 = ListNode(0).apply { next = head }\n        var first: ListNode? = dummy\n        var second: ListNode? = dummy\n\n        // Move first n + 1 steps ahead\n        for (i in 0..n) {\n            first = first?.next\n        }\n\n        // Move first to the end, maintaining the gap of size n\n        while (first != None) {\n            first = first.next\n            second = second?.next\n        }\n\n        // Remove the nth node from the end\n        second?.next = second?.next?.next\n\n        return dummy.next\n    }\n}"  rust="class RemoveNthNodeFromEndOfList {\n    fn $1(head: ListNode?, n -> i32): ListNode? {\n        var size = 0\n        var ptr = head\n\n        while(ptr != None) {\n            size++\n            ptr = ptr.next\n        }\n\n        val indexToRemove = (size - n)\n        return removeNthFromFront(head, indexToRemove, size)\n    }\n\n    private fn $1(head: ListNode?, indexToRemove -> i32, size -> i32): ListNode? {\n        var ptr = head\n        var prev: ListNode? = None\n        for(i in 0 until indexToRemove) {\n            prev = ptr\n            ptr = ptr?.next\n        }\n\n        return when(indexToRemove) {\n            0 -> head?.next\n            else -> {\n                prev?.next = ptr?.next\n                head\n            }\n        }\n    }\n\n    /**\n     * Two Pointer approach\n     */\n    fn $1(head: ListNode?, n -> i32): ListNode? {\n        val dummy = ListNode(0).apply { next = head }\n        var first: ListNode? = dummy\n        var second: ListNode? = dummy\n\n        // Move first n + 1 steps ahead\n        for (i in 0..n) {\n            first = first?.next\n        }\n\n        // Move first to the end, maintaining the gap of size n\n        while (first != None) {\n            first = first.next\n            second = second?.next\n        }\n\n        // Remove the nth node from the end\n        second?.next = second?.next?.next\n\n        return dummy.next\n    }\n}"  cpp="class RemoveNthNodeFromEndOfList {\n    $1(head: ListNode?, n): ListNode? {\n        auto $1 = 0\n        auto $1 = head\n\n        while(ptr != nullptr) {\n            size++\n            ptr = ptr.next\n        }\n\n        auto $1 = (size - n)\n        return removeNthFromFront(head, indexToRemove, size)\n    }\n\n    private $1(head: ListNode?, indexToRemove, size): ListNode? {\n        auto $1 = head\n        var prev: ListNode? = nullptr\n        for(i in 0 until indexToRemove) {\n            prev = ptr\n            ptr = ptr?.next\n        }\n\n        return when(indexToRemove) {\n            0 -> head?.next\n            else -> {\n                prev?.next = ptr?.next\n                head\n            }\n        }\n    }\n\n    /**\n     * Two Pointer approach\n     */\n    $1(head: ListNode?, n): ListNode? {\n        auto $1 = ListNode(0).apply { next = head }\n        var first: ListNode? = dummy\n        var second: ListNode? = dummy\n\n        // Move first n + 1 steps ahead\n        for (i in 0..n) {\n            first = first?.next\n        }\n\n        // Move first to the end, maintaining the gap of size n\n        while (first != nullptr) {\n            first = first.next\n            second = second?.next\n        }\n\n        // Remove the nth node from the end\n        second?.next = second?.next?.next\n\n        return dummy.next\n    }\n}" %}

---

## ReverseLinkedList

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/ReverseLinkedList.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass ReverseLinkedList {\n    fun reverseList(head: ListNode?): ListNode? {\n        if (head?.next == null) {\n            return head\n        }\n\n        val reversedHead = reverseList(head.next)\n\n        head.next?.next = head\n        head.next = null\n\n        return reversedHead\n    }\n}"  java="class ReverseLinkedList {\n    public static void $1(head: ListNode?): ListNode? {\n        if (head?.next == null) {\n            return head\n        }\n\n        val reversedHead = reverseList(head.next)\n\n        head.next?.next = head\n        head.next = null\n\n        return reversedHead\n    }\n}"  python="class ReverseLinkedList {\n    def $1(head: ListNode?): ListNode? {\n        if (head?.next == None) {\n            return head\n        }\n\n        $1 = reverseList(head.next)\n\n        head.next?.next = head\n        head.next = None\n\n        return reversedHead\n    }\n}"  rust="class ReverseLinkedList {\n    fn $1(head: ListNode?): ListNode? {\n        if (head?.next == None) {\n            return head\n        }\n\n        val reversedHead = reverseList(head.next)\n\n        head.next?.next = head\n        head.next = None\n\n        return reversedHead\n    }\n}"  cpp="class ReverseLinkedList {\n    $1(head: ListNode?): ListNode? {\n        if (head?.next == nullptr) {\n            return head\n        }\n\n        auto $1 = reverseList(head.next)\n\n        head.next?.next = head\n        head.next = nullptr\n\n        return reversedHead\n    }\n}" %}

---

## ReverseLinkedListIterative

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/ReverseLinkedListIterative.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass ReverseLinkedListIterative {\n    fun reverseList(head: ListNode?): ListNode? {\n        var prev: ListNode? = null\n        var current = head\n        while (current != null) {\n            val nextTemp = current.next\n            current.next = prev\n            prev = current\n            current = nextTemp\n        }\n        return prev\n    }\n}\n"  java="class ReverseLinkedListIterative {\n    public static void $1(head: ListNode?): ListNode? {\n        var prev: ListNode? = null\n        var current = head\n        while (current != null) {\n            val nextTemp = current.next\n            current.next = prev\n            prev = current\n            current = nextTemp\n        }\n        return prev\n    }\n}"  python="class ReverseLinkedListIterative {\n    def $1(head: ListNode?): ListNode? {\n        var prev: ListNode? = None\n        $1 = head\n        while (current != None) {\n            $1 = current.next\n            current.next = prev\n            prev = current\n            current = nextTemp\n        }\n        return prev\n    }\n}"  rust="class ReverseLinkedListIterative {\n    fn $1(head: ListNode?): ListNode? {\n        var prev: ListNode? = None\n        var current = head\n        while (current != None) {\n            val nextTemp = current.next\n            current.next = prev\n            prev = current\n            current = nextTemp\n        }\n        return prev\n    }\n}"  cpp="class ReverseLinkedListIterative {\n    $1(head: ListNode?): ListNode? {\n        var prev: ListNode? = nullptr\n        auto $1 = head\n        while (current != nullptr) {\n            auto $1 = current.next\n            current.next = prev\n            prev = current\n            current = nextTemp\n        }\n        return prev\n    }\n}" %}

---

## ReverseNodesInKGroups

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/linkedlist/ReverseNodesInKGroups.kt`

{% include code-tabs.html  kotlin="package linkedlist\n\nclass ReverseNodesInKGroups {\n    fun reverseKGroup(head: ListNode?, k: Int): ListNode? {\n        if (head == null || k == 1) return head\n\n        val dummy = ListNode(0)\n        dummy.next = head\n        var start: ListNode? = dummy\n        var end: ListNode? = dummy\n\n        while (end?.next != null) {\n            // Move `end` k steps forward\n            for (i in 0 until k) {\n                end = end?.next\n                if (end == null) return dummy.next // Not enough nodes to reverse\n            }\n\n            // Store the next group start\n            val nextGroupStart = end?.next\n\n            // Reverse the current group\n            val (newStart, newEnd) = reverse(start?.next, end)\n\n            // Connect the reversed group back to the list\n            start?.next = newStart\n            newEnd?.next = nextGroupStart\n\n            // Move `start` to `newEnd` (which was the original `start` before reversing)\n            start = newEnd\n            // Set `end` back to `start` for the next iteration\n            end = newEnd\n        }\n\n        return dummy.next\n    }\n\n    private fun reverse(start: ListNode?, end: ListNode?): Pair<ListNode?, ListNode?> {\n        var prev: ListNode? = null\n        var current = start\n        val stop = end?.next\n\n        while (current != stop) {\n            val next = current?.next\n            current?.next = prev\n            prev = current\n            current = next\n        }\n\n        return Pair(end, start) // `end` is the new start and `start` is the new end after reversing\n    }\n}"  java="class ReverseNodesInKGroups {\n    public static void $1(head: ListNode?, k): ListNode? {\n        if (head == null || k == 1) return head\n\n        val dummy = ListNode(0)\n        dummy.next = head\n        var start: ListNode? = dummy\n        var end: ListNode? = dummy\n\n        while (end?.next != null) {\n            // Move `end` k steps forward\n            for (i in 0 until k) {\n                end = end?.next\n                if (end == null) return dummy.next // Not enough nodes to reverse\n            }\n\n            // Store the next group start\n            val nextGroupStart = end?.next\n\n            // Reverse the current group\n            val (newStart, newEnd) = reverse(start?.next, end)\n\n            // Connect the reversed group back to the list\n            start?.next = newStart\n            newEnd?.next = nextGroupStart\n\n            // Move `start` to `newEnd` (which was the original `start` before reversing)\n            start = newEnd\n            // Set `end` back to `start` for the next iteration\n            end = newEnd\n        }\n\n        return dummy.next\n    }\n\n    private public static void $1(start: ListNode?, end: ListNode?): Pair<ListNode?, ListNode?> {\n        var prev: ListNode? = null\n        var current = start\n        val stop = end?.next\n\n        while (current != stop) {\n            val next = current?.next\n            current?.next = prev\n            prev = current\n            current = next\n        }\n\n        return Pair(end, start) // `end` is the new start and `start` is the new end after reversing\n    }\n}"  python="class ReverseNodesInKGroups {\n    def $1(head: ListNode?, k: int): ListNode? {\n        if (head == None || k == 1) return head\n\n        $1 = ListNode(0)\n        dummy.next = head\n        var start: ListNode? = dummy\n        var end: ListNode? = dummy\n\n        while (end?.next != None) {\n            // Move `end` k steps forward\n            for (i in 0 until k) {\n                end = end?.next\n                if (end == None) return dummy.next // Not enough nodes to reverse\n            }\n\n            // Store the next group start\n            $1 = end?.next\n\n            // Reverse the current group\n            ($1, $2) = reverse(start?.next, end)\n\n            // Connect the reversed group back to the list\n            start?.next = newStart\n            newEnd?.next = nextGroupStart\n\n            // Move `start` to `newEnd` (which was the original `start` before reversing)\n            start = newEnd\n            // Set `end` back to `start` for the next iteration\n            end = newEnd\n        }\n\n        return dummy.next\n    }\n\n    private def $1(start: ListNode?, end: ListNode?): Pair<ListNode?, ListNode?> {\n        var prev: ListNode? = None\n        $1 = start\n        $1 = end?.next\n\n        while (current != stop) {\n            $1 = current?.next\n            current?.next = prev\n            prev = current\n            current = next\n        }\n\n        return Pair(end, start) // `end` is the new start and `start` is the new end after reversing\n    }\n}"  rust="class ReverseNodesInKGroups {\n    fn $1(head: ListNode?, k -> i32): ListNode? {\n        if (head == None || k == 1) return head\n\n        val dummy = ListNode(0)\n        dummy.next = head\n        var start: ListNode? = dummy\n        var end: ListNode? = dummy\n\n        while (end?.next != None) {\n            // Move `end` k steps forward\n            for (i in 0 until k) {\n                end = end?.next\n                if (end == None) return dummy.next // Not enough nodes to reverse\n            }\n\n            // Store the next group start\n            val nextGroupStart = end?.next\n\n            // Reverse the current group\n            val (newStart, newEnd) = reverse(start?.next, end)\n\n            // Connect the reversed group back to the list\n            start?.next = newStart\n            newEnd?.next = nextGroupStart\n\n            // Move `start` to `newEnd` (which was the original `start` before reversing)\n            start = newEnd\n            // Set `end` back to `start` for the next iteration\n            end = newEnd\n        }\n\n        return dummy.next\n    }\n\n    private fn $1(start: ListNode?, end: ListNode?): Pair<ListNode?, ListNode?> {\n        var prev: ListNode? = None\n        var current = start\n        val stop = end?.next\n\n        while (current != stop) {\n            val next = current?.next\n            current?.next = prev\n            prev = current\n            current = next\n        }\n\n        return Pair(end, start) // `end` is the new start and `start` is the new end after reversing\n    }\n}"  cpp="class ReverseNodesInKGroups {\n    $1(head: ListNode?, k): ListNode? {\n        if (head == nullptr || k == 1) return head\n\n        auto $1 = ListNode(0)\n        dummy.next = head\n        var start: ListNode? = dummy\n        var end: ListNode? = dummy\n\n        while (end?.next != nullptr) {\n            // Move `end` k steps forward\n            for (i in 0 until k) {\n                end = end?.next\n                if (end == nullptr) return dummy.next // Not enough nodes to reverse\n            }\n\n            // Store the next group start\n            auto $1 = end?.next\n\n            // Reverse the current group\n            val (newStart, newEnd) = reverse(start?.next, end)\n\n            // Connect the reversed group back to the list\n            start?.next = newStart\n            newEnd?.next = nextGroupStart\n\n            // Move `start` to `newEnd` (which was the original `start` before reversing)\n            start = newEnd\n            // Set `end` back to `start` for the next iteration\n            end = newEnd\n        }\n\n        return dummy.next\n    }\n\n    private $1(start: ListNode?, end: ListNode?): Pair<ListNode?, ListNode?> {\n        var prev: ListNode? = nullptr\n        auto $1 = start\n        auto $1 = end?.next\n\n        while (current != stop) {\n            auto $1 = current?.next\n            current?.next = prev\n            prev = current\n            current = next\n        }\n\n        return Pair(end, start) // `end` is the new start and `start` is the new end after reversing\n    }\n}" %}
