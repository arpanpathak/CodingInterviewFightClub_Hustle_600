---
layout: chapter
title: "Disjoint Set Union"
chapter_number: 9
chapter_title: "Disjoint Set Union"
toc: true
prev_chapter:
  url: "/chapters/08-heaps"
  title: "Heaps"
next_chapter:
  url: "/chapters/10-string-matching"
  title: "String Matching"
---

# Disjoint Set Union

> **3 problems** from the CrackGoogle repository. Click the tabs to switch between **Kotlin, Java, Python, Rust, and C++**.

## Complete Problem Set

| # | Problem | Topic | Code Available |
|---|---------|-------|----------------|
| 1 | **AccountMerge** | disjointset | ✅ All 5 languages |
| 2 | **NumberOfIsland_II** | disjointset | ✅ All 5 languages |
| 3 | **UnionFind** | disjointset | ✅ All 5 languages |

---

## AccountMerge

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/disjointset/AccountMerge.kt`

{% include code-tabs.html  kotlin="package disjointset\n\n\nclass AccountMerge {\n    class UnionFind<T> {\n        data class Node<T>(var parent: T, var rank: Int)\n\n        private val nodes = mutableMapOf<T, Node<T>>()\n\n        fun add(x: T) {\n            nodes.putIfAbsent(x, Node(x, 0))\n        }\n\n        fun find(x: T): T {\n            val node = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n            if (node.parent != x)\n                node.parent = find(node.parent)\n\n            return node.parent\n        }\n\n        fun union(x: T, y: T) {\n            val rootX = find(x)\n            val rootY = find(y)\n            if (rootX != rootY) {\n                val nodeX = nodes[rootX]!!\n                val nodeY = nodes[rootY]!!\n                when {\n                    nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                    nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                    else -> {\n                        nodeY.parent = rootX\n                        nodeX.rank++\n                    }\n                }\n            }\n        }\n    }\n\n    fun accountsMerge(accounts: List<List<String>>): List<List<String>> {\n        val emailToName = mutableMapOf<String, String>()\n        val uf = UnionFind<String>()\n\n        // Add emails to Union-Find, map them to their owner's name, and union them in one pass\n        accounts.forEach { account ->\n            val name = account.first()\n            account.drop(1).let { emails ->\n                val firstEmail = emails.first()\n                emails.forEach { email ->\n                    emailToName[email] = name\n                    uf.add(email)\n                    uf.union(firstEmail, email)\n                }\n            }\n        }\n\n        // Group emails by their root parent\n        val components = emailToName.keys.groupBy { uf.find(it) }\n\n        // Construct the result\n        return components.values.map { emails ->\n            emails.sorted().let { sortedEmails ->\n                listOf(emailToName[sortedEmails.first()]!!) + sortedEmails\n            }\n        }\n    }\n}"  java="class AccountMerge {\n    class UnionFind<T> {\n        data class Node<T>(var parent: T, var rank)\n\n        private val nodes = mutableMapOf<T, Node<T>>()\n\n        public static void $1(x: T) {\n            nodes.putIfAbsent(x, Node(x, 0))\n        }\n\n        public static void $1(x: T): T {\n            val node = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n            if (node.parent != x)\n                node.parent = find(node.parent)\n\n            return node.parent\n        }\n\n        public static void $1(x: T, y: T) {\n            val rootX = find(x)\n            val rootY = find(y)\n            if (rootX != rootY) {\n                val nodeX = nodes[rootX]!!\n                val nodeY = nodes[rootY]!!\n                when {\n                    nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                    nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                    else -> {\n                        nodeY.parent = rootX\n                        nodeX.rank++\n                    }\n                }\n            }\n        }\n    }\n\n    public static void $1(accounts: List<List<String>>): List<List<String>> {\n        val emailToName = mutableMapOf<String, String>()\n        val uf = UnionFind<String>()\n\n        // Add emails to Union-Find, map them to their owner's name, and union them in one pass\n        accounts.forEach { account ->\n            val name = account.first()\n            account.drop(1).let { emails ->\n                val firstEmail = emails.first()\n                emails.forEach { email ->\n                    emailToName[email] = name\n                    uf.add(email)\n                    uf.union(firstEmail, email)\n                }\n            }\n        }\n\n        // Group emails by their root parent\n        val components = emailToName.keys.groupBy { uf.find(it) }\n\n        // Construct the result\n        return components.values.map { emails ->\n            emails.sorted().let { sortedEmails ->\n                listOf(emailToName[sortedEmails.first()]!!) + sortedEmails\n            }\n        }\n    }\n}"  python="class AccountMerge {\n    class UnionFind<T> {\n        data class Node<T>(var parent: T, var rank: int)\n\n        private $1 = mutableMapOf<T, Node<T>>()\n\n        def $1(x: T) {\n            nodes.putIfAbsent(x, Node(x, 0))\n        }\n\n        def $1(x: T): T {\n            $1 = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n            if (node.parent != x)\n                node.parent = find(node.parent)\n\n            return node.parent\n        }\n\n        def $1(x: T, y: T) {\n            $1 = find(x)\n            $1 = find(y)\n            if (rootX != rootY) {\n                $1 = nodes[rootX]!!\n                $1 = nodes[rootY]!!\n                when {\n                    nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                    nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                    else -> {\n                        nodeY.parent = rootX\n                        nodeX.rank++\n                    }\n                }\n            }\n        }\n    }\n\n    def $1(accounts: List<List<String>>): List<List<String>> {\n        $1 = mutableMapOf<String, String>()\n        $1 = UnionFind<String>()\n\n        // Add emails to Union-Find, map them to their owner's name, and union them in one pass\n        accounts.forEach { account ->\n            $1 = account.first()\n            account.drop(1).let { emails ->\n                $1 = emails.first()\n                emails.forEach { email ->\n                    emailToName[email] = name\n                    uf.add(email)\n                    uf.union(firstEmail, email)\n                }\n            }\n        }\n\n        // Group emails by their root parent\n        $1 = emailToName.keys.groupBy { uf.find(it) }\n\n        // Construct the result\n        return components.values.map { emails ->\n            emails.sorted().let { sortedEmails ->\n                listOf(emailToName[sortedEmails.first()]!!) + sortedEmails\n            }\n        }\n    }\n}"  rust="class AccountMerge {\n    class UnionFind<T> {\n        data class Node<T>(var parent: T, var rank -> i32)\n\n        private val nodes = mutableMapOf<T, Node<T>>()\n\n        fn $1(x: T) {\n            nodes.putIfAbsent(x, Node(x, 0))\n        }\n\n        fn $1(x: T): T {\n            val node = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n            if (node.parent != x)\n                node.parent = find(node.parent)\n\n            return node.parent\n        }\n\n        fn $1(x: T, y: T) {\n            val rootX = find(x)\n            val rootY = find(y)\n            if (rootX != rootY) {\n                val nodeX = nodes[rootX]!!\n                val nodeY = nodes[rootY]!!\n                when {\n                    nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                    nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                    else -> {\n                        nodeY.parent = rootX\n                        nodeX.rank++\n                    }\n                }\n            }\n        }\n    }\n\n    fn $1(accounts: List<List<String>>): List<List<String>> {\n        val emailToName = mutableMapOf<String, String>()\n        val uf = UnionFind<String>()\n\n        // Add emails to Union-Find, map them to their owner's name, and union them in one pass\n        accounts.forEach { account ->\n            val name = account.first()\n            account.drop(1).let { emails ->\n                val firstEmail = emails.first()\n                emails.forEach { email ->\n                    emailToName[email] = name\n                    uf.add(email)\n                    uf.union(firstEmail, email)\n                }\n            }\n        }\n\n        // Group emails by their root parent\n        val components = emailToName.keys.groupBy { uf.find(it) }\n\n        // Construct the result\n        return components.values.map { emails ->\n            emails.sorted().let { sortedEmails ->\n                listOf(emailToName[sortedEmails.first()]!!) + sortedEmails\n            }\n        }\n    }\n}"  cpp="class AccountMerge {\n    class UnionFind<T> {\n        data class Node<T>(var parent: T, var rank)\n\n        private auto $1 = mutableMapOf<T, Node<T>>()\n\n        $1(x: T) {\n            nodes.putIfAbsent(x, Node(x, 0))\n        }\n\n        $1(x: T): T {\n            auto $1 = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n            if (node.parent != x)\n                node.parent = find(node.parent)\n\n            return node.parent\n        }\n\n        $1(x: T, y: T) {\n            auto $1 = find(x)\n            auto $1 = find(y)\n            if (rootX != rootY) {\n                auto $1 = nodes[rootX]!!\n                auto $1 = nodes[rootY]!!\n                when {\n                    nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                    nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                    else -> {\n                        nodeY.parent = rootX\n                        nodeX.rank++\n                    }\n                }\n            }\n        }\n    }\n\n    $1(accounts: List<List<String>>): List<List<String>> {\n        auto $1 = mutableMapOf<String, String>()\n        auto $1 = UnionFind<String>()\n\n        // Add emails to Union-Find, map them to their owner's name, and union them in one pass\n        accounts.forEach { account ->\n            auto $1 = account.first()\n            account.drop(1).let { emails ->\n                auto $1 = emails.first()\n                emails.forEach { email ->\n                    emailToName[email] = name\n                    uf.add(email)\n                    uf.union(firstEmail, email)\n                }\n            }\n        }\n\n        // Group emails by their root parent\n        auto $1 = emailToName.keys.groupBy { uf.find(it) }\n\n        // Construct the result\n        return components.values.map { emails ->\n            emails.sorted().let { sortedEmails ->\n                listOf(emailToName[sortedEmails.first()]!!) + sortedEmails\n            }\n        }\n    }\n}" %}

---

## NumberOfIsland_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/disjointset/NumberOfIsland_II.kt`

{% include code-tabs.html  kotlin="package disjointset\n\nclass NumberOfIsland_II {\n    private class UnionFind {\n        val parent = mutableMapOf<Pair<Int, Int>, Pair<Int, Int>>()\n        val rank = mutableMapOf<Pair<Int, Int>, Int>()\n        var count = 0\n\n        fun find(x: Pair<Int, Int>): Pair<Int, Int> {\n            if (parent[x] != x) {\n                parent[x] = find(parent[x]!!) // Path compression\n            }\n            return parent[x]!!\n        }\n\n        fun union(x: Pair<Int, Int>, y: Pair<Int, Int>) {\n            val rootX = find(x)\n            val rootY = find(y)\n            if (rootX == rootY) return\n\n            when {\n                rank[rootX]!! > rank[rootY]!! -> parent[rootY] = rootX\n                rank[rootX]!! < rank[rootY]!! -> parent[rootX] = rootY\n                else -> {\n                    parent[rootY] = rootX\n                    rank[rootX] = rank[rootX]!! + 1\n                }\n            }\n            count--\n        }\n\n        fun addLand(position: Pair<Int, Int>) {\n            if (parent.containsKey(position)) return\n            parent[position] = position\n            rank[position] = 0\n            count++\n        }\n    }\n\n    fun numIslands2(m: Int, n: Int, positions: Array<IntArray>): List<Int> {\n        val uf = UnionFind()\n        val result = mutableListOf<Int>()\n        val directions = arrayOf(0 to 1, 1 to 0, 0 to -1, -1 to 0)\n\n        for (pos in positions) {\n            val (r, c) = pos\n            val current = r to c\n            uf.addLand(current)\n\n            for ((dr, dc) in directions) {\n                val nr = r + dr\n                val nc = c + dc\n                if (nr in 0 until m && nc in 0 until n) {\n                    val neighbor = nr to nc\n                    if (uf.parent.containsKey(neighbor)) {\n                        uf.union(current, neighbor)\n                    }\n                }\n            }\n            result.add(uf.count)\n        }\n        return result\n    }\n}\n"  java="class NumberOfIsland_II {\n    private class UnionFind {\n        val parent = mutableMapOf<Pair<Int, Int>, Pair<Int, Int>>()\n        val rank = mutableMapOf<Pair<Int, Int>, Int>()\n        var count = 0\n\n        public static void $1(x: Pair<Int, Int>): Pair<Int, Int> {\n            if (parent[x] != x) {\n                parent[x] = find(parent[x]!!) // Path compression\n            }\n            return parent[x]!!\n        }\n\n        public static void $1(x: Pair<Int, Int>, y: Pair<Int, Int>) {\n            val rootX = find(x)\n            val rootY = find(y)\n            if (rootX == rootY) return\n\n            when {\n                rank[rootX]!! > rank[rootY]!! -> parent[rootY] = rootX\n                rank[rootX]!! < rank[rootY]!! -> parent[rootX] = rootY\n                else -> {\n                    parent[rootY] = rootX\n                    rank[rootX] = rank[rootX]!! + 1\n                }\n            }\n            count--\n        }\n\n        public static void $1(position: Pair<Int, Int>) {\n            if (parent.containsKey(position)) return\n            parent[position] = position\n            rank[position] = 0\n            count++\n        }\n    }\n\n    public static void $1(m, n, positions: Array<IntArray>): List<Int> {\n        val uf = UnionFind()\n        val result = mutableListOf<Int>()\n        val directions = arrayOf(0 to 1, 1 to 0, 0 to -1, -1 to 0)\n\n        for (pos in positions) {\n            val (r, c) = pos\n            val current = r to c\n            uf.addLand(current)\n\n            for ((dr, dc) in directions) {\n                val nr = r + dr\n                val nc = c + dc\n                if (nr in 0 until m && nc in 0 until n) {\n                    val neighbor = nr to nc\n                    if (uf.parent.containsKey(neighbor)) {\n                        uf.union(current, neighbor)\n                    }\n                }\n            }\n            result.add(uf.count)\n        }\n        return result\n    }\n}"  python="class NumberOfIsland_II {\n    private class UnionFind {\n        $1 = mutableMapOf<Pair<Int, Int>, Pair<Int, Int>>()\n        $1 = mutableMapOf<Pair<Int, Int>, Int>()\n        $1 = 0\n\n        def $1(x: Pair<Int, Int>): Pair<Int, Int> {\n            if (parent[x] != x) {\n                parent[x] = find(parent[x]!!) // Path compression\n            }\n            return parent[x]!!\n        }\n\n        def $1(x: Pair<Int, Int>, y: Pair<Int, Int>) {\n            $1 = find(x)\n            $1 = find(y)\n            if (rootX == rootY) return\n\n            when {\n                rank[rootX]!! > rank[rootY]!! -> parent[rootY] = rootX\n                rank[rootX]!! < rank[rootY]!! -> parent[rootX] = rootY\n                else -> {\n                    parent[rootY] = rootX\n                    rank[rootX] = rank[rootX]!! + 1\n                }\n            }\n            count--\n        }\n\n        def $1(position: Pair<Int, Int>) {\n            if (parent.containsKey(position)) return\n            parent[position] = position\n            rank[position] = 0\n            count++\n        }\n    }\n\n    def $1(m: int, n: int, positions: Array<list[int]>): List<Int> {\n        $1 = UnionFind()\n        $1 = mutableListOf<Int>()\n        $1 = arrayOf(0 to 1, 1 to 0, 0 to -1, -1 to 0)\n\n        for (pos in positions) {\n            ($1, $2) = pos\n            $1 = r to c\n            uf.addLand(current)\n\n            for ((dr, dc) in directions) {\n                $1 = r + dr\n                $1 = c + dc\n                if (nr in 0 until m && nc in 0 until n) {\n                    $1 = nr to nc\n                    if (uf.parent.containsKey(neighbor)) {\n                        uf.union(current, neighbor)\n                    }\n                }\n            }\n            result.add(uf.count)\n        }\n        return result\n    }\n}"  rust="class NumberOfIsland_II {\n    private class UnionFind {\n        val parent = mutableMapOf<Pair<Int, Int>, Pair<Int, Int>>()\n        val rank = mutableMapOf<Pair<Int, Int>, Int>()\n        var count = 0\n\n        fn $1(x: Pair<Int, Int>): Pair<Int, Int> {\n            if (parent[x] != x) {\n                parent[x] = find(parent[x]!!) // Path compression\n            }\n            return parent[x]!!\n        }\n\n        fn $1(x: Pair<Int, Int>, y: Pair<Int, Int>) {\n            val rootX = find(x)\n            val rootY = find(y)\n            if (rootX == rootY) return\n\n            when {\n                rank[rootX]!! > rank[rootY]!! -> parent[rootY] = rootX\n                rank[rootX]!! < rank[rootY]!! -> parent[rootX] = rootY\n                else -> {\n                    parent[rootY] = rootX\n                    rank[rootX] = rank[rootX]!! + 1\n                }\n            }\n            count--\n        }\n\n        fn $1(position: Pair<Int, Int>) {\n            if (parent.containsKey(position)) return\n            parent[position] = position\n            rank[position] = 0\n            count++\n        }\n    }\n\n    fn $1(m -> i32, n -> i32, positions: Array<Vec<i32>>): List<Int> {\n        val uf = UnionFind()\n        val result = mutableListOf<Int>()\n        val directions = arrayOf(0 to 1, 1 to 0, 0 to -1, -1 to 0)\n\n        for (pos in positions) {\n            val (r, c) = pos\n            val current = r to c\n            uf.addLand(current)\n\n            for ((dr, dc) in directions) {\n                val nr = r + dr\n                val nc = c + dc\n                if (nr in 0 until m && nc in 0 until n) {\n                    val neighbor = nr to nc\n                    if (uf.parent.containsKey(neighbor)) {\n                        uf.union(current, neighbor)\n                    }\n                }\n            }\n            result.add(uf.count)\n        }\n        return result\n    }\n}"  cpp="class NumberOfIsland_II {\n    private class UnionFind {\n        auto $1 = mutableMapOf<Pair<Int, Int>, Pair<Int, Int>>()\n        auto $1 = mutableMapOf<Pair<Int, Int>, Int>()\n        auto $1 = 0\n\n        $1(x: Pair<Int, Int>): Pair<Int, Int> {\n            if (parent[x] != x) {\n                parent[x] = find(parent[x]!!) // Path compression\n            }\n            return parent[x]!!\n        }\n\n        $1(x: Pair<Int, Int>, y: Pair<Int, Int>) {\n            auto $1 = find(x)\n            auto $1 = find(y)\n            if (rootX == rootY) return\n\n            when {\n                rank[rootX]!! > rank[rootY]!! -> parent[rootY] = rootX\n                rank[rootX]!! < rank[rootY]!! -> parent[rootX] = rootY\n                else -> {\n                    parent[rootY] = rootX\n                    rank[rootX] = rank[rootX]!! + 1\n                }\n            }\n            count--\n        }\n\n        $1(position: Pair<Int, Int>) {\n            if (parent.containsKey(position)) return\n            parent[position] = position\n            rank[position] = 0\n            count++\n        }\n    }\n\n    $1(m, n, positions: Array<vector<int>>): List<Int> {\n        auto $1 = UnionFind()\n        auto $1 = mutableListOf<Int>()\n        auto $1 = arrayOf(0 to 1, 1 to 0, 0 to -1, -1 to 0)\n\n        for (pos in positions) {\n            val (r, c) = pos\n            auto $1 = r to c\n            uf.addLand(current)\n\n            for ((dr, dc) in directions) {\n                auto $1 = r + dr\n                auto $1 = c + dc\n                if (nr in 0 until m && nc in 0 until n) {\n                    auto $1 = nr to nc\n                    if (uf.parent.containsKey(neighbor)) {\n                        uf.union(current, neighbor)\n                    }\n                }\n            }\n            result.add(uf.count)\n        }\n        return result\n    }\n}" %}

---

## UnionFind

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/disjointset/UnionFind.kt`

{% include code-tabs.html  kotlin="package disjointset\n\nclass UnionFind<T> {\n    data class Node<T>(var parent: T, var rank: Int)\n\n    private val nodes = mutableMapOf<T, Node<T>>()\n\n    fun add(x: T) {\n        nodes.putIfAbsent(x, Node(x, 0))\n    }\n\n    fun find(x: T): T {\n        val node = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n        if (node.parent != x)\n            node.parent = find(node.parent)\n\n        return node.parent\n    }\n\n    fun union(x: T, y: T) {\n        val rootX = find(x)\n        val rootY = find(y)\n        if (rootX != rootY) {\n            val nodeX = nodes[rootX]!!\n            val nodeY = nodes[rootY]!!\n            when {\n                nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                else -> {\n                    nodeY.parent = rootX\n                    nodeX.rank++\n                }\n            }\n        }\n    }\n}\n"  java="class UnionFind<T> {\n    data class Node<T>(var parent: T, var rank)\n\n    private val nodes = mutableMapOf<T, Node<T>>()\n\n    public static void $1(x: T) {\n        nodes.putIfAbsent(x, Node(x, 0))\n    }\n\n    public static void $1(x: T): T {\n        val node = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n        if (node.parent != x)\n            node.parent = find(node.parent)\n\n        return node.parent\n    }\n\n    public static void $1(x: T, y: T) {\n        val rootX = find(x)\n        val rootY = find(y)\n        if (rootX != rootY) {\n            val nodeX = nodes[rootX]!!\n            val nodeY = nodes[rootY]!!\n            when {\n                nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                else -> {\n                    nodeY.parent = rootX\n                    nodeX.rank++\n                }\n            }\n        }\n    }\n}"  python="class UnionFind<T> {\n    data class Node<T>(var parent: T, var rank: int)\n\n    private $1 = mutableMapOf<T, Node<T>>()\n\n    def $1(x: T) {\n        nodes.putIfAbsent(x, Node(x, 0))\n    }\n\n    def $1(x: T): T {\n        $1 = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n        if (node.parent != x)\n            node.parent = find(node.parent)\n\n        return node.parent\n    }\n\n    def $1(x: T, y: T) {\n        $1 = find(x)\n        $1 = find(y)\n        if (rootX != rootY) {\n            $1 = nodes[rootX]!!\n            $1 = nodes[rootY]!!\n            when {\n                nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                else -> {\n                    nodeY.parent = rootX\n                    nodeX.rank++\n                }\n            }\n        }\n    }\n}"  rust="class UnionFind<T> {\n    data class Node<T>(var parent: T, var rank -> i32)\n\n    private val nodes = mutableMapOf<T, Node<T>>()\n\n    fn $1(x: T) {\n        nodes.putIfAbsent(x, Node(x, 0))\n    }\n\n    fn $1(x: T): T {\n        val node = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n        if (node.parent != x)\n            node.parent = find(node.parent)\n\n        return node.parent\n    }\n\n    fn $1(x: T, y: T) {\n        val rootX = find(x)\n        val rootY = find(y)\n        if (rootX != rootY) {\n            val nodeX = nodes[rootX]!!\n            val nodeY = nodes[rootY]!!\n            when {\n                nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                else -> {\n                    nodeY.parent = rootX\n                    nodeX.rank++\n                }\n            }\n        }\n    }\n}"  cpp="class UnionFind<T> {\n    data class Node<T>(var parent: T, var rank)\n\n    private auto $1 = mutableMapOf<T, Node<T>>()\n\n    $1(x: T) {\n        nodes.putIfAbsent(x, Node(x, 0))\n    }\n\n    $1(x: T): T {\n        auto $1 = nodes[x] ?: throw IllegalAccessException(\"Value $x not found\")\n\n        if (node.parent != x)\n            node.parent = find(node.parent)\n\n        return node.parent\n    }\n\n    $1(x: T, y: T) {\n        auto $1 = find(x)\n        auto $1 = find(y)\n        if (rootX != rootY) {\n            auto $1 = nodes[rootX]!!\n            auto $1 = nodes[rootY]!!\n            when {\n                nodeX.rank > nodeY.rank -> nodeY.parent = rootX\n                nodeX.rank < nodeY.rank -> nodeX.parent = rootY\n                else -> {\n                    nodeY.parent = rootX\n                    nodeX.rank++\n                }\n            }\n        }\n    }\n}" %}
