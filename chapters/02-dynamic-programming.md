---
layout: chapter
title: "Dynamic Programming"
chapter_number: 2
chapter_title: "Dynamic Programming"
toc: true
prev_chapter:
  url: "/chapters/01-binary-search"
  title: "Binary Search"
next_chapter:
  url: "/chapters/03-arrays-two-pointers"
  title: "Arrays & Two Pointers"
---

# Dynamic Programming

> **30 problems** from the CrackGoogle repository. Click the tabs to switch between **Kotlin, Java, Python, Rust, and C++**.

## Complete Problem Set

| # | Problem | Topic | Code Available |
|---|---------|-------|----------------|
| 1 | **BurstBaloons** | array/dp | ✅ All 5 languages |
| 2 | **CoinChange** | array/dp | ✅ All 5 languages |
| 3 | **CoinChange_II** | array/dp | ✅ All 5 languages |
| 4 | **CoinChange_II_BottomUp** | array/dp | ✅ All 5 languages |
| 5 | **HouseRobber** | array/dp | ✅ All 5 languages |
| 6 | **HouseRobber_II** | array/dp | ✅ All 5 languages |
| 7 | **LongestCommonSubarray** | array/dp | ✅ All 5 languages |
| 8 | **LongestIncreasingSequenceInAMatrix** | array/dp | ✅ All 5 languages |
| 9 | **LongestIncreasingSubsequence** | array/dp | ✅ All 5 languages |
| 10 | **MaximalSquare** | array/dp | ✅ All 5 languages |
| 11 | **MaximumSumSubArray** | array/dp | ✅ All 5 languages |
| 12 | **MinCostClimbingStaris** | array/dp | ✅ All 5 languages |
| 13 | **MinimumNumberofIncrementsSubarraysFormaTargetArray** | array/dp | ✅ All 5 languages |
| 14 | **MinimumPathSum** | array/dp | ✅ All 5 languages |
| 15 | **PartitionArrayIntoTwoArrayToMinimuzeSumDifference** | array/dp | ✅ All 5 languages |
| 16 | **SplitArrayLargestSum** | array/dp | ✅ All 5 languages |
| 17 | **TargetSum** | array/dp | ✅ All 5 languages |
| 18 | **ClosestSubsequenceSum** | dynamic_programming | ✅ All 5 languages |
| 19 | **MaximumProductSubarray** | dynamic_programming | ✅ All 5 languages |
| 20 | **MaximumProfitInJobScheduling** | dynamic_programming | ✅ All 5 languages |
| 21 | **PartitionEqualSubsetSum** | dynamic_programming | ✅ All 5 languages |
| 22 | **SuperEggDropping** | dynamic_programming | ✅ All 5 languages |
| 23 | **BestTimeToBuyAndSellStock** | stock_market/dp | ✅ All 5 languages |
| 24 | **BestTimeToBuyAndSellStockWithCooldown** | stock_market/dp | ✅ All 5 languages |
| 25 | **BestTimeToBuyAndSellStockWithTransactionFee** | stock_market/dp | ✅ All 5 languages |
| 26 | **BestTimeToBuyAndSellStock_III** | stock_market/dp | ✅ All 5 languages |
| 27 | **CherryPickup** | grid/dynamic_programming | ✅ All 5 languages |
| 28 | **Test** | grid/dynamic_programming | ✅ All 5 languages |
| 29 | **UniquePaths_I** | grid/dynamic_programming | ✅ All 5 languages |
| 30 | **UniquePaths_II** | grid/dynamic_programming | ✅ All 5 languages |

---

## BurstBaloons

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/BurstBaloons.kt`

{% include code-tabs.html  kotlin="package array.dp\n\n// Time COmplexity O(N^3) Space O(N^2)\nclass BurstBaloons {\n    fun maxCoins(nums: IntArray): Int {\n        if (nums.isEmpty()) return 0\n        val dp = Array(nums.size) { IntArray(nums.size) }\n\n        fun dfs(l: Int, r: Int): Int {\n            when {\n                l > r -> return 0\n                dp[l][r] != 0 -> return dp[l][r]\n            }\n\n            var res = 0\n            for (mid in l..r) {\n                // Calculate the maximum coins for this partition\n                val left = if (l == 0) 1 else nums[l - 1]\n                val right = if (r == nums.size - 1) 1 else nums[r + 1]\n\n                res = maxOf(res, dfs(l, mid - 1) +\n                        left * nums[mid] * right +\n                        dfs(mid + 1, r))\n            }\n\n            return res.also {  dp[l][r] = res }\n        }\n        return dfs(0, nums.size - 1)\n    }\n}\n"  java="// Time COmplexity O(N^3) Space O(N^2)\nclass BurstBaloons {\n    public static void $1(nums: IntArray) {\n        if (nums.isEmpty()) return 0\n        val dp = Array(nums.size) { IntArray(nums.size) }\n\n        public static void $1(l, r) {\n            when {\n                l > r -> return 0\n                dp[l][r] != 0 -> return dp[l][r]\n            }\n\n            var res = 0\n            for (mid in l..r) {\n                // Calculate the maximum coins for this partition\n                val left = if (l == 0) 1 else nums[l - 1]\n                val right = if (r == nums.size - 1) 1 else nums[r + 1]\n\n                res = maxOf(res, dfs(l, mid - 1) +\n                        left * nums[mid] * right +\n                        dfs(mid + 1, r))\n            }\n\n            return res.also {  dp[l][r] = res }\n        }\n        return dfs(0, nums.size - 1)\n    }\n}"  python="// Time COmplexity O(N^3) Space O(N^2)\nclass BurstBaloons {\n    def $1(nums: list[int]): int {\n        if (nums.isEmpty()) return 0\n        $1 = Array(nums.size) { list[int](nums.size) }\n\n        def $1(l: int, r: int): int {\n            when {\n                l > r -> return 0\n                dp[l][r] != 0 -> return dp[l][r]\n            }\n\n            $1 = 0\n            for (mid in l..r) {\n                // Calculate the maximum coins for this partition\n                $1 = if (l == 0) 1 else nums[l - 1]\n                $1 = if (r == nums.size - 1) 1 else nums[r + 1]\n\n                res = max(res, dfs(l, mid - 1) +\n                        left * nums[mid] * right +\n                        dfs(mid + 1, r))\n            }\n\n            return res.also {  dp[l][r] = res }\n        }\n        return dfs(0, nums.size - 1)\n    }\n}"  rust="// Time COmplexity O(N^3) Space O(N^2)\nclass BurstBaloons {\n    fn $1(nums: Vec<i32>) -> i32 {\n        if (nums.isEmpty()) return 0\n        val dp = Array(nums.size) { Vec<i32>(nums.size) }\n\n        fn $1(l -> i32, r -> i32) -> i32 {\n            when {\n                l > r -> return 0\n                dp[l][r] != 0 -> return dp[l][r]\n            }\n\n            var res = 0\n            for (mid in l..r) {\n                // Calculate the maximum coins for this partition\n                val left = if (l == 0) 1 else nums[l - 1]\n                val right = if (r == nums.size - 1) 1 else nums[r + 1]\n\n                res = max(res, dfs(l, mid - 1) +\n                        left * nums[mid] * right +\n                        dfs(mid + 1, r))\n            }\n\n            return res.also {  dp[l][r] = res }\n        }\n        return dfs(0, nums.size - 1)\n    }\n}"  cpp="// Time COmplexity O(N^3) Space O(N^2)\nclass BurstBaloons {\n    $1(nums: vector<int>) {\n        if (nums.isEmpty()) return 0\n        auto $1 = Array(nums.size) { vector<int>(nums.size) }\n\n        $1(l, r) {\n            when {\n                l > r -> return 0\n                dp[l][r] != 0 -> return dp[l][r]\n            }\n\n            auto $1 = 0\n            for (mid in l..r) {\n                // Calculate the maximum coins for this partition\n                auto $1 = if (l == 0) 1 else nums[l - 1]\n                auto $1 = if (r == nums.size - 1) 1 else nums[r + 1]\n\n                res = max(res, dfs(l, mid - 1) +\n                        left * nums[mid] * right +\n                        dfs(mid + 1, r))\n            }\n\n            return res.also {  dp[l][r] = res }\n        }\n        return dfs(0, nums.size - 1)\n    }\n}" %}

---

## CoinChange

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/CoinChange.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass CoinChange {\n    fun coinChange(coins: IntArray, amount: Int): Int {\n        val dp = IntArray(amount + 1) { -1 }\n        return coinChange(coins, amount, dp).let { if (it != Int.MAX_VALUE) it else -1 }\n    }\n\n    private fun coinChange(coins: IntArray, amount: Int, dp: IntArray): Int {\n        return when {\n            amount == 0 -> 0\n            dp[amount] != -1 -> dp[amount]\n            else -> {\n                var minCoins = Int.MAX_VALUE\n                for (coin in coins) {\n                    if (coin <= amount) {\n                        val result = coinChange(coins, amount - coin, dp)\n                        if (result != Int.MAX_VALUE) {\n                            minCoins = minOf(minCoins, 1 + result)\n                        }\n                    }\n                }\n                minCoins\n            }\n        }.also { dp[amount] = it }\n    }\n}\n"  java="class CoinChange {\n    public static void $1(coins: IntArray, amount) {\n        val dp = IntArray(amount + 1) { -1 }\n        return coinChange(coins, amount, dp).let { if (it != Int.MAX_VALUE) it else -1 }\n    }\n\n    private public static void $1(coins: IntArray, amount, dp: IntArray) {\n        return when {\n            amount == 0 -> 0\n            dp[amount] != -1 -> dp[amount]\n            else -> {\n                var minCoins = Int.MAX_VALUE\n                for (coin in coins) {\n                    if (coin <= amount) {\n                        val result = coinChange(coins, amount - coin, dp)\n                        if (result != Int.MAX_VALUE) {\n                            minCoins = minOf(minCoins, 1 + result)\n                        }\n                    }\n                }\n                minCoins\n            }\n        }.also { dp[amount] = it }\n    }\n}"  python="class CoinChange {\n    def $1(coins: list[int], amount: int): int {\n        $1 = list[int](amount + 1) { -1 }\n        return coinChange(coins, amount, dp).let { if (it != Int.MAX_VALUE) it else -1 }\n    }\n\n    private def $1(coins: list[int], amount: int, dp: list[int]): int {\n        return when {\n            amount == 0 -> 0\n            dp[amount] != -1 -> dp[amount]\n            else -> {\n                $1 = Int.MAX_VALUE\n                for (coin in coins) {\n                    if (coin <= amount) {\n                        $1 = coinChange(coins, amount - coin, dp)\n                        if (result != Int.MAX_VALUE) {\n                            minCoins = min(minCoins, 1 + result)\n                        }\n                    }\n                }\n                minCoins\n            }\n        }.also { dp[amount] = it }\n    }\n}"  rust="class CoinChange {\n    fn $1(coins: Vec<i32>, amount -> i32) -> i32 {\n        val dp = Vec<i32>(amount + 1) { -1 }\n        return coinChange(coins, amount, dp).let { if (it != Int.MAX_VALUE) it else -1 }\n    }\n\n    private fn $1(coins: Vec<i32>, amount -> i32, dp: Vec<i32>) -> i32 {\n        return when {\n            amount == 0 -> 0\n            dp[amount] != -1 -> dp[amount]\n            else -> {\n                var minCoins = Int.MAX_VALUE\n                for (coin in coins) {\n                    if (coin <= amount) {\n                        val result = coinChange(coins, amount - coin, dp)\n                        if (result != Int.MAX_VALUE) {\n                            minCoins = min(minCoins, 1 + result)\n                        }\n                    }\n                }\n                minCoins\n            }\n        }.also { dp[amount] = it }\n    }\n}"  cpp="class CoinChange {\n    $1(coins: vector<int>, amount) {\n        auto $1 = vector<int>(amount + 1) { -1 }\n        return coinChange(coins, amount, dp).let { if (it != Int.MAX_VALUE) it else -1 }\n    }\n\n    private $1(coins: vector<int>, amount, dp: vector<int>) {\n        return when {\n            amount == 0 -> 0\n            dp[amount] != -1 -> dp[amount]\n            else -> {\n                auto $1 = Int.MAX_VALUE\n                for (coin in coins) {\n                    if (coin <= amount) {\n                        auto $1 = coinChange(coins, amount - coin, dp)\n                        if (result != Int.MAX_VALUE) {\n                            minCoins = min(minCoins, 1 + result)\n                        }\n                    }\n                }\n                minCoins\n            }\n        }.also { dp[amount] = it }\n    }\n}" %}

---

## CoinChange_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/CoinChange_II.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass CoinChange_II {\n    fun change(amount: Int, coins: IntArray): Int {\n        val dp = Array(amount + 1) { IntArray(coins.size) { -1 } }\n        return change(dp, amount, coins, 0)\n    }\n\n    private fun change(dp: Array<IntArray>, amount: Int, coins: IntArray, i: Int): Int {\n        return when {\n            amount < 0 || (i == coins.size && amount > 0) -> 0\n            amount == 0 -> 1\n            dp[amount][i] != -1 -> dp[amount][i]\n            else -> {\n                dp[amount][i] = change(dp, amount - coins[i], coins, i) + change(dp, amount, coins, i + 1)\n                dp[amount][i]\n            }\n        }\n    }\n}"  java="class CoinChange_II {\n    public static void $1(amount, coins: IntArray) {\n        val dp = Array(amount + 1) { IntArray(coins.size) { -1 } }\n        return change(dp, amount, coins, 0)\n    }\n\n    private public static void $1(dp: Array<IntArray>, amount, coins: IntArray, i) {\n        return when {\n            amount < 0 || (i == coins.size && amount > 0) -> 0\n            amount == 0 -> 1\n            dp[amount][i] != -1 -> dp[amount][i]\n            else -> {\n                dp[amount][i] = change(dp, amount - coins[i], coins, i) + change(dp, amount, coins, i + 1)\n                dp[amount][i]\n            }\n        }\n    }\n}"  python="class CoinChange_II {\n    def $1(amount: int, coins: list[int]): int {\n        $1 = Array(amount + 1) { list[int](coins.size) { -1 } }\n        return change(dp, amount, coins, 0)\n    }\n\n    private def $1(dp: Array<list[int]>, amount: int, coins: list[int], i: int): int {\n        return when {\n            amount < 0 || (i == coins.size && amount > 0) -> 0\n            amount == 0 -> 1\n            dp[amount][i] != -1 -> dp[amount][i]\n            else -> {\n                dp[amount][i] = change(dp, amount - coins[i], coins, i) + change(dp, amount, coins, i + 1)\n                dp[amount][i]\n            }\n        }\n    }\n}"  rust="class CoinChange_II {\n    fn $1(amount -> i32, coins: Vec<i32>) -> i32 {\n        val dp = Array(amount + 1) { Vec<i32>(coins.size) { -1 } }\n        return change(dp, amount, coins, 0)\n    }\n\n    private fn $1(dp: Array<Vec<i32>>, amount -> i32, coins: Vec<i32>, i -> i32) -> i32 {\n        return when {\n            amount < 0 || (i == coins.size && amount > 0) -> 0\n            amount == 0 -> 1\n            dp[amount][i] != -1 -> dp[amount][i]\n            else -> {\n                dp[amount][i] = change(dp, amount - coins[i], coins, i) + change(dp, amount, coins, i + 1)\n                dp[amount][i]\n            }\n        }\n    }\n}"  cpp="class CoinChange_II {\n    $1(amount, coins: vector<int>) {\n        auto $1 = Array(amount + 1) { vector<int>(coins.size) { -1 } }\n        return change(dp, amount, coins, 0)\n    }\n\n    private $1(dp: Array<vector<int>>, amount, coins: vector<int>, i) {\n        return when {\n            amount < 0 || (i == coins.size && amount > 0) -> 0\n            amount == 0 -> 1\n            dp[amount][i] != -1 -> dp[amount][i]\n            else -> {\n                dp[amount][i] = change(dp, amount - coins[i], coins, i) + change(dp, amount, coins, i + 1)\n                dp[amount][i]\n            }\n        }\n    }\n}" %}

---

## CoinChange_II_BottomUp

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/CoinChange_II_BottomUp.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass CoinChange_II_BottomUp {\n    fun change(amount: Int, coins: IntArray): Int {\n        val dp = IntArray(amount + 1) { 0 }\n        dp[0] = 1  // Base case: 1 way to make amount 0 (using no coins)\n\n        coins.forEach { coin ->\n            for (j in coin..amount)\n                dp[j] += dp[j - coin]\n        }\n\n        return dp[amount]\n    }\n}\n"  java="class CoinChange_II_BottomUp {\n    public static void $1(amount, coins: IntArray) {\n        val dp = IntArray(amount + 1) { 0 }\n        dp[0] = 1  // Base case: 1 way to make amount 0 (using no coins)\n\n        coins.forEach { coin ->\n            for (j in coin..amount)\n                dp[j] += dp[j - coin]\n        }\n\n        return dp[amount]\n    }\n}"  python="class CoinChange_II_BottomUp {\n    def $1(amount: int, coins: list[int]): int {\n        $1 = list[int](amount + 1) { 0 }\n        dp[0] = 1  // Base case: 1 way to make amount 0 (using no coins)\n\n        coins.forEach { coin ->\n            for (j in coin..amount)\n                dp[j] += dp[j - coin]\n        }\n\n        return dp[amount]\n    }\n}"  rust="class CoinChange_II_BottomUp {\n    fn $1(amount -> i32, coins: Vec<i32>) -> i32 {\n        val dp = Vec<i32>(amount + 1) { 0 }\n        dp[0] = 1  // Base case: 1 way to make amount 0 (using no coins)\n\n        coins.forEach { coin ->\n            for (j in coin..amount)\n                dp[j] += dp[j - coin]\n        }\n\n        return dp[amount]\n    }\n}"  cpp="class CoinChange_II_BottomUp {\n    $1(amount, coins: vector<int>) {\n        auto $1 = vector<int>(amount + 1) { 0 }\n        dp[0] = 1  // Base case: 1 way to make amount 0 (using no coins)\n\n        coins.forEach { coin ->\n            for (j in coin..amount)\n                dp[j] += dp[j - coin]\n        }\n\n        return dp[amount]\n    }\n}" %}

---

## HouseRobber

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/HouseRobber.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass HouseRobber {\n    fun rob(nums: IntArray): Int {\n        val dp = IntArray(nums.size) { -1 }\n        return rob(dp, nums, 0)\n    }\n\n    fun rob(dp: IntArray, nums: IntArray, i: Int): Int {\n        return when {\n            i >= nums.size -> 0\n            dp[i] != -1 -> dp[i]\n            else -> {\n                dp[i] = maxOf(nums[i] + rob(dp, nums, i + 2), rob(dp, nums, i + 1))\n                dp[i]\n            }\n        }\n    }\n\n    fun rob_iterative(nums: IntArray): Int {\n        if (nums.isEmpty()) return 0\n\n        var include = 0\n        var exclude = 0\n\n        for (num in nums) {\n            val temp = include\n            include = num + exclude\n            exclude = maxOf(temp, exclude)\n        }\n\n        return maxOf(include, exclude)\n    }\n}"  java="class HouseRobber {\n    public static void $1(nums: IntArray) {\n        val dp = IntArray(nums.size) { -1 }\n        return rob(dp, nums, 0)\n    }\n\n    public static void $1(dp: IntArray, nums: IntArray, i) {\n        return when {\n            i >= nums.size -> 0\n            dp[i] != -1 -> dp[i]\n            else -> {\n                dp[i] = maxOf(nums[i] + rob(dp, nums, i + 2), rob(dp, nums, i + 1))\n                dp[i]\n            }\n        }\n    }\n\n    public static void $1(nums: IntArray) {\n        if (nums.isEmpty()) return 0\n\n        var include = 0\n        var exclude = 0\n\n        for (num in nums) {\n            val temp = include\n            include = num + exclude\n            exclude = maxOf(temp, exclude)\n        }\n\n        return maxOf(include, exclude)\n    }\n}"  python="class HouseRobber {\n    def $1(nums: list[int]): int {\n        $1 = list[int](nums.size) { -1 }\n        return rob(dp, nums, 0)\n    }\n\n    def $1(dp: list[int], nums: list[int], i: int): int {\n        return when {\n            i >= nums.size -> 0\n            dp[i] != -1 -> dp[i]\n            else -> {\n                dp[i] = max(nums[i] + rob(dp, nums, i + 2), rob(dp, nums, i + 1))\n                dp[i]\n            }\n        }\n    }\n\n    def $1(nums: list[int]): int {\n        if (nums.isEmpty()) return 0\n\n        $1 = 0\n        $1 = 0\n\n        for (num in nums) {\n            $1 = include\n            include = num + exclude\n            exclude = max(temp, exclude)\n        }\n\n        return max(include, exclude)\n    }\n}"  rust="class HouseRobber {\n    fn $1(nums: Vec<i32>) -> i32 {\n        val dp = Vec<i32>(nums.size) { -1 }\n        return rob(dp, nums, 0)\n    }\n\n    fn $1(dp: Vec<i32>, nums: Vec<i32>, i -> i32) -> i32 {\n        return when {\n            i >= nums.size -> 0\n            dp[i] != -1 -> dp[i]\n            else -> {\n                dp[i] = max(nums[i] + rob(dp, nums, i + 2), rob(dp, nums, i + 1))\n                dp[i]\n            }\n        }\n    }\n\n    fn $1(nums: Vec<i32>) -> i32 {\n        if (nums.isEmpty()) return 0\n\n        var include = 0\n        var exclude = 0\n\n        for (num in nums) {\n            val temp = include\n            include = num + exclude\n            exclude = max(temp, exclude)\n        }\n\n        return max(include, exclude)\n    }\n}"  cpp="class HouseRobber {\n    $1(nums: vector<int>) {\n        auto $1 = vector<int>(nums.size) { -1 }\n        return rob(dp, nums, 0)\n    }\n\n    $1(dp: vector<int>, nums: vector<int>, i) {\n        return when {\n            i >= nums.size -> 0\n            dp[i] != -1 -> dp[i]\n            else -> {\n                dp[i] = max(nums[i] + rob(dp, nums, i + 2), rob(dp, nums, i + 1))\n                dp[i]\n            }\n        }\n    }\n\n    $1(nums: vector<int>) {\n        if (nums.isEmpty()) return 0\n\n        auto $1 = 0\n        auto $1 = 0\n\n        for (num in nums) {\n            auto $1 = include\n            include = num + exclude\n            exclude = max(temp, exclude)\n        }\n\n        return max(include, exclude)\n    }\n}" %}

---

## HouseRobber_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/HouseRobber_II.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass HouseRobber_II {\n    fun rob(nums: IntArray): Int {\n        return when {\n            nums.size == 1 -> nums[0]\n            else -> maxOf(rob(nums, 0 , nums.size ), rob(nums, 1, nums.size - 1))\n        }\n    }\n\n    /**\n     * Recusrive Solution\n     */\n    fun rob(nums: IntArray, start: Int, end: Int): Int {\n        var (include, exclude, max) = Triple(0, 0, 0)\n\n        for (i in start until  end) {\n            max = maxOf(include + nums[i], exclude)\n\n            include = exclude\n            exclude = max\n        }\n\n        return max\n    }\n\n\n    /**\n     * Bottom UP DP\n     */\n    fun robRange(nums: IntArray, start: Int, end: Int): Int {\n        if (nums.isEmpty() || start > end) return 0\n        if (start == end) return nums[start]\n\n        // Initialize an array to store the maximum amount that can be robbed up to each house\n        val dp = IntArray(nums.size)\n\n        // Base cases\n        dp[start] = nums[start]\n        dp[start + 1] = maxOf(nums[start], nums[start + 1])\n\n        // Fill the dp array from start + 2 to end\n        for (i in start + 2..end) {\n            dp[i] = maxOf(nums[i] + (if (i >= start + 2) dp[i - 2] else 0), dp[i - 1])\n        }\n\n        // The last element of dp array contains the maximum amount that can be robbed\n        return dp[end]\n    }\n}"  java="class HouseRobber_II {\n    public static void $1(nums: IntArray) {\n        return when {\n            nums.size == 1 -> nums[0]\n            else -> maxOf(rob(nums, 0 , nums.size ), rob(nums, 1, nums.size - 1))\n        }\n    }\n\n    /**\n     * Recusrive Solution\n     */\n    public static void $1(nums: IntArray, start, end) {\n        var (include, exclude, max) = Triple(0, 0, 0)\n\n        for (i in start until  end) {\n            max = maxOf(include + nums[i], exclude)\n\n            include = exclude\n            exclude = max\n        }\n\n        return max\n    }\n\n\n    /**\n     * Bottom UP DP\n     */\n    public static void $1(nums: IntArray, start, end) {\n        if (nums.isEmpty() || start > end) return 0\n        if (start == end) return nums[start]\n\n        // Initialize an array to store the maximum amount that can be robbed up to each house\n        val dp = IntArray(nums.size)\n\n        // Base cases\n        dp[start] = nums[start]\n        dp[start + 1] = maxOf(nums[start], nums[start + 1])\n\n        // Fill the dp array from start + 2 to end\n        for (i in start + 2..end) {\n            dp[i] = maxOf(nums[i] + (if (i >= start + 2) dp[i - 2] else 0), dp[i - 1])\n        }\n\n        // The last element of dp array contains the maximum amount that can be robbed\n        return dp[end]\n    }\n}"  python="class HouseRobber_II {\n    def $1(nums: list[int]): int {\n        return when {\n            nums.size == 1 -> nums[0]\n            else -> max(rob(nums, 0 , nums.size ), rob(nums, 1, nums.size - 1))\n        }\n    }\n\n    /**\n     * Recusrive Solution\n     */\n    def $1(nums: list[int], start: int, end: int): int {\n        var (include, exclude, max) = Triple(0, 0, 0)\n\n        for (i in start until  end) {\n            max = max(include + nums[i], exclude)\n\n            include = exclude\n            exclude = max\n        }\n\n        return max\n    }\n\n\n    /**\n     * Bottom UP DP\n     */\n    def $1(nums: list[int], start: int, end: int): int {\n        if (nums.isEmpty() || start > end) return 0\n        if (start == end) return nums[start]\n\n        // Initialize an array to store the maximum amount that can be robbed up to each house\n        $1 = list[int](nums.size)\n\n        // Base cases\n        dp[start] = nums[start]\n        dp[start + 1] = max(nums[start], nums[start + 1])\n\n        // Fill the dp array from start + 2 to end\n        for (i in start + 2..end) {\n            dp[i] = max(nums[i] + (if (i >= start + 2) dp[i - 2] else 0), dp[i - 1])\n        }\n\n        // The last element of dp array contains the maximum amount that can be robbed\n        return dp[end]\n    }\n}"  rust="class HouseRobber_II {\n    fn $1(nums: Vec<i32>) -> i32 {\n        return when {\n            nums.size == 1 -> nums[0]\n            else -> max(rob(nums, 0 , nums.size ), rob(nums, 1, nums.size - 1))\n        }\n    }\n\n    /**\n     * Recusrive Solution\n     */\n    fn $1(nums: Vec<i32>, start -> i32, end -> i32) -> i32 {\n        var (include, exclude, max) = Triple(0, 0, 0)\n\n        for (i in start until  end) {\n            max = max(include + nums[i], exclude)\n\n            include = exclude\n            exclude = max\n        }\n\n        return max\n    }\n\n\n    /**\n     * Bottom UP DP\n     */\n    fn $1(nums: Vec<i32>, start -> i32, end -> i32) -> i32 {\n        if (nums.isEmpty() || start > end) return 0\n        if (start == end) return nums[start]\n\n        // Initialize an array to store the maximum amount that can be robbed up to each house\n        val dp = Vec<i32>(nums.size)\n\n        // Base cases\n        dp[start] = nums[start]\n        dp[start + 1] = max(nums[start], nums[start + 1])\n\n        // Fill the dp array from start + 2 to end\n        for (i in start + 2..end) {\n            dp[i] = max(nums[i] + (if (i >= start + 2) dp[i - 2] else 0), dp[i - 1])\n        }\n\n        // The last element of dp array contains the maximum amount that can be robbed\n        return dp[end]\n    }\n}"  cpp="class HouseRobber_II {\n    $1(nums: vector<int>) {\n        return when {\n            nums.size == 1 -> nums[0]\n            else -> max(rob(nums, 0 , nums.size ), rob(nums, 1, nums.size - 1))\n        }\n    }\n\n    /**\n     * Recusrive Solution\n     */\n    $1(nums: vector<int>, start, end) {\n        var (include, exclude, max) = Triple(0, 0, 0)\n\n        for (i in start until  end) {\n            max = max(include + nums[i], exclude)\n\n            include = exclude\n            exclude = max\n        }\n\n        return max\n    }\n\n\n    /**\n     * Bottom UP DP\n     */\n    $1(nums: vector<int>, start, end) {\n        if (nums.isEmpty() || start > end) return 0\n        if (start == end) return nums[start]\n\n        // Initialize an array to store the maximum amount that can be robbed up to each house\n        auto $1 = vector<int>(nums.size)\n\n        // Base cases\n        dp[start] = nums[start]\n        dp[start + 1] = max(nums[start], nums[start + 1])\n\n        // Fill the dp array from start + 2 to end\n        for (i in start + 2..end) {\n            dp[i] = max(nums[i] + (if (i >= start + 2) dp[i - 2] else 0), dp[i - 1])\n        }\n\n        // The last element of dp array contains the maximum amount that can be robbed\n        return dp[end]\n    }\n}" %}

---

## LongestCommonSubarray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/LongestCommonSubarray.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass LongestCommonSubarray {\n    val memo = mutableMapOf<String, Int>()\n\n    fun findLength(nums1: IntArray, nums2: IntArray): Int {\n        return dp(nums1, nums2, nums1.lastIndex, nums2.lastIndex, Int.MIN_VALUE)\n    }\n\n    fun dp(nums1: IntArray, nums2: IntArray, i: Int, j: Int, max: Int): Int {\n        if (i == 0 || j == 0)\n            return max\n        val key =\"$i.$j\"\n        if (memo.containsKey(key))\n            return memo[key]!!\n\n        var maxSoFar = max\n\n        if (nums1[i] == nums1[j])\n            maxSoFar = dp(nums1, nums2, i + 1, j + 1, max + 1)\n\n        return maxSoFar\n    }\n\n    fun findLength_tabular(nums1: IntArray, nums2: IntArray): Int {\n        val m = nums1.size\n        val n = nums2.size\n        val dp = Array(m + 1) { IntArray(n + 1) }\n        var maxLength = 0\n\n        // Fill the dp array\n        for (i in 1..m) {\n            for (j in 1..n) {\n                if (nums1[i - 1] == nums2[j - 1]) {\n                    dp[i][j] = dp[i - 1][j - 1] + 1\n                    maxLength = maxOf(maxLength, dp[i][j])\n                } else {\n                    dp[i][j] = 0\n                }\n            }\n        }\n\n        return maxLength\n    }\n}\n"  java="class LongestCommonSubarray {\n    val memo = mutableMapOf<String, Int>()\n\n    public static void $1(nums1: IntArray, nums2: IntArray) {\n        return dp(nums1, nums2, nums1.lastIndex, nums2.lastIndex, Int.MIN_VALUE)\n    }\n\n    public static void $1(nums1: IntArray, nums2: IntArray, i, j, max) {\n        if (i == 0 || j == 0)\n            return max\n        val key =\"$i.$j\"\n        if (memo.containsKey(key))\n            return memo[key]!!\n\n        var maxSoFar = max\n\n        if (nums1[i] == nums1[j])\n            maxSoFar = dp(nums1, nums2, i + 1, j + 1, max + 1)\n\n        return maxSoFar\n    }\n\n    public static void $1(nums1: IntArray, nums2: IntArray) {\n        val m = nums1.size\n        val n = nums2.size\n        val dp = Array(m + 1) { IntArray(n + 1) }\n        var maxLength = 0\n\n        // Fill the dp array\n        for (i in 1..m) {\n            for (j in 1..n) {\n                if (nums1[i - 1] == nums2[j - 1]) {\n                    dp[i][j] = dp[i - 1][j - 1] + 1\n                    maxLength = maxOf(maxLength, dp[i][j])\n                } else {\n                    dp[i][j] = 0\n                }\n            }\n        }\n\n        return maxLength\n    }\n}"  python="class LongestCommonSubarray {\n    $1 = mutableMapOf<String, Int>()\n\n    def $1(nums1: list[int], nums2: list[int]): int {\n        return dp(nums1, nums2, nums1.lastIndex, nums2.lastIndex, Int.MIN_VALUE)\n    }\n\n    def $1(nums1: list[int], nums2: list[int], i: int, j: int, max: int): int {\n        if (i == 0 || j == 0)\n            return max\n        $1 = \"$i.$j\"\n        if (memo.containsKey(key))\n            return memo[key]!!\n\n        $1 = max\n\n        if (nums1[i] == nums1[j])\n            maxSoFar = dp(nums1, nums2, i + 1, j + 1, max + 1)\n\n        return maxSoFar\n    }\n\n    def $1(nums1: list[int], nums2: list[int]): int {\n        $1 = nums1.size\n        $1 = nums2.size\n        $1 = Array(m + 1) { list[int](n + 1) }\n        $1 = 0\n\n        // Fill the dp array\n        for (i in 1..m) {\n            for (j in 1..n) {\n                if (nums1[i - 1] == nums2[j - 1]) {\n                    dp[i][j] = dp[i - 1][j - 1] + 1\n                    maxLength = max(maxLength, dp[i][j])\n                } else {\n                    dp[i][j] = 0\n                }\n            }\n        }\n\n        return maxLength\n    }\n}"  rust="class LongestCommonSubarray {\n    val memo = mutableMapOf<String, Int>()\n\n    fn $1(nums1: Vec<i32>, nums2: Vec<i32>) -> i32 {\n        return dp(nums1, nums2, nums1.len() - 1, nums2.len() - 1, Int.MIN_VALUE)\n    }\n\n    fn $1(nums1: Vec<i32>, nums2: Vec<i32>, i -> i32, j -> i32, max -> i32) -> i32 {\n        if (i == 0 || j == 0)\n            return max\n        val key =\"$i.$j\"\n        if (memo.containsKey(key))\n            return memo[key]!!\n\n        var maxSoFar = max\n\n        if (nums1[i] == nums1[j])\n            maxSoFar = dp(nums1, nums2, i + 1, j + 1, max + 1)\n\n        return maxSoFar\n    }\n\n    fn $1(nums1: Vec<i32>, nums2: Vec<i32>) -> i32 {\n        val m = nums1.size\n        val n = nums2.size\n        val dp = Array(m + 1) { Vec<i32>(n + 1) }\n        var maxLength = 0\n\n        // Fill the dp array\n        for (i in 1..m) {\n            for (j in 1..n) {\n                if (nums1[i - 1] == nums2[j - 1]) {\n                    dp[i][j] = dp[i - 1][j - 1] + 1\n                    maxLength = max(maxLength, dp[i][j])\n                } else {\n                    dp[i][j] = 0\n                }\n            }\n        }\n\n        return maxLength\n    }\n}"  cpp="class LongestCommonSubarray {\n    auto $1 = mutableMapOf<String, Int>()\n\n    $1(nums1: vector<int>, nums2: vector<int>) {\n        return dp(nums1, nums2, nums1.size() - 1, nums2.size() - 1, Int.MIN_VALUE)\n    }\n\n    $1(nums1: vector<int>, nums2: vector<int>, i, j, max) {\n        if (i == 0 || j == 0)\n            return max\n        auto $1 = \"$i.$j\"\n        if (memo.containsKey(key))\n            return memo[key]!!\n\n        auto $1 = max\n\n        if (nums1[i] == nums1[j])\n            maxSoFar = dp(nums1, nums2, i + 1, j + 1, max + 1)\n\n        return maxSoFar\n    }\n\n    $1(nums1: vector<int>, nums2: vector<int>) {\n        auto $1 = nums1.size\n        auto $1 = nums2.size\n        auto $1 = Array(m + 1) { vector<int>(n + 1) }\n        auto $1 = 0\n\n        // Fill the dp array\n        for (i in 1..m) {\n            for (j in 1..n) {\n                if (nums1[i - 1] == nums2[j - 1]) {\n                    dp[i][j] = dp[i - 1][j - 1] + 1\n                    maxLength = max(maxLength, dp[i][j])\n                } else {\n                    dp[i][j] = 0\n                }\n            }\n        }\n\n        return maxLength\n    }\n}" %}

---

## LongestIncreasingSequenceInAMatrix

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/LongestIncreasingSequenceInAMatrix.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass LongestIncreasingSequenceInAMatrix {\n    private val dirs = arrayOf(0 to 1, 1 to 0, -1 to 0, 0 to -1)\n\n    fun longestIncreasingPath(matrix: Array<IntArray>): Int {\n        val (m, n) = matrix.size to matrix[0].size\n        val cache = Array(m) { IntArray(n) }\n\n        fun dfs(i: Int, j: Int): Int {\n            if (cache[i][j] != 0) return cache[i][j]\n\n            cache[i][j] = 1\n            for (delta in dirs) {\n                val (x, y) = i + delta.first to j + delta.second\n\n                if (x in matrix.indices && y in matrix[0].indices && matrix[x][y] > matrix[i][j]) {\n                    cache[i][j] = maxOf(cache[i][j], 1 + dfs( x, y))\n                }\n            }\n\n            return cache[i][j]\n        }\n\n        var max = 0\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                max = maxOf(max, dfs(i, j))\n            }\n        }\n\n        return max\n    }\n}"  java="class LongestIncreasingSequenceInAMatrix {\n    private val dirs = arrayOf(0 to 1, 1 to 0, -1 to 0, 0 to -1)\n\n    public static void $1(matrix: Array<IntArray>) {\n        val (m, n) = matrix.size to matrix[0].size\n        val cache = Array(m) { IntArray(n) }\n\n        public static void $1(i, j) {\n            if (cache[i][j] != 0) return cache[i][j]\n\n            cache[i][j] = 1\n            for (delta in dirs) {\n                val (x, y) = i + delta.first to j + delta.second\n\n                if (x in matrix.indices && y in matrix[0].indices && matrix[x][y] > matrix[i][j]) {\n                    cache[i][j] = maxOf(cache[i][j], 1 + dfs( x, y))\n                }\n            }\n\n            return cache[i][j]\n        }\n\n        var max = 0\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                max = maxOf(max, dfs(i, j))\n            }\n        }\n\n        return max\n    }\n}"  python="class LongestIncreasingSequenceInAMatrix {\n    private $1 = arrayOf(0 to 1, 1 to 0, -1 to 0, 0 to -1)\n\n    def $1(matrix: Array<list[int]>): int {\n        ($1, $2) = matrix.size to matrix[0].size\n        $1 = Array(m) { list[int](n) }\n\n        def $1(i: int, j: int): int {\n            if (cache[i][j] != 0) return cache[i][j]\n\n            cache[i][j] = 1\n            for (delta in dirs) {\n                ($1, $2) = i + delta.first to j + delta.second\n\n                if (x in matrix.indices && y in matrix[0].indices && matrix[x][y] > matrix[i][j]) {\n                    cache[i][j] = max(cache[i][j], 1 + dfs( x, y))\n                }\n            }\n\n            return cache[i][j]\n        }\n\n        $1 = 0\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                max = max(max, dfs(i, j))\n            }\n        }\n\n        return max\n    }\n}"  rust="class LongestIncreasingSequenceInAMatrix {\n    private val dirs = arrayOf(0 to 1, 1 to 0, -1 to 0, 0 to -1)\n\n    fn $1(matrix: Array<Vec<i32>>) -> i32 {\n        val (m, n) = matrix.size to matrix[0].size\n        val cache = Array(m) { Vec<i32>(n) }\n\n        fn $1(i -> i32, j -> i32) -> i32 {\n            if (cache[i][j] != 0) return cache[i][j]\n\n            cache[i][j] = 1\n            for (delta in dirs) {\n                val (x, y) = i + delta.first to j + delta.second\n\n                if (x in matrix.indices && y in matrix[0].indices && matrix[x][y] > matrix[i][j]) {\n                    cache[i][j] = max(cache[i][j], 1 + dfs( x, y))\n                }\n            }\n\n            return cache[i][j]\n        }\n\n        var max = 0\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                max = max(max, dfs(i, j))\n            }\n        }\n\n        return max\n    }\n}"  cpp="class LongestIncreasingSequenceInAMatrix {\n    private auto $1 = arrayOf(0 to 1, 1 to 0, -1 to 0, 0 to -1)\n\n    $1(matrix: Array<vector<int>>) {\n        val (m, n) = matrix.size to matrix[0].size\n        auto $1 = Array(m) { vector<int>(n) }\n\n        $1(i, j) {\n            if (cache[i][j] != 0) return cache[i][j]\n\n            cache[i][j] = 1\n            for (delta in dirs) {\n                val (x, y) = i + delta.first to j + delta.second\n\n                if (x in matrix.indices && y in matrix[0].indices && matrix[x][y] > matrix[i][j]) {\n                    cache[i][j] = max(cache[i][j], 1 + dfs( x, y))\n                }\n            }\n\n            return cache[i][j]\n        }\n\n        auto $1 = 0\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                max = max(max, dfs(i, j))\n            }\n        }\n\n        return max\n    }\n}" %}

---

## LongestIncreasingSubsequence

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/LongestIncreasingSubsequence.kt`

{% include code-tabs.html  kotlin="package tree.bst\n\nimport java.util.*\n\n// Patience sorting algorithm\nclass LongestIncreasingSubsequence {\n    fun lengthOfLIS(nums: IntArray): Int {\n        val tree = TreeSet<Int>()\n        nums.forEach { num ->\n            // Find the smallest element in the tree which is greater than or equal to num\n            tree.ceiling(num)?.also {\n                // If such an element is found, it means num can replace it to potentially\n                // form a new valid increasing subsequence or extend the current one\n                tree.remove(it)\n            }\n            // Add num to the tree. If num is not replacing any element,\n            // it is extending the subsequence with a new larger element.\n            tree.add(num)\n        }\n        // The size of the tree at the end will represent the length of the longest\n        // increasing subsequence\n        return tree.size\n    }\n}\n"  java="// Patience sorting algorithm\nclass LongestIncreasingSubsequence {\n    public static void $1(nums: IntArray) {\n        val tree = TreeSet<Int>()\n        nums.forEach { num ->\n            // Find the smallest element in the tree which is greater than or equal to num\n            tree.ceiling(num)?.also {\n                // If such an element is found, it means num can replace it to potentially\n                // form a new valid increasing subsequence or extend the current one\n                tree.remove(it)\n            }\n            // Add num to the tree. If num is not replacing any element,\n            // it is extending the subsequence with a new larger element.\n            tree.add(num)\n        }\n        // The size of the tree at the end will represent the length of the longest\n        // increasing subsequence\n        return tree.size\n    }\n}"  python="// Patience sorting algorithm\nclass LongestIncreasingSubsequence {\n    def $1(nums: list[int]): int {\n        $1 = TreeSet<Int>()\n        nums.forEach { num ->\n            // Find the smallest element in the tree which is greater than or equal to num\n            tree.ceiling(num)?.also {\n                // If such an element is found, it means num can replace it to potentially\n                // form a new valid increasing subsequence or extend the current one\n                tree.remove(it)\n            }\n            // Add num to the tree. If num is not replacing any element,\n            // it is extending the subsequence with a new larger element.\n            tree.add(num)\n        }\n        // The size of the tree at the end will represent the length of the longest\n        // increasing subsequence\n        return tree.size\n    }\n}"  rust="// Patience sorting algorithm\nclass LongestIncreasingSubsequence {\n    fn $1(nums: Vec<i32>) -> i32 {\n        val tree = TreeSet<Int>()\n        nums.forEach { num ->\n            // Find the smallest element in the tree which is greater than or equal to num\n            tree.ceiling(num)?.also {\n                // If such an element is found, it means num can replace it to potentially\n                // form a new valid increasing subsequence or extend the current one\n                tree.remove(it)\n            }\n            // Add num to the tree. If num is not replacing any element,\n            // it is extending the subsequence with a new larger element.\n            tree.add(num)\n        }\n        // The size of the tree at the end will represent the length of the longest\n        // increasing subsequence\n        return tree.size\n    }\n}"  cpp="// Patience sorting algorithm\nclass LongestIncreasingSubsequence {\n    $1(nums: vector<int>) {\n        auto $1 = TreeSet<Int>()\n        nums.forEach { num ->\n            // Find the smallest element in the tree which is greater than or equal to num\n            tree.ceiling(num)?.also {\n                // If such an element is found, it means num can replace it to potentially\n                // form a new valid increasing subsequence or extend the current one\n                tree.remove(it)\n            }\n            // Add num to the tree. If num is not replacing any element,\n            // it is extending the subsequence with a new larger element.\n            tree.add(num)\n        }\n        // The size of the tree at the end will represent the length of the longest\n        // increasing subsequence\n        return tree.size\n    }\n}" %}

---

## MaximalSquare

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/MaximalSquare.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass MaximalSquare {\n    fun maximalSquare(matrix: Array<CharArray>): Int {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return 0\n        val m = matrix.size\n        val n = matrix[0].size\n        val dp = Array(m) { IntArray(n) }\n        var maxSize = 0\n\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                if (matrix[i][j] == '1') {\n                    if (i == 0 || j == 0) {\n                        dp[i][j] = 1\n                    } else {\n                        dp[i][j] = minOf(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]) + 1\n                    }\n                    maxSize = maxOf(maxSize, dp[i][j])\n                }\n            }\n        }\n\n        return maxSize * maxSize\n    }\n}"  java="class MaximalSquare {\n    public static void $1(matrix: Array<CharArray>) {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return 0\n        val m = matrix.size\n        val n = matrix[0].size\n        val dp = Array(m) { IntArray(n) }\n        var maxSize = 0\n\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                if (matrix[i][j] == '1') {\n                    if (i == 0 || j == 0) {\n                        dp[i][j] = 1\n                    } else {\n                        dp[i][j] = minOf(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]) + 1\n                    }\n                    maxSize = maxOf(maxSize, dp[i][j])\n                }\n            }\n        }\n\n        return maxSize * maxSize\n    }\n}"  python="class MaximalSquare {\n    def $1(matrix: Array<CharArray>): int {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return 0\n        $1 = matrix.size\n        $1 = matrix[0].size\n        $1 = Array(m) { list[int](n) }\n        $1 = 0\n\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                if (matrix[i][j] == '1') {\n                    if (i == 0 || j == 0) {\n                        dp[i][j] = 1\n                    } else {\n                        dp[i][j] = min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]) + 1\n                    }\n                    maxSize = max(maxSize, dp[i][j])\n                }\n            }\n        }\n\n        return maxSize * maxSize\n    }\n}"  rust="class MaximalSquare {\n    fn $1(matrix: Array<CharArray>) -> i32 {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return 0\n        val m = matrix.size\n        val n = matrix[0].size\n        val dp = Array(m) { Vec<i32>(n) }\n        var maxSize = 0\n\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                if (matrix[i][j] == '1') {\n                    if (i == 0 || j == 0) {\n                        dp[i][j] = 1\n                    } else {\n                        dp[i][j] = min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]) + 1\n                    }\n                    maxSize = max(maxSize, dp[i][j])\n                }\n            }\n        }\n\n        return maxSize * maxSize\n    }\n}"  cpp="class MaximalSquare {\n    $1(matrix: Array<CharArray>) {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return 0\n        auto $1 = matrix.size\n        auto $1 = matrix[0].size\n        auto $1 = Array(m) { vector<int>(n) }\n        auto $1 = 0\n\n        for (i in 0 until m) {\n            for (j in 0 until n) {\n                if (matrix[i][j] == '1') {\n                    if (i == 0 || j == 0) {\n                        dp[i][j] = 1\n                    } else {\n                        dp[i][j] = min(dp[i - 1][j], dp[i][j - 1], dp[i - 1][j - 1]) + 1\n                    }\n                    maxSize = max(maxSize, dp[i][j])\n                }\n            }\n        }\n\n        return maxSize * maxSize\n    }\n}" %}

---

## MaximumSumSubArray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/MaximumSumSubArray.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass MaximumSumSubArray {\n    // Kaden's Algorithm Dynamic Programming\n    fun maxSubArray(nums: IntArray): Int {\n        if (nums.isEmpty())\n            return 0\n        var (maxSum, currentSum) = listOf( nums[0], nums[0] )\n\n\n        for (i in 1 until nums.size) {\n            currentSum = maxOf(nums[i], currentSum + nums[i])\n            maxSum = maxOf(maxSum, currentSum)\n        }\n\n        return maxSum\n    }\n}"  java="class MaximumSumSubArray {\n    // Kaden's Algorithm Dynamic Programming\n    public static void $1(nums: IntArray) {\n        if (nums.isEmpty())\n            return 0\n        var (maxSum, currentSum) = listOf( nums[0], nums[0] )\n\n\n        for (i in 1 until nums.size) {\n            currentSum = maxOf(nums[i], currentSum + nums[i])\n            maxSum = maxOf(maxSum, currentSum)\n        }\n\n        return maxSum\n    }\n}"  python="class MaximumSumSubArray {\n    // Kaden's Algorithm Dynamic Programming\n    def $1(nums: list[int]): int {\n        if (nums.isEmpty())\n            return 0\n        var (maxSum, currentSum) = listOf( nums[0], nums[0] )\n\n\n        for (i in 1 until nums.size) {\n            currentSum = max(nums[i], currentSum + nums[i])\n            maxSum = max(maxSum, currentSum)\n        }\n\n        return maxSum\n    }\n}"  rust="class MaximumSumSubArray {\n    // Kaden's Algorithm Dynamic Programming\n    fn $1(nums: Vec<i32>) -> i32 {\n        if (nums.isEmpty())\n            return 0\n        var (maxSum, currentSum) = listOf( nums[0], nums[0] )\n\n\n        for (i in 1 until nums.size) {\n            currentSum = max(nums[i], currentSum + nums[i])\n            maxSum = max(maxSum, currentSum)\n        }\n\n        return maxSum\n    }\n}"  cpp="class MaximumSumSubArray {\n    // Kaden's Algorithm Dynamic Programming\n    $1(nums: vector<int>) {\n        if (nums.isEmpty())\n            return 0\n        var (maxSum, currentSum) = listOf( nums[0], nums[0] )\n\n\n        for (i in 1 until nums.size) {\n            currentSum = max(nums[i], currentSum + nums[i])\n            maxSum = max(maxSum, currentSum)\n        }\n\n        return maxSum\n    }\n}" %}

---

## MinCostClimbingStaris

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/MinCostClimbingStaris.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass MinCostClimbingStaris {\n    fun minCostClimbingStairs_iterative(cost: IntArray): Int {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        val dp = IntArray(cost.size)\n\n        // Base cases\n        dp[0] = cost[0]\n        dp[1] = cost[1]\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            dp[i] = cost[i] + minOf(dp[i - 1], dp[i - 2])\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return minOf(dp[cost.size - 1], dp[cost.size - 2])\n    }\n\n    /**\n     * Constant space iterative approach\n     */\n    fun minCostClimbingStairs_iterative_constant_space(cost: IntArray): Int {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        val dp = IntArray(cost.size)\n\n        // Base cases\n        var prev2 = cost[0] // Previous 2 steps\n        var prev1 = cost[1]  // Previous 1 steps\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            val cur = cost[i] + minOf(prev2, prev1)\n\n            prev2 = prev1\n            prev1 = cur\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return minOf(prev2, prev1)\n    }\n\n\n    /**\n     * Top Down Dynamic Programming Approach\n     */\n    fun minCostClimbingStairs(cost: IntArray): Int {\n        val memo = IntArray(cost.size) { -1 }\n        val n = cost.size\n\n        // We can start from step 0 or step 1, hence we take the minimum of both\n        return minOf(minCost(cost, n - 1, memo), minCost(cost, n - 2, memo))\n    }\n    private fun minCost(cost: IntArray, i: Int, memo: IntArray): Int {\n        // Base cases\n        if (i < 0) return 0\n        if (i == 0 || i == 1) return cost[i]\n\n        // If already calculated, return the stored result\n        if (memo[i] != -1) return memo[i]\n\n        // Recursive calculation with memoization\n        memo[i] = cost[i] + minOf(minCost(cost, i - 1, memo), minCost(cost, i - 2, memo))\n        return memo[i]\n    }\n    /** End of top down Dynamic Programming approach **/\n}"  java="class MinCostClimbingStaris {\n    public static void $1(cost: IntArray) {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        val dp = IntArray(cost.size)\n\n        // Base cases\n        dp[0] = cost[0]\n        dp[1] = cost[1]\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            dp[i] = cost[i] + minOf(dp[i - 1], dp[i - 2])\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return minOf(dp[cost.size - 1], dp[cost.size - 2])\n    }\n\n    /**\n     * Constant space iterative approach\n     */\n    public static void $1(cost: IntArray) {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        val dp = IntArray(cost.size)\n\n        // Base cases\n        var prev2 = cost[0] // Previous 2 steps\n        var prev1 = cost[1]  // Previous 1 steps\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            val cur = cost[i] + minOf(prev2, prev1)\n\n            prev2 = prev1\n            prev1 = cur\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return minOf(prev2, prev1)\n    }\n\n\n    /**\n     * Top Down Dynamic Programming Approach\n     */\n    public static void $1(cost: IntArray) {\n        val memo = IntArray(cost.size) { -1 }\n        val n = cost.size\n\n        // We can start from step 0 or step 1, hence we take the minimum of both\n        return minOf(minCost(cost, n - 1, memo), minCost(cost, n - 2, memo))\n    }\n    private public static void $1(cost: IntArray, i, memo: IntArray) {\n        // Base cases\n        if (i < 0) return 0\n        if (i == 0 || i == 1) return cost[i]\n\n        // If already calculated, return the stored result\n        if (memo[i] != -1) return memo[i]\n\n        // Recursive calculation with memoization\n        memo[i] = cost[i] + minOf(minCost(cost, i - 1, memo), minCost(cost, i - 2, memo))\n        return memo[i]\n    }\n    /** End of top down Dynamic Programming approach **/\n}"  python="class MinCostClimbingStaris {\n    def $1(cost: list[int]): int {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        $1 = list[int](cost.size)\n\n        // Base cases\n        dp[0] = cost[0]\n        dp[1] = cost[1]\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            dp[i] = cost[i] + min(dp[i - 1], dp[i - 2])\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return min(dp[cost.size - 1], dp[cost.size - 2])\n    }\n\n    /**\n     * Constant space iterative approach\n     */\n    def $1(cost: list[int]): int {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        $1 = list[int](cost.size)\n\n        // Base cases\n        $1 = cost[0] // Previous 2 steps\n        $1 = cost[1]  // Previous 1 steps\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            $1 = cost[i] + min(prev2, prev1)\n\n            prev2 = prev1\n            prev1 = cur\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return min(prev2, prev1)\n    }\n\n\n    /**\n     * Top Down Dynamic Programming Approach\n     */\n    def $1(cost: list[int]): int {\n        $1 = list[int](cost.size) { -1 }\n        $1 = cost.size\n\n        // We can start from step 0 or step 1, hence we take the minimum of both\n        return min(minCost(cost, n - 1, memo), minCost(cost, n - 2, memo))\n    }\n    private def $1(cost: list[int], i: int, memo: list[int]): int {\n        // Base cases\n        if (i < 0) return 0\n        if (i == 0 || i == 1) return cost[i]\n\n        // If already calculated, return the stored result\n        if (memo[i] != -1) return memo[i]\n\n        // Recursive calculation with memoization\n        memo[i] = cost[i] + min(minCost(cost, i - 1, memo), minCost(cost, i - 2, memo))\n        return memo[i]\n    }\n    /** End of top down Dynamic Programming approach **/\n}"  rust="class MinCostClimbingStaris {\n    fn $1(cost: Vec<i32>) -> i32 {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        val dp = Vec<i32>(cost.size)\n\n        // Base cases\n        dp[0] = cost[0]\n        dp[1] = cost[1]\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            dp[i] = cost[i] + min(dp[i - 1], dp[i - 2])\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return min(dp[cost.size - 1], dp[cost.size - 2])\n    }\n\n    /**\n     * Constant space iterative approach\n     */\n    fn $1(cost: Vec<i32>) -> i32 {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        val dp = Vec<i32>(cost.size)\n\n        // Base cases\n        var prev2 = cost[0] // Previous 2 steps\n        var prev1 = cost[1]  // Previous 1 steps\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            val cur = cost[i] + min(prev2, prev1)\n\n            prev2 = prev1\n            prev1 = cur\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return min(prev2, prev1)\n    }\n\n\n    /**\n     * Top Down Dynamic Programming Approach\n     */\n    fn $1(cost: Vec<i32>) -> i32 {\n        val memo = Vec<i32>(cost.size) { -1 }\n        val n = cost.size\n\n        // We can start from step 0 or step 1, hence we take the minimum of both\n        return min(minCost(cost, n - 1, memo), minCost(cost, n - 2, memo))\n    }\n    private fn $1(cost: Vec<i32>, i -> i32, memo: Vec<i32>) -> i32 {\n        // Base cases\n        if (i < 0) return 0\n        if (i == 0 || i == 1) return cost[i]\n\n        // If already calculated, return the stored result\n        if (memo[i] != -1) return memo[i]\n\n        // Recursive calculation with memoization\n        memo[i] = cost[i] + min(minCost(cost, i - 1, memo), minCost(cost, i - 2, memo))\n        return memo[i]\n    }\n    /** End of top down Dynamic Programming approach **/\n}"  cpp="class MinCostClimbingStaris {\n    $1(cost: vector<int>) {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        auto $1 = vector<int>(cost.size)\n\n        // Base cases\n        dp[0] = cost[0]\n        dp[1] = cost[1]\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            dp[i] = cost[i] + min(dp[i - 1], dp[i - 2])\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return min(dp[cost.size - 1], dp[cost.size - 2])\n    }\n\n    /**\n     * Constant space iterative approach\n     */\n    $1(cost: vector<int>) {\n        // Handle base cases\n        if (cost.size <= 2) return cost.min()\n\n        // Initialize the dp array with the size of the cost array\n        auto $1 = vector<int>(cost.size)\n\n        // Base cases\n        auto $1 = cost[0] // Previous 2 steps\n        auto $1 = cost[1]  // Previous 1 steps\n\n        // Fill the dp array\n        for (i in 2 until cost.size) {\n            auto $1 = cost[i] + min(prev2, prev1)\n\n            prev2 = prev1\n            prev1 = cur\n        }\n\n        // The result is the minimum cost to reach the last or the second-to-last step\n        return min(prev2, prev1)\n    }\n\n\n    /**\n     * Top Down Dynamic Programming Approach\n     */\n    $1(cost: vector<int>) {\n        auto $1 = vector<int>(cost.size) { -1 }\n        auto $1 = cost.size\n\n        // We can start from step 0 or step 1, hence we take the minimum of both\n        return min(minCost(cost, n - 1, memo), minCost(cost, n - 2, memo))\n    }\n    private $1(cost: vector<int>, i, memo: vector<int>) {\n        // Base cases\n        if (i < 0) return 0\n        if (i == 0 || i == 1) return cost[i]\n\n        // If already calculated, return the stored result\n        if (memo[i] != -1) return memo[i]\n\n        // Recursive calculation with memoization\n        memo[i] = cost[i] + min(minCost(cost, i - 1, memo), minCost(cost, i - 2, memo))\n        return memo[i]\n    }\n    /** End of top down Dynamic Programming approach **/\n}" %}

---

## MinimumNumberofIncrementsSubarraysFormaTargetArray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/MinimumNumberofIncrementsSubarraysFormaTargetArray.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass MinimumNumberofIncrementsSubarraysFormaTargetArray {\n    fun minNumberOperations(target: IntArray): Int {\n        var operations = target[0]\n\n        for (i in 1..target.lastIndex) {\n            operations += maxOf(0, target[i] - target[i-1])\n        }\n\n        return operations\n    }\n}\n"  java="class MinimumNumberofIncrementsSubarraysFormaTargetArray {\n    public static void $1(target: IntArray) {\n        var operations = target[0]\n\n        for (i in 1..target.lastIndex) {\n            operations += maxOf(0, target[i] - target[i-1])\n        }\n\n        return operations\n    }\n}"  python="class MinimumNumberofIncrementsSubarraysFormaTargetArray {\n    def $1(target: list[int]): int {\n        $1 = target[0]\n\n        for (i in 1..target.lastIndex) {\n            operations += max(0, target[i] - target[i-1])\n        }\n\n        return operations\n    }\n}"  rust="class MinimumNumberofIncrementsSubarraysFormaTargetArray {\n    fn $1(target: Vec<i32>) -> i32 {\n        var operations = target[0]\n\n        for (i in 1..target.len() - 1) {\n            operations += max(0, target[i] - target[i-1])\n        }\n\n        return operations\n    }\n}"  cpp="class MinimumNumberofIncrementsSubarraysFormaTargetArray {\n    $1(target: vector<int>) {\n        auto $1 = target[0]\n\n        for (i in 1..target.size() - 1) {\n            operations += max(0, target[i] - target[i-1])\n        }\n\n        return operations\n    }\n}" %}

---

## MinimumPathSum

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/MinimumPathSum.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass MinimumPathSum {\n    fun minPathSum(grid: Array<IntArray>?): Int {\n        if (grid.isNullOrEmpty()) return 0\n\n        val dp = Array(grid.size) { IntArray(grid[0].size) }\n\n        for (i in grid.indices) {\n            for (j in 0 until grid[i].size) {\n                dp[i][j] += grid[i][j]\n                dp[i][j] += when {\n                    i > 0 && j > 0 -> minOf(dp[i - 1][j], dp[i][j - 1])\n                    i > 0 -> dp[i - 1][j]\n                    j > 0 -> dp[i][j - 1]\n                    else -> 0\n                }\n            }\n        }\n        return dp[grid.size - 1][grid[0].size - 1]\n    }\n}"  java="class MinimumPathSum {\n    public static void $1(grid: Array<IntArray>?) {\n        if (grid.isNullOrEmpty()) return 0\n\n        val dp = Array(grid.size) { IntArray(grid[0].size) }\n\n        for (i in grid.indices) {\n            for (j in 0 until grid[i].size) {\n                dp[i][j] += grid[i][j]\n                dp[i][j] += when {\n                    i > 0 && j > 0 -> minOf(dp[i - 1][j], dp[i][j - 1])\n                    i > 0 -> dp[i - 1][j]\n                    j > 0 -> dp[i][j - 1]\n                    else -> 0\n                }\n            }\n        }\n        return dp[grid.size - 1][grid[0].size - 1]\n    }\n}"  python="class MinimumPathSum {\n    def $1(grid: Array<list[int]>?): int {\n        if (grid.isNullOrEmpty()) return 0\n\n        $1 = Array(grid.size) { list[int](grid[0].size) }\n\n        for (i in grid.indices) {\n            for (j in 0 until grid[i].size) {\n                dp[i][j] += grid[i][j]\n                dp[i][j] += when {\n                    i > 0 && j > 0 -> min(dp[i - 1][j], dp[i][j - 1])\n                    i > 0 -> dp[i - 1][j]\n                    j > 0 -> dp[i][j - 1]\n                    else -> 0\n                }\n            }\n        }\n        return dp[grid.size - 1][grid[0].size - 1]\n    }\n}"  rust="class MinimumPathSum {\n    fn $1(grid: Array<Vec<i32>>?) -> i32 {\n        if (grid.isNullOrEmpty()) return 0\n\n        val dp = Array(grid.size) { Vec<i32>(grid[0].size) }\n\n        for (i in grid.indices) {\n            for (j in 0 until grid[i].size) {\n                dp[i][j] += grid[i][j]\n                dp[i][j] += when {\n                    i > 0 && j > 0 -> min(dp[i - 1][j], dp[i][j - 1])\n                    i > 0 -> dp[i - 1][j]\n                    j > 0 -> dp[i][j - 1]\n                    else -> 0\n                }\n            }\n        }\n        return dp[grid.size - 1][grid[0].size - 1]\n    }\n}"  cpp="class MinimumPathSum {\n    $1(grid: Array<vector<int>>?) {\n        if (grid.isNullOrEmpty()) return 0\n\n        auto $1 = Array(grid.size) { vector<int>(grid[0].size) }\n\n        for (i in grid.indices) {\n            for (j in 0 until grid[i].size) {\n                dp[i][j] += grid[i][j]\n                dp[i][j] += when {\n                    i > 0 && j > 0 -> min(dp[i - 1][j], dp[i][j - 1])\n                    i > 0 -> dp[i - 1][j]\n                    j > 0 -> dp[i][j - 1]\n                    else -> 0\n                }\n            }\n        }\n        return dp[grid.size - 1][grid[0].size - 1]\n    }\n}" %}

---

## PartitionArrayIntoTwoArrayToMinimuzeSumDifference

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/PartitionArrayIntoTwoArrayToMinimuzeSumDifference.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nimport kotlin.math.abs\n\nclass PartitionArrayIntoTwoArrayToMinimuzeSumDifference {\n    fun minimumDifference(nums: IntArray): Int {\n        val n = nums.size\n        val totalSum = nums.sum()\n        val halfSize = n / 2\n\n        val leftSubsets = generateSubsetsBySize(nums, 0, halfSize)\n        val rightSubsets = generateSubsetsBySize(nums, halfSize, n)\n\n        var minDiff = Int.MAX_VALUE\n\n        for (k in 0..halfSize) {\n            val leftSums = leftSubsets[k] ?: continue\n            val rightSums = rightSubsets[halfSize - k] ?: continue\n\n            rightSums.sort()\n\n            for (leftSum in leftSums) {\n                val target = (totalSum - 2 * leftSum) / 2\n                val closestRightSums = findClosestValues(rightSums, target)\n\n                closestRightSums.forEach { rightSum ->\n                    val currentDiff = abs(totalSum - 2 * (leftSum + rightSum))\n                    minDiff = minOf(minDiff, currentDiff)\n                    if (minDiff == 0) return 0  // Early exit if perfect match found\n                }\n            }\n        }\n\n        return minDiff\n    }\n\n    private fun generateSubsetsBySize(nums: IntArray, start: Int, end: Int): Array<MutableList<Int>?> {\n        val size = end - start\n        val subsets = Array<MutableList<Int>?>(size + 1) { mutableListOf() }\n\n        (0 until (1 shl size)).forEach { mask ->\n            var sum = 0\n            var count = 0\n            for (i in 0 until size) {\n                if (mask and (1 shl i) != 0) {\n                    sum += nums[start + i]\n                    count++\n                }\n            }\n            subsets[count]?.add(sum)\n        }\n\n        return subsets\n    }\n\n    private fun findClosestValues(sortedList: List<Int>, target: Int): List<Int> {\n        val index = sortedList.binarySearch(target)\n        if (index >= 0) return listOf(sortedList[index])  // Exact match\n\n        val insertPos = -index - 1\n        return buildList {\n            if (insertPos < sortedList.size) add(sortedList[insertPos])\n            if (insertPos > 0) add(sortedList[insertPos - 1])\n        }.distinct()  // Remove duplicates if adjacent values are equal\n    }\n}"  java="class PartitionArrayIntoTwoArrayToMinimuzeSumDifference {\n    public static void $1(nums: IntArray) {\n        val n = nums.size\n        val totalSum = nums.sum()\n        val halfSize = n / 2\n\n        val leftSubsets = generateSubsetsBySize(nums, 0, halfSize)\n        val rightSubsets = generateSubsetsBySize(nums, halfSize, n)\n\n        var minDiff = Int.MAX_VALUE\n\n        for (k in 0..halfSize) {\n            val leftSums = leftSubsets[k] ?: continue\n            val rightSums = rightSubsets[halfSize - k] ?: continue\n\n            rightSums.sort()\n\n            for (leftSum in leftSums) {\n                val target = (totalSum - 2 * leftSum) / 2\n                val closestRightSums = findClosestValues(rightSums, target)\n\n                closestRightSums.forEach { rightSum ->\n                    val currentDiff = abs(totalSum - 2 * (leftSum + rightSum))\n                    minDiff = minOf(minDiff, currentDiff)\n                    if (minDiff == 0) return 0  // Early exit if perfect match found\n                }\n            }\n        }\n\n        return minDiff\n    }\n\n    private public static void $1(nums: IntArray, start, end): Array<MutableList<Int>?> {\n        val size = end - start\n        val subsets = Array<MutableList<Int>?>(size + 1) { mutableListOf() }\n\n        (0 until (1 shl size)).forEach { mask ->\n            var sum = 0\n            var count = 0\n            for (i in 0 until size) {\n                if (mask and (1 shl i) != 0) {\n                    sum += nums[start + i]\n                    count++\n                }\n            }\n            subsets[count]?.add(sum)\n        }\n\n        return subsets\n    }\n\n    private public static void $1(sortedList: List<Int>, target): List<Int> {\n        val index = sortedList.binarySearch(target)\n        if (index >= 0) return listOf(sortedList[index])  // Exact match\n\n        val insertPos = -index - 1\n        return buildList {\n            if (insertPos < sortedList.size) add(sortedList[insertPos])\n            if (insertPos > 0) add(sortedList[insertPos - 1])\n        }.distinct()  // Remove duplicates if adjacent values are equal\n    }\n}"  python="class PartitionArrayIntoTwoArrayToMinimuzeSumDifference {\n    def $1(nums: list[int]): int {\n        $1 = nums.size\n        $1 = nums.sum()\n        $1 = n / 2\n\n        $1 = generateSubsetsBySize(nums, 0, halfSize)\n        $1 = generateSubsetsBySize(nums, halfSize, n)\n\n        $1 = Int.MAX_VALUE\n\n        for (k in 0..halfSize) {\n            $1 = leftSubsets[k] ?: continue\n            $1 = rightSubsets[halfSize - k] ?: continue\n\n            rightSums.sort()\n\n            for (leftSum in leftSums) {\n                $1 = (totalSum - 2 * leftSum) / 2\n                $1 = findClosestValues(rightSums, target)\n\n                closestRightSums.forEach { rightSum ->\n                    $1 = abs(totalSum - 2 * (leftSum + rightSum))\n                    minDiff = min(minDiff, currentDiff)\n                    if (minDiff == 0) return 0  // Early exit if perfect match found\n                }\n            }\n        }\n\n        return minDiff\n    }\n\n    private def $1(nums: list[int], start: int, end: int): Array<MutableList<Int>?> {\n        $1 = end - start\n        $1 = Array<MutableList<Int>?>(size + 1) { mutableListOf() }\n\n        (0 until (1 shl size)).forEach { mask ->\n            $1 = 0\n            $1 = 0\n            for (i in 0 until size) {\n                if (mask and (1 shl i) != 0) {\n                    sum += nums[start + i]\n                    count++\n                }\n            }\n            subsets[count]?.add(sum)\n        }\n\n        return subsets\n    }\n\n    private def $1(sortedList: List<Int>, target: int): List<Int> {\n        $1 = sortedList.binarySearch(target)\n        if (index >= 0) return listOf(sortedList[index])  // Exact match\n\n        $1 = -index - 1\n        return buildList {\n            if (insertPos < sortedList.size) add(sortedList[insertPos])\n            if (insertPos > 0) add(sortedList[insertPos - 1])\n        }.distinct()  // Remove duplicates if adjacent values are equal\n    }\n}"  rust="class PartitionArrayIntoTwoArrayToMinimuzeSumDifference {\n    fn $1(nums: Vec<i32>) -> i32 {\n        val n = nums.size\n        val totalSum = nums.sum()\n        val halfSize = n / 2\n\n        val leftSubsets = generateSubsetsBySize(nums, 0, halfSize)\n        val rightSubsets = generateSubsetsBySize(nums, halfSize, n)\n\n        var minDiff = Int.MAX_VALUE\n\n        for (k in 0..halfSize) {\n            val leftSums = leftSubsets[k] ?: continue\n            val rightSums = rightSubsets[halfSize - k] ?: continue\n\n            rightSums.sort()\n\n            for (leftSum in leftSums) {\n                val target = (totalSum - 2 * leftSum) / 2\n                val closestRightSums = findClosestValues(rightSums, target)\n\n                closestRightSums.forEach { rightSum ->\n                    val currentDiff = abs(totalSum - 2 * (leftSum + rightSum))\n                    minDiff = min(minDiff, currentDiff)\n                    if (minDiff == 0) return 0  // Early exit if perfect match found\n                }\n            }\n        }\n\n        return minDiff\n    }\n\n    private fn $1(nums: Vec<i32>, start -> i32, end -> i32): Array<MutableList<Int>?> {\n        val size = end - start\n        val subsets = Array<MutableList<Int>?>(size + 1) { mutableListOf() }\n\n        (0 until (1 shl size)).forEach { mask ->\n            var sum = 0\n            var count = 0\n            for (i in 0 until size) {\n                if (mask and (1 shl i) != 0) {\n                    sum += nums[start + i]\n                    count++\n                }\n            }\n            subsets[count]?.add(sum)\n        }\n\n        return subsets\n    }\n\n    private fn $1(sortedList: List<Int>, target -> i32): List<Int> {\n        val index = sortedList.binarySearch(target)\n        if (index >= 0) return listOf(sortedList[index])  // Exact match\n\n        val insertPos = -index - 1\n        return buildList {\n            if (insertPos < sortedList.size) add(sortedList[insertPos])\n            if (insertPos > 0) add(sortedList[insertPos - 1])\n        }.distinct()  // Remove duplicates if adjacent values are equal\n    }\n}"  cpp="class PartitionArrayIntoTwoArrayToMinimuzeSumDifference {\n    $1(nums: vector<int>) {\n        auto $1 = nums.size\n        auto $1 = nums.sum()\n        auto $1 = n / 2\n\n        auto $1 = generateSubsetsBySize(nums, 0, halfSize)\n        auto $1 = generateSubsetsBySize(nums, halfSize, n)\n\n        auto $1 = Int.MAX_VALUE\n\n        for (k in 0..halfSize) {\n            auto $1 = leftSubsets[k] ?: continue\n            auto $1 = rightSubsets[halfSize - k] ?: continue\n\n            rightSums.sort()\n\n            for (leftSum in leftSums) {\n                auto $1 = (totalSum - 2 * leftSum) / 2\n                auto $1 = findClosestValues(rightSums, target)\n\n                closestRightSums.forEach { rightSum ->\n                    auto $1 = abs(totalSum - 2 * (leftSum + rightSum))\n                    minDiff = min(minDiff, currentDiff)\n                    if (minDiff == 0) return 0  // Early exit if perfect match found\n                }\n            }\n        }\n\n        return minDiff\n    }\n\n    private $1(nums: vector<int>, start, end): Array<MutableList<Int>?> {\n        auto $1 = end - start\n        auto $1 = Array<MutableList<Int>?>(size + 1) { mutableListOf() }\n\n        (0 until (1 shl size)).forEach { mask ->\n            auto $1 = 0\n            auto $1 = 0\n            for (i in 0 until size) {\n                if (mask and (1 shl i) != 0) {\n                    sum += nums[start + i]\n                    count++\n                }\n            }\n            subsets[count]?.add(sum)\n        }\n\n        return subsets\n    }\n\n    private $1(sortedList: List<Int>, target): List<Int> {\n        auto $1 = sortedList.binarySearch(target)\n        if (index >= 0) return listOf(sortedList[index])  // Exact match\n\n        auto $1 = -index - 1\n        return buildList {\n            if (insertPos < sortedList.size) add(sortedList[insertPos])\n            if (insertPos > 0) add(sortedList[insertPos - 1])\n        }.distinct()  // Remove duplicates if adjacent values are equal\n    }\n}" %}

---

## SplitArrayLargestSum

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/SplitArrayLargestSum.kt`

{% include code-tabs.html  kotlin="package array.dp\n\nclass SplitArrayLargestSum {\n    fun splitArray(nums: IntArray, k: Int): Int {\n        val n = nums.size\n        val prefixSum = IntArray(n + 1)\n        for (i in nums.indices) {\n            prefixSum[i + 1] = prefixSum[i] + nums[i]\n        }\n\n        val memo = mutableMapOf<Pair<Int, Int>, Int>()\n\n        fun dfs(i: Int, splitsLeft: Int): Int {\n            if (splitsLeft == 1) return prefixSum[n] - prefixSum[i] // Last split takes remaining sum\n            if (i == n) return Int.MAX_VALUE\n            if (memo.containsKey(i to splitsLeft)) return memo[i to splitsLeft]!!\n\n            var minLargestSum = Int.MAX_VALUE\n\n            for (j in i until n) {\n                val currentSum = prefixSum[j + 1] - prefixSum[i]\n                val largestInRemainingSplits = dfs(j + 1, splitsLeft - 1)\n                val maxSplitSum = maxOf(currentSum, largestInRemainingSplits)\n\n                minLargestSum = minOf(minLargestSum, maxSplitSum)\n\n                // Prune unnecessary recursion\n                if (currentSum > minLargestSum) break\n            }\n\n            memo[i to splitsLeft] = minLargestSum\n            return minLargestSum\n        }\n\n        return dfs(0, k)\n    }\n}\n"  java="class SplitArrayLargestSum {\n    public static void $1(nums: IntArray, k) {\n        val n = nums.size\n        val prefixSum = IntArray(n + 1)\n        for (i in nums.indices) {\n            prefixSum[i + 1] = prefixSum[i] + nums[i]\n        }\n\n        val memo = mutableMapOf<Pair<Int, Int>, Int>()\n\n        public static void $1(i, splitsLeft) {\n            if (splitsLeft == 1) return prefixSum[n] - prefixSum[i] // Last split takes remaining sum\n            if (i == n) return Int.MAX_VALUE\n            if (memo.containsKey(i to splitsLeft)) return memo[i to splitsLeft]!!\n\n            var minLargestSum = Int.MAX_VALUE\n\n            for (j in i until n) {\n                val currentSum = prefixSum[j + 1] - prefixSum[i]\n                val largestInRemainingSplits = dfs(j + 1, splitsLeft - 1)\n                val maxSplitSum = maxOf(currentSum, largestInRemainingSplits)\n\n                minLargestSum = minOf(minLargestSum, maxSplitSum)\n\n                // Prune unnecessary recursion\n                if (currentSum > minLargestSum) break\n            }\n\n            memo[i to splitsLeft] = minLargestSum\n            return minLargestSum\n        }\n\n        return dfs(0, k)\n    }\n}"  python="class SplitArrayLargestSum {\n    def $1(nums: list[int], k: int): int {\n        $1 = nums.size\n        $1 = list[int](n + 1)\n        for (i in nums.indices) {\n            prefixSum[i + 1] = prefixSum[i] + nums[i]\n        }\n\n        $1 = mutableMapOf<Pair<Int, Int>, Int>()\n\n        def $1(i: int, splitsLeft: int): int {\n            if (splitsLeft == 1) return prefixSum[n] - prefixSum[i] // Last split takes remaining sum\n            if (i == n) return Int.MAX_VALUE\n            if (memo.containsKey(i to splitsLeft)) return memo[i to splitsLeft]!!\n\n            $1 = Int.MAX_VALUE\n\n            for (j in i until n) {\n                $1 = prefixSum[j + 1] - prefixSum[i]\n                $1 = dfs(j + 1, splitsLeft - 1)\n                $1 = max(currentSum, largestInRemainingSplits)\n\n                minLargestSum = min(minLargestSum, maxSplitSum)\n\n                // Prune unnecessary recursion\n                if (currentSum > minLargestSum) break\n            }\n\n            memo[i to splitsLeft] = minLargestSum\n            return minLargestSum\n        }\n\n        return dfs(0, k)\n    }\n}"  rust="class SplitArrayLargestSum {\n    fn $1(nums: Vec<i32>, k -> i32) -> i32 {\n        val n = nums.size\n        val prefixSum = Vec<i32>(n + 1)\n        for (i in nums.indices) {\n            prefixSum[i + 1] = prefixSum[i] + nums[i]\n        }\n\n        val memo = mutableMapOf<Pair<Int, Int>, Int>()\n\n        fn $1(i -> i32, splitsLeft -> i32) -> i32 {\n            if (splitsLeft == 1) return prefixSum[n] - prefixSum[i] // Last split takes remaining sum\n            if (i == n) return Int.MAX_VALUE\n            if (memo.containsKey(i to splitsLeft)) return memo[i to splitsLeft]!!\n\n            var minLargestSum = Int.MAX_VALUE\n\n            for (j in i until n) {\n                val currentSum = prefixSum[j + 1] - prefixSum[i]\n                val largestInRemainingSplits = dfs(j + 1, splitsLeft - 1)\n                val maxSplitSum = max(currentSum, largestInRemainingSplits)\n\n                minLargestSum = min(minLargestSum, maxSplitSum)\n\n                // Prune unnecessary recursion\n                if (currentSum > minLargestSum) break\n            }\n\n            memo[i to splitsLeft] = minLargestSum\n            return minLargestSum\n        }\n\n        return dfs(0, k)\n    }\n}"  cpp="class SplitArrayLargestSum {\n    $1(nums: vector<int>, k) {\n        auto $1 = nums.size\n        auto $1 = vector<int>(n + 1)\n        for (i in nums.indices) {\n            prefixSum[i + 1] = prefixSum[i] + nums[i]\n        }\n\n        auto $1 = mutableMapOf<Pair<Int, Int>, Int>()\n\n        $1(i, splitsLeft) {\n            if (splitsLeft == 1) return prefixSum[n] - prefixSum[i] // Last split takes remaining sum\n            if (i == n) return Int.MAX_VALUE\n            if (memo.containsKey(i to splitsLeft)) return memo[i to splitsLeft]!!\n\n            auto $1 = Int.MAX_VALUE\n\n            for (j in i until n) {\n                auto $1 = prefixSum[j + 1] - prefixSum[i]\n                auto $1 = dfs(j + 1, splitsLeft - 1)\n                auto $1 = max(currentSum, largestInRemainingSplits)\n\n                minLargestSum = min(minLargestSum, maxSplitSum)\n\n                // Prune unnecessary recursion\n                if (currentSum > minLargestSum) break\n            }\n\n            memo[i to splitsLeft] = minLargestSum\n            return minLargestSum\n        }\n\n        return dfs(0, k)\n    }\n}" %}

---

## TargetSum

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/array/dp/TargetSum.kt`

{% include code-tabs.html  kotlin="package array.dp\n\n\nclass TargetSum {\n    private val dp = mutableMapOf<String, Int>()\n\n    fun findTargetSumWays(nums: IntArray, target: Int): Int {\n        return ways(nums, target, 0, 0)\n    }\n\n    private fun ways(nums: IntArray, target: Int, index: Int, sum: Int): Int {\n        val state = \"$index $sum\"\n        return when {\n            index >= nums.size -> if (sum == target) 1 else 0\n            dp.containsKey(state) -> dp[state]!!\n            else -> {\n                val count = ways(nums, target, index + 1, sum + nums[index]) +\n                        ways(nums, target, index + 1, sum - nums[index])\n                dp[state] = count\n                count\n            }\n        }\n    }\n\n\n    /**\n     * Finds the number of ways to assign a plus or minus sign to elements in the given array `nums`\n     * such that their sum equals `target`.\n     *\n     * This problem is transformed into a subset sum problem using dynamic programming.\n     * We calculate the total sum of `nums` and determine if it's possible to partition `nums`\n     * into two subsets with sums `P` and `N` such that:\n     *     P - N = target\n     *\n     * This can be simplified to finding the number of subsets of `nums` that sum up to\n     * (target + sum(nums)) / 2. If this value is not an integer or is negative, return 0.\n     *\n     * @param nums The array of integers to assign plus or minus signs.\n     * @param target The target sum to achieve.\n     * @return The number of ways to achieve the target sum using plus or minus signs.\n     */\n    fun findTargetSumWays_subsetSum(nums: IntArray, target: Int): Int {\n        val sumAll = nums.sum()\n        // If the target sum is not reachable, return 0\n        // If the sumAll + target is not non-negative or even, it's impossible to partition\n        if ((target + sumAll) % 2 != 0 || sumAll < Math.abs(target)) return 0\n        val newTarget = (target + sumAll) / 2\n\n        // DP array to store the number of ways to reach each sum\n        val dp = IntArray(newTarget + 1)\n        dp[0] = 1\n\n        for (num in nums) {\n            for (j in newTarget downTo num) {\n                dp[j] += dp[j - num]\n            }\n        }\n\n        return dp[newTarget]\n    }\n}"  java="class TargetSum {\n    private val dp = mutableMapOf<String, Int>()\n\n    public static void $1(nums: IntArray, target) {\n        return ways(nums, target, 0, 0)\n    }\n\n    private public static void $1(nums: IntArray, target, index, sum) {\n        val state = \"$index $sum\"\n        return when {\n            index >= nums.size -> if (sum == target) 1 else 0\n            dp.containsKey(state) -> dp[state]!!\n            else -> {\n                val count = ways(nums, target, index + 1, sum + nums[index]) +\n                        ways(nums, target, index + 1, sum - nums[index])\n                dp[state] = count\n                count\n            }\n        }\n    }\n\n\n    /**\n     * Finds the number of ways to assign a plus or minus sign to elements in the given array `nums`\n     * such that their sum equals `target`.\n     *\n     * This problem is transformed into a subset sum problem using dynamic programming.\n     * We calculate the total sum of `nums` and determine if it's possible to partition `nums`\n     * into two subsets with sums `P` and `N` such that:\n     *     P - N = target\n     *\n     * This can be simplified to finding the number of subsets of `nums` that sum up to\n     * (target + sum(nums)) / 2. If this value is not an integer or is negative, return 0.\n     *\n     * @param nums The array of integers to assign plus or minus signs.\n     * @param target The target sum to achieve.\n     * @return The number of ways to achieve the target sum using plus or minus signs.\n     */\n    public static void $1(nums: IntArray, target) {\n        val sumAll = nums.sum()\n        // If the target sum is not reachable, return 0\n        // If the sumAll + target is not non-negative or even, it's impossible to partition\n        if ((target + sumAll) % 2 != 0 || sumAll < Math.abs(target)) return 0\n        val newTarget = (target + sumAll) / 2\n\n        // DP array to store the number of ways to reach each sum\n        val dp = IntArray(newTarget + 1)\n        dp[0] = 1\n\n        for (num in nums) {\n            for (j in newTarget downTo num) {\n                dp[j] += dp[j - num]\n            }\n        }\n\n        return dp[newTarget]\n    }\n}"  python="class TargetSum {\n    private $1 = mutableMapOf<String, Int>()\n\n    def $1(nums: list[int], target: int): int {\n        return ways(nums, target, 0, 0)\n    }\n\n    private def $1(nums: list[int], target: int, index: int, sum: int): int {\n        $1 = \"$index $sum\"\n        return when {\n            index >= nums.size -> if (sum == target) 1 else 0\n            dp.containsKey(state) -> dp[state]!!\n            else -> {\n                $1 = ways(nums, target, index + 1, sum + nums[index]) +\n                        ways(nums, target, index + 1, sum - nums[index])\n                dp[state] = count\n                count\n            }\n        }\n    }\n\n\n    /**\n     * Finds the number of ways to assign a plus or minus sign to elements in the given array `nums`\n     * such that their sum equals `target`.\n     *\n     * This problem is transformed into a subset sum problem using dynamic programming.\n     * We calculate the total sum of `nums` and determine if it's possible to partition `nums`\n     * into two subsets with sums `P` and `N` such that:\n     *     P - N = target\n     *\n     * This can be simplified to finding the number of subsets of `nums` that sum up to\n     * (target + sum(nums)) / 2. If this value is not an integer or is negative, return 0.\n     *\n     * @param nums The array of integers to assign plus or minus signs.\n     * @param target The target sum to achieve.\n     * @return The number of ways to achieve the target sum using plus or minus signs.\n     */\n    def $1(nums: list[int], target: int): int {\n        $1 = nums.sum()\n        // If the target sum is not reachable, return 0\n        // If the sumAll + target is not non-negative or even, it's impossible to partition\n        if ((target + sumAll) % 2 != 0 || sumAll < Math.abs(target)) return 0\n        $1 = (target + sumAll) / 2\n\n        // DP array to store the number of ways to reach each sum\n        $1 = list[int](newTarget + 1)\n        dp[0] = 1\n\n        for (num in nums) {\n            for (j in newTarget downTo num) {\n                dp[j] += dp[j - num]\n            }\n        }\n\n        return dp[newTarget]\n    }\n}"  rust="class TargetSum {\n    private val dp = mutableMapOf<String, Int>()\n\n    fn $1(nums: Vec<i32>, target -> i32) -> i32 {\n        return ways(nums, target, 0, 0)\n    }\n\n    private fn $1(nums: Vec<i32>, target -> i32, index -> i32, sum -> i32) -> i32 {\n        val state = \"$index $sum\"\n        return when {\n            index >= nums.size -> if (sum == target) 1 else 0\n            dp.containsKey(state) -> dp[state]!!\n            else -> {\n                val count = ways(nums, target, index + 1, sum + nums[index]) +\n                        ways(nums, target, index + 1, sum - nums[index])\n                dp[state] = count\n                count\n            }\n        }\n    }\n\n\n    /**\n     * Finds the number of ways to assign a plus or minus sign to elements in the given array `nums`\n     * such that their sum equals `target`.\n     *\n     * This problem is transformed into a subset sum problem using dynamic programming.\n     * We calculate the total sum of `nums` and determine if it's possible to partition `nums`\n     * into two subsets with sums `P` and `N` such that:\n     *     P - N = target\n     *\n     * This can be simplified to finding the number of subsets of `nums` that sum up to\n     * (target + sum(nums)) / 2. If this value is not an integer or is negative, return 0.\n     *\n     * @param nums The array of integers to assign plus or minus signs.\n     * @param target The target sum to achieve.\n     * @return The number of ways to achieve the target sum using plus or minus signs.\n     */\n    fn $1(nums: Vec<i32>, target -> i32) -> i32 {\n        val sumAll = nums.sum()\n        // If the target sum is not reachable, return 0\n        // If the sumAll + target is not non-negative or even, it's impossible to partition\n        if ((target + sumAll) % 2 != 0 || sumAll < Math.abs(target)) return 0\n        val newTarget = (target + sumAll) / 2\n\n        // DP array to store the number of ways to reach each sum\n        val dp = Vec<i32>(newTarget + 1)\n        dp[0] = 1\n\n        for (num in nums) {\n            for (j in newTarget downTo num) {\n                dp[j] += dp[j - num]\n            }\n        }\n\n        return dp[newTarget]\n    }\n}"  cpp="class TargetSum {\n    private auto $1 = mutableMapOf<String, Int>()\n\n    $1(nums: vector<int>, target) {\n        return ways(nums, target, 0, 0)\n    }\n\n    private $1(nums: vector<int>, target, index, sum) {\n        auto $1 = \"$index $sum\"\n        return when {\n            index >= nums.size -> if (sum == target) 1 else 0\n            dp.containsKey(state) -> dp[state]!!\n            else -> {\n                auto $1 = ways(nums, target, index + 1, sum + nums[index]) +\n                        ways(nums, target, index + 1, sum - nums[index])\n                dp[state] = count\n                count\n            }\n        }\n    }\n\n\n    /**\n     * Finds the number of ways to assign a plus or minus sign to elements in the given array `nums`\n     * such that their sum equals `target`.\n     *\n     * This problem is transformed into a subset sum problem using dynamic programming.\n     * We calculate the total sum of `nums` and determine if it's possible to partition `nums`\n     * into two subsets with sums `P` and `N` such that:\n     *     P - N = target\n     *\n     * This can be simplified to finding the number of subsets of `nums` that sum up to\n     * (target + sum(nums)) / 2. If this value is not an integer or is negative, return 0.\n     *\n     * @param nums The array of integers to assign plus or minus signs.\n     * @param target The target sum to achieve.\n     * @return The number of ways to achieve the target sum using plus or minus signs.\n     */\n    $1(nums: vector<int>, target) {\n        auto $1 = nums.sum()\n        // If the target sum is not reachable, return 0\n        // If the sumAll + target is not non-negative or even, it's impossible to partition\n        if ((target + sumAll) % 2 != 0 || sumAll < Math.abs(target)) return 0\n        auto $1 = (target + sumAll) / 2\n\n        // DP array to store the number of ways to reach each sum\n        auto $1 = vector<int>(newTarget + 1)\n        dp[0] = 1\n\n        for (num in nums) {\n            for (j in newTarget downTo num) {\n                dp[j] += dp[j - num]\n            }\n        }\n\n        return dp[newTarget]\n    }\n}" %}

---

## ClosestSubsequenceSum

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/dynamic_programming/ClosestSubsequenceSum.kt`

{% include code-tabs.html  kotlin="package array.Combinatorics\n\nimport kotlin.math.abs\n\nclass ClosestSubsequenceSum {\n    fun minAbsDifference(nums: IntArray, goal: Int): Int {\n        var possibleSums = mutableSetOf(0)\n        for (num in nums) {\n            val newSums = mutableSetOf<Int>()\n            for (sum in possibleSums) {\n                newSums.add(sum + num)\n            }\n            possibleSums.addAll(newSums)\n        }\n\n        var minDiff = Int.MAX_VALUE\n        for (sum in possibleSums) {\n            minDiff = minOf(minDiff, abs(sum - goal))\n        }\n\n        return minDiff\n    }\n}"  java="class ClosestSubsequenceSum {\n    public static void $1(nums: IntArray, goal) {\n        var possibleSums = mutableSetOf(0)\n        for (num in nums) {\n            val newSums = mutableSetOf<Int>()\n            for (sum in possibleSums) {\n                newSums.add(sum + num)\n            }\n            possibleSums.addAll(newSums)\n        }\n\n        var minDiff = Int.MAX_VALUE\n        for (sum in possibleSums) {\n            minDiff = minOf(minDiff, abs(sum - goal))\n        }\n\n        return minDiff\n    }\n}"  python="class ClosestSubsequenceSum {\n    def $1(nums: list[int], goal: int): int {\n        $1 = mutableSetOf(0)\n        for (num in nums) {\n            $1 = mutableSetOf<Int>()\n            for (sum in possibleSums) {\n                newSums.add(sum + num)\n            }\n            possibleSums.addAll(newSums)\n        }\n\n        $1 = Int.MAX_VALUE\n        for (sum in possibleSums) {\n            minDiff = min(minDiff, abs(sum - goal))\n        }\n\n        return minDiff\n    }\n}"  rust="class ClosestSubsequenceSum {\n    fn $1(nums: Vec<i32>, goal -> i32) -> i32 {\n        var possibleSums = mutableSetOf(0)\n        for (num in nums) {\n            val newSums = mutableSetOf<Int>()\n            for (sum in possibleSums) {\n                newSums.add(sum + num)\n            }\n            possibleSums.addAll(newSums)\n        }\n\n        var minDiff = Int.MAX_VALUE\n        for (sum in possibleSums) {\n            minDiff = min(minDiff, abs(sum - goal))\n        }\n\n        return minDiff\n    }\n}"  cpp="class ClosestSubsequenceSum {\n    $1(nums: vector<int>, goal) {\n        auto $1 = mutableSetOf(0)\n        for (num in nums) {\n            auto $1 = mutableSetOf<Int>()\n            for (sum in possibleSums) {\n                newSums.add(sum + num)\n            }\n            possibleSums.addAll(newSums)\n        }\n\n        auto $1 = Int.MAX_VALUE\n        for (sum in possibleSums) {\n            minDiff = min(minDiff, abs(sum - goal))\n        }\n\n        return minDiff\n    }\n}" %}

---

## MaximumProductSubarray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/dynamic_programming/MaximumProductSubarray.kt`

{% include code-tabs.html  kotlin="package dynamic_programming\n\nimport dynamic_programming.MaximumProductSubarray.maxProduct\n\nobject MaximumProductSubarray {\n    fun maxProduct(nums: IntArray): Int {\n        // Edge case: empty array\n        if (nums.isEmpty()) return 0\n        val m = mapOf(1 to 2, 2 to 4, 5 to 6)\n\n        // Initialize trackers with first element\n        var maxSoFar = nums[0]  // Tracks maximum product ending at current position\n        var minSoFar = nums[0]  // Tracks minimum product ending at current position (for negative numbers)\n        var result = maxSoFar   // Stores the overall maximum product found\n\n        for (i in 1 until nums.size) {\n            val currentNum = nums[i]\n\n            // Calculate new maximum product ending at current position:\n            // 1. currentNum alone (start new subarray)\n            // 2. currentNum * previous max (extend positive product)\n            // 3. currentNum * previous min (negative * negative = positive)\n            val tempMax = maxOf(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive streak\n                minSoFar * currentNum         // Case 3: Flip negative streak\n            )\n\n            // Calculate new minimum product ending at current position:\n            // (Same cases as max, but tracking minimum for future negative flips)\n            minSoFar = minOf(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive (but could become min)\n                minSoFar * currentNum         // Case 3: Negative * negative (but track min)\n            )\n\n            maxSoFar = tempMax  // Update max product for next iteration\n            result = maxOf(result, maxSoFar)  // Update global maximum\n        }\n\n        return result\n    }\n\n    @JvmStatic\n    fun main(args: Array<String>) {\n        val tests = listOf(\n            intArrayOf(2, 3, -2, 4) to 6,     // Regular case\n            intArrayOf(-2, 0, -1) to 0,       // Zero resets product\n            intArrayOf(-2, 3, -4) to 24,      // Negative flip\n            intArrayOf(-1, -2, -3) to 6,      // All negatives\n            intArrayOf(0, 2) to 2             // Single element\n        )\n\n        tests.forEach { (input, expected) ->\n            val output = maxProduct(input)\n            println(\"Input: ${input.joinToString()}\")\n            println(\"Output: $output (Expected: $expected) ${if (output == expected) \"✓\" else \"✗\"}\\n\")\n        }\n    }\n}\n"  java="object MaximumProductSubarray {\n    public static void $1(nums: IntArray) {\n        // Edge case: empty array\n        if (nums.isEmpty()) return 0\n        val m = mapOf(1 to 2, 2 to 4, 5 to 6)\n\n        // Initialize trackers with first element\n        var maxSoFar = nums[0]  // Tracks maximum product ending at current position\n        var minSoFar = nums[0]  // Tracks minimum product ending at current position (for negative numbers)\n        var result = maxSoFar   // Stores the overall maximum product found\n\n        for (i in 1 until nums.size) {\n            val currentNum = nums[i]\n\n            // Calculate new maximum product ending at current position:\n            // 1. currentNum alone (start new subarray)\n            // 2. currentNum * previous max (extend positive product)\n            // 3. currentNum * previous min (negative * negative = positive)\n            val tempMax = maxOf(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive streak\n                minSoFar * currentNum         // Case 3: Flip negative streak\n            )\n\n            // Calculate new minimum product ending at current position:\n            // (Same cases as max, but tracking minimum for future negative flips)\n            minSoFar = minOf(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive (but could become min)\n                minSoFar * currentNum         // Case 3: Negative * negative (but track min)\n            )\n\n            maxSoFar = tempMax  // Update max product for next iteration\n            result = maxOf(result, maxSoFar)  // Update global maximum\n        }\n\n        return result\n    }\n\n    @JvmStatic\n    public static void $1(args: Array<String>) {\n        val tests = listOf(\n            intArrayOf(2, 3, -2, 4) to 6,     // Regular case\n            intArrayOf(-2, 0, -1) to 0,       // Zero resets product\n            intArrayOf(-2, 3, -4) to 24,      // Negative flip\n            intArrayOf(-1, -2, -3) to 6,      // All negatives\n            intArrayOf(0, 2) to 2             // Single element\n        )\n\n        tests.forEach { (input, expected) ->\n            val output = maxProduct(input)\n            println(\"Input: ${input.joinToString()}\")\n            println(\"Output: $output (Expected: $expected) ${if (output == expected) \"✓\" else \"✗\"}\\n\")\n        }\n    }\n}"  python="object MaximumProductSubarray {\n    def $1(nums: list[int]): int {\n        // Edge case: empty array\n        if (nums.isEmpty()) return 0\n        $1 = mapOf(1 to 2, 2 to 4, 5 to 6)\n\n        // Initialize trackers with first element\n        $1 = nums[0]  // Tracks maximum product ending at current position\n        $1 = nums[0]  // Tracks minimum product ending at current position (for negative numbers)\n        $1 = maxSoFar   // Stores the overall maximum product found\n\n        for (i in 1 until nums.size) {\n            $1 = nums[i]\n\n            // Calculate new maximum product ending at current position:\n            // 1. currentNum alone (start new subarray)\n            // 2. currentNum * previous max (extend positive product)\n            // 3. currentNum * previous min (negative * negative = positive)\n            $1 = max(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive streak\n                minSoFar * currentNum         // Case 3: Flip negative streak\n            )\n\n            // Calculate new minimum product ending at current position:\n            // (Same cases as max, but tracking minimum for future negative flips)\n            minSoFar = min(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive (but could become min)\n                minSoFar * currentNum         // Case 3: Negative * negative (but track min)\n            )\n\n            maxSoFar = tempMax  // Update max product for next iteration\n            result = max(result, maxSoFar)  // Update global maximum\n        }\n\n        return result\n    }\n\n    @JvmStatic\n    def $1(args: Array<String>) {\n        $1 = listOf(\n            intArrayOf(2, 3, -2, 4) to 6,     // Regular case\n            intArrayOf(-2, 0, -1) to 0,       // Zero resets product\n            intArrayOf(-2, 3, -4) to 24,      // Negative flip\n            intArrayOf(-1, -2, -3) to 6,      // All negatives\n            intArrayOf(0, 2) to 2             // Single element\n        )\n\n        tests.forEach { (input, expected) ->\n            $1 = maxProduct(input)\n            print(\"Input: ${input.joinToString()}\")\n            print(\"Output: $output (Expected: $expected) ${if (output == expected) \"✓\" else \"✗\"}\\n\")\n        }\n    }\n}"  rust="object MaximumProductSubarray {\n    fn $1(nums: Vec<i32>) -> i32 {\n        // Edge case: empty array\n        if (nums.isEmpty()) return 0\n        val m = mapOf(1 to 2, 2 to 4, 5 to 6)\n\n        // Initialize trackers with first element\n        var maxSoFar = nums[0]  // Tracks maximum product ending at current position\n        var minSoFar = nums[0]  // Tracks minimum product ending at current position (for negative numbers)\n        var result = maxSoFar   // Stores the overall maximum product found\n\n        for (i in 1 until nums.size) {\n            val currentNum = nums[i]\n\n            // Calculate new maximum product ending at current position:\n            // 1. currentNum alone (start new subarray)\n            // 2. currentNum * previous max (extend positive product)\n            // 3. currentNum * previous min (negative * negative = positive)\n            val tempMax = max(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive streak\n                minSoFar * currentNum         // Case 3: Flip negative streak\n            )\n\n            // Calculate new minimum product ending at current position:\n            // (Same cases as max, but tracking minimum for future negative flips)\n            minSoFar = min(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive (but could become min)\n                minSoFar * currentNum         // Case 3: Negative * negative (but track min)\n            )\n\n            maxSoFar = tempMax  // Update max product for next iteration\n            result = max(result, maxSoFar)  // Update global maximum\n        }\n\n        return result\n    }\n\n    @JvmStatic\n    fn $1(args: Array<String>) {\n        val tests = listOf(\n            intArrayOf(2, 3, -2, 4) to 6,     // Regular case\n            intArrayOf(-2, 0, -1) to 0,       // Zero resets product\n            intArrayOf(-2, 3, -4) to 24,      // Negative flip\n            intArrayOf(-1, -2, -3) to 6,      // All negatives\n            intArrayOf(0, 2) to 2             // Single element\n        )\n\n        tests.forEach { (input, expected) ->\n            val output = maxProduct(input)\n            println!(\"Input: ${input.joinToString()}\")\n            println!(\"Output: $output (Expected: $expected) ${if (output == expected) \"✓\" else \"✗\"}\\n\")\n        }\n    }\n}"  cpp="object MaximumProductSubarray {\n    $1(nums: vector<int>) {\n        // Edge case: empty array\n        if (nums.isEmpty()) return 0\n        auto $1 = mapOf(1 to 2, 2 to 4, 5 to 6)\n\n        // Initialize trackers with first element\n        auto $1 = nums[0]  // Tracks maximum product ending at current position\n        auto $1 = nums[0]  // Tracks minimum product ending at current position (for negative numbers)\n        auto $1 = maxSoFar   // Stores the overall maximum product found\n\n        for (i in 1 until nums.size) {\n            auto $1 = nums[i]\n\n            // Calculate new maximum product ending at current position:\n            // 1. currentNum alone (start new subarray)\n            // 2. currentNum * previous max (extend positive product)\n            // 3. currentNum * previous min (negative * negative = positive)\n            auto $1 = max(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive streak\n                minSoFar * currentNum         // Case 3: Flip negative streak\n            )\n\n            // Calculate new minimum product ending at current position:\n            // (Same cases as max, but tracking minimum for future negative flips)\n            minSoFar = min(\n                currentNum,                   // Case 1: Start fresh\n                maxSoFar * currentNum,        // Case 2: Continue positive (but could become min)\n                minSoFar * currentNum         // Case 3: Negative * negative (but track min)\n            )\n\n            maxSoFar = tempMax  // Update max product for next iteration\n            result = max(result, maxSoFar)  // Update global maximum\n        }\n\n        return result\n    }\n\n    @JvmStatic\n    $1(args: Array<String>) {\n        auto $1 = listOf(\n            intArrayOf(2, 3, -2, 4) to 6,     // Regular case\n            intArrayOf(-2, 0, -1) to 0,       // Zero resets product\n            intArrayOf(-2, 3, -4) to 24,      // Negative flip\n            intArrayOf(-1, -2, -3) to 6,      // All negatives\n            intArrayOf(0, 2) to 2             // Single element\n        )\n\n        tests.forEach { (input, expected) ->\n            auto $1 = maxProduct(input)\n            cout <<(\"Input: ${input.joinToString()}\")\n            cout <<(\"Output: $output (Expected: $expected) ${if (output == expected) \"✓\" else \"✗\"}\\n\")\n        }\n    }\n}" %}

---

## MaximumProfitInJobScheduling

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/dynamic_programming/MaximumProfitInJobScheduling.kt`

{% include code-tabs.html  kotlin="package dynamic_programming\n\nclass MaximumProfitInJobScheduling {\n    data class Job(val start: Int, val end: Int, val profit: Int)\n\n    fun jobScheduling(startTime: IntArray, endTime: IntArray, profit: IntArray): Int {\n        // Create a list of jobs and sort them by end time\n        val jobs = startTime.indices.map { Job(startTime[it], endTime[it], profit[it]) }\n            .sortedBy { it.end }\n\n        // Initialize dp array\n        val dp = IntArray(jobs.size)\n        dp[0] = jobs[0].profit\n\n        for (i in 1 until jobs.size) {\n            // Profit if the current job is not included\n            dp[i] = dp[i - 1]\n\n            // Find the last non-overlapping job\n            val prevJobIndex = findLastNonOverlappingJob(jobs, i)\n\n            // Profit if the current job is included\n            val currentProfit = jobs[i].profit + if (prevJobIndex != -1) dp[prevJobIndex] else 0\n\n            // Take the maximum profit between including and excluding the current job\n            dp[i] = maxOf(dp[i], currentProfit)\n        }\n\n        return dp[dp.size - 1]\n    }\n\n    private fun findLastNonOverlappingJob(jobs: List<Job>, currentIndex: Int): Int {\n        val currentJob = jobs[currentIndex]\n        var low = 0\n        var high = currentIndex - 1\n        var best = -1\n\n        while (low <= high) {\n            val mid = (low + high) / 2\n            if (jobs[mid].end <= currentJob.start) {\n                best = mid\n                low = mid + 1  // Search in the right half\n            } else {\n                high = mid - 1  // Search in the left half\n            }\n        }\n\n        return best\n    }\n}\n"  java="class MaximumProfitInJobScheduling {\n    data class Job(val start, val end, val profit)\n\n    public static void $1(startTime: IntArray, endTime: IntArray, profit: IntArray) {\n        // Create a list of jobs and sort them by end time\n        val jobs = startTime.indices.map { Job(startTime[it], endTime[it], profit[it]) }\n            .sortedBy { it.end }\n\n        // Initialize dp array\n        val dp = IntArray(jobs.size)\n        dp[0] = jobs[0].profit\n\n        for (i in 1 until jobs.size) {\n            // Profit if the current job is not included\n            dp[i] = dp[i - 1]\n\n            // Find the last non-overlapping job\n            val prevJobIndex = findLastNonOverlappingJob(jobs, i)\n\n            // Profit if the current job is included\n            val currentProfit = jobs[i].profit + if (prevJobIndex != -1) dp[prevJobIndex] else 0\n\n            // Take the maximum profit between including and excluding the current job\n            dp[i] = maxOf(dp[i], currentProfit)\n        }\n\n        return dp[dp.size - 1]\n    }\n\n    private public static void $1(jobs: List<Job>, currentIndex) {\n        val currentJob = jobs[currentIndex]\n        var low = 0\n        var high = currentIndex - 1\n        var best = -1\n\n        while (low <= high) {\n            val mid = (low + high) / 2\n            if (jobs[mid].end <= currentJob.start) {\n                best = mid\n                low = mid + 1  // Search in the right half\n            } else {\n                high = mid - 1  // Search in the left half\n            }\n        }\n\n        return best\n    }\n}"  python="class MaximumProfitInJobScheduling {\n    data class Job(val start: int, val end: int, val profit: int)\n\n    def $1(startTime: list[int], endTime: list[int], profit: list[int]): int {\n        // Create a list of jobs and sort them by end time\n        $1 = startTime.indices.map { Job(startTime[it], endTime[it], profit[it]) }\n            .sortedBy { it.end }\n\n        // Initialize dp array\n        $1 = list[int](jobs.size)\n        dp[0] = jobs[0].profit\n\n        for (i in 1 until jobs.size) {\n            // Profit if the current job is not included\n            dp[i] = dp[i - 1]\n\n            // Find the last non-overlapping job\n            $1 = findLastNonOverlappingJob(jobs, i)\n\n            // Profit if the current job is included\n            $1 = jobs[i].profit + if (prevJobIndex != -1) dp[prevJobIndex] else 0\n\n            // Take the maximum profit between including and excluding the current job\n            dp[i] = max(dp[i], currentProfit)\n        }\n\n        return dp[dp.size - 1]\n    }\n\n    private def $1(jobs: List<Job>, currentIndex: int): int {\n        $1 = jobs[currentIndex]\n        $1 = 0\n        $1 = currentIndex - 1\n        $1 = -1\n\n        while (low <= high) {\n            $1 = (low + high) / 2\n            if (jobs[mid].end <= currentJob.start) {\n                best = mid\n                low = mid + 1  // Search in the right half\n            } else {\n                high = mid - 1  // Search in the left half\n            }\n        }\n\n        return best\n    }\n}"  rust="class MaximumProfitInJobScheduling {\n    data class Job(val start -> i32, val end -> i32, val profit -> i32)\n\n    fn $1(startTime: Vec<i32>, endTime: Vec<i32>, profit: Vec<i32>) -> i32 {\n        // Create a list of jobs and sort them by end time\n        val jobs = startTime.indices.map { Job(startTime[it], endTime[it], profit[it]) }\n            .sortedBy { it.end }\n\n        // Initialize dp array\n        val dp = Vec<i32>(jobs.size)\n        dp[0] = jobs[0].profit\n\n        for (i in 1 until jobs.size) {\n            // Profit if the current job is not included\n            dp[i] = dp[i - 1]\n\n            // Find the last non-overlapping job\n            val prevJobIndex = findLastNonOverlappingJob(jobs, i)\n\n            // Profit if the current job is included\n            val currentProfit = jobs[i].profit + if (prevJobIndex != -1) dp[prevJobIndex] else 0\n\n            // Take the maximum profit between including and excluding the current job\n            dp[i] = max(dp[i], currentProfit)\n        }\n\n        return dp[dp.size - 1]\n    }\n\n    private fn $1(jobs: List<Job>, currentIndex -> i32) -> i32 {\n        val currentJob = jobs[currentIndex]\n        var low = 0\n        var high = currentIndex - 1\n        var best = -1\n\n        while (low <= high) {\n            val mid = (low + high) / 2\n            if (jobs[mid].end <= currentJob.start) {\n                best = mid\n                low = mid + 1  // Search in the right half\n            } else {\n                high = mid - 1  // Search in the left half\n            }\n        }\n\n        return best\n    }\n}"  cpp="class MaximumProfitInJobScheduling {\n    data class Job(val start, val end, val profit)\n\n    $1(startTime: vector<int>, endTime: vector<int>, profit: vector<int>) {\n        // Create a list of jobs and sort them by end time\n        auto $1 = startTime.indices.map { Job(startTime[it], endTime[it], profit[it]) }\n            .sortedBy { it.end }\n\n        // Initialize dp array\n        auto $1 = vector<int>(jobs.size)\n        dp[0] = jobs[0].profit\n\n        for (i in 1 until jobs.size) {\n            // Profit if the current job is not included\n            dp[i] = dp[i - 1]\n\n            // Find the last non-overlapping job\n            auto $1 = findLastNonOverlappingJob(jobs, i)\n\n            // Profit if the current job is included\n            auto $1 = jobs[i].profit + if (prevJobIndex != -1) dp[prevJobIndex] else 0\n\n            // Take the maximum profit between including and excluding the current job\n            dp[i] = max(dp[i], currentProfit)\n        }\n\n        return dp[dp.size - 1]\n    }\n\n    private $1(jobs: List<Job>, currentIndex) {\n        auto $1 = jobs[currentIndex]\n        auto $1 = 0\n        auto $1 = currentIndex - 1\n        auto $1 = -1\n\n        while (low <= high) {\n            auto $1 = (low + high) / 2\n            if (jobs[mid].end <= currentJob.start) {\n                best = mid\n                low = mid + 1  // Search in the right half\n            } else {\n                high = mid - 1  // Search in the left half\n            }\n        }\n\n        return best\n    }\n}" %}

---

## PartitionEqualSubsetSum

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/dynamic_programming/PartitionEqualSubsetSum.kt`

{% include code-tabs.html  kotlin="package dynamic_programming\n\nclass PartitionEqualSubsetSum {\n    fun canPartition(nums: IntArray): Boolean {\n        val sum = nums.sum()\n        if (sum % 2 != 0)   return false\n        val target = sum / 2\n        val dp = Array(nums.size) { IntArray(target + 1) { -1 } }\n\n\n        fun dfs(i: Int, currentSum: Int): Boolean = when {\n            currentSum == target -> true\n            dp[i][currentSum] != -1 -> dp[i][currentSum] == 1\n            else -> (\n                dfs(i + 1, currentSum + nums[i]) ||\n                dfs(i + 1, currentSum)\n            ).also { dp[i][currentSum] = if (it) 1 else 0 }\n        }\n\n        return dfs(0, 0)\n    }\n\n    fun canPartitionBottomUp(nums: IntArray): Boolean {\n        val sum = nums.sum()\n        if (sum % 2 != 0)   return false\n\n        val target = sum / 2\n        val dp = BooleanArray(target + 1).apply { this[0] = true }\n        for (num in nums) {\n            for (i in target downTo num)\n                dp[i] = dp[i] || dp[i - num]\n        }\n\n        return dp[target]\n    }\n}"  java="class PartitionEqualSubsetSum {\n    public static void $1(nums: IntArray) {\n        val sum = nums.sum()\n        if (sum % 2 != 0)   return false\n        val target = sum / 2\n        val dp = Array(nums.size) { IntArray(target + 1) { -1 } }\n\n\n        public static void $1(i, currentSum) = when {\n            currentSum == target -> true\n            dp[i][currentSum] != -1 -> dp[i][currentSum] == 1\n            else -> (\n                dfs(i + 1, currentSum + nums[i]) ||\n                dfs(i + 1, currentSum)\n            ).also { dp[i][currentSum] = if (it) 1 else 0 }\n        }\n\n        return dfs(0, 0)\n    }\n\n    public static void $1(nums: IntArray) {\n        val sum = nums.sum()\n        if (sum % 2 != 0)   return false\n\n        val target = sum / 2\n        val dp = BooleanArray(target + 1).apply { this[0] = true }\n        for (num in nums) {\n            for (i in target downTo num)\n                dp[i] = dp[i] || dp[i - num]\n        }\n\n        return dp[target]\n    }\n}"  python="class PartitionEqualSubsetSum {\n    def $1(nums: list[int]): bool {\n        $1 = nums.sum()\n        if (sum % 2 != 0)   return False\n        $1 = sum / 2\n        $1 = Array(nums.size) { list[int](target + 1) { -1 } }\n\n\n        def $1(i: int, currentSum: int): bool = when {\n            currentSum == target -> True\n            dp[i][currentSum] != -1 -> dp[i][currentSum] == 1\n            else -> (\n                dfs(i + 1, currentSum + nums[i]) ||\n                dfs(i + 1, currentSum)\n            ).also { dp[i][currentSum] = if (it) 1 else 0 }\n        }\n\n        return dfs(0, 0)\n    }\n\n    def $1(nums: list[int]): bool {\n        $1 = nums.sum()\n        if (sum % 2 != 0)   return False\n\n        $1 = sum / 2\n        $1 = BooleanArray(target + 1).apply { this[0] = True }\n        for (num in nums) {\n            for (i in target downTo num)\n                dp[i] = dp[i] || dp[i - num]\n        }\n\n        return dp[target]\n    }\n}"  rust="class PartitionEqualSubsetSum {\n    fn $1(nums: Vec<i32>) -> bool {\n        val sum = nums.sum()\n        if (sum % 2 != 0)   return false\n        val target = sum / 2\n        val dp = Array(nums.size) { Vec<i32>(target + 1) { -1 } }\n\n\n        fn $1(i -> i32, currentSum -> i32) -> bool = when {\n            currentSum == target -> true\n            dp[i][currentSum] != -1 -> dp[i][currentSum] == 1\n            else -> (\n                dfs(i + 1, currentSum + nums[i]) ||\n                dfs(i + 1, currentSum)\n            ).also { dp[i][currentSum] = if (it) 1 else 0 }\n        }\n\n        return dfs(0, 0)\n    }\n\n    fn $1(nums: Vec<i32>) -> bool {\n        val sum = nums.sum()\n        if (sum % 2 != 0)   return false\n\n        val target = sum / 2\n        val dp = BooleanArray(target + 1).apply { this[0] = true }\n        for (num in nums) {\n            for (i in target downTo num)\n                dp[i] = dp[i] || dp[i - num]\n        }\n\n        return dp[target]\n    }\n}"  cpp="class PartitionEqualSubsetSum {\n    $1(nums: vector<int>) {\n        auto $1 = nums.sum()\n        if (sum % 2 != 0)   return false\n        auto $1 = sum / 2\n        auto $1 = Array(nums.size) { vector<int>(target + 1) { -1 } }\n\n\n        $1(i, currentSum) = when {\n            currentSum == target -> true\n            dp[i][currentSum] != -1 -> dp[i][currentSum] == 1\n            else -> (\n                dfs(i + 1, currentSum + nums[i]) ||\n                dfs(i + 1, currentSum)\n            ).also { dp[i][currentSum] = if (it) 1 else 0 }\n        }\n\n        return dfs(0, 0)\n    }\n\n    $1(nums: vector<int>) {\n        auto $1 = nums.sum()\n        if (sum % 2 != 0)   return false\n\n        auto $1 = sum / 2\n        auto $1 = BooleanArray(target + 1).apply { this[0] = true }\n        for (num in nums) {\n            for (i in target downTo num)\n                dp[i] = dp[i] || dp[i - num]\n        }\n\n        return dp[target]\n    }\n}" %}

---

## SuperEggDropping

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/dynamic_programming/SuperEggDropping.kt`

{% include code-tabs.html  kotlin="package dynamic_programming\n\nclass SuperEggDropping {\n    fun superEggDrop(k: Int, n: Int): Int {\n        val dp = Array(k + 1) { IntArray(n + 1) }\n        var m = 0\n        while (dp[k][m] < n) {\n            m++\n            for (eggs in 1..k) {\n                dp[eggs][m] = dp[eggs][m - 1] + dp[eggs - 1][m - 1] + 1\n            }\n        }\n        return m\n    }\n}"  java="class SuperEggDropping {\n    public static void $1(k, n) {\n        val dp = Array(k + 1) { IntArray(n + 1) }\n        var m = 0\n        while (dp[k][m] < n) {\n            m++\n            for (eggs in 1..k) {\n                dp[eggs][m] = dp[eggs][m - 1] + dp[eggs - 1][m - 1] + 1\n            }\n        }\n        return m\n    }\n}"  python="class SuperEggDropping {\n    def $1(k: int, n: int): int {\n        $1 = Array(k + 1) { list[int](n + 1) }\n        $1 = 0\n        while (dp[k][m] < n) {\n            m++\n            for (eggs in 1..k) {\n                dp[eggs][m] = dp[eggs][m - 1] + dp[eggs - 1][m - 1] + 1\n            }\n        }\n        return m\n    }\n}"  rust="class SuperEggDropping {\n    fn $1(k -> i32, n -> i32) -> i32 {\n        val dp = Array(k + 1) { Vec<i32>(n + 1) }\n        var m = 0\n        while (dp[k][m] < n) {\n            m++\n            for (eggs in 1..k) {\n                dp[eggs][m] = dp[eggs][m - 1] + dp[eggs - 1][m - 1] + 1\n            }\n        }\n        return m\n    }\n}"  cpp="class SuperEggDropping {\n    $1(k, n) {\n        auto $1 = Array(k + 1) { vector<int>(n + 1) }\n        auto $1 = 0\n        while (dp[k][m] < n) {\n            m++\n            for (eggs in 1..k) {\n                dp[eggs][m] = dp[eggs][m - 1] + dp[eggs - 1][m - 1] + 1\n            }\n        }\n        return m\n    }\n}" %}

---

## BestTimeToBuyAndSellStock

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/stock_market/dp/BestTimeToBuyAndSellStock.kt`

{% include code-tabs.html  kotlin="package stock_market.dp\n\nclass BestTimeToBuyAndSellStock {\n    // The constraint is we can buy a stock in single day and sell in different day. There is no buyback\n    // Stock must be bought once and sold once\n    fun maxProfit(prices: IntArray): Int {\n        if (prices.isEmpty())\n            return 0\n        var ( maxProfit, minElement ) = (0 to prices[0])\n\n        for (i in 1..prices.lastIndex) {\n            maxProfit = maxOf(maxProfit, prices[i] - minElement)\n            minElement = minOf(minElement, prices[i])\n        }\n\n        return maxProfit\n    }\n}"  java="class BestTimeToBuyAndSellStock {\n    // The constraint is we can buy a stock in single day and sell in different day. There is no buyback\n    // Stock must be bought once and sold once\n    public static void $1(prices: IntArray) {\n        if (prices.isEmpty())\n            return 0\n        var ( maxProfit, minElement ) = (0 to prices[0])\n\n        for (i in 1..prices.lastIndex) {\n            maxProfit = maxOf(maxProfit, prices[i] - minElement)\n            minElement = minOf(minElement, prices[i])\n        }\n\n        return maxProfit\n    }\n}"  python="class BestTimeToBuyAndSellStock {\n    // The constraint is we can buy a stock in single day and sell in different day. There is no buyback\n    // Stock must be bought once and sold once\n    def $1(prices: list[int]): int {\n        if (prices.isEmpty())\n            return 0\n        var ( maxProfit, minElement ) = (0 to prices[0])\n\n        for (i in 1..prices.lastIndex) {\n            maxProfit = max(maxProfit, prices[i] - minElement)\n            minElement = min(minElement, prices[i])\n        }\n\n        return maxProfit\n    }\n}"  rust="class BestTimeToBuyAndSellStock {\n    // The constraint is we can buy a stock in single day and sell in different day. There is no buyback\n    // Stock must be bought once and sold once\n    fn $1(prices: Vec<i32>) -> i32 {\n        if (prices.isEmpty())\n            return 0\n        var ( maxProfit, minElement ) = (0 to prices[0])\n\n        for (i in 1..prices.len() - 1) {\n            maxProfit = max(maxProfit, prices[i] - minElement)\n            minElement = min(minElement, prices[i])\n        }\n\n        return maxProfit\n    }\n}"  cpp="class BestTimeToBuyAndSellStock {\n    // The constraint is we can buy a stock in single day and sell in different day. There is no buyback\n    // Stock must be bought once and sold once\n    $1(prices: vector<int>) {\n        if (prices.isEmpty())\n            return 0\n        var ( maxProfit, minElement ) = (0 to prices[0])\n\n        for (i in 1..prices.size() - 1) {\n            maxProfit = max(maxProfit, prices[i] - minElement)\n            minElement = min(minElement, prices[i])\n        }\n\n        return maxProfit\n    }\n}" %}

---

## BestTimeToBuyAndSellStockWithCooldown

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/stock_market/dp/BestTimeToBuyAndSellStockWithCooldown.kt`

{% include code-tabs.html  kotlin="package stock_market.dp\n\n// https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/discuss/75924/Most-consistent-ways-of-dealing-with-the-series-of-stock-problems\nclass BestTimeToBuyAndSellStockWithCooldown {\n    val dp = mutableMapOf<Int, Int>()\n\n    fun maxProfit(prices: IntArray): Int {\n        return maxProfit(prices, 0)\n    }\n\n    fun maxProfit(prices: IntArray, buyAt: Int): Int {\n        return when {\n            buyAt > prices.lastIndex ->  0\n            dp.containsKey(buyAt) ->  dp[buyAt]!!\n            else -> {\n                var maxProfit = 0\n                for (sellAt in buyAt + 1..prices.lastIndex) {\n                    maxProfit = maxOf(\n                        maxProfit,\n                        // HOLD this stock and sell later\n                        maxProfit(prices,  sellAt),\n                        prices[sellAt] - prices[buyAt] + maxProfit(prices, sellAt + 2)\n                    )\n                }\n                maxProfit\n            }\n        }.also {\n            dp[buyAt] = it\n        }\n    }\n\n    /**\n     * Input: prices = [1,2,3,0,2]\n     * Profit = 0\n     * Day Timeline :-\n     * 0    1   2   3   4   5   6\n     * BUY\n     */\n}\n"  java="// https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/discuss/75924/Most-consistent-ways-of-dealing-with-the-series-of-stock-problems\nclass BestTimeToBuyAndSellStockWithCooldown {\n    val dp = mutableMapOf<Int, Int>()\n\n    public static void $1(prices: IntArray) {\n        return maxProfit(prices, 0)\n    }\n\n    public static void $1(prices: IntArray, buyAt) {\n        return when {\n            buyAt > prices.lastIndex ->  0\n            dp.containsKey(buyAt) ->  dp[buyAt]!!\n            else -> {\n                var maxProfit = 0\n                for (sellAt in buyAt + 1..prices.lastIndex) {\n                    maxProfit = maxOf(\n                        maxProfit,\n                        // HOLD this stock and sell later\n                        maxProfit(prices,  sellAt),\n                        prices[sellAt] - prices[buyAt] + maxProfit(prices, sellAt + 2)\n                    )\n                }\n                maxProfit\n            }\n        }.also {\n            dp[buyAt] = it\n        }\n    }\n\n    /**\n     * Input: prices = [1,2,3,0,2]\n     * Profit = 0\n     * Day Timeline :-\n     * 0    1   2   3   4   5   6\n     * BUY\n     */\n}"  python="// https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/discuss/75924/Most-consistent-ways-of-dealing-with-the-series-of-stock-problems\nclass BestTimeToBuyAndSellStockWithCooldown {\n    $1 = mutableMapOf<Int, Int>()\n\n    def $1(prices: list[int]): int {\n        return maxProfit(prices, 0)\n    }\n\n    def $1(prices: list[int], buyAt: int): int {\n        return when {\n            buyAt > prices.lastIndex ->  0\n            dp.containsKey(buyAt) ->  dp[buyAt]!!\n            else -> {\n                $1 = 0\n                for (sellAt in buyAt + 1..prices.lastIndex) {\n                    maxProfit = max(\n                        maxProfit,\n                        // HOLD this stock and sell later\n                        maxProfit(prices,  sellAt),\n                        prices[sellAt] - prices[buyAt] + maxProfit(prices, sellAt + 2)\n                    )\n                }\n                maxProfit\n            }\n        }.also {\n            dp[buyAt] = it\n        }\n    }\n\n    /**\n     * Input: prices = [1,2,3,0,2]\n     * Profit = 0\n     * Day Timeline :-\n     * 0    1   2   3   4   5   6\n     * BUY\n     */\n}"  rust="// https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/discuss/75924/Most-consistent-ways-of-dealing-with-the-series-of-stock-problems\nclass BestTimeToBuyAndSellStockWithCooldown {\n    val dp = mutableMapOf<Int, Int>()\n\n    fn $1(prices: Vec<i32>) -> i32 {\n        return maxProfit(prices, 0)\n    }\n\n    fn $1(prices: Vec<i32>, buyAt -> i32) -> i32 {\n        return when {\n            buyAt > prices.len() - 1 ->  0\n            dp.containsKey(buyAt) ->  dp[buyAt]!!\n            else -> {\n                var maxProfit = 0\n                for (sellAt in buyAt + 1..prices.len() - 1) {\n                    maxProfit = max(\n                        maxProfit,\n                        // HOLD this stock and sell later\n                        maxProfit(prices,  sellAt),\n                        prices[sellAt] - prices[buyAt] + maxProfit(prices, sellAt + 2)\n                    )\n                }\n                maxProfit\n            }\n        }.also {\n            dp[buyAt] = it\n        }\n    }\n\n    /**\n     * Input: prices = [1,2,3,0,2]\n     * Profit = 0\n     * Day Timeline :-\n     * 0    1   2   3   4   5   6\n     * BUY\n     */\n}"  cpp="// https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/discuss/75924/Most-consistent-ways-of-dealing-with-the-series-of-stock-problems\nclass BestTimeToBuyAndSellStockWithCooldown {\n    auto $1 = mutableMapOf<Int, Int>()\n\n    $1(prices: vector<int>) {\n        return maxProfit(prices, 0)\n    }\n\n    $1(prices: vector<int>, buyAt) {\n        return when {\n            buyAt > prices.size() - 1 ->  0\n            dp.containsKey(buyAt) ->  dp[buyAt]!!\n            else -> {\n                auto $1 = 0\n                for (sellAt in buyAt + 1..prices.size() - 1) {\n                    maxProfit = max(\n                        maxProfit,\n                        // HOLD this stock and sell later\n                        maxProfit(prices,  sellAt),\n                        prices[sellAt] - prices[buyAt] + maxProfit(prices, sellAt + 2)\n                    )\n                }\n                maxProfit\n            }\n        }.also {\n            dp[buyAt] = it\n        }\n    }\n\n    /**\n     * Input: prices = [1,2,3,0,2]\n     * Profit = 0\n     * Day Timeline :-\n     * 0    1   2   3   4   5   6\n     * BUY\n     */\n}" %}

---

## BestTimeToBuyAndSellStockWithTransactionFee

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/stock_market/dp/BestTimeToBuyAndSellStockWithTransactionFee.kt`

{% include code-tabs.html  kotlin="package stock_market.dp\n\nclass BestTimeToBuyAndSellStockWithTransactionFee {\n\n    fun maxProfit(prices: IntArray, fee: Int): Int {\n        if (prices.isEmpty()) return 0\n\n        var hold = -prices[0]\n        var cash = 0\n\n        for (i in 1..prices.lastIndex) {\n            cash = maxOf( cash, hold + prices[i] - fee) // Sell the stock\n            hold = maxOf(hold, cash - prices[i]) // buy the stock\n        }\n\n        return cash\n    }\n}\n"  java="class BestTimeToBuyAndSellStockWithTransactionFee {\n\n    public static void $1(prices: IntArray, fee) {\n        if (prices.isEmpty()) return 0\n\n        var hold = -prices[0]\n        var cash = 0\n\n        for (i in 1..prices.lastIndex) {\n            cash = maxOf( cash, hold + prices[i] - fee) // Sell the stock\n            hold = maxOf(hold, cash - prices[i]) // buy the stock\n        }\n\n        return cash\n    }\n}"  python="class BestTimeToBuyAndSellStockWithTransactionFee {\n\n    def $1(prices: list[int], fee: int): int {\n        if (prices.isEmpty()) return 0\n\n        $1 = -prices[0]\n        $1 = 0\n\n        for (i in 1..prices.lastIndex) {\n            cash = max( cash, hold + prices[i] - fee) // Sell the stock\n            hold = max(hold, cash - prices[i]) // buy the stock\n        }\n\n        return cash\n    }\n}"  rust="class BestTimeToBuyAndSellStockWithTransactionFee {\n\n    fn $1(prices: Vec<i32>, fee -> i32) -> i32 {\n        if (prices.isEmpty()) return 0\n\n        var hold = -prices[0]\n        var cash = 0\n\n        for (i in 1..prices.len() - 1) {\n            cash = max( cash, hold + prices[i] - fee) // Sell the stock\n            hold = max(hold, cash - prices[i]) // buy the stock\n        }\n\n        return cash\n    }\n}"  cpp="class BestTimeToBuyAndSellStockWithTransactionFee {\n\n    $1(prices: vector<int>, fee) {\n        if (prices.isEmpty()) return 0\n\n        auto $1 = -prices[0]\n        auto $1 = 0\n\n        for (i in 1..prices.size() - 1) {\n            cash = max( cash, hold + prices[i] - fee) // Sell the stock\n            hold = max(hold, cash - prices[i]) // buy the stock\n        }\n\n        return cash\n    }\n}" %}

---

## BestTimeToBuyAndSellStock_III

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/stock_market/dp/BestTimeToBuyAndSellStock_III.kt`

{% include code-tabs.html  kotlin="package stock_market.dp\n\nclass BestTimeToBuyAndSellStock_III {\n\n}"  java="class BestTimeToBuyAndSellStock_III {\n\n}"  python="class BestTimeToBuyAndSellStock_III {\n\n}"  rust="class BestTimeToBuyAndSellStock_III {\n\n}"  cpp="class BestTimeToBuyAndSellStock_III {\n\n}" %}

---

## CherryPickup

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/grid/dynamic_programming/CherryPickup.kt`

{% include code-tabs.html  kotlin="package grid.dynamic_programming\n\nclass CherryPickup {\n\n    private val dp = mutableMapOf<Triple<Int, Int, Int>, Int>()\n    private lateinit var grid: Array<IntArray>\n    private var n: Int = 0\n\n    fun cherryPickup(grid: Array<IntArray>): Int {\n        this.grid = grid\n        n = grid.size\n        dp.clear()\n        return maxOf(0, dfs(0, 0, 0))\n    }\n\n    private fun dfs(row: Int, col1: Int, col2: Int): Int {\n        val row2 = row + col1 - col2\n        val state = Triple(row, col1, col2)\n\n        return when {\n            dp.contains(state) -> dp[state] !!\n            isInvalidPosition(row, row2, col1, col2) -> Int.MIN_VALUE\n            isFinalPosition(row, col1) -> grid[row][col1]\n            else -> {\n                val cherryCount = when {\n                    (col1 != col2) -> grid[row][col1] + grid[row2][col2]\n                    else ->  grid[row][col1]\n                }\n                val maxCherries = maxOf(\n                    dfs(row, col1 + 1, col2 + 1),\n                    dfs(row, col1 + 1, col2),\n                    dfs(row + 1, col1, col2),\n                    dfs(row + 1, col1, col2 + 1)\n                )\n                (cherryCount + maxCherries)\n            }\n        }.also { dp[state] = it }\n    }\n\n//    private fun isInvalidPosition(row: Int, row2: Int, col1: Int, col2: Int) =\n//        row >= n || row2 >= n || col1 >= n || col2 >= n || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private fun isInvalidPosition(row: Int, row2: Int, col1: Int, col2: Int) =\n        listOf(row, row2, col1, col2).any { it >= n } || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private fun isFinalPosition(row: Int, col1: Int) =\n        row == n - 1 && col1 == n - 1\n\n    companion object {\n        @JvmStatic\n        fun main(args: Array<String>) {\n\n        }\n    }\n}\n"  java="class CherryPickup {\n\n    private val dp = mutableMapOf<Triple<Int, Int, Int>, Int>()\n    private lateinit var grid: Array<IntArray>\n    private var n = 0\n\n    public static void $1(grid: Array<IntArray>) {\n        this.grid = grid\n        n = grid.size\n        dp.clear()\n        return maxOf(0, dfs(0, 0, 0))\n    }\n\n    private public static void $1(row, col1, col2) {\n        val row2 = row + col1 - col2\n        val state = Triple(row, col1, col2)\n\n        return when {\n            dp.contains(state) -> dp[state] !!\n            isInvalidPosition(row, row2, col1, col2) -> Int.MIN_VALUE\n            isFinalPosition(row, col1) -> grid[row][col1]\n            else -> {\n                val cherryCount = when {\n                    (col1 != col2) -> grid[row][col1] + grid[row2][col2]\n                    else ->  grid[row][col1]\n                }\n                val maxCherries = maxOf(\n                    dfs(row, col1 + 1, col2 + 1),\n                    dfs(row, col1 + 1, col2),\n                    dfs(row + 1, col1, col2),\n                    dfs(row + 1, col1, col2 + 1)\n                )\n                (cherryCount + maxCherries)\n            }\n        }.also { dp[state] = it }\n    }\n\n//    private public static void $1(row, row2, col1, col2) =\n//        row >= n || row2 >= n || col1 >= n || col2 >= n || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private public static void $1(row, row2, col1, col2) =\n        listOf(row, row2, col1, col2).any { it >= n } || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private public static void $1(row, col1) =\n        row == n - 1 && col1 == n - 1\n\n    companion object {\n        @JvmStatic\n        public static void $1(args: Array<String>) {\n\n        }\n    }\n}"  python="class CherryPickup {\n\n    private $1 = mutableMapOf<Triple<Int, Int, Int>, Int>()\n    private lateinit var grid: Array<list[int]>\n    private var n: int = 0\n\n    def $1(grid: Array<list[int]>): int {\n        this.grid = grid\n        n = grid.size\n        dp.clear()\n        return max(0, dfs(0, 0, 0))\n    }\n\n    private def $1(row: int, col1: int, col2: int): int {\n        $1 = row + col1 - col2\n        $1 = Triple(row, col1, col2)\n\n        return when {\n            dp.contains(state) -> dp[state] !!\n            isInvalidPosition(row, row2, col1, col2) -> Int.MIN_VALUE\n            isFinalPosition(row, col1) -> grid[row][col1]\n            else -> {\n                $1 = when {\n                    (col1 != col2) -> grid[row][col1] + grid[row2][col2]\n                    else ->  grid[row][col1]\n                }\n                $1 = max(\n                    dfs(row, col1 + 1, col2 + 1),\n                    dfs(row, col1 + 1, col2),\n                    dfs(row + 1, col1, col2),\n                    dfs(row + 1, col1, col2 + 1)\n                )\n                (cherryCount + maxCherries)\n            }\n        }.also { dp[state] = it }\n    }\n\n//    private def $1(row: int, row2: int, col1: int, col2: int) =\n//        row >= n || row2 >= n || col1 >= n || col2 >= n || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private def $1(row: int, row2: int, col1: int, col2: int) =\n        listOf(row, row2, col1, col2).any { it >= n } || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private def $1(row: int, col1: int) =\n        row == n - 1 && col1 == n - 1\n\n    companion object {\n        @JvmStatic\n        def $1(args: Array<String>) {\n\n        }\n    }\n}"  rust="class CherryPickup {\n\n    private val dp = mutableMapOf<Triple<Int, Int, Int>, Int>()\n    private lateinit var grid: Array<Vec<i32>>\n    private var n -> i32 = 0\n\n    fn $1(grid: Array<Vec<i32>>) -> i32 {\n        this.grid = grid\n        n = grid.size\n        dp.clear()\n        return max(0, dfs(0, 0, 0))\n    }\n\n    private fn $1(row -> i32, col1 -> i32, col2 -> i32) -> i32 {\n        val row2 = row + col1 - col2\n        val state = Triple(row, col1, col2)\n\n        return when {\n            dp.contains(state) -> dp[state] !!\n            isInvalidPosition(row, row2, col1, col2) -> Int.MIN_VALUE\n            isFinalPosition(row, col1) -> grid[row][col1]\n            else -> {\n                val cherryCount = when {\n                    (col1 != col2) -> grid[row][col1] + grid[row2][col2]\n                    else ->  grid[row][col1]\n                }\n                val maxCherries = max(\n                    dfs(row, col1 + 1, col2 + 1),\n                    dfs(row, col1 + 1, col2),\n                    dfs(row + 1, col1, col2),\n                    dfs(row + 1, col1, col2 + 1)\n                )\n                (cherryCount + maxCherries)\n            }\n        }.also { dp[state] = it }\n    }\n\n//    private fn $1(row -> i32, row2 -> i32, col1 -> i32, col2 -> i32) =\n//        row >= n || row2 >= n || col1 >= n || col2 >= n || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private fn $1(row -> i32, row2 -> i32, col1 -> i32, col2 -> i32) =\n        listOf(row, row2, col1, col2).any { it >= n } || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private fn $1(row -> i32, col1 -> i32) =\n        row == n - 1 && col1 == n - 1\n\n    companion object {\n        @JvmStatic\n        fn $1(args: Array<String>) {\n\n        }\n    }\n}"  cpp="class CherryPickup {\n\n    private auto $1 = mutableMapOf<Triple<Int, Int, Int>, Int>()\n    private lateinit var grid: Array<vector<int>>\n    private auto $1 = 0\n\n    $1(grid: Array<vector<int>>) {\n        this.grid = grid\n        n = grid.size\n        dp.clear()\n        return max(0, dfs(0, 0, 0))\n    }\n\n    private $1(row, col1, col2) {\n        auto $1 = row + col1 - col2\n        auto $1 = Triple(row, col1, col2)\n\n        return when {\n            dp.contains(state) -> dp[state] !!\n            isInvalidPosition(row, row2, col1, col2) -> Int.MIN_VALUE\n            isFinalPosition(row, col1) -> grid[row][col1]\n            else -> {\n                auto $1 = when {\n                    (col1 != col2) -> grid[row][col1] + grid[row2][col2]\n                    else ->  grid[row][col1]\n                }\n                auto $1 = max(\n                    dfs(row, col1 + 1, col2 + 1),\n                    dfs(row, col1 + 1, col2),\n                    dfs(row + 1, col1, col2),\n                    dfs(row + 1, col1, col2 + 1)\n                )\n                (cherryCount + maxCherries)\n            }\n        }.also { dp[state] = it }\n    }\n\n//    private $1(row, row2, col1, col2) =\n//        row >= n || row2 >= n || col1 >= n || col2 >= n || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private $1(row, row2, col1, col2) =\n        listOf(row, row2, col1, col2).any { it >= n } || grid[row][col1] == -1 || grid[row2][col2] == -1\n\n    private $1(row, col1) =\n        row == n - 1 && col1 == n - 1\n\n    companion object {\n        @JvmStatic\n        $1(args: Array<String>) {\n\n        }\n    }\n}" %}

---

## Test

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/grid/dynamic_programming/Test.kt`

{% include code-tabs.html  kotlin="package grid.dynamic_programming\n\nfun main() {\n    println(\"Isn't it cool Kotlin doesn't even require a main class ? \")\n}"  java="public static void $1() {\n    println(\"Isn't it cool Kotlin doesn't even require a main class ? \")\n}"  python="def $1() {\n    print(\"Isn't it cool Kotlin doesn't even require a main class ? \")\n}"  rust="fn $1() {\n    println!(\"Isn't it cool Kotlin doesn't even require a main class ? \")\n}"  cpp="$1() {\n    cout <<(\"Isn't it cool Kotlin doesn't even require a main class ? \")\n}" %}

---

## UniquePaths_I

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/grid/dynamic_programming/UniquePaths_I.kt`

{% include code-tabs.html  kotlin="package grid.dynamic_programming\n\nclass UniquePaths_I {\n    fun uniquePaths(m: Int, n: Int): Int {\n        val dp = Array(m) { IntArray(n){1} }\n\n        // Fill the first row and first column with 1's using `forEach`\n        dp[0].forEachIndexed { index, _ -> dp[0][index] = 1 }  // First row is filled with 1's\n        dp.forEach { it[0] = 1 }  // First column is filled with 1's\n\n        // Fill the rest of the DP table\n        for (row in 1 until m) {\n            for (col in 1 until n) {\n                dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n            }\n        }\n\n        // Return the value in the bottom-right corner\n        return dp[m - 1][n - 1]\n    }\n}\n"  java="class UniquePaths_I {\n    public static void $1(m, n) {\n        val dp = Array(m) { IntArray(n){1} }\n\n        // Fill the first row and first column with 1's using `forEach`\n        dp[0].forEachIndexed { index, _ -> dp[0][index] = 1 }  // First row is filled with 1's\n        dp.forEach { it[0] = 1 }  // First column is filled with 1's\n\n        // Fill the rest of the DP table\n        for (row in 1 until m) {\n            for (col in 1 until n) {\n                dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n            }\n        }\n\n        // Return the value in the bottom-right corner\n        return dp[m - 1][n - 1]\n    }\n}"  python="class UniquePaths_I {\n    def $1(m: int, n: int): int {\n        $1 = Array(m) { list[int](n){1} }\n\n        // Fill the first row and first column with 1's using `forEach`\n        dp[0].forEachIndexed { index, _ -> dp[0][index] = 1 }  // First row is filled with 1's\n        dp.forEach { it[0] = 1 }  // First column is filled with 1's\n\n        // Fill the rest of the DP table\n        for (row in 1 until m) {\n            for (col in 1 until n) {\n                dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n            }\n        }\n\n        // Return the value in the bottom-right corner\n        return dp[m - 1][n - 1]\n    }\n}"  rust="class UniquePaths_I {\n    fn $1(m -> i32, n -> i32) -> i32 {\n        val dp = Array(m) { Vec<i32>(n){1} }\n\n        // Fill the first row and first column with 1's using `forEach`\n        dp[0].forEachIndexed { index, _ -> dp[0][index] = 1 }  // First row is filled with 1's\n        dp.forEach { it[0] = 1 }  // First column is filled with 1's\n\n        // Fill the rest of the DP table\n        for (row in 1 until m) {\n            for (col in 1 until n) {\n                dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n            }\n        }\n\n        // Return the value in the bottom-right corner\n        return dp[m - 1][n - 1]\n    }\n}"  cpp="class UniquePaths_I {\n    $1(m, n) {\n        auto $1 = Array(m) { vector<int>(n){1} }\n\n        // Fill the first row and first column with 1's using `forEach`\n        dp[0].forEachIndexed { index, _ -> dp[0][index] = 1 }  // First row is filled with 1's\n        dp.forEach { it[0] = 1 }  // First column is filled with 1's\n\n        // Fill the rest of the DP table\n        for (row in 1 until m) {\n            for (col in 1 until n) {\n                dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n            }\n        }\n\n        // Return the value in the bottom-right corner\n        return dp[m - 1][n - 1]\n    }\n}" %}

---

## UniquePaths_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/grid/dynamic_programming/UniquePaths_II.kt`

{% include code-tabs.html  kotlin="package grid.dynamic_programming\n\nimport oracle.net.aso.m\n\nclass UniquePaths_II {\n    fun uniquePathsWithObstacles(obstacleGrid: Array<IntArray>): Int {\n        val (R, C) = obstacleGrid.size to obstacleGrid[0].size\n        val dp = Array(R) { IntArray(C) }.apply { this[0][0] = 1 }\n\n        // Obstacle in the first cell itself hence nothing is feasible\n        if (obstacleGrid[0][0] == 1) return 0\n\n        // Fill the first row\n        for (col in 1 until C) {\n            dp[0][col] = if (obstacleGrid[0][col] == 1) 0 else dp[0][col - 1]\n        }\n\n        // Fill the first column\n        for (row in 1 until R) {\n            dp[row][0] = if (obstacleGrid[row][0] == 1) 0 else dp[row - 1][0]\n        }\n\n        // Fill the rest of the DP table\n        for (row in 1 until R) {\n            for (col in 1 until C) {\n                when(obstacleGrid[row][col]) {\n                    1 -> dp[row][col] = 0\n                    else -> dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n                }\n            }\n        }\n        // Return the value in the bottom-right corner\n        return dp[R - 1][C - 1]\n    }\n}\n"  java="class UniquePaths_II {\n    public static void $1(obstacleGrid: Array<IntArray>) {\n        val (R, C) = obstacleGrid.size to obstacleGrid[0].size\n        val dp = Array(R) { IntArray(C) }.apply { this[0][0] = 1 }\n\n        // Obstacle in the first cell itself hence nothing is feasible\n        if (obstacleGrid[0][0] == 1) return 0\n\n        // Fill the first row\n        for (col in 1 until C) {\n            dp[0][col] = if (obstacleGrid[0][col] == 1) 0 else dp[0][col - 1]\n        }\n\n        // Fill the first column\n        for (row in 1 until R) {\n            dp[row][0] = if (obstacleGrid[row][0] == 1) 0 else dp[row - 1][0]\n        }\n\n        // Fill the rest of the DP table\n        for (row in 1 until R) {\n            for (col in 1 until C) {\n                when(obstacleGrid[row][col]) {\n                    1 -> dp[row][col] = 0\n                    else -> dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n                }\n            }\n        }\n        // Return the value in the bottom-right corner\n        return dp[R - 1][C - 1]\n    }\n}"  python="class UniquePaths_II {\n    def $1(obstacleGrid: Array<list[int]>): int {\n        ($1, $2) = obstacleGrid.size to obstacleGrid[0].size\n        $1 = Array(R) { list[int](C) }.apply { this[0][0] = 1 }\n\n        // Obstacle in the first cell itself hence nothing is feasible\n        if (obstacleGrid[0][0] == 1) return 0\n\n        // Fill the first row\n        for (col in 1 until C) {\n            dp[0][col] = if (obstacleGrid[0][col] == 1) 0 else dp[0][col - 1]\n        }\n\n        // Fill the first column\n        for (row in 1 until R) {\n            dp[row][0] = if (obstacleGrid[row][0] == 1) 0 else dp[row - 1][0]\n        }\n\n        // Fill the rest of the DP table\n        for (row in 1 until R) {\n            for (col in 1 until C) {\n                when(obstacleGrid[row][col]) {\n                    1 -> dp[row][col] = 0\n                    else -> dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n                }\n            }\n        }\n        // Return the value in the bottom-right corner\n        return dp[R - 1][C - 1]\n    }\n}"  rust="class UniquePaths_II {\n    fn $1(obstacleGrid: Array<Vec<i32>>) -> i32 {\n        val (R, C) = obstacleGrid.size to obstacleGrid[0].size\n        val dp = Array(R) { Vec<i32>(C) }.apply { this[0][0] = 1 }\n\n        // Obstacle in the first cell itself hence nothing is feasible\n        if (obstacleGrid[0][0] == 1) return 0\n\n        // Fill the first row\n        for (col in 1 until C) {\n            dp[0][col] = if (obstacleGrid[0][col] == 1) 0 else dp[0][col - 1]\n        }\n\n        // Fill the first column\n        for (row in 1 until R) {\n            dp[row][0] = if (obstacleGrid[row][0] == 1) 0 else dp[row - 1][0]\n        }\n\n        // Fill the rest of the DP table\n        for (row in 1 until R) {\n            for (col in 1 until C) {\n                when(obstacleGrid[row][col]) {\n                    1 -> dp[row][col] = 0\n                    else -> dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n                }\n            }\n        }\n        // Return the value in the bottom-right corner\n        return dp[R - 1][C - 1]\n    }\n}"  cpp="class UniquePaths_II {\n    $1(obstacleGrid: Array<vector<int>>) {\n        val (R, C) = obstacleGrid.size to obstacleGrid[0].size\n        auto $1 = Array(R) { vector<int>(C) }.apply { this[0][0] = 1 }\n\n        // Obstacle in the first cell itself hence nothing is feasible\n        if (obstacleGrid[0][0] == 1) return 0\n\n        // Fill the first row\n        for (col in 1 until C) {\n            dp[0][col] = if (obstacleGrid[0][col] == 1) 0 else dp[0][col - 1]\n        }\n\n        // Fill the first column\n        for (row in 1 until R) {\n            dp[row][0] = if (obstacleGrid[row][0] == 1) 0 else dp[row - 1][0]\n        }\n\n        // Fill the rest of the DP table\n        for (row in 1 until R) {\n            for (col in 1 until C) {\n                when(obstacleGrid[row][col]) {\n                    1 -> dp[row][col] = 0\n                    else -> dp[row][col] = dp[row - 1][col] + dp[row][col - 1]\n                }\n            }\n        }\n        // Return the value in the bottom-right corner\n        return dp[R - 1][C - 1]\n    }\n}" %}
