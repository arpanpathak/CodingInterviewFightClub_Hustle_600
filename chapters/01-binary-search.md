---
layout: chapter
title: "Binary Search"
chapter_number: 1
chapter_title: "Binary Search"
toc: true
next_chapter:
  url: "/chapters/02-dynamic-programming"
  title: "Dynamic Programming"
---

# Binary Search

> **21 problems** from the CrackGoogle repository. Click the tabs to switch between **Kotlin, Java, Python, Rust, and C++**.

## Complete Problem Set

| # | Problem | Topic | Code Available |
|---|---------|-------|----------------|
| 1 | **ApartmentHunting** | binarysearch | ✅ All 5 languages |
| 2 | **CapacityToShipPackageWithinDDays** | binarysearch | ✅ All 5 languages |
| 3 | **ClosestSebsequenceSum** | binarysearch | ✅ All 5 languages |
| 4 | **FindFirstAndLastPosition** | binarysearch | ✅ All 5 languages |
| 5 | **FindKClosestElements** | binarysearch | ✅ All 5 languages |
| 6 | **FindMinimumInRotatedSortedArray** | binarysearch | ✅ All 5 languages |
| 7 | **FindPeakElement** | binarysearch | ✅ All 5 languages |
| 8 | **FindPeakElementBetterSolution** | binarysearch | ✅ All 5 languages |
| 9 | **FirstBadVersion** | binarysearch | ✅ All 5 languages |
| 10 | **GuessNumberHigherOrLower** | binarysearch | ✅ All 5 languages |
| 11 | **HouseRobber_IV** | binarysearch | ✅ All 5 languages |
| 12 | **KThMissingPositiveNumber** | binarysearch | ✅ All 5 languages |
| 13 | **KokoEatingBanana** | binarysearch | ✅ All 5 languages |
| 14 | **MedianOfTwoSortedARrays** | binarysearch | ✅ All 5 languages |
| 15 | **RandomPickWithWeight** | binarysearch | ✅ All 5 languages |
| 16 | **SearchA2dMatrix** | binarysearch | ✅ All 5 languages |
| 17 | **SearchInRotatedArray_II** | binarysearch | ✅ All 5 languages |
| 18 | **SearchInRotatedSortedArray** | binarysearch | ✅ All 5 languages |
| 19 | **SearchInsertionPosition** | binarysearch | ✅ All 5 languages |
| 20 | **SingleElementInASortedArray** | binarysearch | ✅ All 5 languages |
| 21 | **ValleyElement** | binarysearch | ✅ All 5 languages |

---

## ApartmentHunting

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/ApartmentHunting.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nfun findBestBlock(blocks: List<Map<String, Boolean>>, requirements: List<String>): Int {\n    // Step 1: Create a HashMap where each key is an amenity and each value is a sorted list of blocks where the amenity is present\n    val amenityMap = mutableMapOf<String, MutableList<Int>>()\n    blocks.forEachIndexed { index, block ->\n        block.forEach { (amenity, present) ->\n            if (present) {\n                amenityMap.getOrPut(amenity) { mutableListOf() }.add(index)\n            }\n        }\n    }\n\n    // Ensure all required amenities are present\n    requirements.forEach {\n        if (amenityMap[it].isNullOrEmpty()) return -1 // Return -1 if any required amenity is not present in any block\n    }\n\n    // Step 2: Determine the optimal block\n    var bestBlock = -1\n    var bestMaxDistance = Int.MAX_VALUE\n\n    // N * R * Log(K) , K = number of unique amininties present across all the blocks\n    blocks.forEachIndexed { index, _ ->\n        val maxDistance = requirements.map { amenity ->\n            closestDistance(index, amenityMap[amenity]!!)\n        }.maxOrNull()!!\n\n        // Check if the current block has the smallest maximum distance\n        if (maxDistance < bestMaxDistance) {\n            bestMaxDistance = maxDistance\n            bestBlock = index\n        }\n    }\n\n    return bestBlock\n}\n\n// Function to find the minimum distance to the closest block with the given amenity\nfun closestDistance(blockIndex: Int, blocksWithAmenity: List<Int>): Int {\n    val pos = blocksWithAmenity.binarySearch(blockIndex)\n    return if (pos >= 0) 0\n    else {\n        val insertPoint = -pos - 1\n        val leftDistance = if (insertPoint > 0) blockIndex - blocksWithAmenity[insertPoint - 1] else Int.MAX_VALUE\n        val rightDistance = if (insertPoint < blocksWithAmenity.size) blocksWithAmenity[insertPoint] - blockIndex else Int.MAX_VALUE\n        minOf(leftDistance, rightDistance)\n    }\n}\n\n// Custom Binary Search implementation\nfun binarySearch(arr: List<Int>, target: Int): Int {\n    var (low, high) = listOf(0, arr.lastIndex)\n\n    while (low<= high) {\n        val mid = low + (high - low) / 2\n        when {\n            arr[mid] < target -> low = mid + 1\n            arr[mid] > target -> high = mid - 1\n            else -> return mid\n        }\n    }\n\n    return -(low + 1)\n}\n\n\n\nfun main() {\n    val blocks = listOf(\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to true, \"school\" to false, \"store\" to false),\n        mapOf(\"gym\" to true, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to true)\n    )\n    val requirements = listOf(\"gym\", \"school\", \"store\")\n\n\n    val bestBlock = findBestBlock(blocks, requirements)\n    println(\"The best block to choose is: $bestBlock\")\n}\n"  java="public static void $1(blocks: List<Map<String, Boolean>>, requirements: List<String>) {\n    // Step 1: Create a HashMap where each key is an amenity and each value is a sorted list of blocks where the amenity is present\n    val amenityMap = mutableMapOf<String, MutableList<Int>>()\n    blocks.forEachIndexed { index, block ->\n        block.forEach { (amenity, present) ->\n            if (present) {\n                amenityMap.getOrPut(amenity) { mutableListOf() }.add(index)\n            }\n        }\n    }\n\n    // Ensure all required amenities are present\n    requirements.forEach {\n        if (amenityMap[it].isNullOrEmpty()) return -1 // Return -1 if any required amenity is not present in any block\n    }\n\n    // Step 2: Determine the optimal block\n    var bestBlock = -1\n    var bestMaxDistance = Int.MAX_VALUE\n\n    // N * R * Log(K) , K = number of unique amininties present across all the blocks\n    blocks.forEachIndexed { index, _ ->\n        val maxDistance = requirements.map { amenity ->\n            closestDistance(index, amenityMap[amenity]!!)\n        }.maxOrNull()!!\n\n        // Check if the current block has the smallest maximum distance\n        if (maxDistance < bestMaxDistance) {\n            bestMaxDistance = maxDistance\n            bestBlock = index\n        }\n    }\n\n    return bestBlock\n}\n\n// Function to find the minimum distance to the closest block with the given amenity\npublic static void $1(blockIndex, blocksWithAmenity: List<Int>) {\n    val pos = blocksWithAmenity.binarySearch(blockIndex)\n    return if (pos >= 0) 0\n    else {\n        val insertPoint = -pos - 1\n        val leftDistance = if (insertPoint > 0) blockIndex - blocksWithAmenity[insertPoint - 1] else Int.MAX_VALUE\n        val rightDistance = if (insertPoint < blocksWithAmenity.size) blocksWithAmenity[insertPoint] - blockIndex else Int.MAX_VALUE\n        minOf(leftDistance, rightDistance)\n    }\n}\n\n// Custom Binary Search implementation\npublic static void $1(arr: List<Int>, target) {\n    var (low, high) = listOf(0, arr.lastIndex)\n\n    while (low<= high) {\n        val mid = low + (high - low) / 2\n        when {\n            arr[mid] < target -> low = mid + 1\n            arr[mid] > target -> high = mid - 1\n            else -> return mid\n        }\n    }\n\n    return -(low + 1)\n}\n\n\n\npublic static void $1() {\n    val blocks = listOf(\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to true, \"school\" to false, \"store\" to false),\n        mapOf(\"gym\" to true, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to true)\n    )\n    val requirements = listOf(\"gym\", \"school\", \"store\")\n\n\n    val bestBlock = findBestBlock(blocks, requirements)\n    println(\"The best block to choose is: $bestBlock\")\n}"  python="def $1(blocks: List<Map<String, Boolean>>, requirements: List<String>): int {\n    // Step 1: Create a HashMap where each key is an amenity and each value is a sorted list of blocks where the amenity is present\n    $1 = mutableMapOf<String, MutableList<Int>>()\n    blocks.forEachIndexed { index, block ->\n        block.forEach { (amenity, present) ->\n            if (present) {\n                amenityMap.getOrPut(amenity) { mutableListOf() }.add(index)\n            }\n        }\n    }\n\n    // Ensure all required amenities are present\n    requirements.forEach {\n        if (amenityMap[it].isNullOrEmpty()) return -1 // Return -1 if any required amenity is not present in any block\n    }\n\n    // Step 2: Determine the optimal block\n    $1 = -1\n    $1 = Int.MAX_VALUE\n\n    // N * R * Log(K) , K = number of unique amininties present across all the blocks\n    blocks.forEachIndexed { index, _ ->\n        $1 = requirements.map { amenity ->\n            closestDistance(index, amenityMap[amenity]!!)\n        }.maxOrNull()!!\n\n        // Check if the current block has the smallest maximum distance\n        if (maxDistance < bestMaxDistance) {\n            bestMaxDistance = maxDistance\n            bestBlock = index\n        }\n    }\n\n    return bestBlock\n}\n\n// Function to find the minimum distance to the closest block with the given amenity\ndef $1(blockIndex: int, blocksWithAmenity: List<Int>): int {\n    $1 = blocksWithAmenity.binarySearch(blockIndex)\n    return if (pos >= 0) 0\n    else {\n        $1 = -pos - 1\n        $1 = if (insertPoint > 0) blockIndex - blocksWithAmenity[insertPoint - 1] else Int.MAX_VALUE\n        $1 = if (insertPoint < blocksWithAmenity.size) blocksWithAmenity[insertPoint] - blockIndex else Int.MAX_VALUE\n        min(leftDistance, rightDistance)\n    }\n}\n\n// Custom Binary Search implementation\ndef $1(arr: List<Int>, target: int): int {\n    var (low, high) = listOf(0, arr.lastIndex)\n\n    while (low<= high) {\n        $1 = low + (high - low) / 2\n        when {\n            arr[mid] < target -> low = mid + 1\n            arr[mid] > target -> high = mid - 1\n            else -> return mid\n        }\n    }\n\n    return -(low + 1)\n}\n\n\n\ndef $1() {\n    $1 = listOf(\n        mapOf(\"gym\" to False, \"school\" to True, \"store\" to False),\n        mapOf(\"gym\" to True, \"school\" to False, \"store\" to False),\n        mapOf(\"gym\" to True, \"school\" to True, \"store\" to False),\n        mapOf(\"gym\" to False, \"school\" to True, \"store\" to False),\n        mapOf(\"gym\" to False, \"school\" to True, \"store\" to True)\n    )\n    $1 = listOf(\"gym\", \"school\", \"store\")\n\n\n    $1 = findBestBlock(blocks, requirements)\n    print(\"The best block to choose is: $bestBlock\")\n}"  rust="fn $1(blocks: List<Map<String, Boolean>>, requirements: List<String>) -> i32 {\n    // Step 1: Create a HashMap where each key is an amenity and each value is a sorted list of blocks where the amenity is present\n    val amenityMap = mutableMapOf<String, MutableList<Int>>()\n    blocks.forEachIndexed { index, block ->\n        block.forEach { (amenity, present) ->\n            if (present) {\n                amenityMap.getOrPut(amenity) { mutableListOf() }.add(index)\n            }\n        }\n    }\n\n    // Ensure all required amenities are present\n    requirements.forEach {\n        if (amenityMap[it].isNullOrEmpty()) return -1 // Return -1 if any required amenity is not present in any block\n    }\n\n    // Step 2: Determine the optimal block\n    var bestBlock = -1\n    var bestMaxDistance = Int.MAX_VALUE\n\n    // N * R * Log(K) , K = number of unique amininties present across all the blocks\n    blocks.forEachIndexed { index, _ ->\n        val maxDistance = requirements.map { amenity ->\n            closestDistance(index, amenityMap[amenity]!!)\n        }.maxOrNull()!!\n\n        // Check if the current block has the smallest maximum distance\n        if (maxDistance < bestMaxDistance) {\n            bestMaxDistance = maxDistance\n            bestBlock = index\n        }\n    }\n\n    return bestBlock\n}\n\n// Function to find the minimum distance to the closest block with the given amenity\nfn $1(blockIndex -> i32, blocksWithAmenity: List<Int>) -> i32 {\n    val pos = blocksWithAmenity.binarySearch(blockIndex)\n    return if (pos >= 0) 0\n    else {\n        val insertPoint = -pos - 1\n        val leftDistance = if (insertPoint > 0) blockIndex - blocksWithAmenity[insertPoint - 1] else Int.MAX_VALUE\n        val rightDistance = if (insertPoint < blocksWithAmenity.size) blocksWithAmenity[insertPoint] - blockIndex else Int.MAX_VALUE\n        min(leftDistance, rightDistance)\n    }\n}\n\n// Custom Binary Search implementation\nfn $1(arr: List<Int>, target -> i32) -> i32 {\n    var (low, high) = listOf(0, arr.len() - 1)\n\n    while (low<= high) {\n        val mid = low + (high - low) / 2\n        when {\n            arr[mid] < target -> low = mid + 1\n            arr[mid] > target -> high = mid - 1\n            else -> return mid\n        }\n    }\n\n    return -(low + 1)\n}\n\n\n\nfn $1() {\n    val blocks = listOf(\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to true, \"school\" to false, \"store\" to false),\n        mapOf(\"gym\" to true, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to true)\n    )\n    val requirements = listOf(\"gym\", \"school\", \"store\")\n\n\n    val bestBlock = findBestBlock(blocks, requirements)\n    println!(\"The best block to choose is: $bestBlock\")\n}"  cpp="$1(blocks: List<Map<String, Boolean>>, requirements: List<String>) {\n    // Step 1: Create a HashMap where each key is an amenity and each value is a sorted list of blocks where the amenity is present\n    auto $1 = mutableMapOf<String, MutableList<Int>>()\n    blocks.forEachIndexed { index, block ->\n        block.forEach { (amenity, present) ->\n            if (present) {\n                amenityMap.getOrPut(amenity) { mutableListOf() }.add(index)\n            }\n        }\n    }\n\n    // Ensure all required amenities are present\n    requirements.forEach {\n        if (amenityMap[it].isNullOrEmpty()) return -1 // Return -1 if any required amenity is not present in any block\n    }\n\n    // Step 2: Determine the optimal block\n    auto $1 = -1\n    auto $1 = Int.MAX_VALUE\n\n    // N * R * Log(K) , K = number of unique amininties present across all the blocks\n    blocks.forEachIndexed { index, _ ->\n        auto $1 = requirements.map { amenity ->\n            closestDistance(index, amenityMap[amenity]!!)\n        }.maxOrNull()!!\n\n        // Check if the current block has the smallest maximum distance\n        if (maxDistance < bestMaxDistance) {\n            bestMaxDistance = maxDistance\n            bestBlock = index\n        }\n    }\n\n    return bestBlock\n}\n\n// Function to find the minimum distance to the closest block with the given amenity\n$1(blockIndex, blocksWithAmenity: List<Int>) {\n    auto $1 = blocksWithAmenity.binarySearch(blockIndex)\n    return if (pos >= 0) 0\n    else {\n        auto $1 = -pos - 1\n        auto $1 = if (insertPoint > 0) blockIndex - blocksWithAmenity[insertPoint - 1] else Int.MAX_VALUE\n        auto $1 = if (insertPoint < blocksWithAmenity.size) blocksWithAmenity[insertPoint] - blockIndex else Int.MAX_VALUE\n        min(leftDistance, rightDistance)\n    }\n}\n\n// Custom Binary Search implementation\n$1(arr: List<Int>, target) {\n    var (low, high) = listOf(0, arr.size() - 1)\n\n    while (low<= high) {\n        auto $1 = low + (high - low) / 2\n        when {\n            arr[mid] < target -> low = mid + 1\n            arr[mid] > target -> high = mid - 1\n            else -> return mid\n        }\n    }\n\n    return -(low + 1)\n}\n\n\n\n$1() {\n    auto $1 = listOf(\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to true, \"school\" to false, \"store\" to false),\n        mapOf(\"gym\" to true, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to false),\n        mapOf(\"gym\" to false, \"school\" to true, \"store\" to true)\n    )\n    auto $1 = listOf(\"gym\", \"school\", \"store\")\n\n\n    auto $1 = findBestBlock(blocks, requirements)\n    cout <<(\"The best block to choose is: $bestBlock\")\n}" %}

---

## CapacityToShipPackageWithinDDays

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/CapacityToShipPackageWithinDDays.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass CapacityToShipPackageWithinDDays {\n    fun feasible(weights: IntArray, mid:Int, days: Int): Boolean {\n        var (daysNeeded, currentLoad) = 1 to 0\n        weights.forEach { weight ->\n            currentLoad += weight\n            if (currentLoad > mid) {\n                daysNeeded++\n                currentLoad = weight\n            }\n        }\n\n        return daysNeeded <= days\n    }\n\n    fun shipWithinDays(weights: IntArray, days: Int): Int {\n        var (sum, max) = 0 to 0\n\n        weights.forEach{ weight ->\n            sum += weight\n            max = maxOf(max, weight)\n        }\n\n        var (l, r) = max to sum\n\n        while ( l < r) {\n            val mid = l + (r - l) / 2\n            when {\n                feasible(weights, mid, days) -> r = mid\n                else -> l = mid + 1\n            }\n        }\n\n        return l\n    }\n}"  java="class CapacityToShipPackageWithinDDays {\n    public static void $1(weights: IntArray, mid, days) {\n        var (daysNeeded, currentLoad) = 1 to 0\n        weights.forEach { weight ->\n            currentLoad += weight\n            if (currentLoad > mid) {\n                daysNeeded++\n                currentLoad = weight\n            }\n        }\n\n        return daysNeeded <= days\n    }\n\n    public static void $1(weights: IntArray, days) {\n        var (sum, max) = 0 to 0\n\n        weights.forEach{ weight ->\n            sum += weight\n            max = maxOf(max, weight)\n        }\n\n        var (l, r) = max to sum\n\n        while ( l < r) {\n            val mid = l + (r - l) / 2\n            when {\n                feasible(weights, mid, days) -> r = mid\n                else -> l = mid + 1\n            }\n        }\n\n        return l\n    }\n}"  python="class CapacityToShipPackageWithinDDays {\n    def $1(weights: list[int], mid: int, days: int): bool {\n        var (daysNeeded, currentLoad) = 1 to 0\n        weights.forEach { weight ->\n            currentLoad += weight\n            if (currentLoad > mid) {\n                daysNeeded++\n                currentLoad = weight\n            }\n        }\n\n        return daysNeeded <= days\n    }\n\n    def $1(weights: list[int], days: int): int {\n        var (sum, max) = 0 to 0\n\n        weights.forEach{ weight ->\n            sum += weight\n            max = max(max, weight)\n        }\n\n        var (l, r) = max to sum\n\n        while ( l < r) {\n            $1 = l + (r - l) / 2\n            when {\n                feasible(weights, mid, days) -> r = mid\n                else -> l = mid + 1\n            }\n        }\n\n        return l\n    }\n}"  rust="class CapacityToShipPackageWithinDDays {\n    fn $1(weights: Vec<i32>, mid -> i32, days -> i32) -> bool {\n        var (daysNeeded, currentLoad) = 1 to 0\n        weights.forEach { weight ->\n            currentLoad += weight\n            if (currentLoad > mid) {\n                daysNeeded++\n                currentLoad = weight\n            }\n        }\n\n        return daysNeeded <= days\n    }\n\n    fn $1(weights: Vec<i32>, days -> i32) -> i32 {\n        var (sum, max) = 0 to 0\n\n        weights.forEach{ weight ->\n            sum += weight\n            max = max(max, weight)\n        }\n\n        var (l, r) = max to sum\n\n        while ( l < r) {\n            val mid = l + (r - l) / 2\n            when {\n                feasible(weights, mid, days) -> r = mid\n                else -> l = mid + 1\n            }\n        }\n\n        return l\n    }\n}"  cpp="class CapacityToShipPackageWithinDDays {\n    $1(weights: vector<int>, mid, days) {\n        var (daysNeeded, currentLoad) = 1 to 0\n        weights.forEach { weight ->\n            currentLoad += weight\n            if (currentLoad > mid) {\n                daysNeeded++\n                currentLoad = weight\n            }\n        }\n\n        return daysNeeded <= days\n    }\n\n    $1(weights: vector<int>, days) {\n        var (sum, max) = 0 to 0\n\n        weights.forEach{ weight ->\n            sum += weight\n            max = max(max, weight)\n        }\n\n        var (l, r) = max to sum\n\n        while ( l < r) {\n            auto $1 = l + (r - l) / 2\n            when {\n                feasible(weights, mid, days) -> r = mid\n                else -> l = mid + 1\n            }\n        }\n\n        return l\n    }\n}" %}

---

## ClosestSebsequenceSum

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/ClosestSebsequenceSum.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nimport kotlin.math.abs\n\nclass ClosestSebsequenceSum {\n    fun minAbsDifference(nums: IntArray, goal: Int): Int {\n        val leftSums = mutableListOf<Int>()\n        val rightSums = mutableListOf<Int>()\n        val n = nums.size\n        val mid = n / 2\n\n        fun dfs(start: Int, end: Int, currentSum: Int, result: MutableList<Int>) {\n            if (start == end) {\n                result.add(currentSum)\n                return\n            }\n            dfs(start + 1, end, currentSum, result) // exclude\n            dfs(start + 1, end, currentSum + nums[start], result) // include\n        }\n\n        dfs(0, mid, 0, leftSums)\n        dfs(mid, n, 0, rightSums)\n\n        rightSums.sort()\n        var minDiff = Int.MAX_VALUE\n\n        for (sumLeft in leftSums) {\n            val target = goal - sumLeft\n            val idx = rightSums.binarySearch(target)\n            if (idx >= 0) return 0 // exact match found\n\n            val insertPoint = -idx - 1\n            if (insertPoint < rightSums.size)\n                minDiff = minOf(minDiff, abs(sumLeft + rightSums[insertPoint] - goal))\n            if (insertPoint > 0)\n                minDiff = minOf(minDiff, abs(sumLeft + rightSums[insertPoint - 1] - goal))\n        }\n\n        return minDiff\n    }\n}\n"  java="class ClosestSebsequenceSum {\n    public static void $1(nums: IntArray, goal) {\n        val leftSums = mutableListOf<Int>()\n        val rightSums = mutableListOf<Int>()\n        val n = nums.size\n        val mid = n / 2\n\n        public static void $1(start, end, currentSum, result: MutableList<Int>) {\n            if (start == end) {\n                result.add(currentSum)\n                return\n            }\n            dfs(start + 1, end, currentSum, result) // exclude\n            dfs(start + 1, end, currentSum + nums[start], result) // include\n        }\n\n        dfs(0, mid, 0, leftSums)\n        dfs(mid, n, 0, rightSums)\n\n        rightSums.sort()\n        var minDiff = Int.MAX_VALUE\n\n        for (sumLeft in leftSums) {\n            val target = goal - sumLeft\n            val idx = rightSums.binarySearch(target)\n            if (idx >= 0) return 0 // exact match found\n\n            val insertPoint = -idx - 1\n            if (insertPoint < rightSums.size)\n                minDiff = minOf(minDiff, abs(sumLeft + rightSums[insertPoint] - goal))\n            if (insertPoint > 0)\n                minDiff = minOf(minDiff, abs(sumLeft + rightSums[insertPoint - 1] - goal))\n        }\n\n        return minDiff\n    }\n}"  python="class ClosestSebsequenceSum {\n    def $1(nums: list[int], goal: int): int {\n        $1 = mutableListOf<Int>()\n        $1 = mutableListOf<Int>()\n        $1 = nums.size\n        $1 = n / 2\n\n        def $1(start: int, end: int, currentSum: int, result: MutableList<Int>) {\n            if (start == end) {\n                result.add(currentSum)\n                return\n            }\n            dfs(start + 1, end, currentSum, result) // exclude\n            dfs(start + 1, end, currentSum + nums[start], result) // include\n        }\n\n        dfs(0, mid, 0, leftSums)\n        dfs(mid, n, 0, rightSums)\n\n        rightSums.sort()\n        $1 = Int.MAX_VALUE\n\n        for (sumLeft in leftSums) {\n            $1 = goal - sumLeft\n            $1 = rightSums.binarySearch(target)\n            if (idx >= 0) return 0 // exact match found\n\n            $1 = -idx - 1\n            if (insertPoint < rightSums.size)\n                minDiff = min(minDiff, abs(sumLeft + rightSums[insertPoint] - goal))\n            if (insertPoint > 0)\n                minDiff = min(minDiff, abs(sumLeft + rightSums[insertPoint - 1] - goal))\n        }\n\n        return minDiff\n    }\n}"  rust="class ClosestSebsequenceSum {\n    fn $1(nums: Vec<i32>, goal -> i32) -> i32 {\n        val leftSums = mutableListOf<Int>()\n        val rightSums = mutableListOf<Int>()\n        val n = nums.size\n        val mid = n / 2\n\n        fn $1(start -> i32, end -> i32, currentSum -> i32, result: MutableList<Int>) {\n            if (start == end) {\n                result.add(currentSum)\n                return\n            }\n            dfs(start + 1, end, currentSum, result) // exclude\n            dfs(start + 1, end, currentSum + nums[start], result) // include\n        }\n\n        dfs(0, mid, 0, leftSums)\n        dfs(mid, n, 0, rightSums)\n\n        rightSums.sort()\n        var minDiff = Int.MAX_VALUE\n\n        for (sumLeft in leftSums) {\n            val target = goal - sumLeft\n            val idx = rightSums.binarySearch(target)\n            if (idx >= 0) return 0 // exact match found\n\n            val insertPoint = -idx - 1\n            if (insertPoint < rightSums.size)\n                minDiff = min(minDiff, abs(sumLeft + rightSums[insertPoint] - goal))\n            if (insertPoint > 0)\n                minDiff = min(minDiff, abs(sumLeft + rightSums[insertPoint - 1] - goal))\n        }\n\n        return minDiff\n    }\n}"  cpp="class ClosestSebsequenceSum {\n    $1(nums: vector<int>, goal) {\n        auto $1 = mutableListOf<Int>()\n        auto $1 = mutableListOf<Int>()\n        auto $1 = nums.size\n        auto $1 = n / 2\n\n        $1(start, end, currentSum, result: MutableList<Int>) {\n            if (start == end) {\n                result.add(currentSum)\n                return\n            }\n            dfs(start + 1, end, currentSum, result) // exclude\n            dfs(start + 1, end, currentSum + nums[start], result) // include\n        }\n\n        dfs(0, mid, 0, leftSums)\n        dfs(mid, n, 0, rightSums)\n\n        rightSums.sort()\n        auto $1 = Int.MAX_VALUE\n\n        for (sumLeft in leftSums) {\n            auto $1 = goal - sumLeft\n            auto $1 = rightSums.binarySearch(target)\n            if (idx >= 0) return 0 // exact match found\n\n            auto $1 = -idx - 1\n            if (insertPoint < rightSums.size)\n                minDiff = min(minDiff, abs(sumLeft + rightSums[insertPoint] - goal))\n            if (insertPoint > 0)\n                minDiff = min(minDiff, abs(sumLeft + rightSums[insertPoint - 1] - goal))\n        }\n\n        return minDiff\n    }\n}" %}

---

## FindFirstAndLastPosition

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/FindFirstAndLastPosition.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass FindFirstAndLastPosition {\n\n    fun searchRange(nums: IntArray, target: Int): IntArray {\n        val result = intArrayOf(-1, -1)\n\n        // Find the first occurrence\n        result[0] = binarySearch(nums, target, true)\n\n        // If the first occurrence is not found, return [-1, -1]\n        if (result[0] == -1) return result\n\n        // Find the last occurrence\n        result[1] = binarySearch(nums, target, false)\n\n        return result\n    }\n\n    private fun binarySearch(nums: IntArray, target: Int, findFirst: Boolean): Int {\n        var left = 0\n        var right = nums.lastIndex\n        var result = -1\n\n        while (left <= right) {\n            val mid = left + (right - left) / 2\n            when {\n                nums[mid] == target -> {\n                    result = mid\n                    // Adjust the search range based on whether we are looking for the first or last occurrence\n                    if (findFirst) {\n                        right = mid - 1  // Move left to find the first occurrence\n                    } else {\n                        left = mid + 1   // Move right to find the last occurrence\n                    }\n                }\n                nums[mid] < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return result\n    }\n}"  java="class FindFirstAndLastPosition {\n\n    public static void $1(nums: IntArray, target): IntArray {\n        val result = intArrayOf(-1, -1)\n\n        // Find the first occurrence\n        result[0] = binarySearch(nums, target, true)\n\n        // If the first occurrence is not found, return [-1, -1]\n        if (result[0] == -1) return result\n\n        // Find the last occurrence\n        result[1] = binarySearch(nums, target, false)\n\n        return result\n    }\n\n    private public static void $1(nums: IntArray, target, findFirst) {\n        var left = 0\n        var right = nums.lastIndex\n        var result = -1\n\n        while (left <= right) {\n            val mid = left + (right - left) / 2\n            when {\n                nums[mid] == target -> {\n                    result = mid\n                    // Adjust the search range based on whether we are looking for the first or last occurrence\n                    if (findFirst) {\n                        right = mid - 1  // Move left to find the first occurrence\n                    } else {\n                        left = mid + 1   // Move right to find the last occurrence\n                    }\n                }\n                nums[mid] < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return result\n    }\n}"  python="class FindFirstAndLastPosition {\n\n    def $1(nums: list[int], target: int): list[int] {\n        $1 = intArrayOf(-1, -1)\n\n        // Find the first occurrence\n        result[0] = binarySearch(nums, target, True)\n\n        // If the first occurrence is not found, return [-1, -1]\n        if (result[0] == -1) return result\n\n        // Find the last occurrence\n        result[1] = binarySearch(nums, target, False)\n\n        return result\n    }\n\n    private def $1(nums: list[int], target: int, findFirst: bool): int {\n        $1 = 0\n        $1 = nums.lastIndex\n        $1 = -1\n\n        while (left <= right) {\n            $1 = left + (right - left) / 2\n            when {\n                nums[mid] == target -> {\n                    result = mid\n                    // Adjust the search range based on whether we are looking for the first or last occurrence\n                    if (findFirst) {\n                        right = mid - 1  // Move left to find the first occurrence\n                    } else {\n                        left = mid + 1   // Move right to find the last occurrence\n                    }\n                }\n                nums[mid] < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return result\n    }\n}"  rust="class FindFirstAndLastPosition {\n\n    fn $1(nums: Vec<i32>, target -> i32): Vec<i32> {\n        val result = intArrayOf(-1, -1)\n\n        // Find the first occurrence\n        result[0] = binarySearch(nums, target, true)\n\n        // If the first occurrence is not found, return [-1, -1]\n        if (result[0] == -1) return result\n\n        // Find the last occurrence\n        result[1] = binarySearch(nums, target, false)\n\n        return result\n    }\n\n    private fn $1(nums: Vec<i32>, target -> i32, findFirst -> bool) -> i32 {\n        var left = 0\n        var right = nums.len() - 1\n        var result = -1\n\n        while (left <= right) {\n            val mid = left + (right - left) / 2\n            when {\n                nums[mid] == target -> {\n                    result = mid\n                    // Adjust the search range based on whether we are looking for the first or last occurrence\n                    if (findFirst) {\n                        right = mid - 1  // Move left to find the first occurrence\n                    } else {\n                        left = mid + 1   // Move right to find the last occurrence\n                    }\n                }\n                nums[mid] < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return result\n    }\n}"  cpp="class FindFirstAndLastPosition {\n\n    $1(nums: vector<int>, target): vector<int> {\n        auto $1 = intArrayOf(-1, -1)\n\n        // Find the first occurrence\n        result[0] = binarySearch(nums, target, true)\n\n        // If the first occurrence is not found, return [-1, -1]\n        if (result[0] == -1) return result\n\n        // Find the last occurrence\n        result[1] = binarySearch(nums, target, false)\n\n        return result\n    }\n\n    private $1(nums: vector<int>, target, findFirst) {\n        auto $1 = 0\n        auto $1 = nums.size() - 1\n        auto $1 = -1\n\n        while (left <= right) {\n            auto $1 = left + (right - left) / 2\n            when {\n                nums[mid] == target -> {\n                    result = mid\n                    // Adjust the search range based on whether we are looking for the first or last occurrence\n                    if (findFirst) {\n                        right = mid - 1  // Move left to find the first occurrence\n                    } else {\n                        left = mid + 1   // Move right to find the last occurrence\n                    }\n                }\n                nums[mid] < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return result\n    }\n}" %}

---

## FindKClosestElements

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/FindKClosestElements.kt`

{% include code-tabs.html  kotlin="package heap\n\nimport java.util.PriorityQueue\nimport kotlin.math.abs\n\nclass FindKClosestElements {\n    class FindKClosestElements {\n        /**\n         * Solving using Max Heap. O ( N log K + K log K )\n         */\n        fun findClosestElements(arr: Array<Int>, k: Int, x: Int): List<Int> {\n            /**\n             * If we create a Max heap returning ( logic(b)  - logic(a) )\n             * and keep adding element and remove farthest element in each iteration once heap size goes > K\n             * then we'll eliminate (n - k ). Remaining elements will be K closest elements.i.e n - (n-k) = K\n             *\n             *\n             * example of logic function could be frequency count from map eucledian distances\n             */\n           val priorityQueue = PriorityQueue<Int> { a,b ->\n               val diffA = abs(a - x)\n               val diffB = abs(b - x)\n               if (diffA == diffB) b - a else  diffB - diffA\n           }\n            arr.forEach { element ->\n                priorityQueue.offer(element)\n                // Remove the farthest element\n                if (priorityQueue.size > k)\n                    priorityQueue.poll()\n            }\n            val result = mutableListOf<Int>()\n\n            repeat(k) {\n                if (priorityQueue.isNotEmpty())\n                    result.add(priorityQueue.poll())\n            }\n\n            return result\n        }\n\n        // We could also do\n\n        companion object {\n            @JvmStatic\n            fun main(args: Array<String>) {\n                val testClass = FindKClosestElements()\n\n                println(testClass.findClosestElements(arrayOf(1,2,3,4,5,6,7), 4, 4 ))\n            }\n        }\n    }\n}"  java="class FindKClosestElements {\n    class FindKClosestElements {\n        /**\n         * Solving using Max Heap. O ( N log K + K log K )\n         */\n        public static void $1(arr: Array<Int>, k, x): List<Int> {\n            /**\n             * If we create a Max heap returning ( logic(b)  - logic(a) )\n             * and keep adding element and remove farthest element in each iteration once heap size goes > K\n             * then we'll eliminate (n - k ). Remaining elements will be K closest elements.i.e n - (n-k) = K\n             *\n             *\n             * example of logic function could be frequency count from map eucledian distances\n             */\n           val priorityQueue = PriorityQueue<Int> { a,b ->\n               val diffA = abs(a - x)\n               val diffB = abs(b - x)\n               if (diffA == diffB) b - a else  diffB - diffA\n           }\n            arr.forEach { element ->\n                priorityQueue.offer(element)\n                // Remove the farthest element\n                if (priorityQueue.size > k)\n                    priorityQueue.poll()\n            }\n            val result = mutableListOf<Int>()\n\n            repeat(k) {\n                if (priorityQueue.isNotEmpty())\n                    result.add(priorityQueue.poll())\n            }\n\n            return result\n        }\n\n        // We could also do\n\n        companion object {\n            @JvmStatic\n            public static void $1(args: Array<String>) {\n                val testClass = FindKClosestElements()\n\n                println(testClass.findClosestElements(arrayOf(1,2,3,4,5,6,7), 4, 4 ))\n            }\n        }\n    }\n}"  python="class FindKClosestElements {\n    class FindKClosestElements {\n        /**\n         * Solving using Max Heap. O ( N log K + K log K )\n         */\n        def $1(arr: Array<Int>, k: int, x: int): List<Int> {\n            /**\n             * If we create a Max heap returning ( logic(b)  - logic(a) )\n             * and keep adding element and remove farthest element in each iteration once heap size goes > K\n             * then we'll eliminate (n - k ). Remaining elements will be K closest elements.i.e n - (n-k) = K\n             *\n             *\n             * example of logic function could be frequency count from map eucledian distances\n             */\n           $1 = PriorityQueue<Int> { a,b ->\n               $1 = abs(a - x)\n               $1 = abs(b - x)\n               if (diffA == diffB) b - a else  diffB - diffA\n           }\n            arr.forEach { element ->\n                priorityQueue.offer(element)\n                // Remove the farthest element\n                if (priorityQueue.size > k)\n                    priorityQueue.poll()\n            }\n            $1 = mutableListOf<Int>()\n\n            repeat(k) {\n                if (priorityQueue.isNotEmpty())\n                    result.add(priorityQueue.poll())\n            }\n\n            return result\n        }\n\n        // We could also do\n\n        companion object {\n            @JvmStatic\n            def $1(args: Array<String>) {\n                $1 = FindKClosestElements()\n\n                print(testClass.findClosestElements(arrayOf(1,2,3,4,5,6,7), 4, 4 ))\n            }\n        }\n    }\n}"  rust="class FindKClosestElements {\n    class FindKClosestElements {\n        /**\n         * Solving using Max Heap. O ( N log K + K log K )\n         */\n        fn $1(arr: Array<Int>, k -> i32, x -> i32): List<Int> {\n            /**\n             * If we create a Max heap returning ( logic(b)  - logic(a) )\n             * and keep adding element and remove farthest element in each iteration once heap size goes > K\n             * then we'll eliminate (n - k ). Remaining elements will be K closest elements.i.e n - (n-k) = K\n             *\n             *\n             * example of logic function could be frequency count from map eucledian distances\n             */\n           val priorityQueue = PriorityQueue<Int> { a,b ->\n               val diffA = abs(a - x)\n               val diffB = abs(b - x)\n               if (diffA == diffB) b - a else  diffB - diffA\n           }\n            arr.forEach { element ->\n                priorityQueue.offer(element)\n                // Remove the farthest element\n                if (priorityQueue.size > k)\n                    priorityQueue.poll()\n            }\n            val result = mutableListOf<Int>()\n\n            repeat(k) {\n                if (priorityQueue.isNotEmpty())\n                    result.add(priorityQueue.poll())\n            }\n\n            return result\n        }\n\n        // We could also do\n\n        companion object {\n            @JvmStatic\n            fn $1(args: Array<String>) {\n                val testClass = FindKClosestElements()\n\n                println!(testClass.findClosestElements(arrayOf(1,2,3,4,5,6,7), 4, 4 ))\n            }\n        }\n    }\n}"  cpp="class FindKClosestElements {\n    class FindKClosestElements {\n        /**\n         * Solving using Max Heap. O ( N log K + K log K )\n         */\n        $1(arr: Array<Int>, k, x): List<Int> {\n            /**\n             * If we create a Max heap returning ( logic(b)  - logic(a) )\n             * and keep adding element and remove farthest element in each iteration once heap size goes > K\n             * then we'll eliminate (n - k ). Remaining elements will be K closest elements.i.e n - (n-k) = K\n             *\n             *\n             * example of logic function could be frequency count from map eucledian distances\n             */\n           auto $1 = PriorityQueue<Int> { a,b ->\n               auto $1 = abs(a - x)\n               auto $1 = abs(b - x)\n               if (diffA == diffB) b - a else  diffB - diffA\n           }\n            arr.forEach { element ->\n                priorityQueue.offer(element)\n                // Remove the farthest element\n                if (priorityQueue.size > k)\n                    priorityQueue.poll()\n            }\n            auto $1 = mutableListOf<Int>()\n\n            repeat(k) {\n                if (priorityQueue.isNotEmpty())\n                    result.add(priorityQueue.poll())\n            }\n\n            return result\n        }\n\n        // We could also do\n\n        companion object {\n            @JvmStatic\n            $1(args: Array<String>) {\n                auto $1 = FindKClosestElements()\n\n                cout <<(testClass.findClosestElements(arrayOf(1,2,3,4,5,6,7), 4, 4 ))\n            }\n        }\n    }\n}" %}

---

## FindMinimumInRotatedSortedArray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/FindMinimumInRotatedSortedArray.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass FindMinimumInRotatedSortedArray {\n    fun findMin(nums: IntArray): Int {\n        var (left, right) = 0 to nums.size - 1\n        while (left < right) {\n            val mid = left + (right - left) / 2\n            when {\n                nums[mid] > nums[right] -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return nums[left]\n    }\n}"  java="class FindMinimumInRotatedSortedArray {\n    public static void $1(nums: IntArray) {\n        var (left, right) = 0 to nums.size - 1\n        while (left < right) {\n            val mid = left + (right - left) / 2\n            when {\n                nums[mid] > nums[right] -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return nums[left]\n    }\n}"  python="class FindMinimumInRotatedSortedArray {\n    def $1(nums: list[int]): int {\n        var (left, right) = 0 to nums.size - 1\n        while (left < right) {\n            $1 = left + (right - left) / 2\n            when {\n                nums[mid] > nums[right] -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return nums[left]\n    }\n}"  rust="class FindMinimumInRotatedSortedArray {\n    fn $1(nums: Vec<i32>) -> i32 {\n        var (left, right) = 0 to nums.size - 1\n        while (left < right) {\n            val mid = left + (right - left) / 2\n            when {\n                nums[mid] > nums[right] -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return nums[left]\n    }\n}"  cpp="class FindMinimumInRotatedSortedArray {\n    $1(nums: vector<int>) {\n        var (left, right) = 0 to nums.size - 1\n        while (left < right) {\n            auto $1 = left + (right - left) / 2\n            when {\n                nums[mid] > nums[right] -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return nums[left]\n    }\n}" %}

---

## FindPeakElement

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/FindPeakElement.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass FindPeakElement {\n    fun findPeakElement(nums: IntArray): Int {\n        var left = 0\n        var right = nums.lastIndex\n\n        while ( left < right) {\n            val mid = left + (right - left)/2\n\n            if (nums[mid] > nums[mid + 1]) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}\n"  java="class FindPeakElement {\n    public static void $1(nums: IntArray) {\n        var left = 0\n        var right = nums.lastIndex\n\n        while ( left < right) {\n            val mid = left + (right - left)/2\n\n            if (nums[mid] > nums[mid + 1]) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}"  python="class FindPeakElement {\n    def $1(nums: list[int]): int {\n        $1 = 0\n        $1 = nums.lastIndex\n\n        while ( left < right) {\n            $1 = left + (right - left)/2\n\n            if (nums[mid] > nums[mid + 1]) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}"  rust="class FindPeakElement {\n    fn $1(nums: Vec<i32>) -> i32 {\n        var left = 0\n        var right = nums.len() - 1\n\n        while ( left < right) {\n            val mid = left + (right - left)/2\n\n            if (nums[mid] > nums[mid + 1]) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}"  cpp="class FindPeakElement {\n    $1(nums: vector<int>) {\n        auto $1 = 0\n        auto $1 = nums.size() - 1\n\n        while ( left < right) {\n            auto $1 = left + (right - left)/2\n\n            if (nums[mid] > nums[mid + 1]) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}" %}

---

## FindPeakElementBetterSolution

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/FindPeakElementBetterSolution.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass FindPeakElementBetterSolution {\n    fun findPeakElement(nums: IntArray): Int? {\n        if (nums.isEmpty()) return null\n\n        var (left, right) = 0 to nums.size - 1\n\n        while (left < right) {\n            val mid = left + (right - left) / 2\n\n            // Safely handle boundaries\n            val leftNeighbor = if (mid > 0) nums[mid - 1] else Int.MIN_VALUE\n            val rightNeighbor = if (mid < nums.size - 1) nums[mid + 1] else Int.MIN_VALUE\n\n            when {\n                nums[mid] > leftNeighbor && nums[mid] > rightNeighbor -> return mid // Found peak\n                nums[mid] < rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return left // Single peak element when the range narrows down\n    }\n}\n"  java="class FindPeakElementBetterSolution {\n    public static void $1(nums: IntArray)? {\n        if (nums.isEmpty()) return null\n\n        var (left, right) = 0 to nums.size - 1\n\n        while (left < right) {\n            val mid = left + (right - left) / 2\n\n            // Safely handle boundaries\n            val leftNeighbor = if (mid > 0) nums[mid - 1] else Int.MIN_VALUE\n            val rightNeighbor = if (mid < nums.size - 1) nums[mid + 1] else Int.MIN_VALUE\n\n            when {\n                nums[mid] > leftNeighbor && nums[mid] > rightNeighbor -> return mid // Found peak\n                nums[mid] < rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return left // Single peak element when the range narrows down\n    }\n}"  python="class FindPeakElementBetterSolution {\n    def $1(nums: list[int]): int? {\n        if (nums.isEmpty()) return None\n\n        var (left, right) = 0 to nums.size - 1\n\n        while (left < right) {\n            $1 = left + (right - left) / 2\n\n            // Safely handle boundaries\n            $1 = if (mid > 0) nums[mid - 1] else Int.MIN_VALUE\n            $1 = if (mid < nums.size - 1) nums[mid + 1] else Int.MIN_VALUE\n\n            when {\n                nums[mid] > leftNeighbor && nums[mid] > rightNeighbor -> return mid // Found peak\n                nums[mid] < rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return left // Single peak element when the range narrows down\n    }\n}"  rust="class FindPeakElementBetterSolution {\n    fn $1(nums: Vec<i32>) -> i32? {\n        if (nums.isEmpty()) return None\n\n        var (left, right) = 0 to nums.size - 1\n\n        while (left < right) {\n            val mid = left + (right - left) / 2\n\n            // Safely handle boundaries\n            val leftNeighbor = if (mid > 0) nums[mid - 1] else Int.MIN_VALUE\n            val rightNeighbor = if (mid < nums.size - 1) nums[mid + 1] else Int.MIN_VALUE\n\n            when {\n                nums[mid] > leftNeighbor && nums[mid] > rightNeighbor -> return mid // Found peak\n                nums[mid] < rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return left // Single peak element when the range narrows down\n    }\n}"  cpp="class FindPeakElementBetterSolution {\n    $1(nums: vector<int>)? {\n        if (nums.isEmpty()) return nullptr\n\n        var (left, right) = 0 to nums.size - 1\n\n        while (left < right) {\n            auto $1 = left + (right - left) / 2\n\n            // Safely handle boundaries\n            auto $1 = if (mid > 0) nums[mid - 1] else Int.MIN_VALUE\n            auto $1 = if (mid < nums.size - 1) nums[mid + 1] else Int.MIN_VALUE\n\n            when {\n                nums[mid] > leftNeighbor && nums[mid] > rightNeighbor -> return mid // Found peak\n                nums[mid] < rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return left // Single peak element when the range narrows down\n    }\n}" %}

---

## FirstBadVersion

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/FirstBadVersion.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nabstract class VersionControl {\n    fun isBadVersion(mid: Int): Boolean {\n        return false // Return dummy value\n    }\n\n    abstract fun firstBadVersion(n: Int) : Int\n}\nclass FirstBadVersion: VersionControl() {\n    override fun firstBadVersion(n: Int) : Int {\n        var start = 1\n        var end = n\n\n        while (start < end) {\n            val mid = start + (end - start) / 2\n            when {\n                isBadVersion(mid) -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  java="abstract class VersionControl {\n    public static void $1(mid) {\n        return false // Return dummy value\n    }\n\n    abstract public static void $1(n) \n}\nclass FirstBadVersion: VersionControl() {\n    override public static void $1(n)  {\n        var start = 1\n        var end = n\n\n        while (start < end) {\n            val mid = start + (end - start) / 2\n            when {\n                isBadVersion(mid) -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  python="abstract class VersionControl {\n    def $1(mid: int): bool {\n        return False // Return dummy value\n    }\n\n    abstract def $1(n: int) : int\n}\nclass FirstBadVersion: VersionControl() {\n    override def $1(n: int) : int {\n        $1 = 1\n        $1 = n\n\n        while (start < end) {\n            $1 = start + (end - start) / 2\n            when {\n                isBadVersion(mid) -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  rust="abstract class VersionControl {\n    fn $1(mid -> i32) -> bool {\n        return false // Return dummy value\n    }\n\n    abstract fn $1(n -> i32)  -> i32\n}\nclass FirstBadVersion: VersionControl() {\n    override fn $1(n -> i32)  -> i32 {\n        var start = 1\n        var end = n\n\n        while (start < end) {\n            val mid = start + (end - start) / 2\n            when {\n                isBadVersion(mid) -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  cpp="abstract class VersionControl {\n    $1(mid) {\n        return false // Return dummy value\n    }\n\n    abstract $1(n) \n}\nclass FirstBadVersion: VersionControl() {\n    override $1(n)  {\n        auto $1 = 1\n        auto $1 = n\n\n        while (start < end) {\n            auto $1 = start + (end - start) / 2\n            when {\n                isBadVersion(mid) -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}" %}

---

## GuessNumberHigherOrLower

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/GuessNumberHigherOrLower.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\n/**\n * The API guess is defined in the parent class.\n * @param  num   your guess\n * @return 	     -1 if num is higher than the picked number\n *			      1 if num is lower than the picked number\n *               otherwise return 0\n * fun guess(num:Int):Int {}\n */\n\nclass Solution:GuessGame() {\n    override fun guessNumber(n:Int):Int {\n        var start = 1\n        var end = n\n\n        while (start <= end) {\n            val mid = start + (end-start) / 2\n\n            val distance = guess(mid)\n\n\n            when {\n                distance == 0 -> return mid\n                distance < 0 -> end = mid - 1\n                else -> start = mid + 1\n            }\n        }\n\n        return -1\n    }\n\n\n}\n\nopen class GuessGame {\n\n    open fun guessNumber(n: Int): Int {\n        TODO(\"Not yet implemented\")\n    }\n\n    fun guess(mid: Int): Int {\n        return 0\n    }\n}\n"  java="// package binarysearch\n\n/**\n* The API guess is defined in the parent class.\n* @param  num   your guess\n* @return 	     -1 if num is higher than the picked number\n*			      1 if num is lower than the picked number\n*               otherwise return 0\n* fun guess(num:Int):Int {}\n*/\n\nclass Solution:GuessGame() {\noverride fun guessNumber(n:Int):Int {\nvar start = 1\nvar end = n\n\nwhile (start <= end) {\nval mid = start + (end-start) / 2\n\nval distance = guess(mid)\n\n\nwhen {\ndistance == 0 -> return mid\ndistance < 0 -> end = mid - 1\nelse -> start = mid + 1\n}\n}\n\nreturn -1\n}\n\n\n}\n\nopen class GuessGame {\n\nopen fun guessNumber(n: Int): Int {\nTODO(\"Not yet implemented\")\n}\n\n    public int guess(mid: Int) {\n    return 0\n}\n}\n"  python="/**\n* The API guess is defined in the parent class.\n* @param  num   your guess\n* @return 	     -1 if num is higher than the picked number\n*			      1 if num is lower than the picked number\n*               otherwise return 0\n* fun guess(num:int):int {}\n*/\nclass Solution:GuessGame() {\noverride fun guessNumber(n:int):int {\nstart = 1\nend = n\nwhile (start <= end) {\nmid = start + (end-start) / 2\ndistance = guess(mid)\nwhen {\ndistance == 0 -> return mid\ndistance < 0 -> end = mid - 1\nelse -> start = mid + 1\n}\n}\nreturn -1\n}\n}\nopen class GuessGame {\nopen fun guessNumber(n: int): int {\nTODO(\"Not yet implemented\")\n}\ndef guess(mid):\nreturn 0\n}\n}"  rust="/**\n * The API guess is defined in the parent class.\n * @param  num   your guess\n * @return 	     -1 if num is higher than the picked number\n *			      1 if num is lower than the picked number\n *               otherwise return 0\n * fn $1(num -> i32) -> i32 {}\n */\n\nclass Solution:GuessGame() {\n    override fn $1(n -> i32) -> i32 {\n        var start = 1\n        var end = n\n\n        while (start <= end) {\n            val mid = start + (end-start) / 2\n\n            val distance = guess(mid)\n\n\n            when {\n                distance == 0 -> return mid\n                distance < 0 -> end = mid - 1\n                else -> start = mid + 1\n            }\n        }\n\n        return -1\n    }\n\n\n}\n\nopen class GuessGame {\n\n    open fn $1(n -> i32) -> i32 {\n        TODO(\"Not yet implemented\")\n    }\n\n    fn $1(mid -> i32) -> i32 {\n        return 0\n    }\n}"  cpp="/**\n * The API guess is defined in the parent class.\n * @param  num   your guess\n * @return 	     -1 if num is higher than the picked number\n *			      1 if num is lower than the picked number\n *               otherwise return 0\n * $1(num) {}\n */\n\nclass Solution:GuessGame() {\n    override $1(n) {\n        auto $1 = 1\n        auto $1 = n\n\n        while (start <= end) {\n            auto $1 = start + (end-start) / 2\n\n            auto $1 = guess(mid)\n\n\n            when {\n                distance == 0 -> return mid\n                distance < 0 -> end = mid - 1\n                else -> start = mid + 1\n            }\n        }\n\n        return -1\n    }\n\n\n}\n\nopen class GuessGame {\n\n    open $1(n) {\n        TODO(\"Not yet implemented\")\n    }\n\n    $1(mid) {\n        return 0\n    }\n}" %}

---

## HouseRobber_IV

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/HouseRobber_IV.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass HouseRobber_IV {\n    fun minCapability(nums: IntArray, k: Int): Int {\n        var left = nums.minOrNull() ?: 0\n        var right = nums.maxOrNull() ?: 0\n\n        fun canRob(cap: Int): Boolean {\n            var robbed = 0\n            var i = 0\n            while (i < nums.size) {\n                if (nums[i] <= cap) {\n                    robbed++\n                    i += 2  // skip adjacent\n                } else {\n                    i++\n                }\n            }\n            return robbed >= k\n        }\n\n        while (left < right) {\n            val mid = (left + right) / 2\n            if (canRob(mid)) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}"  java="class HouseRobber_IV {\n    public static void $1(nums: IntArray, k) {\n        var left = nums.minOrNull() ?: 0\n        var right = nums.maxOrNull() ?: 0\n\n        public static void $1(cap) {\n            var robbed = 0\n            var i = 0\n            while (i < nums.size) {\n                if (nums[i] <= cap) {\n                    robbed++\n                    i += 2  // skip adjacent\n                } else {\n                    i++\n                }\n            }\n            return robbed >= k\n        }\n\n        while (left < right) {\n            val mid = (left + right) / 2\n            if (canRob(mid)) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}"  python="class HouseRobber_IV {\n    def $1(nums: list[int], k: int): int {\n        $1 = nums.minOrNull() ?: 0\n        $1 = nums.maxOrNull() ?: 0\n\n        def $1(cap: int): bool {\n            $1 = 0\n            $1 = 0\n            while (i < nums.size) {\n                if (nums[i] <= cap) {\n                    robbed++\n                    i += 2  // skip adjacent\n                } else {\n                    i++\n                }\n            }\n            return robbed >= k\n        }\n\n        while (left < right) {\n            $1 = (left + right) / 2\n            if (canRob(mid)) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}"  rust="class HouseRobber_IV {\n    fn $1(nums: Vec<i32>, k -> i32) -> i32 {\n        var left = nums.minOrNull() ?: 0\n        var right = nums.maxOrNull() ?: 0\n\n        fn $1(cap -> i32) -> bool {\n            var robbed = 0\n            var i = 0\n            while (i < nums.size) {\n                if (nums[i] <= cap) {\n                    robbed++\n                    i += 2  // skip adjacent\n                } else {\n                    i++\n                }\n            }\n            return robbed >= k\n        }\n\n        while (left < right) {\n            val mid = (left + right) / 2\n            if (canRob(mid)) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}"  cpp="class HouseRobber_IV {\n    $1(nums: vector<int>, k) {\n        auto $1 = nums.minOrNull() ?: 0\n        auto $1 = nums.maxOrNull() ?: 0\n\n        $1(cap) {\n            auto $1 = 0\n            auto $1 = 0\n            while (i < nums.size) {\n                if (nums[i] <= cap) {\n                    robbed++\n                    i += 2  // skip adjacent\n                } else {\n                    i++\n                }\n            }\n            return robbed >= k\n        }\n\n        while (left < right) {\n            auto $1 = (left + right) / 2\n            if (canRob(mid)) {\n                right = mid\n            } else {\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n}" %}

---

## KThMissingPositiveNumber

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/KThMissingPositiveNumber.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass KThMissingPositiveNumber {\n    fun findKthPositive(arr: IntArray, k: Int): Int {\n        var (left, right) = 0 to arr.size\n\n        while (left < right) {\n            val mid = left + (right - left) / 2\n            val missingCount = arr[mid] - ( mid + 1 )\n\n            when {\n                missingCount < k -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return left + k\n    }\n}"  java="class KThMissingPositiveNumber {\n    public static void $1(arr: IntArray, k) {\n        var (left, right) = 0 to arr.size\n\n        while (left < right) {\n            val mid = left + (right - left) / 2\n            val missingCount = arr[mid] - ( mid + 1 )\n\n            when {\n                missingCount < k -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return left + k\n    }\n}"  python="class KThMissingPositiveNumber {\n    def $1(arr: list[int], k: int): int {\n        var (left, right) = 0 to arr.size\n\n        while (left < right) {\n            $1 = left + (right - left) / 2\n            $1 = arr[mid] - ( mid + 1 )\n\n            when {\n                missingCount < k -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return left + k\n    }\n}"  rust="class KThMissingPositiveNumber {\n    fn $1(arr: Vec<i32>, k -> i32) -> i32 {\n        var (left, right) = 0 to arr.size\n\n        while (left < right) {\n            val mid = left + (right - left) / 2\n            val missingCount = arr[mid] - ( mid + 1 )\n\n            when {\n                missingCount < k -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return left + k\n    }\n}"  cpp="class KThMissingPositiveNumber {\n    $1(arr: vector<int>, k) {\n        var (left, right) = 0 to arr.size\n\n        while (left < right) {\n            auto $1 = left + (right - left) / 2\n            auto $1 = arr[mid] - ( mid + 1 )\n\n            when {\n                missingCount < k -> left = mid + 1\n                else -> right = mid\n            }\n        }\n\n        return left + k\n    }\n}" %}

---

## KokoEatingBanana

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/KokoEatingBanana.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass KokoEatingBanana {\n    fun minEatingSpeed(piles: IntArray, h: Int): Int {\n        // Left is the slowest speed, right is the fastest speed\n        var left = 1\n        var right = piles.maxOrNull()!!\n\n        // Binary search to find the minimum valid eating speed\n        while (left < right) {\n            val mid = left + (right - left) / 2\n            if (canEatAllBananas(piles, h, mid)) {\n                // If Koko can eat all bananas at speed `mid`, try slower speeds\n                right = mid\n            } else {\n                // Otherwise, try faster speeds\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n\n    // Helper function to check if Koko can finish all bananas in `h` hours at speed `k`\n    private fun canEatAllBananas(piles: IntArray, h: Int, k: Int): Boolean {\n        var totalHours = 0\n        for (pile in piles) {\n            // Add the number of hours it takes to finish this pile at speed `k`\n            totalHours += (pile + k - 1) / k // Same as Math.ceil(pile / k)\n        }\n        return totalHours <= h\n    }\n}\n"  java="class KokoEatingBanana {\n    public static void $1(piles: IntArray, h) {\n        // Left is the slowest speed, right is the fastest speed\n        var left = 1\n        var right = piles.maxOrNull()!!\n\n        // Binary search to find the minimum valid eating speed\n        while (left < right) {\n            val mid = left + (right - left) / 2\n            if (canEatAllBananas(piles, h, mid)) {\n                // If Koko can eat all bananas at speed `mid`, try slower speeds\n                right = mid\n            } else {\n                // Otherwise, try faster speeds\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n\n    // Helper function to check if Koko can finish all bananas in `h` hours at speed `k`\n    private public static void $1(piles: IntArray, h, k) {\n        var totalHours = 0\n        for (pile in piles) {\n            // Add the number of hours it takes to finish this pile at speed `k`\n            totalHours += (pile + k - 1) / k // Same as Math.ceil(pile / k)\n        }\n        return totalHours <= h\n    }\n}"  python="class KokoEatingBanana {\n    def $1(piles: list[int], h: int): int {\n        // Left is the slowest speed, right is the fastest speed\n        $1 = 1\n        $1 = piles.maxOrNull()!!\n\n        // Binary search to find the minimum valid eating speed\n        while (left < right) {\n            $1 = left + (right - left) / 2\n            if (canEatAllBananas(piles, h, mid)) {\n                // If Koko can eat all bananas at speed `mid`, try slower speeds\n                right = mid\n            } else {\n                // Otherwise, try faster speeds\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n\n    // Helper function to check if Koko can finish all bananas in `h` hours at speed `k`\n    private def $1(piles: list[int], h: int, k: int): bool {\n        $1 = 0\n        for (pile in piles) {\n            // Add the number of hours it takes to finish this pile at speed `k`\n            totalHours += (pile + k - 1) / k // Same as Math.ceil(pile / k)\n        }\n        return totalHours <= h\n    }\n}"  rust="class KokoEatingBanana {\n    fn $1(piles: Vec<i32>, h -> i32) -> i32 {\n        // Left is the slowest speed, right is the fastest speed\n        var left = 1\n        var right = piles.maxOrNull()!!\n\n        // Binary search to find the minimum valid eating speed\n        while (left < right) {\n            val mid = left + (right - left) / 2\n            if (canEatAllBananas(piles, h, mid)) {\n                // If Koko can eat all bananas at speed `mid`, try slower speeds\n                right = mid\n            } else {\n                // Otherwise, try faster speeds\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n\n    // Helper function to check if Koko can finish all bananas in `h` hours at speed `k`\n    private fn $1(piles: Vec<i32>, h -> i32, k -> i32) -> bool {\n        var totalHours = 0\n        for (pile in piles) {\n            // Add the number of hours it takes to finish this pile at speed `k`\n            totalHours += (pile + k - 1) / k // Same as Math.ceil(pile / k)\n        }\n        return totalHours <= h\n    }\n}"  cpp="class KokoEatingBanana {\n    $1(piles: vector<int>, h) {\n        // Left is the slowest speed, right is the fastest speed\n        auto $1 = 1\n        auto $1 = piles.maxOrNull()!!\n\n        // Binary search to find the minimum valid eating speed\n        while (left < right) {\n            auto $1 = left + (right - left) / 2\n            if (canEatAllBananas(piles, h, mid)) {\n                // If Koko can eat all bananas at speed `mid`, try slower speeds\n                right = mid\n            } else {\n                // Otherwise, try faster speeds\n                left = mid + 1\n            }\n        }\n\n        return left\n    }\n\n    // Helper function to check if Koko can finish all bananas in `h` hours at speed `k`\n    private $1(piles: vector<int>, h, k) {\n        auto $1 = 0\n        for (pile in piles) {\n            // Add the number of hours it takes to finish this pile at speed `k`\n            totalHours += (pile + k - 1) / k // Same as Math.ceil(pile / k)\n        }\n        return totalHours <= h\n    }\n}" %}

---

## MedianOfTwoSortedARrays

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/MedianOfTwoSortedARrays.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\n// These are the challenges that I need to somewhoe solve to get\nclass MedianOfTwoSortedARrays {\n    fun findMedianSortedArrays(nums1: IntArray, nums2: IntArray): Double {\n\n        if (nums1.size > nums2.size ) {\n            return findMedianSortedArrays(nums2, nums1)\n        }\n\n        val m = nums1.size\n        val n = nums2.size\n        var start = 0\n        var end = m\n\n        while (start <= end) {\n            val partitionX = (start + end) / 2\n            val partitionY = (m + n + 1) / 2 - partitionX\n\n            // Use getOrNull with the Elvis operator to handle boundaries\n            val leftX = nums1.getOrNull(partitionX - 1) ?: Int.MIN_VALUE\n            val rightX = nums1.getOrNull(partitionX) ?: Int.MAX_VALUE\n\n            val leftY = nums2.getOrNull(partitionY - 1) ?: Int.MIN_VALUE\n            val rightY = nums2.getOrNull(partitionY) ?: Int.MAX_VALUE\n\n            // Check if we have found the correct partition\n            when {\n                leftX <= rightY && leftY <= rightX -> {\n                    return if ((m + n) % 2 == 0) {\n                        (maxOf(leftX, leftY) + minOf(rightX, rightY)) / 2.0\n                    } else {\n                        maxOf(leftX, leftY).toDouble()\n                    }\n                }\n                leftX > rightY -> end = partitionX - 1\n                else -> start = partitionX + 1\n            }\n        }\n        return -1.0\n    }\n}\n"  java="// These are the challenges that I need to somewhoe solve to get\nclass MedianOfTwoSortedARrays {\n    public static void $1(nums1: IntArray, nums2: IntArray): Double {\n\n        if (nums1.size > nums2.size ) {\n            return findMedianSortedArrays(nums2, nums1)\n        }\n\n        val m = nums1.size\n        val n = nums2.size\n        var start = 0\n        var end = m\n\n        while (start <= end) {\n            val partitionX = (start + end) / 2\n            val partitionY = (m + n + 1) / 2 - partitionX\n\n            // Use getOrNull with the Elvis operator to handle boundaries\n            val leftX = nums1.getOrNull(partitionX - 1) ?.MIN_VALUE\n            val rightX = nums1.getOrNull(partitionX) ?.MAX_VALUE\n\n            val leftY = nums2.getOrNull(partitionY - 1) ?.MIN_VALUE\n            val rightY = nums2.getOrNull(partitionY) ?.MAX_VALUE\n\n            // Check if we have found the correct partition\n            when {\n                leftX <= rightY && leftY <= rightX -> {\n                    return if ((m + n) % 2 == 0) {\n                        (maxOf(leftX, leftY) + minOf(rightX, rightY)) / 2.0\n                    } else {\n                        maxOf(leftX, leftY).toDouble()\n                    }\n                }\n                leftX > rightY -> end = partitionX - 1\n                else -> start = partitionX + 1\n            }\n        }\n        return -1.0\n    }\n}"  python="// These are the challenges that I need to somewhoe solve to get\nclass MedianOfTwoSortedARrays {\n    def $1(nums1: list[int], nums2: list[int]): Double {\n\n        if (nums1.size > nums2.size ) {\n            return findMedianSortedArrays(nums2, nums1)\n        }\n\n        $1 = nums1.size\n        $1 = nums2.size\n        $1 = 0\n        $1 = m\n\n        while (start <= end) {\n            $1 = (start + end) / 2\n            $1 = (m + n + 1) / 2 - partitionX\n\n            // Use getOrNull with the Elvis operator to handle boundaries\n            $1 = nums1.getOrNull(partitionX - 1) ?: int.MIN_VALUE\n            $1 = nums1.getOrNull(partitionX) ?: int.MAX_VALUE\n\n            $1 = nums2.getOrNull(partitionY - 1) ?: int.MIN_VALUE\n            $1 = nums2.getOrNull(partitionY) ?: int.MAX_VALUE\n\n            // Check if we have found the correct partition\n            when {\n                leftX <= rightY && leftY <= rightX -> {\n                    return if ((m + n) % 2 == 0) {\n                        (max(leftX, leftY) + min(rightX, rightY)) / 2.0\n                    } else {\n                        max(leftX, leftY).toDouble()\n                    }\n                }\n                leftX > rightY -> end = partitionX - 1\n                else -> start = partitionX + 1\n            }\n        }\n        return -1.0\n    }\n}"  rust="// These are the challenges that I need to somewhoe solve to get\nclass MedianOfTwoSortedARrays {\n    fn $1(nums1: Vec<i32>, nums2: Vec<i32>): Double {\n\n        if (nums1.size > nums2.size ) {\n            return findMedianSortedArrays(nums2, nums1)\n        }\n\n        val m = nums1.size\n        val n = nums2.size\n        var start = 0\n        var end = m\n\n        while (start <= end) {\n            val partitionX = (start + end) / 2\n            val partitionY = (m + n + 1) / 2 - partitionX\n\n            // Use getOrNull with the Elvis operator to handle boundaries\n            val leftX = nums1.getOrNull(partitionX - 1) ? -> i32.MIN_VALUE\n            val rightX = nums1.getOrNull(partitionX) ? -> i32.MAX_VALUE\n\n            val leftY = nums2.getOrNull(partitionY - 1) ? -> i32.MIN_VALUE\n            val rightY = nums2.getOrNull(partitionY) ? -> i32.MAX_VALUE\n\n            // Check if we have found the correct partition\n            when {\n                leftX <= rightY && leftY <= rightX -> {\n                    return if ((m + n) % 2 == 0) {\n                        (max(leftX, leftY) + min(rightX, rightY)) / 2.0\n                    } else {\n                        max(leftX, leftY).toDouble()\n                    }\n                }\n                leftX > rightY -> end = partitionX - 1\n                else -> start = partitionX + 1\n            }\n        }\n        return -1.0\n    }\n}"  cpp="// These are the challenges that I need to somewhoe solve to get\nclass MedianOfTwoSortedARrays {\n    $1(nums1: vector<int>, nums2: vector<int>): Double {\n\n        if (nums1.size > nums2.size ) {\n            return findMedianSortedArrays(nums2, nums1)\n        }\n\n        auto $1 = nums1.size\n        auto $1 = nums2.size\n        auto $1 = 0\n        auto $1 = m\n\n        while (start <= end) {\n            auto $1 = (start + end) / 2\n            auto $1 = (m + n + 1) / 2 - partitionX\n\n            // Use getOrNull with the Elvis operator to handle boundaries\n            auto $1 = nums1.getOrNull(partitionX - 1) ?.MIN_VALUE\n            auto $1 = nums1.getOrNull(partitionX) ?.MAX_VALUE\n\n            auto $1 = nums2.getOrNull(partitionY - 1) ?.MIN_VALUE\n            auto $1 = nums2.getOrNull(partitionY) ?.MAX_VALUE\n\n            // Check if we have found the correct partition\n            when {\n                leftX <= rightY && leftY <= rightX -> {\n                    return if ((m + n) % 2 == 0) {\n                        (max(leftX, leftY) + min(rightX, rightY)) / 2.0\n                    } else {\n                        max(leftX, leftY).toDouble()\n                    }\n                }\n                leftX > rightY -> end = partitionX - 1\n                else -> start = partitionX + 1\n            }\n        }\n        return -1.0\n    }\n}" %}

---

## RandomPickWithWeight

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/RandomPickWithWeight.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nimport kotlin.random.Random\n\nclass RandomPickWithWeight (w: IntArray) {\n    private val prefixSum = IntArray(w.size){ 0 }\n    private val totalSum: Int\n    private val w: IntArray = w\n    init {\n        for (i in w.indices) {\n            prefixSum[i] = if(i > 0) prefixSum[i-1] + w[i] else w[i]\n        }\n\n        totalSum = prefixSum.last()\n    }\n\n    fun pickIndex(): Int {\n        val randomPick = Random.nextInt(totalSum)\n        var (start, end) = 0 to w.size\n\n        while (start < end) {\n            val mid = start + (end - start)/2\n\n            when {\n                prefixSum[mid] > randomPick -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  java="class RandomPickWithWeight (w: IntArray) {\n    private val prefixSum = IntArray(w.size){ 0 }\n    private val totalSum\n    private val w: IntArray = w\n    init {\n        for (i in w.indices) {\n            prefixSum[i] = if(i > 0) prefixSum[i-1] + w[i] else w[i]\n        }\n\n        totalSum = prefixSum.last()\n    }\n\n    public static void $1() {\n        val randomPick = Random.nextInt(totalSum)\n        var (start, end) = 0 to w.size\n\n        while (start < end) {\n            val mid = start + (end - start)/2\n\n            when {\n                prefixSum[mid] > randomPick -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  python="class RandomPickWithWeight (w: list[int]) {\n    private $1 = list[int](w.size){ 0 }\n    private val totalSum: int\n    private val w: list[int] = w\n    init {\n        for (i in w.indices) {\n            prefixSum[i] = if(i > 0) prefixSum[i-1] + w[i] else w[i]\n        }\n\n        totalSum = prefixSum.last()\n    }\n\n    def $1(): int {\n        $1 = Random.nextInt(totalSum)\n        var (start, end) = 0 to w.size\n\n        while (start < end) {\n            $1 = start + (end - start)/2\n\n            when {\n                prefixSum[mid] > randomPick -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  rust="class RandomPickWithWeight (w: Vec<i32>) {\n    private val prefixSum = Vec<i32>(w.size){ 0 }\n    private val totalSum -> i32\n    private val w: Vec<i32> = w\n    init {\n        for (i in w.indices) {\n            prefixSum[i] = if(i > 0) prefixSum[i-1] + w[i] else w[i]\n        }\n\n        totalSum = prefixSum.last()\n    }\n\n    fn $1() -> i32 {\n        val randomPick = Random.nextInt(totalSum)\n        var (start, end) = 0 to w.size\n\n        while (start < end) {\n            val mid = start + (end - start)/2\n\n            when {\n                prefixSum[mid] > randomPick -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  cpp="class RandomPickWithWeight (w: vector<int>) {\n    private auto $1 = vector<int>(w.size){ 0 }\n    private val totalSum\n    private val w: vector<int> = w\n    init {\n        for (i in w.indices) {\n            prefixSum[i] = if(i > 0) prefixSum[i-1] + w[i] else w[i]\n        }\n\n        totalSum = prefixSum.last()\n    }\n\n    $1() {\n        auto $1 = Random.nextInt(totalSum)\n        var (start, end) = 0 to w.size\n\n        while (start < end) {\n            auto $1 = start + (end - start)/2\n\n            when {\n                prefixSum[mid] > randomPick -> end = mid\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}" %}

---

## SearchA2dMatrix

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/SearchA2dMatrix.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass SearchA2dMatrix {\n    fun searchMatrix(matrix: Array<IntArray>, target: Int): Boolean {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return false\n\n        val (m, n) = matrix.size to matrix[0].size\n\n        var (left, right) = 0 to m * n - 1\n\n        while (left <= right) {\n            val mid = left + (right - left) / 2\n            val midValue = matrix[mid / n][mid % n]  // Convert 1D index to 2D\n\n            when {\n                midValue == target -> return true\n                midValue < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return false\n    }\n}"  java="class SearchA2dMatrix {\n    public static void $1(matrix: Array<IntArray>, target) {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return false\n\n        val (m, n) = matrix.size to matrix[0].size\n\n        var (left, right) = 0 to m * n - 1\n\n        while (left <= right) {\n            val mid = left + (right - left) / 2\n            val midValue = matrix[mid / n][mid % n]  // Convert 1D index to 2D\n\n            when {\n                midValue == target -> return true\n                midValue < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return false\n    }\n}"  python="class SearchA2dMatrix {\n    def $1(matrix: Array<list[int]>, target: int): bool {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return False\n\n        ($1, $2) = matrix.size to matrix[0].size\n\n        var (left, right) = 0 to m * n - 1\n\n        while (left <= right) {\n            $1 = left + (right - left) / 2\n            $1 = matrix[mid / n][mid % n]  // Convert 1D index to 2D\n\n            when {\n                midValue == target -> return True\n                midValue < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return False\n    }\n}"  rust="class SearchA2dMatrix {\n    fn $1(matrix: Array<Vec<i32>>, target -> i32) -> bool {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return false\n\n        val (m, n) = matrix.size to matrix[0].size\n\n        var (left, right) = 0 to m * n - 1\n\n        while (left <= right) {\n            val mid = left + (right - left) / 2\n            val midValue = matrix[mid / n][mid % n]  // Convert 1D index to 2D\n\n            when {\n                midValue == target -> return true\n                midValue < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return false\n    }\n}"  cpp="class SearchA2dMatrix {\n    $1(matrix: Array<vector<int>>, target) {\n        if (matrix.isEmpty() || matrix[0].isEmpty()) return false\n\n        val (m, n) = matrix.size to matrix[0].size\n\n        var (left, right) = 0 to m * n - 1\n\n        while (left <= right) {\n            auto $1 = left + (right - left) / 2\n            auto $1 = matrix[mid / n][mid % n]  // Convert 1D index to 2D\n\n            when {\n                midValue == target -> return true\n                midValue < target -> left = mid + 1\n                else -> right = mid - 1\n            }\n        }\n\n        return false\n    }\n}" %}

---

## SearchInRotatedArray_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/SearchInRotatedArray_II.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass SearchInRotatedArray_II {\n    fun search(nums: IntArray, target: Int): Boolean {\n        var left = 0\n        var right = nums.lastIndex\n\n        while (left <= right) {\n            val mid = left + (right - left) / 2\n\n            when {\n                nums[mid] == target -> return true\n\n                // Handle duplicates: skip the duplicates\n                nums[left] == nums[mid] && nums[mid] == nums[right] -> {\n                    left++\n                    right--\n                }\n\n                // Left portion is sorted\n                nums[left] <= nums[mid] -> {\n                    if (nums[left] <= target && target < nums[mid]) {\n                        right = mid - 1 // Target in left portion\n                    } else {\n                        left = mid + 1 // Target in right portion\n                    }\n                }\n\n                // Right portion is sorted\n                else -> {\n                    if (nums[mid] < target && target <= nums[right]) {\n                        left = mid + 1 // Target in right portion\n                    } else {\n                        right = mid - 1 // Target in left portion\n                    }\n                }\n            }\n        }\n        return false\n    }\n}\n"  java="class SearchInRotatedArray_II {\n    public static void $1(nums: IntArray, target) {\n        var left = 0\n        var right = nums.lastIndex\n\n        while (left <= right) {\n            val mid = left + (right - left) / 2\n\n            when {\n                nums[mid] == target -> return true\n\n                // Handle duplicates: skip the duplicates\n                nums[left] == nums[mid] && nums[mid] == nums[right] -> {\n                    left++\n                    right--\n                }\n\n                // Left portion is sorted\n                nums[left] <= nums[mid] -> {\n                    if (nums[left] <= target && target < nums[mid]) {\n                        right = mid - 1 // Target in left portion\n                    } else {\n                        left = mid + 1 // Target in right portion\n                    }\n                }\n\n                // Right portion is sorted\n                else -> {\n                    if (nums[mid] < target && target <= nums[right]) {\n                        left = mid + 1 // Target in right portion\n                    } else {\n                        right = mid - 1 // Target in left portion\n                    }\n                }\n            }\n        }\n        return false\n    }\n}"  python="class SearchInRotatedArray_II {\n    def $1(nums: list[int], target: int): bool {\n        $1 = 0\n        $1 = nums.lastIndex\n\n        while (left <= right) {\n            $1 = left + (right - left) / 2\n\n            when {\n                nums[mid] == target -> return True\n\n                // Handle duplicates: skip the duplicates\n                nums[left] == nums[mid] && nums[mid] == nums[right] -> {\n                    left++\n                    right--\n                }\n\n                // Left portion is sorted\n                nums[left] <= nums[mid] -> {\n                    if (nums[left] <= target && target < nums[mid]) {\n                        right = mid - 1 // Target in left portion\n                    } else {\n                        left = mid + 1 // Target in right portion\n                    }\n                }\n\n                // Right portion is sorted\n                else -> {\n                    if (nums[mid] < target && target <= nums[right]) {\n                        left = mid + 1 // Target in right portion\n                    } else {\n                        right = mid - 1 // Target in left portion\n                    }\n                }\n            }\n        }\n        return False\n    }\n}"  rust="class SearchInRotatedArray_II {\n    fn $1(nums: Vec<i32>, target -> i32) -> bool {\n        var left = 0\n        var right = nums.len() - 1\n\n        while (left <= right) {\n            val mid = left + (right - left) / 2\n\n            when {\n                nums[mid] == target -> return true\n\n                // Handle duplicates: skip the duplicates\n                nums[left] == nums[mid] && nums[mid] == nums[right] -> {\n                    left++\n                    right--\n                }\n\n                // Left portion is sorted\n                nums[left] <= nums[mid] -> {\n                    if (nums[left] <= target && target < nums[mid]) {\n                        right = mid - 1 // Target in left portion\n                    } else {\n                        left = mid + 1 // Target in right portion\n                    }\n                }\n\n                // Right portion is sorted\n                else -> {\n                    if (nums[mid] < target && target <= nums[right]) {\n                        left = mid + 1 // Target in right portion\n                    } else {\n                        right = mid - 1 // Target in left portion\n                    }\n                }\n            }\n        }\n        return false\n    }\n}"  cpp="class SearchInRotatedArray_II {\n    $1(nums: vector<int>, target) {\n        auto $1 = 0\n        auto $1 = nums.size() - 1\n\n        while (left <= right) {\n            auto $1 = left + (right - left) / 2\n\n            when {\n                nums[mid] == target -> return true\n\n                // Handle duplicates: skip the duplicates\n                nums[left] == nums[mid] && nums[mid] == nums[right] -> {\n                    left++\n                    right--\n                }\n\n                // Left portion is sorted\n                nums[left] <= nums[mid] -> {\n                    if (nums[left] <= target && target < nums[mid]) {\n                        right = mid - 1 // Target in left portion\n                    } else {\n                        left = mid + 1 // Target in right portion\n                    }\n                }\n\n                // Right portion is sorted\n                else -> {\n                    if (nums[mid] < target && target <= nums[right]) {\n                        left = mid + 1 // Target in right portion\n                    } else {\n                        right = mid - 1 // Target in left portion\n                    }\n                }\n            }\n        }\n        return false\n    }\n}" %}

---

## SearchInRotatedSortedArray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/SearchInRotatedSortedArray.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass SearchInRotatedSortedArray {\n    fun search(nums: IntArray, target: Int): Int {\n        var (start, end) = listOf(0, nums.lastIndex)\n\n        while (start <= end) {\n            val mid = start + (end - start) / 2\n\n            if (nums[mid] == target) return mid\n\n            // Determine if Left half is sorted\n            if (nums[start] <= nums[mid]) {\n                if (nums[start] <= target && target < nums[mid]) {\n                    end = mid - 1\n                } else {\n                    start = mid + 1\n                }\n            } else {\n                // Right half is sorted\n                if (nums[mid] < target && target <= nums[end]) {\n                    start = mid + 1\n                } else {\n                    end = mid - 1\n                }\n            }\n        }\n\n        return -1\n\n    }\n}"  java="class SearchInRotatedSortedArray {\n    public static void $1(nums: IntArray, target) {\n        var (start, end) = listOf(0, nums.lastIndex)\n\n        while (start <= end) {\n            val mid = start + (end - start) / 2\n\n            if (nums[mid] == target) return mid\n\n            // Determine if Left half is sorted\n            if (nums[start] <= nums[mid]) {\n                if (nums[start] <= target && target < nums[mid]) {\n                    end = mid - 1\n                } else {\n                    start = mid + 1\n                }\n            } else {\n                // Right half is sorted\n                if (nums[mid] < target && target <= nums[end]) {\n                    start = mid + 1\n                } else {\n                    end = mid - 1\n                }\n            }\n        }\n\n        return -1\n\n    }\n}"  python="class SearchInRotatedSortedArray {\n    def $1(nums: list[int], target: int): int {\n        var (start, end) = listOf(0, nums.lastIndex)\n\n        while (start <= end) {\n            $1 = start + (end - start) / 2\n\n            if (nums[mid] == target) return mid\n\n            // Determine if Left half is sorted\n            if (nums[start] <= nums[mid]) {\n                if (nums[start] <= target && target < nums[mid]) {\n                    end = mid - 1\n                } else {\n                    start = mid + 1\n                }\n            } else {\n                // Right half is sorted\n                if (nums[mid] < target && target <= nums[end]) {\n                    start = mid + 1\n                } else {\n                    end = mid - 1\n                }\n            }\n        }\n\n        return -1\n\n    }\n}"  rust="class SearchInRotatedSortedArray {\n    fn $1(nums: Vec<i32>, target -> i32) -> i32 {\n        var (start, end) = listOf(0, nums.len() - 1)\n\n        while (start <= end) {\n            val mid = start + (end - start) / 2\n\n            if (nums[mid] == target) return mid\n\n            // Determine if Left half is sorted\n            if (nums[start] <= nums[mid]) {\n                if (nums[start] <= target && target < nums[mid]) {\n                    end = mid - 1\n                } else {\n                    start = mid + 1\n                }\n            } else {\n                // Right half is sorted\n                if (nums[mid] < target && target <= nums[end]) {\n                    start = mid + 1\n                } else {\n                    end = mid - 1\n                }\n            }\n        }\n\n        return -1\n\n    }\n}"  cpp="class SearchInRotatedSortedArray {\n    $1(nums: vector<int>, target) {\n        var (start, end) = listOf(0, nums.size() - 1)\n\n        while (start <= end) {\n            auto $1 = start + (end - start) / 2\n\n            if (nums[mid] == target) return mid\n\n            // Determine if Left half is sorted\n            if (nums[start] <= nums[mid]) {\n                if (nums[start] <= target && target < nums[mid]) {\n                    end = mid - 1\n                } else {\n                    start = mid + 1\n                }\n            } else {\n                // Right half is sorted\n                if (nums[mid] < target && target <= nums[end]) {\n                    start = mid + 1\n                } else {\n                    end = mid - 1\n                }\n            }\n        }\n\n        return -1\n\n    }\n}" %}

---

## SearchInsertionPosition

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/SearchInsertionPosition.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass SearchInsertionPosition {\n    fun searchInsert(nums: IntArray, target: Int): Int {\n        var start = 0\n        var end = nums.size - 1\n\n        while (start <= end) {\n            val mid = start + ( end - start ) / 2\n\n            when {\n                nums[mid] == target -> return mid\n                nums[mid] > target -> end = mid - 1\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  java="class SearchInsertionPosition {\n    public static void $1(nums: IntArray, target) {\n        var start = 0\n        var end = nums.size - 1\n\n        while (start <= end) {\n            val mid = start + ( end - start ) / 2\n\n            when {\n                nums[mid] == target -> return mid\n                nums[mid] > target -> end = mid - 1\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  python="class SearchInsertionPosition {\n    def $1(nums: list[int], target: int): int {\n        $1 = 0\n        $1 = nums.size - 1\n\n        while (start <= end) {\n            $1 = start + ( end - start ) / 2\n\n            when {\n                nums[mid] == target -> return mid\n                nums[mid] > target -> end = mid - 1\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  rust="class SearchInsertionPosition {\n    fn $1(nums: Vec<i32>, target -> i32) -> i32 {\n        var start = 0\n        var end = nums.size - 1\n\n        while (start <= end) {\n            val mid = start + ( end - start ) / 2\n\n            when {\n                nums[mid] == target -> return mid\n                nums[mid] > target -> end = mid - 1\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}"  cpp="class SearchInsertionPosition {\n    $1(nums: vector<int>, target) {\n        auto $1 = 0\n        auto $1 = nums.size - 1\n\n        while (start <= end) {\n            auto $1 = start + ( end - start ) / 2\n\n            when {\n                nums[mid] == target -> return mid\n                nums[mid] > target -> end = mid - 1\n                else -> start = mid + 1\n            }\n        }\n\n        return start\n    }\n}" %}

---

## SingleElementInASortedArray

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/SingleElementInASortedArray.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass SingleElementInASortedArray {\n    fun singleNonDuplicate(nums: IntArray): Int {\n        var low = 0\n        var high = nums.size - 1\n\n        while (low < high) {\n            var mid = low + (high - low) / 2\n            // Ensure mid is even for checking pair with the next element\n            if (mid % 2 == 1) {\n                mid--\n            }\n\n            // Check if the pair is valid\n            if (nums[mid] == nums[mid + 1]) {\n                low = mid + 2 // Move to the right half\n            } else {\n                high = mid // Move to the left half\n            }\n        }\n\n        return nums[low] // low == high will give the unique element\n    }\n}\n"  java="class SingleElementInASortedArray {\n    public static void $1(nums: IntArray) {\n        var low = 0\n        var high = nums.size - 1\n\n        while (low < high) {\n            var mid = low + (high - low) / 2\n            // Ensure mid is even for checking pair with the next element\n            if (mid % 2 == 1) {\n                mid--\n            }\n\n            // Check if the pair is valid\n            if (nums[mid] == nums[mid + 1]) {\n                low = mid + 2 // Move to the right half\n            } else {\n                high = mid // Move to the left half\n            }\n        }\n\n        return nums[low] // low == high will give the unique element\n    }\n}"  python="class SingleElementInASortedArray {\n    def $1(nums: list[int]): int {\n        $1 = 0\n        $1 = nums.size - 1\n\n        while (low < high) {\n            $1 = low + (high - low) / 2\n            // Ensure mid is even for checking pair with the next element\n            if (mid % 2 == 1) {\n                mid--\n            }\n\n            // Check if the pair is valid\n            if (nums[mid] == nums[mid + 1]) {\n                low = mid + 2 // Move to the right half\n            } else {\n                high = mid // Move to the left half\n            }\n        }\n\n        return nums[low] // low == high will give the unique element\n    }\n}"  rust="class SingleElementInASortedArray {\n    fn $1(nums: Vec<i32>) -> i32 {\n        var low = 0\n        var high = nums.size - 1\n\n        while (low < high) {\n            var mid = low + (high - low) / 2\n            // Ensure mid is even for checking pair with the next element\n            if (mid % 2 == 1) {\n                mid--\n            }\n\n            // Check if the pair is valid\n            if (nums[mid] == nums[mid + 1]) {\n                low = mid + 2 // Move to the right half\n            } else {\n                high = mid // Move to the left half\n            }\n        }\n\n        return nums[low] // low == high will give the unique element\n    }\n}"  cpp="class SingleElementInASortedArray {\n    $1(nums: vector<int>) {\n        auto $1 = 0\n        auto $1 = nums.size - 1\n\n        while (low < high) {\n            auto $1 = low + (high - low) / 2\n            // Ensure mid is even for checking pair with the next element\n            if (mid % 2 == 1) {\n                mid--\n            }\n\n            // Check if the pair is valid\n            if (nums[mid] == nums[mid + 1]) {\n                low = mid + 2 // Move to the right half\n            } else {\n                high = mid // Move to the left half\n            }\n        }\n\n        return nums[low] // low == high will give the unique element\n    }\n}" %}

---

## ValleyElement

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/binarysearch/ValleyElement.kt`

{% include code-tabs.html  kotlin="package binarysearch\n\nclass ValleyElement {\n    fun findValleyElementBinary(nums: IntArray): Int? {\n        if (nums.isEmpty()) return null\n\n        var (left, right) = 0 to nums.size\n\n        while (left < right) {\n            val mid = left + (right - left) / 2\n\n            // Safely handle boundaries\n            val leftNeighbor = if (mid > 0) nums[mid - 1] else Int.MAX_VALUE\n            val rightNeighbor = if (mid < nums.size - 1) nums[mid + 1] else Int.MAX_VALUE\n\n            when {\n                nums[mid] < leftNeighbor && nums[mid] < rightNeighbor -> return nums[mid] // Found valley\n                nums[mid] > rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return null // No valley found (unlikely for a valid array)\n    }\n}"  java="class ValleyElement {\n    public static void $1(nums: IntArray)? {\n        if (nums.isEmpty()) return null\n\n        var (left, right) = 0 to nums.size\n\n        while (left < right) {\n            val mid = left + (right - left) / 2\n\n            // Safely handle boundaries\n            val leftNeighbor = if (mid > 0) nums[mid - 1] else Int.MAX_VALUE\n            val rightNeighbor = if (mid < nums.size - 1) nums[mid + 1] else Int.MAX_VALUE\n\n            when {\n                nums[mid] < leftNeighbor && nums[mid] < rightNeighbor -> return nums[mid] // Found valley\n                nums[mid] > rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return null // No valley found (unlikely for a valid array)\n    }\n}"  python="class ValleyElement {\n    def $1(nums: list[int]): int? {\n        if (nums.isEmpty()) return None\n\n        var (left, right) = 0 to nums.size\n\n        while (left < right) {\n            $1 = left + (right - left) / 2\n\n            // Safely handle boundaries\n            $1 = if (mid > 0) nums[mid - 1] else Int.MAX_VALUE\n            $1 = if (mid < nums.size - 1) nums[mid + 1] else Int.MAX_VALUE\n\n            when {\n                nums[mid] < leftNeighbor && nums[mid] < rightNeighbor -> return nums[mid] // Found valley\n                nums[mid] > rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return None // No valley found (unlikely for a valid array)\n    }\n}"  rust="class ValleyElement {\n    fn $1(nums: Vec<i32>) -> i32? {\n        if (nums.isEmpty()) return None\n\n        var (left, right) = 0 to nums.size\n\n        while (left < right) {\n            val mid = left + (right - left) / 2\n\n            // Safely handle boundaries\n            val leftNeighbor = if (mid > 0) nums[mid - 1] else Int.MAX_VALUE\n            val rightNeighbor = if (mid < nums.size - 1) nums[mid + 1] else Int.MAX_VALUE\n\n            when {\n                nums[mid] < leftNeighbor && nums[mid] < rightNeighbor -> return nums[mid] // Found valley\n                nums[mid] > rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return None // No valley found (unlikely for a valid array)\n    }\n}"  cpp="class ValleyElement {\n    $1(nums: vector<int>)? {\n        if (nums.isEmpty()) return nullptr\n\n        var (left, right) = 0 to nums.size\n\n        while (left < right) {\n            auto $1 = left + (right - left) / 2\n\n            // Safely handle boundaries\n            auto $1 = if (mid > 0) nums[mid - 1] else Int.MAX_VALUE\n            auto $1 = if (mid < nums.size - 1) nums[mid + 1] else Int.MAX_VALUE\n\n            when {\n                nums[mid] < leftNeighbor && nums[mid] < rightNeighbor -> return nums[mid] // Found valley\n                nums[mid] > rightNeighbor -> left = mid + 1 // Move to the right\n                else -> right = mid // Move to the left\n            }\n        }\n\n        return nullptr // No valley found (unlikely for a valid array)\n    }\n}" %}
