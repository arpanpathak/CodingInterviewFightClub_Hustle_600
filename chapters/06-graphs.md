---
layout: chapter
title: "Graphs"
chapter_number: 6
chapter_title: "Graphs"
toc: true
prev_chapter:
  url: "/chapters/05-trees"
  title: "Trees"
next_chapter:
  url: "/chapters/07-bit-manipulation"
  title: "Bit Manipulation"
---

# Graphs

> **26 problems** from the CrackGoogle repository. Click the tabs to switch between **Kotlin, Java, Python, Rust, and C++**.

## Complete Problem Set

| # | Problem | Topic | Code Available |
|---|---------|-------|----------------|
| 1 | **BusRoutes** | graph | ✅ All 5 languages |
| 2 | **CloneGraph** | graph | ✅ All 5 languages |
| 3 | **DiameterOfBinaryTree** | graph | ✅ All 5 languages |
| 4 | **HouseRobber3** | graph | ✅ All 5 languages |
| 5 | **ReorderRoutesToMakeAllPathsLeadToCityZero** | graph | ✅ All 5 languages |
| 6 | **WordLadder** | graph | ✅ All 5 languages |
| 7 | **FindRedundentConnections** | graph/mst | ✅ All 5 languages |
| 8 | **PrimsAlgorithm** | graph/mst | ✅ All 5 languages |
| 9 | **CriticalConnectionsInANetwork** | graph/articulation_point | ✅ All 5 languages |
| 10 | **CriticalConnectionsInANetworkShortCode** | graph/articulation_point | ✅ All 5 languages |
| 11 | **FindArticulationPoints** | graph/articulation_point | ✅ All 5 languages |
| 12 | **CourseSchedule** | graph/cycle | ✅ All 5 languages |
| 13 | **CourseSchedule_II** | graph/cycle | ✅ All 5 languages |
| 14 | **ParallelCourses** | graph/cycle | ✅ All 5 languages |
| 15 | **AlienDictionary** | graph/topological_sort | ✅ All 5 languages |
| 16 | **AlienDictionary_BFS** | graph/topological_sort | ✅ All 5 languages |
| 17 | **ApplySubstitutions** | graph/topological_sort | ✅ All 5 languages |
| 18 | **CourseSchedule_II** | graph/topological_sort | ✅ All 5 languages |
| 19 | **CourseSchedule_II_BFS** | graph/topological_sort | ✅ All 5 languages |
| 20 | **CheapestFlightWithKStops** | graph/greedy | ✅ All 5 languages |
| 21 | **CheapestFlightsWithKStops** | graph/greedy | ✅ All 5 languages |
| 22 | **DonaldTrumpAlgorithm** | graph/greedy | ✅ All 5 languages |
| 23 | **TheMaze_III** | graph/greedy | ✅ All 5 languages |
| 24 | **CheapestFlightsWithinKStops** | graph/dp | ✅ All 5 languages |
| 25 | **BinarySearchTreeToGreaterSumTree** | graph/bst | ✅ All 5 languages |
| 26 | **RangeSumOfBST** | graph/bst | ✅ All 5 languages |

---

## BusRoutes

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/BusRoutes.kt`

{% include code-tabs.html  kotlin="package graph\n\nimport java.util.*\n\nimport java.util.*\n\nclass BusRoutes {\n    data class Node(val stop: Int, val busCount: Int)\n\n    fun numBusesToDestination(routes: Array<IntArray>, source: Int, target: Int): Int {\n        if (source == target) return 0\n\n        val graph = mutableMapOf<Int, MutableList<Int>>() // Stop -> List of buses passing through\n\n        // Build the graph\n        for (bus in routes.indices) {\n            for (stop in routes[bus]) {\n                graph.getOrPut(stop) { mutableListOf() }.add(bus)\n            }\n        }\n\n        val queue: Queue<Node> = LinkedList()\n        val visitedBuses = mutableSetOf<Int>()\n        val visitedStops = mutableSetOf<Int>()\n\n        queue.offer(Node(source, 0))\n        visitedStops.add(source)\n\n        while (queue.isNotEmpty()) {\n            val (currentStop, busCount) = queue.poll()\n\n            if (currentStop == target) return busCount\n\n            // Explore all buses passing through the current stop\n            graph[currentStop]?.let { buses ->\n                for (bus in buses) {\n                    if (bus !in visitedBuses) {\n                        visitedBuses.add(bus)\n\n                        for (stop in routes[bus]) {\n                            if (stop !in visitedStops) {\n                                queue.offer(Node(stop, busCount + 1))\n                                visitedStops.add(stop)\n                            }\n                        }\n                    }\n                }\n            }\n        }\n        return -1\n    }\n}\n\n// (1) =>"  java="class BusRoutes {\n    data class Node(val stop, val busCount)\n\n    public static void $1(routes: Array<IntArray>, source, target) {\n        if (source == target) return 0\n\n        val graph = mutableMapOf<Int, MutableList<Int>>() // Stop -> List of buses passing through\n\n        // Build the graph\n        for (bus in routes.indices) {\n            for (stop in routes[bus]) {\n                graph.getOrPut(stop) { mutableListOf() }.add(bus)\n            }\n        }\n\n        val queue: Queue<Node> = LinkedList()\n        val visitedBuses = mutableSetOf<Int>()\n        val visitedStops = mutableSetOf<Int>()\n\n        queue.offer(Node(source, 0))\n        visitedStops.add(source)\n\n        while (queue.isNotEmpty()) {\n            val (currentStop, busCount) = queue.poll()\n\n            if (currentStop == target) return busCount\n\n            // Explore all buses passing through the current stop\n            graph[currentStop]?.let { buses ->\n                for (bus in buses) {\n                    if (bus !in visitedBuses) {\n                        visitedBuses.add(bus)\n\n                        for (stop in routes[bus]) {\n                            if (stop !in visitedStops) {\n                                queue.offer(Node(stop, busCount + 1))\n                                visitedStops.add(stop)\n                            }\n                        }\n                    }\n                }\n            }\n        }\n        return -1\n    }\n}\n\n// (1) =>"  python="class BusRoutes {\n    data class Node(val stop: int, val busCount: int)\n\n    def $1(routes: Array<list[int]>, source: int, target: int): int {\n        if (source == target) return 0\n\n        $1 = mutableMapOf<Int, MutableList<Int>>() // Stop -> List of buses passing through\n\n        // Build the graph\n        for (bus in routes.indices) {\n            for (stop in routes[bus]) {\n                graph.getOrPut(stop) { mutableListOf() }.add(bus)\n            }\n        }\n\n        val queue: Queue<Node> = LinkedList()\n        $1 = mutableSetOf<Int>()\n        $1 = mutableSetOf<Int>()\n\n        queue.offer(Node(source, 0))\n        visitedStops.add(source)\n\n        while (queue.isNotEmpty()) {\n            ($1, $2) = queue.poll()\n\n            if (currentStop == target) return busCount\n\n            // Explore all buses passing through the current stop\n            graph[currentStop]?.let { buses ->\n                for (bus in buses) {\n                    if (bus !in visitedBuses) {\n                        visitedBuses.add(bus)\n\n                        for (stop in routes[bus]) {\n                            if (stop !in visitedStops) {\n                                queue.offer(Node(stop, busCount + 1))\n                                visitedStops.add(stop)\n                            }\n                        }\n                    }\n                }\n            }\n        }\n        return -1\n    }\n}\n\n// (1) =>"  rust="class BusRoutes {\n    data class Node(val stop -> i32, val busCount -> i32)\n\n    fn $1(routes: Array<Vec<i32>>, source -> i32, target -> i32) -> i32 {\n        if (source == target) return 0\n\n        val graph = mutableMapOf<Int, MutableList<Int>>() // Stop -> List of buses passing through\n\n        // Build the graph\n        for (bus in routes.indices) {\n            for (stop in routes[bus]) {\n                graph.getOrPut(stop) { mutableListOf() }.add(bus)\n            }\n        }\n\n        val queue: Queue<Node> = LinkedList()\n        val visitedBuses = mutableSetOf<Int>()\n        val visitedStops = mutableSetOf<Int>()\n\n        queue.offer(Node(source, 0))\n        visitedStops.add(source)\n\n        while (queue.isNotEmpty()) {\n            val (currentStop, busCount) = queue.poll()\n\n            if (currentStop == target) return busCount\n\n            // Explore all buses passing through the current stop\n            graph[currentStop]?.let { buses ->\n                for (bus in buses) {\n                    if (bus !in visitedBuses) {\n                        visitedBuses.add(bus)\n\n                        for (stop in routes[bus]) {\n                            if (stop !in visitedStops) {\n                                queue.offer(Node(stop, busCount + 1))\n                                visitedStops.add(stop)\n                            }\n                        }\n                    }\n                }\n            }\n        }\n        return -1\n    }\n}\n\n// (1) =>"  cpp="class BusRoutes {\n    data class Node(val stop, val busCount)\n\n    $1(routes: Array<vector<int>>, source, target) {\n        if (source == target) return 0\n\n        auto $1 = mutableMapOf<Int, MutableList<Int>>() // Stop -> List of buses passing through\n\n        // Build the graph\n        for (bus in routes.indices) {\n            for (stop in routes[bus]) {\n                graph.getOrPut(stop) { mutableListOf() }.add(bus)\n            }\n        }\n\n        val queue: Queue<Node> = LinkedList()\n        auto $1 = mutableSetOf<Int>()\n        auto $1 = mutableSetOf<Int>()\n\n        queue.offer(Node(source, 0))\n        visitedStops.add(source)\n\n        while (queue.isNotEmpty()) {\n            val (currentStop, busCount) = queue.poll()\n\n            if (currentStop == target) return busCount\n\n            // Explore all buses passing through the current stop\n            graph[currentStop]?.let { buses ->\n                for (bus in buses) {\n                    if (bus !in visitedBuses) {\n                        visitedBuses.add(bus)\n\n                        for (stop in routes[bus]) {\n                            if (stop !in visitedStops) {\n                                queue.offer(Node(stop, busCount + 1))\n                                visitedStops.add(stop)\n                            }\n                        }\n                    }\n                }\n            }\n        }\n        return -1\n    }\n}\n\n// (1) =>" %}

---

## CloneGraph

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/CloneGraph.kt`

{% include code-tabs.html  kotlin="package graph\n\ndata class Node(val `val`: Int, val neighbors: MutableList<Node?> = mutableListOf())\n\nclass CloneGraph {\n    val map = mutableMapOf<Node, Node>()\n\n    fun cloneGraph(node: Node?): Node? {\n\n        if (node == null) return null\n        map[node]?.let { return it }\n\n        val clonedNode = Node(node?.`val`!!)\n        map[node] = clonedNode\n\n        node.neighbors.forEach{ clonedNode.neighbors.add(cloneGraph(it)) }\n\n        return clonedNode\n    }\n}"  java="data class Node(val `val`, val neighbors: MutableList<Node?> = mutableListOf())\n\nclass CloneGraph {\n    val map = mutableMapOf<Node, Node>()\n\n    public static void $1(node: Node?): Node? {\n\n        if (node == null) return null\n        map[node]?.let { return it }\n\n        val clonedNode = Node(node?.`val`!!)\n        map[node] = clonedNode\n\n        node.neighbors.forEach{ clonedNode.neighbors.add(cloneGraph(it)) }\n\n        return clonedNode\n    }\n}"  python="data class Node(val `val`: int, val neighbors: MutableList<Node?> = mutableListOf())\n\nclass CloneGraph {\n    $1 = mutableMapOf<Node, Node>()\n\n    def $1(node: Node?): Node? {\n\n        if (node == None) return None\n        map[node]?.let { return it }\n\n        $1 = Node(node?.`val`!!)\n        map[node] = clonedNode\n\n        node.neighbors.forEach{ clonedNode.neighbors.add(cloneGraph(it)) }\n\n        return clonedNode\n    }\n}"  rust="data class Node(val `val` -> i32, val neighbors: MutableList<Node?> = mutableListOf())\n\nclass CloneGraph {\n    val map = mutableMapOf<Node, Node>()\n\n    fn $1(node: Node?): Node? {\n\n        if (node == None) return None\n        map[node]?.let { return it }\n\n        val clonedNode = Node(node?.`val`!!)\n        map[node] = clonedNode\n\n        node.neighbors.forEach{ clonedNode.neighbors.add(cloneGraph(it)) }\n\n        return clonedNode\n    }\n}"  cpp="data class Node(val `val`, val neighbors: MutableList<Node?> = mutableListOf())\n\nclass CloneGraph {\n    auto $1 = mutableMapOf<Node, Node>()\n\n    $1(node: Node?): Node? {\n\n        if (node == nullptr) return nullptr\n        map[node]?.let { return it }\n\n        auto $1 = Node(node?.`val`!!)\n        map[node] = clonedNode\n\n        node.neighbors.forEach{ clonedNode.neighbors.add(cloneGraph(it)) }\n\n        return clonedNode\n    }\n}" %}

---

## DiameterOfBinaryTree

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/DiameterOfBinaryTree.kt`

{% include code-tabs.html  kotlin="package graph\n\nimport javax.swing.Spring.height\n\nclass DiameterOfBinaryTree {\n    fun diameterOfBinaryTree(root: TreeNode?): Int {\n        var max = 0\n        fun height(root: TreeNode?): Int {\n            if (null == root) return -1\n            val left = height(root.left)\n            val right = height(root.right)\n            max = Math.max(max, 2 + left + right)\n            return 1 + Math.max(left, right)\n        }\n\n        height(root)\n        return max\n    }\n}"  java="class DiameterOfBinaryTree {\n    public static void $1(root: TreeNode?) {\n        var max = 0\n        public static void $1(root: TreeNode?) {\n            if (null == root) return -1\n            val left = height(root.left)\n            val right = height(root.right)\n            max = Math.max(max, 2 + left + right)\n            return 1 + Math.max(left, right)\n        }\n\n        height(root)\n        return max\n    }\n}"  python="class DiameterOfBinaryTree {\n    def $1(root: TreeNode?): int {\n        $1 = 0\n        def $1(root: TreeNode?): int {\n            if (None == root) return -1\n            $1 = height(root.left)\n            $1 = height(root.right)\n            max = Math.max(max, 2 + left + right)\n            return 1 + Math.max(left, right)\n        }\n\n        height(root)\n        return max\n    }\n}"  rust="class DiameterOfBinaryTree {\n    fn $1(root: TreeNode?) -> i32 {\n        var max = 0\n        fn $1(root: TreeNode?) -> i32 {\n            if (None == root) return -1\n            val left = height(root.left)\n            val right = height(root.right)\n            max = Math.max(max, 2 + left + right)\n            return 1 + Math.max(left, right)\n        }\n\n        height(root)\n        return max\n    }\n}"  cpp="class DiameterOfBinaryTree {\n    $1(root: TreeNode?) {\n        auto $1 = 0\n        $1(root: TreeNode?) {\n            if (nullptr == root) return -1\n            auto $1 = height(root.left)\n            auto $1 = height(root.right)\n            max = Math.max(max, 2 + left + right)\n            return 1 + Math.max(left, right)\n        }\n\n        height(root)\n        return max\n    }\n}" %}

---

## HouseRobber3

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/HouseRobber3.kt`

{% include code-tabs.html  kotlin="package graph\n\nimport kotlin.math.max\n\nclass TreeNode(var `val`: Int) {\n    var left: TreeNode? = null\n    var right: TreeNode? = null\n}\n\nclass HouseRobber3 {\n    fun rob(root: TreeNode?): Int {\n        fun dfs (node: TreeNode?): IntArray {\n            if (node == null)\n                return intArrayOf(0 , 0)\n\n\n            val (left, right) = arrayOf( dfs(node?.left), dfs(node?.right) )\n\n            // If we rob this house then we can't rob children\n            val toRob = node.`val` + left[1] + right[1]\n\n            // If we don't rob then we could rob children and add them up\n            val notToRob = maxOf(left[0], left[1]) + max(right[0], right[1])\n\n            return intArrayOf(toRob, notToRob)\n        }\n\n        val result = dfs(root)\n\n        return result.max()\n    }\n\n}"  java="class TreeNode(var `val`) {\n    var left: TreeNode? = null\n    var right: TreeNode? = null\n}\n\nclass HouseRobber3 {\n    public static void $1(root: TreeNode?) {\n        fun dfs (node: TreeNode?): IntArray {\n            if (node == null)\n                return intArrayOf(0 , 0)\n\n\n            val (left, right) = arrayOf( dfs(node?.left), dfs(node?.right) )\n\n            // If we rob this house then we can't rob children\n            val toRob = node.`val` + left[1] + right[1]\n\n            // If we don't rob then we could rob children and add them up\n            val notToRob = maxOf(left[0], left[1]) + max(right[0], right[1])\n\n            return intArrayOf(toRob, notToRob)\n        }\n\n        val result = dfs(root)\n\n        return result.max()\n    }\n\n}"  python="class TreeNode(var `val`: int) {\n    var left: TreeNode? = None\n    var right: TreeNode? = None\n}\n\nclass HouseRobber3 {\n    def $1(root: TreeNode?): int {\n        fun dfs (node: TreeNode?): list[int] {\n            if (node == None)\n                return intArrayOf(0 , 0)\n\n\n            ($1, $2) = arrayOf( dfs(node?.left), dfs(node?.right) )\n\n            // If we rob this house then we can't rob children\n            $1 = node.`val` + left[1] + right[1]\n\n            // If we don't rob then we could rob children and add them up\n            $1 = max(left[0], left[1]) + max(right[0], right[1])\n\n            return intArrayOf(toRob, notToRob)\n        }\n\n        $1 = dfs(root)\n\n        return result.max()\n    }\n\n}"  rust="class TreeNode(var `val` -> i32) {\n    var left: TreeNode? = None\n    var right: TreeNode? = None\n}\n\nclass HouseRobber3 {\n    fn $1(root: TreeNode?) -> i32 {\n        fun dfs (node: TreeNode?): Vec<i32> {\n            if (node == None)\n                return intArrayOf(0 , 0)\n\n\n            val (left, right) = arrayOf( dfs(node?.left), dfs(node?.right) )\n\n            // If we rob this house then we can't rob children\n            val toRob = node.`val` + left[1] + right[1]\n\n            // If we don't rob then we could rob children and add them up\n            val notToRob = max(left[0], left[1]) + max(right[0], right[1])\n\n            return intArrayOf(toRob, notToRob)\n        }\n\n        val result = dfs(root)\n\n        return result.max()\n    }\n\n}"  cpp="class TreeNode(var `val`) {\n    var left: TreeNode? = nullptr\n    var right: TreeNode? = nullptr\n}\n\nclass HouseRobber3 {\n    $1(root: TreeNode?) {\n        fun dfs (node: TreeNode?): vector<int> {\n            if (node == nullptr)\n                return intArrayOf(0 , 0)\n\n\n            val (left, right) = arrayOf( dfs(node?.left), dfs(node?.right) )\n\n            // If we rob this house then we can't rob children\n            auto $1 = node.`val` + left[1] + right[1]\n\n            // If we don't rob then we could rob children and add them up\n            auto $1 = max(left[0], left[1]) + max(right[0], right[1])\n\n            return intArrayOf(toRob, notToRob)\n        }\n\n        auto $1 = dfs(root)\n\n        return result.max()\n    }\n\n}" %}

---

## ReorderRoutesToMakeAllPathsLeadToCityZero

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/ReorderRoutesToMakeAllPathsLeadToCityZero.kt`

{% include code-tabs.html  kotlin="package graph\n\nclass ReorderRoutesToMakeAllPathsLeadToCityZero {\n    fun minReorder(n: Int, connections: Array<IntArray>): Int {\n        val graph = Array(n) { mutableListOf<IntArray>() }\n\n        // Build adjacency list as an undirected graph\n        for (edge in connections) {\n            graph[edge[0]].add(edge)  // Forward edge\n            graph[edge[1]].add(edge)  // Backward edge\n        }\n\n        var changes = 0\n        val visited = BooleanArray(n)\n\n        fun dfs(city: Int) {\n            visited[city] = true\n            for (edge in graph[city]) {\n                val (from, to) = edge\n                val neighbor = if (from == city) to else from\n                if (!visited[neighbor]) {\n                    if (from == city) changes++ // Forward edge needs reversal\n                    dfs(neighbor)\n                }\n            }\n        }\n\n        dfs(0) // Start DFS from city 0\n        return changes\n    }\n}"  java="class ReorderRoutesToMakeAllPathsLeadToCityZero {\n    public static void $1(n, connections: Array<IntArray>) {\n        val graph = Array(n) { mutableListOf<IntArray>() }\n\n        // Build adjacency list as an undirected graph\n        for (edge in connections) {\n            graph[edge[0]].add(edge)  // Forward edge\n            graph[edge[1]].add(edge)  // Backward edge\n        }\n\n        var changes = 0\n        val visited = BooleanArray(n)\n\n        public static void $1(city) {\n            visited[city] = true\n            for (edge in graph[city]) {\n                val (from, to) = edge\n                val neighbor = if (from == city) to else from\n                if (!visited[neighbor]) {\n                    if (from == city) changes++ // Forward edge needs reversal\n                    dfs(neighbor)\n                }\n            }\n        }\n\n        dfs(0) // Start DFS from city 0\n        return changes\n    }\n}"  python="class ReorderRoutesToMakeAllPathsLeadToCityZero {\n    def $1(n: int, connections: Array<list[int]>): int {\n        $1 = Array(n) { mutableListOf<list[int]>() }\n\n        // Build adjacency list as an undirected graph\n        for (edge in connections) {\n            graph[edge[0]].add(edge)  // Forward edge\n            graph[edge[1]].add(edge)  // Backward edge\n        }\n\n        $1 = 0\n        $1 = BooleanArray(n)\n\n        def $1(city: int) {\n            visited[city] = True\n            for (edge in graph[city]) {\n                ($1, $2) = edge\n                $1 = if (from == city) to else from\n                if (!visited[neighbor]) {\n                    if (from == city) changes++ // Forward edge needs reversal\n                    dfs(neighbor)\n                }\n            }\n        }\n\n        dfs(0) // Start DFS from city 0\n        return changes\n    }\n}"  rust="class ReorderRoutesToMakeAllPathsLeadToCityZero {\n    fn $1(n -> i32, connections: Array<Vec<i32>>) -> i32 {\n        val graph = Array(n) { mutableListOf<Vec<i32>>() }\n\n        // Build adjacency list as an undirected graph\n        for (edge in connections) {\n            graph[edge[0]].add(edge)  // Forward edge\n            graph[edge[1]].add(edge)  // Backward edge\n        }\n\n        var changes = 0\n        val visited = BooleanArray(n)\n\n        fn $1(city -> i32) {\n            visited[city] = true\n            for (edge in graph[city]) {\n                val (from, to) = edge\n                val neighbor = if (from == city) to else from\n                if (!visited[neighbor]) {\n                    if (from == city) changes++ // Forward edge needs reversal\n                    dfs(neighbor)\n                }\n            }\n        }\n\n        dfs(0) // Start DFS from city 0\n        return changes\n    }\n}"  cpp="class ReorderRoutesToMakeAllPathsLeadToCityZero {\n    $1(n, connections: Array<vector<int>>) {\n        auto $1 = Array(n) { mutableListOf<vector<int>>() }\n\n        // Build adjacency list as an undirected graph\n        for (edge in connections) {\n            graph[edge[0]].add(edge)  // Forward edge\n            graph[edge[1]].add(edge)  // Backward edge\n        }\n\n        auto $1 = 0\n        auto $1 = BooleanArray(n)\n\n        $1(city) {\n            visited[city] = true\n            for (edge in graph[city]) {\n                val (from, to) = edge\n                auto $1 = if (from == city) to else from\n                if (!visited[neighbor]) {\n                    if (from == city) changes++ // Forward edge needs reversal\n                    dfs(neighbor)\n                }\n            }\n        }\n\n        dfs(0) // Start DFS from city 0\n        return changes\n    }\n}" %}

---

## WordLadder

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/WordLadder.kt`

{% include code-tabs.html  kotlin="package graph\n\nimport java.util.*\n\nclass WordLadder {\n    fun ladderLength(beginWord: String, endWord: String, wordList: List<String>): Int {\n        if (endWord !in wordList) return 0 // if end word is not word bank then it's not possible\n\n        val wordSet = wordList.toHashSet() // Store the word list in a set for O(1) lookups\n        val queue: Queue<Pair<String, Int>> = LinkedList() // Queue to store word and level (distance)\n        queue.offer(beginWord to 1) // Start with the beginWord and level 1\n\n        while (queue.isNotEmpty()) {\n            val (currentWord, level) = queue.poll()\n\n            // Check all possible transformations by changing each character\n            for (i in currentWord.indices) {\n                val originalChar = currentWord[i]\n                for (ch in 'a'..'z') {\n                    // Create a new word by replacing the i-th character\n                    val newWord = currentWord.substring(0, i) + ch + currentWord.substring(i + 1)\n\n                    // If the new word is the end word, return the result\n                    if (newWord == endWord) return level + 1\n\n                    // If the new word is in the word set, we can add it to the queue\n                    if (newWord in wordSet) {\n                        wordSet.remove(newWord) // Remove to avoid revisiting\n                        queue.offer(newWord to level + 1)\n                    }\n                }\n            }\n        }\n\n        return 0 // If no transformation is found\n    }\n}"  java="class WordLadder {\n    public static void $1(beginWord, endWord, wordList: List<String>) {\n        if (endWord !in wordList) return 0 // if end word is not word bank then it's not possible\n\n        val wordSet = wordList.toHashSet() // Store the word list in a set for O(1) lookups\n        val queue: Queue<Pair<String, Int>> = LinkedList() // Queue to store word and level (distance)\n        queue.offer(beginWord to 1) // Start with the beginWord and level 1\n\n        while (queue.isNotEmpty()) {\n            val (currentWord, level) = queue.poll()\n\n            // Check all possible transformations by changing each character\n            for (i in currentWord.indices) {\n                val originalChar = currentWord[i]\n                for (ch in 'a'..'z') {\n                    // Create a new word by replacing the i-th character\n                    val newWord = currentWord.substring(0, i) + ch + currentWord.substring(i + 1)\n\n                    // If the new word is the end word, return the result\n                    if (newWord == endWord) return level + 1\n\n                    // If the new word is in the word set, we can add it to the queue\n                    if (newWord in wordSet) {\n                        wordSet.remove(newWord) // Remove to avoid revisiting\n                        queue.offer(newWord to level + 1)\n                    }\n                }\n            }\n        }\n\n        return 0 // If no transformation is found\n    }\n}"  python="class WordLadder {\n    def $1(beginWord: str, endWord: str, wordList: List<String>): int {\n        if (endWord !in wordList) return 0 // if end word is not word bank then it's not possible\n\n        $1 = wordList.toHashSet() // Store the word list in a set for O(1) lookups\n        val queue: Queue<Pair<String, Int>> = LinkedList() // Queue to store word and level (distance)\n        queue.offer(beginWord to 1) // Start with the beginWord and level 1\n\n        while (queue.isNotEmpty()) {\n            ($1, $2) = queue.poll()\n\n            // Check all possible transformations by changing each character\n            for (i in currentWord.indices) {\n                $1 = currentWord[i]\n                for (ch in 'a'..'z') {\n                    // Create a new word by replacing the i-th character\n                    $1 = currentWord.substring(0, i) + ch + currentWord.substring(i + 1)\n\n                    // If the new word is the end word, return the result\n                    if (newWord == endWord) return level + 1\n\n                    // If the new word is in the word set, we can add it to the queue\n                    if (newWord in wordSet) {\n                        wordSet.remove(newWord) // Remove to avoid revisiting\n                        queue.offer(newWord to level + 1)\n                    }\n                }\n            }\n        }\n\n        return 0 // If no transformation is found\n    }\n}"  rust="class WordLadder {\n    fn $1(beginWord -> String, endWord -> String, wordList: List<String>) -> i32 {\n        if (endWord !in wordList) return 0 // if end word is not word bank then it's not possible\n\n        val wordSet = wordList.toHashSet() // Store the word list in a set for O(1) lookups\n        val queue: Queue<Pair<String, Int>> = LinkedList() // Queue to store word and level (distance)\n        queue.offer(beginWord to 1) // Start with the beginWord and level 1\n\n        while (queue.isNotEmpty()) {\n            val (currentWord, level) = queue.poll()\n\n            // Check all possible transformations by changing each character\n            for (i in currentWord.indices) {\n                val originalChar = currentWord[i]\n                for (ch in 'a'..'z') {\n                    // Create a new word by replacing the i-th character\n                    val newWord = currentWord.substring(0, i) + ch + currentWord.substring(i + 1)\n\n                    // If the new word is the end word, return the result\n                    if (newWord == endWord) return level + 1\n\n                    // If the new word is in the word set, we can add it to the queue\n                    if (newWord in wordSet) {\n                        wordSet.remove(newWord) // Remove to avoid revisiting\n                        queue.offer(newWord to level + 1)\n                    }\n                }\n            }\n        }\n\n        return 0 // If no transformation is found\n    }\n}"  cpp="class WordLadder {\n    $1(beginWord, endWord, wordList: List<String>) {\n        if (endWord !in wordList) return 0 // if end word is not word bank then it's not possible\n\n        auto $1 = wordList.toHashSet() // Store the word list in a set for O(1) lookups\n        val queue: Queue<Pair<String, Int>> = LinkedList() // Queue to store word and level (distance)\n        queue.offer(beginWord to 1) // Start with the beginWord and level 1\n\n        while (queue.isNotEmpty()) {\n            val (currentWord, level) = queue.poll()\n\n            // Check all possible transformations by changing each character\n            for (i in currentWord.indices) {\n                auto $1 = currentWord[i]\n                for (ch in 'a'..'z') {\n                    // Create a new word by replacing the i-th character\n                    auto $1 = currentWord.substring(0, i) + ch + currentWord.substring(i + 1)\n\n                    // If the new word is the end word, return the result\n                    if (newWord == endWord) return level + 1\n\n                    // If the new word is in the word set, we can add it to the queue\n                    if (newWord in wordSet) {\n                        wordSet.remove(newWord) // Remove to avoid revisiting\n                        queue.offer(newWord to level + 1)\n                    }\n                }\n            }\n        }\n\n        return 0 // If no transformation is found\n    }\n}" %}

---

## FindRedundentConnections

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/mst/FindRedundentConnections.kt`

{% include code-tabs.html  kotlin="package graph.mst\n\nclass FindRedundentConnections {\n    fun findRedundantConnection(edges: Array<IntArray>): IntArray {\n        val parent = IntArray(1001) { it }\n\n        fun find(x: Int): Int {\n            if (parent[x] != x) parent[x] = find(parent[x])\n            return parent[x]\n        }\n\n        fun union(x: Int, y: Int): Boolean {\n            val rootX = find(x)\n            val rootY = find(y)\n            if (rootX == rootY) return false\n            parent[rootY] = rootX\n            return true\n        }\n\n        for ((u, v) in edges) {\n            if (!union(u, v)) return intArrayOf(u, v)\n        }\n        return intArrayOf()\n    }\n\n}"  java="class FindRedundentConnections {\n    public static void $1(edges: Array<IntArray>): IntArray {\n        val parent = IntArray(1001) { it }\n\n        public static void $1(x) {\n            if (parent[x] != x) parent[x] = find(parent[x])\n            return parent[x]\n        }\n\n        public static void $1(x, y) {\n            val rootX = find(x)\n            val rootY = find(y)\n            if (rootX == rootY) return false\n            parent[rootY] = rootX\n            return true\n        }\n\n        for ((u, v) in edges) {\n            if (!union(u, v)) return intArrayOf(u, v)\n        }\n        return intArrayOf()\n    }\n\n}"  python="class FindRedundentConnections {\n    def $1(edges: Array<list[int]>): list[int] {\n        $1 = list[int](1001) { it }\n\n        def $1(x: int): int {\n            if (parent[x] != x) parent[x] = find(parent[x])\n            return parent[x]\n        }\n\n        def $1(x: int, y: int): bool {\n            $1 = find(x)\n            $1 = find(y)\n            if (rootX == rootY) return False\n            parent[rootY] = rootX\n            return True\n        }\n\n        for ((u, v) in edges) {\n            if (!union(u, v)) return intArrayOf(u, v)\n        }\n        return intArrayOf()\n    }\n\n}"  rust="class FindRedundentConnections {\n    fn $1(edges: Array<Vec<i32>>): Vec<i32> {\n        val parent = Vec<i32>(1001) { it }\n\n        fn $1(x -> i32) -> i32 {\n            if (parent[x] != x) parent[x] = find(parent[x])\n            return parent[x]\n        }\n\n        fn $1(x -> i32, y -> i32) -> bool {\n            val rootX = find(x)\n            val rootY = find(y)\n            if (rootX == rootY) return false\n            parent[rootY] = rootX\n            return true\n        }\n\n        for ((u, v) in edges) {\n            if (!union(u, v)) return intArrayOf(u, v)\n        }\n        return intArrayOf()\n    }\n\n}"  cpp="class FindRedundentConnections {\n    $1(edges: Array<vector<int>>): vector<int> {\n        auto $1 = vector<int>(1001) { it }\n\n        $1(x) {\n            if (parent[x] != x) parent[x] = find(parent[x])\n            return parent[x]\n        }\n\n        $1(x, y) {\n            auto $1 = find(x)\n            auto $1 = find(y)\n            if (rootX == rootY) return false\n            parent[rootY] = rootX\n            return true\n        }\n\n        for ((u, v) in edges) {\n            if (!union(u, v)) return intArrayOf(u, v)\n        }\n        return intArrayOf()\n    }\n\n}" %}

---

## PrimsAlgorithm

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/mst/PrimsAlgorithm.kt`

{% include code-tabs.html  kotlin="package graph.mst\n\nimport java.util.PriorityQueue\n\ndata class Node(val dest: Int, val weight: Int, val src: Int? = null)\n\nclass Graph {\n    private val graph = mutableMapOf<Int, MutableList<Node>>()\n\n    fun addEdge(source: Int, dest: Int, weight: Int) {\n        graph.computeIfAbsent(source) { mutableListOf() }.add(Node(dest, weight))\n        graph.computeIfAbsent(dest) { mutableListOf() }.add(Node(source, weight)) // For undirected graph\n    }\n\n    fun primsAlgorithm(startVertex: Int): Int {\n        val minHeap = PriorityQueue<Node>(compareBy { it.weight })\n        val inMST = mutableSetOf<Int>()\n        var totalWeight = 0\n\n        // Start by adding a pseudo-edge for the start vertex with zero weight\n        minHeap.add(Node(startVertex, 0))\n\n        while (minHeap.isNotEmpty()) {\n            val current = minHeap.poll()\n\n            // If current destination is already included in MST, skip it\n            if (current.dest in inMST) continue\n\n            // Add current vertex to MST\n            inMST.add(current.dest)\n            totalWeight += current.weight\n\n            // Add all adjacent edges to the heap if their destinations are not yet in MST\n            graph[current.dest]?.forEach {\n                if (it.dest !in inMST) {\n                    minHeap.add(it)\n                }\n            }\n        }\n\n        // Ensure all vertices are included in the MST to confirm the graph is connected\n        if (inMST.size != graph.size) {\n            println(\"The graph is not connected, and thus, a spanning tree cannot be formed.\")\n            return -1\n        }\n\n        return totalWeight\n    }\n}\n\nfun main() {\n    val graph = Graph()\n    graph.addEdge(0, 1, 10)\n    graph.addEdge(0, 2, 6)\n    graph.addEdge(0, 3, 5)\n    graph.addEdge(1, 3, 15)\n    graph.addEdge(2, 3, 4)\n\n    val totalWeight = graph.primsAlgorithm(0)\n    if (totalWeight != -1) {\n        println(\"Total weight of the minimum spanning tree: $totalWeight\")\n    }\n}"  java="data class Node(val dest, val weight, val src? = null)\n\nclass Graph {\n    private val graph = mutableMapOf<Int, MutableList<Node>>()\n\n    public static void $1(source, dest, weight) {\n        graph.computeIfAbsent(source) { mutableListOf() }.add(Node(dest, weight))\n        graph.computeIfAbsent(dest) { mutableListOf() }.add(Node(source, weight)) // For undirected graph\n    }\n\n    public static void $1(startVertex) {\n        val minHeap = PriorityQueue<Node>(compareBy { it.weight })\n        val inMST = mutableSetOf<Int>()\n        var totalWeight = 0\n\n        // Start by adding a pseudo-edge for the start vertex with zero weight\n        minHeap.add(Node(startVertex, 0))\n\n        while (minHeap.isNotEmpty()) {\n            val current = minHeap.poll()\n\n            // If current destination is already included in MST, skip it\n            if (current.dest in inMST) continue\n\n            // Add current vertex to MST\n            inMST.add(current.dest)\n            totalWeight += current.weight\n\n            // Add all adjacent edges to the heap if their destinations are not yet in MST\n            graph[current.dest]?.forEach {\n                if (it.dest !in inMST) {\n                    minHeap.add(it)\n                }\n            }\n        }\n\n        // Ensure all vertices are included in the MST to confirm the graph is connected\n        if (inMST.size != graph.size) {\n            println(\"The graph is not connected, and thus, a spanning tree cannot be formed.\")\n            return -1\n        }\n\n        return totalWeight\n    }\n}\n\npublic static void $1() {\n    val graph = Graph()\n    graph.addEdge(0, 1, 10)\n    graph.addEdge(0, 2, 6)\n    graph.addEdge(0, 3, 5)\n    graph.addEdge(1, 3, 15)\n    graph.addEdge(2, 3, 4)\n\n    val totalWeight = graph.primsAlgorithm(0)\n    if (totalWeight != -1) {\n        println(\"Total weight of the minimum spanning tree: $totalWeight\")\n    }\n}"  python="data class Node(val dest: int, val weight: int, val src: int? = None)\n\nclass Graph {\n    private $1 = mutableMapOf<Int, MutableList<Node>>()\n\n    def $1(source: int, dest: int, weight: int) {\n        graph.computeIfAbsent(source) { mutableListOf() }.add(Node(dest, weight))\n        graph.computeIfAbsent(dest) { mutableListOf() }.add(Node(source, weight)) // For undirected graph\n    }\n\n    def $1(startVertex: int): int {\n        $1 = PriorityQueue<Node>(compareBy { it.weight })\n        $1 = mutableSetOf<Int>()\n        $1 = 0\n\n        // Start by adding a pseudo-edge for the start vertex with zero weight\n        minHeap.add(Node(startVertex, 0))\n\n        while (minHeap.isNotEmpty()) {\n            $1 = minHeap.poll()\n\n            // If current destination is already included in MST, skip it\n            if (current.dest in inMST) continue\n\n            // Add current vertex to MST\n            inMST.add(current.dest)\n            totalWeight += current.weight\n\n            // Add all adjacent edges to the heap if their destinations are not yet in MST\n            graph[current.dest]?.forEach {\n                if (it.dest !in inMST) {\n                    minHeap.add(it)\n                }\n            }\n        }\n\n        // Ensure all vertices are included in the MST to confirm the graph is connected\n        if (inMST.size != graph.size) {\n            print(\"The graph is not connected, and thus, a spanning tree cannot be formed.\")\n            return -1\n        }\n\n        return totalWeight\n    }\n}\n\ndef $1() {\n    $1 = Graph()\n    graph.addEdge(0, 1, 10)\n    graph.addEdge(0, 2, 6)\n    graph.addEdge(0, 3, 5)\n    graph.addEdge(1, 3, 15)\n    graph.addEdge(2, 3, 4)\n\n    $1 = graph.primsAlgorithm(0)\n    if (totalWeight != -1) {\n        print(\"Total weight of the minimum spanning tree: $totalWeight\")\n    }\n}"  rust="data class Node(val dest -> i32, val weight -> i32, val src -> i32? = None)\n\nclass Graph {\n    private val graph = mutableMapOf<Int, MutableList<Node>>()\n\n    fn $1(source -> i32, dest -> i32, weight -> i32) {\n        graph.computeIfAbsent(source) { mutableListOf() }.add(Node(dest, weight))\n        graph.computeIfAbsent(dest) { mutableListOf() }.add(Node(source, weight)) // For undirected graph\n    }\n\n    fn $1(startVertex -> i32) -> i32 {\n        val minHeap = PriorityQueue<Node>(compareBy { it.weight })\n        val inMST = mutableSetOf<Int>()\n        var totalWeight = 0\n\n        // Start by adding a pseudo-edge for the start vertex with zero weight\n        minHeap.add(Node(startVertex, 0))\n\n        while (minHeap.isNotEmpty()) {\n            val current = minHeap.poll()\n\n            // If current destination is already included in MST, skip it\n            if (current.dest in inMST) continue\n\n            // Add current vertex to MST\n            inMST.add(current.dest)\n            totalWeight += current.weight\n\n            // Add all adjacent edges to the heap if their destinations are not yet in MST\n            graph[current.dest]?.forEach {\n                if (it.dest !in inMST) {\n                    minHeap.add(it)\n                }\n            }\n        }\n\n        // Ensure all vertices are included in the MST to confirm the graph is connected\n        if (inMST.size != graph.size) {\n            println!(\"The graph is not connected, and thus, a spanning tree cannot be formed.\")\n            return -1\n        }\n\n        return totalWeight\n    }\n}\n\nfn $1() {\n    val graph = Graph()\n    graph.addEdge(0, 1, 10)\n    graph.addEdge(0, 2, 6)\n    graph.addEdge(0, 3, 5)\n    graph.addEdge(1, 3, 15)\n    graph.addEdge(2, 3, 4)\n\n    val totalWeight = graph.primsAlgorithm(0)\n    if (totalWeight != -1) {\n        println!(\"Total weight of the minimum spanning tree: $totalWeight\")\n    }\n}"  cpp="data class Node(val dest, val weight, val src? = nullptr)\n\nclass Graph {\n    private auto $1 = mutableMapOf<Int, MutableList<Node>>()\n\n    $1(source, dest, weight) {\n        graph.computeIfAbsent(source) { mutableListOf() }.add(Node(dest, weight))\n        graph.computeIfAbsent(dest) { mutableListOf() }.add(Node(source, weight)) // For undirected graph\n    }\n\n    $1(startVertex) {\n        auto $1 = PriorityQueue<Node>(compareBy { it.weight })\n        auto $1 = mutableSetOf<Int>()\n        auto $1 = 0\n\n        // Start by adding a pseudo-edge for the start vertex with zero weight\n        minHeap.add(Node(startVertex, 0))\n\n        while (minHeap.isNotEmpty()) {\n            auto $1 = minHeap.poll()\n\n            // If current destination is already included in MST, skip it\n            if (current.dest in inMST) continue\n\n            // Add current vertex to MST\n            inMST.add(current.dest)\n            totalWeight += current.weight\n\n            // Add all adjacent edges to the heap if their destinations are not yet in MST\n            graph[current.dest]?.forEach {\n                if (it.dest !in inMST) {\n                    minHeap.add(it)\n                }\n            }\n        }\n\n        // Ensure all vertices are included in the MST to confirm the graph is connected\n        if (inMST.size != graph.size) {\n            cout <<(\"The graph is not connected, and thus, a spanning tree cannot be formed.\")\n            return -1\n        }\n\n        return totalWeight\n    }\n}\n\n$1() {\n    auto $1 = Graph()\n    graph.addEdge(0, 1, 10)\n    graph.addEdge(0, 2, 6)\n    graph.addEdge(0, 3, 5)\n    graph.addEdge(1, 3, 15)\n    graph.addEdge(2, 3, 4)\n\n    auto $1 = graph.primsAlgorithm(0)\n    if (totalWeight != -1) {\n        cout <<(\"Total weight of the minimum spanning tree: $totalWeight\")\n    }\n}" %}

---

## CriticalConnectionsInANetwork

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/articulation_point/CriticalConnectionsInANetwork.kt`

{% include code-tabs.html  kotlin="package graph.articulation_point\n\nclass CriticalConnectionsInANetwork {\n    private lateinit var G: Array<MutableList<Int>>\n    private lateinit var result: MutableList<List<Int>>\n    private var depth = 0\n    private lateinit var label: IntArray\n    private lateinit var low: IntArray\n    private lateinit var visited: BooleanArray\n\n    fun criticalConnections(n: Int, connections: List<List<Int>>): List<List<Int>> {\n        G = Array(n) { mutableListOf() }\n        result = mutableListOf()\n        depth = 0\n        label = IntArray(n)\n        low = IntArray(n)\n        visited = BooleanArray(n)\n\n        for (i in 0 until n) {\n            G[i] = mutableListOf()\n        }\n\n        for (edge in connections) {\n            val i = edge[0]\n            val j = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1)\n\n        return result\n    }\n\n    private fun dfs(node: Int, parent: Int) {\n        visited[node] = true\n        label[node] = depth\n        low[node] = depth\n        depth++\n\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node)\n\n                if (label[node] < low[neighbour]) {\n                    result.add(listOf(node, neighbour))\n                }\n            }\n\n            low[node] = minOf(low[node], low[neighbour])\n        }\n    }\n}"  java="class CriticalConnectionsInANetwork {\n    private lateinit var G: Array<MutableList<Int>>\n    private lateinit var result: MutableList<List<Int>>\n    private var depth = 0\n    private lateinit var label: IntArray\n    private lateinit var low: IntArray\n    private lateinit var visited: BooleanArray\n\n    public static void $1(n, connections: List<List<Int>>): List<List<Int>> {\n        G = Array(n) { mutableListOf() }\n        result = mutableListOf()\n        depth = 0\n        label = IntArray(n)\n        low = IntArray(n)\n        visited = BooleanArray(n)\n\n        for (i in 0 until n) {\n            G[i] = mutableListOf()\n        }\n\n        for (edge in connections) {\n            val i = edge[0]\n            val j = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1)\n\n        return result\n    }\n\n    private public static void $1(node, parent) {\n        visited[node] = true\n        label[node] = depth\n        low[node] = depth\n        depth++\n\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node)\n\n                if (label[node] < low[neighbour]) {\n                    result.add(listOf(node, neighbour))\n                }\n            }\n\n            low[node] = minOf(low[node], low[neighbour])\n        }\n    }\n}"  python="class CriticalConnectionsInANetwork {\n    private lateinit var G: Array<MutableList<Int>>\n    private lateinit var result: MutableList<List<Int>>\n    private $1 = 0\n    private lateinit var label: list[int]\n    private lateinit var low: list[int]\n    private lateinit var visited: BooleanArray\n\n    def $1(n: int, connections: List<List<Int>>): List<List<Int>> {\n        G = Array(n) { mutableListOf() }\n        result = mutableListOf()\n        depth = 0\n        label = list[int](n)\n        low = list[int](n)\n        visited = BooleanArray(n)\n\n        for (i in 0 until n) {\n            G[i] = mutableListOf()\n        }\n\n        for (edge in connections) {\n            $1 = edge[0]\n            $1 = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1)\n\n        return result\n    }\n\n    private def $1(node: int, parent: int) {\n        visited[node] = True\n        label[node] = depth\n        low[node] = depth\n        depth++\n\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node)\n\n                if (label[node] < low[neighbour]) {\n                    result.add(listOf(node, neighbour))\n                }\n            }\n\n            low[node] = min(low[node], low[neighbour])\n        }\n    }\n}"  rust="class CriticalConnectionsInANetwork {\n    private lateinit var G: Array<MutableList<Int>>\n    private lateinit var result: MutableList<List<Int>>\n    private var depth = 0\n    private lateinit var label: Vec<i32>\n    private lateinit var low: Vec<i32>\n    private lateinit var visited: BooleanArray\n\n    fn $1(n -> i32, connections: List<List<Int>>): List<List<Int>> {\n        G = Array(n) { mutableListOf() }\n        result = mutableListOf()\n        depth = 0\n        label = Vec<i32>(n)\n        low = Vec<i32>(n)\n        visited = BooleanArray(n)\n\n        for (i in 0 until n) {\n            G[i] = mutableListOf()\n        }\n\n        for (edge in connections) {\n            val i = edge[0]\n            val j = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1)\n\n        return result\n    }\n\n    private fn $1(node -> i32, parent -> i32) {\n        visited[node] = true\n        label[node] = depth\n        low[node] = depth\n        depth++\n\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node)\n\n                if (label[node] < low[neighbour]) {\n                    result.add(listOf(node, neighbour))\n                }\n            }\n\n            low[node] = min(low[node], low[neighbour])\n        }\n    }\n}"  cpp="class CriticalConnectionsInANetwork {\n    private lateinit var G: Array<MutableList<Int>>\n    private lateinit var result: MutableList<List<Int>>\n    private auto $1 = 0\n    private lateinit var label: vector<int>\n    private lateinit var low: vector<int>\n    private lateinit var visited: BooleanArray\n\n    $1(n, connections: List<List<Int>>): List<List<Int>> {\n        G = Array(n) { mutableListOf() }\n        result = mutableListOf()\n        depth = 0\n        label = vector<int>(n)\n        low = vector<int>(n)\n        visited = BooleanArray(n)\n\n        for (i in 0 until n) {\n            G[i] = mutableListOf()\n        }\n\n        for (edge in connections) {\n            auto $1 = edge[0]\n            auto $1 = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1)\n\n        return result\n    }\n\n    private $1(node, parent) {\n        visited[node] = true\n        label[node] = depth\n        low[node] = depth\n        depth++\n\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node)\n\n                if (label[node] < low[neighbour]) {\n                    result.add(listOf(node, neighbour))\n                }\n            }\n\n            low[node] = min(low[node], low[neighbour])\n        }\n    }\n}" %}

---

## CriticalConnectionsInANetworkShortCode

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/articulation_point/CriticalConnectionsInANetworkShortCode.kt`

{% include code-tabs.html  kotlin="package graph.articulation_point\n\nclass CriticalConnectionsInANetworkShortCode {\n    fun criticalConnections(n: Int, connections: List<List<Int>>): List<List<Int>> {\n        val graph = Array(n) { mutableListOf<Int>() }.apply {\n            connections.forEach { (u, v) -> this[u].add(v); this[v].add(u) }\n        }\n        val result = mutableListOf<List<Int>>()\n        val labels = IntArray(n) { -1 }\n        val low = IntArray(n)\n        var time = 0\n\n        fun dfs(node: Int, parent: Int) {\n            labels[node] = time.also { low[node] = it; time++ }\n            graph[node].forEach { neighbour ->\n                if (neighbour != parent) {\n                    if (labels[neighbour] == -1) {\n                        dfs(neighbour, node)\n                        if (low[neighbour] > labels[node]) result.add(listOf(node, neighbour))\n                    }\n                    low[node] = minOf(low[node], low[neighbour])\n                }\n            }\n        }\n\n        dfs(0, -1)\n        return result\n    }\n}"  java="class CriticalConnectionsInANetworkShortCode {\n    public static void $1(n, connections: List<List<Int>>): List<List<Int>> {\n        val graph = Array(n) { mutableListOf<Int>() }.apply {\n            connections.forEach { (u, v) -> this[u].add(v); this[v].add(u) }\n        }\n        val result = mutableListOf<List<Int>>()\n        val labels = IntArray(n) { -1 }\n        val low = IntArray(n)\n        var time = 0\n\n        public static void $1(node, parent) {\n            labels[node] = time.also { low[node] = it; time++ }\n            graph[node].forEach { neighbour ->\n                if (neighbour != parent) {\n                    if (labels[neighbour] == -1) {\n                        dfs(neighbour, node)\n                        if (low[neighbour] > labels[node]) result.add(listOf(node, neighbour))\n                    }\n                    low[node] = minOf(low[node], low[neighbour])\n                }\n            }\n        }\n\n        dfs(0, -1)\n        return result\n    }\n}"  python="class CriticalConnectionsInANetworkShortCode {\n    def $1(n: int, connections: List<List<Int>>): List<List<Int>> {\n        $1 = Array(n) { mutableListOf<Int>() }.apply {\n            connections.forEach { (u, v) -> this[u].add(v); this[v].add(u) }\n        }\n        $1 = mutableListOf<List<Int>>()\n        $1 = list[int](n) { -1 }\n        $1 = list[int](n)\n        $1 = 0\n\n        def $1(node: int, parent: int) {\n            labels[node] = time.also { low[node] = it; time++ }\n            graph[node].forEach { neighbour ->\n                if (neighbour != parent) {\n                    if (labels[neighbour] == -1) {\n                        dfs(neighbour, node)\n                        if (low[neighbour] > labels[node]) result.add(listOf(node, neighbour))\n                    }\n                    low[node] = min(low[node], low[neighbour])\n                }\n            }\n        }\n\n        dfs(0, -1)\n        return result\n    }\n}"  rust="class CriticalConnectionsInANetworkShortCode {\n    fn $1(n -> i32, connections: List<List<Int>>): List<List<Int>> {\n        val graph = Array(n) { mutableListOf<Int>() }.apply {\n            connections.forEach { (u, v) -> this[u].add(v); this[v].add(u) }\n        }\n        val result = mutableListOf<List<Int>>()\n        val labels = Vec<i32>(n) { -1 }\n        val low = Vec<i32>(n)\n        var time = 0\n\n        fn $1(node -> i32, parent -> i32) {\n            labels[node] = time.also { low[node] = it; time++ }\n            graph[node].forEach { neighbour ->\n                if (neighbour != parent) {\n                    if (labels[neighbour] == -1) {\n                        dfs(neighbour, node)\n                        if (low[neighbour] > labels[node]) result.add(listOf(node, neighbour))\n                    }\n                    low[node] = min(low[node], low[neighbour])\n                }\n            }\n        }\n\n        dfs(0, -1)\n        return result\n    }\n}"  cpp="class CriticalConnectionsInANetworkShortCode {\n    $1(n, connections: List<List<Int>>): List<List<Int>> {\n        auto $1 = Array(n) { mutableListOf<Int>() }.apply {\n            connections.forEach { (u, v) -> this[u].add(v); this[v].add(u) }\n        }\n        auto $1 = mutableListOf<List<Int>>()\n        auto $1 = vector<int>(n) { -1 }\n        auto $1 = vector<int>(n)\n        auto $1 = 0\n\n        $1(node, parent) {\n            labels[node] = time.also { low[node] = it; time++ }\n            graph[node].forEach { neighbour ->\n                if (neighbour != parent) {\n                    if (labels[neighbour] == -1) {\n                        dfs(neighbour, node)\n                        if (low[neighbour] > labels[node]) result.add(listOf(node, neighbour))\n                    }\n                    low[node] = min(low[node], low[neighbour])\n                }\n            }\n        }\n\n        dfs(0, -1)\n        return result\n    }\n}" %}

---

## FindArticulationPoints

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/articulation_point/FindArticulationPoints.kt`

{% include code-tabs.html  kotlin="package graph.articulation_point\n\nclass FindArticulationPoints {\n    private lateinit var G: Array<MutableList<Int>>\n    private var time = 0 // Time counter for DFS\n    private lateinit var label: IntArray\n    private lateinit var low: IntArray\n    private lateinit var visited: BooleanArray\n\n    fun findCutVertices(n: Int, connections: List<List<Int>>): Set<Int> {\n        G = Array(n) { mutableListOf() }\n        time = 0\n        label = IntArray(n)\n        low = IntArray(n)\n        visited = BooleanArray(n)\n        val cutVertices = mutableSetOf<Int>()\n\n        for (edge in connections) {\n            val i = edge[0]\n            val j = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1, cutVertices);\n\n        return cutVertices\n    }\n\n    private fun dfs(node: Int, parent: Int, cutVertices: MutableSet<Int>) {\n        visited[node] = true\n        label[node] = time++\n        low[node] = label[node]\n\n        var childrenCount = 0\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node, cutVertices)\n                childrenCount++\n\n                // If node is not root and has a child with low link >= label[node], it's a cut vertex\n                if (parent == -1 && childrenCount > 1 || low[neighbour] >= label[node] && parent != -1) {\n                    cutVertices.add(node)\n                }\n            }\n\n            low[node] = Math.min(low[node], label[neighbour]) // Update low link for back edges\n        }\n    }\n}"  java="class FindArticulationPoints {\n    private lateinit var G: Array<MutableList<Int>>\n    private var time = 0 // Time counter for DFS\n    private lateinit var label: IntArray\n    private lateinit var low: IntArray\n    private lateinit var visited: BooleanArray\n\n    public static void $1(n, connections: List<List<Int>>): Set<Int> {\n        G = Array(n) { mutableListOf() }\n        time = 0\n        label = IntArray(n)\n        low = IntArray(n)\n        visited = BooleanArray(n)\n        val cutVertices = mutableSetOf<Int>()\n\n        for (edge in connections) {\n            val i = edge[0]\n            val j = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1, cutVertices);\n\n        return cutVertices\n    }\n\n    private public static void $1(node, parent, cutVertices: MutableSet<Int>) {\n        visited[node] = true\n        label[node] = time++\n        low[node] = label[node]\n\n        var childrenCount = 0\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node, cutVertices)\n                childrenCount++\n\n                // If node is not root and has a child with low link >= label[node], it's a cut vertex\n                if (parent == -1 && childrenCount > 1 || low[neighbour] >= label[node] && parent != -1) {\n                    cutVertices.add(node)\n                }\n            }\n\n            low[node] = Math.min(low[node], label[neighbour]) // Update low link for back edges\n        }\n    }\n}"  python="class FindArticulationPoints {\n    private lateinit var G: Array<MutableList<Int>>\n    private $1 = 0 // Time counter for DFS\n    private lateinit var label: list[int]\n    private lateinit var low: list[int]\n    private lateinit var visited: BooleanArray\n\n    def $1(n: int, connections: List<List<Int>>): Set<Int> {\n        G = Array(n) { mutableListOf() }\n        time = 0\n        label = list[int](n)\n        low = list[int](n)\n        visited = BooleanArray(n)\n        $1 = mutableSetOf<Int>()\n\n        for (edge in connections) {\n            $1 = edge[0]\n            $1 = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1, cutVertices);\n\n        return cutVertices\n    }\n\n    private def $1(node: int, parent: int, cutVertices: MutableSet<Int>) {\n        visited[node] = True\n        label[node] = time++\n        low[node] = label[node]\n\n        $1 = 0\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node, cutVertices)\n                childrenCount++\n\n                // If node is not root and has a child with low link >= label[node], it's a cut vertex\n                if (parent == -1 && childrenCount > 1 || low[neighbour] >= label[node] && parent != -1) {\n                    cutVertices.add(node)\n                }\n            }\n\n            low[node] = Math.min(low[node], label[neighbour]) // Update low link for back edges\n        }\n    }\n}"  rust="class FindArticulationPoints {\n    private lateinit var G: Array<MutableList<Int>>\n    private var time = 0 // Time counter for DFS\n    private lateinit var label: Vec<i32>\n    private lateinit var low: Vec<i32>\n    private lateinit var visited: BooleanArray\n\n    fn $1(n -> i32, connections: List<List<Int>>): Set<Int> {\n        G = Array(n) { mutableListOf() }\n        time = 0\n        label = Vec<i32>(n)\n        low = Vec<i32>(n)\n        visited = BooleanArray(n)\n        val cutVertices = mutableSetOf<Int>()\n\n        for (edge in connections) {\n            val i = edge[0]\n            val j = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1, cutVertices);\n\n        return cutVertices\n    }\n\n    private fn $1(node -> i32, parent -> i32, cutVertices: MutableSet<Int>) {\n        visited[node] = true\n        label[node] = time++\n        low[node] = label[node]\n\n        var childrenCount = 0\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node, cutVertices)\n                childrenCount++\n\n                // If node is not root and has a child with low link >= label[node], it's a cut vertex\n                if (parent == -1 && childrenCount > 1 || low[neighbour] >= label[node] && parent != -1) {\n                    cutVertices.add(node)\n                }\n            }\n\n            low[node] = Math.min(low[node], label[neighbour]) // Update low link for back edges\n        }\n    }\n}"  cpp="class FindArticulationPoints {\n    private lateinit var G: Array<MutableList<Int>>\n    private auto $1 = 0 // Time counter for DFS\n    private lateinit var label: vector<int>\n    private lateinit var low: vector<int>\n    private lateinit var visited: BooleanArray\n\n    $1(n, connections: List<List<Int>>): Set<Int> {\n        G = Array(n) { mutableListOf() }\n        time = 0\n        label = vector<int>(n)\n        low = vector<int>(n)\n        visited = BooleanArray(n)\n        auto $1 = mutableSetOf<Int>()\n\n        for (edge in connections) {\n            auto $1 = edge[0]\n            auto $1 = edge[1]\n            G[i].add(j)\n            G[j].add(i)\n        }\n\n        dfs(0, -1, cutVertices);\n\n        return cutVertices\n    }\n\n    private $1(node, parent, cutVertices: MutableSet<Int>) {\n        visited[node] = true\n        label[node] = time++\n        low[node] = label[node]\n\n        auto $1 = 0\n        for (neighbour in G[node]) {\n            if (neighbour == parent) continue\n\n            if (!visited[neighbour]) {\n                dfs(neighbour, node, cutVertices)\n                childrenCount++\n\n                // If node is not root and has a child with low link >= label[node], it's a cut vertex\n                if (parent == -1 && childrenCount > 1 || low[neighbour] >= label[node] && parent != -1) {\n                    cutVertices.add(node)\n                }\n            }\n\n            low[node] = Math.min(low[node], label[neighbour]) // Update low link for back edges\n        }\n    }\n}" %}

---

## CourseSchedule

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/cycle/CourseSchedule.kt`

{% include code-tabs.html  kotlin="package graph.cycle\n\nclass CourseSchedule {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    private fun hasCycle(numCourses: Int): Boolean {\n        // Define dfs function inside hasCycle to limit its scope\n        fun dfs(node: Int): Boolean = when (status[node]) {\n            NodeStatus.EXPLORING -> true\n            NodeStatus.DONE -> false\n            NodeStatus.UNVISITED -> {\n                status[node] = NodeStatus.EXPLORING\n                graph[node]?.forEach {\n                    if (dfs(it)) return true\n                }\n                status[node] = NodeStatus.DONE\n                false\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED && dfs(i)) {\n                return true\n            }\n        }\n        return false\n    }\n\n    fun canFinish(numCourses: Int, prerequisites: Array<IntArray>): Boolean {\n        // Reset graph for each invocation to handle multiple calls\n        graph = mutableMapOf()\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n\n        return !hasCycle(numCourses)\n    }\n}"  java="class CourseSchedule {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    private public static void $1(numCourses) {\n        // Define dfs function inside hasCycle to limit its scope\n        public static void $1(node) = when (status[node]) {\n            NodeStatus.EXPLORING -> true\n            NodeStatus.DONE -> false\n            NodeStatus.UNVISITED -> {\n                status[node] = NodeStatus.EXPLORING\n                graph[node]?.forEach {\n                    if (dfs(it)) return true\n                }\n                status[node] = NodeStatus.DONE\n                false\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED && dfs(i)) {\n                return true\n            }\n        }\n        return false\n    }\n\n    public static void $1(numCourses, prerequisites: Array<IntArray>) {\n        // Reset graph for each invocation to handle multiple calls\n        graph = mutableMapOf()\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n\n        return !hasCycle(numCourses)\n    }\n}"  python="class CourseSchedule {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    private def $1(numCourses: int): bool {\n        // Define dfs function inside hasCycle to limit its scope\n        def $1(node: int): bool = when (status[node]) {\n            NodeStatus.EXPLORING -> True\n            NodeStatus.DONE -> False\n            NodeStatus.UNVISITED -> {\n                status[node] = NodeStatus.EXPLORING\n                graph[node]?.forEach {\n                    if (dfs(it)) return True\n                }\n                status[node] = NodeStatus.DONE\n                False\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED && dfs(i)) {\n                return True\n            }\n        }\n        return False\n    }\n\n    def $1(numCourses: int, prerequisites: Array<list[int]>): bool {\n        // Reset graph for each invocation to handle multiple calls\n        graph = mutableMapOf()\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n\n        return !hasCycle(numCourses)\n    }\n}"  rust="class CourseSchedule {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    private fn $1(numCourses -> i32) -> bool {\n        // Define dfs function inside hasCycle to limit its scope\n        fn $1(node -> i32) -> bool = when (status[node]) {\n            NodeStatus.EXPLORING -> true\n            NodeStatus.DONE -> false\n            NodeStatus.UNVISITED -> {\n                status[node] = NodeStatus.EXPLORING\n                graph[node]?.forEach {\n                    if (dfs(it)) return true\n                }\n                status[node] = NodeStatus.DONE\n                false\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED && dfs(i)) {\n                return true\n            }\n        }\n        return false\n    }\n\n    fn $1(numCourses -> i32, prerequisites: Array<Vec<i32>>) -> bool {\n        // Reset graph for each invocation to handle multiple calls\n        graph = mutableMapOf()\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n\n        return !hasCycle(numCourses)\n    }\n}"  cpp="class CourseSchedule {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    private $1(numCourses) {\n        // Define dfs function inside hasCycle to limit its scope\n        $1(node) = when (status[node]) {\n            NodeStatus.EXPLORING -> true\n            NodeStatus.DONE -> false\n            NodeStatus.UNVISITED -> {\n                status[node] = NodeStatus.EXPLORING\n                graph[node]?.forEach {\n                    if (dfs(it)) return true\n                }\n                status[node] = NodeStatus.DONE\n                false\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED && dfs(i)) {\n                return true\n            }\n        }\n        return false\n    }\n\n    $1(numCourses, prerequisites: Array<vector<int>>) {\n        // Reset graph for each invocation to handle multiple calls\n        graph = mutableMapOf()\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n\n        return !hasCycle(numCourses)\n    }\n}" %}

---

## CourseSchedule_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/cycle/CourseSchedule_II.kt`

{% include code-tabs.html  kotlin="package graph.topological_sort\n\nclass CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: IntArray\n    private var index: Int = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    fun findOrder(numCourses: Int, prerequisites: Array<IntArray>): IntArray {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = IntArray(numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private fun hasCycle(numCourses: Int): Boolean {\n        // Nested DFS function for checking cycles and building the result\n        fun dfs(node: Int): Boolean {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return true\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return false\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return true\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return false\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return true\n                }\n            }\n        }\n        return false\n    }\n}"  java="class CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: IntArray\n    private var index = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    public static void $1(numCourses, prerequisites: Array<IntArray>): IntArray {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = IntArray(numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private public static void $1(numCourses) {\n        // Nested DFS function for checking cycles and building the result\n        public static void $1(node) {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return true\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return false\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return true\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return false\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return true\n                }\n            }\n        }\n        return false\n    }\n}"  python="class CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: list[int]\n    private var index: int = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    def $1(numCourses: int, prerequisites: Array<list[int]>): list[int] {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = list[int](numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private def $1(numCourses: int): bool {\n        // Nested DFS function for checking cycles and building the result\n        def $1(node: int): bool {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return True\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return False\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return True\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return False\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return True\n                }\n            }\n        }\n        return False\n    }\n}"  rust="class CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: Vec<i32>\n    private var index -> i32 = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    fn $1(numCourses -> i32, prerequisites: Array<Vec<i32>>): Vec<i32> {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = Vec<i32>(numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private fn $1(numCourses -> i32) -> bool {\n        // Nested DFS function for checking cycles and building the result\n        fn $1(node -> i32) -> bool {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return true\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return false\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return true\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return false\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return true\n                }\n            }\n        }\n        return false\n    }\n}"  cpp="class CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: vector<int>\n    private auto $1 = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    $1(numCourses, prerequisites: Array<vector<int>>): vector<int> {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = vector<int>(numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private $1(numCourses) {\n        // Nested DFS function for checking cycles and building the result\n        $1(node) {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return true\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return false\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return true\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return false\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return true\n                }\n            }\n        }\n        return false\n    }\n}" %}

---

## ParallelCourses

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/cycle/ParallelCourses.kt`

{% include code-tabs.html  kotlin="package graph.cycle\n\nclass ParallelCourses {\n    fun minimumSemesters(n: Int, relations: Array<IntArray>): Int {\n        val inDegree = IntArray(n + 1) // One-based index\n        val graph = Array(n + 1) { mutableListOf<Int>() }\n\n        relations.forEach { (u, v) ->\n            graph[u].add(v)\n            inDegree[v]++\n        }\n\n        val queue = ArrayDeque<Int>()\n        var semesters = 0\n        var completed = 0\n\n        // Add courses with no prerequisites (in-degree 0)\n        (1..n).filter { inDegree[it] == 0 }.forEach { queue.add(it) }\n\n        while (queue.isNotEmpty()) {\n            val size = queue.size\n            semesters++\n            repeat(size) {\n                val course = queue.removeFirst()\n                completed++\n                graph[course].forEach { next ->\n                    if (--inDegree[next] == 0) queue.add(next)\n                }\n            }\n        }\n\n        return if (completed == n) semesters else -1\n    }\n}\n"  java="class ParallelCourses {\n    public static void $1(n, relations: Array<IntArray>) {\n        val inDegree = IntArray(n + 1) // One-based index\n        val graph = Array(n + 1) { mutableListOf<Int>() }\n\n        relations.forEach { (u, v) ->\n            graph[u].add(v)\n            inDegree[v]++\n        }\n\n        val queue = ArrayDeque<Int>()\n        var semesters = 0\n        var completed = 0\n\n        // Add courses with no prerequisites (in-degree 0)\n        (1..n).filter { inDegree[it] == 0 }.forEach { queue.add(it) }\n\n        while (queue.isNotEmpty()) {\n            val size = queue.size\n            semesters++\n            repeat(size) {\n                val course = queue.removeFirst()\n                completed++\n                graph[course].forEach { next ->\n                    if (--inDegree[next] == 0) queue.add(next)\n                }\n            }\n        }\n\n        return if (completed == n) semesters else -1\n    }\n}"  python="class ParallelCourses {\n    def $1(n: int, relations: Array<list[int]>): int {\n        $1 = list[int](n + 1) // One-based index\n        $1 = Array(n + 1) { mutableListOf<Int>() }\n\n        relations.forEach { (u, v) ->\n            graph[u].add(v)\n            inDegree[v]++\n        }\n\n        $1 = ArrayDeque<Int>()\n        $1 = 0\n        $1 = 0\n\n        // Add courses with no prerequisites (in-degree 0)\n        (1..n).filter { inDegree[it] == 0 }.forEach { queue.add(it) }\n\n        while (queue.isNotEmpty()) {\n            $1 = queue.size\n            semesters++\n            repeat(size) {\n                $1 = queue.removeFirst()\n                completed++\n                graph[course].forEach { next ->\n                    if (--inDegree[next] == 0) queue.add(next)\n                }\n            }\n        }\n\n        return if (completed == n) semesters else -1\n    }\n}"  rust="class ParallelCourses {\n    fn $1(n -> i32, relations: Array<Vec<i32>>) -> i32 {\n        val inDegree = Vec<i32>(n + 1) // One-based index\n        val graph = Array(n + 1) { mutableListOf<Int>() }\n\n        relations.forEach { (u, v) ->\n            graph[u].add(v)\n            inDegree[v]++\n        }\n\n        val queue = ArrayDeque<Int>()\n        var semesters = 0\n        var completed = 0\n\n        // Add courses with no prerequisites (in-degree 0)\n        (1..n).filter { inDegree[it] == 0 }.forEach { queue.add(it) }\n\n        while (queue.isNotEmpty()) {\n            val size = queue.size\n            semesters++\n            repeat(size) {\n                val course = queue.removeFirst()\n                completed++\n                graph[course].forEach { next ->\n                    if (--inDegree[next] == 0) queue.add(next)\n                }\n            }\n        }\n\n        return if (completed == n) semesters else -1\n    }\n}"  cpp="class ParallelCourses {\n    $1(n, relations: Array<vector<int>>) {\n        auto $1 = vector<int>(n + 1) // One-based index\n        auto $1 = Array(n + 1) { mutableListOf<Int>() }\n\n        relations.forEach { (u, v) ->\n            graph[u].add(v)\n            inDegree[v]++\n        }\n\n        auto $1 = ArrayDeque<Int>()\n        auto $1 = 0\n        auto $1 = 0\n\n        // Add courses with no prerequisites (in-degree 0)\n        (1..n).filter { inDegree[it] == 0 }.forEach { queue.add(it) }\n\n        while (queue.isNotEmpty()) {\n            auto $1 = queue.size\n            semesters++\n            repeat(size) {\n                auto $1 = queue.removeFirst()\n                completed++\n                graph[course].forEach { next ->\n                    if (--inDegree[next] == 0) queue.add(next)\n                }\n            }\n        }\n\n        return if (completed == n) semesters else -1\n    }\n}" %}

---

## AlienDictionary

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/topological_sort/AlienDictionary.kt`

{% include code-tabs.html  kotlin="package graph.topological_sort\n\nclass AlienDictionary {\n    enum class State {\n        NOT_VISITED, VISITING, VISITED\n    }\n\n    fun alienOrder(words: Array<String>): String {\n        val graph = mutableMapOf<Char, MutableSet<Char>>()\n        val visited = mutableMapOf<Char, State>()  // 0 = visiting, 1 = visited, -1 = not visited\n        val result = StringBuilder()\n\n        // Step 1: Build the graph and visited map\n        words.forEach { word ->\n            word.forEach { char ->\n                graph.putIfAbsent(char, mutableSetOf())\n                visited.putIfAbsent(char, State.NOT_VISITED)\n            }\n        }\n\n        // Step 2: Build edges between characters\n        for (i in 1 until words.size) {\n            val (word1, word2) = words[i - 1] to words[i]\n\n            // Check if the second word is a prefix of the first\n            if (word1.startsWith(word2) && word1.length > word2.length) {\n                return \"\"  // Invalid case, prefix conflict\n            }\n\n            for (j in 0 until minOf(word1.length, word2.length)) {\n                if (word1[j] != word2[j]) {\n                    graph[word1[j]]?.add(word2[j])\n                    break\n                }\n            }\n        }\n\n        // Step 3: Perform DFS to find topological sort\n        fun dfs(node: Char): Boolean {\n            if (visited[node] == State.VISITING) return false  // Cycle detected\n            if (visited[node] == State.VISITED) return true  // Already visited\n\n            visited[node] = State.VISITING  // Mark as visiting\n            graph[node]?.forEach { if (!dfs(it)) return false }\n\n            visited[node] = State.VISITED  // Mark as visited\n            result.append(node) // Add to result in topological order\n            return true\n        }\n\n        // Step 4: Process all nodes (characters)\n        for (char in visited.keys) {\n            if (visited[char] == State.NOT_VISITED && !dfs(char)) {\n                return \"\"  // Cycle detected\n            }\n        }\n\n        return result.reverse().toString() // Reverse to get the correct order\n    }\n}"  java="class AlienDictionary {\n    enum class State {\n        NOT_VISITED, VISITING, VISITED\n    }\n\n    public static void $1(words: Array<String>) {\n        val graph = mutableMapOf<Char, MutableSet<Char>>()\n        val visited = mutableMapOf<Char, State>()  // 0 = visiting, 1 = visited, -1 = not visited\n        val result = StringBuilder()\n\n        // Step 1: Build the graph and visited map\n        words.forEach { word ->\n            word.forEach { char ->\n                graph.putIfAbsent(char, mutableSetOf())\n                visited.putIfAbsent(char, State.NOT_VISITED)\n            }\n        }\n\n        // Step 2: Build edges between characters\n        for (i in 1 until words.size) {\n            val (word1, word2) = words[i - 1] to words[i]\n\n            // Check if the second word is a prefix of the first\n            if (word1.startsWith(word2) && word1.length > word2.length) {\n                return \"\"  // Invalid case, prefix conflict\n            }\n\n            for (j in 0 until minOf(word1.length, word2.length)) {\n                if (word1[j] != word2[j]) {\n                    graph[word1[j]]?.add(word2[j])\n                    break\n                }\n            }\n        }\n\n        // Step 3: Perform DFS to find topological sort\n        public static void $1(node: Char) {\n            if (visited[node] == State.VISITING) return false  // Cycle detected\n            if (visited[node] == State.VISITED) return true  // Already visited\n\n            visited[node] = State.VISITING  // Mark as visiting\n            graph[node]?.forEach { if (!dfs(it)) return false }\n\n            visited[node] = State.VISITED  // Mark as visited\n            result.append(node) // Add to result in topological order\n            return true\n        }\n\n        // Step 4: Process all nodes (characters)\n        for (char in visited.keys) {\n            if (visited[char] == State.NOT_VISITED && !dfs(char)) {\n                return \"\"  // Cycle detected\n            }\n        }\n\n        return result.reverse().toString() // Reverse to get the correct order\n    }\n}"  python="class AlienDictionary {\n    enum class State {\n        NOT_VISITED, VISITING, VISITED\n    }\n\n    def $1(words: Array<String>): str {\n        $1 = mutableMapOf<Char, MutableSet<Char>>()\n        $1 = mutableMapOf<Char, State>()  // 0 = visiting, 1 = visited, -1 = not visited\n        $1 = StringBuilder()\n\n        // Step 1: Build the graph and visited map\n        words.forEach { word ->\n            word.forEach { char ->\n                graph.putIfAbsent(char, mutableSetOf())\n                visited.putIfAbsent(char, State.NOT_VISITED)\n            }\n        }\n\n        // Step 2: Build edges between characters\n        for (i in 1 until words.size) {\n            ($1, $2) = words[i - 1] to words[i]\n\n            // Check if the second word is a prefix of the first\n            if (word1.startsWith(word2) && word1.length > word2.length) {\n                return \"\"  // Invalid case, prefix conflict\n            }\n\n            for (j in 0 until min(word1.length, word2.length)) {\n                if (word1[j] != word2[j]) {\n                    graph[word1[j]]?.add(word2[j])\n                    break\n                }\n            }\n        }\n\n        // Step 3: Perform DFS to find topological sort\n        def $1(node: Char): bool {\n            if (visited[node] == State.VISITING) return False  // Cycle detected\n            if (visited[node] == State.VISITED) return True  // Already visited\n\n            visited[node] = State.VISITING  // Mark as visiting\n            graph[node]?.forEach { if (!dfs(it)) return False }\n\n            visited[node] = State.VISITED  // Mark as visited\n            result.append(node) // Add to result in topological order\n            return True\n        }\n\n        // Step 4: Process all nodes (characters)\n        for (char in visited.keys) {\n            if (visited[char] == State.NOT_VISITED && !dfs(char)) {\n                return \"\"  // Cycle detected\n            }\n        }\n\n        return result.reverse().toString() // Reverse to get the correct order\n    }\n}"  rust="class AlienDictionary {\n    enum class State {\n        NOT_VISITED, VISITING, VISITED\n    }\n\n    fn $1(words: Array<String>) -> String {\n        val graph = mutableMapOf<Char, MutableSet<Char>>()\n        val visited = mutableMapOf<Char, State>()  // 0 = visiting, 1 = visited, -1 = not visited\n        val result = StringBuilder()\n\n        // Step 1: Build the graph and visited map\n        words.forEach { word ->\n            word.forEach { char ->\n                graph.putIfAbsent(char, mutableSetOf())\n                visited.putIfAbsent(char, State.NOT_VISITED)\n            }\n        }\n\n        // Step 2: Build edges between characters\n        for (i in 1 until words.size) {\n            val (word1, word2) = words[i - 1] to words[i]\n\n            // Check if the second word is a prefix of the first\n            if (word1.startsWith(word2) && word1.length > word2.length) {\n                return \"\"  // Invalid case, prefix conflict\n            }\n\n            for (j in 0 until min(word1.length, word2.length)) {\n                if (word1[j] != word2[j]) {\n                    graph[word1[j]]?.add(word2[j])\n                    break\n                }\n            }\n        }\n\n        // Step 3: Perform DFS to find topological sort\n        fn $1(node: Char) -> bool {\n            if (visited[node] == State.VISITING) return false  // Cycle detected\n            if (visited[node] == State.VISITED) return true  // Already visited\n\n            visited[node] = State.VISITING  // Mark as visiting\n            graph[node]?.forEach { if (!dfs(it)) return false }\n\n            visited[node] = State.VISITED  // Mark as visited\n            result.append(node) // Add to result in topological order\n            return true\n        }\n\n        // Step 4: Process all nodes (characters)\n        for (char in visited.keys) {\n            if (visited[char] == State.NOT_VISITED && !dfs(char)) {\n                return \"\"  // Cycle detected\n            }\n        }\n\n        return result.reverse().toString() // Reverse to get the correct order\n    }\n}"  cpp="class AlienDictionary {\n    enum class State {\n        NOT_VISITED, VISITING, VISITED\n    }\n\n    $1(words: Array<String>) {\n        auto $1 = mutableMapOf<Char, MutableSet<Char>>()\n        auto $1 = mutableMapOf<Char, State>()  // 0 = visiting, 1 = visited, -1 = not visited\n        auto $1 = StringBuilder()\n\n        // Step 1: Build the graph and visited map\n        words.forEach { word ->\n            word.forEach { char ->\n                graph.putIfAbsent(char, mutableSetOf())\n                visited.putIfAbsent(char, State.NOT_VISITED)\n            }\n        }\n\n        // Step 2: Build edges between characters\n        for (i in 1 until words.size) {\n            val (word1, word2) = words[i - 1] to words[i]\n\n            // Check if the second word is a prefix of the first\n            if (word1.startsWith(word2) && word1.length > word2.length) {\n                return \"\"  // Invalid case, prefix conflict\n            }\n\n            for (j in 0 until min(word1.length, word2.length)) {\n                if (word1[j] != word2[j]) {\n                    graph[word1[j]]?.add(word2[j])\n                    break\n                }\n            }\n        }\n\n        // Step 3: Perform DFS to find topological sort\n        $1(node: Char) {\n            if (visited[node] == State.VISITING) return false  // Cycle detected\n            if (visited[node] == State.VISITED) return true  // Already visited\n\n            visited[node] = State.VISITING  // Mark as visiting\n            graph[node]?.forEach { if (!dfs(it)) return false }\n\n            visited[node] = State.VISITED  // Mark as visited\n            result.append(node) // Add to result in topological order\n            return true\n        }\n\n        // Step 4: Process all nodes (characters)\n        for (char in visited.keys) {\n            if (visited[char] == State.NOT_VISITED && !dfs(char)) {\n                return \"\"  // Cycle detected\n            }\n        }\n\n        return result.reverse().toString() // Reverse to get the correct order\n    }\n}" %}

---

## AlienDictionary_BFS

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/topological_sort/AlienDictionary_BFS.kt`

{% include code-tabs.html  kotlin="package graph.topological_sort\n\nclass AlienDictionary_BFS {\n    fun alienOrder(words: Array<String>): String {\n        val graph = mutableMapOf<Char, HashSet<Char>>()\n        val inDegree = mutableMapOf<Char, Int>()\n        words.forEach { word ->\n            word.forEach { char ->\n                inDegree.putIfAbsent(char, 0)\n                graph.putIfAbsent(char, hashSetOf())\n            }\n        }\n\n        for (i in 0 until words.size - 1) {\n            val currentWord = words[i]\n            val nextWord = words[i + 1]\n            val minLength = minOf(currentWord.length, nextWord.length)\n            for (j in 0 until minLength) {\n                val currentChar = currentWord[j]\n                val nextChar = nextWord[j]\n                if (currentChar != nextChar) {\n                    if (graph[currentChar]!!.add(nextChar)) {\n                        inDegree[nextChar] = inDegree[nextChar]!! + 1\n                    }\n                    break\n                }\n                // Handle invalid case (e.g., \"abc\" before \"ab\")\n                if (j == minLength - 1 && currentWord.length > nextWord.length) return \"\"\n            }\n        }\n\n        val queue = ArrayDeque<Char>().apply { addAll(inDegree.filter { it.value == 0 }.keys) }\n        val result = StringBuilder()\n        while (queue.isNotEmpty()) {\n            val char = queue.removeFirst()\n            result.append(char)\n            graph[char]?.forEach { neighbor ->\n                inDegree[neighbor] = inDegree[neighbor]!! - 1\n                if (inDegree[neighbor] == 0) queue.add(neighbor)\n            }\n        }\n\n        return if (result.length == inDegree.size) result.toString() else \"\"\n    }\n}"  java="class AlienDictionary_BFS {\n    public static void $1(words: Array<String>) {\n        val graph = mutableMapOf<Char, HashSet<Char>>()\n        val inDegree = mutableMapOf<Char, Int>()\n        words.forEach { word ->\n            word.forEach { char ->\n                inDegree.putIfAbsent(char, 0)\n                graph.putIfAbsent(char, hashSetOf())\n            }\n        }\n\n        for (i in 0 until words.size - 1) {\n            val currentWord = words[i]\n            val nextWord = words[i + 1]\n            val minLength = minOf(currentWord.length, nextWord.length)\n            for (j in 0 until minLength) {\n                val currentChar = currentWord[j]\n                val nextChar = nextWord[j]\n                if (currentChar != nextChar) {\n                    if (graph[currentChar]!!.add(nextChar)) {\n                        inDegree[nextChar] = inDegree[nextChar]!! + 1\n                    }\n                    break\n                }\n                // Handle invalid case (e.g., \"abc\" before \"ab\")\n                if (j == minLength - 1 && currentWord.length > nextWord.length) return \"\"\n            }\n        }\n\n        val queue = ArrayDeque<Char>().apply { addAll(inDegree.filter { it.value == 0 }.keys) }\n        val result = StringBuilder()\n        while (queue.isNotEmpty()) {\n            val char = queue.removeFirst()\n            result.append(char)\n            graph[char]?.forEach { neighbor ->\n                inDegree[neighbor] = inDegree[neighbor]!! - 1\n                if (inDegree[neighbor] == 0) queue.add(neighbor)\n            }\n        }\n\n        return if (result.length == inDegree.size) result.toString() else \"\"\n    }\n}"  python="class AlienDictionary_BFS {\n    def $1(words: Array<String>): str {\n        $1 = mutableMapOf<Char, HashSet<Char>>()\n        $1 = mutableMapOf<Char, Int>()\n        words.forEach { word ->\n            word.forEach { char ->\n                inDegree.putIfAbsent(char, 0)\n                graph.putIfAbsent(char, hashSetOf())\n            }\n        }\n\n        for (i in 0 until words.size - 1) {\n            $1 = words[i]\n            $1 = words[i + 1]\n            $1 = min(currentWord.length, nextWord.length)\n            for (j in 0 until minLength) {\n                $1 = currentWord[j]\n                $1 = nextWord[j]\n                if (currentChar != nextChar) {\n                    if (graph[currentChar]!!.add(nextChar)) {\n                        inDegree[nextChar] = inDegree[nextChar]!! + 1\n                    }\n                    break\n                }\n                // Handle invalid case (e.g., \"abc\" before \"ab\")\n                if (j == minLength - 1 && currentWord.length > nextWord.length) return \"\"\n            }\n        }\n\n        $1 = ArrayDeque<Char>().apply { addAll(inDegree.filter { it.value == 0 }.keys) }\n        $1 = StringBuilder()\n        while (queue.isNotEmpty()) {\n            $1 = queue.removeFirst()\n            result.append(char)\n            graph[char]?.forEach { neighbor ->\n                inDegree[neighbor] = inDegree[neighbor]!! - 1\n                if (inDegree[neighbor] == 0) queue.add(neighbor)\n            }\n        }\n\n        return if (result.length == inDegree.size) result.toString() else \"\"\n    }\n}"  rust="class AlienDictionary_BFS {\n    fn $1(words: Array<String>) -> String {\n        val graph = mutableMapOf<Char, HashSet<Char>>()\n        val inDegree = mutableMapOf<Char, Int>()\n        words.forEach { word ->\n            word.forEach { char ->\n                inDegree.putIfAbsent(char, 0)\n                graph.putIfAbsent(char, hashSetOf())\n            }\n        }\n\n        for (i in 0 until words.size - 1) {\n            val currentWord = words[i]\n            val nextWord = words[i + 1]\n            val minLength = min(currentWord.length, nextWord.length)\n            for (j in 0 until minLength) {\n                val currentChar = currentWord[j]\n                val nextChar = nextWord[j]\n                if (currentChar != nextChar) {\n                    if (graph[currentChar]!!.add(nextChar)) {\n                        inDegree[nextChar] = inDegree[nextChar]!! + 1\n                    }\n                    break\n                }\n                // Handle invalid case (e.g., \"abc\" before \"ab\")\n                if (j == minLength - 1 && currentWord.length > nextWord.length) return \"\"\n            }\n        }\n\n        val queue = ArrayDeque<Char>().apply { addAll(inDegree.filter { it.value == 0 }.keys) }\n        val result = StringBuilder()\n        while (queue.isNotEmpty()) {\n            val char = queue.removeFirst()\n            result.append(char)\n            graph[char]?.forEach { neighbor ->\n                inDegree[neighbor] = inDegree[neighbor]!! - 1\n                if (inDegree[neighbor] == 0) queue.add(neighbor)\n            }\n        }\n\n        return if (result.length == inDegree.size) result.toString() else \"\"\n    }\n}"  cpp="class AlienDictionary_BFS {\n    $1(words: Array<String>) {\n        auto $1 = mutableMapOf<Char, HashSet<Char>>()\n        auto $1 = mutableMapOf<Char, Int>()\n        words.forEach { word ->\n            word.forEach { char ->\n                inDegree.putIfAbsent(char, 0)\n                graph.putIfAbsent(char, hashSetOf())\n            }\n        }\n\n        for (i in 0 until words.size - 1) {\n            auto $1 = words[i]\n            auto $1 = words[i + 1]\n            auto $1 = min(currentWord.length, nextWord.length)\n            for (j in 0 until minLength) {\n                auto $1 = currentWord[j]\n                auto $1 = nextWord[j]\n                if (currentChar != nextChar) {\n                    if (graph[currentChar]!!.add(nextChar)) {\n                        inDegree[nextChar] = inDegree[nextChar]!! + 1\n                    }\n                    break\n                }\n                // Handle invalid case (e.g., \"abc\" before \"ab\")\n                if (j == minLength - 1 && currentWord.length > nextWord.length) return \"\"\n            }\n        }\n\n        auto $1 = ArrayDeque<Char>().apply { addAll(inDegree.filter { it.value == 0 }.keys) }\n        auto $1 = StringBuilder()\n        while (queue.isNotEmpty()) {\n            auto $1 = queue.removeFirst()\n            result.append(char)\n            graph[char]?.forEach { neighbor ->\n                inDegree[neighbor] = inDegree[neighbor]!! - 1\n                if (inDegree[neighbor] == 0) queue.add(neighbor)\n            }\n        }\n\n        return if (result.length == inDegree.size) result.toString() else \"\"\n    }\n}" %}

---

## ApplySubstitutions

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/topological_sort/ApplySubstitutions.kt`

{% include code-tabs.html  kotlin="package string\n\nclass ApplySubstitutions {\n    fun applySubstitutions(replacements: List<List<String>>, text: String): String {\n        val map = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        fun resolve(s: String): String {\n            val sb = StringBuilder()\n            var i = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    val key = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            val result = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}\n"  java="class ApplySubstitutions {\n    public static void $1(replacements: List<List<String>>, text) {\n        val map = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        public static void $1(s) {\n            val sb = StringBuilder()\n            var i = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    val key = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            val result = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}"  python="class ApplySubstitutions {\n    def $1(replacements: List<List<String>>, text: str): str {\n        $1 = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        def $1(s: str): str {\n            $1 = StringBuilder()\n            $1 = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    $1 = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            $1 = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}"  rust="class ApplySubstitutions {\n    fn $1(replacements: List<List<String>>, text -> String) -> String {\n        val map = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        fn $1(s -> String) -> String {\n            val sb = StringBuilder()\n            var i = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    val key = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            val result = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}"  cpp="class ApplySubstitutions {\n    $1(replacements: List<List<String>>, text) {\n        auto $1 = mutableMapOf<String, String>()\n\n        for ((key, value) in replacements) {\n            map[key] = value\n        }\n\n        $1(s) {\n            auto $1 = StringBuilder()\n            auto $1 = 0\n\n            while (i < s.length) {\n                if (i + 2 < s.length && s[i] == '%' && s[i + 2] == '%') {\n                    auto $1 = s[i + 1].toString()\n                    sb.append(map[key] ?: \"%$key%\")  // Replace if found, else keep original\n                    i += 3  // Move past the placeholder\n                } else {\n                    sb.append(s[i])\n                    i++\n                }\n            }\n\n            auto $1 = sb.toString()\n            return if (result == s) result else resolve(result)  // Continue resolving if changed\n        }\n\n        return resolve(text)\n    }\n}" %}

---

## CourseSchedule_II

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/topological_sort/CourseSchedule_II.kt`

{% include code-tabs.html  kotlin="package graph.topological_sort\n\nclass CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: IntArray\n    private var index: Int = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    fun findOrder(numCourses: Int, prerequisites: Array<IntArray>): IntArray {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = IntArray(numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private fun hasCycle(numCourses: Int): Boolean {\n        // Nested DFS function for checking cycles and building the result\n        fun dfs(node: Int): Boolean {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return true\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return false\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return true\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return false\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return true\n                }\n            }\n        }\n        return false\n    }\n}"  java="class CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: IntArray\n    private var index = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    public static void $1(numCourses, prerequisites: Array<IntArray>): IntArray {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = IntArray(numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private public static void $1(numCourses) {\n        // Nested DFS function for checking cycles and building the result\n        public static void $1(node) {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return true\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return false\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return true\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return false\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return true\n                }\n            }\n        }\n        return false\n    }\n}"  python="class CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: list[int]\n    private var index: int = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    def $1(numCourses: int, prerequisites: Array<list[int]>): list[int] {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = list[int](numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private def $1(numCourses: int): bool {\n        // Nested DFS function for checking cycles and building the result\n        def $1(node: int): bool {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return True\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return False\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return True\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return False\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return True\n                }\n            }\n        }\n        return False\n    }\n}"  rust="class CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: Vec<i32>\n    private var index -> i32 = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    fn $1(numCourses -> i32, prerequisites: Array<Vec<i32>>): Vec<i32> {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = Vec<i32>(numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private fn $1(numCourses -> i32) -> bool {\n        // Nested DFS function for checking cycles and building the result\n        fn $1(node -> i32) -> bool {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return true\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return false\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return true\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return false\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return true\n                }\n            }\n        }\n        return false\n    }\n}"  cpp="class CourseSchedule_II {\n    private var graph: MutableMap<Int, MutableList<Int>> = mutableMapOf()\n    private lateinit var status: Array<NodeStatus>\n    private lateinit var result: vector<int>\n    private auto $1 = 0\n\n    private enum class NodeStatus { UNVISITED, EXPLORING, DONE }\n\n    $1(numCourses, prerequisites: Array<vector<int>>): vector<int> {\n        // Initialize graph, status, and result array\n        status = Array(numCourses) { NodeStatus.UNVISITED }\n        result = vector<int>(numCourses)\n        index = numCourses - 1\n\n        // Build the graph\n        prerequisites.forEach { (course, prereq) ->\n            graph.getOrPut(prereq) { mutableListOf() }.add(course)\n        }\n\n        // Attempt to find a topological order\n        if (hasCycle(numCourses)) {\n            return intArrayOf()\n        }\n\n        return result\n    }\n\n    private $1(numCourses) {\n        // Nested DFS function for checking cycles and building the result\n        $1(node) {\n            when (status[node]) {\n                // If an already visited node re-appears then it means we looped back to an exploring node\n                NodeStatus.EXPLORING -> return true\n                // If an already done node re-appears it doesn't mean cycle rather it means\n                // we fully explored the node previously and found no cycle in the path.\n                NodeStatus.DONE -> return false\n                else -> {\n                    status[node] = NodeStatus.EXPLORING\n                    graph[node]?.forEach { neighbor ->\n                        if (dfs(neighbor)) return true\n                    }\n\n                    status[node] = NodeStatus.DONE\n                    result[index--] = node\n                    return false\n                }\n            }\n        }\n\n        for (i in 0 until numCourses) {\n            if (status[i] == NodeStatus.UNVISITED) {\n                if (dfs(i)) {\n                    return true\n                }\n            }\n        }\n        return false\n    }\n}" %}

---

## CourseSchedule_II_BFS

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/topological_sort/CourseSchedule_II_BFS.kt`

{% include code-tabs.html  kotlin="package graph.topological_sort\n\nclass CourseSchedule_II_BFS {\n    fun findOrder(numCourses: Int, prerequisites: Array<IntArray>): IntArray {\n        val graph = Array<MutableList<Int>>(numCourses) { mutableListOf() }\n        val inDegree = IntArray(numCourses)\n        val result = mutableListOf<Int>()\n\n        // Build the graph and calculate in-degrees\n        prerequisites.forEach { (course, preReq) ->\n            graph[preReq].add(course)\n            inDegree[course]++\n        }\n\n        // Initialize queue with courses having no prerequisites\n        val queue = ArrayDeque<Int>().apply { inDegree.indices.filter { inDegree[it] == 0 }.forEach { add(it) } }\n\n        // Perform BFS (Kahn's Algorithm)\n        while (queue.isNotEmpty()) {\n            queue.removeFirst().also {\n                result.add(it)\n                graph[it].forEach { neighbor ->\n                    if (--inDegree[neighbor] == 0) queue.add(neighbor)\n                }\n            }\n        }\n\n        return if (result.size == numCourses) result.toIntArray() else intArrayOf()\n    }\n}\n\nfun main() {\n    val solver = graph.topological_sort.CourseSchedule_II_BFS()\n\n    // Test 1: Simple case\n    val result1 = solver.findOrder(2, arrayOf(intArrayOf(1, 0)))\n    assert(result1 contentEquals intArrayOf(0, 1)) { \"Test 1 failed: $result1\" }\n\n    // Test 2: Multiple dependencies\n    val result2 = solver.findOrder(4, arrayOf(intArrayOf(1, 0), intArrayOf(2, 0), intArrayOf(3, 1), intArrayOf(3, 2)))\n    assert(result2 contentEquals intArrayOf(0, 1, 2, 3) || result2 contentEquals intArrayOf(0, 2, 1, 3)) { \"Test 2 failed: $result2\" }\n\n    // Test 3: Cycle in the graph (no valid order)\n    val result3 = solver.findOrder(2, arrayOf(intArrayOf(0, 1), intArrayOf(1, 0)))\n    assert(result3.isEmpty()) { \"Test 3 failed: $result3\" }\n\n    // Test 4: No prerequisites\n    val result4 = solver.findOrder(3, arrayOf())\n    assert(result4.toSet() == setOf(0, 1, 2)) { \"Test 4 failed: $result4\" }\n\n    // Test 5: Self-loop (invalid schedule)\n    val result5 = solver.findOrder(1, arrayOf(intArrayOf(0, 0)))\n    assert(result5.isEmpty()) { \"Test 5 failed: $result5\" }\n\n    println(\"All tests passed.\")\n}\n"  java="class CourseSchedule_II_BFS {\n    public static void $1(numCourses, prerequisites: Array<IntArray>): IntArray {\n        val graph = Array<MutableList<Int>>(numCourses) { mutableListOf() }\n        val inDegree = IntArray(numCourses)\n        val result = mutableListOf<Int>()\n\n        // Build the graph and calculate in-degrees\n        prerequisites.forEach { (course, preReq) ->\n            graph[preReq].add(course)\n            inDegree[course]++\n        }\n\n        // Initialize queue with courses having no prerequisites\n        val queue = ArrayDeque<Int>().apply { inDegree.indices.filter { inDegree[it] == 0 }.forEach { add(it) } }\n\n        // Perform BFS (Kahn's Algorithm)\n        while (queue.isNotEmpty()) {\n            queue.removeFirst().also {\n                result.add(it)\n                graph[it].forEach { neighbor ->\n                    if (--inDegree[neighbor] == 0) queue.add(neighbor)\n                }\n            }\n        }\n\n        return if (result.size == numCourses) result.toIntArray() else intArrayOf()\n    }\n}\n\npublic static void $1() {\n    val solver = graph.topological_sort.CourseSchedule_II_BFS()\n\n    // Test 1: Simple case\n    val result1 = solver.findOrder(2, arrayOf(intArrayOf(1, 0)))\n    assert(result1 contentEquals intArrayOf(0, 1)) { \"Test 1 failed: $result1\" }\n\n    // Test 2: Multiple dependencies\n    val result2 = solver.findOrder(4, arrayOf(intArrayOf(1, 0), intArrayOf(2, 0), intArrayOf(3, 1), intArrayOf(3, 2)))\n    assert(result2 contentEquals intArrayOf(0, 1, 2, 3) || result2 contentEquals intArrayOf(0, 2, 1, 3)) { \"Test 2 failed: $result2\" }\n\n    // Test 3: Cycle in the graph (no valid order)\n    val result3 = solver.findOrder(2, arrayOf(intArrayOf(0, 1), intArrayOf(1, 0)))\n    assert(result3.isEmpty()) { \"Test 3 failed: $result3\" }\n\n    // Test 4: No prerequisites\n    val result4 = solver.findOrder(3, arrayOf())\n    assert(result4.toSet() == setOf(0, 1, 2)) { \"Test 4 failed: $result4\" }\n\n    // Test 5: Self-loop (invalid schedule)\n    val result5 = solver.findOrder(1, arrayOf(intArrayOf(0, 0)))\n    assert(result5.isEmpty()) { \"Test 5 failed: $result5\" }\n\n    println(\"All tests passed.\")\n}"  python="class CourseSchedule_II_BFS {\n    def $1(numCourses: int, prerequisites: Array<list[int]>): list[int] {\n        $1 = Array<MutableList<Int>>(numCourses) { mutableListOf() }\n        $1 = list[int](numCourses)\n        $1 = mutableListOf<Int>()\n\n        // Build the graph and calculate in-degrees\n        prerequisites.forEach { (course, preReq) ->\n            graph[preReq].add(course)\n            inDegree[course]++\n        }\n\n        // Initialize queue with courses having no prerequisites\n        $1 = ArrayDeque<Int>().apply { inDegree.indices.filter { inDegree[it] == 0 }.forEach { add(it) } }\n\n        // Perform BFS (Kahn's Algorithm)\n        while (queue.isNotEmpty()) {\n            queue.removeFirst().also {\n                result.add(it)\n                graph[it].forEach { neighbor ->\n                    if (--inDegree[neighbor] == 0) queue.add(neighbor)\n                }\n            }\n        }\n\n        return if (result.size == numCourses) result.toIntArray() else intArrayOf()\n    }\n}\n\ndef $1() {\n    $1 = graph.topological_sort.CourseSchedule_II_BFS()\n\n    // Test 1: Simple case\n    $1 = solver.findOrder(2, arrayOf(intArrayOf(1, 0)))\n    assert(result1 contentEquals intArrayOf(0, 1)) { \"Test 1 failed: $result1\" }\n\n    // Test 2: Multiple dependencies\n    $1 = solver.findOrder(4, arrayOf(intArrayOf(1, 0), intArrayOf(2, 0), intArrayOf(3, 1), intArrayOf(3, 2)))\n    assert(result2 contentEquals intArrayOf(0, 1, 2, 3) || result2 contentEquals intArrayOf(0, 2, 1, 3)) { \"Test 2 failed: $result2\" }\n\n    // Test 3: Cycle in the graph (no valid order)\n    $1 = solver.findOrder(2, arrayOf(intArrayOf(0, 1), intArrayOf(1, 0)))\n    assert(result3.isEmpty()) { \"Test 3 failed: $result3\" }\n\n    // Test 4: No prerequisites\n    $1 = solver.findOrder(3, arrayOf())\n    assert(result4.toSet() == setOf(0, 1, 2)) { \"Test 4 failed: $result4\" }\n\n    // Test 5: Self-loop (invalid schedule)\n    $1 = solver.findOrder(1, arrayOf(intArrayOf(0, 0)))\n    assert(result5.isEmpty()) { \"Test 5 failed: $result5\" }\n\n    print(\"All tests passed.\")\n}"  rust="class CourseSchedule_II_BFS {\n    fn $1(numCourses -> i32, prerequisites: Array<Vec<i32>>): Vec<i32> {\n        val graph = Array<MutableList<Int>>(numCourses) { mutableListOf() }\n        val inDegree = Vec<i32>(numCourses)\n        val result = mutableListOf<Int>()\n\n        // Build the graph and calculate in-degrees\n        prerequisites.forEach { (course, preReq) ->\n            graph[preReq].add(course)\n            inDegree[course]++\n        }\n\n        // Initialize queue with courses having no prerequisites\n        val queue = ArrayDeque<Int>().apply { inDegree.indices.filter { inDegree[it] == 0 }.forEach { add(it) } }\n\n        // Perform BFS (Kahn's Algorithm)\n        while (queue.isNotEmpty()) {\n            queue.removeFirst().also {\n                result.add(it)\n                graph[it].forEach { neighbor ->\n                    if (--inDegree[neighbor] == 0) queue.add(neighbor)\n                }\n            }\n        }\n\n        return if (result.size == numCourses) result.toIntArray() else intArrayOf()\n    }\n}\n\nfn $1() {\n    val solver = graph.topological_sort.CourseSchedule_II_BFS()\n\n    // Test 1: Simple case\n    val result1 = solver.findOrder(2, arrayOf(intArrayOf(1, 0)))\n    assert(result1 contentEquals intArrayOf(0, 1)) { \"Test 1 failed: $result1\" }\n\n    // Test 2: Multiple dependencies\n    val result2 = solver.findOrder(4, arrayOf(intArrayOf(1, 0), intArrayOf(2, 0), intArrayOf(3, 1), intArrayOf(3, 2)))\n    assert(result2 contentEquals intArrayOf(0, 1, 2, 3) || result2 contentEquals intArrayOf(0, 2, 1, 3)) { \"Test 2 failed: $result2\" }\n\n    // Test 3: Cycle in the graph (no valid order)\n    val result3 = solver.findOrder(2, arrayOf(intArrayOf(0, 1), intArrayOf(1, 0)))\n    assert(result3.isEmpty()) { \"Test 3 failed: $result3\" }\n\n    // Test 4: No prerequisites\n    val result4 = solver.findOrder(3, arrayOf())\n    assert(result4.toSet() == setOf(0, 1, 2)) { \"Test 4 failed: $result4\" }\n\n    // Test 5: Self-loop (invalid schedule)\n    val result5 = solver.findOrder(1, arrayOf(intArrayOf(0, 0)))\n    assert(result5.isEmpty()) { \"Test 5 failed: $result5\" }\n\n    println!(\"All tests passed.\")\n}"  cpp="class CourseSchedule_II_BFS {\n    $1(numCourses, prerequisites: Array<vector<int>>): vector<int> {\n        auto $1 = Array<MutableList<Int>>(numCourses) { mutableListOf() }\n        auto $1 = vector<int>(numCourses)\n        auto $1 = mutableListOf<Int>()\n\n        // Build the graph and calculate in-degrees\n        prerequisites.forEach { (course, preReq) ->\n            graph[preReq].add(course)\n            inDegree[course]++\n        }\n\n        // Initialize queue with courses having no prerequisites\n        auto $1 = ArrayDeque<Int>().apply { inDegree.indices.filter { inDegree[it] == 0 }.forEach { add(it) } }\n\n        // Perform BFS (Kahn's Algorithm)\n        while (queue.isNotEmpty()) {\n            queue.removeFirst().also {\n                result.add(it)\n                graph[it].forEach { neighbor ->\n                    if (--inDegree[neighbor] == 0) queue.add(neighbor)\n                }\n            }\n        }\n\n        return if (result.size == numCourses) result.toIntArray() else intArrayOf()\n    }\n}\n\n$1() {\n    auto $1 = graph.topological_sort.CourseSchedule_II_BFS()\n\n    // Test 1: Simple case\n    auto $1 = solver.findOrder(2, arrayOf(intArrayOf(1, 0)))\n    assert(result1 contentEquals intArrayOf(0, 1)) { \"Test 1 failed: $result1\" }\n\n    // Test 2: Multiple dependencies\n    auto $1 = solver.findOrder(4, arrayOf(intArrayOf(1, 0), intArrayOf(2, 0), intArrayOf(3, 1), intArrayOf(3, 2)))\n    assert(result2 contentEquals intArrayOf(0, 1, 2, 3) || result2 contentEquals intArrayOf(0, 2, 1, 3)) { \"Test 2 failed: $result2\" }\n\n    // Test 3: Cycle in the graph (no valid order)\n    auto $1 = solver.findOrder(2, arrayOf(intArrayOf(0, 1), intArrayOf(1, 0)))\n    assert(result3.isEmpty()) { \"Test 3 failed: $result3\" }\n\n    // Test 4: No prerequisites\n    auto $1 = solver.findOrder(3, arrayOf())\n    assert(result4.toSet() == setOf(0, 1, 2)) { \"Test 4 failed: $result4\" }\n\n    // Test 5: Self-loop (invalid schedule)\n    auto $1 = solver.findOrder(1, arrayOf(intArrayOf(0, 0)))\n    assert(result5.isEmpty()) { \"Test 5 failed: $result5\" }\n\n    cout <<(\"All tests passed.\")\n}" %}

---

## CheapestFlightWithKStops

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/greedy/CheapestFlightWithKStops.kt`

{% include code-tabs.html  kotlin="package graph.greedy\n\nimport java.util.*\n\nclass CheapestFlightWithKStops {\n    data class Flight(val node: Int, val cost: Int, val stops: Int)\n\n    fun findCheapestPrice(n: Int, flights: Array<IntArray>, src: Int, dst: Int, k: Int): Int {\n        val graph = flights.groupBy({ it[0] }) { Flight(it[1], it[2], 0) }\n        val pq = PriorityQueue<Flight>(compareBy { it.cost })\n        val minStops = IntArray(n) { Int.MAX_VALUE }\n\n        pq.offer(Flight(src, 0, 0))\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            if (stops > k + 1 || stops >= minStops[node]) continue\n            minStops[node] = stops\n\n            if (node == dst) return currentCost\n\n            graph[node]?.forEach { (next, cost, _) ->\n                pq.offer(Flight(next, currentCost + cost, stops + 1))\n            }\n        }\n        return -1\n    }\n}\n"  java="class CheapestFlightWithKStops {\n    data class Flight(val node, val cost, val stops)\n\n    public static void $1(n, flights: Array<IntArray>, src, dst, k) {\n        val graph = flights.groupBy({ it[0] }) { Flight(it[1], it[2], 0) }\n        val pq = PriorityQueue<Flight>(compareBy { it.cost })\n        val minStops = IntArray(n) { Int.MAX_VALUE }\n\n        pq.offer(Flight(src, 0, 0))\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            if (stops > k + 1 || stops >= minStops[node]) continue\n            minStops[node] = stops\n\n            if (node == dst) return currentCost\n\n            graph[node]?.forEach { (next, cost, _) ->\n                pq.offer(Flight(next, currentCost + cost, stops + 1))\n            }\n        }\n        return -1\n    }\n}"  python="class CheapestFlightWithKStops {\n    data class Flight(val node: int, val cost: int, val stops: int)\n\n    def $1(n: int, flights: Array<list[int]>, src: int, dst: int, k: int): int {\n        $1 = flights.groupBy({ it[0] }) { Flight(it[1], it[2], 0) }\n        $1 = PriorityQueue<Flight>(compareBy { it.cost })\n        $1 = list[int](n) { Int.MAX_VALUE }\n\n        pq.offer(Flight(src, 0, 0))\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            if (stops > k + 1 || stops >= minStops[node]) continue\n            minStops[node] = stops\n\n            if (node == dst) return currentCost\n\n            graph[node]?.forEach { (next, cost, _) ->\n                pq.offer(Flight(next, currentCost + cost, stops + 1))\n            }\n        }\n        return -1\n    }\n}"  rust="class CheapestFlightWithKStops {\n    data class Flight(val node -> i32, val cost -> i32, val stops -> i32)\n\n    fn $1(n -> i32, flights: Array<Vec<i32>>, src -> i32, dst -> i32, k -> i32) -> i32 {\n        val graph = flights.groupBy({ it[0] }) { Flight(it[1], it[2], 0) }\n        val pq = PriorityQueue<Flight>(compareBy { it.cost })\n        val minStops = Vec<i32>(n) { Int.MAX_VALUE }\n\n        pq.offer(Flight(src, 0, 0))\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            if (stops > k + 1 || stops >= minStops[node]) continue\n            minStops[node] = stops\n\n            if (node == dst) return currentCost\n\n            graph[node]?.forEach { (next, cost, _) ->\n                pq.offer(Flight(next, currentCost + cost, stops + 1))\n            }\n        }\n        return -1\n    }\n}"  cpp="class CheapestFlightWithKStops {\n    data class Flight(val node, val cost, val stops)\n\n    $1(n, flights: Array<vector<int>>, src, dst, k) {\n        auto $1 = flights.groupBy({ it[0] }) { Flight(it[1], it[2], 0) }\n        auto $1 = PriorityQueue<Flight>(compareBy { it.cost })\n        auto $1 = vector<int>(n) { Int.MAX_VALUE }\n\n        pq.offer(Flight(src, 0, 0))\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            if (stops > k + 1 || stops >= minStops[node]) continue\n            minStops[node] = stops\n\n            if (node == dst) return currentCost\n\n            graph[node]?.forEach { (next, cost, _) ->\n                pq.offer(Flight(next, currentCost + cost, stops + 1))\n            }\n        }\n        return -1\n    }\n}" %}

---

## CheapestFlightsWithKStops

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/greedy/CheapestFlightsWithKStops.kt`

{% include code-tabs.html  kotlin="package graph.greedy\n\nimport java.util.*\n\nclass CheapestFlightsWithKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(val dest: Int, val cost: Int)\n\n    // Define the State class for priority queue elements\n    data class State(val node: Int, val cost: Int, val stops: Int)\n\n    fun findCheapestPrice(n: Int, flights: Array<IntArray>, src: Int, dst: Int, k: Int): Int {\n        // Build the graph from the input flights\n        val graph = mutableMapOf<Int, MutableList<Node>>()\n        flights.forEach { flight ->\n            val from = flight[0]\n            val to = flight[1]\n            val cost = flight[2]\n            graph.getOrPut(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // E + EK log\n\n        // Initialize the priority queue and cost tracking\n        val minStops = Array(n) { Int.MAX_VALUE }\n        val pq = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(0, src, 0))\n        minStops[src] = 0\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            // Skip if the number of stops exceeds the limit or if the path is not optimal\n            if (stops > k + 1 || currentCost > minStops[node]) continue\n\n            minStops[node] = currentCost\n            // Return the cost if the destination is reached\n            if (node == dst) return currentCost\n\n            // Explore neighbors\n            graph[node]?.forEach { neighbor ->\n                    pq.offer(State(neighbor.dest, currentCost + neighbor.cost, stops + 1))\n            }\n        }\n\n        // If no path was found within the constraints\n        return -1\n    }\n}\n\n"  java="class CheapestFlightsWithKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(val dest, val cost)\n\n    // Define the State class for priority queue elements\n    data class State(val node, val cost, val stops)\n\n    public static void $1(n, flights: Array<IntArray>, src, dst, k) {\n        // Build the graph from the input flights\n        val graph = mutableMapOf<Int, MutableList<Node>>()\n        flights.forEach { flight ->\n            val from = flight[0]\n            val to = flight[1]\n            val cost = flight[2]\n            graph.getOrPut(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // E + EK log\n\n        // Initialize the priority queue and cost tracking\n        val minStops = Array(n) { Int.MAX_VALUE }\n        val pq = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(0, src, 0))\n        minStops[src] = 0\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            // Skip if the number of stops exceeds the limit or if the path is not optimal\n            if (stops > k + 1 || currentCost > minStops[node]) continue\n\n            minStops[node] = currentCost\n            // Return the cost if the destination is reached\n            if (node == dst) return currentCost\n\n            // Explore neighbors\n            graph[node]?.forEach { neighbor ->\n                    pq.offer(State(neighbor.dest, currentCost + neighbor.cost, stops + 1))\n            }\n        }\n\n        // If no path was found within the constraints\n        return -1\n    }\n}"  python="class CheapestFlightsWithKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(val dest: int, val cost: int)\n\n    // Define the State class for priority queue elements\n    data class State(val node: int, val cost: int, val stops: int)\n\n    def $1(n: int, flights: Array<list[int]>, src: int, dst: int, k: int): int {\n        // Build the graph from the input flights\n        $1 = mutableMapOf<Int, MutableList<Node>>()\n        flights.forEach { flight ->\n            $1 = flight[0]\n            $1 = flight[1]\n            $1 = flight[2]\n            graph.getOrPut(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // E + EK log\n\n        // Initialize the priority queue and cost tracking\n        $1 = Array(n) { Int.MAX_VALUE }\n        $1 = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(0, src, 0))\n        minStops[src] = 0\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            // Skip if the number of stops exceeds the limit or if the path is not optimal\n            if (stops > k + 1 || currentCost > minStops[node]) continue\n\n            minStops[node] = currentCost\n            // Return the cost if the destination is reached\n            if (node == dst) return currentCost\n\n            // Explore neighbors\n            graph[node]?.forEach { neighbor ->\n                    pq.offer(State(neighbor.dest, currentCost + neighbor.cost, stops + 1))\n            }\n        }\n\n        // If no path was found within the constraints\n        return -1\n    }\n}"  rust="class CheapestFlightsWithKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(val dest -> i32, val cost -> i32)\n\n    // Define the State class for priority queue elements\n    data class State(val node -> i32, val cost -> i32, val stops -> i32)\n\n    fn $1(n -> i32, flights: Array<Vec<i32>>, src -> i32, dst -> i32, k -> i32) -> i32 {\n        // Build the graph from the input flights\n        val graph = mutableMapOf<Int, MutableList<Node>>()\n        flights.forEach { flight ->\n            val from = flight[0]\n            val to = flight[1]\n            val cost = flight[2]\n            graph.getOrPut(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // E + EK log\n\n        // Initialize the priority queue and cost tracking\n        val minStops = Array(n) { Int.MAX_VALUE }\n        val pq = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(0, src, 0))\n        minStops[src] = 0\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            // Skip if the number of stops exceeds the limit or if the path is not optimal\n            if (stops > k + 1 || currentCost > minStops[node]) continue\n\n            minStops[node] = currentCost\n            // Return the cost if the destination is reached\n            if (node == dst) return currentCost\n\n            // Explore neighbors\n            graph[node]?.forEach { neighbor ->\n                    pq.offer(State(neighbor.dest, currentCost + neighbor.cost, stops + 1))\n            }\n        }\n\n        // If no path was found within the constraints\n        return -1\n    }\n}"  cpp="class CheapestFlightsWithKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(val dest, val cost)\n\n    // Define the State class for priority queue elements\n    data class State(val node, val cost, val stops)\n\n    $1(n, flights: Array<vector<int>>, src, dst, k) {\n        // Build the graph from the input flights\n        auto $1 = mutableMapOf<Int, MutableList<Node>>()\n        flights.forEach { flight ->\n            auto $1 = flight[0]\n            auto $1 = flight[1]\n            auto $1 = flight[2]\n            graph.getOrPut(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // E + EK log\n\n        // Initialize the priority queue and cost tracking\n        auto $1 = Array(n) { Int.MAX_VALUE }\n        auto $1 = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(0, src, 0))\n        minStops[src] = 0\n\n        while (pq.isNotEmpty()) {\n            val (node, currentCost, stops) = pq.poll()\n\n            // Skip if the number of stops exceeds the limit or if the path is not optimal\n            if (stops > k + 1 || currentCost > minStops[node]) continue\n\n            minStops[node] = currentCost\n            // Return the cost if the destination is reached\n            if (node == dst) return currentCost\n\n            // Explore neighbors\n            graph[node]?.forEach { neighbor ->\n                    pq.offer(State(neighbor.dest, currentCost + neighbor.cost, stops + 1))\n            }\n        }\n\n        // If no path was found within the constraints\n        return -1\n    }\n}" %}

---

## DonaldTrumpAlgorithm

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/greedy/DonaldTrumpAlgorithm.kt`

{% include code-tabs.html  kotlin="package graph.greedy\n\nclass DonaldTrumpAlgorithm {\n    enum class ParentStatus { CITIZEN, LEGAL_VISA, ILLEGAL }\n    enum class CitizenshipStatus { CITIZEN, NOT_A_CITIZEN }\n\n    fun determineChildCitizenship(fatherStatus: ParentStatus, motherStatus: ParentStatus, bornInUsa: Boolean): CitizenshipStatus =\n        if (bornInUsa) {\n            when {\n                fatherStatus == ParentStatus.CITIZEN || motherStatus == ParentStatus.CITIZEN -> CitizenshipStatus.CITIZEN\n                else -> CitizenshipStatus.NOT_A_CITIZEN\n            }\n        } else CitizenshipStatus.NOT_A_CITIZEN\n\n    fun main() {\n        // Test cases\n        val testCases = listOf(\n            Pair(ParentStatus.CITIZEN, ParentStatus.LEGAL_VISA) to true, // Child should be Citizen\n            Pair(ParentStatus.ILLEGAL, ParentStatus.ILLEGAL) to true, // Child should not be Citizen\n            Pair(ParentStatus.LEGAL_VISA, ParentStatus.LEGAL_VISA) to true, // Child should not be Citizen\n            Pair(ParentStatus.CITIZEN, ParentStatus.CITIZEN) to false // Child born outside USA, should not be Citizen\n        )\n\n        testCases.forEach { (parents, bornInUsa) ->\n            val result = determineChildCitizenship(parents.first, parents.second, bornInUsa)\n            println(\"Parents: ${parents.first}, ${parents.second}, Born in USA: $bornInUsa => Citizenship: $result\")\n        }\n    }\n}"  java="class DonaldTrumpAlgorithm {\n    enum class ParentStatus { CITIZEN, LEGAL_VISA, ILLEGAL }\n    enum class CitizenshipStatus { CITIZEN, NOT_A_CITIZEN }\n\n    public static void $1(fatherStatus: ParentStatus, motherStatus: ParentStatus, bornInUsa): CitizenshipStatus =\n        if (bornInUsa) {\n            when {\n                fatherStatus == ParentStatus.CITIZEN || motherStatus == ParentStatus.CITIZEN -> CitizenshipStatus.CITIZEN\n                else -> CitizenshipStatus.NOT_A_CITIZEN\n            }\n        } else CitizenshipStatus.NOT_A_CITIZEN\n\n    public static void $1() {\n        // Test cases\n        val testCases = listOf(\n            Pair(ParentStatus.CITIZEN, ParentStatus.LEGAL_VISA) to true, // Child should be Citizen\n            Pair(ParentStatus.ILLEGAL, ParentStatus.ILLEGAL) to true, // Child should not be Citizen\n            Pair(ParentStatus.LEGAL_VISA, ParentStatus.LEGAL_VISA) to true, // Child should not be Citizen\n            Pair(ParentStatus.CITIZEN, ParentStatus.CITIZEN) to false // Child born outside USA, should not be Citizen\n        )\n\n        testCases.forEach { (parents, bornInUsa) ->\n            val result = determineChildCitizenship(parents.first, parents.second, bornInUsa)\n            println(\"Parents: ${parents.first}, ${parents.second}, Born in USA: $bornInUsa => Citizenship: $result\")\n        }\n    }\n}"  python="class DonaldTrumpAlgorithm {\n    enum class ParentStatus { CITIZEN, LEGAL_VISA, ILLEGAL }\n    enum class CitizenshipStatus { CITIZEN, NOT_A_CITIZEN }\n\n    def $1(fatherStatus: ParentStatus, motherStatus: ParentStatus, bornInUsa: bool): CitizenshipStatus =\n        if (bornInUsa) {\n            when {\n                fatherStatus == ParentStatus.CITIZEN || motherStatus == ParentStatus.CITIZEN -> CitizenshipStatus.CITIZEN\n                else -> CitizenshipStatus.NOT_A_CITIZEN\n            }\n        } else CitizenshipStatus.NOT_A_CITIZEN\n\n    def $1() {\n        // Test cases\n        $1 = listOf(\n            Pair(ParentStatus.CITIZEN, ParentStatus.LEGAL_VISA) to True, // Child should be Citizen\n            Pair(ParentStatus.ILLEGAL, ParentStatus.ILLEGAL) to True, // Child should not be Citizen\n            Pair(ParentStatus.LEGAL_VISA, ParentStatus.LEGAL_VISA) to True, // Child should not be Citizen\n            Pair(ParentStatus.CITIZEN, ParentStatus.CITIZEN) to False // Child born outside USA, should not be Citizen\n        )\n\n        testCases.forEach { (parents, bornInUsa) ->\n            $1 = determineChildCitizenship(parents.first, parents.second, bornInUsa)\n            print(\"Parents: ${parents.first}, ${parents.second}, Born in USA: $bornInUsa => Citizenship: $result\")\n        }\n    }\n}"  rust="class DonaldTrumpAlgorithm {\n    enum class ParentStatus { CITIZEN, LEGAL_VISA, ILLEGAL }\n    enum class CitizenshipStatus { CITIZEN, NOT_A_CITIZEN }\n\n    fn $1(fatherStatus: ParentStatus, motherStatus: ParentStatus, bornInUsa -> bool): CitizenshipStatus =\n        if (bornInUsa) {\n            when {\n                fatherStatus == ParentStatus.CITIZEN || motherStatus == ParentStatus.CITIZEN -> CitizenshipStatus.CITIZEN\n                else -> CitizenshipStatus.NOT_A_CITIZEN\n            }\n        } else CitizenshipStatus.NOT_A_CITIZEN\n\n    fn $1() {\n        // Test cases\n        val testCases = listOf(\n            Pair(ParentStatus.CITIZEN, ParentStatus.LEGAL_VISA) to true, // Child should be Citizen\n            Pair(ParentStatus.ILLEGAL, ParentStatus.ILLEGAL) to true, // Child should not be Citizen\n            Pair(ParentStatus.LEGAL_VISA, ParentStatus.LEGAL_VISA) to true, // Child should not be Citizen\n            Pair(ParentStatus.CITIZEN, ParentStatus.CITIZEN) to false // Child born outside USA, should not be Citizen\n        )\n\n        testCases.forEach { (parents, bornInUsa) ->\n            val result = determineChildCitizenship(parents.first, parents.second, bornInUsa)\n            println!(\"Parents: ${parents.first}, ${parents.second}, Born in USA: $bornInUsa => Citizenship: $result\")\n        }\n    }\n}"  cpp="class DonaldTrumpAlgorithm {\n    enum class ParentStatus { CITIZEN, LEGAL_VISA, ILLEGAL }\n    enum class CitizenshipStatus { CITIZEN, NOT_A_CITIZEN }\n\n    $1(fatherStatus: ParentStatus, motherStatus: ParentStatus, bornInUsa): CitizenshipStatus =\n        if (bornInUsa) {\n            when {\n                fatherStatus == ParentStatus.CITIZEN || motherStatus == ParentStatus.CITIZEN -> CitizenshipStatus.CITIZEN\n                else -> CitizenshipStatus.NOT_A_CITIZEN\n            }\n        } else CitizenshipStatus.NOT_A_CITIZEN\n\n    $1() {\n        // Test cases\n        auto $1 = listOf(\n            Pair(ParentStatus.CITIZEN, ParentStatus.LEGAL_VISA) to true, // Child should be Citizen\n            Pair(ParentStatus.ILLEGAL, ParentStatus.ILLEGAL) to true, // Child should not be Citizen\n            Pair(ParentStatus.LEGAL_VISA, ParentStatus.LEGAL_VISA) to true, // Child should not be Citizen\n            Pair(ParentStatus.CITIZEN, ParentStatus.CITIZEN) to false // Child born outside USA, should not be Citizen\n        )\n\n        testCases.forEach { (parents, bornInUsa) ->\n            auto $1 = determineChildCitizenship(parents.first, parents.second, bornInUsa)\n            cout <<(\"Parents: ${parents.first}, ${parents.second}, Born in USA: $bornInUsa => Citizenship: $result\")\n        }\n    }\n}" %}

---

## TheMaze_III

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/greedy/TheMaze_III.kt`

{% include code-tabs.html  kotlin="package graph.greedy\n\nimport java.util.*\n\nclass TheMaze_III {\n    data class State(val dist: Int, val x: Int, val y: Int)\n\n    fun findShortestWay(maze: Array<IntArray>, ball: IntArray, hole: IntArray): String {\n        val m = maze.size\n        val n = maze[0].size\n        val directions = listOf(\n            Triple(1, 0, \"d\"),\n            Triple(0, -1, \"l\"),\n            Triple(0, 1, \"r\"),\n            Triple(-1, 0, \"u\")\n        )\n\n        val distance = Array(m) { IntArray(n) { Int.MAX_VALUE } }\n        val path = Array(m) { Array(n) { \"\" } }\n        val pq = PriorityQueue<State>(compareBy { it.dist })\n\n        distance[ball[0]][ball[1]] = 0\n        path[ball[0]][ball[1]] = \"\"\n        pq.offer(State(0, ball[0], ball[1]))\n\n        while (pq.isNotEmpty()) {\n            val (d, x, y) = pq.poll()\n\n            if (x == hole[0] && y == hole[1]) {\n                return path[x][y]\n            }\n\n            if (d > distance[x][y]) continue\n\n            for ((dx, dy, dir) in directions) {\n                var nx = x\n                var ny = y\n                var steps = 0\n\n                while (nx + dx in 0 until m && ny + dy in 0 until n && maze[nx + dx][ny + dy] == 0) {\n                    nx += dx\n                    ny += dy\n                    steps++\n                    if (nx == hole[0] && ny == hole[1]) break\n                }\n\n                val newDist = d + steps\n                val newPath = path[x][y] + dir\n\n                if (newDist < distance[nx][ny]) {\n                    distance[nx][ny] = newDist\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                } else if (newDist == distance[nx][ny] && newPath < path[nx][ny]) {\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                }\n            }\n        }\n\n        return \"impossible\"\n    }\n}\n"  java="class TheMaze_III {\n    data class State(val dist, val x, val y)\n\n    public static void $1(maze: Array<IntArray>, ball: IntArray, hole: IntArray) {\n        val m = maze.size\n        val n = maze[0].size\n        val directions = listOf(\n            Triple(1, 0, \"d\"),\n            Triple(0, -1, \"l\"),\n            Triple(0, 1, \"r\"),\n            Triple(-1, 0, \"u\")\n        )\n\n        val distance = Array(m) { IntArray(n) { Int.MAX_VALUE } }\n        val path = Array(m) { Array(n) { \"\" } }\n        val pq = PriorityQueue<State>(compareBy { it.dist })\n\n        distance[ball[0]][ball[1]] = 0\n        path[ball[0]][ball[1]] = \"\"\n        pq.offer(State(0, ball[0], ball[1]))\n\n        while (pq.isNotEmpty()) {\n            val (d, x, y) = pq.poll()\n\n            if (x == hole[0] && y == hole[1]) {\n                return path[x][y]\n            }\n\n            if (d > distance[x][y]) continue\n\n            for ((dx, dy, dir) in directions) {\n                var nx = x\n                var ny = y\n                var steps = 0\n\n                while (nx + dx in 0 until m && ny + dy in 0 until n && maze[nx + dx][ny + dy] == 0) {\n                    nx += dx\n                    ny += dy\n                    steps++\n                    if (nx == hole[0] && ny == hole[1]) break\n                }\n\n                val newDist = d + steps\n                val newPath = path[x][y] + dir\n\n                if (newDist < distance[nx][ny]) {\n                    distance[nx][ny] = newDist\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                } else if (newDist == distance[nx][ny] && newPath < path[nx][ny]) {\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                }\n            }\n        }\n\n        return \"impossible\"\n    }\n}"  python="class TheMaze_III {\n    data class State(val dist: int, val x: int, val y: int)\n\n    def $1(maze: Array<list[int]>, ball: list[int], hole: list[int]): str {\n        $1 = maze.size\n        $1 = maze[0].size\n        $1 = listOf(\n            Triple(1, 0, \"d\"),\n            Triple(0, -1, \"l\"),\n            Triple(0, 1, \"r\"),\n            Triple(-1, 0, \"u\")\n        )\n\n        $1 = Array(m) { list[int](n) { Int.MAX_VALUE } }\n        $1 = Array(m) { Array(n) { \"\" } }\n        $1 = PriorityQueue<State>(compareBy { it.dist })\n\n        distance[ball[0]][ball[1]] = 0\n        path[ball[0]][ball[1]] = \"\"\n        pq.offer(State(0, ball[0], ball[1]))\n\n        while (pq.isNotEmpty()) {\n            val (d, x, y) = pq.poll()\n\n            if (x == hole[0] && y == hole[1]) {\n                return path[x][y]\n            }\n\n            if (d > distance[x][y]) continue\n\n            for ((dx, dy, dir) in directions) {\n                $1 = x\n                $1 = y\n                $1 = 0\n\n                while (nx + dx in 0 until m && ny + dy in 0 until n && maze[nx + dx][ny + dy] == 0) {\n                    nx += dx\n                    ny += dy\n                    steps++\n                    if (nx == hole[0] && ny == hole[1]) break\n                }\n\n                $1 = d + steps\n                $1 = path[x][y] + dir\n\n                if (newDist < distance[nx][ny]) {\n                    distance[nx][ny] = newDist\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                } else if (newDist == distance[nx][ny] && newPath < path[nx][ny]) {\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                }\n            }\n        }\n\n        return \"impossible\"\n    }\n}"  rust="class TheMaze_III {\n    data class State(val dist -> i32, val x -> i32, val y -> i32)\n\n    fn $1(maze: Array<Vec<i32>>, ball: Vec<i32>, hole: Vec<i32>) -> String {\n        val m = maze.size\n        val n = maze[0].size\n        val directions = listOf(\n            Triple(1, 0, \"d\"),\n            Triple(0, -1, \"l\"),\n            Triple(0, 1, \"r\"),\n            Triple(-1, 0, \"u\")\n        )\n\n        val distance = Array(m) { Vec<i32>(n) { Int.MAX_VALUE } }\n        val path = Array(m) { Array(n) { \"\" } }\n        val pq = PriorityQueue<State>(compareBy { it.dist })\n\n        distance[ball[0]][ball[1]] = 0\n        path[ball[0]][ball[1]] = \"\"\n        pq.offer(State(0, ball[0], ball[1]))\n\n        while (pq.isNotEmpty()) {\n            val (d, x, y) = pq.poll()\n\n            if (x == hole[0] && y == hole[1]) {\n                return path[x][y]\n            }\n\n            if (d > distance[x][y]) continue\n\n            for ((dx, dy, dir) in directions) {\n                var nx = x\n                var ny = y\n                var steps = 0\n\n                while (nx + dx in 0 until m && ny + dy in 0 until n && maze[nx + dx][ny + dy] == 0) {\n                    nx += dx\n                    ny += dy\n                    steps++\n                    if (nx == hole[0] && ny == hole[1]) break\n                }\n\n                val newDist = d + steps\n                val newPath = path[x][y] + dir\n\n                if (newDist < distance[nx][ny]) {\n                    distance[nx][ny] = newDist\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                } else if (newDist == distance[nx][ny] && newPath < path[nx][ny]) {\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                }\n            }\n        }\n\n        return \"impossible\"\n    }\n}"  cpp="class TheMaze_III {\n    data class State(val dist, val x, val y)\n\n    $1(maze: Array<vector<int>>, ball: vector<int>, hole: vector<int>) {\n        auto $1 = maze.size\n        auto $1 = maze[0].size\n        auto $1 = listOf(\n            Triple(1, 0, \"d\"),\n            Triple(0, -1, \"l\"),\n            Triple(0, 1, \"r\"),\n            Triple(-1, 0, \"u\")\n        )\n\n        auto $1 = Array(m) { vector<int>(n) { Int.MAX_VALUE } }\n        auto $1 = Array(m) { Array(n) { \"\" } }\n        auto $1 = PriorityQueue<State>(compareBy { it.dist })\n\n        distance[ball[0]][ball[1]] = 0\n        path[ball[0]][ball[1]] = \"\"\n        pq.offer(State(0, ball[0], ball[1]))\n\n        while (pq.isNotEmpty()) {\n            val (d, x, y) = pq.poll()\n\n            if (x == hole[0] && y == hole[1]) {\n                return path[x][y]\n            }\n\n            if (d > distance[x][y]) continue\n\n            for ((dx, dy, dir) in directions) {\n                auto $1 = x\n                auto $1 = y\n                auto $1 = 0\n\n                while (nx + dx in 0 until m && ny + dy in 0 until n && maze[nx + dx][ny + dy] == 0) {\n                    nx += dx\n                    ny += dy\n                    steps++\n                    if (nx == hole[0] && ny == hole[1]) break\n                }\n\n                auto $1 = d + steps\n                auto $1 = path[x][y] + dir\n\n                if (newDist < distance[nx][ny]) {\n                    distance[nx][ny] = newDist\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                } else if (newDist == distance[nx][ny] && newPath < path[nx][ny]) {\n                    path[nx][ny] = newPath\n                    pq.offer(State(newDist, nx, ny))\n                }\n            }\n        }\n\n        return \"impossible\"\n    }\n}" %}

---

## CheapestFlightsWithinKStops

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/dp/CheapestFlightsWithinKStops.kt`

{% include code-tabs.html  kotlin="package graph.dp\n\nimport java.util.*\n\nclass CheapestFlightsWithinKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(var dest: Int, var price: Int)\n\n    // Graph map where each integer key maps to a list of Node objects\n    private var graph = mutableMapOf<Int, MutableList<Node>>()\n\n    // Solving using Bellman Ford\n    fun findCheapestPrice(n: Int, flights: Array<IntArray>, src: Int, dst: Int, k: Int): Int {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            val from = flight[0]\n            val to = flight[1]\n            val cost = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // DP table for storing minimum cost to each node with up to i flights\n        val dp = Array(k + 2) { IntArray(n) { Int.MAX_VALUE } }\n        dp[0][src] = 0  // Cost to reach source from itself is 0\n\n        // Perform relaxation for each possible number of stops\n        for (i in 1..k + 1) {\n            dp[i][src] = 0  // No cost to stay at the source\n            graph.forEach { (from, neighbors) ->\n                neighbors.forEach { node ->\n                    if (dp[i - 1][from] != Int.MAX_VALUE) { // Ensure the source node is reachable\n                        dp[i][node.dest] = minOf(dp[i][node.dest], dp[i - 1][from] + node.price)\n                    }\n                }\n            }\n        }\n\n        // Find the minimum cost to the destination node considering all possible stops\n        val cheapest = (0..k + 1).minOfOrNull { dp[it][dst] } ?: Int.MAX_VALUE\n        return if (cheapest == Int.MAX_VALUE) -1 else cheapest\n    }\n\n    fun findCheapestPriceBfs(n: Int, flights: Array<IntArray>, src: Int, dst: Int, k: Int): Int {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            val from = flight[0]\n            val to = flight[1]\n            val cost = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        val cost = bfs(src, dst, n, k)\n\n        return if (cost[dst] == Int.MAX_VALUE) -1 else cost[dst]\n    }\n\n\n    fun dijkstra(graph: MutableMap<Int, MutableList<Node>>, src: Int): Map<Int, Int> {\n        val dist = mutableMapOf<Int, Int>().apply {\n            graph.keys.forEach { this[it] = Int.MAX_VALUE }\n        }\n        dist[src] = 0\n\n        // Define priority queue with a comparator provided inline\n        val priorityQueue = PriorityQueue<Node>(compareBy { it.price })\n        priorityQueue.add(Node(src, 0))\n\n        while (priorityQueue.isNotEmpty()) {\n            val current = priorityQueue.poll()\n\n            // If current node's distance in queue is greater than known shortest distance, skip it\n            if (current.price > dist[current.dest]!!) continue\n\n            graph[current.dest]?.forEach { neighbor ->\n                val newDistance = current.price + neighbor.price\n                if (newDistance < dist[neighbor.dest]!!) {\n                    dist[neighbor.dest] = newDistance\n                    priorityQueue.add(Node(neighbor.dest, newDistance))\n                }\n            }\n        }\n\n        return dist\n    }\n\n    fun bfs(src: Int, dest: Int, n: Int, k: Int ): IntArray {\n        // To track the minimum cost to reach each destination\n        val minCost = IntArray(n) { Int.MAX_VALUE }\n        minCost[src] = 0\n\n        // Using a queue for BFS; store the current node, the cost to reach it, and the number of stops made\n        val queue: Queue<Triple<Int, Int, Int>> = LinkedList()\n        queue.add(Triple(src, 0, 0))\n\n        while (queue.isNotEmpty()) {\n            val (current, cost, stops) = queue.poll()\n\n            // Explore each neighbor\n            if (stops > k) continue  // Do not explore further if stops exceed k\n\n            graph[current]?.forEach { neighbor ->\n                val newCost = cost + neighbor.price\n                // Check if this path is better\n                if (newCost < minCost[neighbor.dest]) {\n                    queue.add(Triple(neighbor.dest, newCost, stops + 1))\n                    minCost[neighbor.dest] = newCost\n                }\n            }\n        }\n\n        return minCost\n    }\n\n    // Solve using Dijkstra\n    data class State(val node: Int, val cost: Int, val stops: Int)\n\n    fun dijkstraWithMaxKStops(graph: MutableMap<Int, MutableList<Node>>, src: Int, k: Int): Map<Int, Int> {\n        val pq = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(src, 0, 0))\n\n        val minCostAtStops = mutableMapOf<Int, IntArray>().apply {\n            graph.keys.forEach { this[it] = IntArray(k + 1) { Int.MAX_VALUE } }\n        }\n        minCostAtStops[src] = IntArray(k + 1) { 0 }\n\n        while (pq.isNotEmpty()) {\n            val (node, cost, stops) = pq.poll()\n\n            if (stops > k) continue\n\n            graph[node]?.forEach { neighbor ->\n                val nextCost = cost + neighbor.price\n                if (stops < k && nextCost < minCostAtStops.getOrDefault(neighbor.dest, IntArray(k + 1) { Int.MAX_VALUE })[stops + 1]) {\n                    minCostAtStops[neighbor.dest]?.set(stops + 1, nextCost)\n                    pq.offer(State(neighbor.dest, nextCost, stops + 1))\n                }\n            }\n        }\n\n        return minCostAtStops.mapValues { (_, costs) -> costs.filter { it != Int.MAX_VALUE }.minOrNull() ?: Int.MAX_VALUE }\n    }\n\n    companion object {\n        @JvmStatic\n        fun main(args: Array<String>) {\n\n        }\n    }\n}"  java="class CheapestFlightsWithinKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(var dest, var price)\n\n    // Graph map where each integer key maps to a list of Node objects\n    private var graph = mutableMapOf<Int, MutableList<Node>>()\n\n    // Solving using Bellman Ford\n    public static void $1(n, flights: Array<IntArray>, src, dst, k) {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            val from = flight[0]\n            val to = flight[1]\n            val cost = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // DP table for storing minimum cost to each node with up to i flights\n        val dp = Array(k + 2) { IntArray(n) { Int.MAX_VALUE } }\n        dp[0][src] = 0  // Cost to reach source from itself is 0\n\n        // Perform relaxation for each possible number of stops\n        for (i in 1..k + 1) {\n            dp[i][src] = 0  // No cost to stay at the source\n            graph.forEach { (from, neighbors) ->\n                neighbors.forEach { node ->\n                    if (dp[i - 1][from] != Int.MAX_VALUE) { // Ensure the source node is reachable\n                        dp[i][node.dest] = minOf(dp[i][node.dest], dp[i - 1][from] + node.price)\n                    }\n                }\n            }\n        }\n\n        // Find the minimum cost to the destination node considering all possible stops\n        val cheapest = (0..k + 1).minOfOrNull { dp[it][dst] } ?.MAX_VALUE\n        return if (cheapest == Int.MAX_VALUE) -1 else cheapest\n    }\n\n    public static void $1(n, flights: Array<IntArray>, src, dst, k) {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            val from = flight[0]\n            val to = flight[1]\n            val cost = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        val cost = bfs(src, dst, n, k)\n\n        return if (cost[dst] == Int.MAX_VALUE) -1 else cost[dst]\n    }\n\n\n    public static void $1(graph: MutableMap<Int, MutableList<Node>>, src): Map<Int, Int> {\n        val dist = mutableMapOf<Int, Int>().apply {\n            graph.keys.forEach { this[it] = Int.MAX_VALUE }\n        }\n        dist[src] = 0\n\n        // Define priority queue with a comparator provided inline\n        val priorityQueue = PriorityQueue<Node>(compareBy { it.price })\n        priorityQueue.add(Node(src, 0))\n\n        while (priorityQueue.isNotEmpty()) {\n            val current = priorityQueue.poll()\n\n            // If current node's distance in queue is greater than known shortest distance, skip it\n            if (current.price > dist[current.dest]!!) continue\n\n            graph[current.dest]?.forEach { neighbor ->\n                val newDistance = current.price + neighbor.price\n                if (newDistance < dist[neighbor.dest]!!) {\n                    dist[neighbor.dest] = newDistance\n                    priorityQueue.add(Node(neighbor.dest, newDistance))\n                }\n            }\n        }\n\n        return dist\n    }\n\n    public static void $1(src, dest, n, k ): IntArray {\n        // To track the minimum cost to reach each destination\n        val minCost = IntArray(n) { Int.MAX_VALUE }\n        minCost[src] = 0\n\n        // Using a queue for BFS; store the current node, the cost to reach it, and the number of stops made\n        val queue: Queue<Triple<Int, Int, Int>> = LinkedList()\n        queue.add(Triple(src, 0, 0))\n\n        while (queue.isNotEmpty()) {\n            val (current, cost, stops) = queue.poll()\n\n            // Explore each neighbor\n            if (stops > k) continue  // Do not explore further if stops exceed k\n\n            graph[current]?.forEach { neighbor ->\n                val newCost = cost + neighbor.price\n                // Check if this path is better\n                if (newCost < minCost[neighbor.dest]) {\n                    queue.add(Triple(neighbor.dest, newCost, stops + 1))\n                    minCost[neighbor.dest] = newCost\n                }\n            }\n        }\n\n        return minCost\n    }\n\n    // Solve using Dijkstra\n    data class State(val node, val cost, val stops)\n\n    public static void $1(graph: MutableMap<Int, MutableList<Node>>, src, k): Map<Int, Int> {\n        val pq = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(src, 0, 0))\n\n        val minCostAtStops = mutableMapOf<Int, IntArray>().apply {\n            graph.keys.forEach { this[it] = IntArray(k + 1) { Int.MAX_VALUE } }\n        }\n        minCostAtStops[src] = IntArray(k + 1) { 0 }\n\n        while (pq.isNotEmpty()) {\n            val (node, cost, stops) = pq.poll()\n\n            if (stops > k) continue\n\n            graph[node]?.forEach { neighbor ->\n                val nextCost = cost + neighbor.price\n                if (stops < k && nextCost < minCostAtStops.getOrDefault(neighbor.dest, IntArray(k + 1) { Int.MAX_VALUE })[stops + 1]) {\n                    minCostAtStops[neighbor.dest]?.set(stops + 1, nextCost)\n                    pq.offer(State(neighbor.dest, nextCost, stops + 1))\n                }\n            }\n        }\n\n        return minCostAtStops.mapValues { (_, costs) -> costs.filter { it != Int.MAX_VALUE }.minOrNull() ?.MAX_VALUE }\n    }\n\n    companion object {\n        @JvmStatic\n        public static void $1(args: Array<String>) {\n\n        }\n    }\n}"  python="class CheapestFlightsWithinKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(var dest: int, var price: int)\n\n    // Graph map where each integer key maps to a list of Node objects\n    private $1 = mutableMapOf<Int, MutableList<Node>>()\n\n    // Solving using Bellman Ford\n    def $1(n: int, flights: Array<list[int]>, src: int, dst: int, k: int): int {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            $1 = flight[0]\n            $1 = flight[1]\n            $1 = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // DP table for storing minimum cost to each node with up to i flights\n        $1 = Array(k + 2) { list[int](n) { Int.MAX_VALUE } }\n        dp[0][src] = 0  // Cost to reach source from itself is 0\n\n        // Perform relaxation for each possible number of stops\n        for (i in 1..k + 1) {\n            dp[i][src] = 0  // No cost to stay at the source\n            graph.forEach { (from, neighbors) ->\n                neighbors.forEach { node ->\n                    if (dp[i - 1][from] != Int.MAX_VALUE) { // Ensure the source node is reachable\n                        dp[i][node.dest] = min(dp[i][node.dest], dp[i - 1][from] + node.price)\n                    }\n                }\n            }\n        }\n\n        // Find the minimum cost to the destination node considering all possible stops\n        $1 = (0..k + 1).minOfOrNull { dp[it][dst] } ?: int.MAX_VALUE\n        return if (cheapest == Int.MAX_VALUE) -1 else cheapest\n    }\n\n    def $1(n: int, flights: Array<list[int]>, src: int, dst: int, k: int): int {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            $1 = flight[0]\n            $1 = flight[1]\n            $1 = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        $1 = bfs(src, dst, n, k)\n\n        return if (cost[dst] == Int.MAX_VALUE) -1 else cost[dst]\n    }\n\n\n    def $1(graph: MutableMap<Int, MutableList<Node>>, src: int): Map<Int, Int> {\n        $1 = mutableMapOf<Int, Int>().apply {\n            graph.keys.forEach { this[it] = Int.MAX_VALUE }\n        }\n        dist[src] = 0\n\n        // Define priority queue with a comparator provided inline\n        $1 = PriorityQueue<Node>(compareBy { it.price })\n        priorityQueue.add(Node(src, 0))\n\n        while (priorityQueue.isNotEmpty()) {\n            $1 = priorityQueue.poll()\n\n            // If current node's distance in queue is greater than known shortest distance, skip it\n            if (current.price > dist[current.dest]!!) continue\n\n            graph[current.dest]?.forEach { neighbor ->\n                $1 = current.price + neighbor.price\n                if (newDistance < dist[neighbor.dest]!!) {\n                    dist[neighbor.dest] = newDistance\n                    priorityQueue.add(Node(neighbor.dest, newDistance))\n                }\n            }\n        }\n\n        return dist\n    }\n\n    def $1(src: int, dest: int, n: int, k: int ): list[int] {\n        // To track the minimum cost to reach each destination\n        $1 = list[int](n) { Int.MAX_VALUE }\n        minCost[src] = 0\n\n        // Using a queue for BFS; store the current node, the cost to reach it, and the number of stops made\n        val queue: Queue<Triple<Int, Int, Int>> = LinkedList()\n        queue.add(Triple(src, 0, 0))\n\n        while (queue.isNotEmpty()) {\n            val (current, cost, stops) = queue.poll()\n\n            // Explore each neighbor\n            if (stops > k) continue  // Do not explore further if stops exceed k\n\n            graph[current]?.forEach { neighbor ->\n                $1 = cost + neighbor.price\n                // Check if this path is better\n                if (newCost < minCost[neighbor.dest]) {\n                    queue.add(Triple(neighbor.dest, newCost, stops + 1))\n                    minCost[neighbor.dest] = newCost\n                }\n            }\n        }\n\n        return minCost\n    }\n\n    // Solve using Dijkstra\n    data class State(val node: int, val cost: int, val stops: int)\n\n    def $1(graph: MutableMap<Int, MutableList<Node>>, src: int, k: int): Map<Int, Int> {\n        $1 = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(src, 0, 0))\n\n        $1 = mutableMapOf<Int, list[int]>().apply {\n            graph.keys.forEach { this[it] = list[int](k + 1) { Int.MAX_VALUE } }\n        }\n        minCostAtStops[src] = list[int](k + 1) { 0 }\n\n        while (pq.isNotEmpty()) {\n            val (node, cost, stops) = pq.poll()\n\n            if (stops > k) continue\n\n            graph[node]?.forEach { neighbor ->\n                $1 = cost + neighbor.price\n                if (stops < k && nextCost < minCostAtStops.getOrDefault(neighbor.dest, list[int](k + 1) { Int.MAX_VALUE })[stops + 1]) {\n                    minCostAtStops[neighbor.dest]?.set(stops + 1, nextCost)\n                    pq.offer(State(neighbor.dest, nextCost, stops + 1))\n                }\n            }\n        }\n\n        return minCostAtStops.mapValues { (_, costs) -> costs.filter { it != Int.MAX_VALUE }.minOrNull() ?: int.MAX_VALUE }\n    }\n\n    companion object {\n        @JvmStatic\n        def $1(args: Array<String>) {\n\n        }\n    }\n}"  rust="class CheapestFlightsWithinKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(var dest -> i32, var price -> i32)\n\n    // Graph map where each integer key maps to a list of Node objects\n    private var graph = mutableMapOf<Int, MutableList<Node>>()\n\n    // Solving using Bellman Ford\n    fn $1(n -> i32, flights: Array<Vec<i32>>, src -> i32, dst -> i32, k -> i32) -> i32 {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            val from = flight[0]\n            val to = flight[1]\n            val cost = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // DP table for storing minimum cost to each node with up to i flights\n        val dp = Array(k + 2) { Vec<i32>(n) { Int.MAX_VALUE } }\n        dp[0][src] = 0  // Cost to reach source from itself is 0\n\n        // Perform relaxation for each possible number of stops\n        for (i in 1..k + 1) {\n            dp[i][src] = 0  // No cost to stay at the source\n            graph.forEach { (from, neighbors) ->\n                neighbors.forEach { node ->\n                    if (dp[i - 1][from] != Int.MAX_VALUE) { // Ensure the source node is reachable\n                        dp[i][node.dest] = min(dp[i][node.dest], dp[i - 1][from] + node.price)\n                    }\n                }\n            }\n        }\n\n        // Find the minimum cost to the destination node considering all possible stops\n        val cheapest = (0..k + 1).minOfOrNull { dp[it][dst] } ? -> i32.MAX_VALUE\n        return if (cheapest == Int.MAX_VALUE) -1 else cheapest\n    }\n\n    fn $1(n -> i32, flights: Array<Vec<i32>>, src -> i32, dst -> i32, k -> i32) -> i32 {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            val from = flight[0]\n            val to = flight[1]\n            val cost = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        val cost = bfs(src, dst, n, k)\n\n        return if (cost[dst] == Int.MAX_VALUE) -1 else cost[dst]\n    }\n\n\n    fn $1(graph: MutableMap<Int, MutableList<Node>>, src -> i32): Map<Int, Int> {\n        val dist = mutableMapOf<Int, Int>().apply {\n            graph.keys.forEach { this[it] = Int.MAX_VALUE }\n        }\n        dist[src] = 0\n\n        // Define priority queue with a comparator provided inline\n        val priorityQueue = PriorityQueue<Node>(compareBy { it.price })\n        priorityQueue.add(Node(src, 0))\n\n        while (priorityQueue.isNotEmpty()) {\n            val current = priorityQueue.poll()\n\n            // If current node's distance in queue is greater than known shortest distance, skip it\n            if (current.price > dist[current.dest]!!) continue\n\n            graph[current.dest]?.forEach { neighbor ->\n                val newDistance = current.price + neighbor.price\n                if (newDistance < dist[neighbor.dest]!!) {\n                    dist[neighbor.dest] = newDistance\n                    priorityQueue.add(Node(neighbor.dest, newDistance))\n                }\n            }\n        }\n\n        return dist\n    }\n\n    fn $1(src -> i32, dest -> i32, n -> i32, k -> i32 ): Vec<i32> {\n        // To track the minimum cost to reach each destination\n        val minCost = Vec<i32>(n) { Int.MAX_VALUE }\n        minCost[src] = 0\n\n        // Using a queue for BFS; store the current node, the cost to reach it, and the number of stops made\n        val queue: Queue<Triple<Int, Int, Int>> = LinkedList()\n        queue.add(Triple(src, 0, 0))\n\n        while (queue.isNotEmpty()) {\n            val (current, cost, stops) = queue.poll()\n\n            // Explore each neighbor\n            if (stops > k) continue  // Do not explore further if stops exceed k\n\n            graph[current]?.forEach { neighbor ->\n                val newCost = cost + neighbor.price\n                // Check if this path is better\n                if (newCost < minCost[neighbor.dest]) {\n                    queue.add(Triple(neighbor.dest, newCost, stops + 1))\n                    minCost[neighbor.dest] = newCost\n                }\n            }\n        }\n\n        return minCost\n    }\n\n    // Solve using Dijkstra\n    data class State(val node -> i32, val cost -> i32, val stops -> i32)\n\n    fn $1(graph: MutableMap<Int, MutableList<Node>>, src -> i32, k -> i32): Map<Int, Int> {\n        val pq = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(src, 0, 0))\n\n        val minCostAtStops = mutableMapOf<Int, Vec<i32>>().apply {\n            graph.keys.forEach { this[it] = Vec<i32>(k + 1) { Int.MAX_VALUE } }\n        }\n        minCostAtStops[src] = Vec<i32>(k + 1) { 0 }\n\n        while (pq.isNotEmpty()) {\n            val (node, cost, stops) = pq.poll()\n\n            if (stops > k) continue\n\n            graph[node]?.forEach { neighbor ->\n                val nextCost = cost + neighbor.price\n                if (stops < k && nextCost < minCostAtStops.getOrDefault(neighbor.dest, Vec<i32>(k + 1) { Int.MAX_VALUE })[stops + 1]) {\n                    minCostAtStops[neighbor.dest]?.set(stops + 1, nextCost)\n                    pq.offer(State(neighbor.dest, nextCost, stops + 1))\n                }\n            }\n        }\n\n        return minCostAtStops.mapValues { (_, costs) -> costs.filter { it != Int.MAX_VALUE }.minOrNull() ? -> i32.MAX_VALUE }\n    }\n\n    companion object {\n        @JvmStatic\n        fn $1(args: Array<String>) {\n\n        }\n    }\n}"  cpp="class CheapestFlightsWithinKStops {\n    // Define the Node class to encapsulate flight details\n    data class Node(var dest, var price)\n\n    // Graph map where each integer key maps to a list of Node objects\n    private auto $1 = mutableMapOf<Int, MutableList<Node>>()\n\n    // Solving using Bellman Ford\n    $1(n, flights: Array<vector<int>>, src, dst, k) {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            auto $1 = flight[0]\n            auto $1 = flight[1]\n            auto $1 = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        // DP table for storing minimum cost to each node with up to i flights\n        auto $1 = Array(k + 2) { vector<int>(n) { Int.MAX_VALUE } }\n        dp[0][src] = 0  // Cost to reach source from itself is 0\n\n        // Perform relaxation for each possible number of stops\n        for (i in 1..k + 1) {\n            dp[i][src] = 0  // No cost to stay at the source\n            graph.forEach { (from, neighbors) ->\n                neighbors.forEach { node ->\n                    if (dp[i - 1][from] != Int.MAX_VALUE) { // Ensure the source node is reachable\n                        dp[i][node.dest] = min(dp[i][node.dest], dp[i - 1][from] + node.price)\n                    }\n                }\n            }\n        }\n\n        // Find the minimum cost to the destination node considering all possible stops\n        auto $1 = (0..k + 1).minOfOrNull { dp[it][dst] } ?.MAX_VALUE\n        return if (cheapest == Int.MAX_VALUE) -1 else cheapest\n    }\n\n    $1(n, flights: Array<vector<int>>, src, dst, k) {\n        // Build the graph from the input flights\n        flights.forEach { flight ->\n            auto $1 = flight[0]\n            auto $1 = flight[1]\n            auto $1 = flight[2]\n            // Append the destination and cost to the list of nodes for the source\n            graph.computeIfAbsent(from) { mutableListOf() }.add(Node(to, cost))\n        }\n\n        auto $1 = bfs(src, dst, n, k)\n\n        return if (cost[dst] == Int.MAX_VALUE) -1 else cost[dst]\n    }\n\n\n    $1(graph: MutableMap<Int, MutableList<Node>>, src): Map<Int, Int> {\n        auto $1 = mutableMapOf<Int, Int>().apply {\n            graph.keys.forEach { this[it] = Int.MAX_VALUE }\n        }\n        dist[src] = 0\n\n        // Define priority queue with a comparator provided inline\n        auto $1 = PriorityQueue<Node>(compareBy { it.price })\n        priorityQueue.add(Node(src, 0))\n\n        while (priorityQueue.isNotEmpty()) {\n            auto $1 = priorityQueue.poll()\n\n            // If current node's distance in queue is greater than known shortest distance, skip it\n            if (current.price > dist[current.dest]!!) continue\n\n            graph[current.dest]?.forEach { neighbor ->\n                auto $1 = current.price + neighbor.price\n                if (newDistance < dist[neighbor.dest]!!) {\n                    dist[neighbor.dest] = newDistance\n                    priorityQueue.add(Node(neighbor.dest, newDistance))\n                }\n            }\n        }\n\n        return dist\n    }\n\n    $1(src, dest, n, k ): vector<int> {\n        // To track the minimum cost to reach each destination\n        auto $1 = vector<int>(n) { Int.MAX_VALUE }\n        minCost[src] = 0\n\n        // Using a queue for BFS; store the current node, the cost to reach it, and the number of stops made\n        val queue: Queue<Triple<Int, Int, Int>> = LinkedList()\n        queue.add(Triple(src, 0, 0))\n\n        while (queue.isNotEmpty()) {\n            val (current, cost, stops) = queue.poll()\n\n            // Explore each neighbor\n            if (stops > k) continue  // Do not explore further if stops exceed k\n\n            graph[current]?.forEach { neighbor ->\n                auto $1 = cost + neighbor.price\n                // Check if this path is better\n                if (newCost < minCost[neighbor.dest]) {\n                    queue.add(Triple(neighbor.dest, newCost, stops + 1))\n                    minCost[neighbor.dest] = newCost\n                }\n            }\n        }\n\n        return minCost\n    }\n\n    // Solve using Dijkstra\n    data class State(val node, val cost, val stops)\n\n    $1(graph: MutableMap<Int, MutableList<Node>>, src, k): Map<Int, Int> {\n        auto $1 = PriorityQueue<State>(compareBy { it.cost })\n        pq.offer(State(src, 0, 0))\n\n        auto $1 = mutableMapOf<Int, vector<int>>().apply {\n            graph.keys.forEach { this[it] = vector<int>(k + 1) { Int.MAX_VALUE } }\n        }\n        minCostAtStops[src] = vector<int>(k + 1) { 0 }\n\n        while (pq.isNotEmpty()) {\n            val (node, cost, stops) = pq.poll()\n\n            if (stops > k) continue\n\n            graph[node]?.forEach { neighbor ->\n                auto $1 = cost + neighbor.price\n                if (stops < k && nextCost < minCostAtStops.getOrDefault(neighbor.dest, vector<int>(k + 1) { Int.MAX_VALUE })[stops + 1]) {\n                    minCostAtStops[neighbor.dest]?.set(stops + 1, nextCost)\n                    pq.offer(State(neighbor.dest, nextCost, stops + 1))\n                }\n            }\n        }\n\n        return minCostAtStops.mapValues { (_, costs) -> costs.filter { it != Int.MAX_VALUE }.minOrNull() ?.MAX_VALUE }\n    }\n\n    companion object {\n        @JvmStatic\n        $1(args: Array<String>) {\n\n        }\n    }\n}" %}

---

## BinarySearchTreeToGreaterSumTree

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/bst/BinarySearchTreeToGreaterSumTree.kt`

{% include code-tabs.html  kotlin="package graph.bst\n\nclass TreeNode(var `val`: Int) {\n    var left: TreeNode? = null\n    var right: TreeNode? = null\n}\n\nclass BinarySearchTreeToGreaterSumTree {\n\n    fun bstToGst(root: TreeNode?): TreeNode? {\n        var sum = 0\n\n        // Helper function to perform reverse in-order traversal\n        fun dfs (node: TreeNode?) {\n            if (node == null)\n                return\n\n            dfs(node.right)\n            sum += node.`val`\n            node.`val` = sum\n            dfs(node.left)\n\n        }\n\n        dfs(root)\n        return root\n    }\n\n}"  java="class TreeNode(var `val`) {\n    var left: TreeNode? = null\n    var right: TreeNode? = null\n}\n\nclass BinarySearchTreeToGreaterSumTree {\n\n    public static void $1(root: TreeNode?): TreeNode? {\n        var sum = 0\n\n        // Helper function to perform reverse in-order traversal\n        fun dfs (node: TreeNode?) {\n            if (node == null)\n                return\n\n            dfs(node.right)\n            sum += node.`val`\n            node.`val` = sum\n            dfs(node.left)\n\n        }\n\n        dfs(root)\n        return root\n    }\n\n}"  python="class TreeNode(var `val`: int) {\n    var left: TreeNode? = None\n    var right: TreeNode? = None\n}\n\nclass BinarySearchTreeToGreaterSumTree {\n\n    def $1(root: TreeNode?): TreeNode? {\n        $1 = 0\n\n        // Helper function to perform reverse in-order traversal\n        fun dfs (node: TreeNode?) {\n            if (node == None)\n                return\n\n            dfs(node.right)\n            sum += node.`val`\n            node.`val` = sum\n            dfs(node.left)\n\n        }\n\n        dfs(root)\n        return root\n    }\n\n}"  rust="class TreeNode(var `val` -> i32) {\n    var left: TreeNode? = None\n    var right: TreeNode? = None\n}\n\nclass BinarySearchTreeToGreaterSumTree {\n\n    fn $1(root: TreeNode?): TreeNode? {\n        var sum = 0\n\n        // Helper function to perform reverse in-order traversal\n        fun dfs (node: TreeNode?) {\n            if (node == None)\n                return\n\n            dfs(node.right)\n            sum += node.`val`\n            node.`val` = sum\n            dfs(node.left)\n\n        }\n\n        dfs(root)\n        return root\n    }\n\n}"  cpp="class TreeNode(var `val`) {\n    var left: TreeNode? = nullptr\n    var right: TreeNode? = nullptr\n}\n\nclass BinarySearchTreeToGreaterSumTree {\n\n    $1(root: TreeNode?): TreeNode? {\n        auto $1 = 0\n\n        // Helper function to perform reverse in-order traversal\n        fun dfs (node: TreeNode?) {\n            if (node == nullptr)\n                return\n\n            dfs(node.right)\n            sum += node.`val`\n            node.`val` = sum\n            dfs(node.left)\n\n        }\n\n        dfs(root)\n        return root\n    }\n\n}" %}

---

## RangeSumOfBST

Source: `/Users/arpanpathak/Projects/data_science/CrackGoogle/bin/main/graph/bst/RangeSumOfBST.kt`

{% include code-tabs.html  kotlin="package graph.bst\n\nclass RangeSumOfBST {\n    fun rangeSumBST(root: TreeNode?, low: Int, high: Int): Int {\n        var sum = 0\n        fun dfs(node: TreeNode?) {\n            if (node == null)\n                return\n\n            dfs(node.left)\n\n            if (node.`val` in low..high)\n                sum += node.`val`\n\n            dfs(node.right)\n        }\n        dfs(root)\n\n        return sum\n    }\n}"  java="class RangeSumOfBST {\n    public static void $1(root: TreeNode?, low, high) {\n        var sum = 0\n        public static void $1(node: TreeNode?) {\n            if (node == null)\n                return\n\n            dfs(node.left)\n\n            if (node.`val` in low..high)\n                sum += node.`val`\n\n            dfs(node.right)\n        }\n        dfs(root)\n\n        return sum\n    }\n}"  python="class RangeSumOfBST {\n    def $1(root: TreeNode?, low: int, high: int): int {\n        $1 = 0\n        def $1(node: TreeNode?) {\n            if (node == None)\n                return\n\n            dfs(node.left)\n\n            if (node.`val` in low..high)\n                sum += node.`val`\n\n            dfs(node.right)\n        }\n        dfs(root)\n\n        return sum\n    }\n}"  rust="class RangeSumOfBST {\n    fn $1(root: TreeNode?, low -> i32, high -> i32) -> i32 {\n        var sum = 0\n        fn $1(node: TreeNode?) {\n            if (node == None)\n                return\n\n            dfs(node.left)\n\n            if (node.`val` in low..high)\n                sum += node.`val`\n\n            dfs(node.right)\n        }\n        dfs(root)\n\n        return sum\n    }\n}"  cpp="class RangeSumOfBST {\n    $1(root: TreeNode?, low, high) {\n        auto $1 = 0\n        $1(node: TreeNode?) {\n            if (node == nullptr)\n                return\n\n            dfs(node.left)\n\n            if (node.`val` in low..high)\n                sum += node.`val`\n\n            dfs(node.right)\n        }\n        dfs(root)\n\n        return sum\n    }\n}" %}
