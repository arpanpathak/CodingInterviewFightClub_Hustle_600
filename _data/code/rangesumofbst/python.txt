class RangeSumOfBST {
    def $1(root: TreeNode?, low: int, high: int): int {
        $1 = 0
        def $1(node: TreeNode?) {
            if (node == None)
                return

            dfs(node.left)

            if (node.`val` in low..high)
                sum += node.`val`

            dfs(node.right)
        }
        dfs(root)

        return sum
    }
}