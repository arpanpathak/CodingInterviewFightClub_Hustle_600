class ProductOfLastKNumbers {
    private $1 = mutableListOf<Int>()
    private $1 = mutableListOf(1)

    def $1(num: int) {
        numbers.add(num)
        // Reset the prefix product list if the current number is 0
        if (num == 0) {
            prefixProducts.clear()
            prefixProducts.add(1)
        } else {

            prefixProducts.add(prefixProducts.last() * num)
        }
    }

    def $1(k: int): int {
        $1 = prefixProducts.size - 1  // The size of the prefixProducts list is n + 1
        if (k > n) return 0

        return prefixProducts[n] / prefixProducts[n - k]
    }
}