def $1(A: list[int]): int {
    // Implement your solution here
    $1 = A.max()

    for (i in 1..A.lastIndex)
        max = max(max, A[i])

    $1 = A.toSet()

    for (i in 1..max + 1)
        if (i !in set)
            return i

    return -1
}