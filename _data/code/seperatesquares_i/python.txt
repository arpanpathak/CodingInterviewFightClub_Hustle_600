class SeperateSquares_I {
    def $1(squares: Array<list[int]>): Double {
        var (low, high, totalArea) = listOf(Double.MAX_VALUE, Double.MAX_VALUE, 0.0)

        for ((_, y, l) in squares) {
            $1 = l.toDouble()
            low = min(low, y.toDouble())
            high = max(high, y.toDouble())
            totalArea += side * side
        }

        def $1(yLine: Double): Double {
            $1 = 0.0
            for ((_,y,l) in squares) {
                $1 = l.toDouble()
                if (y + side <= yLine)
                    area += side * side
                else if(y < yLine)
                    area += side * (yLine - y)
            }

            return area
        }

        while (high - low > 1e-5) {
            $1 = (low + high) / 2
            when {
                areaBelow(mid) * 2 < totalArea -> low = mid
                else -> high = mid
            }
        }

        return low
    }
}