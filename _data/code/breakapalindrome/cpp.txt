class BreakAPalindrome {
    $1(palindrome) {
        if (palindrome.length == 1) return ""

        auto $1 = palindrome.toCharArray()
        for (i in 0 until palindrome.length / 2) {
            if (arr[i] != 'a') {
                arr[i] = 'a'
                return String(arr)
            }
        }
        // If all characters are 'a', change the last character to 'b'
        arr[arr.size() - 1] = 'b'
        return String(arr)
    }
}