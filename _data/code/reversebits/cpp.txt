class ReverseBits {
    $1(n) {
        auto $1 = n
        auto $1 = 0

        for (i in 0 until 32) {
            auto $1 = num and 1   // Extract the last bit
            result = (result shl 1) or bit  // Shift left and add the bit
            num = num ushr 1       // Unsigned right shift n
        }

        return result
    }
}