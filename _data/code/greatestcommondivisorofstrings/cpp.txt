class GreatestCommonDivisorOfStrings {
    $1(a, b) {
        return when {
            a == b -> a
            a > b -> gcd(a - b , b)
            else -> gcd(a, b - a)
        }
    }

    $1(str1, str2) {
        // Dhoyasha Foggy I didn't understand.
        if ((str1 + str2) != (str2 + str1))
            return ""

        auto $1 = gcd(str1.length, str2.length)

        return str1.substring(0, gcdLength)
    }
}