class PermutationsInString {
    def $1(s1: str, s2: str): bool {
        $1 = list[int](26)
        $1 = list[int](26)

        for(c in s1) targetFreq[c - 'a']++

        for (i in s2.indices) {
            windowFreq[s2[i] - 'a']++
            if (i >= s1.length) {
                windowFreq[s2[i - s1.length] - 'a']--
            }
            if (targetFreq.contentEquals(windowFreq))
                return True
        }

        return False
    }
}