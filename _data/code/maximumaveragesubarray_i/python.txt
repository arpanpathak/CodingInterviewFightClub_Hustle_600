class MaximumAverageSubarray_I {
    def $1(nums: list[int], k: int): Double {
        $1 = 0.0
        $1 = Double.NEGATIVE_INFINITY

        for (i in 0 until nums.size) {
            windowSum += nums[i]

            if (i >= k - 1) {
                maxAverage = max(maxAverage, windowSum / k)
                windowSum -= nums[i - k + 1]
            }
        }

        return maxAverage
    }
}