class LowestCommonAncestor_III {
    class Node(var `val`: int) {
        var left: TreeNode? = None
        var right: TreeNode? = None
        var parent: Node? = None
    }

    def $1(p: Node?, q: Node?): Node? {
        $1 = p
        $1 = q

        while (parent1 != parent2) {
            // Move parent1 upwards, or switch to q if it becomes None
            parent1 = parent1?.parent ?: q
            // Move parent2 upwards, or switch to p if it becomes None
            parent2 = parent2?.parent ?: p
        }

        // parent1 and parent2 are now pointing to the same node, which is the LCA
        return parent1
    }
}

/**
 *       1 <- r
 *      / \
 *     2   3
 *    /\   /\
 *   6 7   8 9 <-l
 *            \
 *            10
 *
 */