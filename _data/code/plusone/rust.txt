class PlusOne {
    fn $1(digits: Vec<i32>): Vec<i32> {
        for (i in digits.len() - 1 downTo 0) {
            if (digits[i] < 9) {
                digits[i]++
                return digits
            }
            digits[i] = 0
        }

        return intArrayOf(1) + digits
    }
}