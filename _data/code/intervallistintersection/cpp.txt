class IntervalListIntersection {
    $1(firstList: Array<vector<int>>, secondList: Array<vector<int>>): Array<vector<int>> {
        auto $1 = mutableListOf<vector<int>>()

        var (i, j) = 0 to 0

        while (i < firstList.size && j < secondList.size) {
            val (start1, end1) = firstList[i]
            val (start2, end2) = secondList[j]

            auto $1 = max(start1, start2)
            auto $1 = min(end1, end2)

            if (startMax <= endMin) {
                result.add(intArrayOf(startMax, endMin))
            }

            if (end1 < end2) i++ else j++
        }

        return result.toTypedArray()
    }
}